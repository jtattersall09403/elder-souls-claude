2026-08-06-one-field-short — W1-00-r2 FAIL 4.1/10: the save drops enemies[].prev_state (219 of 600 frames diverge after a reload), a pre-existing defect round one recorded as passing; plus allocation under-reported 30-117x by the profiler's default mode.
- 2026-08-06-fight-with-no-danger — W1-09 combat verdict FAIL 4/10: the RI-CMB07 exemplar fight has zero damage taken, 11 of 24 rows out of band, and silently validated RI-CMB03 M7, RI-CMB05 §B, RI-CMB09 §3 and RI-AI03; compare of our combat framing against SB-TEL__ds3-3625638110.
- 2026-08-06-thirteen-regions-on-paper — W1-01 RI-WLD04: 8 of 9 M18 axes read strings out of regions.json (audio 78/78 with audioMB 0), and the M17 blind pack shipped unshuffled so 39/39 came off the file index; compare of Blackwood vs Eastern Rootlands frames
- 2026-08-06-drowned-road — W1-01 (3.1 FAIL): 502 m of THE CROSSING under 65.6 m of water; the road's 85 m cutting dug the hole it drowned in, not an A* routing miss; cut/fill limits + deck clearance now land 0 offending metres at all four tide phases
- 2026-08-06-four-critics-one-failure — feature: W1-07 (1.5), W1-10 (0), W1-14 (2.7) and W1-15 (2.0) independently found the same failure — a correct, instrumented model nothing in the running world reads: 20 byte-identical census PNGs (sha 7277a273), setLoadout rejecting all 87 movesets, 5 of 55 magic effects working, enemy detection coupling 0.00; and RI-MTH07 / ARBITRATION §3 CONSUMPTION, the three orphan shapes
- 2026-08-07-eighty-seven-weapons-one-stick — W1-10-r3 FAIL 3/10: game/src/render/ draws no weapon at all (one 0.95 m box welded to the hip for all 87), three weapons' frame-0 screenshots byte-identical md5 9bdb823f, 0.19-0.21% peak pixel change across a full attack; and it is why a halberd swept 0.67-1.33 m below the floor on 100% of its active frames for three rounds, usable reach 1.99 m against 2.86 declared
- 2026-08-07-a-main-quest-played-to-the-end — W1-19: "The Rootward Tide", 32 quests / 5 acts / 6 endings, intended chain 29/29 and backpath 17/17 from setGold(0) with no faction rank, 0 violent resolutions, 121 world flags differing between endings, three sabotage controls stopping at three different gates; all 253 journal entries share one date; and every NPC's `disposition` field had no consumer, so 36 of 69 quests were unofferable from a cold start until Engine.seedDispositions()
- 2026-08-07-an-argonian-with-nothing-to-say — W1-07-r3: with the race-gated topic consumer newly wired, "race changes the topic list" passed 4/4 while a Saxhleel got zero topics from 3 of 4 Helstrom NPCs (30 of 130 mute pairs); causes were 13 topic ids never written, 22 with no info for the NPC's actor, and 8 topics whose race variants were shadowed by an ungated info (incl. the-hist and slavery); closed by 45-speaker-coverage.json (34 topics / 56 infos) plus two delete-the-fix guards
- 2026-08-07-three-frames-in-twelve-minutes — orchestration: 14 agents on 4 cores took load to 44-103, a 1280x720 capture from 25 s to 150-260 s, and W1-01-r4's 117-frame region pack managed 3 frames in 12 minutes before being abandoned, leaving the round's headline number unmeasured and the day figures coming from reports/region-shots-prebush/; AGENT-PROTOCOL now caps browser work at 6-7 agents
- 2026-08-07-a-sword-in-the-ground — the renderer landed (13a40b4): three weapons at f8 now differ by 60,794-65,434 px in the character region against 0 pre-fix, a 60-frame attack moves 43.76% of the character box (max single frame) / 54.96% union against 0.19%, drawn tip vs hit socket agree to 0.0008 mm; and cgs_drowned_reaper's tip is above ground on 0 of 40 samples (-1.917 to -0.815 m, inclination -36.7 worsening to -66.3), which the renderer builder refused to correct in the renderer because _bladeLength solves 3.098 m of blade so the tip radius equals declared reach_m
- 2026-08-07-nobody-in-the-photographs — renderer.js read the player's visibility as the negation of a camera override, so every hand-placed-camera capture drew no player (prefix probe: actors_drawn 0, player_visible false, empty field); compare of cgs f8 prefix vs f8 baseline. Records honestly that the W1-10-r3 byte-identical weapon frames were follow-camera and DO contain the character, contra NEXT-DISPATCH §1a
- 2026-08-07-the-save-that-empties-your-purse — W1-SAVE-r1 FAIL 2/10: combat.world.gold has no writer in the save, 600 -> 0 on helstrom-market, rootlands-well-graph and stormhold-street (3 of 38 shipped states, the only three holding a purse), survives writeSave + page reload; parley GOLD costs 180 on inf_trash so the non-lethal exit and travel fares die with it; 76/76 was a fixed-point test on the serialiser and no-gold is blind to all four shipped instruments; plus a save taken airborne lands hp 0 against hp 620 on the control
- 2026-08-07-ten-starts-and-one-bad-moment — W1-SAVE-r1 method: widening the repair's 38-state x 2-seed / 76-clean sweep to ten seeds found nothing; widening the *moment* to 9 scenarios x 10 seeds = 90 trials gave 69 pass / 20 defect / 1 unmeasurable, with mid-death and mid-crime failing 10/10 and the other seven holding 10/10; plus three province scenarios reported unmeasurable because the reproducibility control gave a live player in one repeat and a corpse in the other
- 2026-08-07-the-quest-that-shuts-itself — W1-19-r1 (FAIL 4/10): Q-MAIN-01/res_carry banks the_drowned_court +4, which enrols the player at rank 0, and house-dres->drowned-court -4 x3 = -12 takes saxhleel/interior from 15 (= the gate exactly) to 3 at salt-factor-neloth-vaun, refusing Q-MAIN-23 twenty-two quests later; the eighth of eight Act IV voices, and Q-MAIN-24 requires all eight. W1-07 r4's clamp set nine main-spine gates to the worst cold-start signature exactly (Q-MAIN-23 40 -> 15), so all nine have worst_margin 0; the offer-gate probe that certified it only ever posed cold-start characters. Also: q.factions[f].member is written false and read nowhere
- 2026-08-07-eleven-of-forty — W1-19-r1 race sweep (mainline-race-trace.mjs, written for the verdict): 29/40 signatures complete both chains; 4 saxhleel + 4 naga blocked at Q-MAIN-23 on standing, dunmer/lukiul, foreign-born and blackrose blocked at Q-MAIN-10 (rootkeeper-ashul-tei 0/1) at 9/29 while dunmer/interior walks through, so upbringing alone discriminates; frames it as a consequence of the W1-07 r4 gate work landing ~40 min before the review, against that fix's own note ("The main quest is a writ, not a favour")
- 2026-08-07-nobody-can-find-the-main-quest — W1-19-r1 GAP-W1-mainquest-topic-bootstrap: 0/32 main quests offerable cold, 32/32 refused on opens_by.topic, 0 topics known; 0/32 opening topics exist as a dialogue topic id raw (3 folded), 23/32 supplied only by the quest they open, 5 by nothing; 0 rumour lines naming a main-quest noun and 0 code consumers of opens_by.overheard_from; --sabotage drop-topic is the measurement not a control; and 32 authored q.directions strings, validated for length, never drawn (journal.js write() copies def.text only). Cites S30, "there is nowhere to put a pin"
- 2026-08-07-walking-off-the-edge — CAPTURE-SERVICE-R1 §4 escalation, and its fix: renderer.province was pumped only by _applyCell, teleport, walkRoute behind an opt-in flag and the harness verb, and Province.update() had no caller, so the drawn province was anchored to the last teleport; BEFORE-walk3km lost ground underfoot past ~800 m, 20 of 30 samples with nothing beneath. Streamer wired into _afterStep at bd3630e; AFTER-crossing walks the full 6,615 m Stormhold-Helstrom-Blackrose-Lilmoth in 55 min with 0 of 34 samples missing ground. Compare of valus-ridge (~460 m in) vs western-rootlands (~3.9 km in)
- 2026-08-07-breaking-into-our-own-photographer — CAPTURE-SERVICE-R1 gates + the R2 repair: the arrival classifier went 28/30 laundering specs ACCEPTED -> 0/30 (word-list boundary misses, plurals, evidence_of defaulting to permitted); the cache replayed a hand-written sidecar in 24 ms stamped arrival:"walked" with a fabricated sha256, now re-derived + hash-checked and falling through to a real 68 s render; P4 same-spec-same-picture byte-identical (time/weather used to be inherited from the previous job); and the builder's own honesty about P6 going inert (F13) and the delete-the-fix reproduction at 25 ms
- 2026-08-07-souls-you-cannot-spend — W1-13-r1 (FAIL 4/10): engine.js:2011 calls HearthSystem.atHearth(), which HearthSystem does not define, so levelup is refused at all 29 wells (3/3 measured, control setAtHearth(true) opens); souls have no sink anywhere in the build; walked loop 217 m out / 46.1 m back returns 4,200 with an 80 m stain relocation; plus the mid-death save destroying 4,200 (0/4200 on 2 of 2 routes, control clean) and the caveat that nothing shipped calls writeSave()
- 2026-08-07-the-word-eleven — W1-LIBRARY-r1 §1-2: "eleven" in 49 of our 65 books vs 7 of Morrowind's 241 (84x per-word), one-token sort 92.5% against a 78.8% majority baseline, the same tic across a surveyor's claim / leave book / poler's manual / counting rhyme / toll receipt / romance / gossip sheet / clerk's handbook; the critic voided its own blind pair (it built the packs) and stood on a pre-registered hashed arithmetic rule instead, 4/4; and RI-LOR03 §1's recalled Morrowind lengths measured 45% low at p10
- 2026-08-07-a-library-with-no-reader — W1-LIBRARY-r1 §4 (FAIL 3/10): 65 books / 37,819 words / 324 pages all open, and both consumers are dead code — topics_taught 116 declared, 0 delivered after opening all 60; the three knowledge-gated non-violent resolutions byte-identical read vs unread; 28 books contradict across 24 pairs, 23 genuine, the a-progress-ii/pilots-chart-book joke living only in the metadata; and both checking tools passed 28/28 on a corpus with every book's text deranged
- 2026-08-07-ask-what-the-town-is-saying — W1-19 round 2 gen 3, the non-mainline half of the topic-supply finding: 34 quest keywords with no body anywhere in dialogue/topics/** (55-utility.json, 34 topic bodies), 32 rumour rows keyed to the giver's settlement, topic-supply-audit A 34->0 / C 35->0 / D 27->0; utility-findability with nothing hand-fed opens 10 of 35 (helstrom 4, stormhold 6, soulrest 0) from 0 topics known cold, and --sabotage no-rumours puts it back to 0/35; Soulrest learns "the tally of the dead" and is still refused on Drowned Court rank
- 2026-08-07-a-map-after-all — ARBITRATION S30 (no map, "nowhere to put a pin", W1-21 builder refused the lead's brief and was upheld) overruled by the owner as S35 after reading S30 in a blog post: Morrowind has a map and no quest markers in the same game. S30 kept in full as the specification of what the map may not contain; roads deliberately undrawn as lines. Plus the empty-map defect: Discovery constructed before data.mapUI/data.pois loaded, so radius 0 and no sites — 255 cells vs 4,571 and 0 places ever — while map-probe passed 30/30 against a copy it had rebuilt itself
- 2026-08-07-a-demand-nobody-could-meet — quest resolutions authored on Morrowind's 0-100 attribute scale against a 3-31 sheet: personality ceiling 31 max / 22 median / 18 p10 over 240 signatures (2 governing skills x +6). Six endings unreachable by every character; three mainline re-scaled with slack (40->17, 35->20, 45->20) rather than clamped to the ceiling; three still open incl. Q-MAG-10 strength 45 (ceiling 37). Corrects the "made the game more violent" reading — no unreachable ending or fallback is violent
- 2026-08-07-checked-on-the-way-in — W1-FACTIONS-r1 (4.1 FAIL) GAP-FCT-01: resolve() gates only on record existence and reveal() mints the record, so a non-member refused by name completed Q-LEDG-07 (rank 6 / rep 90) and took +40 Imperial Assize, +25 dockhands, -40 House Dres, unlocking Q-LEDG-08; not player-reachable (0 of 110 hooks rows pair quest+reveal) but live in faction-probe, so every recorded walk was gate-off. Plus the arbitration: reputation 112 IS reachable (unmet drops out at exactly 112, null control 60->61 flat), and build-viability.mjs:1191 pins reputation at 100 while granting gold 1e9 — still reporting 0 of 540 today
- 2026-08-07-four-endings-owed — FOLLOW-UP closing four threads: (1) the blade underground, W1-10-r4-blade — cgs_drowned_reaper 0/40 -> 32/40 samples above ground (tip -1.33 m -> +0.905 m at f16), all-weapon active frames 546/974 -> 894/974, weapons_never_above_ground 13 -> 0, halberd reach 2 m -> 3.25 m; residue = socket steps >1 m 267 -> 307 of 2689 clips (~15% worse, self-reported) and tip-speed unmoved 46.15% -> 45.15%. (2) souls, W1-13-R2 — levelup opens at 3/3 walked wells, 4200 -> 3782 = cost(2) 418, refused at 48.5 m, mid-death save survivable; residue = no enemy carried a `souls` value until W1-SOULS this afternoon (state verifying). (3) library, W1-LIBRARY r2 — topics 0 -> 122, 3/3 talk-out gates open, 32 skill books +1, delete-the-fix returns 0 and 3/3 refusals; residue = 11 endings ask for `scribing` / `root_speech`, skills that do not exist. (4) main quest, W1-19 r2 — 29/40 -> 40/40 signatures, no purse, no violence; residue = 9 of 94 quest givers talkable and 8 of 31 main gates now disposition_min 0
- 2026-08-07-a-menu-is-a-photograph — W1-MAP finding 2: UISystem.build()'s cache key was frame+mode+touch, and the frame is frozen while any menu pauses the world, so no button press on a paused screen ever redrew — map confirm moved focus world->local with the drawn element still view=world at rect 552x634, journal page 0->1 byte-identical; missed because uiFocus/openMenu/closeMenu force a rebuild and every probe used them, caught only by driving real input; A/B against the restored pre-fix code. Same root cause behind tools/capture/server.mjs leaving menus open between jobs (blank map shots, every agent affected) and jrn06-death's twenty deaths that killed nobody while the log printed OK nineteen times. Open: the first pooled capture's emptiness never isolated
- 2026-08-07-eleven-buttons-nobody-could-see — W1-08/W1-29 r2: handheld and desktop framebuffers were byte-identical (a4932a97f50021ee) with touchLayout() reporting 11 controls in both; overlay now drawn, ablated not asserted (desktop 73c633ee15bc82aa / handheld 90318e3504ddda6c, delete-the-fix returns the desktop hash exactly), 11 marks and no text on any of them per M-K20. The builder's own two wrong drafts: hiding the ring on the map took the drawer with it and trapped a touch-only player inside the map, then filtering the model left the controls invisible but still hit-testable; shipped version drives drawing and hit test off one list (drawn == hit-testable in all 6 rows). Tool defects: M-K8 reloaded without waiting for boot so results flapped and a crash discarded 20 rows unwritten; M-K22's lexicon fired on "Ocheeva Salt-Hand" / "Waits-For-Salt the Shorter". Still red: M-P20, smallest run on the dialogue surface 7 CSS px against a floor of 18 on an 844x390 phone, left red rather than softened
- 2026-08-07-nothing-had-ever-given-you-a-soul — W1-SOULS: soulsHeld had NO producer anywhere in game/src, so the 139-row curve, the level-up screen at 29 sapwells and the whole bloodstain loop were correct and unfundable; values now derived from each statblock (inf_trash 136, champion 2,423, 15 props 0, K anchored to RI-PRG06's 136.18 R1 mean kill), 6-man ambush pays 816 = level 2, vigour buy takes hp_max 196->222 so a 210-pt blow leaves 12 hp; compare of 4,200 test-written souls vs 816 earned. RETURNED 5/10 (W1-SOULS-r1 HF-1): the anchor uses the superseded 576-enemy census against constants.json world.enemy_census 1230, so every value is ~2.14x too high (inf_trash should be 64) and a 100% clear lands on level 120 exactly, which RI-PRG06 forbids; plus hazard kills pay nothing and resurrect, and all 12 builder arms killed by harness verb. Closes the souls residue named in 2026-08-07-four-endings-owed; and W1-POPULATION landed 144 posts / 267 bodies / 34,646 souls = level 24, so "the world is empty" is already stale
- 2026-08-07-the-file-that-says-what-is-true — W1-23: corpus/60-lore/data/canon-facts.json had 54 facts, 6 disputes and two readers, both critic scripts; RI-LOR06 method §4 (disputes with both sides voiced by a shipped book or NPC) had never been run and read 0 of 6 against a bar of 8 the six-dispute registry could not reach — now 21 of 21, 71 facts, 50/50 contradiction edges registered. game/data/lore/canon.json ships with authorially_true replaced by truth_seal (0 rulings shipped, 20 of 21 sealed, CF-D002 deliberately open); converse.js infoFor() offers a side only to a holder, perturbation 11/11 lines vanish, control 409/409 untagged answers byte-identical. Residue: canon-census.mjs is not wired as a gate and CF-D021 went pass->fail->pass inside one session
- 2026-08-07-what-the-province-is-missing — W1-23 §5-6: ~20 distinct years in all of game/data, none outside 3E, spanning 3E 344 to the present 3E 427, against "four hundred" 121x and "four thousand" 18x as vague spans; 12 facts now carry `when` (4 honestly null, the Duskfall undated on purpose) and 5 of 21 disputes sit before it. Zero Argonian tribes (Agacephs/Paatru/Archein/Sarpa/Dead-Water/Miredancers all 0 — the only hits are inside canon.json's own record of the absence), Kothringi/Lilmothiit present but extinct, province organised by five invented bodies instead; registered as CF-C023/CF-C024. Slave trade appears chiefly as "lease", and CF-D008 now makes the euphemism the Provincial Office's POSITION rather than the world's voice, ruling sealed out of the build. CORRECTS the W1-23 report's own 50:1 ratio — it counted the city of Blackrose; word-bounded over books+dialogue it is ~10:1 (lease 89, slave/slavery 9)
- 2026-08-07-nobody-was-home — FOLLOW-UP closing the giver residue named in 2026-08-07-four-endings-owed: W1-GIVER-PRESENCE found that 9 of the 94 named quest-givers existed anywhere a player could stand; every giver now has a post + schedule and accepting a quest checks presence, taking talkable 9->120 of 120 (like-for-like on the original four states, 9->52), delete-the-fix returns exactly 9/0/0; VP04-settlement-street repointed from empty ground to Thorn's own high street. Residue: the "10 quests findable" headline is honestly 7, and 8 of 31 main-quest gates are still disposition_min 0
- 2026-08-07-the-noise-was-at-your-own-feet — W1-11: combat had no sound at all until this round (12 classes / 48 variants built and wired to the hit-resolution moment); the first version positioned every landed hit's sound at the attacker instead of the body the weapon met, invisible to a front-on test (28 impacts, most of any fixture, unable to distinguish correct from backwards) and only exposed by a circling target — fixed rule correlates pan-to-bearing at 0.9999, the old rule at -0.5207 (anti-correlated). Weapon tip speed (whip 163.3 m/s vs a 26 m/s band) left untouched
- 2026-08-07-twenty-one-minutes-and-nothing-happens — W1-22-B2 blind judge: 32/32 SAME/DIFFERENT across 64 region-ambience recordings (21m20s), but the judge itself flagged the perfect score as a reason to distrust the pack — same-place pairs differ 0.14-0.95dB, different-place pairs 4.39-35.76dB, an empty gap any threshold clears. Real finding: transient_onsets is empty for all 64 recordings (crest factor never exceeds 17.4dB), so regions are told apart only by tone colour and stereo width, nothing ever happens within one
- 2026-08-07-two-towns-and-the-second-found-everything — W1-SPEAKERS: root-topic coverage 133/336 -> 336/336 via a generic-floor + cell tier; browser probe in Gideon passed 8/10 (both failures the check's own design/API mistakes), rerun in Stormhold caught 4 more probe-only defects Gideon missed (an over-broad relocation that renamed an NPC's id, a correctly-unreachable indoor speaker, a visibility flag read before the world had stepped, and a repeat of the first design mistake). Mid-run, 11 brand-new NPCs (Agaceph/Paatru/Sarpa/Archein/Miredancer/Dead-Water) landed and answered all nine roots immediately from the floor, with zero bespoke lines — vindicating the floor-not-table shape. Residue: those 11 have no voice of their own on the self-roots, and 40 dialogue lines (23 for "magister") address actors no NPC record wears
- 2026-08-07-the-order-the-numbers-arrived-in — W1-22-r3: two background-sound recordings (street, well) would not reproduce themselves byte-for-byte; root cause proved in bare browser audio nodes with none of the project's code present — 1 and 2 summed inputs identical every run, 3+ diverge by one float32 ULP because addition is not associative; fixed by chaining sums two at a time instead of flat N-input mixing; all 20 beds x 2 tod x 3 captures now bit-identical, delete-the-fix reproduces divergence on 8/20 beds
- 2026-08-07-the-door-that-opened-onto-nothing — W1-04-r1 verdict (3.2/10) + W1-04-r2 fix: 115/115 interiors entered, 0/115 switched the drawn cell (sim.env.interior right, renderer.setCell never called from useDoor/leaveInterior); 113/115 interiors resolved to one generic 249-triangle hall regardless of address; both fixed — door switches on the press frame, rooms built from each interior's own file with town-specific materials; people now walk between rooms and 94/23 stand outdoors at noon/3am (was 0 at any hour). Residue: building exteriors are still bare doors with nothing drawn, and the repair is unreviewed by a fresh critic
- 2026-08-07-the-camera-thought-everyone-was-the-same-height — W1-06/W1-06-r2: camera targetHeight() read t.archetype_id, a field spawn() never sets, so every locked enemy (0.6m rat to 8m boss) framed as the 1.9m default; found and fixed same round — lock_height_m now reads 0.6/1.9/2.6/4.5/8 instead of a flat 1.9, delete-the-fix reproduces the flat reading. Residue: renderer.js mesh.scale.y is still hardcoded to 1, so the correctly-framed boss is still drawn at ordinary size
- 2026-08-07-the-list-of-who-tells-you — W1-18-r2: deceit.revealed_by's 186 authored rows (120 quests) naming who/what tells you a quest's demanded knowledge had zero readers in game/src; built the reader for the two channels whose sources are people (talk_to_target, rival_npc), demanded reveals with a route in play 8/121 -> 48/121, note() journal coverage 11 quests -> 48; delete-the-fix (router removed, call site removed) both return to 0/37 and false. Residue: ledger/letter/environment channels (58 of the remainder) name objects that don't exist in the build, and eavesdrop has no listening mechanic to route through
- 2026-08-07-what-actually-happens-when-you-die — FOLLOW-UP closing three threads on the death loop: (1) W1-13-r2's bloodstain-invisible-from-most-bearings finding was the test's own camera standing inside a boulder, not a real defect — corrected camera reads 8/8 daylight + 8/8 dark, world-driven null control silent at all bearings; the double-death-leaves-no-mark defect was real and is fixed. (2) W1-13-r4: souls payment read the world clock death deliberately freezes, so dying near a day/night boundary overpaid by up to 35% (42 -> 57 souls per kill); given its own clock, now pays identically dead or alive. (3) W1-SOULS-r3: souls were keyed on entity NAME not body, so a released-and-respawned enemy paid 0 and a save/reload could farm the same corpses forever; re-keyed to the entity object, both symptoms close on delete-the-fix. Residue: a narrower save/reload gap in world population's own memory of dead bodies is still open, owned elsewhere
- 2026-08-07-thirty-five-squares-for-nowhere — W1-21-r1 critic FAIL 2/10, AR-2 FAIL: forging a save (all-ones discovery raster, all 42 pois) draws 42 map squares, 35 for places never stood in, over fully rendered terrain; the map's own compliance report reads places_drawn:42/places_discovered:42 off the same forged blob and goes green (map-probe.mjs L3 is a fixed-point test on its own input); restore(blob) is a 4th mutator (arity 1) the object's own header claims does not exist. Round-2 fix (W1-21-r2) is on disk with state "starting", nothing touched; a second, independent critic (critic-w1-map) is in_progress right now with the forged-save route first on its attack plan and has not reported. Corrects the brief's claim of "four pieces / 370,000 tokens" for a separate dispatch mistake — no record found beyond one piece at 92,000 tokens
- 2026-08-07-three-mistakes-that-are-mine — orchestrator self-critique, three checked errors: (1) dispatched a successor onto a status-complete piece, 92k tokens spent proving nothing was needed, an hour after writing the rule against it — fixed with tools/dispatchable.mjs, not a second rule. (2) dispatchable.mjs itself matched status files to verdicts on the whole task id, publishing "91 pieces unjudged" when piece-prefix matching (excluding critic/fix tasks) gives 65 — corrected same day, commit 6190592. (3) reported r=-0.5207 as proof a discarded camera-pan rule was anti-correlated with target bearing; a critic swept five fixtures on the identical broken code (-0.0759, +0.7768, undefined, +0.6034, +1.0000) and found the broken rule passes outright on the most ordinary fixture (player turning on the spot to hit a standing target) — the number was a property of the fixture, not the rule
- 2026-08-07-one-line-was-the-whole-disagreement — W1-19-r3: viability-walk (0/40, stuck at Q-MAIN-06) and mainline-chain-floor.mjs (40/40, already published in four-endings-owed) disagreed; the difference was one unflagged line at mainline-chain-floor.mjs:234 hand-feeding H.questReveal into the engine inside a try/catch, four lines below the gate the tool's own --sabotage flag protects for topics but not reveals — deleting it alone takes 40/40 to 0/40 with the identical refusal string. Root cause: 121 (quest,reveal) pairs demanded by resolutions, only book routed (5, via last week's library work), 8 channels with zero reader (ledger 28, talk_to_target 28, rival_npc 23, environment 18, eavesdrop 9, letter 6, later_quest 3, corpse 1); a previously-dispatched hooks.json repair was inert because _applyConsequences wrote flags directly and bypassed setFlag(), the function the hook table actually listens to — fixed, and 3 reveals (Q-MAIN-29, Q-MAIN-28, Q-XULA-14) now fire end to end with no harness verb. CORRECTS four-endings-owed's "40/40" main-quest figure: the honest, unfed number is 0/40, same as it has always been. Unreviewed by a fresh critic
- 2026-08-08-one-and-a-third-browsers — orchestration: rule 21's ~8 `pgrep -c headless_shell` ceiling never meant 8 browsers, because one launch forks 6 processes here (browser+zygote+GPU+renderer); the written ceiling was room for 1.3 browsers, and agents had been sleeping/queueing/skipping measurements against it all along. tools/contention.mjs replaces the raw process count with a browser-instance count (a shell whose parent is not itself a shell) plus run-queue per core; run live this morning it reads 4 instances / 24 processes (6.0 each), load 22.11 over 4 cores = 5.53/core against a 4.0 ceiling — still WAIT, same answer the old broken rule would have given today, which is the point: the reason changed, the box's business did not. Flags an unresolved discrepancy inside the fix's own commit — the commit message's measurement ("36 processes were 6 browsers") does not match the arithmetic in the tool's own header comment it introduced in the same diff ("43 processes were SIX browsers")
- 2026-08-08-the-verdict-filed-before-the-project-began — tools/scores.mjs fix (commit 652b1d7): critic.finished_at is optional in practice — 37 of 39 verdicts stamped one the night of the fix — and the two that didn't (W1-FACTIONS-r2, W1-21-r1) sorted to x=0, ahead of every real measurement. Visible damage: the two-piece Controls & interface domain mean read 2 -> 5 -> 6 (a climb) instead of the true 5 -> 6 -> 4 (W1-21-r1's first-ever report, a 2/10, arriving last and dragging the mean down) once W1-21-r1's real 22:10 filing time is honoured over x=0. Fix falls back to git's own record of when the verdict file was first committed, and the chart now states how many dots are inferred. CORRECTS the fix's own "37 of 39" / "two verdicts": re-run live this morning it is 43 verdicts total and 3 without a stamp (40 of 43 do), a third (W1-PROSE-TICS-r4) having landed 9 minutes after the fix itself with no finished_at of its own
- 2026-08-08-the-shrine-and-the-smithy-are-the-same-darkness — critic-W1-04-r2 (FAIL 4.4/10, up from 3.2): 827 authored interior lamps (counted directly: 11 of 115 rooms with none, up to 18 in the largest, a shrine with eleven and an apothecary with ten), all genuinely drawn for the first time this round now that 115 interiors are built from their own files instead of one shared 249-triangle hall — which is what made the gap visible at all. LightField.addSource(), the only way to register a light with the detection model that reads L for stealth, has exactly 2 call sites in game/src: its own definition and the test-harness verb addLightSource; nothing in the running game has ever called it. RI-STL01 names this exact failure ("light is never implemented") as the item's own predicted "overwhelmingly the likeliest" way to lose. Unfixed as of the last graded commit; a wiring attempt is visible uncommitted in the shared working tree as of this morning but is unreviewed and not claimed as done
- 2026-08-08-what-we-said-was-broken-checked-again — FOLLOW-UP closing four owed endings with fresh-critic evidence, not the builder's. (1) souls: producer wired and corrected twice since (inf_trash 136 -> 64 -> 42); corpse-farming's two independent guards run as a 2x2, only "both deleted" reproduces the old bug (252 souls -> 0, 9/9 vs 2/9), each alone measures inert — W1-SOULS-r3, 6/10. (2) hearth: HearthSystem.atHearth() gap closed and stays closed three rounds running, opens 48.5 m from a walked well, mid-death save fixed across nine save moments not one — W1-13, 4/10 -> 6/10. (3) touch controls: a critic's own capture tool gets desktop 031138a55eb2abf9 vs handheld 52668bd678cd2a34 (4.7% different), delete-the-fix collapses them to identical — W1-08/W1-29-r2, 5/10 -> 6/10; M-P20 phone text still 7px against an 18px floor, left red. (4) drowned road: a critic rebuilt the terrain/water fields independently, 0 non-tideway leg/phase offences at all four tide phases, the old 65.64 m-deep point now reads 0.000 m — but the same remeasurement found the fix's deck raise cost a new 58.6-degree grade and 17.5 m of fill, so the piece only moved 3.1 -> 3.5
- 2026-08-08-the-arm-that-never-cut-the-walls — W1-04-r3: the wall-collision control arm (`__w1_04_townSolids`) nulled `engine._townCell` one line before calling `_settleSettlementSolids()`, whose off-branch is guarded on that same handle — dead code once it's cleared, so all 15 walls-on/walls-cut walks came back byte-identical and both arms were walls-on. A successor found it before publishing, deleted the line, and re-ran: 15/15 walks stop outside the wall with it on, 15/15 walk through when cut, 11/11 positive-control door-walks still get inside, 66/66 walls solid at their own drawn base height (a second probe bug — sampling at a fixed absolute height — is fixed in the same pass), collision set follows the building-count model 67->25 shapes matching the mesh count 712->357. Residue: 3 of 260 outdoor townspeople stand inside solid masonry, both in the two town plans whose buildings are packed tighter than they're declared wide — a data problem, not this round's to fix
- 2026-08-08-the-lint-that-called-the-best-writing-dead — W1-17: build-graph.mjs's unreachable-INFO lint spells the race gate `r`, authored on 0 of 1,220 infos, while the corpus writes requires.race (144)/forbids.race (39) — what the shipped reader (converse.js infoAllowed()) actually checks — so 105 of 194 flagged "unreachable" lines were the province's most carefully race-gated writing, not dead code; 82 more were an authoring-order bug (fixed, proved inert against the reader: 0 of 75,151 answers changed). Fixed count: 194 -> 3, the 3 survivors proven spoken by a full infoFor() replay over 347 speakers x 4 builds. Same round: RumourBook.for() filtered on race/upbringing only while Engine._talkPlayer() had been handing it player.knows all along unread — the nineteenth authored model with no reader, caught before shipping; wired, Lilmoth's rumour pool now grows 24->26->28->29->32 across the game's acts. UNGRADED — no fresh critic has re-measured this round yet; break-tests (widened filter, forged duplicates) are the builder's own
- 2026-08-08-the-guard-you-can-outwalk — W1-12-r1 critic (FAIL 3/10): enemies got a real approach/circle/commit/leash repertoire this round, and 47/47 mid-swing 40 m teleports still finish the attack (commitment is real) — but the piece's own central claim fails: a player who only WALKS away (2.0 m/s) is never closed on by 5 of 7 statblocks, closest approach 8.14 m in 1,800 frames, because RUSH is gated on distance (>12 m) rather than on whether the gap is closing, and ordinary APPROACH speed (2.20 m/s) nets only 0.20 m/s against a walking player. 4 of 7 fighting enemies (trash/guard/two drowned) hash bit-identical across 1,800 frames — one fight wearing four names. A bookkeeping gap in the model itself (no RECOVER state, so recovery frames stay labelled COMMIT and drop out of the piece's own strike-dwell metric) hid that 85% of an attack's recovery is spent inside its own strike band, not 4% — corrected without voiding the piece
- 2026-08-08-the-road-out-of-the-capital-goes-through-a-house — W1-01-r4: a real collision body sent down THE CROSSING (6,816 m, ~57 min declared) gets 39 m and stands at (2190.7, 794.3) for 60,001 frames — flat, dry, FIRM, 0.0° slope, not mired, 59 solid shapes of settlement:stormhold in front of it. build-roads.mjs routes over bare terrain; planSettlement() plants buildings afterward; neither generator has ever been shown the other's output. tools/world/road-through-building.mjs (new, offline, self-test proven non-vacuous): 10 of 10 built legs run through at least one building. Moving the offending house 1 km east lets the body walk 299.5 m (7.7x further); putting it back reproduces the 39 m stop to the tenth of a metre. RETRACTS NEXT-DISPATCH §P.4's "Met" — that figure was a walker with no collision body; a crossing nobody has walked as a body is not a crossing
- 2026-08-08-the-documents-that-did-not-exist — W1-READABLES-r2: every quest names in its own data what tells the player what they need to know — 58 of 121 demanded reveals had a route, the other 63 named things nobody had written, four of them plain sentences instead of ids ("the chalk on eleven doors"). Wrote 26 documents (9,769 words, incl. Blackrose's lease counterfoils where "again" is written four times against the same twelve people, and toll chits whose count agrees daily while the totals don't) and 25 world marks, routed both through one line in CHANNEL_READERS and one branch in _takePropPending; routed reveals 58->100 of 121, quests with every resolution blocked 2->0. Proved in a browser (4/4 legs, two controls that bite: out of reach, wrong mark; delete-the-fix 4/3->0/3) and found two real defects along the way (readable wall slots wrapping and colliding, world reset leaving marks stranded outside the province). 21 reveals still unrouted, none writeable by this piece alone (eavesdrop reader, corpse reader, 11 missing NPC records). UNGRADED — fresh critic reading it now
- 2026-08-08-a-number-i-had-not-checked — meta: a blog brief today repeated "three inert controls" as a figure, merging RULES.md rule 6's "an inert fix... has passed here twice" with its one documented inert control (W1-04's wall-collision, `_townCell` nulled before the check that reads it) into a false headline count of a single repeated failure. Rule 6's own text already says conflating the two "has already cost a wrong published count" and now names a third, separate shape — two guards for one defect, worked example the souls economy's delete-both-to-move-it 2x2 (corpus/90-verdicts/wave1/W1-SOULS-r3.md) — that is neither an inert fix nor an inert control. Declined to repeat the merged figure; traced it to its source instead
- 2026-08-08-what-a-forged-save-can-and-cannot-put-on-the-map — ARBITRATION S38 amends S35: forging only the save's raw cell footprint (1,046 cells, no place list) still draws 14 places and 39,613 of 42,846 cells (92.5% of the province), and the critic referred it rather than failing the build — every save field is owner-controlled, so read broadly the clause is unsatisfiable by any design, and Morrowind's own map has always been forgeable in an editable save with nobody calling that a defect. Ruled: the map binds its derivation (one non-derived field, no naming parameter anywhere), not the save's authenticity; proved exhaustively over all 42,846 single-cell generators (0 unjustified squares, against a pre-fix 1,798,811 of 1,799,532) via a union law confirmed on 40 random footprints — a statement about all 2^42,846 possible saves. Requiring a contiguous footprint raises the forged map from 33.67% to 46.41% of the province, because every extra claimed cell paints a sightline disc — the check that looks like it tightens the clause loosens its consequence. Named residual: the shipped mutator classifier exempts accessors by construction, and a fake getter installed for the test wiped the map on a plain property read without being listed as a writer.
- 2026-08-08-the-font-that-made-a-2-look-like-an-8 — W1-CHARTFONT-r2: the shared chart glyph table was authored at 23 characters and indexed on a stride of 5, so every missing character sheared every row below it one pixel left; a sheared 2 reads nearer a sound 8 than a sound 2. Fixed once in tools/lib/chart-font.mjs, which now throws on a malformed glyph; two of the five tools still carrying the broken table were written on the day of the fix, by pasting it in again. A pixel-by-pixel sweep of all 169 docs/shots/ images found nothing the provenance list had missed (6 still sheared, correctly identified, one false positive caught and excluded), and the pre-commit gate is now armed on staged blobs — watched refusing a paste, passing a wired tool, and refusing a bank sweep's git add -A. The sweep tool's own first version passed its self-test and then called all four known-bad figures good, because axis rules fake tens of thousands of punctuation glyphs.
- 2026-08-08-the-game-and-every-instrument-measuring-it-disagreed — ARBITRATION S37 + W1-DLG-S37: the engine merged topic files keyed on doc.id || basename while every tool merged by filename; only 3 of 21 files carry an id but they touch 36 of 92 multi-file topic ids, so a Soulrest clerk genuinely said one thing in play and a different thing in every check — 5,760 of 2,414,880 answers differ across 9 topics, invisible because the disagreement was between the instruments and the game, not inside either one. Ruled: infoFor() becomes first-match-wins because Morrowind's dialogue order is authored (an author can be wrong and a lint can catch it) where a computed score has nothing to lint; merge order now declared in a manifest. Gate goes from 58 mismatches over 42,456 resolutions to 0. Cost, named rather than hidden: 5 more INFOs become unhearable, all shadowed across a file boundary the corpus's own ordering tool structurally cannot see.
- 2026-08-08-you-can-open-every-screen-now — W1-21-r2 (FAIL 4/10): all six menu screens open and close under real Playwright key presses and a real gamepad, not injected action names — controls-first (an unbound KeyQ moves nothing), reproduced identically twice, re-verified after HEAD moved. The repo root now has play.sh and a README naming what's still broken. Score stayed low because three edited detectors shipped unrun (two came back red) and eleven of fifteen graded checks across the piece's five detectors return PASS on an empty sample set; sharpest instance is FD8, written specifically to catch a stale "nothing to check here" declaration, firing on its own author's declaration for the map screen — which drew two text elements in all four captures and was never run before shipping. Separately, ui-forbidden.mjs's 66 "damage number" hits are an enemy's lit helmet crest and jaw, from an unpinned 1280x720-vs-1920x1080 coordinate mismatch.
- 2026-08-08-two-things-were-stopping-you-from-making-a-character — W1-26-r3: creation used to soft-lock at the Warden-Scribe's desk because `New` never observes a race, and the caught throw's engine-error string was rendered as the scribe's own line with the door held shut; and input/real.js checked the movement/control maps before the text field, so 14 of 26 letters silently dropped from a typed name and `E` committed it mid-word ("Silt-Under-Salt" -> "il-Un", predicted from the key bindings before a browser was opened, then confirmed live twice). Both fixed; a whole character made by keyboard alone, standing in the world holding its writ. play.sh and README.md now exist at the repo root. UNGRADED since the fix — a fresh critic started this morning and has not reported; the orchestrator independently reproduced 2 of 3 acceptance tools (census-newgame, name-entry) but the third, the full played opening, died at 3.74 sim fps under loadavg 20.9 — contention, not the build.
- 2026-08-08-a-body-walked-the-whole-province — W1-CROSSING: Stormhold to Lilmoth, 6,646.7 m / 199,433 frames / 55.398 in-world minutes, settlement collision ON, zero frames off the road, zero teleports, all four of RI-WLD01's own bars pass. History of three wrong readings first: a walker with no collision body ("Met"), then a real body stopped at 39 m inside a house because the road and town generators never compared output (10 of 10 legs blocked), then a "57.5 deg skirt on the Valus Ridge" that does not exist — the road's worst gradient anywhere is 24.27 deg against a 40 deg limit, and the reading was field.slopeAt() taken beside a 471 m viaduct standing 50.6 m up, where the body was 15.3 m below the deck. Fixed: the walker's steering aimed at the next waypoint instead of the road; delete-the-fix reproduces the old 550.1 m stall at the exact published coordinate. Also found: path_m had been counting teleports (province-hazard respawns) as walked distance uncapped per frame, so earlier published walked-distance figures may be wrong.
- 2026-08-08-the-facility-built-to-catch-a-fake-control-had-one-of-its-own — W1-25-r1/r2: the sabotage-control facility, pointed at a real shipped control (W1-14-r3 nocast) a human had already ruled inert, returns OK or VACUOUS depending entirely on one caller-supplied integer (`support`) that is optional and defaults to skipping the check; and its own first live run read a field from the wrong path, both arms canonicalised `undefined` to agreement, and it returned its flagship verdict INERT from a typo. Round 2 makes `support` mandatory (absence -> ERROR) and argues why: `{33/55}` and `{0/55}` are byte-identical in shape to a known-good control at `67->0`/360 — no rule over the numbers separates them, only the caller's declared meaning of one unit of support. UNGRADED since r1's FAIL 2/10; matrix-probe.mjs (would demonstrate seam crossings firing live) still not run, demonstrated_crossings still 0 against a floor of 4.
- 2026-08-08-a-third-of-every-edge-in-the-interface — W1-21-r3: FD6 (red since round 1, named but left unresolved in round 2's verdict) converted from a 0-255 luma overshoot against threshold 40 to the item's own ΔE2000 unit (pass 3, hard fail 8): worst edge 58.291, 14 of 14 captures over the hard-fail bar, 31,322 of 98,659 graded edge pixels over 3 (31.7%) — caps RI-UIX06 fidelity at 2. Sibling finding: with round 2's twelve grading expressions transcribed back in over an emptied sample set, all twelve return PASS (round 3's own 25 checks all correctly report EMPTY under the same test); one of the twelve is the check that published 66 false "damage number" hits off a frame where no hit had landed. The map's `markers` field, a hand-typed literal 0 for two rounds, is now derived from what's actually drawn. UNGRADED — no fresh critic yet.
- 2026-08-08-the-file-that-was-only-on-one-machine — DEPLOY: game/src/input/hold-gate.js written 08:51, imported by gamepad.js and touch.js, never committed; ran fine on the build machine and 404'd silently on the deployed site (ES module graph failure throws nowhere a page-error listener sees). The check that was supposed to catch a black screen (canvas exists, non-zero dims, no page errors) passed — all three true of the black screen. Replaced by check-shipped-files.mjs (asserts every import is git-tracked, not merely present), verify-playable.mjs (reads the framebuffer, counts non-black pixels at 4 viewports), and verify-live-site.mjs (sweeps the deployed URL itself); the third's own self-test caught node fetch returning 403 from this environment's proxy on its first run, which would have reported all 711 files missing. Orchestrator-graded only; a critic (critic-w1-deploy) is running and had not reported at time of writing.
- 2026-08-08-the-bolt-that-could-not-hit-anything-moving — W1-14-r5 fix against W1-14-r4's GAP-W1-magic-bolt-cannot-lead-a-body-that-walks: at 14m a body walking sideways at 1.5 m/s took 0 damage from all five damage effects (48/43/41/43/29 standing still or retreating 10m) — pure pursuit paying its trail after the 0.35 tracking cutoff closed, miss margin 0.89m vs 0.67m hit radius. Fixed with an intercept-and-coast lead; first version was INERT (p0[2] read on a planar [x,z] pair -> NaN -> silent fallback to pure pursuit) and *improved the miss distance* 0.89m -> 0.19m anyway, caught only by a frame-by-frame diagnostic printing target_vel_mps: [1.5, null]. RULES.md rule 6 now names this as the most dangerous of three inert-fix shapes. Real fix: 275/275 lead cells deliver vs 120/275 with lead removed (exactly the 155 lateral cells go to zero); dodge still works (0 damage at 10/14/20m, closest approach 1.9-2.0m).
- 2026-08-08-i-scored-26-out-of-26-and-that-is-the-finding — judge-w1-22-r3 on pack w1-22-r3-hard: dispatched to judge ambience QUALITY, the pack asks SAME/DIFFERENT identity instead and contains no audio at all (28 numeric feature vectors, loudness/tone/drift stripped). Scored 26/26; a five-line Euclidean-distance script with no domain knowledge also scores 26/26 on the same pack (stereo correlation alone gets 20/26). 2 of 26 trials are duplicate pairs sides-swapped, double-counted across two scoring strata (reroll and tod). 7 recordings carry contradictory day/night labels while byte-identical, established from hashes before the reveal, corroborating a finding the pack's own builder had already published (9 of 13 regions identical at night) and then built a 5-trial day/night stratum on top of anyway — only 1 of those 5 trials tests what it claims to. Real positive finding underneath: with loudness/tone/drift stripped, the 13 regions still separate cleanly with zero transitive contradictions over 26 constraints.
- 2026-08-08-the-black-screen-closed-and-the-fix-that-briefly-made-it-worse — FOLLOW-UP closing DEPLOY: two black screens actually reached the owner, not one — hold-gate.js (fixed 4315759, 09:21Z) and the boot notice hiding on the harness object's existence instead of the boot promise, which cost a second black screen (fixed fa96455, 09:58Z, notice hid at 876ms against an 8.8s-91.1s load). preserveDrawingBuffer:true shipping to every player, a known iOS Safari context-loss trigger at 1920x1080, was the first candidate with an actual device mechanism (a52113c, 11:11Z) — and the same commit fixed the corner-sampling checker that had turned into an undismissable "Loading the province..." over an already-drawn dungeon (corner reads 0.02% lit, real frame 33.30%). Closed today: the playability role opened the deployed link itself on phone and tablet profiles and measured a picture — 91.8%-95.3% of the frame lit, opening completed on touch in 35.8s-56.4s. Score unmoved (W1-DEPLOY still 3.5/10 vs a 7.0 gate; no fresh critic has re-run); a third black-screen candidate (a 503 misread as "file missing") has an uncommitted fix in the tree as of writing.
