# W1-21 builder aggregate at `b5850ff` + builder working tree

This is builder evidence, not an independent verdict. Blind and fresh-judge rows remain
`not_run`; their reproducible source-pack specification is `ui-comparison-packs.json`.

## Builder changes and repaired gates

- Removed ten unused `quest` keys from physically placed interior site marks. Those keys sat
  beside authored coordinates and were a ready-made quest-marker mount point. The full 578-file,
  373,743-node marker scan is now 0 F1-F6 hits (one existing declared zero-count exception).
- `ui-pause.mjs` now catches streamed entities disappearing between census and cleanup inside
  the page instead of letting `tools/lib/browser.mjs` convert an expected stale entity into a
  process exit.
- Added an exhaustive 12/12 UI-model consumption probe. Each production model/drawer changes a
  visible/actionable consequence under a separated value and returns to baseline when its
  consumer is equalised.
- The interaction probe reports its stricter D-pad column assertion red (1 -> 1), so that row
  is recorded red rather than hidden. A plausible button/action suppression change was tested,
  proved inert against this observable, and removed rather than shipped.

## Eleven-item row accounting

| Governing item | Builder-owned result | Evidence / remaining independent or sibling work |
|---|---|---|
| RI-UIX01 | **PARTIAL**: U1/U2/U3/U4/U4b/U6/U7/U8/U10/numeric 10/10 on 1,800 combat frames; declared X1-X12/N4-N5 0/119. | `ui-census.json`, `ui-forbidden.json`. U5 is `EMPTY` because the declared-only repair run intentionally did not invent a post-hit sample; full resolution/background population remains for the fresh critic. |
| RI-UIX02 / S35 | **PASS for rerun owned rows**: K1 0/373,743; K3 0/10 quest/surface differentials; K4 0/20 elements across 3×24 yaw samples. | `marker-diff.json`; previous K2/K5/K6 captures require commit/config diff validation by critic. |
| RI-UIX03 | **AUTOMATIC NATIVE 9/9, interaction row red**: 0/120 calm, 120/120 combat, 37/37 no dilation, 50% opacity, 57.2% area, 78% centre response, roll live, 30-frame equip, no combat toast, seven surfaces draw. | `ui-pause.json`. GameSir pad opens, stick-navigates, confirms and closes, but D-pad column assertion remains red. Step-8 independent comparison is `not_run` with a prepared specification. |
| RI-UIX04 | **PASS 11/11 automatic**: 19 entries/5 quests, interleave 0.8889, zero forbidden/coordinate text, chronological search, S35 route zero, 0/120 pause. | `journal-ui.json`. JU12 is `not_run` and reserved for an independent judge. |
| RI-UIX06 ART | **not_run**. | Ordered declaration, six-screen hashes, black-world capture spec, and six-judge handoff are prepared; builder did not judge. |
| RI-UIX06 FIDELITY | **not_run at this changed tree**. | Ordered 48-capture declaration and native measurement/CC-7 specification prepared. Historical FD6 hard fail is not claimed repaired or green. |
| RI-CAM03 | **not_run as a complete item**. | HUD consumer rows above are reusable; camera-owned framing/latency sibling evidence needs independent commit validation. |
| RI-CAM05 | **not_run as a complete item**. | Menu-in-combat visibility and seven native surfaces are measured; camera/interior/death sibling rows remain camera-owned. |
| RI-PRG01 | **not_run as a complete item**. | Level-up drawer consumption is green; arithmetic/cost sibling evidence remains progression-owned. |
| RI-PRG02 | **not_run as a complete item**. | Level/sheet drawer consumption is green; ten-attribute curve/scaling sibling evidence remains progression-owned. |
| RI-VIS05 | **not_run as a complete item**. | UI ART declaration/source hashes prepared; non-UI visual population and independent Bootstrap judgements remain independent. |
| RI-WLD06 | **not_run as a complete item**. | Owned no-marker UI rows are green; road/sign/dialogue/route sibling populations remain world-owned. |

## Exhaustive model population

`model-consumption.json` records 12/12: `_hudModel`, `_inventoryModel`, `_containerModel`,
`_journalModel`, `_bookModel`, `_levelModel`, `_sheetModel`, `_spellModel`, `_mapModel`, the
touch overlay, rotate/calibration view, and rebinding/refusal view. Each screen row records the
production drawer, changed register, equalised null and restored arm. No model is omitted.

## Hard-fail and handoff matrix

| Condition | State |
|---|---|
| Marker/data mount point | repaired; rerun green |
| Empty-population false green | repaired in canonical graders; U5 honestly EMPTY |
| UI model with zero consumption | none in the 12-row population |
| D-pad inventory-column interaction | **RED; fresh builder/critic must falsify or repair** |
| UIX03 structure judge | `not_run` (independent) |
| UIX04 JU12 | `not_run` (independent) |
| UIX06 ART six-judge worst-of-six | `not_run` (independent) |
| UIX06 FIDELITY full 48 captures / known-50%-composite / CC-7 | `not_run` |
| Historical FD6 DeltaE > 8 | stale but unrepaired; do not infer green |

The next independent actors must use the prepared pack specification, verify source hashes,
produce actual opaque screenshots transiently or in an artifact-capable channel, and score only
their assigned native rows. A fresh critic must also rerun/falsify the remaining D-pad row and
validate mixed-item sibling evidence before any complete eleven-item score is asserted.
