# W1-26 continuation builder delivery

This continuation is builder evidence, not an independent PASS or critic verdict.

## Four public-input journeys

`opening-modalities.mjs` starts from the shipping title and reaches `census.done` through every creation node in four separate browsers. Keyboard and mouse+keyboard use DOM input; gamepad snapshots enter `RealInput.pushGamepadState` and the released router/profile; touch dispatches DOM `PointerEvent`s against the coordinates returned by the drawn shipping touch layout. The harness advances fixed frames and reads observations only. All four artifacts report 7/7: action/scene reachability, completion, continuation into the Writ House, created fields, typed-name persistence where typing exists, and the stamped carried writ.

The journey accepts `--shot PATH` to capture its exact terminal frame locally. PNGs produced by the original run are deliberately not committed because this PR transport rejects binary files; the committed traces identify that policy rather than pointing at absent files.

Each modality has a perturbation artifact. It disconnects that modality's movement consumer while leaving the driver and observations intact; every arm fails before scene reachability and records `expected_red/observed_red: true`.

## Independent boundary

The copied `exp01-session-protocol.json` is the tell-free handoff already preserved by PR #108. No builder-authored or builder-judged first-hour session is represented here. RI-EXP01 driving, blind annotation, judgement, and native PASS remain `NOT_RUN` for independent roles. This builder did not self-award those rows.
