# SUPERSEDED — do not judge this pack

These trials were built at 17:03 against the corpus **as it stood before the round-2 successor's
flattening pass**. Between that build and the end of the session roughly 380 dialogue lines were
rewritten across nineteen files: every closing sting in the dialogue register was removed
(43.47 -> 0.70 per 10,000 words against the reference's 3.42), questions went from 24.34 to 56.22,
and the house-cadence and `-body` tics were cleared.

So this pack asks a judge to grade prose that is no longer in the game. A verdict on it would be
a verdict on work that has already been replaced, and it would read as a verdict on the current
build.

**Judge `reports/packs/prose-tics-r4/` instead.** Same format, same builder, same protocol
(RI-MTH03 §D quality question, both sides name-masked), built against the corpus at commit
`439fa54`, still unanswered, mapping in the sibling `prose-tics-r4.reveal/`.

This directory is kept only so the two packs can be compared if anyone wants to see what the pass
changed.

---

## Amended 2026-08-08 — the pack this file points at is itself now closed

The paragraph above says "judge `prose-tics-r4/` instead". **Do not.** `prose-tics-r4/` was judged
three times and its own judgement found it decidable 15/15 by counting `[NAME-n]` tokens; it now
carries a `SUPERSEDED.md` of its own. `prose-tics-r2/` is likewise closed as the pre-masking pack.

**The live pack is `reports/packs/prose-tics-r5/`.** Everything in this file's original reasoning
still stands: r3 grades ~380 dialogue lines that were rewritten before it was built, so a verdict on
it would be a verdict on prose that is not in the game.

All three of r2, r3 and r4 are closed as `superseded`, not `awaiting judge`, so no future
orchestrator spends a judge on any of them.

Closed by `W1-PROSE-TICS-r4-instrument`.
