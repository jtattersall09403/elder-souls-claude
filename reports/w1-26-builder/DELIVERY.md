# W1-26 builder delivery

Commit measured before the delivery commit: `c9603f6` plus the working-tree keyboard journey repair.
This is builder evidence, not an independent PASS declaration.

## Implemented repair

The keyboard-only journey formerly changed `sim.camera.yaw` through Playwright while claiming a real-input run. Its walker now projects each target onto the existing camera basis and drives only real W/S/A/D DOM events. The transient screenshot no longer changes camera mode. The repaired run reaches `census.done`, records all 11 nodes, preserves the typed name, and carries the stamped writ.

## Builder accounting

* RI-JRN09 native M1–M4: the complete current-HEAD population reports DTR_q 1.0000, DTR_scene 1.0000, AC 6/7, NAMED 14/14, and no native hard fail. The method's RI-EXP01-dependent citations remain blocked for the independent session rather than being self-scored.
* RI-JRN01 native runner: every emitted runnable row is OK (including the 60 s still-driver bound, real input, complete census walk, M9/M15, and surface checks). The command exits 1 solely for three explicitly absent amendment rows; they are corpus debt under the Wave-1 denominator and were not silently removed. A fresh critic must compute the authoritative 67-point numerator.
* Race/guard: current native verifier proves the shipped race begins and finishes; `altmer`, `argonian`, and `zzz-not-a-race` fail before node 1 with the id visible, and delete-the-fix restores the historical four-node late refusal. The existing JRN09 population crosses all ten legal race ids (240 completions).
* Title/text: zero title HUD strings with a red teardown restoring three; semantic fixtures catch 7/7 escaped polite directions and the historic teaching clause, with 0/10 false positives on allowed diegetic lines.

## Honest remaining independent/build separation

RI-EXP01 is intentionally `NOT_RUN`; `exp01-session-protocol.json` is the tell-free exact handoff. No session has been driven or scored by this builder. Mouse+keyboard, gamepad, and touch component evidence exists in W1-08/W1-29, but no newly authored W1-26 whole-scene run reaches `census.done` for those three modalities in this delivery; they must not be represented as 4/4. This is the remaining builder gap, not an independent-only row.
