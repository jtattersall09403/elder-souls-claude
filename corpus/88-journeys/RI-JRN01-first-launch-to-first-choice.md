---
id: RI-JRN01
title: First launch to first meaningful choice — the opening as an unbroken act
kind: structure
side: morrowind
judges: [journey.firstlaunch.flow, journey.chargen.diegesis, journey.onboarding.explanation]
provenance: canonical-recall
confidence: medium
blind_pair: yes
---

> **This item judges a JOURNEY, not a subsystem.** It is one of eight in `corpus/88-journeys/`.
> A journey is the thing a player actually does, end to end, crossing every subsystem on the
> way. No item outside this area judges the sum.
>
> **Division of labour, binding:** `RI-EXP01` (95-experience) owns the *beats* of the first
> hour — who acts, what is learned, what verb is taught. **This item owns the first ~15
> minutes as an unbroken chain of screens, inputs and states**, and stops at the first
> meaningful choice. Where both measure the same clock, `RI-EXP01` owns `T-choice`,
> `T-found`, `T-death` and this item cites them. **`RI-PLT03` owns the number
> `TTFP` (time to first playable); this item consumes it and never restates it.**
> `RI-CHR01` (if it exists) owns the *content* of the origin/birthsign tables; this item owns
> only whether that content is delivered diegetically.
>
> **Subsystem paths pending taxonomy registration** — see `corpus/88-journeys/paths-requested.json`.

## The bar

A player double-clicks a link and, without reading anything, without being told anything, and
without crossing a single screen that is not either the world or a piece of paper inside the
world, ends up **standing in a place, in a body, holding an input device that works**, with
somebody in front of them who wants something. Everything that defines the character is
extracted from them *by that person, in that place, as part of what that person is doing* —
and the world is visible behind their head while it happens.

That is Morrowind's opening and the reason it is still the most-praised opening in the genre
twenty-four years later. It is not praised for its content; the content is a customs officer
doing paperwork. It is praised because **character creation is a scene rather than a screen**.
The questions are asked by a bored bureaucrat who is going to stamp a form; the answers are
recorded on that form; you walk out of the room carrying the form. Nothing is ever described
to you as a "class", a "build", or a "step 2 of 4". The game does not begin after character
creation — character creation is the first ten minutes *of the game*, and you are walking
around inside it.

The bar, stated so a builder can aim at it: **zero full-screen non-diegetic panels between the
page load and the first meaningful choice**, one title surface, control of a body before the
game asks a single question about that body, every character-defining input delivered as an
answer to a named NPC standing in a room, and not one word of instruction that a person
standing in that room could not plausibly be told.

## The reference artifact

### A. `MW/OPEN` — Morrowind, Seyda Neen, load → out of the door (canonical-recall)

Beat-for-beat, with the property each beat establishes. Times are from a cold start on
period-typical hardware and are recalled, not measured.

| # | t (approx) | What happens | Player state | Property established |
|---|---|---|---|---|
| 1 | 0:00 | Title. A menu with **New / Load / Options / Credits**. No splash video that cannot be skipped. | no body | One surface, one decision |
| 2 | 0:05 | Black. A voice: *"Wake up. We're here. Why are you shaking? Are you okay?"* | no body | The game addresses you before it defines you |
| 3 | 0:20 | You are **in control**, in the hold of a ship, undefined. Jiub is standing there. Crates. A door. | **CONTROLLABLE, UNDEFINED** | **You get a body before you get a character** |
| 4 | 0:40 | Jiub asks your **name**. You type it. He remarks on it. | controllable | Name is asked by a person who then uses it |
| 5 | 1:10 | Up the stairs, onto the deck. Weather, gulls, a coastline, a guard telling you to get moving. | controllable | The world is shown before it is explained |
| 6 | 1:40 | Down the gangplank into the **Census and Excise Office**. Socucius Ergalla behind a desk. | controllable | The interface for creation is a desk |
| 7 | 2:00 | *"...and your race?"* You choose. He writes it down. | controllable | **Race is a fact recorded on a form** |
| 8 | 2:30 | *"Under which sign were you born?"* You choose. He writes it down. | controllable | Birthsign is asked as small talk |
| 9 | 3:00 | Class: **(a)** name a profession from a list, **(b)** describe yourself in detail (custom class), or **(c)** *"answer some questions"* — ten in-fiction dilemmas ("A wounded dog lies in the road…") that infer the class. | controllable | **The class system is a personality quiz administered by a customs officer** |
| 10 | 5:00 | He confirms the record aloud, stamps it, hands you a **document**, and tells you to see Sellus Gravius in the next room. | controllable | Creation ends with an object you carry |
| 11 | 5:30 | Sellus Gravius: a package, a note, orders to go to Balmora and find Caius Cosades. He does not explain who that is. | controllable | The main quest arrives as a job, not a prophecy |
| 12 | 6:00 | You open a door and are **outside, in Seyda Neen**, with no objective marker and no instruction. | controllable | The tutorial ends by ending |
| 13 | ~8:00 | Fargoth is looking for a ring. The ring is in a barrel. You may give it back, keep it, or tell him where it is. | controllable | **First meaningful choice with two persistent consequences** |

**Screens crossed between load and control: one** (the title menu).
**Full-screen non-diegetic panels between control and the door: zero.**
**Tutorial pop-ups in the entire sequence: zero.**
**Number of times the player is told what they are: zero.** (You are not told you may be the
Nerevarine until hours later, and the first person who says it does not believe it.)

### B. `DS/OPEN` — Dark Souls, Asylum → first bonfire (canonical-recall)

The Souls half of the comparison, and the half we **lose** on where it conflicts.

| # | t | What happens | Property |
|---|---|---|---|
| 1 | 0:00 | Title, then an unskippable-once cinematic (skippable thereafter) | — |
| 2 | 1:30 | **Class + Gift selection: a full-screen menu on a black background** | The non-diegetic pattern we reject |
| 3 | 3:00 | You wake in a cell. A corpse drops a key through the grate. | Control arrives with no explanation |
| 4 | 4:00 | Corridor. **Scrawled messages on the floor teach the verbs**: attack, roll, parry. | **Diegetic-adjacent instruction: writing on the ground, not a pop-up** |
| 5 | 5:00 | The **Asylum Demon** drops through the ceiling. You have a broken sword hilt. You cannot win. You run through a side door. | The game teaches you to leave |
| 6 | 6:30 | **First bonfire.** | Checkpoint established before the first real fight |
| 7 | 8:00 | Sword, shield, the roll corridor, then the demon again — now winnable | The loop is taught by repetition, not by text |

**Ruling under ARBITRATION.** Character creation is *outside the fight*, therefore **Morrowind
is authoritative** and Dark Souls' class-select-on-black-background **loses**. We take Souls'
*instruction* method — writing scratched into the world at the exact place the verb is first
needed — because Morrowind's opening has no combat instruction at all and ours must (see
`RI-JRN03` M-DISC). We take Morrowind's *creation* method entirely.

### C. `ES/OPEN` — the constructed target (BINDING)

The sequence our build must produce. Every row is a check.

| # | Requirement | Value |
|---|---|---|
| O1 | Surfaces between page load and a controllable body | **≤ 2**, being: (i) a title surface, (ii) at most one save-slot list. Nothing else. |
| O2 | Unskippable time on any surface | **0 s**. Every logo, splash and cinematic is skippable on the first frame it appears, by any input on any device. |
| O3 | The title surface offers | `Continue` (if a save exists, focused by default), `New`, `Load`, `Settings`, `Quit-to-menu`. Nothing else. **No "Play" button that leads to another menu.** |
| O4 | First input accepted | The same gesture that starts the game unlocks audio. There is no separate "click to enable sound". |
| O5 | `T_control` — navigation start → first frame in which a player input moves the character | Owned by **`RI-PLT03` P1**. Cited here, not redefined. |
| O6 | **Control precedes definition** | The player is controllable for **≥ 60 s of available play** before the first character-defining question is asked, and that time contains a walkable space, at least one other person, and at least one object that can be picked up. |
| O7 | Character-defining inputs (name, origin/race, birthsign-equivalent, class/profession) | **100% delivered as answers to a named NPC**, in a named interior, with the 3D world rendered behind the dialogue surface (S18: third-person, the body visible). |
| O8 | Full-screen non-diegetic panels during creation | **0.** No black background. No rotating model on a void. No stat sliders. No "Step 2 of 4". No "Confirm your build". |
| O9 | The class/profession route | **≥ 3 routes**, mirroring `MW/OPEN` #9: pick a profession, declare a custom one, or **answer ≥ 8 in-fiction dilemma questions** that infer it. The questionnaire route is **mandatory** and its questions are prose about the world, never about statistics. |
| O10 | Creation terminates with | A **carried object** (a stamped document / a chit / a debt-marker) that exists in the inventory, is readable, has the player's answers written on it, and is referenced later by at least one NPC. |
| O11 | Instruction budget: pop-ups, modals, tooltips, "press X to Y" banners, tutorial overlays | **0**, for the entire journey. |
| O12 | Instruction budget: in-world inscriptions (the `DS/OPEN` #4 mechanism) | **≤ 6** before the first meaningful choice, each physically placed at the point of use, each written in-fiction, each readable as a thing a previous prisoner/pilgrim scratched. |
| O13 | Objective text, quest markers, compass, "new quest added" toasts | **0.** (AR-2, seam S8.) The main-quest hand-off is spoken, then written into the journal in first person (`RI-DLG05`). |
| O14 | The player is told they are chosen / prophesied / special | **Not before minute 45.** (Inherited from `RI-EXP01`'s hard fail; restated here because this journey is where it would happen.) |
| O15 | Loading discontinuities ≥ 2 s between first control and stepping outside | **≤ 1.** Interior transitions are covered by `RI-PLT03` L4. |
| O16 | `T_choice` — first decision with two consequences that persist | Owned by **`RI-EXP01`**: ≤ 12 min. This item asserts the choice is *reachable from the opening without leaving the opening's space*, i.e. it is in the first settlement. |
| O17 | Input-modality parity | The entire journey, including every creation question, is completable **(a)** on keyboard only, **(b)** on mouse+keyboard, **(c)** on gamepad only, **(d)** on touch only. Any surface that requires a mouse cursor is a hard fail (`RI-JRN04` M-PAR). |
| O18 | Re-entry | Starting a second new game skips nothing automatically but allows every surface to be skipped in **≤ 5 inputs** total. |

## Comparison method

Run by `critic.journey` (fleet critic **JC-01**, see `JOURNEY-CRITIC-FLEET.md`). Two passes:
an **instrumented pass** (the numbers) and a **naive pass** (a first-time-user agent, isolation
`enforced`).

### Instrumented pass

```bash
node tools/journey/journey-run.mjs --journey jrn01-opening --seed 4711 \
     --profile desktop-1080p --input-mode real --trace-events \
     --out reports/journeys/<runId>
```

Requires harness amendment **`A-JRN1`** (real input path + first-input timing + the new trace
events). Until it exists every check below is `unmeasurable` and scores **0**, fail-closed
(CRITIC-DOCTRINE §7.3).

| # | Check | Procedure | Threshold |
|---|---|---|---|
| **M1** | Surfaces to control | From the trace and a screenshot every 250 ms, count distinct full-viewport UI states with no 3D world rendered behind them, from `navigationStart` to the `first_control` event. | ≤ 2 (O1). **Hard fail ≥ 4.** |
| **M2** | Unskippable time | For each surface, dispatch a real input on its first rendered frame; measure frames until the surface is gone. | 0 surfaces exceed 30 frames after an input (O2). **Hard fail:** any surface that ignores input entirely. |
| **M3** | Title composition | Screenshot the title; enumerate every focusable element via the accessibility tree (`page.accessibility.snapshot()`). | Exactly the O3 set. **Hard fail:** a `Play` button whose activation leads to another menu rather than to the world. |
| **M4** | Control-before-definition | From the trace: frame of `first_control`, frame of the first `dialogue_open` event whose node writes a character field. Convert to available play seconds. | ≥ 60 s (O6). Also assert ≥ 1 other NPC entity and ≥ 1 takeable item entity exist in `listEntities()` during that window. |
| **M5** | Creation diegesis | For every character-defining input, capture a screenshot at the moment of the input and compute the fraction of the frame occupied by opaque non-world UI, plus whether any 3D entity is visible. | 100% of inputs occur with world visible; opaque UI ≤ **55%** of frame area; **0** frames with a uniform background covering ≥ 90%. **Hard fail:** any creation input taken on a screen with no world behind it (O8). |
| **M6** | Named interlocutor | For each character-defining input, resolve the speaking entity to an `npcs/*.json` record with a `name`, a `settlement` and ≥ 3 topics. | 100%. **Hard fail:** any field set by a menu with no speaker. |
| **M7** | Class routes | Enumerate the routes offered at the class node from the dialogue graph *as presented in play* (not from the data file). | ≥ 3 routes; the questionnaire route present with ≥ 8 questions; **0** questions containing the tokens `Strength`, `Endurance`, `skill`, `+`, `%`, or any numeral used as a stat (O9). |
| **M8** | The carried object | After creation, `getPlayerStats()` / inventory contains the O10 object; open it and assert its text contains the player's name, origin and profession as given. | Present and correct. **Hard fail:** absent. |
| **M9** | Instruction budget | Grep the captured UI text stream (every string rendered outside a dialogue/journal/book surface) over the whole journey for imperative second-person instruction and for the substrings `Press `, `Tap `, `Click `, `Tutorial`, `Objective`, `Quest added`, `New quest`, `Tip:`. | **0 hits** (O11, O13). **Hard fail:** any hit. |
| **M10** | In-world inscriptions | Count entities of kind `inscription` encountered before the first meaningful choice; for each, assert it is a world entity with a position, is readable via `interact`, and is placed within **8 m** of the first situation requiring its verb. | ≤ 6, ≥ 1, 100% placed (O12). |
| **M11** | Marker sweep | `RI-WLD06` M31 run over this journey's HUD frames. | 0 markers/arrows/minimap/compass. **Hard fail:** any (AR-2). |
| **M12** | Loading discontinuities | From the trace, count gaps where the sim advanced 0 frames for ≥ 120 render frames between `first_control` and the `region_stream_in` for the exterior. | ≤ 1 (O15). |
| **M13** | Modality parity | Re-run the whole journey four times: keyboard-only, mouse+keyboard, gamepad-only (via `tools/journey/gamepad-shim.mjs`), touch-only (CDP touch emulation, 390×844 → landscape 844×390). | 4/4 complete. **Hard fail:** any run that cannot complete (O17). |
| **M14** | Second-run skip cost | Start a new game from an existing save state; count inputs to reach `first_control`. | ≤ 5 (O18). |
| **M15** | Chosen-one sweep | Grep every string rendered before minute 45 for the prophecy vocabulary registered in `RI-LOR01`. | 0 hits (O14). **Hard fail:** any. |

### Naive pass (isolation: `enforced`)

A fresh agent (fleet role `JC-01N`) receives: the URL, the driver's stdio protocol, and one
sentence of premise. It receives **no corpus, no data files, no source, no screenshots from
anyone else**. It is asked, in this order, and its answers are recorded verbatim before any
reveal:

1. Before touching anything: *"You are about to start a third-person action RPG in a browser.
   Write down what you expect the first two minutes to be."* (This is the pre-registration
   that makes M17 computable.)
2. Play until you are outside the first building. Then: *"Describe everything that happened,
   in order. Mark each thing: **the game told me**, or **I found it**."*
3. *"Who are you? How do you know?"*
4. *"What were you asked, and by whom, and where were you standing?"*
5. *"What are you supposed to do now, and who told you?"*
6. *"List everything you did not understand."*

| # | Check | Threshold |
|---|---|---|
| **M16** | The agent can answer 3, 4 and 5 **without** naming a menu | 3/3. **Hard fail:** the answer to 4 is "a character creation screen". |
| **M17** | Expectation delta | The agent's pre-registered expectation (1) mentions a title screen and a character creator; the actual sequence must differ from it in a way the agent describes as *the game starting earlier than expected*. Recorded as a quotation, not a number. |
| **M18** | `found_fraction` — items in answer 2 marked "I found it" | ≥ **0.30**. A first ten minutes that is 100% delivered is a corridor. |
| **M19** | Confusion census (answer 6) | Every entry is triaged: **legitimate mystery** (the world withheld something on purpose) vs **defect** (the player could not act). Defects ≤ 1; **hard fail** ≥ 3, or any defect that blocked progress. |

### Blind pair (`blind_pair: yes`)

Pack: **A** = a 12-line beat list extracted from our run by `tools/journey/beat-extract.mjs`
(who acted, where, what was asked, what was given), stripped of proper nouns and replaced with
`«person»`, `«place»`. **B** = the same extraction of `MW/OPEN` rows 3–12, identically stripped
and length-matched. Assignment by recorded coin flip.

**The discriminating question, written before looking:** *"In which of these two openings is the
player's identity produced by something a character in the world is doing for a reason of their
own?"*

If the judge picks ours, CRITIC-DOCTRINE §2.5 applies in full.

## Scoring

Native scale: **0–100**, weighted, plus hard fails that cap the item at **2** regardless.

| Block | Weight | Checks |
|---|---|---|
| **Diegesis** — creation is a scene, not a screen | **35** | M5 (15), M6 (8), M7 (7), M8 (5) |
| **Flow** — nothing between the click and the body | 25 | M1 (10), M2 (5), M3 (5), M12 (5) |
| **Restraint** — the game explains nothing it should not | 20 | M9 (10), M10 (4), M11 (3), M15 (3) |
| **Reach** — everyone can actually do it | 12 | M13 (10), M14 (2) |
| **Naive corroboration** | 8 | M16 (4), M18 (2), M19 (2) |

| Native | Band | Ladder ceiling |
|---|---|---|
| ≥ 90 | Meets the bar | 8 |
| 70–89 | Below bar — named remedy required | 6 |
| 50–69 | Recognisably attempting it | 5 |
| < 50 | **We lose** | 4 |

**Hard fails (any one caps the item at 2 and sets `status: FAIL`):**

- **HF1** — Any character-defining input taken on a screen with no world rendered behind it.
  *This is the failure the item exists to prevent.*
- **HF2** — ≥ 4 surfaces between load and control, or a `Play` button that leads to a menu.
- **HF3** — Any tutorial pop-up, objective banner, or "quest added" toast (M9).
- **HF4** — Any marker, arrow, compass or minimap (M11) — also an AR-2 fail.
- **HF5** — The journey cannot be completed on gamepad only or on touch only (M13).
- **HF6** — The naive agent's answer to "what were you asked, and by whom" names a menu (M16).
- **HF7** — No questionnaire route to the profession (M7).
- **HF8** — A defect in the confusion census that blocked progress (M19).

**Ladder mapping (per `SCORING.md` §1.2 / BAR-CRITIQUE W7):** native 50 → ladder **4**;
native 75 → ladder **6**; native 92 → ladder **8**.

## How we lose

Written pessimistically, in advance, so a critic can tick them off.

1. **The title screen with a Play button.** Load → logo → "PLAY" → a second menu → "NEW GAME" →
   a loading bar → a character creator on a black background → *then* the game. Five surfaces,
   two of which exist because a framework generated them. This is the single most likely
   outcome and it is a hard fail.
2. **Character creation is a React component.** Sliders, a rotating model on a gradient, tabs
   labelled Race / Class / Appearance, a "Randomise" button, and a big "BEGIN" at the bottom
   right. Every individual piece is competent. The whole thing is the opposite of the bar.
3. **The bureaucrat is a skin on the menu.** An NPC portrait is drawn next to the same sliders,
   the world is a blurred backdrop image rather than the live scene, and the questions are
   *"Choose your class:"* with a dropdown. Diegesis is claimed and not delivered. M5's frame-area
   and live-entity checks exist specifically to catch this.
4. **The questionnaire is cut for time.** It is the most expensive of the three class routes to
   write (ten dilemmas, each with three answers, each mapping to a profession weighting) and the
   easiest to defer to "wave 3". It never gets written, and the opening becomes a dropdown.
5. **Control arrives after definition.** The build does creation first because that is the order
   the data needs, so the player's first thirty seconds are a form. Morrowind's actual trick —
   *you walk around as an undefined person* — is the cheapest and best part of the opening and
   will be the first thing lost to load-order convenience.
6. **A tutorial appears because the controls are not discoverable.** `RI-JRN03` fails, so
   somebody adds a control overlay to the opening to compensate, and O11 dies to fix a different
   item's gap. **Cross-rule: an instruction pop-up may never be added as the remedy for an
   `RI-JRN03` or `RI-JRN04` gap.**
7. **"Quest added: Deliver the package."** A toast, because a toast is what every engine ships
   with and because it feels helpful. AR-2 fail, and it costs nothing to not do.
8. **The opening is built for mouse.** Creation uses a cursor, hover states and drag; it works
   beautifully on a desktop and is unusable on a phone with a GameSir clamped to it. M13 catches
   it; the fix is expensive if it is found late, which is why parity is a hard fail here rather
   than an observation.
9. **An unskippable studio logo.** Two seconds, once, forever, on every reload the critic does.
   It is also the thing the player sees forty times during their first hour of dying.
10. **The stamped document is a flag, not an object.** It exists in the save file, is never
    rendered, cannot be read, and no NPC mentions it. O10 is satisfied on paper and delivers
    nothing.
11. **The first meaningful choice is not in the first settlement.** The opening is beautiful and
    then the player walks for twelve minutes to reach anything with two outcomes. `T_choice` is
    met by a technicality and the opening does not close.
12. **Audio unlock becomes a surface.** A "Click to enable sound" panel appears because autoplay
    policy required a gesture and nobody folded it into the New Game click. One more screen.
13. **Everything works on the second run and nobody tested the first.** Cold cache, cold IndexedDB,
    no save present, first-ever pointer lock request. The critic must always measure with a fresh
    browser profile — recorded in the artifact or the measurement is void.

## Provenance note

- **`canonical-recall`, confidence medium** — everything in **§A** (`MW/OPEN`) and **§B**
  (`DS/OPEN`). These are recalled sequences from Morrowind (2002) and Dark Souls (2011).
  The *order* of beats, the existence of the ten-question class route, the three class routes,
  the census-office framing, the Jiub name prompt, the Sellus Gravius package, the Asylum floor
  messages and the pre-Asylum class menu are recalled with high confidence. **Every timestamp in
  §A and §B is an estimate and is not evidence of anything.** No check in this item compares our
  timings to theirs; §C's numbers are ours.
- **`constructed`, confidence high, and binding** — every row of **§C** (`ES/OPEN`), every
  threshold in the comparison method, and the weights in `## Scoring`. We defined them because no
  upstream number exists in a form we can measure, and a constructed bar we can measure beats a
  recalled one we cannot (CORPUS-CONTRACT §3).
- **Owned elsewhere, cited not restated:** `T_control` / TTFP → `RI-PLT03` P1. `T-choice`,
  `T-found`, `T-death`, the minute-45 prophecy rule → `RI-EXP01`. The marker sweep → `RI-WLD06`
  M31. Journal voice and the first entry → `RI-DLG05`. Origin/birthsign table content →
  `RI-CHR01` (proposed by BAR-CRITIQUE-01 rank 10; if it does not exist, O7 still binds on
  *delivery* and the *content* is a corpus hole to be reported, not invented here).
- **Harness dependency.** Checks M1–M15 require amendment **`A-JRN1`** (real-input mode,
  `first_input`/`first_control` trace events, UI-text stream capture) and M13 requires
  **`A-JRN2`** (gamepad shim) and **`A-JRN4`** (viewport/orientation control). Until those land,
  this item is **unmeasurable** and scores **0**, fail-closed. The full request is in the reply
  that accompanied this item's creation and in `JOURNEY-CRITIC-FLEET.md` §7.
