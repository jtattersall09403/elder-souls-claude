# GAP LEDGER

> **GENERATED FILE — DO NOT HAND-EDIT.** Regenerate with `node tools/gap-ledger.mjs`.
> Canonical data: `corpus/90-verdicts/GAP-LEDGER.json`. Source: every verdict's
> `biggest_gap` (opens) and `gap_closure[]` (closes). Rules: `SCORING.md` §5.

Generated: 2026-08-15T08:45:48Z · verdicts read: 88 · waves: 1

**The three rules that matter**
1. Every unsatisfied verdict opens exactly one gap. An evidence-earned PASS may open none.
2. A gap is closed only by a **later wave's critic**, re-measuring the gap's own
   `acceptance` condition — **never** by the agent that built the fix.
3. Every open gap on a subsystem path is handed to that path's next builder **up
   front**, as required work (`BUILDER-PROMPT-TEMPLATE.md`).

| total | open | partially-closed | closed | superseded | invalid |
|---:|---:|---:|---:|---:|---:|
| 83 | 82 | 0 | 0 | 0 | 0 |

## Open gaps — required work for the next wave

| Gap | Subsystem path | Sev | Age (waves) | Opened | Filed since | What | Remedy → acceptance |
|---|---|---|---:|---|---|---|---|
| `AUDB2-F1` | `audio.ambience.events` | major | 0 | w1 / W1-22-B2-blind | — | The regions are distinguishable but inert. Zero discrete sound events across 21 minutes 20 seconds of ambience in 64 recordings; crest factor never exceeds 17.4 dB anywhere. | — → **—** |
| `GAP-FCT-03` | `quests.faction.measurement` | blocking | 0 | w1 / W1-FACTIONS | — | The round's headline measurements were taken in a world with nobody in it and no longer reproduce on the build that ships. machine.js:472 added a giver-presence term to open() — `giver absent: <npc> is not in this world`, gate 'giver_presence', with presenceMode defaulting to 'on' at machine.js:93 — after the round-2 numbers were taken. Neither faction-probe.mjs nor faction-seam-probe.mjs calls questPresenceGate, and both boot a bare world. Re-running the builder's own tool with its own command line takes the Wet Ledger from the 16/16 recorded on disk to 8/16: P3 7/18 quests offerable (was 18/18), P4 top derived rank 4 (was 7), P13 the seat is NOT vacated by play, P14 rank-7-and-non-violent FAIL, P15 17 questOpen calls refused, and P16 — the round's strongest claim — flips from 'the walk wrote no reputation at all; 398 was paid entirely by the quests' own consequences' to 'the walk WROTE reputation 10, 22, 36, 52, 70, 90, 112'. The cascade is causal: because open() refuses, faction-probe's grantRep() fallback fires to satisfy rank terms by hand, so the no-reputation-writes property was never independent of the gate. | — → **—** |
| `GAP-PACK-01` | `process.critic.discipline` | high | 0 | w1 / w1-prose-tics-r4-blind-pack-judged-and-voided | `judge-prose-packs-r1` (BELOW BAR), `crit-w1-30c-r1-2634eb0e` (FAIL) | The blind pack cannot separate quality from provenance, and each round's fix creates the next round's tell. r1 leaked setting nouns (17/17); r4 masked them and now leaks mask density (15/15). | — → **—** |
| `GAP-PROSE-01` | `dialogue.voice` | high | 0 | w1 / w1-prose-blind-in-world-prose-and-npc-voice | — | Our characters do not address the player. Questions at 0.2x the reference, exclamations at 0.6x, first person at 0.5x, and every line closes on an epigram instead of a want. The world is narrated AT the player by 300 speakers who share one wit and none of whom is curious about them. | — → **—** |
| `GAP-W1-02-m68-unanswerable` | `world.region.transition` | blocking | 0 | w1 / W1-02-r1 | — | The RI-WLD12 M68 blind pack cannot answer the question it exists to ask, because the camera is standing inside the scenery. Of 24 frames shipped, 16 have a foreground boulder within a few metres of the lens and 5 are unreadable; I answered 'two places' on 52.6% of usable frames against an 80% bar, and the >=25%-'one' fail clause fired at 47.4%. Stratified by whether the lens was blocked, the two-rate is 75.0% on the 8 unoccluded frames and 36.4% on the 11 usable occluded ones. The pose is computed in node at the crossover coordinate with no occlusion test, and the province scatters its props in the renderer from noise, so nothing on the node side knows what is in front of the lens. The pack is also half-built — the builder's process died at 24 of 48 frames, so KEY.json and QUESTION.md were never written — and the frames are 1280x720 where M68 specifies 1920x1080. | Add an occlusion test to tools/world/wld12-blind-pack.mjs: before accepting a pose, cast from the eye toward the intended subject and reject or re-place any pose with geometry inside the first 4 m, reusing the --candidates=N sweep threshold-shots.mjs already has. Shoot at 1920x1080 as M68 specifies, complete all 48 frames, write KEY.json into a sibling wld12-blind.reveal/ directory rather than inside the pack, and remove the scoring rule from QUESTION.md so the judge is not told which cue loses. → **48 frames at 1920x1080, no more than 4 of them with geometry inside 4 m of the lens; KEY.json outside the pack directory; QUESTION.md silent on scoring; and a fresh judge who did not build the pack answers >=80% 'two' with >=70% naming an object or a landform. Re-running the same judgement on the CURRENT 24 frames must still reproduce 52.6%, so the change is demonstrably the camera and not the scoring.** |
| `GAP-W1-03-water-surface-undrawn-and-frozen` | `world.water.marsh` | blocking | 0 | w1 / W1-03-r1 | — | The waterline drawn on the player's body is live and correct to about 0.07 m — I measured it climbing calf to thigh to waist across a 1.178 m tide swing at one fixed point and camera, 21,804 changed pixels every one of which is on the character. The water surface it is a line OF is not. (a) game/src/world/province.js:747-773 builds each tile's water mesh on a 12.5 m lattice and drops any quad with a single dry corner, so on the paddy-and-channel tile both rounds photographed it keeps 32.1% of quads over ground that is 61.3% wet — and the shot point's own quad, corners [1.650, 1.600, null, null], is dropped: no water is drawn on the square metre the player stands in, in 0.886 m of it. (b) Where water IS drawn the mesh is sampled at tile-build time and nothing rebuilds it when the tide moves: moving the tide changes 21,804 pixels and not one of them is ground, while forcing a rebuild at the same tide changes 657,561. (c) The 20 s drying window §10.3 requires does not exist at all — a body dry throughout and the same body one second out of 1.05 m of water are BYTE-IDENTICAL, md5 8531d20d4f3dc7f82b8b6be2b9c63a77, because esBand is a world-space cutoff that the body rises above the moment it steps up a bank. | Three changes, all local. (1) province.js:756 — stop dropping a water quad because one corner is dry: emit the quad whenever ANY corner is wet, clamping each dry corner's y down to the local ground height, so a channel narrower than the 12.5 m lattice still gets a surface that meets the bank. (2) Mark province tiles water-dirty when field.tidePhase crosses a threshold and rewrite only the water sub-mesh's vertex Y from waterSurfaceAt() — the per-region geometry already exists per tile, so this is a position-attribute update, not a tile rebuild. (3) renderer.js _playerWaterline() — while drying, carry uWaterY relative to the body: hold the offset between the surface and the player's foot height at the moment the band left W0 rather than a frozen world Y, so stepping up a bank keeps the wet band on the legs and it fades over the declared window. → **(1) A capture at (2226.9, 4376.1) shows a drawn water surface meeting the body at the height the shader shades to. (2) probe-water-quad-coverage.mjs reports kept-quad fraction >= 0.90 x wet-area fraction on tiles (2100,4200) and (2400,4200), against today's 32.1%/61.3% and 62.0%/85.0%. (3) The LOW->HIGH pair at (2620,4362) changes ground pixels as well as body pixels with NO forced rebuild in ops. (4) The matched drying pair stops being byte-identical, and a capture 19 s after leaving water still shows wet-shading while one at 21 s does not.** |
| `GAP-W1-12-approach-cannot-close-on-a-walking-player` | `combat.enemy.movement` | blocking | 0 | w1 / W1-12-r1 | `crit-w1-12-r2-3c19` (FAIL) | RUSH is gated on a distance band (dist > 5.0*omega) rather than on whether the distance is actually closing, and APPROACH advances at archetype.*.walk_mps, which for INFANTRY is 2.20 m/s against a player walk of 2.00 m/s. An enemy 10 m from a player walking away in a straight line closes at 0.20 m/s, NEVER enters RUSH in 1,800 frames, never gets nearer than 8.14 m, reaches its 32 m hard leash, oscillates REPOSITION<->LEASH_RETURN for 235 frames at the boundary and walks home. Five of the seven fighting statblocks never enter RUSH against a walking player. The enemy does WORSE against a walking player than a jogging one, because walking keeps the gap just under the band boundary that would trigger a sprint. | In game/src/combat/ai.js case 'APPROACH', add a closure test alongside the band test: track dist over a rolling window and enter RUSH regardless of band when closure stays below a declared threshold for a declared number of frames; leave RUSH when closure recovers or the band drops to DANCE. Put both numbers in game/data/combat/ai.json under `movement` as rush_when_closure_below_mps and rush_closure_window_f, and give the file a document-level "unit": "f@60" so the frame figure carries its unit (S22). → **node tools/combat/critic-w1-12-census.mjs --mode=flee shows entered_RUSH true and min_gap_m <= 0.85*omega for EVERY statblock in the fighting roster at player speeds 2.0 and 3.2 m/s, while the 5.0 m/s row still shows the enemy losing ground (an enemy you cannot outrun at a sprint is a different game). Both new leaves appear in --mode=census's consumed list, not its inert list.** |
| `GAP-W1-12-m3-chase-bot-dwell-on-the-items-own-fixture` | `combat.enemy.statemachine` | blocking | 0 | w1 / W1-12-r2 | — | On RI-AI01 M3's own fixture — one enemy aggroed, the player holding position and never attacking for 60 s — min_dist_dwell is 0.3183 to 0.4373 across the fighting roster against a PASS of 0.10 and a chase-bot HARD FAIL of 0.35 that six of the seven statblocks cross. The round published 0.2521 from the round-1 critic's moving fixture and scored M3 = 1/2. The cause is measured: on a passive player the enemy is in COMMIT or RECOVER for 1,896 of 3,600 frames, every attack carries 0.6-1.2 m of forward root motion, and the AI cannot steer during the swing, so it ends each attack inside its own strike band and stays there for the whole recovery (85% of recovery frames inside 0.85*omega). The number rises with duration at every start range: 0.3807 / 0.4373 / 0.4399 at 1800 / 3600 / 5400 frames from 6 m. | In game/src/combat/ai.js, re-space during RECOVER rather than only after it. REPOSITION already runs a sprint back-off toward circle.preferred_band_multiple; RECOVER currently only renames the leaf. The root motion is the body's and must NOT be cancelled — the commitment property (47/47 swings to full clip, zero frames in which the enemy can change its mind) is the best thing in this subsystem and must survive untouched — but the frames after hit_active ends are steerable in principle and are 85%-inside-band in practice. Declare the back-off's start frame as a leaf of game/data/combat/ai.json so the consumption census can see it. Separately, and outside this piece's files, RI-AI01 M3's denominator should be changed from 'while state != COMMIT' to 'while the enemy is in the startup or active frames of an attack', as the round-1 critic proposed in its §14.3 and this round endorsed; until that moves, the item as written is the bar. → **node tools/combat/critic-w1-12-r2-m3-chart.mjs shows min_dist_dwell <= 0.35 for EVERY fighting statblock on the item's own fixture at 1800, 3600 and 5400 frames and at start ranges 3, 6, 10 and 15 m — AND node tools/combat/w1-12-r2-teardown.mjs shows flee_frames_inside_strike no lower than round 2's base row for any statblock (inf_trash 294, guard_legion 214, drowned_lesser 320, drowned_greater 107, champion_hist_marked 752, cst_sap_speaker 13 from 12 m). The second half is not optional: M3 can be passed by making the enemy timid, and that would undo the one thing round 2 got right.** |
| `GAP-W1-13-clock-consequences-never-fire` | `world.time.daynight` | major | 0 | w1 / w1-13-r3 | `crit-w1-13-r4-clock-and-ar1` (FAIL) | RI-PRG04's own Comparison method #8, 'Clock consequence', has never been run. It is four assertions and it is written out in the item: "Rest at in-game 17:00. ASSERT the clock reads 23:00, the night roster is active, merchants are closed, and any active timed quest has consumed 6 hours. Rest 4x in a row: ASSERT the clock advances 24 h and returns to the same shop state." Across three rounds the aggregation has measured exactly the first clause — rest moves the clock 12.005093 -> 18.005093, exactly 6 h — and none of the other five. No round has checked a roster, a merchant, a schedule, a quest deadline or the four-rest round trip. That axis is scored 'Clock cost', its 10 rung is 'full consequence chain fires' and its 6 rung is 'clock advances AND ROSTER SWAPS', so on evidence it cannot rise above 6 and it is the min over the item's eight axes. This round made the axis matter more, not less: it shipped a new clock model (Environment.step holds the clock while a death is in flight, RI-PRG04 §6 rule 4) whose own CONSUMPTION arm reports T5_consumer_moved: false — reports/runs/W1-13-R3/clock.json names sim/settlement.js#isOpen and sim/npc.js#slotAt in its header, then reads getEnvironment().phase (which returns 'night' on both sides of its comparison) and getWorldStats().settlement (null, because that report has no such key), and ships the false. Under ARBITRATION.md §3 a model with no demonstrated consumer is unmeasurable => 0. | Run RI-PRG04 Comparison method #8 as an instrument inside tools/journey/jrn06-death.mjs, verbatim: rest at 17:00, then read four world-side consumers back with the rest removed as the control arm — souls paid for one ordinary kill (sim/souls.js awardFor, the night roster's 1.35x), a merchant's open/closed state (sim/settlement.js isOpen), a named NPC's scheduled location (sim/npc.js slotAt), and an active timed quest's remaining window. Then rest 4x and assert the clock returns to the same shop state. Then run the same instrument with game/src/sim/environment.js's `heldByDeath` branch monkeypatched back to the unconditional tick, which turns it into the CONSUMPTION demonstration reports/runs/W1-13-R3/clock.json is missing. Separately, re-run tools/harness/w1-13-r3-clock.mjs so its stored verdict matches the predicate now in the tree: T5_consumer_moved reads false in the file and evaluates true on the file's own numbers. → **One new aggregation row reports, in a single run: clock 17:00 -> 23:00 after the rest; souls_per_ordinary_kill 1.35x higher after the rest than before and 1.00x in the no-rest control arm; at least 1 merchant is_open true before and false after; at least 1 named NPC whose slotAt() location differs across the rest; and 4 consecutive rests advancing the clock 24 h and returning every one of those three readings to its starting value. Plus, in the death arm, 4 deaths at 20:58 moving the clock 0.000000 h with the fix in and >= 0.05 h with it out, with the same-kill souls award differing between the two arms. RI-PRG04's Clock cost axis then has evidence for every clause of its own 6 rung and for four of the six consequences its 10 rung names.** |
| `GAP-W1-13-roster-clause-has-no-control` | `world.time.daynight` | major | 0 | w1 / w1-13-r4 | — | The clause that sets this item's score is passing against a control that contains no people, and the round's own aggregation says so. RI-PRG04's Clock cost axis puts 'clock advances AND ROSTER SWAPS' at its 6 rung, and the round-3 verdict's acceptance named the roster half explicitly: '>= 1 named NPC whose slotAt() location differs across the rest, WITH THE REST REMOVED AS THE CONTROL ARM'. In reports/runs/W1-13-R4/clock-consequences.json — and identically in -a.json — the rested arm's roster has 52 entries, the no-rest control's has 0, and the four-rest arm's has 0. 'control_npcs_moved: 0' is an empty town, not a control, and the probe's own control_note says such a row is void. All three arms record well: 'hearth-archon' while the 52 people are Lilmoth's residents, so the population the clause passes on is not the population of the town the arm is standing in. c6's roster_changed: 0 is vacuous for the same reason. And in reports/journeys/w1-13-r4-jrn06/journey.json at 1110bf7 with page_errors: [], m_prg04_m8_clock_consequence is the ONE failing row of 43 and it fails on c2_night_roster_active: false with roster: {} in all three arms. | Make the population an asserted precondition of the arm rather than a hope. stepSettlement installs a town only on a CROSSING (settlement.js:154, `if (want !== sim.env.settlement)` then `sim.populate(want)`), and game/data/states/default.json names no settlement and no npcs, so an arm that starts inside the radius never crosses and an arm that reloads mid-sequence may not re-cross. Either (a) give the method-8 arm a named state that declares env.settlement so engine.js:950-956 populates on load and the crossing is not depended on, or (b) teleport OUTSIDE the settlement radius, step, then teleport in — and in both cases assert roster_n > 0 before reading anything, so an empty town turns the row red instead of green. → **In one `node tools/journey/journey-run.mjs --journey jrn06-death` run: m_prg04_m8_clock_consequence.rested.before.roster_n >= 20 AND control.before.roster_n >= 20 AND four_rests.before.roster_n >= 20, every arm's roster drawn from the settlement named in `well`; rested.npcs_moved_n >= 1; control.npcs_moved_n === 0 WITH PEOPLE IN IT; four_rests.npcs_moved_n === 0 WITH PEOPLE IN IT; and the row's own pass: true. Plus a guard inside the instrument that fails the row when any arm's roster is empty, so this cannot recur silently.** |
| `GAP-W1-16-a-save-and-a-reload-change-your-roll-class` | `progression.equipment.encumbrance` | blocking | 0 | w1 / W1-16-r3 | — | Engine._restoreFightFromSave() clears _equipLoadPinned, and game/src/save/fight.js persists fight.loadout.equip_load_pct which nothing ever reads back. A scenario that DECLARES its equip load therefore keeps it across a loadState() and loses it across a save. Round 3's new hands term (+9.5 kg for the default loadout) makes the recomputed answer land on the other side of the 30% cliff. Measured live, hostiles on the floor, nothing despawned: arena_duel with a shell-scale-hauberk and legion-greaves on reads 24.000000% / LIGHT / 26 i-frames / 22 f@60 recovery / 52 f@60 total before saveRoundTrip() and 33.424658% / MEDIUM / 22 i-frames / 34 recovery / 60 total after it. arena_flat gives the identical pair. Bare, arena_duel goes 24.000000% -> 13.013699%, same tier but still not the saved number. The counterfactual is round 3's OWN delete-the-fix switch: with __breakW116('hands') the same save and the same reload land at 20.410959%, LIGHT, 26 i-frames, so the hands term is exactly what crosses the cliff. | 1. game/src/save/fight.js — persist the PIN and not only its value: write `equip_load_pinned: !!engine._equipLoadPinned` beside the existing `equip_load_pct`. 2. Engine._restoreFightFromSave() — restore both, and seed _equipLoadBase from the restored equipLoadPct the same way _buildCombat seeds it at engine.js:1104, so the first post-load engagement is a delta and not an assignment. A save that carries a pin comes back pinned; a save that does not comes back deriving, which is the behaviour round 2 verified and correctly praised. 3. Extend round 3's own D4 probe (tools/harness/w1-16-r3-live.mjs --probe states) to SAVE AND RELOAD each state and re-read the tier, not only load it — the check was written for exactly this question and cannot reach it. 4. Separately and smaller: give the equip-load spell effect its own offset field in sim/magic/apply.js instead of mutating combat.player.equipLoadPct, which removes the Math.max(0, ...) clamp asymmetry (Feather at 13.013699% currently leaves the caster at 42.833333% MEDIUM when it expires) and removes the need for the delta-origin state at the same time. → **node tools/harness/critic-w1-16-r3-live.mjs --probe pinfight reports states_whose_ROLL_TIER_changes_across_a_save_and_reload: [] and equip_load_moved_across_the_save: false on every pinned row — with the __breakW116('hands') counterfactual row still present and still unchanged, so the arm has not been made vacuous by removing the thing it compares against. node tools/harness/critic-w1-16-r3-offline.mjs stays at six PASS and --selftest still fails all six. node tools/harness/critic-w1-16-r3-live.mjs --probe antimerge still reports pack_left_the_roll_byte_identical: true, so the anti-merge has not been traded for the pin.** |
| `GAP-W1-16-equip-load-cannot-see-the-thing-in-your-hands` | `progression.equipment.encumbrance` | blocking | 0 | w1 / W1-16-r2 | `crit-w1-16-r3-3c19` (FAIL) | Engine._recomputeEquipLoad() implements RI-PRG07 §2's formula minus its first two terms. §2 reads `equipRatio = (weight of equipped weapons, shields, armour, talismans) / maxLoad`; the producer iterates sim.inventory and sums the carried.json weight of rows carrying a `slot`. The weapon the fight swings is combat.player.weaponId, set by CombatSystem.setLoadout() from loadout.weapon, and is not an inventory row; neither is the shield. Measured live on one fixture: a character holding ssw_garrison_sword and marsh_oak_medium reads 0.000000%; equipping a bog-iron-maul through the real equip queue moves the ratio to 15.753425% AND LEAVES combat.player.weapon AT ssw_garrison_sword; setLoadout({weapon:'ultra-greatsword', shield:'greatshield_xanmeer'}) then leaves the ratio at 15.753425%, unchanged to six decimal places. There are two right hands — one that is weighed and one that fights — which is RULES 10's `two parallel implementations of one system`, the same shape this round found for sprint and walked past for the weapon. The weight is not missing from the tree: game/data/weapons/classes.json ships an `equip_weight` column on all fifteen classes (Dagger 1 ... Ultra greatsword 20), combat/moveset.js:187 copies it onto the move table, and nothing in game/src ever reads it. The shields block carries guard angle and parry windows and no weight column at all, while RI-PRG07 §4 declares buckler 4 / kite 9 / greatshield 18. | 1. In Engine._recomputeEquipLoad(), add this.combat.player.moves.equip_weight (already carried by combat/moveset.js:187) and the shield's weight into `w`, beside the inventory `slot` sum. Do not touch the divisor — _equipCapacity() is correct and the two-name split must survive. 2. Add a `weight` field to every row of game/data/weapons/classes.json's `shields` block from RI-PRG07 §4: buckler 4, small 4, medium 9, greatshield 18, parry_tool 1. 3. Make Engine._finishEquipCommit(), on a row whose slot resolves to 'right' or 'left', call setLoadout({weapon}) / setLoadout({shield}) so the hand that is weighed and the hand that fights are one object — this is the RULES 10 half and it is what stops the defect returning. 4. Add equip_load.weapon_weight, equip_load.shield_weight and equip_load.overloaded_denies_sprint to getBurden()'s consumer table, and implement the third (RI-CMB01 §B) at the same action gate sim/player.js already uses for worldDeny.sprint. 5. Seed _equipLoadBase to the computed base on the frame the producer first engages, BEFORE applying any delta, so a live Feather (magnitude 30) or Burden (magnitude 40) offset is not destroyed by the first equip and then subtracted a second time by its own _undo. → **node tools/harness/critic-w1-16-live.mjs --probe parallel,oversprint reports inventory_equip_changed_the_weapon_the_fight_swings: true, swapping_the_fight_weapon_changed_the_ratio: true, an_OVERLOADED_player_still_sprints: false, and coupled: true on both probes. node tools/harness/critic-w1-16-offline.mjs passes C5, C6 and C7 with exit 0 — C7 meaning a legal character can dress into HEAVY from shipped data with no item weight invented. node tools/harness/prg-encumbrance.mjs --probe separation still reports coupled: true, so the fix has not merged the two ratios.** |
| `GAP-W1-17-an-archetype-with-no-mouth` | `dialogue.npc.identity` | blocking | 0 | w1 / w1-17 | `crit-w1-first-ten-minutes-r1-a8e4c93` (FAIL) | Forty infos are written for `a` values no NPC record carries, including all 23 `magister` lines — an entire RI-DLG06 §B archetype (row 2, the Telvanni-analogue), whose register voice-metrics.mjs measures and whose bands speakers.json sets. infoFor() matches info.a === npc.actor exactly, so none of it reaches a player. A further 90 infos are gated to an (actor, town) pair no such actor stands in. Total: 133 of 1,280 infos (10.4%) that no character in a 5,400-strong player space can hear from any of 347 speakers, against a reported 3. The nine near-misses are the worst: notary-sedda-vell exists and is tagged actor 'archivist'; harbourmistress-tesh exists and is tagged 'clerk'; eleven scribes exist and are tagged 'clerk'. A topic `magister-varo-dren` answers 'I am he' and Varo Dren is not in the roster at all. | (1) Add an NPC record for the House Dres magister the corpus already names, or an actor_aliases table mapping magister->dres-factor, notary->archivist, scribe/harbourmistress->clerk; nine of the forty are people who already exist under a different actor tag. (2) Add a CHECK, not a constructor assert (RULES 14): every info.a must appear in game/data/npcs/**, and every (a, cell) pair must have at least one speaker; exit non-zero. (3) Promote tools/dialogue/critic-reach.mjs or an equivalent to the gate in place of the superset lint, because the superset lint tests a rule the engine does not implement. (4) DECIDE FIRST, because everything above depends on it: either implement first-match-wins in infoFor() as RI-DLG01 §A requires, or amend RI-DLG01 §A to specify the scoring rule and rewrite the §D step-4 lint to match. → **node tools/dialogue/critic-reach.mjs reports <= 3 unreachable infos with the 'no such actor in the roster' bucket at 0, and node tools/dialogue/voice-metrics.mjs reports a non-zero entry count for all six archetypes from speakers who exist in game/data/npcs/**.** |
| `GAP-W1-22-event-level-graded-on-a-median` | `audio.ambience.region` | major | 0 | w1 / w1-22-r2 | — | RI-AUD03 §A's 'Level (rel. bed)' band is a claim about EVENTS, and every mechanism that touches it is a per-layer median: gate O3 grades the median of a bed/layer's events, and the corrective trim `event_gain_db` is a single number per layer, while the second gain stage it corrects (`synth.gain_db`, -5..-13) varies PER EVENT. A per-layer trim cannot in principle unbury an event quieter than its layer-mates; it can only move the whole spread. The result is that the round-1 defect this piece exists to close -- declared in band, buried by a second gain stage -- is still present for individual events while the gate reports zero failures: 54 of 413 measured events sit outside §A's band with the tool's own +/-2 dB tolerance, 33 of them buried below the floor, in 11 of 33 bed/layer pairs whose median passes. | Gate O3 on EVERY measured event rather than on the layer median -- the check is already written and falsifiable at tools/audio/critic-w1-22-r2-bands.mjs B1, which goes red on the shipped tree, redder under --sabotage widen, and GREEN under --sabotage centre (every event collapsed onto its layer median), proving its reading is about the spread and not about the level. Then give the trim the same granularity as the stage it corrects: calibrate a per-EVENT gain rather than one `event_gain_db` per layer, since the quantity being corrected varies per event. Both halves are small and neither needs new engine code. → **Re-run `node tools/audio/critic-w1-22-r2-bands.mjs` at the fixing commit and require B1 to report 0 of N events outside RI-AUD03 SectionA's band, with `--sabotage widen` still red and `--sabotage centre` still green so the check is known to be live. Then re-run `node tools/analysis/ambience-onsets.mjs --seconds 180` and require O1, O2 AND O3 green together on the same render (rule 9), with O3 rewritten to grade every event rather than the layer median.** |
| `GAP-W1-23-both-registered-voices-on-slavery-are-colonists` | `lore.canon.registry` | major | 0 | w1 / w1-23-lore-registry-and-the-provinces-canon | `crit-w1-23-r4` (FAIL), `crit-w1-23-r3` (FAIL) | The piece diagnosed the province's worst authorial habit exactly right — 'a province that only ever says lease has adopted the Provincial Office's vocabulary as narration' — and prescribed exactly the right remedy: 'the euphemism must be somebody's POSITION and not the world's voice'. CF-D008 was then registered with two positions, and BOTH are voiced in dialogue by the coloniser. Position A is an Imperial clerk: 'No subject of the Empire may be held as property inside the Empire's law. Where it happens it happens outside that law, off foreign keels, and the Office has made representations. I've copied three of them myself.' Position B is a House Dres slave-factor: 'The House keeps a stock column and people are entered in it and taken out of it, and the law you are quoting changes the word on the paper and nothing else.' Both books voicing B are Dunmer and Imperial hands — a salt-factor's ledger and marginalia in a borrowed Imperial address. Of 347 people in the province, 227 are Argonian; 66 of those have any answer to the topic `slavery`; between them they give three distinct sentences, and the only one written for an Argonian mouth is untagged and is a refusal to speak: 'This one will not discuss it with you standing there, and this one is sorry.' Carrying no `cf`, it contributes nothing to the 27-of-27 census, nothing to the 43/43 perturbation, and nothing to the register. Meanwhile CF-D008/B's held_by names 'every soul who has been on the Blackrose roll' and its machine holders list `dres-factor, sapcutter, mudborn, outlaw` — of which only dres-factor has a line. The registry credits the enslaved with a position and the build gives them no sentence. And the ruling on this dispute, which the registry's own note calls 'the province's central moral fact', is one of the nine that recover from the shipped bytes in three guesses. | Three edits, all in data. (1) Tag the existing Saxhleel refusal in game/data/dialogue/topics/40-race-gated.json with `cf: CF-D008` and a third position C — a refusal to answer an Imperial question in front of an Imperial building is a claim about what is the case, held by the people it is the case about — and register position C with holders drawn from Argonian actors. (2) Write one cf-tagged CF-D008/B line for `mudborn`, `sapcutter` or `outlaw`, actors the registry ALREADY declares hold that position and which no line reaches. (3) Salt the truth_seal (`sha256(id + '\|' + ruling + '\|' + corpus-side salt)`) so CF-D008's ruling stops recovering in three guesses, and drop the >=24-character guard from both leak checks so the instrument can see the case it is currently blind to. → **node tools/lore/critic-w1-23-r1-voices.mjs reports at least one distinct answer to `slavery` that is cf-tagged AND written for an Argonian actor, in addition to the clerk and the dres-factor. For CF-D008, the set of actors that can be OFFERED a cf-tagged line is no longer a subset of {clerk, dres-factor}: at least one of mudborn, sapcutter or outlaw is in it. node tools/lore/critic-w1-23-r1.mjs reports 0 of 26 sealed rulings recoverable (currently 9), and its --self-test still passes 6/6. node tools/lore/canon-census.mjs and node tools/lore/canon-consumption.mjs still PASS, and the untagged-voice count from the critic probe falls from 20 toward 0.** |
| `GAP-W1-25-the-control-facility-has-an-optional-control` | `experience.permissiveness.register` | blocking | 0 | w1 / W1-25 | — | The sabotage facility fails when its arms agree, and whether it can SEE that they agree is delegated to `support` - a caller-supplied field that is optional, undefined by default, unvalidated, and whose two honest readings give opposite verdicts on the strongest real inert control on this tree. W1-14-r3's shipped `--break=nocast`, which a human critic ruled inert, comes back OK when support is 'effects examined' (55/55), VACUOUS when it is 'effects delivered' (55/0), and OK again when the caller omits support entirely. Four further holes in the same cascade: two arms whose value is `undefined` canonicalise to agreement and return INERT (a confident claim about a measurement that never happened - I hit this by accident on my first run, against a real artifact); MASKED is last in the cascade and is shadowed by any unmet margin or wrong direction; armsAgree is decided on intact-vs-all-broken alone, so a control with a demonstrably live factor is reported INERT under cancellation while the same record lists that factor as moving it; and the documented obligation on the `supportArms:'intact'` escape hatch (a caller 'must say why, in support_note') is enforced nowhere and support_note is not even copied onto the result, so the canonical W1-13 failure passes with a one-key spec change. | Four changes, all in tools/experience/lib/sabotage.mjs. (1) MAKE SUPPORT MANDATORY: runControl already throws on a spec with no factors ('a control with no factors is not a control'); throw the same way when any arm returns no finite support - a control that cannot say what it ranged over is not a control. Amend the docstring's 'the number of units the measurement ranged over' to 'units that REACHED THE COMPARATOR', which is the single word separating A1 from A2. (2) ADD `NO_MEASUREMENT`: when every arm's value canonicalises to 'null', say that instead of INERT - the same argument the file already makes for VACUOUS. (3) ADD `SHORT_CIRCUIT` for the fourth failure shape, fired when the broken arm's support collapses relative to the intact arm's; both numbers are already on every arm record. (4) FIX THE CASCADE: hoist `masked` above the margin and direction tests, make armsAgree read `distinct_values === 1` (computed one line above), and throw when supportArms:'intact' arrives with no support_note, carrying that note onto the result so a report can list every control that opted out. → **Re-run `node tools/experience/critic-w1-25.mjs --a --b` against the amended facility. A1 and A3 must become ERROR or NO_MEASUREMENT rather than OK; A4 must become NO_MEASUREMENT; B-COLLIDE-1 must become MASKED; B-COLLIDE-2 must stop being INERT; B-COLLIDE-4 must throw. `--self-test` 6/6, the three existing facility breaks and `--cases` 8/8 must all still pass, and a FOURTH facility break `--break=support-optional` must be added and shown going red. The acceptance harness is already on the tree and does not need writing.** |
| `GAP-W1-26-creation-cannot-be-completed-from-the-title` | `journey.firstlaunch.flow` | blocking | 0 | w1 / w1-26-r2 | `critic-w1-26-r3` (FAIL) | Engine._titleApply('new') — the row the title's `New` commits to — calls censusBegin({}) with no options. Census.spec.race is set by exactly one function, Census.observe(race), called from exactly one place: censusBegin, when opts.race is present. So on the player's path race is never observed, and Census.answer() at writ.race-observed throws 'race must be observed before the scene reaches the desk'. _censusApplyPending() catches it, emits census_refused and draws the reason on the surface, so the player is left in front of the Warden-Scribe reading an engine error string as dialogue, with no way forward and no way out of the room (the Writ House door is held until you are written down). | Observe the race of the body the player is actually in at the moment `New` begins the scene. RI-CHR01 §1 row 2 is explicit that race is observed and never asked, so the value must come from the body — which means the choice of that body is W1-07's and must exist before this can be done honestly. A hardcoded default would satisfy the acceptance test below and betray the item, so the acceptance names that too. Separately, replace journey-run.mjs's bare `catch { break; }` at the census walk with a recorded failure, so the driver can never again report an absence as N/A. → **node tools/journey/census-newgame.mjs exits 0 with player_can_finish_creation true; AND node tools/journey/opening-play.mjs reaches P10 with the census past writ.race-observed, driven only through real DOM input from the title, with zero census_refused events in the run; AND the race the character ends up with is one the player chose or the body already had, not a constant in the source.** |
| `GAP-W1-26-r3-body-race-fails-silently-and-is-refused-eleven-nodes-later` | `journey.firstlaunch.flow` | major | 0 | w1 / w1-26-r3 | — | Engine.bodyRace() ends `return rows.some((r) => r.id === id) ? id : null`. A body whose sim.identity.race is not an id in game/data/progression/races.json is observed as null, SILENTLY, at censusBegin — and Census.answer() throws round 2's exact sentence eleven nodes later, at writ.race-observed. On the player's path that throw is caught and rendered as the single authored refusal line, so the player reads 'She stops writing, and does not look up. — No. Not in that box, and not in those words.' in front of a door that is held shut, with no way forward. MEASURED: with sim.identity.race = 'altmer', bodyRace() returns null, titleActivate('new') succeeds, and the scene stops at writ.race-observed. | Move the guard from Census.answer() at writ.race-observed to Engine.censusBegin(), where the observation is made: if bodyRace() returns null, throw naming the id that was rejected and the file it is missing from. Add tools/check-body-race.mjs (rule 14: a check, not a constructor) reconciling creation.json starting_body, every race literal in game/src/sim/state.js and game/src/save/state.js, and every named state, against game/data/progression/races.json. → **(1) A body carrying a race not in races.json is refused at scene START, with the offending id in the message, and the check is watched going RED by planting a bad id. (2) With the assertion armed, all ten races in races.json still complete the scene end to end. (3) census-newgame.mjs still exits 0 and its no-body control still stops.** |
| `GAP-W1-30c-wetness-mask-blind-to-instancematrix` | `render.fidelity.materials` | major | 0 | w1 / w1-30c | `crit-w1-30de-r1-3c19` (FAIL), `crit-w1-visual-blind-protocol-a-chunk2` (FAIL), `crit-w1-orphaned-surface-shaders-r1-3c19` (PROVISIONAL) | installSurfaceShader() computes the world height its wetness mask rides on as `vEsSurfaceWorldY=(modelMatrix*vec4(transformed,1.0)).y`, inserted after `#include <begin_vertex>`. three.js applies `instanceMatrix` in `project_vertex`, which runs later — its own `worldpos_vertex` chunk is `worldPosition = instanceMatrix * worldPosition; worldPosition = modelMatrix * worldPosition;`. So every instance of an InstancedMesh is handed the base geometry's height and the whole mesh shares one position on the wetness ramp. Measured with a saturating band: two separate Meshes at y -1.2 and y +1.2 separate by 0.02393 in mean luminance (the high quad moving by precisely 0.00000, i.e. fully dry, as it must); two INSTANCES at the same two heights separate by 0.00150 — 6% of the control. Reproduced identically on SwiftShader (0.023926 / 0.001497) and on an RTX A4500 (0.023896 / 0.001511). The same line also omits `batchingMatrix` and is taken before `skinning_vertex`. | One narrow commit in game/src/render/visual-foundation.js plus the matching two rows of game/src/render/MATERIAL_API.md. PRESERVE: the whole authored library, the 20-family registry, FAMILY_SET, the detail-normal blend, the wear mask and its wearFrom axis, the magenta fallback, the palette swatches, and every existing option key and its meaning — the freeze is additive-only and nothing here removes or repurposes an option. (1) PRIMARY, the gap above: in installSurfaceShader's vertex injection, replace `vEsSurfaceWorldY=(modelMatrix*vec4(transformed,1.0)).y;` with three.js's own worldpos_vertex form — build a `vec4 esWorldPos = vec4(transformed,1.0);` then `#ifdef USE_BATCHING esWorldPos = batchingMatrix * esWorldPos; #endif` `#ifdef USE_INSTANCING esWorldPos = instanceMatrix * esWorldPos; #endif` `esWorldPos = modelMatrix * esWorldPos; vEsSurfaceWorldY = esWorldPos.y;` — and move the injection point from `#include <begin_vertex>` to after `#include <skinning_vertex>` so a skinned character's wetness rides its posed height rather than its bind pose. Bump the customProgramCacheKey version string. (2) COMPANION, same file, three lines: bind the trim atlas when `variant.trim` is set (trimAtlasTextures() already returns the maps and trimSlot() already returns the band) or, if binding it needs geometry decisions that belong to W1-30E, change MATERIAL_API.md section 3 to say `recorded` for `trim` exactly as it already does for `lod` and delete 'and it is live now' from reports/w1-30/C-DELIVERY.md — either is acceptable, silently declaring it live is not; pass `uDetailTiling: DETAIL_NORMAL_TILING` rather than `DETAIL_NORMAL_TILING/ts`, since vNormalMapUv already carries the base repeat of 2/ts and the published contract is a fixed 8x; and correct MATERIAL_API.md section 5's adjacency arithmetic to root 0.9 m against mud 3.5 m = 3.89x. (3) Do NOT touch world/province.js's diffuse floor, the styleboard clone route, or the 19 census bypasses in this commit — they are other children's files and other verdicts' business. Do not add a second tool: tools/critic/w1-30c/mech.mjs already exercises all of the above and already goes red on today's tree. → **Cheapest-first, all offline before any browser: (i) `node tools/boot-check.mjs` PASS and `node tools/render/w1-30-assets.mjs` GREEN — static, seconds. (ii) `node tools/critic/w1-30c/mech.mjs` goes from 4 failures to 0 with `controls ok`, which requires specifically: wetness-instancing separation >= 25% of the non-instanced control (today 6%), wetness-instancing-source finding instanceMatrix on the height line, detail-tiling-scale delivering exactly 8x at tilingScale 1, 2, 4 and 8, and either the trim axis moving pixels or the contract no longer claiming it does. (iii) The positive controls must still be green — the non-instanced arm still separating by ~0.024 with its high quad at precisely 0.00000, the palette axis still moving the frame, and the wear-ceiling control still byte-identical at wear 0; a run where the controls have gone quiet is a failed run, not a passed one. (iv) Delete-the-fix: revert only the vertex-line change on a copy and confirm the instanced separation returns to ~0.0015, so the repair is demonstrated to be what moved the number. (v) One hardware confirmation only if (ii) passes: `node tools/critic/w1-30c/mech.mjs --hardware` inside an existing pod run, since every delta in this round agreed across rasterisers and a second pod for this alone is not warranted. Publish the five figures. STOP AND ESCALATE if binding the trim atlas turns out to need a UV convention that only W1-30E can supply — in that case take the documentation arm of (2), say so, and leave the axis honestly marked `recorded`; do not invent a trim UV convention on E's behalf.** |
| `GAP-W1-act5-argument-entry-shadowed` | `dialogue.topics.filtering` | blocking | 0 | w1 / w1-17-act5-argument | `crit-w1-17-r1-3c19` (FAIL) | game/data/dialogue/topics/50-mainline.json and game/data/dialogue/topics/main-quest-argument.json both declare a topic record with id 'the-steward-of-the-count' -- Q-MAIN-26's own opens_by.topic and the entry line of the whole Act V climax. converse.js buildTopicIndex() merges same-id records across files by design; infoFor()'s specificity score (matchesActor 8 + requires 4 + dScore + forbids 0.5) lets the older file's ungated-race, d:20 third-person stub ('Ixtu-Meer. He has kept the Count...', score 9.2) outscore the newer file's race-specific, undated warmblood greeting (score 8.5) for any player whose race is outside {saxhleel,naga,dunmer} and whose disposition clears 20. | Resolve the 3-way id collision between main-quest-argument.json and 50-mainline.json (delete 50-mainline.json's now-superseded the-steward-of-the-count/the-sill/the-opening-of-the-count stubs, or rename this file's three quest-entry ids so no other file shares them and update hooks.json/NPC topics[] to match); give the entry info a d value at or above the stub's, or add a tie-break in converse.js infoFor() so an actor-matched race-gated info always outranks an actor-matched race-blind one regardless of d; add a check alongside build-graph.mjs's unreachable-INFO lint that, for a named hand-authored file, asserts the info actually delivered to every documented signature is one of that file's own infos rather than a merged stranger's; and either give keeper-ei-vashan and gallery-intake-keepers a post.site that is not stone-wastes-count, or move their two overlapping topic ids off this file's twelve. → **gen-converse-node-repro.mjs Part A shows 0 of 11 documented signatures receiving the 50-mainline.json stub on the-steward-of-the-count (currently 3 of 11), and Part B shows 0 of 2 sibling rootkeeper records able to deliver any of the twelve Act-V topic ids once each is given a spawn point (currently 2 of 2 can).** |
| `GAP-W1-attr-scale-ceilings-rest-on-an-unmeasured-souls-economy` | `progression.level.attributes` | major | 0 | w1 / w1-attr-scale-r1 | — | The 42 rescaled attribute and skill demands were fitted to a ceiling of creation + earned skill growth only, on the piece's stated premise that souls are never awarded and the bought attribute stream is therefore unfundable. Souls ARE awarded — 42 per infantry body, 10 real kills for 378 souls on HEAD with a red control that went red — so the true ceiling is higher and rises further as the enemy roster is populated (9 hand-placed enemies today against RI-PRG06 §7's planning figure of ~1,230). The ceilings themselves are read at run time from reports/faction-signature-sweep.json, which is gitignored and in no commit; it moved 29 minutes after the fix landed, and as a result the published headline '28 defects -> 0' re-measures as 23 -> 0. With the sweep's bought_points term folded in, the tool's RESERVE = 2 buys exactly ZERO margin against the earned-only ceiling for all ten attributes. | 1. Make reports/faction-signature-sweep.json a tracked artifact — drop the entry at reports/.gitignore:3 or move the file to game/data/progression/ — so every ceiling becomes a claim about a commit (rule 12). 2. Replace the sweep's bought_points derivation, currently a JSON census of game/data/combat/enemies x encounters.json, with a MEASURED figure taken from the running world by tools/quests/critic-attr-scale-souls.mjs arm 2: kill N bodies, read soulsHeld, run it through game/data/progression/levels.json. 3. Fix trial C of tools/quests/attr-scale-consumption.mjs — use `eid` not `id`, filter `kind === 'enemy'`, step the world before killing so SoulsSystem.step has SEEN the alive->dead transition, and make the predicate fail on a thrown kill rather than pass on one — then flip it under --after like the other seven trials. 4. Re-run attr-scale-audit --sheet p10 against the corrected sweep and rescale whatever the bands then say, in BOTH directions; the tool already reports `vacuous` for a demand everybody meets. → **tools/quests/critic-attr-scale-souls.mjs exits 0 (i.e. the piece's claim holds after the economy is re-derived) OR the sweep's bought_points equals the measured award divided through levels.json to within one point; AND tools/quests/critic-attr-scale-dtf.mjs reports a non-zero margin_at_hard_cap for every one of the ten attributes, where it currently reports 0 for all ten.** |
| `GAP-W1-audio-emitter-consumption` | `audio.ambience.region` | blocking | 0 | w1 / w1-22-r1 | `crit-w1-22-r2-s2` (FAIL) | All four R7 positional emitters are computed every frame into an observation surface and produce no sound on any reachable path. Perturbing bell_buoy.level_db by +40 dB leaves the live-path rendered peak BIT-IDENTICAL (0.4197283983230591 before and after) and the no-listener offline render bit-identical (peak 0.342586, LUFS-I -23.991 before and after), while the same +40 dB on L1 moves peak 0.342586 -> 1.000031 and LUFS -23.991 -> -5.336. buildGrain() has three call sites; the live emitter path is not one of them, and no caller anywhere passes `listener` to ambienceCapture/renderBedOffline, so opts.listener has been null in every render this project has taken. B6 scores 0/3 under RI-MTH07 (unmeasurable => 0). | In AmbienceDriver.step(), give the emitters their own LayerClock keyed on period_s and call buildGrain(this.ctx, {...e, level_db: e.level_db + bedTrimDb(bed)}, this.live.bus, this.rng, when, pl.pan, pl.gain) whenever this.ctx && this.live && pl.audible. _emit() an 'ambience_emitter' row carrying pan, gain and distance_m so that B6's stated instrument (audioLog.pan) exists at all. Pass `listener` through from Engine.ambienceCapture() so emitters appear in a measured clip. Bound period_s in tools/analysis/ambience-census.mjs: kiln declares none (silently defaulting to 20 s) and bell_buoy is 13 s, which is inside RI-AUD03's own named 'identity becomes irritation' failure. → **Perturbing any R7 emitter's level_db by +20 dB moves the LIVE-path rendered peak by more than 1 dB; ambienceLog() returns emitter rows whose pan is strictly monotonic along the 200 m transect (B6 as written, not the placement-arithmetic proxy); every declared emitter carries a period_s of at least 45 s.** |
| `GAP-W1-character-surface-is-assembled-from-primitives-not-built` | `render.art.character` | blocking | 0 | w1 / W1-F10-CHARACTERS | — | The visible body of every character in the game is an assembly of primitive solids rather than a built figure, and the numbers say so on both axes at once. Shoulders are glossy ellipsoids attached to a flat rectangular torso plate; the pelvis is a trapezoid; limbs are tapered cones; the player's legs terminate in points with no foot. The face carries 1 of 7 landmarks (a crest root, and nothing else — no brow ridge, orbit rim, naris, cheek-to-jaw line, mandible line or chin underside). The whole shipped roster is 5.87-6.84 heads tall (median 6.58) against RI-VIS10 C1's 7.0-8.0 band, with 0 of 41 figures inside it. The player is 15142 triangles against RI-VIS08 B2's 25000 and the creature 4834 against 12000. The skin opens 289 crack pixels across 179 of 640 posed frames at the calf/foot and head/hand seams. The art family that carries 329 of 408 NPCs has no eye geometry at all. Crucially this is NOT a rig defect: the same build passes RI-VIS08 C1, C3, C4, C6, C7, C8 and C9 with foot slide at 6 mm against a 30 mm bar, root-motion r = 1.0000, grip error 0.000 m and foot IK holding 100% of planted frames on a 20-degree slope and on stairs. | Rebuild the visible character surface on the existing skeleton — do not touch the rig, the clips or the IK, which measure well. In game/src/render/actor.js: (1) replace the ellipsoid-shoulder / slab-torso construction with a continuous shoulder-to-chest surface and tapered limbs, and give the player a foot; (2) add the facial landmark geometry — brow ridge, orbit rim, naris, cheek-to-jaw line, mandible line, chin underside — to both base.saxhleel and base.humanoid, and give base.humanoid the eye and pupil parts base.saxhleel already has; (3) change the per-family head scale in CHARACTER_SPECS so the head-count canon lands in band. → **node tools/visual/f10-silhouette.mjs --res=768 --pose=idle_loop reports >= 90% of shipped figures with R inside 7.0-8.0 (6.5-7.5 for naga and orsimer) and none outside 6.0-8.5; RI-VIS08 B2 reports >= 25000 triangles for the player and >= 12000 for the beast family; RI-VIS10 B4 counts >= 5 of 7 facial landmarks producing visible form shading under a 45-degree key; RI-VIS08 B8 passes on every art family, not only saxhleel; node tools/visual/actor-orbit-holes.mjs --res=512 --angles=8 --distances=4 reports 0 crack pixels.** |
| `GAP-W1-combat-exemplar-has-no-danger` | `combat.frames.timing` | blocking | 0 | w1 / W1-09 | `crit-w1-09-r2-3c19` (FAIL) | The regenerated RI-CMB07 exemplar is a 120-second, 7200-frame fight in which the player is never hit. Measured from the shipped statistics file and independently recomputed from the JSONL: swings taken 0 (band 0.05-0.35), damage taken / max HP 0 (band 0.4-1.6), player hp at fight end 620/620, frames at exactly 0 stamina 0 (band 20-600), mean stamina 0.8303 (band 0.55-0.80), 45.87% of frames above 90% stamina (band 0.12-0.45), whiff rate 0.72 (band 0.03-0.25), swings negated by i-frames 0.115 (band 0.4-0.8), longest committed run 155 f (band 180-520), punish-window usage rate null. Eleven of twenty-four banded rows are out of band against RI-CMB07 M2's floor of at most four, over one fight against a floor of five. Of the enemy's 26 swings, 11.5% were dodged and 7.7% blocked; the other 81% simply missed. | Rewrite the exemplar bot in tools/harness/cmb-exemplar.mjs so it fights inside the enemy's reach band instead of drifting out of it: hold 1.2-2.2 m from the target during ATK_WINDUP rather than retreating, commit to a punish inside every declared recovery window, and take the trade when stamina is short instead of walking away. Add an explicit assertion pass that refuses to write the exemplar unless it clears RI-CMB07 M2, and generate five seeded fights and take the row-wise median as the item requires. → **Over the row-wise median of five seeded fights: at least 20 of the 24 banded rows inside band, with rows 13 (swings taken) in 0.05-0.35, 14 (damage taken / max HP) in 0.4-1.6, 16 (frames at exactly 0 stamina) in 20-600, 17 in 0.03-0.20, 18 in 0.12-0.45, 19 in 0.55-0.80 and 25 in 180-520 all inside band individually, row 27 (punish-window usage rate) a number in 0.50-0.95 rather than null, and at least one GUARD_BREAK and one stagger present in the trace so RI-CMB03 M5 and RI-CMB05 M3 have something to measure.** |
| `GAP-W1-consumption-sweep-tripwire-is-a-name-grep` | `combat.camera.behaviour` | major | 0 | w1 / first-ten-minutes | — | tools/render/consumption-sweep.mjs tests whether a field NAME is MENTIONED anywhere under game/src/render/, not whether it is consumed. Deleting the call site this._applyCharacterFade(c) leaves 3 mentions and exit 0; gutting the method body to `return;` leaves 2 mentions, both inside its own doc comment, and exit 0. Only renaming every occurrence — the one deletion nobody performs by accident — reaches exit 1. The piece's own control for the tool is that single trivial arm. | Make the sweep a consumption test rather than a mention test, in tools/render/consumption-sweep.mjs only. Minimum viable change: strip block comments, line comments and string literals from each render/ source before matching, so prose can never satisfy the tripwire; then require the surviving match to sit on a call path reachable from Renderer.render(). Cheapest sufficient form of the second half, if the static call-path walk proves awkward, is a live arm: boot the game through tools/lib/browser.mjs, perturb the field on the sim object, render one shipping frame and require some observable to change — the machinery already exists in tools/harness/critic-first-ten-play.mjs phase `fade`. Do not add a second tool; extend this one. Preserve the existing VISUAL/INTERNAL/AMBER classification and the three hand-checked amber routes, which are correct and were checked by hand. → **Re-run the three delete-the-fix arms in corpus/90-verdicts/wave1/artifacts/first-ten-minutes/consumption-sweep-delete-the-fix.log on a sparse copy. Required: arm A exit 1, arm B exit 1, arm C exit 1, and the unmodified shipped tree exit 0. Three red arms and one green; today it is one red and three green. Publish the four exit codes.** |
| `GAP-W1-creation-is-an-api-not-a-scene` | `journey.chargen.diegesis` | blocking | 0 | w1 / w1-07 | `crit-w1-07-r2-3e7c` (FAIL), `crit-w1-07-r3-3c19` (FAIL), `crit-w1-26-r1-3c19` (FAIL), `crit-w1-26-r2-3c19` (FAIL), `critic-w1-26-r3` (FAIL) | The entire Writ House creation sequence renders nothing. Driving all 19 census nodes through window.__HARNESS and screenshotting after each at 1920x1080 produces 20 byte-identical PNGs (sha256 7277a273453ac8631c318f3d9120d7b12b7f562b187adabf59fe3ca7b5daaeb5) showing a body standing in an empty green field: no barge hold, no Writ House interior, no Jeeh-Ei, no Warden-Scribe Tuleeh-Ma, no question, no answer options, and document.body.innerText empty at every node. listEntities() returns 0 entities before and after creation. The scene exists only as a JSON state machine whose getCensusState() self-reports full_screen:false, world_visible:true and full_screen_panels:0 — three fields with no rendered counterpart. RI-CHR01's first bar is 'creation is a scene, not a screen'; what shipped is neither, and RI-JRN01's whole Diegesis block (35 of its 100 points) has nothing to measure. | Render the census scene over the live 3D view. (1) In game/data/states/writ-house.json, place the two NPC entities the scene already names — jeeh-ei in the barge hold and warden-scribe-tuleeh-ma behind the desk — as real entities with positions and the npcs/writ-house.json records attached, so listEntities() returns >= 2 throughout creation and the speaker named by getCensusState().speaker has a body in the frame. (2) Add a dialogue surface to game/src/render that draws getCensusState()'s `line`, `input.kind` and `input.options` as a bottom-anchored panel occupying <= 55% of frame area with the world rendering behind it, and drives censusAnswer() from real input (press/release on the closed action set, so keyboard, gamepad and touch all work). (3) Move the camera to the interior named in getCensusState().interior for the duration of the scene. (4) Render readWrit() as an openable inventory object rather than a string returned by an API. → **Re-run tools/harness/critic-w1-07a.mjs. Acceptance is met when: (a) the 19 creation-node screenshots yield >= 15 distinct sha256 hashes rather than 1; (b) listEntities() returns >= 1 NPC entity at every node and the entity id equals getCensusState().speaker at the node where that speaker talks; (c) the line returned by getCensusState() appears as rendered text in the frame at every node, with opaque non-world UI <= 55% of frame area and 0 frames whose background is uniform over >= 90% of the frame; (d) the whole sequence is completable on keyboard only and on gamepad only. RI-JRN01 M5/M6 then become measurable and the item can leave 0.** |
| `GAP-W1-crime-enforcement-is-a-free-win` | `crime.guard.response` | blocking | 0 | w1 / w1-15 | `crit-w1-15-r3-3c19` (FAIL), `crit-w1-15-r4-3c19` (FAIL) | The only guard in the build cannot attack and surrenders for nothing: guard_legion declares ai 'hold_ground' (combat/enemy.js only runs an action for ai === 'scripted', so an AGGRO guard held at 2.0 m for 900 frames deals 0 damage and never leaves REPOSITION) and its parley declares gold_price 0 against base_disposition 35, which makes combat/parley.js's GOLD ground unconditional — so one `interact` press yields a band-3 attack-on-sight guard at full HP for 0 gold and 0 stamina, leaving the imperial bounty at 3000 and jail null. | In game/data/combat/enemies/guard_legion.json set parley.gold_price to a live figure (the current imperial bounty, or a fixed value >= 200 g) and raise the GOLD ground's disposition requirement for guards to 40 so a cold surrender fails; in game/src/combat/player.js::_resolveParley route an accepted surrender against a guard into engine.arrestTopics()/answerArrest() so the three answers fire and the bounty is paid, served or refused rather than left standing; and give guard_legion an AI that swings — ai 'scripted' with a scenario file today, W1-12's behaviour when it lands. → **A probe that (a) sets imperial bounty to 3000, spawns guard_legion at 2.0 m and steps 900 frames records >= 1 HIT event against the player; (b) presses `interact` with 0 gold and records PARLEY_REFUSE, not YIELDED; (c) presses `interact` with gold >= the quoted price and records YIELDED followed by a bounty of 0 and a non-null jail ledger entry, or a serve/resist branch; and (d) shows getCrimeState().bounty.imperial changing across the interaction in every accepted case.** |
| `GAP-W1-deploy-instruments-point-at-the-tester-not-the-player` | `platform.load.ttfp` | major | 0 | w1 / w1-deploy-r2 | — | Four instruments now guard the one defect that has already been fixed (an untracked module plainly imported by `import … from`), while six deployment failure classes have zero instruments: a transient 5xx on the module graph, cache headers and a returning visitor, HTTP/2, the CDN's TLS as a browser sees it, a slow connection, and whether the Pages build ran at all. Two of those six — a warm cache on a second visit, and a slow connection — are precisely the conditions the owner's phone was in when it showed a black screen. Meanwhile the instruments that do exist fail in the direction of the tester: verify-playable's control is red because of its own screenshots (boot 10.7 s unobserved vs 69.7 s observed, slower in 3 of 3 paired runs, 3 of 6 screenshots timing out); verify-live-site's staleness check hashes against the dirty working tree rather than HEAD (3 of 60 sampled files differ from the worktree, 0 from HEAD) and looks at 2 of 715 files; and the aggregation that would run them all together is called by nothing. | Three changes, smallest first. (1) Give verify-playable a sampling mode that does not screenshot inside the wait loop — read the DOM only until `__HARNESS.ready()` resolves, then take one screenshot — so its control can go green and its six sabotages mean something; and stop the boot notice's 20 s ceiling from hiding the `bn-bad` state (shrink it to a dismissible corner banner instead), since two instruments read that state as the page's own verdict. (2) Point verify-live-site's staleness check at HEAD (`git show HEAD:game/<rel>`) rather than `readFileSync(game/<rel>)`, widen it from 2 files to index.html + data/index.json + every .js under src/ and vendor/, and give it and check-image-refs the 5xx retry that verify-links.mjs already has. (3) Wire tools/playability/standing-check.mjs into tools/bank.mjs as a non-blocking post-push follow-up, and add check-image-refs and loader-retry to it. Then, and only then, add the missing class: one instrument that loads the deployed page twice with the cache warm between visits. → **`node tools/world/verify-playable.mjs --self-test` exits 0 with the control GREEN and all six sabotages red, at a contention of at least 4.0 per core; `critic-deploy-r2-notice.mjs --notice` shows the AS SHIPPED arm still displaying `bn-bad` at 45 s; `critic-deploy-r2-drift.mjs --n 60` reports the same drift count against the working tree and against HEAD (i.e. the check is pointed at HEAD), with at least 140 of 715 files hashed; and `grep -rn standing-check tools/bank.mjs` returns a call site.** |
| `GAP-W1-enemy-attacks-cannot-reach-contact-range` | `combat.hitbox.sweep` | blocking | 0 | w1 / w1-09-combat-core-r2 | `crit-w1-09-r3-3c19` (FAIL) | An enemy attack's swept volume does not cover the attacker's own root translation, so it cannot hit a target standing inside its forward root travel. Measured on a stationary player against a stationary champion_hist_marked in arena_champion, sweeping 0.0-4.0 m in 0.1 m steps: the chop hits from 1.2 m to 3.6 m and misses at EVERY distance from 0.0 to 1.1 m; the thrust hits 1.6-4.0 m and misses 0.0-1.5 m; combo_a hits 1.1-2.9 m and misses 0.0-1.0 m. Those minimum distances are the attacks' declared root_dz_m of 1.2 / 1.6 / 0.9. Zeroing root_dz_m moves the inner boundary to 0.6 / 0.8 / 0.8 m, confirming the root translation is the cause (data file reverted, git clean). The player's own weapons have no such hole — dagger, straight sword, spear, greatsword and halberd all hit from 0.0 m. The consequence, measured: a player who stands completely still at 0.8-1.1 m and does nothing but mash the light attack kills the 2876-HP champion in 1153-1377 frames taking ZERO damage, while the enemy completes 44-54 frames of live hitbox and suffers zero stagger frames. This is also the mechanical cause of the exemplar's headline out-of-band row: 141 of 211 enemy swings resolve as WHIFF, and 35 swings across five fights had the player invulnerable for every active frame at 0.01-0.60 m — inside the hole, where the swing could not have connected anyway, so i-frame negation reads 0.111 against a band of 0.40-0.80. | Sweep the attacker's root translation as well as its animated pose. RI-CMB04 §A's step order runs 5 (consume root motion) before 8 (sweep + query, this frame's pose vs last frame's); the sweep must interpolate the weapon socket capsule between last frame's WORLD-space pose and this frame's WORLD-space pose — root delta included — and the SUBSTEPS count must be derived from the larger of the arc travel and the root travel, not from the arc alone. A root_dz_m of 1.2 m spread over a 10-frame active window is 0.12 m per frame, which is inside one capsule radius, so the fix is a space change rather than a substep increase; verify both. Then re-derive SUBSTEPS from the measured maximum of (per-frame tip travel, per-frame root travel). → **critic-r2-reach.json shows min_hit_distance_m <= 0.35 m for chop, thrust and combo_a with root_dz_m unchanged at 1.2 / 1.6 / 0.9, and no miss at any distance between 0.0 m and the attack's outer reach; AND critic-r2-turtle.json shows a motionless mashing player at 0.8, 1.0, 1.1 and 1.5 m taking >= 0.4 x max HP; AND the RI-CMB07 exemplar's row 11 (swings negated by i-frames) reaches 0.40-0.80 on the median of five fights with the enemy whiff share below 0.35.** |
| `GAP-W1-enemy-weapon-volume-still-cannot-reach-and-the-body-pays-its-damage` | `combat.hitbox.sweep` | major | 0 | w1 / w1-09-r3 | — | The round-2 hole in the weapon volume is untouched; it has been covered by a second volume that pays the weapon's damage. Ablation, measured on a copy of the loaded data with no file edited: delete `hitgeometry.json §body_hazard` and the champion's minimum reaching distance against a stationary player returns to chop 1.2 m, thrust 1.6 m, combo_a 1.1 m, combo_b 1.2 m — round 2's numbers to the decimal. With the block present, every hit inside those radii carries `via: "body"`, and a body hit deals the attack's full weapon damage: chop 96 at 0.05 m via body is the same 96 as at 3.65 m via weapon, thrust 75, combo_a 63. It is the same on the player's side — the dagger's blade cannot cut inside 0.75 m and the straight sword's inside 0.80 m, so 14 of the dagger's 19 connecting distances and 15 of the straight sword's 36 are torso-checks. Two of the seven player classes fail S26's contiguity clause outright, and the gaps sit exactly at the seam between the two volumes: the spear's body corridor stops at 1.15 m and its blade starts at 1.30 m, leaving 1.20-1.25 m dead; the halberd's stops at 1.10 m and starts at 1.35 m, leaving 1.15-1.30 m dead. Reproduced identically in headless Chromium and in the node arena. And it is what the fight is made of: in the shipping game, 5 to 7 of the 8 blows that kill a motionless player are `via: body`. | Two changes, in this order. (1) Fix the weapon volume rather than compensating for it. At the champion's first active frame the chop's socket capsule spans z = [1.34, 2.57] with the root at z = 0.384, i.e. the blade's near end is 0.95 m in front of its own root and never re-enters the 0.4-1.1 m band. Give the enemy archetypes' `chop_overhead` and `thrust` clip archetypes in game/data/combat/clips.json a low phase — a follow-through that brings the guard socket down through the attacker's own front quarter — so the swept hull covers the band the attacker is walking through. Verify with `--root-dz 0`, which isolates the blade from the corridor. (2) Stop the body corridor paying the weapon's damage. Give `hitgeometry.json §body_hazard` its own `motion_value` and `poise_damage` (a shoulder-check is a shove, not a cut) and resolve it against `struck_part` with that value in game/src/combat/resolve.js step 8b, so a trace reader and a player can tell the two apart. Then re-run the contiguity acceptance: with the weapon volume fixed, the spear's and halberd's dead ring disappears because the blade band now starts inside the corridor's outer edge. → **With `hitgeometry.json §body_hazard` removed, `min_hit_distance_m <= 0.35 m` for all four champion attacks and for all seven player weapon classes, `root_dz_m` unchanged. With it present, every attack's hit band is contiguous from 0.05 m for all eleven attacks (4 enemy + 7 player), no interior gap at any 0.05 m step. A body hit's `dmg` is strictly less than the same attack's weapon `dmg` in the trace. And in the browser turtle, the majority of hits taken carry `via: "weapon"` rather than `via: "body"`.** |
| `GAP-W1-f1-actor-body-shader-does-not-link` | `render.fidelity.character` | blocking | 0 | w1 / w1-orphaned-surface-shaders-r1 | `crit-w1-f10-characters-a71c` (FAIL) | Restoring the surface pass on cloned materials put two fragment-shader preambles into one program on actor body materials, and they collide on two identifiers. installSurfaceShader (visual-foundation.js:521, :574) and installWaterline (actor.js:302, :310) each declare `uniform float uWetness` and each declare a local `float esWet`. The program does not link. Measured at HEAD: 124 actor-body materials carry the duplicate declaration, 66 of 124 report program.diagnostics.runnable false with 'uWetness : redefinition' and 'esWet : redefinition', and 0 of 65 equipment/frill controls do either. | PRESERVE the copy seam exactly as built — installCopySeam/adoptMaterialCopy, the JSON recipe in userData, the clone-not-copy choice and the chaining in installWaterline are all correct and are not what is broken. The delta is a rename, in one function. In game/src/render/actor.js#installWaterline (lines 287-327): rename the uniform `uWetness` to `uWaterlineWetness` at its declaration (:302), at the shader.uniforms assignment (:297) and at both uses (:310, :311); rename the local `float esWet` to `esWaterlineWet` at its declaration (:310) and both uses (:313, :319). Prefix `esBand` and `esMeniscus` in the same pass — they are unique today by luck rather than by design, and the next hook added to this chain will collide with them. Do NOT rename anything in installSurfaceShader: it is the shared library and its names are load-bearing for setWorldWetness(). Then add the missing tripwire, which is the durable half of this remedy: a check that no material in the live scene has program.diagnostics.runnable === false, wired into tools/visual/w1-30-surface-orphan-census.mjs so the standing census can see a link failure — its five existing checks structurally cannot, which is why all of them were green over a broken build. → **Cheapest gate first, and it already exists and is already red: `node tools/visual/w1-f1-critic-shader-collision.mjs` returns 0 for all four checks, with BODY-PROGRAM-IS-RUNNABLE reporting 0 of at least 100 actor-body programs failing to link and NO-DUPLICATE-UNIFORM-DECLARATION reporting 0 duplicates, while CONTROL-IS-CLEAN and CONTROL-PROGRAM-IS-RUNNABLE stay green. Then, and only then, re-take the frames: re-run `node corpus/90-verdicts/wave1/artifacts/W1-ORPHANED-SURFACE-SHADERS-CRITIC/base-frame-ab.mjs` against a fresh two-arm capture and require the player's skin and cloth body surfaces to be PRESENT at all 8 orbit angles in BOTH arms, so the shipped-configuration player delta is attributable to shading rather than to geometry appearing. Hard fail: any actor-body material with runnable false. The gap closes when the census itself goes red on a deliberately re-introduced duplicate and green on the shipped tree.** |
| `GAP-W1-faction-ladders-are-bricked-up-at-rank-5` | `quests.faction.rankgating` | blocking | 0 | w1 / W1-20 | — | RI-QST03 §B puts a world-state requirement on ranks 5, 6 and 7 of every ladder. On four of the eight lines the world states named are raised by nothing: the_dockhands (dockhands_gang_held, dockhands_wharf_decided, dockhands_harbourmaster_vacant), the_drowned_court (court_yard_held, court_tally_decided, court_crown_vacant), the_ixtu_vakh (sapcutters_well_cut, sapcutters_buyer_decided, sapcutters_cutter_vacant) and the_rootkeepers (rootkeepers_sapwell_kept, rootkeepers_chapter_decided, rootkeepers_voice_vacant). `grep -rl dockhands_gang_held game/data/` returns exactly one file, faction-gates.json. The other four lines are clean — ledger_gate_held, assize_seal_held and the Deep-Kin/Xul-Aneekh states are each raised by a real quest resolution. Measured live: a character given every favoured skill at 100, every favoured attribute at 100 and 400 reputation reaches rank 4 on all eight lines and stops, held by the world state; in the null control, with that column flattened, the same character reaches rank 7 on all eight. So the live arm establishes what holds everyone at 4 and the static grep establishes that on four lines nothing can ever move it. 21 authored quests are stranded above the ceiling — 4 on the Dockhands, 6 on the Drowned Court, 6 on the Ixtu-Vakh, 5 on the Rootkeepers — including every succession quest on those lines. | Twelve world_flags entries on quests that already exist at the right ranks. For each of the four lines: give its rank-4 or rank-5 quest a resolution that raises the line's *_held flag, its rank-6 quest one that raises *_decided, and its rank-7 quest one that raises *_vacant. The four carried lines already show the pattern — copy game/data/quests/faction-wet-ledger.json's ledger_gate_held. → **node tools/quests/critic-w1-20-r2-census.mjs returns zero QST03.ceiling.* hard fails, AND node tools/quests/critic-w1-20-r2-live.mjs reports A4.lines_reaching_rank_ge_5 = 8 of 8 on the shipping tree (it is 0 of 8 today).** |
| `GAP-W1-faction-ladders-are-doors-onto-empty-corridors` | `quests.faction.escalation` | blocking | 0 | w1 / W1-20 | `critic-w1-20-r2` (FAIL) | Eight eight-rank ladders exist and are richly specified — every rank gates on reputation, an attribute, two skills and a world state. Three lines carry 18 quests each; the other five carry 1, 2, 2, 3 and 4, giving mean quests-per-rank of 0.12 to 0.50 against RI-QST01's floor of 2.0, with six or seven EMPTY rank bands per line. the_dockhands additionally has four faction quests and not one with deceit != null. The four lines this round opened have no expulsion, no readmission, and — for three of the four — no interior anywhere in the populated world, so a rank in them changes nothing about property or trespass. Measured per line (critic-w1-20-census.mjs): imperial_assize 18 quests / 2.25 per rank / 0 empty bands, wet_ledger 18 / 2.25 / 0, xul_aneekh 18 / 2.25 / 0, dockhands 4 / 0.50 / 7 empty bands / 0 deceit, rootkeepers 3 / 0.38 / 7, drowned_court 2 / 0.25 / 6, ixtu_vakh 2 / 0.25 / 6, deep_kin 1 / 0.13 / 7. faction_interior zones: wet-ledger 18, drowned-court 14, and ZERO for the other six ladders. Expulsion and readmission exist for exactly three lines. reach.json scores the_rootkeepers null on both theft and trespass because no hall exists in the world to test against. | Author quests at ranks 1-7 on the four lines this round opened, rather than a second quest at rank 1; give the_dockhands at least one quest with deceit != null at rank <= 3 per RI-QST01 Table B; add faction-discipline.json rows (expulsion flags plus readmission price_gold, reputation_floor and offered_by) for the_rootkeepers, the_dockhands, the_drowned_court and the_ixtu_vakh; place a faction_interior zone for at least the Rootkeepers and the Dockhands in game/data/world/property/**; and split the doubled faction_reputation in faction-joining-wave1.json so a joining quest pays 10 rather than 20. → **`node tools/quests/critic-w1-20-census.mjs` exits 0 — which requires every line at >= 18 faction quests, mean >= 2.0 quests per rank, zero empty rank bands, no line with deceit == null throughout, and earnable reputation within 1.6x of the rank-7 requirement of 112 on all seven joinable lines (today: 1.86x to 9.23x). And `node tools/quests/critic-w1-20-reach.mjs` reports a non-null theft and trespass readout for every joinable line, instead of null on five of seven.** |
| `GAP-W1-hearth-levelup-gate-reads-a-method-that-does-not-exist` | `progression.bonfire.function` | blocking | 0 | w1 / w1-13-death-and-the-hearth-loop | `crit-w1-13-r2-death-hearth` (FAIL), `crit-w1-13-r3-death-hearth` (FAIL), `crit-w1-13-r4-clock-and-ar1` (FAIL) | The level-up screen — which RI-PRG04 §1 makes the hearth's defining affordance and calls 'THE ONLY PLACE LEVELLING IS POSSIBLE' — cannot be opened at any of the 29 shipped sapwells. Measured at hearth-archon, hearth-lilmoth and hearth-gideon: standing on the basin, with the hearth registry itself reporting `standing_at: hearth-<id>` and immediately after a successful `restAt()`, `openMenu('levelup')` is REFUSED 3/3 with "the level-up screen exists at a HEARTH only (RI-UIX03 L1)"; with the harness override `setAtHearth(true)` — which has no in-game counterpart — the same call OPENS 1/1. The gate at game/src/engine.js:2011 is `!!(this.hearths && this.hearths.atHearth && this.hearths.atHearth(this.sim)) \|\| !!this.sim._uiForceHearth`, and HearthSystem (game/src/sim/hearth.js) defines list/get/count/nearest/at/gateAt/menu and no `atHearth`. The first disjunct is `undefined` at every well in the province and the only writer of the second is `__HARNESS.setAtHearth()`. `_spendSouls()` is reachable from exactly one producer (game/src/ui/system.js:322, inside `case 'levelup'`), so souls have no sink anywhere in the running build. | Add the missing predicate to the hearth registry that already answers the question. In game/src/sim/hearth.js, add to HearthSystem: `atHearth(sim) { return !!this.at(sim.player.pos[0], sim.player.pos[2]); }` — `at(x, z)` already exists and already tests each well's own `interact_radius_m`, and `listHearths().standing_at` is built from it, so the registry is right and only the method name the UI calls is missing. Then demote `sim._uiForceHearth` to a declared test override: keep it (arena scenarios need it) but have `setAtHearth()` set a flag that `getUIState()` reports, so a probe that leans on it is visible in its own output. While in this file, note the same shape elsewhere: `game/data/world/respawn.json` names `engine.spawn(id, x, z, {named: true})` as the producer of the six never-respawn flags and `engine.spawn()` copies only `opts.as` and `opts.yaw` — fix that in the same pass (three lines) so the S5 classifier has a world-side writer and is not reachable only through `setEntityNamed()`. → **`node tools/harness/critic-w1-13-f.mjs` reports `levelup_reachable: true` at all three wells with NO `setAtHearth()` call anywhere in the probe, and `false` when the player is teleported 50 m off the basin; a `level_up` trace event fires from a well and from nowhere else in the province; and `soulsHeld` falls by exactly `soulsToNextLevel()` with `level` rising by 1. Separately, `node tools/harness/critic-w1-13-b.mjs` reports `flags_honoured_via_spawn_opts` listing all six flags (it currently lists none) and `protected_that_stood_back_up: []` on both the rest and the death trigger (it currently lists six).** |
| `GAP-W1-height-fog-credit-unanchored` | `render.fidelity.atmosphere` | major | 0 | w1 / w1-30-shadow-casters-r1 | — | The thirteen-region height-fog result in circulation — 13 of 13 regions improved, unusable frames 9 of 13 down to 5 — was rendered through world/aerial.js's shader chunks while they were clobbering render/sky.js, and has never been re-taken under the model that ships. It is currently the headline evidence for the atmosphere and for region identity. | One capture run, no source edit. Re-render the thirteen Deck region vistas at HEAD twice with tools/visual/deck.mjs at one stamped commit: once as shipped, once with sky.js's heightFog feature off (setFeature('heightFog', false) sets scene.fog.heightFalloff = 0, which degenerates the Beer-Lambert integral to the height-independent case and is the honest before-arm). Run frame-stats.mjs over both. Retire reports/w1-30f/deck/F-before and F-after and mark the 9->5 figure superseded wherever it is quoted. Separately, add a subtractive mode to tools/visual/w1-30-shadow-casters.mjs#applyArm() so an arm can write false as well as true. → **A committed pair of thirteen-vista captures at one commit sha, both rendered with render/sky.js owning THREE.ShaderChunk.fog_fragment (assert it, as tools/visual/w1-30-fog-owner-ab.mjs does), with the per-region edge_density, dominant_frac and luma span of each arm published; and a restated count of vistas flagged unusable by frame-stats.mjs in each arm. A later critic can re-measure by re-running the same two commands. The gap closes when no document in the tree quotes 9->5 without naming the commit it was taken at.** |
| `GAP-W1-input-calibration-outlives-its-pad-on-disconnect` | `input.gamepad.lifecycle` | major | 0 | w1 / w1-08-w1-29-r2 | — | Round 2's build fix guards the COMPLETED calibration by pad id — `const perm = this.calibrationResult && this.calibrationResult.id === st.id ? ... : null` — and I re-derived it: calibrate a stranger through all six prompts, unplug, plug in a standard pad, and index 0 fires `interact` and index 5 fires `light`. Correct. But nothing guards the calibration IN PROGRESS. `this.calibration` is instance state with no pad id on it, `_beginCalibration()` is triggered from `_adoptPad()`, `_dropPad()` never clears it, and it never times out. `poll()` then runs `_calibrationPoll(st)` against whatever pad is currently active. Measured on dcf1d36 with my own driver: answer 2 of 6 prompts on an unrecognised pad, unplug it (n_pads 0) -> `calibrating: true`, prompt still 3/6; plug in a mapping:'standard' pad -> indices 0, 1, 2 and 3 ALL produce NOTHING, a completely dead pad; those four presses are consumed as the stranger's calibration answers and the prompt advances 3/6 -> 5/6; finishing the sequence writes result_id 'Xbox 360 Controller (XInput STANDARD GAMEPAD)' carrying the stranger's permutation, after which index 0 fires `roll`, index 4 `menu` and index 5 `lock_on`; after 600 further frames it is still calibrating. | game/src/input/gamepad.js, in _dropPad() after `this.pads.delete(idx)`: `if (st && this.calibration && !this.calibration.done) this.calibration = null;` — a calibration belongs to the pad it was begun on, exactly as the completed result now does. I applied this in the page and re-ran the identical sequence: `calibrating: false` after the unplug, and the standard pad's index 0 fires `interact` and index 5 fires `light`. Delete-the-fix reproduces the defect exactly, in both directions. → **A --self-test leg that begins a calibration, disconnects the pad mid-sequence, connects a mapping:'standard' pad and asserts index 0 fires `interact` — and that the leg goes red when the one line is removed. Plus M-P15 extended to run its sequence a second time with a disconnect at prompt 3, reporting the action the pad really fired when it is not `interact`, which is the property the repaired M-K18/M-P23 already have.** |
| `GAP-W1-input-discoverability-unmeasured` | `input.discoverability` | major | 0 | w1 / w1-08-w1-29-r1 | `crit-w1-08-29-r2` (FAIL), `crit-w1-map-defects-r1-3c19` (FAIL) | RI-JRN03's entire 'Discoverability without instruction' block — 14 of its 100 points, and the home of hard fail HF5 — is unmeasured, and the one check in it that reports a result reports it from an empty buffer. M-K20 searches window.__HARNESS.getRenderedText() for instruction tokens and passed with `frames: 0`. I drove the register in seven named states (arena_flat, helstrom-market, thorn-hall, ui-journal, npc_showcase, default, settlement_primary_street) and through uiOpen('dialogue'), uiOpen('menu') and uiOpen('rest'): entries = 0 and summary.surfaces = {} in every single one, while getUIState() at the same instant reports three HUD elements carrying drawn text ('5', 'LIGHT', 'Spark-Dart'). game/src/render/text-register.js instruments only the 'dialogue' and 'title' surfaces and neither ever writes. M-K21 and M-K22 — the other 8 points of the block, M-K22 being the second half of HF5 — do not exist in tools/journey/input-checks.mjs at all; nor do M-K8 (HF3), M-K9 (HF4), M-N1/M-N2/M-N3/M-N5(HF10)/M-N7, M-P14, M-P20, M-P24 (HF7) or M-P25. That is 13 scored ids returning 0 to `grep -c` over the instrument, 28 of RI-JRN03's 100 points and 12 of RI-JRN04's, and five named hard-fail gates that no instrument can fire. The build's `--self-test` takes 5 instruments red — M-P5, M-P3, M-P1, M-P6, M-P17 — and not one of them is a check in this block. | Three steps, in order. (1) MAKE THE REGISTER REAL: game/src/render/text-register.js wraps CanvasRenderingContext2D.fillText but only for the 'dialogue' and 'title' surfaces; route every text-drawing surface through it — HUD, menu, rest, the rebinding screen, and the touch/rotate surfaces once they are drawn — so getRenderedText() returns what is on the glass. (2) GIVE M-K20 A POSITIVE CONTROL IN --self-test: add a leg that draws a known token ('Press E') on a non-settings surface and asserts M-K20 goes red; a leg that asserts the token is NOT found when drawn on the settings surface; and make the check itself report `unmeasurable` rather than `pass` whenever entries === 0, so an empty stream can never again read as a clean bill of health. (3) IMPLEMENT THE MISSING IDS: M-K8, M-K9, M-K21, M-K22, M-P14, M-P20, M-P24 and M-P25 as instrument checks in tools/journey/input-checks.mjs, each with a --self-test perturbation leg; and run the Naive pass (M-N1/N2/N3/N5/N7 for RI-JRN03, M-N1/N3/N5 for RI-JRN04) as the separate fresh-agent protocol its own section specifies, since it is a procedure and not an instrument. → **`node tools/journey/input-checks.mjs --group desktop,pad,touch,viewport,rebind --self-test` reports (a) M-K20 with entries > 0 in at least one ordinary gameplay state and 0 instruction-token hits, (b) a --self-test leg that draws 'Press E' outside the settings surface and drives M-K20 red, (c) non-null results for all thirteen ids currently missing, and (d) a --self-test leg for each newly added check that goes red under perturbation with `vacuous: []`. Re-measured by a later critic with `grep -c "'M-K8'" tools/journey/input-checks.mjs` returning >= 1 for every id in RI-JRN03 and RI-JRN04's ## Scoring tables.** |
| `GAP-W1-interior-door-draws-nothing` | `world.interior.named` | blocking | 0 | w1 / W1-04-r1 | `crit-w1-04-r2-3c19` (FAIL), `crit-w1-04-r3-3c19` (FAIL), `crit-w1-04-r4-3c19` (FAIL), `crit-w1-04-r5-3c19` (FAIL) | Walking through a door sets sim.env.interior and moves the body to the declared interior spawn, and does not change what is drawn: renderer.setCell() has exactly one caller, Engine._applyCell(), and _applyCell() is called from loadState, the census staging, the barge reset and the save-load path but never from useDoor() or leaveInterior(). Measured over the WHOLE population: all 115 interiors entered through enterInterior() from a province start — 115 of 115 entered, 0 refused, 0 errors — and 0 of 115 changed the drawn cell; the visible set reads ['province'] after every door. In detail, after enterInterior('thorn-hall'), across six fixed steps and an explicit renderFrame(), the visible cell set is still ['province'] while cellFor(env) reads 'interior'; calling _applyCell() by hand switches it to ['interior'] (the control arm). After exitInterior the visible cell is still ['interior'] while the body stands outdoors in Thorn. And there would be almost nothing to draw if it did switch: cellFor() maps five interiors to hand-built W1-07 rooms and returns the generic 'interior' cell — scene.js buildHall(), 249 triangles, one light — for the other 113 of 115, while interior.props (18 on a typical shop), .lights (10), .containers and .unique_item have zero consumers anywhere in game/src. | Two changes, in this order. (1) Switch the drawn cell when the door is taken: install the Engine's _applyCell() as a sim.applyCell hook of the same family as sim.placeBody, sim.populate and sim.doorVeto — so game/src/sim/settlement.js keeps knowing nothing about the renderer — and call it from useDoor() and leaveInterior() immediately after placeBody(). (2) Build the interior cell FROM THE RECORD instead of resolving 113 interiors to buildHall(): instantiate bounds_m as a shell with a doorway at continuity.interior_spawn, lights[] as point lights at their declared positions and intensities, and props[] against a shared prop-name table, so the room the player stands in is the room game/data/world/interiors/<id>.json describes. → **(a) A probe that enters all 115 interiors through enterInterior() and reads renderer.cells[*].visible finds the visible cell equal to cellFor(env) on 115 of 115, and 0 of 115 still drawing the exterior; leaving returns the visible cell to 'province' on 115 of 115. (b) A pixel-hash sweep over one fixed camera pose in all 115 interiors returns at least 100 distinct images, where it currently returns 1. (c) The number of interiors resolving to the generic 'interior' cell falls from 113 to 0. (d) interior.props and interior.lights each acquire at least one consumer in game/src, demonstrated by perturbation: deleting the lights array from one record changes the number of lights in the drawn scene. Re-measurable with tools/world/critic-w1-04-r1b.mjs and tools/world/critic-w1-04-r1c.mjs as written.** |
| `GAP-W1-library-martial-single-hand-fingerprint` | `lore.register` | major | 0 | w1 / w1-library-martial-r4 | — | All 43 martial texts carry the same three orthographic and page-furniture fingerprints, every one of them in a place tools/check-prose.mjs does not read. Bare Arabic numerals run 128.5 per 10k words against Morrowind's measured 9.3 (13.8x) and against the rest of this project's own library at 24.9; 65.1% of the texts carry two or more ALL-CAPS section headings against Morrowind's 2.1% (31x) and this library's 8.4%; and 60 of the 188 headings carry a ', WHICH ...' appositive against 1 in Morrowind's whole 241-book corpus. Two of the three are gate artifacts: check-prose RULE-06 penalises SPELLED counts four..twenty, so de-spelling turns the row green (the status file banks it as a win, 90.77 -> 52.42), and RULE-17/RULE-28's ', which is' tic did not go away in round 4 — it moved one line up into the headings, where a sentence-level detector cannot see it. | Three mechanical edits, none of which rewrites an argument, an image or a joke. (1) Re-spell numerals in narrative prose across the 43 texts, keeping Arabic figures ONLY where a document would really carry them — the muster leaf, the rack tally, the day-book, the handbill's stoppages, registry numbers and 3E dates. This will re-redden check-prose RULE-06, and that is the correct outcome: the rule's own reference is Morrowind and Morrowind spells them, so re-baseline RULE-06 against tools/uesp/mw-book-stats.py's measured distribution (one 68.0% decaying smoothly to thirteen 2.9%, no bump) instead of a flat penalty. (2) Break the heading template: take texts carrying two or more ALL-CAPS headings from 65% to at most 20%, delete the ', WHICH ...' appositive from at least 45 of the 60 headings, and leave at most 12 headings beginning with WHAT — several of these texts should carry no headings at all, since a hunter's oral teaching, a nephew's account of two uncles arguing and a letter found folded in a helmet do not come with a table of contents. (3) Convert Imperial-year counts to sap-falls in at least 8 of the 15 Argonian-authored texts and let at least one refuse to convert, the way game/data/books/quest-keys.json already does. Separately, extend tools/check-prose.mjs to read headings: it reads sentences only, which is why 60 instances of a tracked tic survived a round whose entire subject was tics. → **node tools/lore/critic-martial-r4-fingerprint.mjs --mw=<dump> exits 0 — that is, bare numerals in the martial set at most 3x the Morrowind reference of 9.3 per 10k, and texts carrying two or more ALL-CAPS headings at most 3x the reference 2.1% — with sap-fall occurrences in the martial set >= 8, and tools/check-prose.mjs still clean on all seven files except a deliberately re-baselined RULE-06 whose new bar is quoted from mw-book-stats.py output.** |
| `GAP-W1-LIBRARY-the-library-has-no-reader-on-the-world-side` | `ui.readables` | critical | 0 | w1 / w1-library-books-and-the-reading-surface | — | 37,819 words in 65 texts open 65/65 in the engine across 324 pages, and the only thing a player can do with any of it is read it. Both mechanical consumers the two items name are dead code. (1) `book.knowledge_key` is read by no file in game/src/; the three non-violent quest resolutions that gate on those keys — Q-SOUL-02 res_seal, Q-LILM-01 res_rootkeepers, Q-DEEP-01 res_broker — are unreachable by any route in the shipped build, and the engine reports the identical refusal before and after the book has been opened and read to the last page. (2) `book.topics_taught` (60 books, 116 topics) is read by nothing; opening all 60 in the engine leaves topicsKnown empty. That field is RI-UIX05 R3's permitted exception and the item's entire declared AR-3 seam crossing. | — → **—** |
| `GAP-W1-library-unreachable-contradiction-pairs` | `undefined` | blocking | 0 | w1 / w1-23-lore-registry-and-the-provinces-canon | — | Three of nineteen registered contradiction pairs have both sides reachable by a player. Fifteen have neither side reachable anywhere under game/. The ford pair — the best writing in the project — and the province's central moral fact (CF-062/CF-D008, whether subjects of the Empire are held as property, argued by the-blessings-of-the-coast against ledger-and-journal-of-andrel-vorin) are each argued by two books that are referenced nowhere in the world. | Place the 30 books carrying the 15 unreachable disputes as `readable` entries in interiors that already exist, starting with the four pairs that carry the province own subjects (the ford, slavery, the Knahaten Flu, the Hist-soul question); then lift the pair-reach measure out of tools/lore/critic-w1-23-r3-reach.mjs into tools/lore/canon-census.mjs so it runs where check-content.mjs runs. → **node tools/lore/critic-w1-23-r3-reach.mjs reports >=15 of 19 mutual pairs with BOTH sides reachable (today 3), and canon-census.mjs exits non-zero when any registered dispute has fewer than 2 of its sides placed. Re-measurable by re-running both tools at the fix commit.** |
| `GAP-W1-m6-pan-correlation-passes-the-panner-it-was-built-to-catch` | `audio.combat.impact` | major | 0 | w1 / W1-11 | — | This piece's headline artifact is a three-arm experiment showing the shipped panner at r = 0.9999 and the rule it replaced at r = −0.5207 on the same fixture, and it concludes `detector_goes_red = true`. The −0.5207 is a property of that fixture, not of the rule. Under the legacy rule a blow the player lands is positioned at the attacker, so dx = dz = 0 and pan = sin(atan2(0,0) − playerYaw) = sin(−playerYaw): the target's position is NEVER READ, and the correlation with the target's true bearing is therefore whatever the fixture's player-yaw history happens to make it. Sweeping the fixture on the shipped code at HEAD 5c4c5b0, the same broken rule scores r = −0.0759 (orbit anchored to player facing), +0.7768 (orbit anchored to world — within 0.03 of the bar), +0.6034 (stationary target, player free to turn), null (still target), and +1.0000 with 100 impacts, 99 distinct pan values and a 70° bearing spread on 'stationary enemy, player turning on the spot'. On that last fixture the BROKEN rule PASSES M6 outright and the shipped `detector_goes_red` predicate reports green. That fixture is not exotic: it is a player circling a standing enemy, which is what M6's own method text ('correlate audioLog.pan against the enemy's bearing from the player') invites a critic to build. The invariant that does discriminate is already in every log row and is not in the pass condition: distance_m_max is 0.0000 on all five legacy arms and 1.13–1.45 m on all five shipped arms. | Make M6's pass condition test the property that is actually invariant, and make the fixture sweep part of the check rather than a single hand-picked orbit. Concretely: (1) add `distance_m` to the P4 pass condition — a player-landed impact must report a non-zero distance to the listener, and the legacy rule cannot satisfy that on any fixture; (2) run P4 over at least the 'stationary target, player turning' fixture in addition to the orbit, and require the shipped rule to pass ALL of them and the sabotaged rule to fail ALL of them, rather than requiring one arm to go red; (3) fold tools/audio/critic-m6-fixture-sweep.mjs (or its logic) into tools/audio/impact-browser.mjs so the sweep runs where the real WebAudio panner does. The tool already exits 3 when it finds a fixture on which the broken rule passes, so it is its own regression test. → **node tools/audio/critic-m6-fixture-sweep.mjs exits 0 with finding 'LEGACY_NEVER_PASSES' — i.e. no fixture in the sweep passes the acceptance criterion under panSource='attacker' — while the shipped rule passes every fixture in the sweep; and impact-browser.mjs P4 reports the same across at least two fixture geometries.** |
| `GAP-W1-magic-effects-no-behaviour` | `magic.effects.utility` | blocking | 0 | w1 / W1-14 | `crit-w1-14-r2-7c31` (FAIL), `crit-w1-14-r3c-4d18` (FAIL), `crit-w1-14-r4` | The effect catalogue is a complete, correctly priced, deterministic parameter space with one behaviour behind it. Measured across all 71 shipped spells: 5 of 55 effects produce the mechanical consequence their own record describes (fire, frost, shock and poison damage, and damage_health); 1 is partial (levitate meters altitude and charges 1.5 Focus/m but never moves pos[1]); 16 resolve only as HP damage equal to their own magnitude — demoralise deals 111 hp and the enemy stays AGGRO, calm_beast deals 31 hp and the beast stays in COMBAT, open_lock deals 31 hp to a creature, charm 184, corrode 310, lock_lock 184, silence 31, paralyse 12 with no buildup field on the enemy record at all; and 33 are a row in effects_active that no other system in the build reads — shield and resist_element each mitigate 0 of a scripted 100-damage hit, the_greater_mending heals 0 with the player at 420 of 620, feather leaves equip_load_pct at 24 and roll_class at LIGHT, fortify_skill satisfies no gate, bind_lesser summons nothing (entity count 0 to 0), wall creates no collider, leap adds 0 m of altitude, recall completes its 210-frame ritual and leaves the player at exactly the coordinates it started from, hist_sight completes and writes 0 journal entries, and all five Veiling effects leave getStealthState() byte-identical to baseline against the stealth system shipping in the same build. | Replace the single generic applicator with a per-effect handler registry. Concretely: (1) In game/src/sim/combat-bridge.js, the spell-contact callback currently accumulates `dmg += outputOf(effect, magnitude)` for every effect whose utility_class is null OR whose geometry is not 'none' — restrict that accumulation to the five damage effects (fire_damage, frost_damage, shock_damage, poison_damage, damage_health) plus the status buildup each declares, so a non-damage effect can never deal HP damage. (2) Add game/src/sim/magic/apply.js: a table keyed by effect id, one handler per effect, called from MagicSystem.applyEffects(), each handler writing into the system RI-MAG06 §B names for that effect. (3) Wire the eight consumers that already exist in this build before touching anything that does not: CombatBody.equipLoadPct/tier for feather and burden (RI-CMB01's 30.00/70.00 cliff must stay exactly where it is); CombatBody.hp for restore_health, drain_health and absorb_health, and hpMax for drain; a mitigation multiplier ahead of the damage write for shield, resist_element and sap_ward; sim.stealth.d.detection's V terms and sound radius for chameleon (clamped 80), invisibility (breaking on attack, cast, interact, container and COMBAT), muffle, night_eye and false_face; the S11 buildup meter for paralyse, so it crosses a threshold instead of arriving in one frame; enemy alert_state and the leash for calm_beast, demoralise and frenzy, ending COMBAT with zero death events; CombatBody.pos for mark/recall/intervention, so a completed ritual actually moves the player; and a real locomotion mode for levitate that writes pos[1], caps horizontal speed at 1.4 m/s and drops light, heavy, block, roll and parry. (4) Every effect whose consuming system genuinely does not exist yet (water bands, the lock register, item condition, the quest knowledge store) gets an explicit UNMEASURABLE row in the census naming the piece that owns it, capped at five per RI-MAG06 M6. → **Re-run RI-MAG06's census (reference implementation at corpus/90-verdicts/wave1/artifacts/W1-14/consolidate.mjs). EFFECT-FUNCTION >= 50/55; HP_DAMAGE_ONLY == 0 (hard fail above 3); UNREAD_TIMER <= 2; UNMEASURABLE <= 5, each naming its owning piece. Plus these five named single-trace assertions, each of which fails today: still_the_beast on an aggroed beast_slitherfang ends COMBAT with alert_state != AGGRO and zero death events; feather moves roll_class across RI-CMB01's 30.00% cliff with the discontinuity still at exactly 30.00; thick_skin reduces a scripted 100-damage hit by the declared fraction; the_deep_chameleon changes getStealthState().V; recall moves getPlayerStats().pos to the mark.** |
| `GAP-W1-magic-skill-frozen` | `magic.gating.skills` | blocking | 0 | w1 / W1-14-r2 | `crit-w1-14-r3c-4d18` (FAIL), `crit-w1-14-r4` | MagicSystem.skills is a private object initialised to {sorcery:30, root_speech:30, warding:30, veiling:30} that no gameplay path writes. Measured in four shipped states (default, arena_flat, dungeon_primary, helstrom-market): 47 of 72 shipped spells attunable and 25 refused — all 17 tier-3 and all 8 tier-4 — and setAttuned() drops them silently. 40 casts at a live target, a HEARTH rest, H.setSkills() on the character sheet and H.grantSkillUse('cast_effective') all leave the tier-3 attune at 0/1 and quoteSpell().attunable_now false; only H.setMagicSkills(), a harness call with no in-game counterpart, flips it to 1/1 and true. Spellmaking is capped at two effects for the whole game ('3 effects needs skill 50; your best relevant school is 30'). In the other direction, RI-MAG04's magic resolutions gate on sim.progression.skills, which is {} in every shipped state and which the magic system never writes: the coupling between the character's magic skill and the magic system is 0.00 in both directions, with the null control clean (perturbing magic.skills 30->100 does flip the gate, so the probe is driving a live gate and not a stub). | Make MagicSystem.skills a VIEW of sim.progression.skills rather than a private copy: replace the literal at game/src/sim/magic/system.js:59 with a getter that reads sim.progression.skills (mapping root_speech <-> root-speech, which are spelled differently in the two files today), so _effectiveSkillFor, focusCost, the tier gate and quoteSpell's effect-count all read the character sheet. Then close the loop that fills the sheet: add `skill` to the effect_apply and spell_hit event payloads in game/src/sim/magic/system.js and game/src/sim/combat-bridge.js so game/src/character/skilluse.js:stepSkillUse banks the SPELL'S OWN school instead of hardcoding 'sorcery'; and make setAttuned() emit an `attune_refused` event with the shortfall instead of silently returning a shorter list. Ensure sim.progression.skills is non-empty in the shipped states (engine.js:575 fills it from ch.skills, which is empty when no character has been composed) — coordinate with W1-07, whose census owns that seeding, and record the seam if it cannot be done here. → **Re-run tools/harness/critic-w1-14-r2f.mjs, which never calls setMagicSkills(). Required: (a) in every shipped state, the attunable count rises when the character sheet's warding/sorcery rise, and 72/72 are attunable at skill 100; (b) 40 casts of a warding spell at a live target raise sim.progression.skills.warding by a non-zero amount and emit >= 1 skill_use event with skill == 'warding' (not 'sorcery'); (c) a three-effect spell is quotable once any relevant school reaches 50; (d) H.questResolutions('Q-MAG-01') reports the magic resolution available after the skills were reached BY CASTING, with no setSkills or setMagicSkills call anywhere in the probe; (e) setAttuned() on an under-skilled spell emits an attune_refused event naming the shortfall.** |
| `GAP-W1-magic-spellmaking-has-no-world-side-surface` | `magic.effects.catalogue` | blocking | 0 | w1 / W1-14-r3 | `crit-w1-14-r4` | The spell-making and enchanting systems — the entire Morrowind half of this piece and the reason RI-MAG03 exists — are reachable only from window.__HARNESS. `makeSpell` has exactly one caller in the whole build and it is the test harness; so does `enchantQuote`. The eight menus a player can open are inventory, journal, book, levelup, sheet, spells, container and map, and `spells` is a list of what is already attuned. No NPC in game/data/npcs/** offers a spellmaking or enchanting service; the shipped service vocabulary has 17 values and neither word is among them; no dialogue topic mentions either. game/data/magic/enchanting.json names seven enchanters and spellwrights with settlements attached — journeyman_lilmoth, journeyman_gideon, journeyman_stormhold, journeyman_helstrom, master_blackrose, master_soulrest, recluse_stone_wastes — and its own note reads 'Four in the world, and walking to one is part of the cost (RI-MAG03 §A, S7)'. None of the seven exists as a person anywhere in the world; their ids appear in exactly one other file, enchant-services.json, as a foreign key to nobody. | Place the seven enchanters/spellwrights enchanting.json already names as NPCs in the settlements it already names them at (lilmoth, gideon, stormhold, helstrom, blackrose, soulrest, and the Stone Wastes recluse), each carrying a `service` of spellmaking or enchanting in game/data/npcs/**. Give each a dialogue topic that opens the commission surface, so the entry point is Morrowind's topic list and not a Souls vendor grid. Add a `spellmaking` mode to ui/system.js's OPENABLE list, driven by quoteSpell — the quote already returns everything the screen needs: focus_base, focus_cost, tier, skill_req, gold, the clamped effect terms and the refusal reason. Do not put one in the starting town: the item's own note makes distance part of the price and S7 backs it. → **A probe that NEVER calls H.makeSpell, H.enchantQuote or H.enchanterOpen walks the player to a named settlement, opens a conversation with a named NPC through H.talkTo, selects a spellmaking topic through H.conversationSay, and commissions 19 of 20 tuples that are legal under RI-MAG02 and absent from spells.json — including one three-effect spell, one at an effect's magnitude.max, one at duration.max_s, one whose focus_cost exceeds focus_max, and one deliberately nonsensical (invisibility + area fire_damage) — asserting each appears in saveState(), each computes RI-MAG02 §D's focus_base exactly, and each castable one delivers its commissioned magnitude. The twentieth, the effect-count-over-skill-cap case, is refused with its number. Gold decreases by the quoted price and by nothing else.** |
| `GAP-W1-mainquest-topic-bootstrap` | `quests.mainline.acts` | blocking | 0 | w1 / w1-19 | `crit-w1-19-r2-3c19` (FAIL) | The main quest cannot be entered or advanced by playing. Booted cold with nothing granted through the harness, 0 of 32 category:"main" quests are offerable and 32 of 32 are refused by gate.js:153 on `opens_by.topic`; `topicsKnown` is length 0 and nothing in the running world can ever add to it in the spelling the gate compares. Of the 32 AddTopic edges W1-19 appended to hooks.json, 23 seed only the topic that opens the very quest firing them (Q-MAIN-nn#10 adds the topic Q-MAIN-nn's own opens_by demands), 5 seed a topic no quest asks for, and only 4 point forward. Separately, 2 of 32 opening topics exist in the world's raw spelling at all, 0 exist as a dialogue topic id, 0 rumour lines anywhere in game/data/dialogue/** name a main-quest noun, and `opens_by.overheard_from` — declared on the three quests that are supposed to be overheard — has zero code consumers. The builder's green 29/29 trace does not contradict this: tools/quests/mainline-trace.mjs calls `H.learnTopic(step.topic)` with the exact string the gate is about to ask for, immediately before asking. Running the builder's own drop-topic sabotage — which the builder framed as a control proving the instrument can fail — is in fact the measurement of the shipping build, and it stops at Q-MAIN-01 with 0 journal entries. MEASURED VALUES BY ARTIFACT: corpus/90-verdicts/wave1/artifacts/W1-19-r1/mainline-findability.json — live cold boot: main quests offerable 0/32, blocked on the topic gate 32/32, topics known at a cold start 0 \|\| corpus/90-verdicts/wave1/artifacts/W1-19-r1/mainline-topic-supply.json — supplied only by itself 23, no supplier at all 5, supplied by something else 4 \|\| corpus/90-verdicts/wave1/artifacts/W1-19-r1/mainline-trace.drop-topic.json — 'Q-MAIN-01: could not open — the topic "the drowned tally" has not come up yet', 0/29 and 0/17, 0 journal entries \|\| corpus/90-verdicts/wave1/artifacts/W1-19-r1/world-coupling.json — coupling 0 on opens_by.topic, opens_by.overheard_from, q.directions and the entry_topics edges \|\| game/src/core/topics.js — landed UNTRACKED during this measurement and states the same finding independently; it is imported by game/src/character/converse.js and NOT by game/src/sim/quest/gate.js or game/src/sim/quest/machine.js, so the fold does not reach the quest gate at commit be17753 | Two edits, in this order. (1) Import `topicKey` from game/src/core/topics.js into game/src/sim/quest/gate.js and fold both sides of the two topic comparisons (line 151 prerequisite_topics, line 153 opens_by.topic) and the topicsKnown set built in machine.js context(). That alone lifts the world-suppliable count from 2/32 to 27/32 in spelling terms. (2) Re-author the AddTopic graph so it points FORWARD: each main quest's opening topic must be granted by an earlier quest's journal entry, a book, or a rumour — never by its own index 10 — and Q-MAIN-01 must have a world-side entry that owes nothing to a prior quest, which means >= 1 rumour line in game/data/dialogue/rumours.json in a settlement other than Soulrest naming the Drowned Tally, plus greeting or topic content on the three NPCs already declared in Q-MAIN-01's opens_by.overheard_from, and a consumer for that field. → **node tools/quests/mainline-findability.mjs reports `main quests offerable` >= 1 and `blocked on the topic` <= 31 at a cold start with nothing learned through the harness, AND a played run of tools/quests/mainline-trace.mjs with every H.learnTopic() call removed completes 29/29 on the intended chain and 17/17 on the backpath. Equivalently: mainline-topic-supply.json reports supplied_only_by_itself = 0 and no_supplier_at_all <= 1 (Q-MAIN-01, whose supplier must be a rumour rather than a quest).** |
| `GAP-W1-map-fix-drops-the-journal-from-the-page-walk` | `input.modality.parity` | major | 0 | w1 / w1-map-defects-r1 | — | Moving `map` to second in `UISystem.WALK_ORDER` put it immediately before `journal`, and `map`'s own navigable ring excludes `journal` (NEVER_ADJACENT, enforcing RI-UIX04 Q7). The forward page-walk from the inventory therefore steps over the journal and never returns: inventory -> map -> sheet -> spells -> wait -> (closes). Before the fix the same walk visited all five peer screens and reached the journal on the first press. The journal now costs three page-turns WITH a direction reversal on desktop and eight taps with a reversal on a phone, and swap_left from the inventory closes the menu rather than paging backwards, so the reversal is not something a player discovers by accident. The map went from four presses to one; the journal went from one press to off the forward walk entirely. | Separate `map` and `journal` by at least one screen in `UISystem.WALK_ORDER`, keeping `map` second. `['world', 'inventory', 'map', 'sheet', 'journal', 'spells', 'wait', 'levelup']` is measured and works: the forward walk becomes inventory -> map -> sheet -> journal -> spells -> wait, the map stays one page-turn from the screen `menu` opens, and nothing is skipped. The constraint is structural rather than cosmetic and belongs in a comment beside the constant: any two screens in NEVER_ADJACENT must not be neighbours in WALK_ORDER, because the ring is filtered per-mode and an excluded neighbour is stepped over rather than stopped at. Add the assertion to tools/harness/map-probe.mjs so a future reorder fails a check instead of a player. → **`node corpus/90-verdicts/wave1/artifacts/w1-map-defects/walk-reachability.mjs` exits 0 with `SKIPPED by the forward walk: nothing` for the shipped WALK_ORDER, and the fewest-turns line still reads map=1.** |
| `GAP-W1-map-the-audit-is-computed-and-never-published` | `platform.save.persistence` | blocking | 0 | w1 / w1-map-r2 | — | Discovery.restore() computes a seven-field audit of the loaded footprint against the blob's own claims — dropped, phantom_cells, missing_cells, claimed_places, derived_places, stood_cells, legacy — and every published surface throws it away: save/state.js:676 keeps only .dropped and mapState() publishes only dropped_on_load. ARBITRATION S38 clause (d) makes 'a discrepancy between the blob's claims and the derivation is ITEMISED, not swallowed' a hard fail, and the S38 gate's own assertion for it (S38-4) cannot see this gap, because S38-4 forges the CLAIM FIELDS — an all-ones raster and all 42 names — and so always produces an itemisation to read. The forgery a forger would actually write supplies no claims at all: with cells:"" and places:[] the two fields S38-4 reads are both ZERO on a save that fills the entire province, while missing_cells reads 42,846 and derived_places reads 42. | (1) PUBLISH THE AUDIT: carry the whole lastRestore record onto sim in save/state.js (which currently keeps only .dropped at :676) and surface it through Engine.mapState() and UISystem.state()'s `map` block, alongside the existing dropped_on_load. (2) EXTEND S38-4 TO THE SILENT FORGERY: add an arm to tools/map/arbiter-map-s38.mjs that forges `stood` with cells:"" and places:[] and requires at least one moved audit field to be readable from the published surface — as it stands, clause (d)'s gate passes on the one save shape a forger would write. (3) REPLACE map-probe L3, which forges world.discovery.places and is green; S38 already names S38-3 as its replacement. (4) FREEZE THE SLOT: Object.defineProperty(sim, 'discovery', {value: d, writable: false, configurable: false}) after construction — one line, and it closes clause (c) as well. → **node tools/harness/critic-map-r2.mjs reports A5 and A13 green: on a forged footprint with cells:"" and places:[], at least one of missing_cells / stood_cells / derived_places is readable through mapState() or getUIState().map. AND node tools/map/arbiter-map-s38.mjs still reports 6/6 with the new silent-forgery arm included, and --control=pre-s38 still exits 1. AND node tools/harness/critic-map-r2.mjs reports Q1/Q2/Q3 green: the descriptor for sim.discovery is writable:false configurable:false and a substitution attempt through questEngine.sim or window.__ENGINE draws 0 squares.** |
| `GAP-W1-mark-teaches-nothing-to-the-journal` | `journal.entry.voice` | blocking | 0 | w1 / W1-READABLES-r2 | — | Looking at a mark writes a know: flag and no journal entry. journal [10] -> [10] on all four browser legs, before and after. The person channel writes both (37 entries from play); the environment channel writes one. | Author deceit.revealed_by[].journal for the 16 demanded environment rows against the prose, the way W1-18 hand-picked its 37. The schema field, the note() pre-checks and the check-quests guard all already exist; this is authoring, not building. → **reveal-route-audit section E reports a journal write on every one of its 15 legs, not zero,mark-route-world shows journal [10] -> [10, n] on all 4 browser legs,check-quests stays green and its self-test stays 10/10,falsify the indices away and the journal counts return to zero** |
| `GAP-W1-pbr-material-set-unbound` | `render.fidelity.materials` | blocking | 0 | w1 / w1-visual-blind-protocol-a-r1 | `crit-w1-orphaned-surface-shaders-r1-3c19` (PROVISIONAL) | Across five blind pairs, four of five judges described our surfaces as flat untextured fills with no albedo/normal/roughness response — three of them named it as the single biggest gap, all three at CONFIDENCE: high. In the judges' own words: 'every surface reads as flat diffuse albedo with no specular, roughness variation, or normal-mapped relief, so thatch, plaster, stone and grass all respond to light identically'. The mechanical corroboration is independent of the judges: image-leakcheck's local_contrast_med statistic names the reference on 5 of 5 pairs. | Author and bind a real PBR material set — albedo + normal + roughness/metalness — to the terrain, architecture and foliage materials, so different surfaces produce different specular and micro-shading response under the same light. This is R1 of the ranked, buildable remedy list in corpus/90-verdicts/wave1/W1-VISUAL-BLIND-PROTOCOL-A-r1.md §3; R2 (contact shadows + AO), R3 (ambient/GI fill with tone-mapped shadow lift) and R4 (ground-plane dressing) follow it there with their own acceptance criteria, and R5 (depth of field) is listed there explicitly TO BE STRUCK because the only evidence for it was manufactured by the pair02/pair03 bokeh confound. → **Two named materials in a single capture from the fixed eight slots produce measurably different specular response under the same light; and a wave-2 Protocol A re-run (counterbalanced sides, clean judge context, pair02/pair03 rebuilt against an in-focus plate) returns 'everything responds to light identically' as no pair's SINGLE BIGGEST GAP.** |
| `GAP-W1-platform-prng-never-drawn` | `platform.determinism.harness` | blocking | 0 | w1 / w1-00 | `crit-w1-00-r2-k4m9` (FAIL), `crit-w1-09-r2-3c19` (FAIL), `crit-w1-09-r3-3c19` (FAIL), `crit-w1-deploy-r2-3c19` (FAIL), `crit-w1-30s-r1-3c19` (FAIL) | The seeded PRNG is reseeded correctly and never drawn from. rng.draws is 0 on every one of 3600 frames in every scenario, state and seed measured. Seeds 1337 and 4242 over a 3600-frame duel differ in exactly one field — .rng.seed, the echo of the input — and in zero simulation fields (r4-seed-sensitivity.txt: 'frames differing in ANY field other than .rng.seed: 0'). RI-MTH02 R4's 5%-of-frames clause is therefore satisfied at 100% by that echo alone, so the rung, tools/harness/determinism.mjs, and any future critic reading either will report seed sensitivity on a build that has none. The build's own ladder prints 'PASS R4 — seed-sensitive ... frames_differing_pct: 100'. | (1) Route at least one simulation quantity through rng.next() inside the fixed step and emit the draw count truthfully — the enemy idle-loop phase offset chosen at spawn is sufficient, cheap, and is exactly the field AM-W1-00-01 wants excluded from R5, so seeding it converts that argument into a measurement. (2) Change tools/harness/determinism.mjs's R4 computation to drop the rng.seed field before counting differing frames, and to fail the rung with reason 'prng_never_drawn' when max(rng.draws) == 0 in either run. (3) Adopt orchestration/amendments/AM-W1-00-C1-mth02-r4-discriminator.md, which states the sharpened rung and moves no threshold. → **Re-run node tools/harness/trace.mjs --scenario cmb-duel-infantry at seeds 1337 and 4242 and diff field-by-field: max(rng.draws) > 0 in both runs, and >= 5% of frames differ in at least one field OTHER than .rng.seed. The second number is currently 0.0% (0 of 3600 frames) and is recorded in r4-seed-sensitivity.txt.** |
| `GAP-W1-platform-save-drops-entity-prev-state` | `platform.save.persistence` | blocking | 0 | w1 / w1-00-r2 | `crit-w1-save-r1-live-audit` (FAIL), `crit-w1-map-r2-a91f` (FAIL) | The save does not carry enemies[].prev_state. RI-JRN05 M5 run as written — identical 120-frame pre-roll on both sides, identical 600-frame script, seed 4711, control vs loaded — gives control body_sha256 5d0830f9b9e3ea71… and loaded d52edb76370c7c08…, differing on 219 of 600 frames in exactly that field: control "IDLE", loaded null, from frame 0 onward. The serialised entity record carries state, anim_frame, anim_phase0 and state_entered_ago_frames and stops there. world.entities is a durable path in game/data/save-manifest.json and prev_state is on no volatile list, so manifest rule V1 makes it a defect by the manifest's own terms. The same probe against round-1 commit 8714e38 reproduces it exactly, so it is pre-existing and was missed by both the build and the previous verdict, which recorded M5 as passing. | Serialise prev_state in the entity record next to state and state_entered_ago_frames and restore it in the load path; add prev_state to the World group field list in game/data/save-manifest.json so RI-JRN05 M4's two-directional set-difference covers it; and extend the R9 rung and RI-JRN05 M5 to compare the full field set of the control and loaded traces rather than only body_sha256 of a window, printing the differing field names — round 1's M5 run passed precisely because it did not. → **Re-run RI-JRN05 M5 (or corpus/90-verdicts/wave1/artifacts/W1-00-r2/p10-m5.mjs) at seed 4711 and at one other seed: the control and loaded traces must have identical body_sha256 after re-basing the absolute frame indices, and the field-level diff must be EMPTY. Today it is {"enemies[].prev_state": 219} of 600 frames.** |
| `GAP-W1-population-save-reload-repays-every-corpse` | `combat.encounter.placement` | major | 0 | w1 / w1-population | `critic-w1-souls-r2` (PASS), `critic-w1-souls` (FAIL) | Engine.applySave() and Engine.loadState() both call PopulationSystem.reset(), which sets all 144 posts back to DORMANT, while SoulsSystem.reset() empties the eid ledger the S5 epoch gate is keyed on and death.ordinaryRespawnEpoch never moves. Measured at aaa2f1f on pop-0001, with no hearth rest and no harness respawn verb anywhere: L1 kill 2 bodies -> +98 souls; L2 CONTROL walk out past release_radius_m and back -> post stays CLEARED, 0 bodies, delta 0 (correctly refused); L3 saveState -> loadState -> the post re-materialises with 2 bodies -> kill again -> +98 souls with the epoch unmoved at 0. Repeatable without bound. | On applySave, restore each post's CLEARED/DORMANT state from the save instead of resetting all 144 posts to DORMANT — which is also what a player expects, since a reloaded save should not hand back a province they had already cleared. If persisting post state is out of scope for round 2, the narrower alternative is to have SoulsSystem's first-sight seed record a body as SETTLED at the current epoch when its post was recorded cleared in the save. → **Re-run tools/world/critic-population-r1.mjs --arms C3. All three legs must hold: L1 delta > 0 (a first kill pays), L2 delta == 0 (walking away and back is refused, as it is today), and L3 delta == 0 with death.ordinaryRespawnEpoch unmoved — where it is +98 today. Then a hearth rest must still re-arm: restAt() bumps the epoch and the next kill of the same post pays again.** |
| `GAP-W1-provstream-arrival-burst` | `platform.load.hitches` | blocking | 0 | w1 / w1-01-province-stream-pump | — | The urgent tile-build branch in Engine._streamProvince() is exempt from the one-unit-of-work-per-step budget AND is evaluated BEFORE it, so any frame on which the body ARRIVES somewhere - a respawn, a teleport, a future fast-travel or cutscene relocation - pays a ~30-tile release plus updateSkin (15,625 vertices) plus updateCover plus updateNear plus two full _buildTile calls in one fixed step. | Make the urgent budget respect the work the frame has already done, and declare the arrival as a load boundary. → **node tools/world/critic-prov-r1.mjs --mode displace reports worst_step_ms_after_jump <= 120 on a box at loadavg < 4, with frames_standing_on_no_ground still 0 and frames_until_near9_complete <= 16; --mode ride reports over_16_7 <= 1% of steps; --mode fight reports iframe_frames_over_16_7 = 0; --mode loop unchanged.** |
| `GAP-W1-quest-givers-not-in-the-world` | `quests.mainline.acts` | blocking | 0 | w1 / w1-19 | — | Nine of the ninety-four quest givers in this build exist anywhere a player can stand, and none of the thirty-five quests behind the '10 of 35' findability headline has a giver in the world at all. QuestEngine.open() has no presence or proximity term, so a quest reports ok in a world containing nobody to say the keyword to. | — → **—** |
| `GAP-W1-questionnaire-route-is-unreadable-and-unnamed` | `character.creation.flow` | blocking | 0 | w1 / w1-07-r2 | `crit-w1-07-r3-3c19` (FAIL) | The questionnaire — RI-CHR01 §4 Route C, RI-JRN01 O9's mandatory route — reaches the player with neither its questions nor its answer. (a) buildCensusModel() at game/src/character/scene.js:267 sets `line: state.line` and never reads `state.question.text`, so all ten dilemma nodes draw one identical stock transition line above four unattached answers: 10 distinct question texts in the model, 1 distinct drawn line, 0 of 10 reaching the frame. (b) Across 240 runs covering all 10 races x 4 upbringings x 8 answer permutations with a verified even answer histogram (600 each of a/b/c/d), the route produced a named class 0 times out of 14, against RI-CHR01's pass floor of >= 6 and its 0-band of < 6. | 1) In buildCensusModel(), when state.input.kind === 'questionnaire', set line: state.question.text and move the scribe's transition line to the aside slot (or to the writ.class-routes answer). The model already carries the text; this is one line. 2) In character/questionnaire.js's scoreAnswers, make the named-class match reachable: compare the resulting 3-primary/2-secondary skill set against classes.json's fourteen with a tolerance (>= 3 of 5 shared skills plus a matching family) rather than requiring an exact set — census.js already has on_match_named_class and %ClassName wired for the moment it succeeds. 3) In CensusSurface.sync(), filter a pick node's options against attributes already taken by earlier pick nodes so writ.class-custom-neglected cannot offer a favoured attribute, and wrap _censusApplyPending() so a refused answer can never throw out of the frame step. → **getCensusState().surface.rendered_text contains getCensusState().question.text at 10 of 10 questionnaire nodes and the 10 nodes yield 10 distinct drawn lines; a 240-run sweep over all 10 races x 4 upbringings with an even a/b/c/d answer histogram reaches >= 6 distinct named class_id values (>= 10 for the item's 10 band) and the Warden-Scribe's on_match_named_class line fires on every named match; and the full custom-class route completes on gamepad alone with 0 exceptions out of stepFrames.** |
| `GAP-W1-rendered-text-accessor-blind-to-the-hud-surface` | `journey.opening.legibility` | blocking | 0 | w1 / w1-26-r1 | `crit-w1-26-r2-3c19` (FAIL) | The build's rendered-text accessor — `__HARNESS.getRenderedText()`, fed by render/text-register.js — is STRUCTURALLY INCAPABLE of seeing the one surface RI-JRN01 M9 and M15 are aimed at, and the build's own report records a pass over an empty set as a result. render/render.js:90 carries the comment 'Every 2D surface this renderer owns is registered against the one rendered-text register' and then instruments two of three: `this.ui` as 'dialogue' and `this.title` as 'title'. The third, `this.menus = new UISurface(...)` at renderer.js:86, is W1-21's HUD and menus — which is precisely M9's domain, 'every string rendered OUTSIDE a dialogue/journal/book surface'. FALSIFIED, not inferred: I instrumented the menus context at runtime through the register's OWN public instrument() and forced a repaint, and got ZERO entries. Then, in the same freshly-instrumented context, I drew one sentinel through ui/glyphs.js drawText() — the path every string in that surface takes — and one through ctx.fillText(). The register saw the fillText sentinel (1 row) and did not see the glyphs sentinel (0 rows). glyphs.js renders every glyph as a stroked Path (moveTo / lineTo / quadraticCurveTo / stroke) and never calls fillText or strokeText, which the register wraps. So the hole cannot be closed by calling instrument() — the register is blind to that surface by construction. Enumerated directly through UISurface.elements[].text, the opening draws five strings the accessor cannot see: '5', '0', 'LIGHT', 'Spark-Dart' and 'Take A tithe-gourd, empty'. reports/journeys/w1-26-opening.json records `m9.surfaces_searched: 'title (the only non-dialogue drawn surface in the build)'` — which is false — with `distinct_searched: 0` and `hits: []`, and the piece reports M9 as scoreable for the first time on that basis. | Make the register see the third surface, then re-run M9 and M15 over it. Two routes, either acceptable. (a) Give ui/glyphs.js drawText() a register call of its own: it already holds the string, x, y, the face and the size at the top of the function, so one `textRegister.note(ctx, s, x, y, size)` before the glyph loop puts it in the same register with the same clip shadow. (b) Have UISurface.el() route every record carrying `text` through a shim that the register wraps, so the tag stays on the element rather than being inferred. Route (a) is smaller and catches anything that draws glyphs, including surfaces nobody has written yet; route (b) is safer against a future surface that draws text some third way. Whichever is taken, `getRenderedText()` must stop advertising `surfaces_instrumented` as a static array and derive it from the contexts actually wrapped, and tools/harness/w1-26-opening.mjs must FAIL rather than pass when its M9 grep searches zero strings — a probe that cannot go red is worse than no probe. Separately, remove or rewrite the interact prompt so it names the object without commanding the player ('A tithe-gourd, empty' rather than 'Take A tithe-gourd, empty'). → **On a frame of the opening where UISurface.elements[].text is non-empty, `__HARNESS.getRenderedText({notSurface:['dialogue']})` returns a distinct set that CONTAINS every one of those strings (set equality, not merely non-empty), and the register reports them with a clip flag. Then M9's grep over that set returns 0 hits and M15's returns 0 hits, and tools/harness/w1-26-opening.mjs exits non-zero if `m9.distinct_searched === 0`. Falsification required in the same run: draw a sentinel through ui/glyphs.js drawText() and assert the register sees it, exactly as this verdict's probe asserts it currently does not.** |
| `GAP-W1-road-join-has-no-gate` | `world.traversal.roads` | blocking | 0 | w1 / W1-ROAD-JOIN | `critic-w1-crossing` (FAIL) | The road/settlement join is a BUILD-TIME consumption with no gate. game/data/world/roads.json carries the answer; planSettlement() produces the question; nothing re-asks it and nothing notices when it changes. The check that would notice exists, is offline, runs in under a second, already exits non-zero on failure, and is run by nothing. The join went stale twenty-four minutes after it was cut and has been stale for every commit since. | Four steps, in order. (1) Run `node tools/world/build-roads.mjs` and commit game/data/world/roads.json — verified on a scratch copy of the tree exactly as it stands (R-REMEDY in reports/critic-road-join/consume.json). (2) Add `node tools/world/road-through-building.mjs` to the gate set by editing the SOURCE LIST the INDEX.md gate table is generated from, not the table. (3) Change road-through-building.mjs to plan with the interiors — planSettlement(doc, interiors), or the union of that and planSettlement(doc, {}) — so the gate asks the game's question rather than the check's; at HEAD the difference is 6 blocked samples and one whole building (blackrose-house-1). (4) Replace road-through-building.mjs --self-test's hard-coded stormhold-scribe subject with a run-time search over every building x every leg point for the nearest (building, road point) pair, perturb THAT through the same path the game uses, and require flag / restore-to-baseline; tools/world/critic-road-join-ingame.mjs --self-test is a working implementation of exactly this. → **On a clean tree at the commit that lands the remedy, ALL of: (i) `node tools/world/road-through-building.mjs` exits 0 with 0 of 10 legs blocked and 0 named-route offences; (ii) the same question asked with all 115 interiors loaded also returns 0 of 10 and 0 blocked samples; (iii) `node tools/world/critic-road-join-ingame.mjs` returns 0 legs inside a game footprint and 0 samples closer than PLAYER_RADIUS_M = 0.32 m to a settlementSolids() wall slab; (iv) `road-through-building.mjs --self-test` exits 0 ON THAT FIXED TREE; (v) the check is named in the generated gate list and a deliberate 1 m edit to any settlement offset_m, committed without a road rebuild, makes the gate fail.** |
| `GAP-W1-save-purse-has-no-writer` | `journey.save.roundtrip` | blocking | 0 | w1 / W1-SAVE-r1 | — | The gold a player can actually spend is held in `combat.world.gold` (rebuilt by `Engine._buildCombat`) and `sim.stealth.p.gold`, and neither is ever written back into `sim.progression.gold` — the only gold register the save carries. On 3 of the 38 shipped named states (helstrom-market, rootlands-well-graph, stormhold-street) a plain save/load destroys 600 gold, on 10 of 10 seeds, and it survives a real cold reload through IndexedDB as well. Every shipped instrument reports clean while it happens: M1 hash equal, M2 diff empty, M4 set difference empty both ways, `getDurableFieldCensus().ok` true, and the M5 trace byte-identical. Falsified with the null control — blanking `progression.gold` and `fight.loadout.gold` out of the blob produces exactly the same world as leaving them alone, which is CONSUMPTION coupling 0 on a field this very repair added to the manifest. | Make one register the authority for the purse and give the save that register, then widen the census so the class of defect cannot recur. (a) In `game/src/engine.js`, have `setGold`, `combat/parley.js applyAccept` (`world.gold -= spent`), `travelRide` (`world.gold -= fare`), `fenceSell`/`bribeWitness` (`stealth.p.gold`) and the capture path all write through to `sim.progression.gold`, and have `_buildCombat` read only from it — or, if the three registers must stay, serialise `combat.world.gold` and `sim.stealth.p.gold` as their own manifest paths under Progression with the same rebase discipline the rest of the group uses. (b) Extend `Engine.getDurableFieldCensus()`'s `shot()` to clone `this.combat.world`, `this.travel`, `this.crime`/`sim.stealth`, `this.conversation` and `sim._traversal` — it currently walks a declared subset and its own comment warns that 'a census that walks a subset of the simulation is an instrument that certifies a subset'. (c) Add a save point with money in it to `tools/journey/state-diff.mjs`'s pre-roll, so the sweep can fail. → **On helstrom-market, rootlands-well-graph and stormhold-street, at ten seeds: `getTravelState().gold` and `getCombatState().world_knowledge.gold` are unchanged across `saveRoundTrip()` AND across a `writeSave` + page reload + `readSave`; and `node tools/harness/save-break-critic.mjs` reports `no-gold` as `detected_by_any_shipped_instrument: true` — that is, blanking the purse out of the blob now makes at least one of M1, M4 or the census go red.** |
| `GAP-W1-settlement-has-no-outside` | `world.settlement.anatomy` | blocking | 0 | w1 / W1-04-r2 | `crit-w1-04-r3-3c19` (FAIL), `critic-road-join` (FAIL), `crit-w1-04-r4-3c19` (FAIL), `crit-w1-04-r5-3c19` (FAIL) | Round 2 built the inside of 115 buildings and none of the outside of 202. settlement.buildings is read at exactly two sites in game/src: the door table at sim/settlement.js:52 and a .length at engine.js:7749. Each town declares 8-10 bespoke architecture meshes and one silhouette element; 4 per town have a builder (32 of 69) and every one of the 32 is placed INSIDE a room. The 260 townspeople this round put outdoors stand on bare terrain and VP04 is a picture of ground. | Draw the settlement from settlement.buildings: a mesh per building placed at its offset_m and yaw_deg, drawn from that town architecture_kit.meshes, with the door table already in place as the entry points. Give the draw call a cut arm so the before-picture can be produced on demand. → **A probe standing at each of the eight settlement centres and reading the SCENE GRAPH reports >= 8 building meshes drawn per town and 202 of 202 across the province, with a --control arm that cuts the draw call and reports 0; the two arms must differ.,Removing five buildings from one settlement's `buildings` list changes the drawn count by five and restores when put back; perturbing architecture_kit.meshes changes which meshes appear.,A pixel sweep of one fixed camera pose at all eight town centres returns 8 distinct images; the control arm returns 1.,VP04-settlement-street contains a building, and the verdict states plainly whether a capture through VP04 may be cited as evidence of a settlement.** |
| `GAP-W1-seven-tenths-of-the-indoor-floor-is-at-the-top-of-the-light-scale` | `stealth.detection.model` | blocking | 0 | w1 / w1-15 | — | On the round's own 16,996-cell chest-height grid over all 115 interiors, 12,210 cells (71.8%) read L = 1.0000 at noon clear and 10 rooms are entirely flat at 1.0000. Under round 3's flat 0.04 ambient the same lamps give 11,800 (69.4%) and 7 flat rooms, so this is a standing defect of the LAMP SCALE and not a regression introduced by this round's ambient — the derived ambient costs 2.4 percentage points at noon clear and 0.3 at 03:00. The round measured the same defect at ROOM granularity and published it as '11 of 115 rooms are flat at noon', which is the same fact one order of magnitude too small. The four-arm comparison: round 3's flat 0.04 gives 69.4% and 7 flat rooms, round 4 derived at noon clear 71.8% and 10, at noon overcast 71.1% and 10, at 03:00 overcast 69.7% and 7, and the round-3 critic's arm-01 control at 0.75 gives 94.4% and 63. archon-apothecary is 50 of 50 cells at L = 1.0000 with an ambient of 0.2264 in a 9.83 x 4.98 m room with 8 lamps. | Re-derive authored_intensity_to_L_scale and reach_m in game/data/stealth/detection.json on the JOINED corpus, exactly as this round re-derived window_daylight_k. RI-STL01 §3's 'within 4 m of a flame' (L 0.60) and '4-9 m from a flame' (L 0.25) rows were transcribed against rooms that applyInteriorBounds() now shrinks to a fifth of their declared area, so the same scale that reads 0.60 at 4 m in a 13.6 x 15.6 m shop saturates a 9.83 x 4.98 m one. Then add the SATURATED-CELL FRACTION to tools/harness/w1-15-r4-lights.mjs's discrimination table beside mean_spread, because a room with one dark corner and forty-nine clamped cells currently scores as discriminating. → **On the same 16,996-cell chest-height grid over all 115 joined interiors, fewer than 40% of cells read L >= 0.9999 at noon clear, and no more than 3 rooms are entirely flat — against 71.8% and 10 today. The delete-the-fix control must bring 71.8% back.** |
| `GAP-W1-souls-alive-keyed-on-eid` | `progression.souls.economy` | major | 0 | w1 / w1-souls-r2 | `critic-w1-souls` (FAIL), `critic-w1-attr-scale` (FAIL) | SoulsSystem._alive is keyed on the eid and nothing in the build is authorised to clear it. One bug, two faces: every probe in this tree (HF-1) and every player (HF-2). Both were produced by the round-2 epoch gate, which is otherwise a correct and well-evidenced fix. | (1) In engine.js applyNamedState(), beside the existing stealth and population re-seeds: `if (this.sim.souls) this.sim.souls.reset();`. (2) In sim/souls.js, stop keying the gate on the eid alone — have spawnEncounter stamp a spawn generation on the entity and record `{ alive, paidEpoch, gen }`, re-arming when the generation moves OR the rest epoch moves. That keeps round 1's HF-3 farm shut while restoring §4's x1.00 to a body that was genuinely built again. → **Two arms in souls-consumption.mjs, neither able to pass while the other fails under a delete-the-fix. (a) An UNTAGGED fight, killed, crossed over loadState('<named>'), killed again, must pay both times. (b) A post of n bodies with n-1 killed, despawned wholesale and re-spawned under the SAME tag, must pay for all n-1 recycled bodies on the second visit AND must still pay nothing for a body that was never despawned and never rested past. tools/progression/critic-souls-r2.mjs arms C4 and C5 are those two arms.** |
| `GAP-W1-souls-world-copy-of-the-values-is-53pc-stale` | `progression.souls.economy` | major | 0 | w1 / w1-souls-r3 | `critic-w1-attr-scale` (FAIL) | Round 3 re-derived seven enemy statblocks. game/data/world/population-posts.json is GENERATED and caches a souls figure per post plus by_tier, by_region and a crossing summary, all computed against the OLD values, and nothing re-ran it. The world file publishes 16,335 souls for the 267 road bodies where the shipped statblocks pay 10,679 (+53.0%), tier 1 570 against 374, tier 5 5,015 against 3,286, and a crossing headline of 'level_if_fully_cleared: 5' where the true figure today is level 3. | Add a WORLD row to tools/check-souls-corpus.mjs, or a new tools/check-souls-world.mjs: walk game/data/world/population-posts.json, recompute every post's `souls` from game/data/world/encounters.json x the shipped game/data/combat/enemies/*.json, recompute report.souls / report.by_tier / report.by_region / report.crossing.souls / report.crossing.level_if_fully_cleared from those, and fail on any drift. The arithmetic already exists — placedByTier() in tools/progression/derive-soul-values.mjs does exactly this walk against the same three files. Only the comparison against the file's own cached copy is missing. Then W1-POPULATION re-runs tools/world/build-population.mjs so the cached copy is true, and the check is armed AFTER that regeneration, never before (RULES 13). → **Three legs, and the middle one is the delete-the-fix. (a) On the tree as it stands today the new check goes RED and names population-posts.json report.souls 16,335 against 10,679 recomputed, plus the five by_tier rows and crossing.level_if_fully_cleared 5 against 3. (b) Change any one statblock's `souls` field by 1 and the check goes RED again, naming that statblock's posts — proving it reads the statblocks and not a second cached copy of its own. (c) Re-run tools/world/build-population.mjs and the check goes GREEN, with `derive-soul-values --census` reporting the same placed-world total as the world file's own report for the first time.** |
| `GAP-W1-stealth-crime-model-not-coupled-to-the-world` | `crime.witness.model` | blocking | 0 | w1 / w1-15 | `crit-w1-15-r2-3c19` (FAIL), `crit-w1-15-r3-3c19` (FAIL), `crit-w1-15-r4-3c19` (FAIL) | The stealth and crime models are computed beside the world instead of by it, and the coupling is measurably zero at the two places the reference items make load-bearing. (1) DETECTION: an INFANTRY at 8 m reaches alert_state AGGRO at frame 30 — 0.500 s — identically at V = 0.6669 (midday, standing, heavy armour, Sneak 5) and at V = 0.0500 (unlit, crouched, in cover, Sneak 100), with identical alert curves of 40/80/100 at f=10/20/30. RI-STL01 2's own table says those two configurations are 1.03 s and 26.7 s apart. The alert meter fills at a flat +4/frame whenever the player is inside the archetype's sight radius and cone, and never multiplies by V; the control at 30 m confirms a live entity is being driven (alert stays 0). Coupling = 0.00. The same is true of the sound model: r_effective computes to 36.31 m at the loud end and a civilian 2 m away, facing away, registers suspicion 0.00 after 10 s of sprinting in heavy armour across dry reed. (2) WITNESSES: a civilian standing 3 m from the player in full daylight, at contextWeight 3.00, already at civ_state ALARM with suspicion 100, watches takeObject() succeed on an owned object. getCrimeState() returns witnesses: [], the trace reports witnesses_pending 0, no witness event fires, and the bounty is still 0 five seconds later. The crime record exists with landed: false and can never land. Every witness that appears anywhere in this build was created by a critic calling addWitness(crime_id, {eid, identified}). The civilian suspicion machine — which IS wired, and which I measured responding correctly to light (10.4x), context weight (1.86x), distance, sight cone and raceSuspicion (1.49x and 1.69x) — and the crime ledger are two correct systems in the same process that never speak to each other. | Two call sites, both inside code this piece owns, plus one trace field. (A) game/src/sim/entities.js, the perception branch: replace `e.alert += 4` with the shipped model — `e.alert += baseFill(sim.stealth.p.V, d, e.sight_radius_m) / 60` from game/src/sim/stealth/detection.js, with the peripheral cone scaled by 0.45 as RI-AI01 §B specifies; add the `alert_channel` field ('sight'\|'peripheral'\|'hearing') to the enemy trace record so a light failure can be told from a sound failure; and route the continuous sound radius (soundRadiusFor) into the same fill on the hearing channel. (B) game/src/sim/stealth/system.js, in the fixed step: whenever a crime record is created (takeObject, lockBegin, a blow on a non-hostile, standing over a corpse), iterate this.civilians and any guard entities and call this.crime.witness(crime.id, ...) for each whose civ_state advanced to CHALLENGE or ALARM this frame with V >= 0.10 and dist <= 1.4 x R(npc), setting identified from V >= 0.30; then drive reportRoute() from that entity's own path to the nearest guard entity and flip it to the fleeing behaviour. → **(A) RI-STL01 method 2 run exactly as written — one INFANTRY facing the player at 8 m, the five configurations of §2, first frame of alert_state == 'AGGRO' — lands within 5% of {1.03, 3.94, 8.5, 26.7, 26.7} s, and method 4 (crouched, unlit, still, 1.0 m behind a stationary enemy, 3,600 f@60) ends with alert == 0.000. (B) RI-CRM01 method 3 passes end to end with ZERO addWitness() calls in the script: a `witness` event on the crime frame, reported == false, bounty 0 until a `report` event fires, and the 120 m run landing within 10% of 35.3 s. Both are re-measurable with `node tools/harness/critic-w1-15.mjs`, whose RI-STL01-M2-enemy-detection and RI-CRM01-M3-witness-from-the-world blocks are written for exactly this closure; today they read coupling 0.00 and witnesses [].** |
| `GAP-W1-the-building-is-smaller-than-the-room` | `world.settlement.anatomy` | blocking | 0 | w1 / W1-04 | `critic-road-join` (FAIL), `crit-w1-04-r4-3c19` (FAIL), `crit-w1-04-r5-3c19` (FAIL) | 54 of 202 exteriors are shrunk to fit plans authored tighter than the interiors declare themselves to be wide, while interior bounds_m is defined as the DECLARED footprint on 115 of 115. 41 of 112 enterable buildings therefore draw an exterior smaller than the room behind their door; blackrose-inn draws a 3.4 x 3.9 m shed over a 13.6 x 15.6 m inn. | Repair the conflict in the DATA, not the renderer. (1) Re-derive settlement.buildings[].offset_m so nearest-neighbour spacing is at least the sum of the two declared half-footprints plus a 2 m street, in the five tight plans (archon, blackrose, lilmoth, soulrest, stormhold), re-derived WITH RI-WLD03 R4's legibility rule rather than hand-nudged. (2) OR, if the plans are canon, shrink the interiors to match, moving bounds_m and continuity.exterior_footprint_m together, which also gives RI-WLD13 N1 a ratio that can fail. (3) Add a rule to tools/check-data.mjs asserting drawn_footprint_m >= bounds_m on every enterable building. → **`node tools/world/w1-04-r3-exterior.mjs --offline` reports 0 buildings whose drawn footprint is smaller than their own interior's bounds_m (today 41 of 112) and 0 residual deep overlaps (today 12); `node tools/world/w1-04-r3-collision.mjs` §C reports 0 people in solid masonry at 03:00, 09:00 and 20:00 (today 1, 2, 0); and `node tools/check-data.mjs` exits 1 naming the record when a single footprint is perturbed and 0 when it is restored.** |
| `GAP-W1-the-door-puts-you-inside-the-building` | `world.interior.named` | blocking | 0 | w1 / W1-04 | `crit-w1-04-r5-3c19` (FAIL) | buildings[].door is the building's own centre on 112 of 112 (max door-to-centre distance 0.00 m), and every declared continuity.exterior_spawn lies inside its own building's wall box - settlementSolids() centres the four wall slabs on b.x,b.z with half-extents w/2,d/2, so this is not a matter of interpretation. Live, through the engine's own exitInterior() and buildingAt(), 39 of 40 doors tested leave the player inside the building they just left, 35 of them by more than a metre of clearance. Round 4's roof fix, which is correct, closes the lid. | — → **—** |
| `GAP-W1-the-doorstep-cannot-get-you-back-in` | `world.interior.continuity` | blocking | 0 | w1 / W1-04 | — | Pressing `interact` on the doorstep you were just put on — the real latch, sim/settlement.js#stepSettlement() reading input.pressedName('interact') and handing sim.door.interior to useDoor() — returns you to the room you left on 96 of 115 doors. On 10 it opens a DIFFERENT building (blackrose-house-0 -> blackrose-house-1 at 0.50 m, blackrose-inn -> blackrose-warders-hall 0.50, blackrose-market -> blackrose-smithy 0.50, blackrose-rootpost -> blackrose-clerk 0.50, lilmoth-customs -> lilmoth-yard 1.56, soulrest-boneyard -> soulrest-pawn 2.12, soulrest-court-steps -> soulrest-market 0.88, soulrest-grey-hist -> soulrest-court-steps 0.45, thorn-hall -> thorn-apothecary 0.16, thorn-house-0 -> thorn-gate 0.46). On 9 it opens nothing. Before this round moved the doors onto entry walls the same measurement was 108 / 5 / 2. The cause is that doorAt() returns the NEAREST door of the town within DOOR_REACH_M and there are now five pairs of doors within 2.6 m of each other, four of them exactly 1.00 m apart. Separately and by the same cause, 10 of 115 bodies are standing inside a drawn building 120 fixed steps after the door, having been clear of every footprint at frame 1. | Derive the doorstep against the door table, not only against the footprints. The ring search at render/exterior.js:556-574 already walks candidates outward from the wall normal and rejects any point with insideBuilding(plan, x, z, 0). Add the second rejection: a point is standable only if doorAt(plan.id, x, z) returns THIS interior. Because the doors themselves move, this must run as a second pass after every door in a settlement has been placed. Measure at rest, not at frame 1. The live acceptance should read buildingAt() at 120 fixed steps as well as at 1, and require 0 at both. Assert over 115 records, not 112 enterable plan buildings. check-building-fits-room.mjs iterates plan.buildings and cannot see thorn-house-0, barge-hold or writ-house. Iterate Object.keys(interiors) and report records with no plan rather than skipping them; and move the `if (!plan) continue` in applyInteriorBounds()'s orphan pass below its own counter so the count is not blind to its own skips. → **A live sweep of all 115 doors reports 0 doorsteps inside a drawn footprint at BOTH 1 and 120 fixed steps; pressing interact on the doorstep returns the body to the room it left on 115 of 115; and check-building-fits-room.mjs enumerates 115 records with --self-break red on both assertions. All three are re-measurable with tools/world/critic-w1-04-r5-live.mjs and tools/world/critic-w1-04-r5-offline.mjs --self-break.** |
| `GAP-W1-the-fence-buys-your-victims-own-furniture` | `stealth.fence.economy` | blocking | 0 | w1 / w1-15 | `crit-w1-15-r4-3c19` (FAIL) | Engine.fenceQuote() (game/src/engine.js:8541) hands sim/stealth/theft.js::willBuy() a stub world — `const world = { npcById: () => null, dispositionBetween: () => 0 }`. willBuy() resolves an owner's settlement and faction through npcById and the friend-of-the-owner test through dispositionBetween, so for any object owned by a PERSON all four of its refusal branches are unreachable. Measured live at 9126e02: an object owned by npc:archon-legionary-13 of archon is bought by fence.archon.salvage — archon's own fence — at 15 g, EXACTLY the price it pays for a clean-item control, and LESS than three out-of-town fences pay (16, 17, 17 g). 1,787 of 1,950 property objects (91.6%) are affected; the 123 faction:- and 40 settlement:-owned rows do reach the refusals, because willBuy() parses those out of the owner string with a regex needing no npcById. The control separates cleanly: the identical willBuy() call with a real npcById over game/data/npcs/ returns {buys:false, reason:'same_settlement'} with an authored refusal line naming the victim. | In Engine.fenceQuote() (game/src/engine.js:8541) replace the stub world with a real one: npcById resolving `npc:<id>` against this.data.npcs — the 347-record table the Engine already holds and already walks in populateSettlement() — and dispositionBetween reading the disposition model W1-18 owns, or, if that is not yet addressable, an explicit `() => 0` carrying a why_not note so the friend_of_the_owner branch is DECLARED dead rather than silently so. Then add a crime-audit assertion that quotes the same stolen object at the owner's local fence and at an out-of-town fence and REQUIRES THEM TO DIFFER — the current FNC-4 checks the authored refusal lines exist and never checks they can fire. Budget a regression pass: this moves existing stl-probe and crime-audit fencing assertions, which is exactly why the round correctly declined to attempt it without a browser. → **A stolen object whose owner lives in the fence's own settlement must return buys:false with reason 'same_settlement'. Reachable today: 0 of 1,950 property objects. Required: >= 1,787 (91.6%, every npc:-owned object), plus the same_faction refusal firing for all 123 faction:-owned rows. Re-measure with `node tools/stealth/critic-w1-15-r3.mjs fence`, whose control already separates the two worlds.** |
| `GAP-W1-the-naming-moment-is-still-orphan-text` | `journey.chargen.diegesis` | blocking | 0 | w1 / w1-07-r3 | `crit-w1-26-r1-3c19` (FAIL), `crit-w1-26-r2-3c19` (FAIL), `critic-w1-26-r3` (FAIL) | RI-CHR01 §4 states the questionnaire's purpose in its own words — the Warden-Scribe says the profession aloud, 'which is the moment the route is for' — and RI-JRN09 M3 requires that line on the frame in 100% of named runs. Measured at the node the line is spoken (writ.birthsign, its only opportunity, because `spoken` carries only the reply to the last answer): 0 of 12 named runs across six races and two upbringings. The mechanism is ui.js's overflow-sacrifice loop — `while (contentH > maxH && spokenLines.length) spokenLines.shift()` — which drops the scribe's previous reply from the front until the panel fits its 48%-of-frame-height budget, and at the birthsign node (nine options plus a two-line naming sentence) the naming line is always what goes. The builder's own instrument reports 100% because it does not test that line: jrn09-exchange.mjs M3 runs `blob.indexOf(norm(c.class_name)) >= 0` over the concatenated drawn text of every node in the walk, and the same round added 'Trade declared: %ClassName' to the writ's drawn identity block at writ.stamp, so the class name is on the frame whatever the scribe did or did not say. Measured side by side on the same twelve runs: the naming LINE 0 of 12; the class NAME substring 12 of 12. The same mechanism eats the upbringing reply ('The interior. I will write the marsh. There are people here who will read that and stand up straighter, and people who will read it and count their doors.') and the birthsign reply ('The tide was in and the root had more than it needed. It gave you a swallow it did not owe you.'), which is why AC = 6 of 7 is evidenced entirely by fields of one printed form. | Two changes and a test, in this order. (1) Give the naming moment its own beat. The line is the payoff of a ten-node route and it is currently one entry in a `spoken` array that the layout treats as the least important thing on the panel. Add a node between the last question and writ.birthsign whose `line` IS the naming sentence and whose only input is an acknowledgement — the same shape writ.stamp was given this round for exactly this reason — so it occupies the one slot ui.js never sacrifices. Morrowind gives Socucius Ergalla his own beat to say 'you are a Battlemage'; this is that beat. (2) If the sacrifice loop is kept, make it declare what it dropped: have UILayer._redraw record the sacrificed lines in `metrics().sacrificed` so that any probe reading the surface can see the difference between 'not computed' and 'computed and thrown away'. Round 3's own defect is invisible in every instrument the build ships. (3) Fix tools/harness/jrn09-exchange.mjs M3 to test the naming LINE at the node it is spoken, not the class-name substring over the whole walk. The current test cannot go red while the writ draws the class name, which makes it a probe that cannot fail. → **Over >= 240 completions covering all 10 races x 4 upbringings with a published a/b/c/d histogram: the text of on_match_named_class / on_nearest_named_class appears in __HARNESS.getRenderedText() at the node it is spoken in >= 99% of named runs, measured by corpus/90-verdicts/wave1/artifacts/W1-07-r3/critic-w1-07-r3g.mjs unchanged. Separately, deleting the naming line from writ-house.json must take that figure to 0 and restoring it must return it — the delete-the-fix check this round applied to the greeting data and not to this. And AC's drawn evidence must include >= 4 responsive lines rather than 6 fields of the writ.** |
| `GAP-W1-two-placement-mechanisms-collide` | `undefined` | blocking | 0 | w1 / w1-23-lore-registry-and-the-provinces-canon | `crit-w1-23-r3` (FAIL) | Two placement mechanisms put objects into the same interior without either knowing where the other's are, and four documents — one of them half of a registered contradiction pair — are placed, drawn, standable-at, and open NOTHING when a player presses interact at them. render/interior.js allocates a wall slot per `readable[]` entry with a 2.6 m minimum separation computed over that list AND NOTHING ELSE. game/data/world/readables/site-marks.json puts twenty-five quest documents into interiors at hard-coded coordinates by a different route. engine.js:3270 gives the press to the nearest prop inside its reach. Measured: gideon-court/a-short-account-of-the-pacification loses to a site-mark 0.72 m away, gideon-court/the-legates-seal-register to one 0.50 m away, helstrom-undertemple/the-coast-survey-line to one 0.90 m away, helstrom-undertemple/the-helstrom-kin-list to one 0.77 m away. Two controls opened: the-casebook-i in the same room, and the-blessings-of-the-coast in a clean one. | Seed render/interior.js's `taken` array — the one `farEnough(s)` tests candidate wall slots against — with every site-marks.json entry whose `at.interior` is this room, before the readable loop runs. The coordinates are static data in a file that is already loaded, so this is a read and a concat, not a new system. Do the same for `unique_item` and `containers`, which are placed by the same function a few lines above and have the identical problem waiting. Then arm it where a check can see it, because a separation rule with no test is how this happened: extend tools/check-content.mjs (which runs for everybody, not just this piece) with the assertion the census makes — for every interior carrying a `readable`, no two interactable props within min(reach_m) of each other. That is a pure-data check, it needs no browser, and it would have failed on this tree today. → **node tools/lore/critic-w1-23-r4-shelf.mjs reports 45/45 separately openable across the six most loaded rooms (today 41/45, with gideon-court 4/6 and helstrom-undertemple 3/5), and node tools/lore/critic-w1-23-r4-blocked.mjs exits 0 with all six legs opening their book (today it exits 1 with four unexplained). The new data check in tools/check-content.mjs fails on a tree where the render/interior.js change is reverted, so the guard is watched failing rather than assumed.** |
| `GAP-W1-ui-detectors-that-cannot-see` | `ui.hud.minimalism` | blocking | 0 | w1 / W1-21 | `crit-w1-map-defects-r1-3c19` (FAIL) | The piece's detectors report PASS about things they did not sample, and its one detector that looks at the framebuffer rather than at the build's own register is looking at the wrong pixels. ui-forbidden.mjs is RI-UIX01 §B's OBSERVED probe — the only check in W1-21 that answers an AR-2 question from the picture instead of from getUIState() — and its first run against any build returns 66 digit glyphs over six enemies, all of which are helmet crests and jaw lines. It scans a 1280x720 coordinate space in a 1920x1080 frame because it never pins the drawing buffer; it grades a frame whose hit_frame is null, so the capture is not the post-hit frame the method requires; and its own self-test finds 10 glyphs where 3 were drawn. Around it, eleven of the fifteen graded checks across the five detectors return PASS on an empty sample set, three of them BY CONSTRUCTION on absence: FD7's `gradient_bits === null \|\| >= 7`, FD6's edgeFringe() returning {worst: 0} with zero samples, and K4's bare `catch { continue }` on a missing state. | (a) ui-forbidden.mjs: call setDevicePixelRatio(1) before the first capture and assert screenshot().width === viewport.width; make hit_frame === null an exit 2 rather than a graded run; calibrate looksLikeDigit() against the --calibrate output rather than a hand-written band, since the self-test already draws the ground truth. (b) Give every graded check a sample count and assert it: K2 fails when covered_all_viewpoints is false; K3/K4 fail on an empty differential or sweep list and record WHY a state was skipped instead of `continue`; U4 records how many elements on how many screens it scanned; FD6 fails when edge_fringe_samples === 0; FD7 fails when gradient_bits === null. A check with an empty sample set must report EMPTY, never PASS. (c) Replace `markers: 0` in game/src/ui/system.js with a count off the element census, and take the map's four AR-2 filters off the id.startsWith('map.') prefix so they cover every element on the screen. (d) Classify accessors in Discovery.mutatorReport() by the same fail-closed rule as methods. (e) Restate FD6's threshold in the item's own unit — deltaE, not 0-255 luma — so a two-round-old failure can be graded as a fail or as a hard fail. → **node tools/analysis/ui-forbidden.mjs --self-test reports a false-positive count of 0 on a frame with no digits drawn and >=2 on one with 247 drawn, at a pinned 1:1 buffer, with a non-null hit_frame; every check in ui-layer.mjs, ui-forbidden.mjs, marker-diff.mjs, ui-metrics.mjs and ui-census.mjs emits a sample count and returns EMPTY rather than PASS when it is zero, demonstrated by running each with its subject removed and confirming none goes green; getUIState().map.markers changes when a map_place element is added, and mutatorReport() lists a state-writing accessor; ui-metrics.mjs FD6 reports a deltaE against the item's 3 and 8.** |
| `GAP-W1-ui-hud-world-set-unbuilt-and-misplaced` | `ui.hud.world` | blocking | 0 | w1 / T4-r1 | — | The out-of-combat HUD has 1 of its 6 required elements, and the one element it has is in the wrong half of the screen. RI-UIX07 §B closes the world set at six: W1 bearing dial, W2 active-effect strip, W3 sneak state, W4 condition and charge marks on the quick slots, W5 place-name announcement, W6 breath meter. Only W1 exists — W2, W3, W4, W5 and W6 appear in no element-kind vocabulary on any frame captured, in any mode, across four separate runs. And W1's rect is [1782, 84, 96, 96], so y + h = 180 against H/2 = 540: it sits wholly in the top half, where §D1 requires the bottom half and §D2 requires the bottom-right corner or the rail inboard of it. Measured against the item's own §A, every persistent element of Morrowind's HUD is anchored Left Bottom or Right Bottom and the top of the screen is empty. | Move the dial, then build the missing five cheapest-first. (1) In game/src/ui/compass.js, dialGeometry() computes cy = max(84*s + r, margin + r) — the top edge. Recompute it against the bottom edge so the dial anchors bottom-right, and where RI-UIX01's quick slots (E5, currently [1728,888,150,150]) and equip-load gauge (E9, [1758,852,120,24]) already hold that corner, place the dial immediately inboard on the same bottom rail; add the D4 overlap assertion against every RI-UIX01 rect. (2) Build W4 next because it costs nothing: a condition rule under the weapon slot and a cast-chance rule under the spell slot, drawn INSIDE E5's existing 150x150 rect, adding zero screen coverage — Morrowind draws both and they are visible as thin red rules in REF-A12c. (3) Then W3 sneak_state (40x40, both phases, only while hidden), W5 place_name (520x36, out of combat, <=3 s on cell change), W2 effect_strip (one glyph per active effect, <=12, growing leftward from the dial along the bottom edge), W6 breath_meter (400x24, both phases, only while submerged). (4) Add combat_phase, combat_phase_source: 'encounter' and frames_since_phase_change to getUIState(), read from the encounter state the AI reads and never recomputed in the UI layer, so V7 and V8 stop being proxy measurements. → **RI-UIX07 V1 reads 6 of 6: all six world-set kinds (bearing_dial, effect_strip, sneak_state, the condition/charge marks inside E5, place_name, breath_meter) appear in getUIState().elements under their declared conditions on a world frame, with the four refusals B1-B4 still held. V6 reads pass: every world-set element satisfies y + h > H/2 at 1920x1080 AND at 844x390, and intersects no RI-UIX01 element rect except W4, which is defined as being inside one. C3 world-set persistent coverage stays <= 1.5% and C4 union persistent stays <= 5.0% at 1920x1080 (the specified set computes to 0.52% and the shipped Souls set measures 2.49%, so the union has real headroom). And getUIState() returns a combat_phase field whose value on an aggro frame is 'fight' and on a quiet frame is 'world'.** |
| `GAP-W1-ui-map-outside-the-ar2-detectors` | `ui.hud.minimalism` | blocking | 0 | w1 / W1-21 | `critic-w1-21-r2` (FAIL), `crit-w1-map-defects-r1-3c19` (FAIL) | Every AR-2 detector this piece ships predates the map and none of them opens it — ui-layer's 8 viewpoints, ui-metrics' 6 screens, ui-forbidden's 4 menus, ui-census and marker-diff all stop at the six pre-S35 screens. The only compliance report on the surface S35 defines by its refusals is the one the map screen computes about itself, and the structural half of that report — getUIState().map.mutator_arities, the exact field AMENDMENT-W1-MAP-01 §3b tells a critic to read from the running object instead of trusting the source — is a hand-written literal of three method names that omits the object's fourth state-writing method, Discovery.restore(blob), arity 1, which names places by id and repaints the raster. Demonstrated end to end: a save whose world.discovery block is forged in both its fields loads with ZERO dropped, and the map draws 42 place squares, 35 of them for places the body has never stood in, over a fully rendered province, while the screen reports places_drawn == places_discovered == 42 and every check in the build stays green. map-probe.mjs L3 is the check that exists to catch this and it forges only the place list, leaving the raster honest — so the corroboration it performs is a fixed-point test on an input the same attacker controls. | Four changes, in this order. (1) Put the map inside the detector suite: add ui_menu_map in BOTH the world and local views to ui-layer.mjs VIEWPOINTS; add `map` to ui-metrics.mjs SCREENS; add `map` to ui-forbidden.mjs's declared menu sweep; run marker-diff.mjs's quest-state differential with the map open as well as closed. (2) Replace the mutator_arities literal in game/src/ui/system.js with an enumeration over Object.getOwnPropertyNames(Object.getPrototypeOf(sim.discovery)), classified by whether the method writes state, so a mutator nobody remembered is counted rather than omitted. (3) Close restore(): drop `places` from the save block entirely and re-derive the list on load from the raster plus the site radii in terrain.json sites[].r_flat — which deletes the forgeable field rather than validating it against another forgeable field. (4) Take ui-metrics.mjs's region selector off its four-kind whitelist, and make a screen whose stem sample count is zero a REPORTED FAILURE rather than a row filtered out of the median. → **ui-layer.mjs, ui-metrics.mjs, ui-forbidden.mjs and marker-diff.mjs each visit the map screen in both views and report 0 undeclared components, 0 forbidden kinds and 0 quest-conditioned elements there; getUIState().map.mutator_arities lists every state-writing method on the discovery prototype and every one of them has arity 0; ui-metrics.mjs reports a non-null stem sample count for all 7 screens it opens or names the screens it could not measure and exits non-zero; and node tools/harness/critic-w1-21-r1b.mjs returns F1, F2 and F3 PASS — a forged save naming all 42 places over an all-ones raster draws no more place squares than the body has stood in.** |
| `GAP-W1-w1-30d-guard-misses-part-deletion` | `render.process.measurement` | major | 0 | w1 / W1-30D-r1 | `crit-w1-30de-r1-3c19` (FAIL), `crit-w1-orphaned-surface-shaders-r1-3c19` (PROVISIONAL) | The anti-gaming guard on the character defect metric is a scalar silhouette-area check, and a scalar cannot notice that a named part stopped existing. Deleting both of the character's hands — palms, three fingers and an opposed thumb each — improves the headline crack metric by 31% and moves the guarded quantity by 0.31% against a 3.0% threshold. It is not caught. | Replace the scalar silhouette-area guard with a PER-PART PRESENCE CENSUS in tools/visual/actor-hole-control.mjs, and keep the area guard as its second half. rasterise() in actor-orbit-holes.mjs already carries owner[] and the part label, so classifyPockets's caller can record the set of part names contributing at least one pixel anywhere in the run and return it on the summary. The comparison tool then fails any build whose part set is a strict subset of the baseline's. Roughly a dozen lines and no new threshold to argue about. Keep the 3 percent area guard for the opposite direction, because it is what catches weld-arm-to-rib, which adds rather than removes. → **All 7 controls behave: the 4 red-required arms go red, hide-arm and weld-arm-to-rib stay caught, and hide-both-hands is CAUGHT where it is currently missed at -0.31 percent drift against a 3.0 percent threshold. Additionally the part census must report zero missing parts on the shipped build and at least 2 missing parts on the hide-both-hands arm.** |
| `GAP-W1-w1-30e-street-gate-never-ran` | `render.process.measurement` | blocking | 0 | w1 / W1-30E-r1 | `crit-w1-orphaned-surface-shaders-r1-3c19` (PROVISIONAL) | The eight settlement street shots, which the plan names as this child's deciding gate, place the camera inside building geometry. Lilmoth is filled by the courses of a single reed wall with no street visible; Helstrom is jammed against a stone wall with the player half-clipped into it; Gideon is a timber post filling the frame with the player entirely inside the wall. The one measurement that could confirm or refute this child has never executed. | Move the 8 street-* setups in tools/visual/deck.json off the raw gameplay camera at yaw 270 and onto the settlement's own street centreline with a collision push-out. settlementApproach() in game/src/render/exterior.js already carries the settlement's approach vector and the plan carries every building footprint, so the push-out has the data it needs. Then re-run the child-scoped profile and score the row. This is a W1-30V edit, not a W1-30E one, and it should be dispatched there. → **All 8 street frames show at least 2 buildings whose facades are visible and unoccluded, with the near facade between 4 and 8 m from the camera, and zero frames in which the player capsule intersects building geometry. Re-measured by re-running the 8 street setups and checking the depth buffer's nearest non-player surface is >= 1.5 m in every frame.** |
| `GAP-W1-w1-30s-seam-incomplete` | `render.fidelity.vfx` | major | 0 | w1 / W1-30S-r1 | — | The seam publishes the boundary for 8 of 10 children and leaves 2 stranded. registerPrePass exists and has one registrant (water reflection), but the spell-VFX prepass named in the plan as its other registrant is still inline in renderer.js — construction, setSize, update and the gated this.vfx.prepass(this.camera) — so W1-30H cannot change prepass ordering, gating or lifecycle without writing into W1-30A's exclusive file. And models.js publishes an async loadModel() while every character mesh is built synchronously in render/scene.js:365/390 via makeRiggedActor, a file the parent plan declares read-only to all children and the ownership registry says nobody has declared at all — so W1-30D has no seam through which an awaited glTF mesh can reach the scene. | — → **—** |
| `GAP-W1-walkpath-absorbs-teleports` | `world.traversal.locomotion` | major | 0 | w1 / W1-CROSSING | — | engine.walkPath() still adds a teleport to path_m — it accumulates the per-frame planar step with no upper bound and returns no teleports / teleported_m field — while the round’s own headline finding F7 and survey §2c both state that "walkRoute AND walkPath now record those separately and EXCLUDE them from path_m". | Port the six lines already written in walkRoute into walkPath: hold out any per-frame step over 1 m from `dist`, record it in a jumps array with its endpoints and HP, and return teleports / teleported_m / teleport_log. Then correct finding F7 and reports/w1-crossing-survey.md §2c to say what the code does. → **A walk driven through walkPath that is killed mid-route reports teleports >= 1 and a path_m that rises by less than 20 m across the respawn, while an unbounded control counter over the same frames rises by the full respawn distance (3,000 m+). Re-run tools/world/w1-01-r4-soulrest-leg.mjs afterwards and confirm every published path_m for that leg carries teleports: 0.** |
| `GAP-W1-weapon-animation-model-has-no-rendered-consumer` | `weapon.animation.reuse` | blocking | 0 | w1 / W1-10 | — | The moveset animation model — 1150 distinct clips over 2689 slots and 87 weapons, the solved arcs, the solved blade lengths, the 25-slot contract — has no renderer. `grep -rn "weapon" game/src/render/` returns nothing, `grep -rn "bone" game/src/render/` returns nothing, and no SkinnedMesh is constructed anywhere in game/src. `game/src/render/scene.js` makeActor() builds a rigid group of nine primitives — torso, head, snout, hips, two static legs, two static arms — plus `new THREE.BoxGeometry(0.055, 0.95, 0.014)` welded at local (0.40, 1.28, 0.10) as 'the blade', one 0.95 m box for all eighty-seven weapons. `game/src/render/renderer.js:398-399` is the whole of the player's animation: playerMesh.position.set(sim.player.pos) and playerMesh.rotation.y = yaw. Measured in the shipping build: across a full 60-frame r1.1, the character-box pixel change is 0.19% peak for hlb_garrison_bill (2.85 m, a 146.8 deg sweep over 14 active frames) and 0.21% for cgs_drowned_reaper (a `spin` over 18 active frames); ssw_garrison_sword's 2.4% is flat from f030 to f060 and is the root lunge translating a rigid mesh, not a swing. Frame-0 screenshots of ssw_garrison_sword (1.95 m), hlb_garrison_bill (2.85 m) and cgs_drowned_reaper (2.75 m) are byte-identical, md5 9bdb823fbcdb3ba820b2946b05ffa55a. | Pose the rendered actor from the transforms the simulation already computes every frame. CombatBody.rig produces socketA/socketB and a full bone dump and engine.getHitGeometry() already returns both in world space, so nothing new has to be computed — makeActor() must expose its arm and weapon nodes, and the per-frame update must write those nodes' positions and orientations from combat.bodyOf('player') (and from each enemy body) instead of writing only the group's position and yaw. Scale the weapon mesh from the moveset's socket_b distance so a 3.6 m whip is not a 0.95 m box. → **(a) At the mid-active frame of r1.1, screenshots of ssw_garrison_sword, hlb_garrison_bill and whp_hide_lash differ from each other AND from their own frame 0 by >= 5% of character-box pixels; the three frame-0 images are no longer byte-identical. (b) On every active frame of 20 sampled clips the rendered weapon-tip world position is within 0.10 m of getHitGeometry().actors[player].weapon.now[3..5]. (c) With (b) holding, a screenshot of hlb_garrison_bill at its widest active frame shows the blade above the ground plane — which is the same measurement as taking kritik3-tip-height.json's HLB tip_below_floor_frac from 0.821 to 0.** |
| `GAP-W1-weapon-impact-and-material-model-has-no-consumer` | `weapon.feel.material` | blocking | 0 | w1 / W1-10-r2 | `crit-w1-10-r3-weapons-kritik3` (FAIL), `critic-w1-11-r1` (FAIL) | The RI-WPN05 impact and material model is a data island, exactly as the 87 movesets were in round 1. Five of six perturbations of the shipped tables change nothing in the fight: setting every non-flesh cell of the 5x7 attacker hitstop grid to 99 f, setting every material damage multiplier to 0.01, setting every knockback value to 9 m, and setting the deflection multiplier to 20x all produce a byte-identical fight (dmg 118, hit at frame 32, hitstop held 3 frames). Only the flesh column is read, and it is read from a hard-coded `.flesh` lookup at game/src/combat/moves.js:166. MovesetLibrary.materialMultiplier, .deflects, .knockbackFor and .victimHitstopFor are imported by exactly one file in the repository — game/src/harness/weapons.js, the harness. And there is nothing for them to apply to: 0 of the 14 enemy statblocks in game/data/combat/enemies/ declares a material at all. The hard-coded lookup is game/src/combat/moves.js:166 — `hitstop_frames: (s.hitstop_f \|\| lib.classes.hitstop.attacker[moveset.weight_tier] \|\| {}).flesh \|\| 6` — and a grep for materialMultiplier\|deflects\|knockbackFor\|victimHitstopFor across game/src/ returns only game/src/harness/weapons.js. | Make the fight read the material at hit time, and give it a material to read. → **node corpus/90-verdicts/wave1/artifacts/W1-10-r2/kritik-consumption3.mjs reports 6 of 6 CONSUMED; the 5x7 attacker hitstop grid is recoverable from traces against seven material-carrying dummies with every cell differing from the flesh column where RI-WPN05 A says it should; ILS >= 0.80 on the observed column; and AR-3 seam_sterile becomes false with a driven demonstration (a mace beating a stone enemy that a rapier cannot).** |
| `GAP-W1-weapon-movesets-not-wired-to-the-runtime` | `weapon.moveset.slots` | blocking | 0 | w1 / W1-10 | `crit-w1-10-r2-weapons-kritik` (FAIL), `crit-w1-10-r3-weapons-kritik3` (FAIL) | The 87 authored movesets are unreachable from the running game. setLoadout() rejects all 87 roster ids with 'no moveset <id>. Known: axe, dagger, greatsword, halberd, spear, straight-sword, ultra-greatsword', and eight of the fifteen classes (CSW TSW FST MCE WHP CGS GHM BOW) have no live representation at all. What a player can press is game/data/combat/spine/*.json, which declares three slots — light, heavy, two_handed{light,heavy} — so every weapon in the game plays exactly four distinct animation ids. Measured consequences: CFS_live = 0.0000 with 20 of 20 (weapon x contextual state) combinations playing the standing r1.1 clip byte-identically; distinct_anim = 4 against RI-WPN01 M3's floor of 18; RI-WPN01 M6 declared-vs-observed agreement 0.5357 with the clip id — the field RI-WPN01 §B calls 'the load-bearing field of the whole weapons area' — matching on 0 of 14 rows. | Make MovesetLibrary the runtime's move source instead of a harness-only reader. (1) In game/src/combat/system.js createPlayer(), build the move table from game/data/combat/movesets/*.json via MovesetLibrary rather than from game/data/combat/spine/*.json, so setLoadout({weapon:'ssw_garrison_sword'}) resolves. (2) In game/src/combat/player.js, dispatch the attack buttons through MovesetLibrary.resolveSlot(weaponId, button, ctx) with ctx carrying state, state_frame, roll_tier, stance, offhand and descending/fall_height, and delete the code path that falls through to the standing light attack when no window matches — a missed window must produce nothing, per RI-WPN04 §B rule 2. (3) Add player.anim_slot, player.hitstop_f and player.weapon_tip to the frame record and a block_success event to the trace vocabulary. (4) Restore the R2 active/recovery split of the seven spine classes to game/data/combat/frames.json, which RI-WPN02 §B's authority note makes binding. (5) Repair the 82 weapons whose chains_to points at a slot they do not declare (50x 2h.r2 -> 2h.r2.follow, 32x r2 -> r2.follow, 4x r1.3 -> r1.4). → **node tools/harness/critic-w1-10a.mjs reports equipped_ok 87/87 and classes_with_zero_equippable []; node tools/harness/critic-w1-10c.mjs reports CFS_live == 1.0000 with a `contextual` band exactly on RI-WPN04 §B's frame windows for all four equip-load tiers; node tools/harness/critic-w1-10f.mjs reports M6 agreement == 1.0000 including the anim id; and the observed distinct_anim per weapon is >= 18.** |
| `GAP-W1-world-region-identity-is-a-tint` | `world.region.identity` | blocking | 0 | w1 / w1-01-r2 | `crit-w1-02-r1-3c19` (FAIL) | All thirteen regions are the same three primitives — canopy, under, rock — recoloured, and not one of the thirteen declared ONLY-HERE elements exists in the world. Every region in game/data/world/regions.json carries the identical prop schema `canopy+under+rock`, drawing on seven shared canopy shapes and five shared under shapes; the whole 14.31 km2 province renders with textureMB 0, atlasCount 0, 17 materials and 8 programs. `only_here: {id, instances}` — welkynd_pillar x34, petrified_bole x400, rock_flute_spire x14, glassed_crater x16, beached_hull_house x12 and nine more — is an integer literal in tools/world/build-regions.mjs and nothing places an instance, so RI-WLD04 M19 measures 0 of 13. Five of M18's nine axes (fauna, architecture, audio, weather, hazard) are DROPPED rather than measured, which is the same statement in the instrument's own words. The consequence is measurable two ways. A human taking M17 on the now-honest pack scores 4/8 against a pass bar of 85%, and three of the four misses are one miss: frames 023, 042 and 050 are the Stone Wastes — 'glassed salt crust', glass_thorn, whale_bone_frame, salt_block_wall, crater_rim — and all three read as Marauder's Coast, because what is rendered is a khaki flat under a white fog band. And with colour and exposure removed from the machine descriptor, leave-one-out identification falls 74.4% -> 30.8% and Fisher 3.10 -> 1.77: about two-thirds of the measured region separability is carried by tint alone, which is RI-WLD04's own named way to lose. | Place the ONLY-HERE elements as real instances, and give each region at least one prop class that no other region has. Concretely: (1) extend the region prop schema in tools/world/build-regions.mjs and game/src/world/province.js beyond `canopy+under+rock` with a fourth slot `signature`, carrying its own mesh generator per region — a welkynd pillar for Blackwood, a rock-flute spire for Valus Ridge, a whale-bone frame and crater rim for the Stone Wastes, a beached hull house and tide pole for Marauder's Coast, a drowned xanmeer tooth and corpse lily for the Deep Marshes, a stilt row and tideway marker for the Eastern Rootlands; (2) place them at the counts regions.json already declares, from the same seeded lattice the existing props use, so the number in the table becomes the number in the world; (3) add a `signature_silhouette` axis and an `architecture_placed` axis to tools/world/region-axes.mjs, computed from placed instances rather than from a table row, and un-drop `architecture`; (4) give the ground more than one material — at present textureMB is 0, so albedo is a per-region constant and RI-WLD04's 'albedo must come from different materials, not a LUT' cannot be satisfied at all. → **RI-WLD04 M19 reads 13/13: each region's ONLY-HERE element is queryable in the running world at >=8 instances inside its own region and 0 instances in every other region. AND the structure-only re-measurement (critic-structure-only.json, colour and exposure removed) rises from 30.8% to >=55% leave-one-out on the 39 day frames, so region identity survives having the tint taken away. AND M18 measures >=7 axes on every one of the 78 pairs with `architecture` un-dropped and computed from placed instances.** |
| `GAP-W1-world-trunk-road-below-the-waterline` | `world.terrain.form` | blocking | 0 | w1 / W1-01 | `crit-w1-01-r2-world-b4e2` (FAIL) | The trunk road network is routed along the floors of water bodies. Sampling getWaterAt() at every point of all ten built legs at tide LOW: 1,364 m of road (5.4%) is above knee depth and 574 m (2.3%) is above chest depth, concentrated on the Stormhold-Helstrom leg, which carries 538 m over-knee and 502 m over-chest and reaches a maximum depth of 65.64 m at (2182.52, 2214.63) - ground at 69.66 m under a water surface at 135.30 m. That leg is the first of THE CROSSING, so 502 m of the canonical 6,788 m walk - 7.4%, about 4.2 minutes of the hour - is walked along a lake bed. The player traverses it at 2.0000 m/s in state WALK at full HP and full stamina, with no swim, no wade band, no breath clock and no drowning; a screenshot from that point is a uniformly black-green frame of water volume. Nothing in the instrument suite asks the question: scale-audit.mjs has a TIDEWAY-INVERSION check that measures water depth on exactly one of the ten legs and none on the other nine. | Add water depth to the trunk-routing cost in tools/world/build-roads.mjs: make any cell whose water depth exceeds 0.60 m (band W2) impassable to A*, except on the leg RI-WLD01 §4 declares 'tideway', and re-run the build chain build-terrain -> build-water-hazards -> build-roads. Where a leg genuinely must cross a channel, emit a bridge or causeway deck into game/data/world/roads.json as a raised polyline carrying its own surface height, so the road is over the water rather than under it. Then add the check that does not exist to tools/world/scale-audit.mjs as M2-ROAD-ABOVE-WATER, covering all ten legs at all four tide phases rather than one leg at one phase. → **tools/world/scale-audit.mjs reports 0 m of trunk road above band W2 on every leg except lilmoth-archon, at all four tide phases (RISING, HIGH, FALLING, LOW); lilmoth-archon keeps its declared inversion at <=0.85 m deep at LOW and >=1.40 m at HIGH; and tools/world/crossing.mjs re-measures the crossing on the re-routed network inside 52-65 min and 6,300-7,600 m with mean ground speed still 2.00 +/-0.05 m/s.** |

## Open gaps grouped by subsystem path (the builder hand-off)

- `undefined` — `GAP-W1-library-unreachable-contradiction-pairs`, `GAP-W1-two-placement-mechanisms-collide`
- `audio.ambience.events` — `AUDB2-F1`
- `audio.ambience.region` — `GAP-W1-22-event-level-graded-on-a-median`, `GAP-W1-audio-emitter-consumption`
- `audio.combat.impact` — `GAP-W1-m6-pan-correlation-passes-the-panner-it-was-built-to-catch`
- `character.creation.flow` — `GAP-W1-questionnaire-route-is-unreadable-and-unnamed`
- `combat.camera.behaviour` — `GAP-W1-consumption-sweep-tripwire-is-a-name-grep`
- `combat.encounter.placement` — `GAP-W1-population-save-reload-repays-every-corpse`
- `combat.enemy.movement` — `GAP-W1-12-approach-cannot-close-on-a-walking-player`
- `combat.enemy.statemachine` — `GAP-W1-12-m3-chase-bot-dwell-on-the-items-own-fixture`
- `combat.frames.timing` — `GAP-W1-combat-exemplar-has-no-danger`
- `combat.hitbox.sweep` — `GAP-W1-enemy-attacks-cannot-reach-contact-range`, `GAP-W1-enemy-weapon-volume-still-cannot-reach-and-the-body-pays-its-damage`
- `crime.guard.response` — `GAP-W1-crime-enforcement-is-a-free-win`
- `crime.witness.model` — `GAP-W1-stealth-crime-model-not-coupled-to-the-world`
- `dialogue.npc.identity` — `GAP-W1-17-an-archetype-with-no-mouth`
- `dialogue.topics.filtering` — `GAP-W1-act5-argument-entry-shadowed`
- `dialogue.voice` — `GAP-PROSE-01`
- `experience.permissiveness.register` — `GAP-W1-25-the-control-facility-has-an-optional-control`
- `input.discoverability` — `GAP-W1-input-discoverability-unmeasured`
- `input.gamepad.lifecycle` — `GAP-W1-input-calibration-outlives-its-pad-on-disconnect`
- `input.modality.parity` — `GAP-W1-map-fix-drops-the-journal-from-the-page-walk`
- `journal.entry.voice` — `GAP-W1-mark-teaches-nothing-to-the-journal`
- `journey.chargen.diegesis` — `GAP-W1-creation-is-an-api-not-a-scene`, `GAP-W1-the-naming-moment-is-still-orphan-text`
- `journey.firstlaunch.flow` — `GAP-W1-26-creation-cannot-be-completed-from-the-title`, `GAP-W1-26-r3-body-race-fails-silently-and-is-refused-eleven-nodes-later`
- `journey.opening.legibility` — `GAP-W1-rendered-text-accessor-blind-to-the-hud-surface`
- `journey.save.roundtrip` — `GAP-W1-save-purse-has-no-writer`
- `lore.canon.registry` — `GAP-W1-23-both-registered-voices-on-slavery-are-colonists`
- `lore.register` — `GAP-W1-library-martial-single-hand-fingerprint`
- `magic.effects.catalogue` — `GAP-W1-magic-spellmaking-has-no-world-side-surface`
- `magic.effects.utility` — `GAP-W1-magic-effects-no-behaviour`
- `magic.gating.skills` — `GAP-W1-magic-skill-frozen`
- `platform.determinism.harness` — `GAP-W1-platform-prng-never-drawn`
- `platform.load.hitches` — `GAP-W1-provstream-arrival-burst`
- `platform.load.ttfp` — `GAP-W1-deploy-instruments-point-at-the-tester-not-the-player`
- `platform.save.persistence` — `GAP-W1-map-the-audit-is-computed-and-never-published`, `GAP-W1-platform-save-drops-entity-prev-state`
- `process.critic.discipline` — `GAP-PACK-01`
- `progression.bonfire.function` — `GAP-W1-hearth-levelup-gate-reads-a-method-that-does-not-exist`
- `progression.equipment.encumbrance` — `GAP-W1-16-a-save-and-a-reload-change-your-roll-class`, `GAP-W1-16-equip-load-cannot-see-the-thing-in-your-hands`
- `progression.level.attributes` — `GAP-W1-attr-scale-ceilings-rest-on-an-unmeasured-souls-economy`
- `progression.souls.economy` — `GAP-W1-souls-alive-keyed-on-eid`, `GAP-W1-souls-world-copy-of-the-values-is-53pc-stale`
- `quests.faction.escalation` — `GAP-W1-faction-ladders-are-doors-onto-empty-corridors`
- `quests.faction.measurement` — `GAP-FCT-03`
- `quests.faction.rankgating` — `GAP-W1-faction-ladders-are-bricked-up-at-rank-5`
- `quests.mainline.acts` — `GAP-W1-mainquest-topic-bootstrap`, `GAP-W1-quest-givers-not-in-the-world`
- `render.art.character` — `GAP-W1-character-surface-is-assembled-from-primitives-not-built`
- `render.fidelity.atmosphere` — `GAP-W1-height-fog-credit-unanchored`
- `render.fidelity.character` — `GAP-W1-f1-actor-body-shader-does-not-link`
- `render.fidelity.materials` — `GAP-W1-30c-wetness-mask-blind-to-instancematrix`, `GAP-W1-pbr-material-set-unbound`
- `render.fidelity.vfx` — `GAP-W1-w1-30s-seam-incomplete`
- `render.process.measurement` — `GAP-W1-w1-30d-guard-misses-part-deletion`, `GAP-W1-w1-30e-street-gate-never-ran`
- `stealth.detection.model` — `GAP-W1-seven-tenths-of-the-indoor-floor-is-at-the-top-of-the-light-scale`
- `stealth.fence.economy` — `GAP-W1-the-fence-buys-your-victims-own-furniture`
- `ui.hud.minimalism` — `GAP-W1-ui-detectors-that-cannot-see`, `GAP-W1-ui-map-outside-the-ar2-detectors`
- `ui.hud.world` — `GAP-W1-ui-hud-world-set-unbuilt-and-misplaced`
- `ui.readables` — `GAP-W1-LIBRARY-the-library-has-no-reader-on-the-world-side`
- `weapon.animation.reuse` — `GAP-W1-weapon-animation-model-has-no-rendered-consumer`
- `weapon.feel.material` — `GAP-W1-weapon-impact-and-material-model-has-no-consumer`
- `weapon.moveset.slots` — `GAP-W1-weapon-movesets-not-wired-to-the-runtime`
- `world.interior.continuity` — `GAP-W1-the-doorstep-cannot-get-you-back-in`
- `world.interior.named` — `GAP-W1-interior-door-draws-nothing`, `GAP-W1-the-door-puts-you-inside-the-building`
- `world.region.identity` — `GAP-W1-world-region-identity-is-a-tint`
- `world.region.transition` — `GAP-W1-02-m68-unanswerable`
- `world.settlement.anatomy` — `GAP-W1-settlement-has-no-outside`, `GAP-W1-the-building-is-smaller-than-the-room`
- `world.terrain.form` — `GAP-W1-world-trunk-road-below-the-waterline`
- `world.time.daynight` — `GAP-W1-13-clock-consequences-never-fire`, `GAP-W1-13-roster-clause-has-no-control`
- `world.traversal.locomotion` — `GAP-W1-walkpath-absorbs-teleports`
- `world.traversal.roads` — `GAP-W1-road-join-has-no-gate`
- `world.water.marsh` — `GAP-W1-03-water-surface-undrawn-and-frozen`

## Closed and superseded

| Gap | Subsystem path | Status | Opened | Closed | Measured | Closed by |
|---|---|---|---|---|---|---|
| `undefined` | `undefined` | CLOSED | wundefined | w1 | — | crit-w1-19-r2-3c19 |

## Warnings

- corpus/90-verdicts/wave1/W1-DEPLOY-r1.json: unsatisfied verdict has no biggest_gap; validator should reject it.
- undefined: remedy.acceptance has no number or observable condition — not re-measurable, verdict should be PROVISIONAL (SCORING.md §4).
- corpus/90-verdicts/wave1/W1-TOUCH-r1.json: unsatisfied verdict has no biggest_gap; validator should reject it.
- GAP-W1-the-door-puts-you-inside-the-building: remedy.acceptance has no number or observable condition — not re-measurable, verdict should be PROVISIONAL (SCORING.md §4).
- corpus/90-verdicts/wave1/W1-13-r2.json: duplicate gap_id undefined (already opened by corpus/90-verdicts/wave1/W1-MAP-r1.json).
- corpus/90-verdicts/wave1/W1-14-r4.json: unsatisfied verdict has no biggest_gap; validator should reject it.
- GAP-W1-quest-givers-not-in-the-world: remedy.acceptance has no number or observable condition — not re-measurable, verdict should be PROVISIONAL (SCORING.md §4).
- AUDB2-F1: remedy.acceptance has no number or observable condition — not re-measurable, verdict should be PROVISIONAL (SCORING.md §4).
- GAP-W1-w1-30s-seam-incomplete: remedy.acceptance has no number or observable condition — not re-measurable, verdict should be PROVISIONAL (SCORING.md §4).
- GAP-FCT-03: remedy.acceptance has no number or observable condition — not re-measurable, verdict should be PROVISIONAL (SCORING.md §4).
- corpus/90-verdicts/wave1/W1-FACTIONS-r1.json: unsatisfied verdict has no biggest_gap; validator should reject it.
- GAP-W1-LIBRARY-the-library-has-no-reader-on-the-world-side: remedy.acceptance has no number or observable condition — not re-measurable, verdict should be PROVISIONAL (SCORING.md §4).
- GAP-PROSE-01: remedy.acceptance has no number or observable condition — not re-measurable, verdict should be PROVISIONAL (SCORING.md §4).
- GAP-PACK-01: remedy.acceptance has no number or observable condition — not re-measurable, verdict should be PROVISIONAL (SCORING.md §4).
- corpus/90-verdicts/wave1/W1-00-r2.json: closure of GAP-W1-platform-prng-never-drawn is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-01-province-stream-r1.json: gap_closure references unknown gap_id GAP-W1-province-never-streamed.
- corpus/90-verdicts/wave1/W1-01-r2.json: closure of GAP-W1-world-trunk-road-below-the-waterline is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-02-r1.json: gap_closure references unknown gap_id GAP-W1-region-axes-read-from-json.
- corpus/90-verdicts/wave1/W1-02-r1.json: gap_closure references unknown gap_id GAP-W1-blind-pack-unshuffled.
- corpus/90-verdicts/wave1/W1-02-r1.json: gap_closure references unknown gap_id GAP-W1-02-texture-swap-all-nine-axes.
- corpus/90-verdicts/wave1/W1-02-r1.json: gap_closure references unknown gap_id GAP-W1-02-thresholds-not-drawn.
- corpus/90-verdicts/wave1/W1-02-r1.json: gap_closure references unknown gap_id GAP-W1-02-clock-never-advances.
- corpus/90-verdicts/wave1/W1-02-r1.json: gap_closure references unknown gap_id GAP-W1-02-first-hostile-min-m.
- corpus/90-verdicts/wave1/W1-02-r1.json: gap_closure references unknown gap_id GAP-W1-02-height-fog-unread.
- corpus/90-verdicts/wave1/W1-03-r1.json: gap_closure references unknown gap_id GAP-W1-03-amphibious-races-indistinguishable.
- corpus/90-verdicts/wave1/W1-03-r1.json: gap_closure references unknown gap_id GAP-W1-03-no-waterline-on-the-character.
- corpus/90-verdicts/wave1/W1-03-r1.json: gap_closure references unknown gap_id GAP-W1-03-enemy-water-band-absent.
- corpus/90-verdicts/wave1/W1-04-r3.json: closure of GAP-W1-interior-door-draws-nothing is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-04-r3.json: closure of GAP-W1-settlement-has-no-outside is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-04-r3.json: gap_closure references unknown gap_id GAP-W1-interior-lights-unread-by-detection.
- corpus/90-verdicts/wave1/W1-04-r3.json: gap_closure references unknown gap_id GAP-W1-collision-control-arm-inert.
- corpus/90-verdicts/wave1/W1-04-r4.json: closure of GAP-W1-the-building-is-smaller-than-the-room is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-04-r4.json: gap_closure references unknown gap_id GAP-W1-pixel-acceptance-is-unmeasurable.
- corpus/90-verdicts/wave1/W1-04-r4.json: gap_closure references unknown gap_id GAP-W1-inert-control-nulling-pattern.
- corpus/90-verdicts/wave1/W1-04-r4.json: gap_closure references unknown gap_id GAP-W1-exterior-restored-on-exit-is-vacuous.
- corpus/90-verdicts/wave1/W1-04-r5.json: closure of GAP-W1-the-door-puts-you-inside-the-building is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-04-r5.json: gap_closure references unknown gap_id GAP-W1-the-join-left-the-lamps-behind.
- corpus/90-verdicts/wave1/W1-04-r5.json: gap_closure references unknown gap_id GAP-W1-a-street-signature-is-not-a-street-signature.
- corpus/90-verdicts/wave1/W1-04-r2.json: closure of GAP-W1-interior-door-draws-nothing is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-07-r2.json: closure of GAP-W1-creation-is-an-api-not-a-scene is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-07-r3.json: closure of GAP-W1-questionnaire-route-is-unreadable-and-unnamed is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-07-r3.json: closure of GAP-W1-creation-is-an-api-not-a-scene is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-07-r3.json: gap_closure references unknown gap_id GAP-W1-greetings-and-race-gates-have-no-world-side-consumer.
- corpus/90-verdicts/wave1/W1-07-r3.json: gap_closure references unknown gap_id GAP-W1-the-writ-is-a-return-value.
- corpus/90-verdicts/wave1/W1-07-r3.json: gap_closure references unknown gap_id GAP-W1-no-title-surface.
- corpus/90-verdicts/wave1/W1-09-r2.json: closure of GAP-W1-combat-exemplar-has-no-danger is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-09-r3.json: closure of GAP-W1-enemy-attacks-cannot-reach-contact-range is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-09-r3.json: closure of GAP-W1-combat-exemplar-has-no-danger is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-10-r3.json: closure of GAP-W1-weapon-impact-and-material-model-has-no-consumer is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-10-r3.json: gap_closure references unknown gap_id GAP-W1-weapon-hitbox-band-non-contiguous-S26.
- corpus/90-verdicts/wave1/W1-10-r3.json: gap_closure references unknown gap_id GAP-W1-weapon-movesets-unreachable-from-the-running-game.
- corpus/90-verdicts/wave1/W1-10-r2.json: closure of GAP-W1-weapon-movesets-not-wired-to-the-runtime is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-11-r1.json: closure of GAP-W1-weapon-impact-and-material-model-has-no-consumer is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-12-r2.json: closure of GAP-W1-12-approach-cannot-close-on-a-walking-player is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-13-r2.json: gap_closure is object where the schema requires an array. Its closures are IGNORED — any gap it meant to close stays open.
- corpus/90-verdicts/wave1/W1-13-r3.json: gap_closure references unknown gap_id GAP-W1-bloodstain-invisible-from-most-bearings.
- corpus/90-verdicts/wave1/W1-13-r3.json: gap_closure references unknown gap_id GAP-W1-13-a-level-buys-nothing.
- corpus/90-verdicts/wave1/W1-13-r3.json: gap_closure references unknown gap_id GAP-W1-13-second-death-leaves-no-stain.
- corpus/90-verdicts/wave1/W1-13-r3.json: gap_closure references unknown gap_id GAP-W1-13-respawn-drift-measured-with-the-stick-released.
- corpus/90-verdicts/wave1/W1-13-r3.json: gap_closure references unknown gap_id GAP-W1-13-journeys-ungated-and-control-observed-unmeasurable.
- corpus/90-verdicts/wave1/W1-13-r3.json: gap_closure references unknown gap_id GAP-W1-13-md2-md10-unreachable.
- corpus/90-verdicts/wave1/W1-13-r4.json: closure of GAP-W1-13-clock-consequences-never-fire is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-13-r4.json: gap_closure references unknown gap_id GAP-W1-13-r3-clock-artifact-carries-a-stale-verdict.
- corpus/90-verdicts/wave1/W1-14-r2.json: closure of GAP-W1-magic-effects-no-behaviour is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-14-r2.json: gap_closure references unknown gap_id GAP-W1-magic-vfx-nothing-renders.
- corpus/90-verdicts/wave1/W1-14-r3.json: gap_closure references unknown gap_id GAP-W1-magic-bind-magnitude-unread.
- corpus/90-verdicts/wave1/W1-14-r3.json: gap_closure references unknown gap_id GAP-W1-magic-demoralise-equals-calm.
- corpus/90-verdicts/wave1/W1-14-r3.json: gap_closure references unknown gap_id GAP-W1-magic-cannot-hit-a-moving-target.
- corpus/90-verdicts/wave1/W1-15-r2.json: closure of GAP-W1-stealth-crime-model-not-coupled-to-the-world is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-15-r3.json: closure of GAP-W1-crime-enforcement-is-a-free-win is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-15-r3.json: gap_closure references unknown gap_id GAP-W1-the-lamps-are-drawn-and-detection-has-never-been-told.
- corpus/90-verdicts/wave1/W1-15-r3.json: gap_closure references unknown gap_id GAP-W1-plausible-set-returns-empty.
- corpus/90-verdicts/wave1/W1-15-r4.json: closure of GAP-W1-the-fence-buys-your-victims-own-furniture is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-15-r4.json: gap_closure references unknown gap_id GAP-W1-cover-volumes-are-hand-fed-like-the-lamps-were.
- corpus/90-verdicts/wave1/W1-15-r4.json: gap_closure references unknown gap_id GAP-W1-trespass-has-no-zone-producer.
- corpus/90-verdicts/wave1/W1-16-r2.json: gap_closure references unknown gap_id equipLoadPct has no producer (NEXT-DISPATCH.md §Q3, W1-16 round 1 notes_for_successor #2).
- corpus/90-verdicts/wave1/W1-16-r2.json: gap_closure references unknown gap_id _finishEquipCommit() could fill only the `right` slot.
- corpus/90-verdicts/wave1/W1-16-r2.json: gap_closure references unknown gap_id NEXT-DISPATCH.md §Q1 — setBurden() inert on any stepping run.
- corpus/90-verdicts/wave1/W1-16-r2.json: gap_closure references unknown gap_id NEXT-DISPATCH.md §Q2 — two parallel sprint implementations, worldDeny.sprint read only at the press gate.
- corpus/90-verdicts/wave1/W1-16-r3.json: gap_closure references unknown gap_id GAP-W1-16-equip-load-cannot-see-the-thing-in-your-hands (round-2 verdict's biggest gap).
- corpus/90-verdicts/wave1/W1-16-r3.json: gap_closure references unknown gap_id RI-CMB01 §B — `OVERLOADED additionally forbids sprinting`, measured absent by round 2.
- corpus/90-verdicts/wave1/W1-16-r3.json: gap_closure references unknown gap_id round-2 verdict §E — the first-engagement delta collapse eats a Feather offset.
- corpus/90-verdicts/wave1/W1-16-r3.json: gap_closure references unknown gap_id round-2 verdict §D — three of four tiers reachable only through a pin.
- corpus/90-verdicts/wave1/W1-16-r3.json: gap_closure references unknown gap_id round-3's own report — the oversprint control was inert and was fixed.
- corpus/90-verdicts/wave1/W1-16-r3.json: gap_closure references unknown gap_id round-2 verdict secondary — save/fight.js SKIP omits `ai`, so any save/load with a hostile throws.
- corpus/90-verdicts/wave1/W1-21-r2.json: closure of GAP-W1-ui-map-outside-the-ar2-detectors is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-23-r1.json: gap_closure references unknown gap_id canon-registry-has-no-world-side-consumer.
- corpus/90-verdicts/wave1/W1-23-r1.json: gap_closure references unknown gap_id RI-LOR06-method-4-never-run.
- corpus/90-verdicts/wave1/W1-23-r1.json: gap_closure references unknown gap_id the-registry-goes-stale-within-minutes.
- corpus/90-verdicts/wave1/W1-23-r3.json: gap_closure references unknown gap_id W1-23-r1-G1.
- corpus/90-verdicts/wave1/W1-23-r3.json: gap_closure references unknown gap_id W1-23-r1-G2.
- corpus/90-verdicts/wave1/W1-23-r3.json: gap_closure references unknown gap_id W1-23-r1-G3.
- corpus/90-verdicts/wave1/W1-23-r4.json: gap_closure references unknown gap_id W1-23-r3-G1.
- corpus/90-verdicts/wave1/W1-26-r1.json: closure of GAP-W1-creation-is-an-api-not-a-scene is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-26-r1.json: closure of GAP-W1-the-naming-moment-is-still-orphan-text is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-26-r2.json: closure of GAP-W1-rendered-text-accessor-blind-to-the-hud-surface is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- corpus/90-verdicts/wave1/W1-26-r2.json: gap_closure references unknown gap_id RI-JRN01/HF3-imperative-on-the-hud.
- corpus/90-verdicts/wave1/W1-26-r2.json: gap_closure references unknown gap_id RI-JRN01/HF5-touch-leg-unmeasurable-round-1.
- corpus/90-verdicts/wave1/W1-26-r2.json: gap_closure references unknown gap_id RI-JRN01/O6-control-precedes-definition.
- corpus/90-verdicts/wave1/W1-FACTIONS-r2.json: gap_closure is object where the schema requires an array. Its closures are IGNORED — any gap it meant to close stays open.
- corpus/90-verdicts/wave1/W1-LIBRARY-r1.json: gap_closure references unknown gap_id K9-book-item-word-ratio.
- corpus/90-verdicts/wave1/W1-LIBRARY-MARTIAL-r4.json: gap_closure references unknown gap_id GAP-W1-library-l8-corpus-short.
- corpus/90-verdicts/wave1/W1-LIBRARY-MARTIAL-r4.json: gap_closure references unknown gap_id GAP-W1-library-martial-prose-tics.
- corpus/90-verdicts/wave1/W1-MAP-r2.json: gap_closure references unknown gap_id GAP-W1-map-c2-d1-d2-d3-measured-on-a-dead-walk.
- corpus/90-verdicts/wave1/W1-MAP-r2.json: gap_closure references unknown gap_id GAP-W1-map-trusts-one-field-the-player-writes.
- corpus/90-verdicts/wave1/W1-MAP-r2.json: gap_closure references unknown gap_id GAP-W1-map-writable-discovery-slot.
- corpus/90-verdicts/wave1/W1-POPULATION-r1.json: gap_closure references unknown gap_id GAP-W1-population-machine-never-called.
- corpus/90-verdicts/wave1/W1-READABLES-r2.json: gap_closure references unknown gap_id GAP-W1-library-has-no-reader-on-the-world-side.
- corpus/90-verdicts/wave1/W1-READABLES-r2.json: gap_closure references unknown gap_id GAP-W1-environment-channel-has-no-object.
- corpus/90-verdicts/wave1/W1-SAVE-r1.json: closure of GAP-W1-platform-save-drops-entity-prev-state is in the same wave (1) that opened it. Closure must come from a later wave's critic. Ignored.
- STALE-RISK GAP-W1-platform-prng-never-drawn: still open, and 5 later verdict(s) have been filed on the same piece or path since (crit-w1-00-r2-k4m9, crit-w1-09-r2-3c19, crit-w1-09-r3-3c19, crit-w1-deploy-r2-3c19, crit-w1-30s-r1-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-platform-save-drops-entity-prev-state: still open, and 2 later verdict(s) have been filed on the same piece or path since (crit-w1-save-r1-live-audit, crit-w1-map-r2-a91f). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-world-trunk-road-below-the-waterline: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-01-r2-world-b4e2). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-world-region-identity-is-a-tint: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-02-r1-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-the-building-is-smaller-than-the-room: still open, and 3 later verdict(s) have been filed on the same piece or path since (critic-road-join, crit-w1-04-r4-3c19, crit-w1-04-r5-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-the-door-puts-you-inside-the-building: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-04-r5-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-interior-door-draws-nothing: still open, and 4 later verdict(s) have been filed on the same piece or path since (crit-w1-04-r2-3c19, crit-w1-04-r3-3c19, crit-w1-04-r4-3c19, crit-w1-04-r5-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-settlement-has-no-outside: still open, and 4 later verdict(s) have been filed on the same piece or path since (crit-w1-04-r3-3c19, critic-road-join, crit-w1-04-r4-3c19, crit-w1-04-r5-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-creation-is-an-api-not-a-scene: still open, and 5 later verdict(s) have been filed on the same piece or path since (crit-w1-07-r2-3e7c, crit-w1-07-r3-3c19, crit-w1-26-r1-3c19, crit-w1-26-r2-3c19, critic-w1-26-r3). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-questionnaire-route-is-unreadable-and-unnamed: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-07-r3-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-the-naming-moment-is-still-orphan-text: still open, and 3 later verdict(s) have been filed on the same piece or path since (crit-w1-26-r1-3c19, crit-w1-26-r2-3c19, critic-w1-26-r3). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-input-discoverability-unmeasured: still open, and 2 later verdict(s) have been filed on the same piece or path since (crit-w1-08-29-r2, crit-w1-map-defects-r1-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-combat-exemplar-has-no-danger: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-09-r2-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-enemy-attacks-cannot-reach-contact-range: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-09-r3-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-weapon-movesets-not-wired-to-the-runtime: still open, and 2 later verdict(s) have been filed on the same piece or path since (crit-w1-10-r2-weapons-kritik, crit-w1-10-r3-weapons-kritik3). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-weapon-impact-and-material-model-has-no-consumer: still open, and 2 later verdict(s) have been filed on the same piece or path since (crit-w1-10-r3-weapons-kritik3, critic-w1-11-r1). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-12-approach-cannot-close-on-a-walking-player: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-12-r2-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-hearth-levelup-gate-reads-a-method-that-does-not-exist: still open, and 3 later verdict(s) have been filed on the same piece or path since (crit-w1-13-r2-death-hearth, crit-w1-13-r3-death-hearth, crit-w1-13-r4-clock-and-ar1). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-13-clock-consequences-never-fire: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-13-r4-clock-and-ar1). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-magic-effects-no-behaviour: still open, and 3 later verdict(s) have been filed on the same piece or path since (crit-w1-14-r2-7c31, crit-w1-14-r3c-4d18, crit-w1-14-r4). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-magic-skill-frozen: still open, and 2 later verdict(s) have been filed on the same piece or path since (crit-w1-14-r3c-4d18, crit-w1-14-r4). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-magic-spellmaking-has-no-world-side-surface: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-14-r4). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-crime-enforcement-is-a-free-win: still open, and 2 later verdict(s) have been filed on the same piece or path since (crit-w1-15-r3-3c19, crit-w1-15-r4-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-the-fence-buys-your-victims-own-furniture: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-15-r4-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-stealth-crime-model-not-coupled-to-the-world: still open, and 3 later verdict(s) have been filed on the same piece or path since (crit-w1-15-r2-3c19, crit-w1-15-r3-3c19, crit-w1-15-r4-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-16-equip-load-cannot-see-the-thing-in-your-hands: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-16-r3-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-17-an-archetype-with-no-mouth: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-first-ten-minutes-r1-a8e4c93). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-act5-argument-entry-shadowed: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-17-r1-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-mainquest-topic-bootstrap: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-19-r2-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-faction-ladders-are-doors-onto-empty-corridors: still open, and 1 later verdict(s) have been filed on the same piece or path since (critic-w1-20-r2). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-ui-map-outside-the-ar2-detectors: still open, and 2 later verdict(s) have been filed on the same piece or path since (critic-w1-21-r2, crit-w1-map-defects-r1-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-ui-detectors-that-cannot-see: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-map-defects-r1-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-audio-emitter-consumption: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-22-r2-s2). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-23-both-registered-voices-on-slavery-are-colonists: still open, and 2 later verdict(s) have been filed on the same piece or path since (crit-w1-23-r4, crit-w1-23-r3). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-two-placement-mechanisms-collide: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-23-r3). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-rendered-text-accessor-blind-to-the-hud-surface: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-26-r2-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-26-creation-cannot-be-completed-from-the-title: still open, and 1 later verdict(s) have been filed on the same piece or path since (critic-w1-26-r3). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-30c-wetness-mask-blind-to-instancematrix: still open, and 3 later verdict(s) have been filed on the same piece or path since (crit-w1-30de-r1-3c19, crit-w1-visual-blind-protocol-a-chunk2, crit-w1-orphaned-surface-shaders-r1-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-w1-30d-guard-misses-part-deletion: still open, and 2 later verdict(s) have been filed on the same piece or path since (crit-w1-30de-r1-3c19, crit-w1-orphaned-surface-shaders-r1-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-w1-30e-street-gate-never-ran: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-orphaned-surface-shaders-r1-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-f1-actor-body-shader-does-not-link: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-f10-characters-a71c). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-population-save-reload-repays-every-corpse: still open, and 2 later verdict(s) have been filed on the same piece or path since (critic-w1-souls-r2, critic-w1-souls). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-PACK-01: still open, and 2 later verdict(s) have been filed on the same piece or path since (judge-prose-packs-r1, crit-w1-30c-r1-2634eb0e). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-road-join-has-no-gate: still open, and 1 later verdict(s) have been filed on the same piece or path since (critic-w1-crossing). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-souls-alive-keyed-on-eid: still open, and 2 later verdict(s) have been filed on the same piece or path since (critic-w1-souls, critic-w1-attr-scale). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-souls-world-copy-of-the-values-is-53pc-stale: still open, and 1 later verdict(s) have been filed on the same piece or path since (critic-w1-attr-scale). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
- STALE-RISK GAP-W1-pbr-material-set-unbound: still open, and 1 later verdict(s) have been filed on the same piece or path since (crit-w1-orphaned-surface-shaders-r1-3c19). Read those before acting on this row's `what` — it is a snapshot of the tree on the day it was written, not of the tree today.
