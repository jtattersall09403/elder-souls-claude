# W1-F4-r2 — VERDICT: **FAIL, 2/10**

**Piece:** F4 round 2 — light, sky and atmosphere: the daytime horizon recolour
**Critic:** independent, fresh context, built none of this. **Judgement side: FIDELITY.**
**Build judged:** `codex/wave1-build-experiment`, working tree byte-identical to `HEAD` at
`game/src/render/sky.js` `5fd64d93…` and `game/src/render/lib/lighting-recipes.js` `24169068…` —
the same two shas the round's own `budget-r2-horizon-SHIPPED.json` records as SERVED.
**Every capture of mine is SOFTWARE (SwiftShader). Every claim below is a differential.**

---

## 0. The one-paragraph answer

**The round is right that it did not close `RI-VIS04` §2, and it is wrong about why.** Its ruling —
*"F4 CANNOT CLOSE `RI-VIS04` §2 BY ITSELF"*, on the ground that eleven configurations span
5.84–7.34° against a bar of 15 — rests on a battery that **structurally could not test the one
direction that works.** `f4-r2-light.mjs`'s `candidateCfg()` applies `warm` **only** to the sun and
`cool` **only** to the hemisphere and the ambient fill, in both its `soft` and its `hard` set. There
is no arm in the round, in either set, that makes the **key cooler**. I ran that arm. Recolouring
the key to `sky.js:644`'s own hardcoded night blue — nothing else touched — reads **17.07°** on the
sealed judged crop, against a minimum of 15 and a control of 7.35. Every ambient-side arm I ran sits
within 1.5° of that control, exactly as the round found. **The lever is the key's hue, in the cool
direction, and `noon-marsh` already carries the field that steers it: `keyWarmth`, currently `0.0`,
which `sky.js:854` lerps toward the zenith blue for negative values. Round 1's remedy said to set it
*positive*. Both rounds went the wrong way.**

That does not make the build pass. **`RI-VIS04` §2 is still absent by its own DETECT**, the score is
still capped at 2, and the round's headline gain on the sealed crop (+0.16°) is **smaller than the
metric's own run-to-run noise, which I measured at 0.22° on the same configuration in the same
process.** But the round's own full frame moved **3.75° → 6.38°**, out of the hard-fail band, and
**the round never reported that** — it published the full frame for the arm it *refused* and not for
the arm it *shipped*. So the correction runs in both directions, and I have reported both.

---

## 1. What I tried to falsify and could not — the round earns this

### 1.1 The instrument. I re-implemented it and it holds.

The round-1 critic wrote against itself: *"MY M6 IMPLEMENTATION IS MINE … a re-implementation by
another critic is the cheap way to falsify me."* The round-2 builder **reused** that implementation
rather than reimplementing — correct for comparability, and it left the instrument that carries both
rounds' hard fail unfalsified. I wrote `tools/m6-independent.mjs` from `RI-VIS03` §0 and §M6's
pseudocode **without opening `f4c-analyse.mjs`**. It agrees:

| | round's figure | mine, independent |
|---|---|---|
| pair01 sealed crop, landed r1 | 7.16 | **7.18** |
| pair01 sealed crop, shipped r2 | 7.32 | **7.34** |
| `REF-ER…2764066988` plate | 96.08 | **96.10** |
| `REF-ER…2772935437` plate | 16.06 | **16.99** |
| `REF-ER…2774897392` plate | 12.49 | **12.38** |

`C_shadow` reproduces to three decimals (14.146 → my 14.148; 12.377 → my 12.379), `shadow_mask_frac`
exactly (0.2458). **The hard fail is not an instrument artefact, and the instrument separates the
reference population from ours exactly as the item says it should.**

### 1.2 The light-budget ablation reproduces exactly on my own code

I wrote my own `crop-delta.mjs` and ran it over the round's own PNGs. Sealed judged crop, pair01,
mean |Δ|rgb: `key_off` **26.3266**, `env_off` **8.1013**, `hemi_off` **2.1774**, `fill_off`
**2.0384**, noise floor **1.8557**. Every figure is the round's, to four decimals. The **full-frame**
form, which is what is actually banked, says it harder: `hemi_off` 2.5208 and `fill_off` 2.4074
against a floor of **2.5028** — *below* it. **This is the first per-term light ablation in the
project and it is sound.**

### 1.3 The two premises the brief gave it were wrong, and it was right to refuse both

- **The moon.** Verified from a live per-light readback at 19:30, not from source alone: sun
  **0.0126** with colour `[0.4238, 0.3278, 0.4909]`, moon **2.952** with `[0.2623, 0.3968, 0.6867]`
  — which is linear-space `0x8ca9d8` exactly. `keyWarmth` reaches only `sunCol` →
  `this.sun.color`; nothing in any recipe reaches `this.moon.color`. The night's 22–33° comes from a
  hardcoded blue key that no recipe field touches. **`keyWarmth` is not the lever the night proves.**
- **Warming the key measures worse — and I falsified my own best counter-hypothesis.** I predicted
  that on the *new blue* horizon, warming the key would finally open the gap. It does not:
  `d2-warmhard100` (key forced to `(1.0, 0.76, 0.48)`, harder than any arm the round ran, on the
  shipped build) reads **3.34** against a control of 7.35. `d3` 3.31, `d4` 4.22, `d6` 5.49. **Warm is
  the wrong direction and the round proved it before I did.**

### 1.4 The cheap shadow ablation is genuinely equivalent — and I can make the case stronger

Re-derived from the round's own frames: the two ablation forms differ by mean |Δ|luma **0.3368** at
08:00 and **0.1365** at 13:00, while the *same configuration re-captured* differs by **1.0918** and
**0.4460**. 31% of the capture's own noise, at both hours. **Stronger form, which the round did not
state:** swapping which `base` frame you subtract moves `cast_shadow_area@tau8` by **0.00266**
(0.10364 → 0.10630) — **four times** the 0.00067 between the two ablation forms. *The choice of base
frame matters more than the choice of ablation form.* This belongs in `HAZARDS.md` and it makes
`RI-VIS04` §3-D2 affordable at breadth.

### 1.5 The twelve-angle tally, the matched luminance, and the §22 discriminator all check out

- Every count in the round's orbit tally re-derived from its banked JSON: **12 of 12** below the 15°
  minimum, **4 of 12** below the 6° hard fail, **5 of 12** retention below 0.60 with **1** below
  0.30, **2 of 12** cast-shadow area below §3-D2's floor. All correct.
- **Matched luminance holds.** `three.core.js:6309` sets `workingColorSpace = LinearSRGBColorSpace`,
  so Rec.709 on the raw triple is the right computation: **0.777124 → 0.777095**, Δ 2.9 × 10⁻⁵. (The
  round quotes 0.77706 → 0.77707; the digits do not reproduce and the true movement is a *fall*, not
  a rise. Immaterial.) Empirically better: mean `Yp` on the shadow quartile is **0.1714 → 0.1714**
  and on the lit quartile **0.3945 → 0.3940**. **The lever genuinely cannot have bought hue with
  brightness.**
- **HAZARDS §22.** The three source arms served three *different* lighting shas, recorded in each
  manifest. This is not one tree measured three times.

---

## 2. The ruling. It is not overturned — it is unsupported by its own evidence.

The round's ruling, marked reversible, with its own overturn condition: *"any configuration of
`key`/`sky`/`fill`/`env`/`keyWarmth`/`envGroundBounce` and the sky colours that measures
`hue_offset >= 15` on the sealed pair01 crop without failing S60 clause (a)."*

**First: the round's own tool contains a second candidate set it never fired.** `f4-r2-light.mjs`
defines `SETS.soft` and `SETS.hard` — `WARM_HARD`, `COOL_HARD` and `mixRaw`, the same levers *without*
the constant-luminance normaliser its own comment says *"pulls a lerp back toward where it started"*.
`grep -rn "coolhard\|warmhard\|both100\|amb250\|amb180"` over the round's artifacts, its status file
and its reports returns **nothing**. Seven arms, written, never run, and the lever class ruled
exhausted without them. I fired them (`d1`–`d6` below).

**Second, and this is the finding: neither set can cool the key.** In `candidateCfg()`, `warm` is
applied only where `o === window.__sunLight`; `cool` only to `isHemisphereLight` and `isAmbientLight`.
**The screening instrument was structurally incapable of testing the key in the cool direction.**

### The battery, pair01, 08:00, sealed judged crop `[300,150,512,512]`, my instrument

| arm | what it does | `hue_offset` | `retention` | `C_shadow` | `C_local_med` | mean `Yp` lit |
|---|---|---:|---:|---:|---:|---:|
| `x0-control` | shipped build | 7.35 | 0.6805 | 12.379 | 0.0245 | 0.3940 |
| `x0b-control-recheck` | **same config, re-captured** | **7.57** | 0.6646 | 12.785 | 0.0250 | 0.3881 |
| `d1-coolhard100` | ambient → hard blue | 7.30 | 0.6593 | 12.272 | 0.0245 | 0.3916 |
| `d2-warmhard100` | key → hard warm | 3.34 | 0.6386 | 14.150 | 0.0230 | 0.3785 |
| `d3-both100` | both | 3.31 | 0.6392 | 14.028 | 0.0228 | 0.3766 |
| `d4-both100-amb250` | both + ambient ×2.5 | 4.22 | 0.6760 | 14.476 | 0.0240 | 0.3862 |
| `d5-both100-amb250-env50` | + probe ×0.5 | 4.36 | 0.6622 | 14.167 | 0.0233 | 0.3627 |
| `d6-both60-amb180` | both 0.6 + ambient ×1.8 | 5.49 | 0.6811 | 13.793 | 0.0244 | 0.3869 |
| `x4-amb400-only` | ambient ×4, no recolour | 8.87 | 0.7530 | 13.995 | 0.0271 | 0.4121 |
| `x5-env300-only` | probe ×3 | 6.22 | 0.7180 | 13.780 | 0.0282 | 0.4623 |
| `x6-warmamb-only` | ambient → warm | 7.37 | 0.6639 | 12.981 | 0.0250 | 0.3884 |
| **`x1-moonkey`** | **key → `0x8ca9d8`, the game's own night blue** | **17.07** | 0.6037 | 8.533 | 0.0214 | 0.3597 |
| `x2-moonkey-warmamb` | + warm ambient | 16.51 | 0.6076 | 8.524 | 0.0213 | 0.3595 |
| `x3-moonkey-warmamb-amb400` | + ambient ×4 | **16.15** | **0.7132** | **10.371** | 0.0239 | 0.3847 |

Every arm is `LIVE` with `cfgError: null` and a readback proving it applied (`battery.json`).

**Three things this says, and they are not the same thing.**

1. **The round's conclusion about the ambient side is correct and now much better evidenced.**
   Ambient *colour* does nothing in either direction (`d1` −0.05, `x6` +0.02, both inside the 0.22°
   noise). The probe at ×3 makes it **worse** (6.22), which independently vindicates the refusal of
   the `env` raise. Ambient *intensity* at ×4 is worth +1.52 and improves `retention`, `C_shadow` and
   `C_local_med` — so the hemisphere and fill **are** levers once they are resolvable, which is why
   §2-D2 below forbids reading "unresolvable at its shipped value" as "not a lever".
2. **The key's hue in the cool direction clears the bar on the crop.** 17.07 against 15. That is the
   ruling's overturn condition as written.
3. **`x1` as it stands buys part of it with brightness, so I ran the S59-guarded version.** Mean `Yp`
   lit 0.394 → 0.3597 and shadow 0.1714 → 0.1303, because `(0.549, 0.663, 0.847)` carries Rec.709
   luminance 0.6521 against the shipped key's 0.9441. **So I compensated the key's intensity by
   0.9441/0.6521 = 1.4478 and re-measured** (`metrics/matched-key-hue-s59-and-s60.json`):

| `y1-moonkey-lummatched`, pair01 sealed crop | control | matched arm | Δ |
|---|---:|---:|---:|
| **`hue_offset`** | 7.35 | **17.11** | **+9.76**, above the 15 minimum |
| mean `Yp`, lit quartile | 0.3940 | 0.3906 | **−0.9%** |
| mean `Yp`, shadow quartile | 0.1714 | 0.1683 | **−1.8%** |
| `retention` (min 0.60) | 0.6805 | 0.6759 | flat, passes |
| `C_shadow` (min 6) | 12.379 | 8.935 | falls, passes |
| `C_local_med` (min 0.045) | 0.0245 | **0.0245** | identical |

**It is not bought with brightness.** Both luminance quartiles are matched to within 2%,
`C_local_med` is unchanged to four decimals, and `hue_offset` more than doubles. This is the S59
guard applied to the arm that threatens the ruling, and the arm survives it.

**And S60 clause (a), measured *inside* that configuration rather than inherited from the shipped
one** — its `shadows_off`, `key_off` and `env_off` arms captured in the same process, same pose:

| `lit_ratio` (bar 2.0) | τ1 | τ2 | τ4 | τ8 | τ16 |
|---|---:|---:|---:|---:|---:|
| sealed judged crop | 3.482 | 3.452 | 3.424 | **3.402** | 3.370 |
| full frame | 3.270 | 3.248 | 3.219 | **3.182** | 3.142 |

**It passes at every τ in both domains, and it passes *higher* than the shipped build does** — whose
own table runs 3.383 down to 3.301 across τ1–τ16 on the crop and 3.192 down to 3.096 on the frame.
The ruling's overturn condition — *"without failing S60 clause (a)"* — is met. The companion arm `y2` (blue key + warm ambient at 4×) reads **16.42** with
`retention` **0.7747** and `C_shadow` **10.694**, both *better* than the shipped build, at the cost of
running 4–6% bright.

*(Clause **(b)** is **not measured** for this configuration and I am recording that rather than
comparing across runs: it needs a `shadows_off` arm on the **control** in the same process, which this
battery does not carry. A cross-run area comparison is exactly the mistake HAZARDS §12 and §22 warn
about. Round 3 must capture both.)*

**The one caveat I will not bury: the page-side arm is an UPPER BOUND on the source change**, not a
lower one. `bakeEnvironmentProbe()` consumes the `uSunColour` *uniform*, which a page-side light
recolour does not touch, so in source a bluer key would also blue the probe's sun lobe and pull the
two quartiles back toward each other. *(I wrote the opposite in my own tool header and corrected it
before filing.)*

**So: the ruling's overturn condition is met on the sealed judged crop, at matched luminance, with
every companion metric preserved — subject to that upper bound and to S60 clause (a), measured
below. The ruling does not stand as written. Round 3 must not treat "F4 cannot close §2" as settled,
and ring 1's ordering does not need the rewrite the ruling asked for.** The measurement that finishes
it is a *source* arm on `keyWarmth` — this verdict's single biggest gap.

---

## 3. Corrections. Two matter; one is precision; one runs in the build's favour.

### 3.1 *"a HemisphereLight of live intensity 5.0011"* is **0.371** in the frame

`f4-r2-light.mjs`'s readback sums `o.intensity` over **every** `isHemisphereLight` in the scene
graph, and `grep -n HemisphereLight game/src/` returns five construction sites. My census, with each
light's parent chain and its visibility (`metrics/light-census-visible-vs-scenewide.json`), at 08:00:

| | scene-wide sum | **visible chain** |
|---|---:|---:|
| HemisphereLight | 5.0011 | **0.37107** |
| AmbientLight | 0.4669 | **0.18693** |

The other **4.63** sits inside hidden `Group`s — `arena-bounded-readable-fill` (1.30),
`rootway-bounded-fill` (0.48), three interior sets (1.05 / 0.95 / 0.85) and more. **The measurement
stands; the sentence built on it is wrong by 13×**, and it matters because the round turns
"unresolvable" into a property of the *lever class* when it is a property of the *value round 1 left
the term at* — 0.371, down from 0.4948. `x4-amb400-only` is that hypothesis tested: at 4× it is worth
+1.52°. **This is the S58/S60/S63 pattern arriving for the fourth time — a sound measurement with an
invented mechanism attached — and it is exactly what `ARBITRATION.md` warns about.**

### 3.2 *"`overcast-flat` and `storm` ARE UNTOUCHED AT THEIR OWN WEATHER … safe by arithmetic"* — false for the recipe that carries most of the daylight

`recipeForConditions()` (`lighting-recipes.js:346-352`) selects `overcast-flat` at
**`overcast >= 0.50`**, not at 1.0. `hor.lerp(neutral, overcast)` therefore retains **`1 − overcast`**
of the edit. From `sky.js`'s own `WEATHER` table:

| state selecting `overcast-flat` | `overcast` | **fraction of the edit that survives** |
|---|---:|---:|
| `dry_thunder` | 0.55 | **45%** |
| `sea_fog` | 0.62 | 38% |
| `fog` | 0.70 | 30% |
| `fever_fog` | 0.75 | 25% |
| `overcast` | 0.80 | 20% |
| `warm_rain` | 0.82 | 18% |
| `salt_storm` | 0.88 | 12% |
| `cold_rain` | 0.90 | 10% |
| `rain` | 0.92 | 8% |

`storm` the *recipe* is also selected by `heavy_rain` (`overcast` 0.94, `rain` 1.0) → 6% survives.
Only the `storm` and `sea_squall` *states*, at `overcast` 1.00, are genuinely erased. The round-1
critic costed `overcast-flat` + `storm` at **59.1% of daylight**.

**And this is measured, not argued.** `sky.js:889` does `this.hemi.color.copy(hor).lerp(regionNight,
night*0.85)`, and at 13:00 `night = 0` — **so the hemisphere's live colour *is* `hor` after the
overcast lerp.** Read back off the running build:

| weather | `overcast` | measured `hemi.color` | the neutral | erased? |
|---|---:|---|---|---|
| `clear` | 0.00 | `[0.677, 0.790, 0.944]` | — | n/a — this is the new day-end triple, exactly |
| `fog` | 0.70 | `[0.4635, 0.5023, 0.5486]` | `[0.43, 0.44, 0.46]` | **no** |
| `overcast` | 0.80 | `[0.4604, 0.4910, 0.5417]` | `[0.43, 0.44, 0.46]` | **no** |
| `rain` | 0.92 | `[0.4346, 0.4541, 0.4866]` | `[0.43, 0.44, 0.46]` | **no** |
| `storm` | 1.00 | **`[0.4300, 0.4400, 0.4600]`** | `[0.43, 0.44, 0.46]` | **yes, exactly** |

Both endpoints validate the arithmetic: at `overcast = 1.00` the measured colour is the neutral to
four decimals and independent of that state's tint, and at `clear` it is the new day-end triple. In
between, `1 − overcast` of the edit survives — **0.0449 of blue at the `overcast` state**, against a
neutral of 0.46.

**So this round did change the recipe that carries most of the daytime, and the shipped source comment
asserts the opposite — *"Both are checked as null controls rather than argued"* — while the status
file says plainly *"I captured nothing in overcast or storm."* The comment claims a control that was
never run.** `metrics/weather-preservation-claim.json`,
`frames/weather-{overcast,storm}-hemi-*.png`.

*What this does not say:* nobody has measured M6 or S60 in overcast or storm on **either** build, which
is the round's own point and its own recommendation. It says the claim of untouchedness is false, and
that a preservation clause was argued instead of captured.

### 3.3 *"NIGHT IS UNTOUCHED … `night-moon` cannot have moved"* — an overreach, and negligible

`day = clamp(elev·1.6 + 0.28)` is 0 only for `h ∉ (5.33, 18.67)`, while `night-moon` holds
`h ∈ (18.18, 5.82)`. So roughly **one hour a day** of `night-moon` has `day > 0` and the edit does
reach it. **Magnitude at the boundary: ≤ 0.005 in the ambient's blue channel**, after the
`regionNight` lerp. Correction of precision, not a defect — the *measured* window at 19:30 sits
squarely in the `day = 0` band and is genuinely bit-identical.

### 3.4 In the build's favour: the round under-reported its own result

S64 requires the full frame *always*, alongside the crop. `M6-fullframe-before-after.json` contains
**two** rows — the BEFORE arm and the **REFUSED** arm. **The arm that shipped is not in it.** I
measured it:

| pair01, 08:00, **full frame** | `hue_offset` | `retention` | `C_shadow` |
|---|---:|---:|---:|
| BEFORE, landed r1 | 3.75 | 0.9672 | 11.600 |
| **SHIPPED, r2 horizon** | **6.38** | 0.9415 | 10.279 |
| REFUSED, env 0.55 | 4.51 | 0.8533 | 10.911 |

**The shipped change moved the full frame +2.63°, a 70% rise, and lifted it from below the 6° hard-fail
line to above it.** The crop moved +0.16°. **That is a 16× divergence between the two domains — which
S64 says is itself a finding — and the round asserted the opposite** (*"the crop and the frame AGREE
— no divergence"*), on the S60 table, having never computed M6 on the shipped full frame.

### 3.5 Two banked frames named `AFTER-r2` are different arms

`frames/AFTER-r2__pair01-crop.png` measures **7.27** — it is the **refused** `env 0.55` arm.
`frames/pair01__AFTER-r2__judged-crop.png` measures **7.34** — that one is the shipped build. Nothing
in the names distinguishes them, and a reader opening the first to see "what F4 r2 shipped" is looking
at a configuration that is not in the tree. Same family as HAZARDS §17.

---

## 4. The acceptance is still not met, and this round measured one window of five

`S60-clauses-three-arms.json` carries `"window": "pair01 08:00 clear"`. **One.** S60 says *"in all
five judged windows"* and S64 fixed the *domain*, not the *count*. Round 1 measured all five and
failed clause (a) at **4 of 5** in each domain — pair03's crop at 0.473, pair04's frame at 1.821.
**Neither was re-measured this round.** The round's *"clause (a) PASSES at every tau on the shipped
arm"* is true and is scoped to pair01; it must not be read as the acceptance being met. **S60 clause
(a) remains NOT MET.**

Clause (b) at pair01 moves −0.35% to +0.37% and changes sign with tau — the round is right to call
that unmoved-within-the-instrument rather than preserved.

---

## 5. `CRITIC-DOCTRINE` §1.3 — I extended the bar, ADD only, and it is not why I fail this

`RI-VIS04` §2 gains **§2-D2** (`corpus/70-visual/RI-VIS04-renderer-feature-checklist.md`): *enumerate
the light budget per light, on the visible rig, against the capture's own noise floor, before calling
any term a lever or ruling one out.* Four steps: a census of lights whose **parent chain is visible**
(not a scene-wide sum — §3.1 above is the 13× error that motivates it); one arm per visible light plus
`scene.environment`; a `base` re-capture last as the run's own floor; and — the clause that matters —
**a term inside that floor is `unresolvable at its shipped value` and may never be reported as absent,
as "not a lever", or as evidence about a class of levers.**

**Bounded exactly as §1.3 requires.** DETECT and method only. It adds no route to a higher §14 mark
and no route to a higher score for any build; a critic may cite it to withhold a mark and never to
grant one. It lowers no threshold and changes no number in §2. **It is not the reason for any pass:**
this verdict is a FAIL and the FAIL is set by §2's own pre-existing DETECT, which predates me.
Overturned if a visible-chain census proves unavailable to a harness, in which case §2 records the
budget `UNVERIFIED`, which counts as absent.

I checked the whole corpus before declaring the hole: `RI-VIS03` M6 defines the metric and no method
for attributing it; `RI-VIS04` §2 names the DETECT and no budget; §3-D2/§3-D3 (added by the r1 critic)
cover shadows, not the light budget; `ARBITRATION` S60 clause (a) compares key against probe and puts
the hemisphere and the fill in **neither arm**. Nothing covered this.

---

## 6. Motion — the half of the directive this round dropped

The round captured twelve orbit angles and **zero moving frames**, which by its own account makes it
weaker than the critic that judged the round before it. I drove a walk through the shipped input
pipeline — real `queueInputs` `{f, move:[0,1]}` events, the closed key set — at two hours, camera
trailing the moving body, **displacement measured per frame per HAZARDS §16 so a dead input path would
show as data**:

- **08:00:** 0 → 0.267 → 0.960 → 1.653 → 2.347 → **3.040 m**
- **13:00:** 0.533 → 1.227 → 1.920 → 2.613 → 3.307 → **4.000 m**

**12 of 12 frames LIVE, no aborts.** The character really moves, and the figures reproduce the r1
critic's driven walk exactly — which is itself a determinism check on the input path.

**What the motion shows, and it is the honest answer rather than the flattering one:** nothing the
stills did not. Through both sequences the ground stays one flat olive plane whose lit and shadowed
faces differ in **brightness** and not in **hue**. That is what `hue_offset` 7.34 means, and motion
neither hides it nor reveals anything new — because it is a property of the light budget, not of the
pose. Which is consistent with the twelve-angle orbit reading below the bar at 12 of 12.
`metrics/driven-walk.json`, `frames/walk-t8-f05-driven-3.04m.png`.

---

## 7. Blind gate — `not_possible`, failed closed, and the orchestrator owes it

`RI-VIS04` declares `blind_pair: no`, so no gate is owed on this item directly. But F4 exists because
Protocol A ranked us **last of five, twice**, and the judgement this round is trying to move is a
blind one. A critic cannot spawn a fresh judge — `create_session` is approval-gated and `CLAUDE.md`
rule 0 forbids it; rule 0e places this with the **orchestrator** and says an unrun gate is a live
blocker, not a footnote. **Measured this turn, not inherited from my brief — which said 100 and was
already stale:** `grep -rl "blind_pair: yes" corpus/ --include=*.md | wc -l` → **102**.
`grep -rln "blind_status: *run" corpus/90-verdicts/ | wc -l` returns **1**, and that one hit is
`W1-F7-WATER-r2.md:209` **quoting this very command**, not recording a run — so the true count of
verdicts that have ever run a blind pair is still **0**. The population has grown by 4 since rule 0e
was written and the number of gates run has not moved. **The orchestrator owes this one.**

---

## 8. Score

**Min over axes. Any triggered hard fail caps the item at 2 (`SCORING.md` §1.1). Overall 2. FAIL.**

| axis | measured | score |
|---|---|---:|
| A — `RI-VIS04` §2 by its own DETECT | crop 7.16 → 7.32, full frame 3.75 → **6.38**; 12/12 orbit angles below the 15 minimum and **4/12 below the 6 hard fail** | **2 (hard fail, caps the item)** |
| B — S60 clause (a), the piece's own acceptance | measured at **1 of 5** windows; passes there (3.33 at τ8); the two windows that failed in r1 were not re-measured | 3 |
| C — clause (b) / §3-D2 area | unmoved within the instrument at pair01; **2 of 12** orbit angles below the floor | 5 |
| D — §3-D3 retention / `C_shadow` | not triggered (area did not rise); `retention` flat, `C_shadow` −12.5% but above 6; on the orbit 5/12 below the minimum, 1 below the hard fail | 4 |
| E — the motion directive | twelve angles, **zero moving frames** from the build; the critic supplied the walk | 4 |
| F — coverage | one judged window; `overcast-flat`/`storm` uncaptured **and**, contrary to the claim, touched | 3 |
| G — evidence integrity | the headline crop ablation is banked only in its full-frame form; two frames named `AFTER-r2` are different arms; the shipped arm's full-frame M6 is missing while the refused arm's is present | 4 |
| H — diagnostic value and candour | the first per-term light ablation in this project; a reusable ~10× cheaper §3-D2; a gain measured and refused; two brief premises falsified rather than inherited | **8** |

**What the 2 is and is not.** It is not a claim that the round did nothing or made the game worse —
the full frame moved out of the hard-fail band and the round's diagnostic product is the best in this
file. It is the same pre-existing absence that capped round 1, still open, now with a much better map
of where it is not and one measured pointer to where it is.

---

## 9. The single biggest gap, and the remedy is one number in a file F4 already owns

**`GAP-W1-f4-the-key-has-no-hue-and-the-field-that-would-give-it-one-is-set-to-zero`.**

`noon-marsh` declares `keyWarmth: 0.0`. `sky.js:853-854` reads it **both ways** — positive lerps
`sunCol` toward the horizon (warm), **negative lerps it toward `zen`**, which at `day = 1` is
`(0.115, 0.305, 0.620)`, a strong blue. Round 1's remedy said *"start at +0.45"*. Round 2 measured
warm, found it worse, and generalised that to the whole lever class using a battery whose `cool` term
is wired only to the hemisphere and the ambient fill. **Two rounds, one sign, never tried.**

**The remedy must be in source, not page-side, and that is the point.** `bakeEnvironmentProbe()` bakes
from `uSunColour`, so only a source change moves the key *and* the probe together — which is exactly
why my page-side number is an upper bound and why the source arm is the measurement that finishes
this. Two shapes, either acceptable: set `noon-marsh`'s `keyWarmth` negative **and** raise the 0.30
lerp coefficient at `sky.js:854` (at `keyWarmth = -1.0` the coefficient alone reaches only
`(0.7345, 0.7495, 0.7600)` — near-neutral, and 21% dimmer, so it is not enough on its own); or give
the day end of `sunCol` at `sky.js:849-850` a cool triple directly, in the same style as the horizon
triple this round already shipped, **with `R.key` raised by the reciprocal of the Rec.709 luminance
ratio** so the key's luminous output does not move.

**Acceptance — at all five judged windows, because S60 says five and this round did one.** (i)
`hue_offset` ≥ 15 on the sealed crop at pair01–pair03 and not lower at pair04–pair05. (ii) **S59
preservation, paired:** mean `Yp` on the lit **and** shadow quartiles each within ±3% of the shipped
build, at every window. (iii) `retention` ≥ 0.60, `C_shadow` ≥ 6, `C_local_med` not lower. (iv) S60
clause (a) ≥ 2.0 on the lit subset, measured *inside* the new configuration; clause (b) not falling.
(v) **The noise floor published with the result** (S61) — capture the shipped configuration twice in
one process and report the spread; mine was 0.17 and 0.22°, larger than the whole of round 2's crop
gain. (vi) The twelve-angle orbit re-shot, since 4 of 12 angles currently sit below the 6° hard fail
and one pose passing is not the feature being present.

---

## 10. What I could not do, said as plainly as what I did

- **I measured one judged window, pair01 — and so did the round.** S60 names five. Four more windows
  across three arms was not reachable at ~60–75 s per 1920×1080 capture with the box at load 17. I
  recorded the shortfall as a finding against the round rather than repeating it silently, and the
  remedy's acceptance names all five.
- **My key-hue arms are page-side and are an UPPER BOUND**, for the `uSunColour` reason above. I first
  wrote the opposite in my own tool header, caught the inverted sign, and corrected the file before
  filing. It is the single cheapest way to falsify this verdict: one source arm.
- **S60 clause (b) is unmeasured for the key-hue configuration.** It needs a `shadows_off` arm on the
  *control* in the same process and this battery does not carry one. Comparing across runs is the
  mistake HAZARDS §12 and §22 exist for, so I did not.
- **No overcast or storm before/after capture.** I evidenced §3.2 from the live hemisphere colour plus
  the affine structure of the expression, which is weaker than a before/after pair and is recorded as
  such.
- **`RI-VIS04` §14's thirteen-feature table is not filled** (rows 2 and 3 only), and §3's cascade-ring
  DETECT and penumbra pixel count were not run. Both recorded `UNVERIFIED`, which counts as absent.
- **Every capture is SwiftShader.** No Pod, no hardware confirmation of any window. Every claim here is
  a differential inside one process, and every run carries its own noise floor.
- **My M6 is mine, like the r1 critic's before it.** It reproduces both the round's figures and the
  reference population, and it was written without reading the implementation it agrees with — but a
  third implementation is the cheap way to falsify me.
