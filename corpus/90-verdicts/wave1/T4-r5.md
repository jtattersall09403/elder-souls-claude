# T4 — the Morrowind screens, round 5

**Verdict: FAIL.** Piece score **2** — min over `RI-UIX03` **3**, `RI-UIX04` **6**, `RI-UIX06` **2**,
`RI-UIX07` 5, `RI-UIX09` **5**, `RI-UIX10` **6**; `RI-UIX08` **2** recorded and excluded (below).
Wave-1 gate **7.0**. **AR-1 pass · AR-2 pass.**
Critic `crit-t4-r5`, fresh context. **I built none of this and I am not the round-1, -2, -3 or -4
critic.**

Judged at **`HEAD` = `76cd4848`**, and at **blobs**, because siblings moved `HEAD` while I measured:
`game/src/ui/chrome.js` **d584b196**, `game/src/ui/screens/progress.js` **90c31d0d**,
`game/src/ui/screens/inventory.js` **0bee2636**, `game/src/ui/screens/text.js` **cfc9125f**
(unchanged since round 4), `game/src/ui/system.js` **830ae9de** (unchanged since round 4). All five
on disk equal `git ls-tree HEAD`.

**Delete-the-fix ran for real, from an explicit pinned sha — `cfed8aaf65bcb0508ac3367266350241acfff14e`,
the parent of round 5's first UI commit `80829a44`, NOT `HEAD~1`.** Its three UI blobs are
`5921cc2b` / `f46f7fe6` / `79d64ff3` — byte-identical to the ones `T4-r4.md` cites, so the control arm
is round 4's build exactly. The full seven-screen sweep re-run there reproduces round 4's own numbers
to four decimals on every screen (journal **0.1805**, sheet **0.1713**, spells **0.3026**, level-up
**0.1855**, container **0.1526**, inventory **0.1557**), which is the check that the control is the
build I think it is.

---

## The three-paragraph answer

**Round 5 did the work it was sent to do, and it is visible.** The five hard fails round 4 cleared are
still clear, and **five of the six FD4 instances that made round 4 a 2 are fixed**. On the **level-up screen the ten attribute rows now read** — `STRENGTH 12`, `ENDURANCE 20`,
… `LUCK 10`, name, value and gauge in three columns with the soft/hard cap ticks still cut into the
bar (`crops/levelup-rows-6x.png`, `crops/levelup-personality-10x.png`) — against a control at
`cfed8aaf` where the same rows read `STREN⟨12⟩`, `ENDO⟨20⟩`, `AGILI⟨10⟩`
(`crops/control-cfed8aaf-levelup-rows-6x.png`). The **character sheet** shows all eleven attributes,
both skill columns and the identity block clean (`crops/sheet-panel-3x.png`). The **journal** is the
one I most expected to still be broken and it is not: driving the pager with real `KeyD` presses
through an input layer I asserted attached (`real.attached=true`, `input.moveX=1`), **19 of 19
entries over 10 spreads, and 0 of 19 blocks past the panel foot and 0 of 19 over the foot hint** —
against **8 of 19** and **11 of 19** in round 4, worst then +105 px onto the world, worst now **−89 px
inside it** (`reports/journal-per-page-overflow.json`). Both cut foot hints wrap and read whole
(`crops/container-panel-3x.png`, `crops/spells-panel-3x.png`). Density holds on the full seven-screen
sweep I ran myself, agreeing with the project instrument to four decimals on **all seven**:
inventory 0.1557 · journal 0.1740 · sheet 0.1730 · spells 0.3040 · level-up 0.1522 · container
0.1538 · container-with-one-item 0.1449. `frame-liveness.mjs` reports **8 of 8 LIVE**.

**The builder's headline explanation of its own number is wrong, and this is evidence about the bar
rather than about the builder.** The status file says level-up's density *fell* 0.1855 → 0.1521
because "fixing the collision removes the overlapping-ink pixels that were inflating round 4's
number, which is S58's own finding stated the other direction." **It cannot be.** `D2` is the
cardinality of a set — the pixels inside the panel rect differing from its modal colour — and two
glyphs drawn on one pixel make that pixel differ **once**. Collapsing three columns into one can only
*shrink* the union; separating them can only *grow* it. So the fall had to come from something that
was removed, and it did: measured on the two arms with the same panel rect
(`[720,300,480,480]`, identical in both manifests) and the same row rects,
**95.7 % of the whole panel's density fall happens inside the ten attribute rows**, and inside them
the **solid-trough (gauge) columns collapse from 918 to 551 — a 40 % cut** — which is exactly
`progress.js` moving the gauge from `r[2] * 0.64` (156 units) to `r[2] − gaugeX0 − 10` (72 units)
(`reports/t4-r5c-attribution-levelup.json`, `reports/t4-r5c-attribution-split.json`). **Level-up now
clears DN4's floor by 0.0022 because its gauge is half as long, not because its screen got fuller.**
S58's *ruling* — DN4 necessary, not sufficient; a density figure counts only with a legibility pass —
is untouched and correct. S58's stated *mechanism* is not what happened, in round 4 either: level-up's
0.0910 → 0.1855 was the **denominator** falling 5.1× (57.2 % → 11.1 % of frame) while the numerator
fell less. That distinction matters, because a builder who believes ink-over-ink inflates `D2` will
reach for the wrong lever next time.

**And then I opened the one screen round 5 touched last, and it is still clipping text.** On the
**container**, `getUIState()` declares `container.band` as the whole 137-character bark-token
description and the panel renders it ending at *"…who sees it knows what it means"* — **ten words
gone, no ellipsis, drawn nowhere in the frame** (`crops/container-desc-tail-5x.png`; the identical
string renders **whole** on the inventory screen's bigger detail panel, `crops/inventory-panel-1x.png`,
which is the cleanest possible demonstration that this is box-size and not data). And **every one of
the seven list rows declares a gold value that no row draws** — `"Black-water draught  0.4  30"`,
`"Bog-iron maul  11.5  96"`, `"Reed-cutter  3.0  42"` — because the three columns are pitched at
`0.60 / 0.19 / 0.19` of `half` on top of a `ICON + 10 = 38`-unit inset inside a rect only
`half − 16` wide, so the gold column *begins* 12 units past the row's own right edge and
`surface.js`'s *"the declared rect IS the clip"* removes all of it
(`crops/container-blackwater-8x.png`, `excerpts/container-text-clipped.md`). `RI-UIX06` **FD4** —
*"0 clipping, 0 overlap, 0 overflow … hard fail: any at 1920×1080"* — is therefore still triggered,
on a value screen where the number you are deciding on is the one that is missing.

---

## Per-item

| Item | Native | Ladder | Movement | State |
|---|---|---|---|---|
| `RI-UIX03` inventory + pause + level-up | 8 / 13 | **3** | 7/13 → 8/13, ladder flat | **M-E2 flips back to pass** — the ten values are readable at 1920×1080 and at 1280×720. M-C1 / M-L1 / M-D1 still fail on data that `git diff --stat 7c63111d..HEAD -- game/data/` proves has not moved for a fourth round |
| `RI-UIX04` journal | 10 / 12 | **6** | **5 → 6** | **JU9 flips to pass**: 0 of 19 entry blocks past the panel foot over all 10 spreads, page count drawn on every spread. JU5, JU12 still fail |
| `RI-UIX06` diegesis and UI style | ART 5/7 · **FID hard fail + 6 unmeasured** | **2** | — | **FD4 still triggered** (container band + gold column). And see the finding below: `tools/metrics/ui-metrics.mjs` **exists**, which this item and three verdicts say it does not |
| `RI-UIX07` out-of-combat HUD | 10 / 11 | 5 | — | carried on an empty diff of `hud.js` since round 4's pin; V11 (§G) still unrun |
| `RI-UIX08` dialogue window | — | 2 | — | procedural cap, unrun §G. **Judged elsewhere; recorded, not credited, not duplicated, and EXCLUDED from the aggregate — see below** |
| `RI-UIX09` populated density | 5 / 7, no hard fail | **5** | — | full seven-screen sweep re-run; **the 0.35 bar is met on none of them**, and level-up's margin over the floor is now **0.0022** |
| `RI-UIX10` operability | 7 / 8 | **6** | **8 → 6** | re-pressed after a reflow that touches hit-testing — **22/24** on round 3's instrument: `JX0–JX6`, `EQ1`, `EQ1b`, `EQ2`, `TS0–TS3b`, `MAP1`, plus my own `JP0–JP5`, `CP0–CP2`. **The drop is the gamepad arm, which was not re-run and is scored 0 fail-closed** — not a demonstrated defect |
| `RI-UIX01` combat HUD | — | — | — | **excluded for the fifth round**; §D still unmeasured. Round 3's recommendation stands and has still not been dispatched |

**The min is 2 and it is set by `RI-UIX06`, which belongs to this round.**

### The ruling on `RI-UIX08`, and it is reversible in one line

Round 4 put `RI-UIX08`'s procedural **2** into the min alongside its own `RI-UIX06` **2**, so the
piece scored 2 twice over. My brief says *"`RI-UIX08` remains capped at 2 by its own unrun §G — do
not credit it and do not duplicate it."* **I record it at 2 and exclude it from the aggregate, exactly
as `RI-UIX01` has been excluded for five rounds, and I state both numbers so nothing is hidden: min
including it is 2; min excluding it is also 2.** The exclusion changes nothing this round. It is
written down because it *would* have changed something if `RI-UIX06` had cleared, and a critic should
not discover that rule for the first time in the round where it matters. **What would overturn me:** a
ruling that a cap owned by another piece is part of this piece's gate, in which case T4 cannot exceed
2 until `RI-UIX08` §G runs and the number says nothing about T4's own work.

---

## `RI-UIX09` — the full sweep the builder could not run, and what it shows

`node tools/ui/t4-r2-measure.mjs --out …/T4-r5c/measure --label head` (all **eight** states, exit 0),
then `node tools/ui/t4-r3-density.mjs --measure …/T4-r5c/measure-live --out …/T4-r5c/reports`.
`node tools/visual/frame-liveness.mjs --in …/T4-r5c/measure/screens` → **8 frames, `{"LIVE":8}`**.

> **Instrument note, because it nearly cost me the run.** `t4-r3-density.mjs` hard-codes the label
> `live` (`measure-live-1920x1080.json`, `screens/live-*.png`) and, when it does not find it,
> `t4-r2-critic-measure.mjs` — which **executes its whole CLI on import** — silently reports the
> **T4-r2** artefacts instead. My first invocation printed a full report about captures from another
> round. The sweep was re-labelled into `measure-live/` rather than re-run. This is the fourth tool in
> this family with a hard-coded path and the second that runs on import.

| Screen | `D2` round 4 | `D2` **round 5** | `D2` **control at `cfed8aaf`** | panel % of frame | agrees with builder instrument |
|---|---:|---:|---:|---:|:--:|
| inventory | 0.1557 | **0.1557** | 0.1557 | 57.18 % | ✓ |
| journal | 0.1805 | **0.1740** | 0.1805 | 15.14 % | ✓ |
| sheet | 0.1713 | **0.1730** | 0.1713 | 13.23 % | ✓ |
| spells | 0.3026 | **0.3040** | 0.3026 | 7.76 % | ✓ |
| **level-up** | 0.1855 | **0.1522** | 0.1855 | 11.11 % | ✓ |
| container (3 items) | 0.1525 | **0.1538** | 0.1526 | 10.65 % | ✓ |
| container (1 item) | 0.1434 | **0.1449** | — | 10.65 % | ✓ |

| ID | Result | Measurement |
|---|---|---|
| DN1 | **pass** | inventory `item_icon` 22 pictorial in panel over 20 `list_row`; 22 ≥ 0.8 × 20 |
| DN2 | **pass** | one `doll`; `inventory.worn.body` / `.right` follow a real equip press at f+1 (`EQ1`, re-pressed at these blobs) |
| DN3 | **pass** | quick-slot cluster draws objects; no truncated label in it. *(A truncated label exists in a container list row and a clipped one in the band — see the gap; DN3's own words scope it to the quick-slot cluster, so I am not failing it here either)* |
| DN4 | **fail — bar not met, hard fail NOT triggered** | six named screens ≥ 0.15, **none ≥ 0.35**. Ruling on the seventh below |
| DN5 | **pass** | `container.band.depiction` renders the selected object at its full 140 px plate — the round-5 `listTop` compensation held it there |
| DN6 | **pass** | nine materials, one family, all painted: `chitin, root, ink, parchment, bone, resin, reed, shell, clay` |
| DN7 | **fail** | `blind_status: not_possible` — I saw the attribution before a pack could exist, and rule 0 forbids me spawning the fresh judge. **Fifth round running** |

**5 / 7, no hard fail → ladder 5.**

### The `container_no_name` ruling — upheld, and the overturn condition is STILL not measured

The sweep's seventh state — the container holding **one** item — measures **0.1449**, below the floor,
and `t4-r3-density.mjs` exits non-zero with `hard_fails: ["container_no_name"]`. Round 4 excluded it
because that state exists to drive the **container-title guard** with four bad names, and `RI-UIX09`'s
own *"how we lose"* warns in terms against measuring `D2` on a near-empty sample. **I uphold that, for
the same reason — and I have to report that the exact evidence round 4 named as its overturn
condition still does not exist.** Round 4 wrote: *"a capture of an ordinarily-looted container — two
or three things in a crate — measuring below 0.15."* Three items measures **0.1538**. **Two items has
never been captured by anyone**, and it lies between the measured 0.1449 and 0.1538 — i.e. it
**straddles the floor**, and I will not rule on an interpolation. *What it costs to close: one opener,
`{ containerName: 'Reed Creel', contents: [reed-cutter, bog-iron-maul] }`, added beside the existing
`container` / `container_no_name` states in `tools/ui/t4-r2-measure.mjs`.* If it lands below 0.15,
**DN4 hard-fails and `RI-UIX09` is 2, not 5.**

### The honest sentence about level-up's 0.0022

It is above the floor and it is not a hard fail, so I am not failing anything on it. But the number is
now **a property of gauge geometry**: 95.7 % of the 0.0333 fall is inside the attribute rows and the
solid-trough columns fell 918 → 551. Any future tweak to that gauge moves `D2` by more than the whole
margin, in either direction, with no relationship to whether the screen reads. **`RI-UIX09` P4's 0.35
is met on nothing, and the box-shrinking lever that took every screen over 0.15 is now exhausted** —
level-up's is 480 × 480 and shrinking it further will start clipping the rows that were just fixed.
The next movement on this item has to be **drawn matter**, which is what the item was written to ask
for.

---

## The layout regression — `RI-UIX06` FD4, and what is left of it

**Bifurcation: this section is the FIDELITY pass** (`RI-UIX06` §C F18). It cites our own captures at
1920×1080 and 1280×720 and **no Morrowind plate**; the ART pass cites `REF-A12b` and no modern
reference. The two sets are disjoint.

### What round 5 fixed — five of round 4's six FD4 instances

| Round 4 instance | Round 5 |
|---|---|
| level-up: 9 of 10 rows draw name/value/gauge into the same pixels | **fixed.** 6× and 10× crops show ten clean rows at 1920×1080 and eleven at 1280×720 |
| character sheet: ten rows collide | **fixed.** `crops/sheet-panel-3x.png`, `crops/sheet-willpower-10x.png` |
| journal: 8 of 19 blocks past the panel foot, 11 of 19 over the hint | **fixed.** 0 and 0 over all ten spreads |
| container hint cut mid-word (`…what is worth keep`) | **fixed.** wraps to two lines, ends `…worth keeping.` |
| spells hint loses its last eight words | **fixed.** wraps to two lines, ends `…the fight does not stop for it.` |

### What is still triggered — and one round-4 instance I am NOT counting

**M-F18.1, instance 1 — `container.band` clips its own text.** Declared 137 characters
(`game/data/items/carried.json:426`, echoed verbatim in the census); rendered ends at *"what it
means"*. `writeLines()` has no line cap and no ellipsis, the band's declared rect is
`[ix+160s, by, iw−160s, bh]`, and `surface.js:299` states the rule: *"The declared rect IS the clip.
An element cannot paint outside what it declared."*

**M-F18.1, instance 2 — the gold column is drawn entirely outside its row and clipped away**, on all
seven rows, both sides. Seven rows declare a value; zero render one.

**NOT counted, and this corrects round 4 in the build's favour.** Round 4 listed four *ellipsised*
strings under M-F18.1 — two quest-index names, one carried item name. They are still ellipsised
(`crops/journal-panel-plus-margin-2x.png`: `The Reading of the ...`, `The Count of the Dr...`), and
**ellipsis is not clipping**: `chrome.js:304` shortens the string *before* drawing, so the rendered
bounding box does not exceed its container, which is what M-F18.1 measures. `RI-UIX04` J4/J5 place no
untruncated-name requirement on the quest index and `RI-UIX09` DN3's truncation clause is scoped to
the quick-slot cluster. **Nothing in the seven items I was handed fails an ellipsised list name.** I
am recording that as a hole, not scoring it.

### The two overlap numbers, and which I trust

`node tools/ui/t4-r4-critic-overlap.mjs` — the round-4 critic's own instrument, its own rects, run
against the **LIVE** `t4-r2-measure` captures — reports **level-up 1 of 10** rows with fewer than two
separators (round 4: 9 of 10) and **sheet 3 of 10** (round 4: 3 of 10). The round-4 remedy's
acceptance condition was *zero on both*, so **on the number the acceptance is not met.**

**I do not believe either number, and I checked rather than assumed.** `levelup.attr.personality` at
10× is `PERSONALITY   10   ▮▮▮` with clear space between all three; the metric loses the separator
because a candle flame behind the translucent panel bridges the gap with non-modal pixels.
`sheet.attr.willpower` at 10× is `WILLPOWER   10` with its gauge on the line *below*, so a per-column
profile finds ink in every column of the row band and reports **zero** separators no matter how
legible the row is. **The sheet's arm of that acceptance condition is unsatisfiable by construction**
and cannot be used to close the gap; the level-up arm is defeated by translucency. Numbers and
pictures are both in the artefacts and I am saying which is which.

### The instrument that was already void, and the one that has existed all along

**`t4-r4-critic.mjs`'s own `OV-levelup` / `OV-sheet` legs FAILED in my run and I am not reporting them
as defects.** `frame-liveness.mjs` on that tool's four captures returns **`{"LIVE":1,"DUPLICATE":3}`**
— the same defect its sibling's header documents, `page.screenshot()` under `setRenderRate(0)` — and
its ink/gauge classifier is the colour heuristic the round-4 critic explicitly discarded (*"on the
character sheet the panel's own ground is (44,57,45)… reported 73 % of the row as ink"*). Its
**rects** are census self-reports and are sound; those are what I used.

**And the one that matters: `tools/metrics/ui-metrics.mjs` EXISTS, and I ran it.** `RI-UIX06` says otherwise in two
places — line 283, *"It is owed by the methods owner and does not exist"*, and line 442, *"Owed and
non-existent: `tools/metrics/ui-metrics.mjs`… Until they exist this item is `unmeasurable` ⇒ **0** on
the FIDELITY side"* — and round 4's verdict repeated it as fact. The file is on disk, 
written by the `W1-21` builder and declared in `orchestration/status/W1-21.json`; it implements §D
(F17), §E (F18) and §F (F19), takes `--screens`, `--scales`, `--dpr`, `--self-test` and `--teardown`.
**Six of `RI-UIX06`'s seven FIDELITY checks have been scored 0 fail-closed for four rounds on the
strength of a sentence in the item that is false.** That is `CLAUDE.md` rule 0b arriving from inside
the corpus, and it is the fourth instance this project has recorded of something declared absent while
it sat on disk. I ran it; see `notes` for exactly how far it got under this box's load.

---

## `RI-UIX10` — re-pressed after a reflow, and it holds

A reflow touches hit-testing, so nothing here is carried. Every leg is real `KeyboardEvent`s through
the shipped handler with **I0 asserted first** (`real.attached=true`, `input.moveX=1`).

| ID | Result | Measurement at these blobs |
|---|---|---|
| OP1 keyboard | **pass** | `JX0–JX6` green: `'BCD' → 'BC' → 'B' → '' → chronicle → world`, the quest index exits into the chronicle, back on the chronicle leaves the screen. `JP1–JP4` 19/19 entries over 10 spreads and 5/5 index rows. `CP1` 44/44 carried rows. The inventory list walks by keyboard (`MAP1`: rowIdx 11 → 12 → 11 on `mode='inventory'`, 20 `list_row`s) and by touch (`TS1`/`TS2`). **One leg is red — `IC1`, below — and it is not a dead affordance** |
| OP2 gamepad | **fail (0, fail-closed)** | `ui-pause.mjs`'s PAD leg was not re-run this round. `CRITIC-DOCTRINE` §1.2b point 4 says an untested device arm scores 0, so it does — even though `system.js`'s focus code is byte-unchanged since round 4 measured it green |
| OP3 touch | **pass** | `I1` asserted (`real.touch.attached=true`); `TS0` the list is at row 0 first; `TS1` the floating stick walks it, rowIdx 0 → 1 on a 120 px drag; `TS2` three separate flicks reach rowIdx 4; `TS3` a sideways 140 px drag moves col 1 → 0; `TS3b` returns. `TS5` is red and is round 3's own declared **device-neutral diagnostic**, unchanged |
| OP4 mouse | **pass** | `system.js` rule 2 declares no pointer path for these surfaces and nothing drawn implies one; no drawn control takes a click and ignores it |
| OP5 exits | **pass** | `JX1` 1 of 15 actions escapes search to the chronicle; `JX2b` back on an empty line returns to the chronicle; `JX5` the index exits into the journal; `JX6` back on the chronicle leaves it. **No state I could drive into had no exit** |
| OP6 persistence | **pass** | `JX3` close-and-reopen → `chronicle`, query `''`; `JX4` peer walk → same |
| OP7 O3, 15 frames | **pass** | `EQ1`: `["chitin-cuirass@chest","reed-cutter@right"]` → `["bog-iron-maul@right","chitin-cuirass@chest"]` **at f+1**, `getFrame()` 43 → 43 (paused, as `RI-UIX03` P1 requires); `EQ1b` confirms the first frame; `EQ2` the refused row still narrates *"You cannot get a grip on the sapwood bow…"* within the window |
| OP8 instrument honesty | **pass** | I0 asserted; **I2 named and not reported as a defect**; three instrument defects found in my own run are recorded as instrument defects and not shipped as findings — the duplicate-frame `OV` legs, `t4-r3-density.mjs`'s hard-coded label, and the paging leg below |

**`IC1`, the one red I could not resolve, and why it is not a dead affordance.** In combat, a 60-press
`KeyS` walk of the inventory list did not reach any of `reed-cutter`, `chitin-cuirass` or
`bog-iron-maul`, and round 3 passed the same leg. **`OP1`'s hard-fail tell is *"a drawn affordance
with no input that works it"*, and that is definitively not met**: in the same run, on the same
screen, the list focus walks by keyboard (`MAP1`, rowIdx 11 → 12 → 11, 20 `list_row` elements) and by
touch (`TS1` 0 → 1, `TS2` three flicks to 4). The likeliest reading is a fixture artefact — by the
time `IC` runs, `EQ1` has equipped `bog-iron-maul` and displaced `reed-cutter`, and `chitin-cuirass`
is worn, so two of the three wanted ids are no longer carried rows. **I am recording it, not
resolving it, and naming the run that would:** `--legs ic` at `cfed8aaf` and at `HEAD`, with the leg
printing the focused row's `meta.item_id` on each of its 60 presses.

**The paging leg, and why it is an instrument note rather than a hard fail.** The builder's own
`tools/ui/t4-r5-legibility.mjs`, run by me, reports `journal_pages_checked: **2**` — its `KeyD`
presses did not turn the page, so its overflow sweep effectively examined page 0. It never calls
`setMode('play-instrumented')` and never asserts `real.attached`, which is `RI-UIX10` trap **I0**, and
`RI-UIX10` OP8 is precisely the row that exists for this. **The journal pager is fine**: the same
presses through `t4-r4-critic.mjs`, which does assert I0, walk **10 spreads and 19 of 19 entries**.
**But it means the builder's headline "0 of 19, paging sweep" is not evidenced by anything in the
repo** — its legibility report is not committed; the only `t4-r5-legibility.json` on disk is mine.
**The claim is true. It was not proven.** I proved it, per page, from `t4-r4-critic.mjs`'s own
per-page `entry_rects`.

**7 / 8, no hard fail → ladder 6.**
**Justification (`SCORING.md` §1.1 at ≥ 7):** `…/T4-r5c/reports/t4-r4-critic.json` (`JP1` 19/19 over
10 spreads, `JP5` −89 px, `CP1` 44/44, `CP2` `{"from":40,"shown":4,"total":44}`),
`…/T4-r5c/reports-repress/t4-r3-critic.json` (**22/24**: `JX0–JX6`, `EQ1`, `EQ1b`, `EQ2`, `TS0–TS3b`, `MAP1`),
`…/T4-r5c/reports/journal-per-page-overflow.json`.

---

## `ARBITRATION` S57 — re-checked, not assumed

The journal's box grew this round (550 → 730 wide, 15.1 % of frame), so element counts rose rather
than fell and S57's guard is not engaged in the direction it was written for. Both conditions were
operated anyway: **no element type disappeared** on any screen, and **paging still reaches
everything** — 19 of 19 journal entries by real `KeyD`, 44 of 44 carried rows by real `KeyS`, with the
extent marker reading `{"from":40,"shown":4,"total":44}` at the end of the list.

**S56 still holds and is not undone by the journal growing.** 730 × 430 is 15.1 % of frame against the
57.2 % ceiling S56 ruled on, and round 4's own remedy note authorised exactly this.

---

## AR-1 / AR-2

**AR-1: not applicable, with a reason, and A3 is satisfied from this round's own captures.** Of
§4.1's ten probes only **A3** (pause mid-fight) and **A10** (talking mid-fight) can reach a screens
piece. `EQ1` shows `getFrame()` **frozen at 43** with the inventory open out of combat — the pause
rule working — and `t4-r3-critic.mjs`'s `IC0` gate confirms a real fight running with the screen
drivable. A10 belongs to the dialogue piece's probe and is not duplicated.

**AR-2: pass, on three probes at these blobs.** **B1** — the world frame's census carries exactly ten
kinds (`health_bar, stamina_bar, focus_bar, heal_charges, equip_load, quick_slots, interact_prompt,
bearing_dial, effect_strip, place_name`) and its whole text is `["5","LIGHT","Sesh-Anaat","S",
"thorn-hall"]`: no arrow, pin, waypoint, distance readout or world anchor. **B5** — the level-up
screen carries **zero** gold elements and zero gold-word matches; souls are `souls_held` and
`souls_to_next`. S15 the right way round. **B6** — carried from round 3 at 0.0129 against a hard fail
of 0.20, on data an empty `git diff` proves has not moved. **B12/B13** are not reachable from a
screens critique and are marked so.

---

## The single biggest gap

**`GAP-W1-ui-container-clips-its-own-declared-text`.**

**What.** The container screen renders less than it declares, at 1920×1080, in the shipped fixture,
and the clip is silent. (a) `container.band`'s declared description is 137 characters and the panel
draws 100 of them — *"…Everyone who sees it knows what it means"* — dropping *"and nobody has yet
explained it to you."* with no ellipsis and no line drawn anywhere in the frame. (b) All **seven**
list rows on both sides declare a gold value (`"Bog-iron maul  11.5  96"`) and **none draws one**: the
row's three columns are pitched at `0.60 / 0.19 / 0.19` of `half` (= 194 units at `half` = 198) on top
of an `ICON + 10 = 38`-unit inset, inside a row rect `half − 16` = 182 units wide, so the gold column
starts 12 units past the rect's own right edge and `surface.js`'s clip removes it entirely.

**Why it matters.** The container is the screen where the player decides what a thing is worth
carrying, and the price is the one column that is missing — on both sides of the trade. The same item
description renders whole on the inventory's larger detail panel, so a player sees the object's story
truncated only when they are standing at a chest.

**Evidence.**
`corpus/90-verdicts/wave1/artifacts/T4-r5c/crops/container-panel-3x.png` ·
`…/crops/container-desc-tail-5x.png` · `…/crops/container-blackwater-8x.png` ·
`…/crops/inventory-panel-1x.png` (the same string, whole, on the bigger box) ·
`…/excerpts/container-text-clipped.md` (declared text with `path:line`) ·
`…/measure/measure-head-1920x1080.json` (`screens.container.texts`) ·
`…/reports/frame-liveness-head.txt` (8/8 LIVE).

**Remedy — concrete, and it does not undo S56.**

1. **`game/src/ui/screens/inventory.js`, `drawContainer()`, the three row columns.** Subtract the
   inset before apportioning: with `usable = (half − 16) − (ICON + 10)`, pitch the columns as
   fractions of `usable`, not of `half`. **Done when** the census's declared row text and the rendered
   row agree — every one of the seven rows shows its gold figure at 1920×1080 **and** at 1280×720,
   `0` rows with a column origin beyond `rect[0] + rect[2]`.
2. **The same function's `container.band`.** Either size the band for its own wrapped description —
   `wrap(description, f, size, r[2] * 0.94).length` lines at `size * 1.44`, added to the 106-unit
   header block, is the height it needs — or, if the box cannot hold it, ellipsise explicitly so the
   player can see the sentence was cut. **Done when** the rendered description equals the declared
   string, or ends in `…`, for every item in `game/data/items/carried.json` reachable in the
   container fixture. **Preservation clause (`ARBITRATION` S59):** the depiction plate stays at its
   full **140 px** (`DN5`) and container `D2` stays **≥ 0.15** — round 5 already had to spend
   `listTop` to protect exactly this, and the fix must not take it back.
3. **Then re-run all three numbers together**, because they trade off: `t4-r2-measure.mjs` +
   `t4-r3-density.mjs` must still report every named screen ≥ 0.15, `t4-r5-legibility.mjs` must stay
   clean — **and it must first be fixed to call `setMode('play-instrumented')` and assert
   `real.attached` before its paging sweep, or its journal leg keeps checking one page and reporting
   a pass** — and the level-up/journal/sheet crops must still read.

---

## Gap closure — round 4's gap is **partially closed**

`GAP-W1-ui-boxes-were-shrunk-without-re-pitching-what-is-inside-them`, re-measured against its own
four acceptance clauses, not against an impression:

| Clause | Measured | Status |
|---|---|---|
| 1. `t4-r4-critic-overlap.mjs` reports `rows_with_fewer_than_two_separators: 0` on level-up **and** sheet (was 9 and 3) | **1 and 3** | **not met on the number**; both shown by 10× crops to be metric artefacts (translucency; a gauge drawn under the text). The sheet arm is unsatisfiable by construction |
| 2. `JP5` on **every** page: no `journal_entry` rect past the panel's inner foot — 0 of 19, against 8 of 19 | **0 of 19 over 10 spreads**, worst −89 px | **met** |
| 3. Foot hints contain the whole declared string | container and spells both whole | **met** |
| 4. Re-run both numbers together: every named screen ≥ 0.15, and the delete-the-fix arm puts the five hard fails back | six screens ≥ 0.15 ✓; the arm I ran is `cfed8aaf` (round 4's build), not `c792e8d7` | **partly met** — I proved round 5's fix is round 5's; I did not re-prove round 4's box-shrink, and did not need to |

**`status: partially-closed`.** Three of four clauses met, the fourth met in substance and failed on a
metric that cannot pass.

---

## Secondary observations — explicitly **not** work for the next builder

1. **`RI-UIX06` asserts a tool does not exist that does.** Lines 283 and 442 name
   `tools/metrics/ui-metrics.mjs` as *"owed and non-existent"* and fix the FIDELITY side at 0 until it
   appears. It has been on disk since `W1-21`. Six checks have been fail-closed at 0 for four rounds
   on the strength of it. **This is a corpus defect and it belongs to `RI-UIX06`'s owner, not to a
   builder** — one sentence, and a re-run.
2. **`ARBITRATION` S58's mechanism is wrong even though its ruling is right.** Ink over ink cannot
   inflate `D2`; a set has no multiplicity. Round 4's level-up rise was the panel area falling 5.1×.
   The correction is one sentence and it changes no threshold.
3. **The spells screen still prints `NaN`.** `spells.row.spark_dart` reads `"Spark-Dart  NaN"` and the
   detail panel renders `focus  NaN` (`crops/spells-panel-3x.png`). Four rounds of critics have now
   photographed this screen. **Nothing in the seven items I was handed fails a rendered `NaN`**, which
   is a hole worth someone writing a row for.
4. **`D2` still counts the world bleeding through a translucent panel.** Spells scores 0.3040 — the
   best on the build — and the capture shows candle flames and a figure clearly through it. It is also
   what defeats the separator metric on level-up. A `--blank-world` arm would separate them.
5. **The `container_no_name` overturn condition remains unmeasured after two rounds.** One opener.
6. **`RI-UIX01` §D is unmeasured for the fifth consecutive round.** Rounds 3 and 4 both recommended it
   become its own piece on `ui.hud.combat`. It has still not been dispatched. I repeat it and did not
   create it — that is dispatch, not critique.

---

## Did I judge the bar as well as the build? (`CRITIC-DOCTRINE` §1.3)

**Yes, and this round the answer is that two of the bar's own instruments were lying about
themselves.** I asked the §1.3 question — *if this piece scored full marks on every item I was handed,
would it actually be good?* — and the honest answer is **no**, for a reason that has nothing to do
with the builder: a screen can print `NaN` to the player, ellipsise a quest's name down to *"The
Reading of the ..."*, and drop the price column out of a trade window, and of the seven items I hold
only the third fails anything. `RI-UIX09` measures how *full* a panel is, `RI-UIX06` FD4 measures
whether text *exceeds its box*, and neither asks whether what is drawn is **correct**.

**I checked before declaring, as §1.3's second guard requires, and I did not extend the bar.** Ids
read in full or in their scoring sections: `RI-UIX01`, `RI-UIX03`, `RI-UIX04`, `RI-UIX06`, `RI-UIX07`,
`RI-UIX09`, `RI-UIX10`; `SCORING.md`, `CRITIC-DOCTRINE.md`, `ARBITRATION.md` S56–S59. **I did not
write a new row, and the reason is §1.3's third guard: the hole I found is a *method* hole, not a
missing standard.** `RI-UIX06`'s FIDELITY side already contains seven rows that would catch most of
this, and they have been scored 0 for four rounds because the item says its own instrument does not
exist. **Writing an eighth row while six existing ones go unrun would be corpus growth for its own
sake.** The correction I would file is two sentences in `RI-UIX06` — delete the "non-existent" claim
at lines 283 and 442 — and it is an addition of truth, not a weakening of any threshold. I am
recording it rather than editing another item mid-critique.

---

## What would make this verdict wrong

**The strongest evidence is the pair of arms.** The same sweep, the same instrument, one pinned commit
apart: round 4's exact numbers reproduced at `cfed8aaf` and round 4's exact collision photographed
there, against ten clean rows at `HEAD`. Next strongest is the journal, which is 19 rects per page
across ten spreads rather than an impression.

**The five weakest claims, named so they can be attacked:**

1. **`RI-UIX03` §A is carried, not re-run.** `ui-pause.mjs` did not run this round; `M-P1`–`M-P5` come
   from round 4 on `system.js` blob `830ae9de`, which `git ls-tree` proves is byte-identical. `M-P4`'s
   occlusion percentages are therefore round 4's numbers on unchanged code and I am saying so rather
   than implying I re-took them.
2. **`RI-UIX10` OP2 is not re-pressed and I scored it 0 rather than carrying it.** The gamepad leg
   (`ui-pause.mjs` PAD) did not run this round. `system.js`'s focus code is byte-unchanged since round 4
   measured it green, so a critic who carried it would score the item **8** and would be defensible;
   §1.2b point 4 says an untested device arm scores 0, so it does, and the item is **6**. The
   touch arm *was* re-pressed and is green.
3. **`FD1` and `FD2` are scored off a crashed run's log lines, not off its grader.** Five screens at
   DPR 1 and two at DPR 2 is real measurement and the numbers are nowhere near their thresholds, but it
   is not the full eight-configuration sweep `RI-UIX06` §E asks for, and I have not seen the tool's own
   verdict on them.
4. **The FD4 hard fail rests on two instances I found and round 4 did not.** Both are visible in a
   3× crop of a screen anyone can open, and both are corroborated by the census declaring text the
   picture does not show. If a ruling says a *clipped* column is out of FD4's scope, `RI-UIX06` is
   still 2 on its six unmeasured FIDELITY checks and the piece does not move.
5. **`RI-UIX07` is carried entirely** on `git diff --stat 7c63111d..HEAD -- game/src/ui/hud.js` being
   empty. Nothing in it was re-measured.
6. **The attribution analysis uses one set of row rects for both arms.** That is sound only because the
   two panels are byte-identical rects and the row pitch did not change; the report records
   `panel_rect_identical: true` so the assumption is visible rather than assumed.

**And the claim I would most like to be wrong about and am most confident in is the container's
missing gold column**, because seven rows declare a number, none of them draws it, and the inventory
screen draws the same column correctly six inches away.
