# W1-F4-r1-critic — what is in here

Produced by `tools/visual/f4c-critic.mjs` (capture) and `tools/visual/f4c-analyse.mjs` (analysis),
both written for this verdict. Copies of both are in `tools/` beside this file so the numbers can be
recomputed from the committed tree.

`reports/` is gitignored (HAZARDS §9), so nothing cited here points into it.
