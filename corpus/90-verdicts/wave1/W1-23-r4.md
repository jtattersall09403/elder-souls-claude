# W1-23 round 4 — the library, its names, and whether the world can reach it

**Critic:** fresh context. Did not write any of what is judged, did not write round 3's verdict, and
did not build the blind pack judged in §5 — it is built by `tools/lore/critic-w1-23-r4-blind.mjs`,
which is mine, and it prints its own tell checks and rejected its own first two drafts before I
answered it.
**Commit:** HEAD moved five times under this run. Every figure names the commit it was taken at;
where a tool stamps its own, the stamp is quoted rather than the commit I started from.
The reach and naming figures were re-taken today; the browser figures are mine and were taken at
`aa542ec`–`6803a05`. RULES #12.
**Items:** RI-LOR03, RI-LOR06, RI-LOR04, plus `ARBITRATION.md` §3 / RI-MTH07 (CONSUMPTION).
**Score: 4 / 10, min-over-axes — FAIL against the wave-1 gate of 7.0** (round 3: 4 / 10).
**The number is flat and the work is not, and §9 explains why in one paragraph before it does
anything else.** Read that paragraph before reading the number as "no progress"; it is the most
important thing in this verdict.
**Instruments, all mine, all able to fail:** `tools/lore/critic-w1-23-r4-onscreen.mjs` (12 legs, a
control that asserts the prop is present, and a cross-talk arm), `critic-w1-23-r4-shelf.mjs`
(crowding), `critic-w1-23-r4-band.mjs` (four arms on the shipped validator),
`critic-w1-23-r4-blind.mjs` (the attribution pack, with six tell checks it enforces on itself).
**A browser was used.** `node tools/contention.mjs --gate` returned WAIT at the start of this run
and I did the whole of §2–§5 offline before it returned GO; the browser work was then done inside
one GO window, one browser at a time. **No timing figure is published, because none was taken.**

---

## 0. What is true, said first

**Round 4 answered its three charges, and I could not break the largest one by trying hard.** The
placement is real, the renames are real, the validator is real, and the one thing the round said it
could not close — whether the words reach the screen — closes, because the blindness it reported
was not structural. Details below, and each is a claim I re-took rather than read.

Round 3's finding stands: **the prose is not the gap.** I did not re-read the corpus end to end; the
round-3 verdict did, at 110,420 words, and nothing this round rewrote a line of it (verified: the
r4 diff touches no existing book text). The corpus is now **163 texts, 112,884 words**, median 547,
p90 1,453, 21 `wrong_on_purpose`, 0 unfiled.

---

## 1. "163 of 163 referenced" — the number I was sent to break, and it holds

**What the instrument means by "referenced" is a substring test**, and it says so:
`critic-w1-23-r3-reach.mjs` builds a haystack of every `.json`/`.js`/`.mjs` under `game/` outside
the books directory and asks `hay.includes('"' + id + '"')`. A book named once in a comment would
count. So I did not accept it, and checked the harder things myself.

**Offline, first.** Every one of the 163 has a real route: **157 are a `readable` entry in an
interior record and the other 6 are named by a `book_id` on an item**. Not one of the 163 is
reachable only by the substring test. `place-library.mjs --self-test` 7/7,
`critic-w1-23-r3-reach.mjs --self-test` 14/14, both re-run.

**Then in a browser, twelve times, standing in the room.**
`tools/lore/critic-w1-23-r4-onscreen.mjs`, both halves of six registered contradiction pairs —
the ford, held-as-property, the shield line, the Flu, the grange tree, and the Recension pair that
was already reachable before this round:

```
  opened the right book:                12/12
  drew ITS OWN words on the screen:     12/12   (1,328 – 1,754 characters per leg)
  drew another book's words as well:     0
  control, out of reach, prop present:   4/4 present, 4/4 stayed shut
  another document within 1 m:           0 of 12
```

Each leg asserts three things, not one: the id that opened, that the frame drew a non-zero number
of characters, and that **long words out of that book's own record are among the strings the frame
put on the screen** — `Lilmothiit` and `Kothringi` on the Knahaten page, `grandmother's` and
`hatchlings` on the grange book, `INSTRUCTION` and `AUXILIARY` on the standing instruction. The
cross-talk arm exists because a build that drew one hard-coded page of parchment for every book
would otherwise pass twelve times; it fired zero times.

### 1a. The blindness the round declared unfixable is a two-line diagnosis, and it is fixed here

`orchestration/status/W1-23-r4.json` lists under `not_done`:

> *"THE RENDERED-TEXT ACCESSOR IS BLIND TO THE BOOK SCREEN. `getRenderedText()` returns 0
> characters on every leg with a book open… Not mine to fix."*

**It is not blind.** `ui/system.js build()` returns early on
`builtFrame === ctx.frame && lastMode === mode && lastTouchSig === sig`, and while a screen is up
the world is paused, so `ctx.frame` never advances and the cache key is constant for the entire
life of the screen. The round-4 tool cleared the text register **after** the book was already open
and then asked for a frame; `build()` returned immediately, nothing was drawn, and the register it
had just emptied stayed empty. **Zero the register before the player presses the button** and the
forced build that opens the screen draws straight into it. That is the only difference between my
tool and theirs on this point — no harness door, no engine back door, both asserted against my own
bytes — and it moves the answer from *0 chars, judged on the paginator instead* to *1,531
characters of the Blessings of the Coast, on the screen, with `chartered` and `grandparents` in
them*.

This matters beyond one tool. The round judged its perturbation arm on
`bookPagination()` because it thought it had to. It did not have to, and neither does anyone else.

**Screenshot:** `docs/shots/2026-08-08-w1-23-r4-critic-a-book-open-in-the-lilmoth-customs-house.png`
— the customs house, one metre from the shelf, the Empire's own answer to whether subjects are held
as property, open.

## 2. The placement rule (B) — the round's best idea, and it survives being checked

*A book lives where its argument is had.* `place-library.mjs --check` exits 0 and reports
19 mutual pairs, both sides placed, **16 with the halves in two different rooms and three on one
shelf**. The round says those three are an earlier piece's and that it declined to move them to
flatter its number. **I checked rather than took it:** all three sit in `soulrest-court-steps`, and
`git show e97347f:game/data/world/interiors/soulrest-court-steps.json` — a commit that is an
ancestor of every r4 commit — already carries `the-seventh-recension`, `for-the-ninth-clause`,
`against-the-ninth-clause` and both Drowned Tally volumes in that room. **The claim is true.**

The rule reads well in the world, too. The standing instruction is in the Stormhold Praetorium and
Serjeant Brell's answer to it is in the Blackrose warders' hall, and I walked to both. The prefect's
relief of the ford is in the Praetorium that commissioned it and the weir people's account is at
Thorn Hall. That is an argument you have to travel to find, which is the whole point.

**One gap in the gate, not in the placement.** `--check` counts a same-room pair as a failure *only
when this table caused it* (`const mine = table[a] === ra && table[c] === rc`). That is fair to this
round and wrong as a standing check: any future piece can shelve both halves of a dispute together
and `--check` will stay green, because the pair was not put there by `PLACEMENTS`. The remedy is one
line — report same-room pairs as a failure and carry the three known ones as a named exemption list
rather than as a silent class.

## 3. The naming before/after (C) — the renames are real, and I counted them

**521 of 692 named people were actually renamed** between `97bb918` and HEAD, and **zero people
moved between `pop-*.json` and an authored file**. There is no reclassification here: the
percentage moved because the names moved. `lor04-validate --at 97bb918` reproduces the round's
BEFORE arm exactly (descriptive 69.7%, violations 76.19%, unclassifiable 12.70%, 43 reused, 18
slugs) and the working tree reproduces the AFTER (10.2%, 1.85%, 0.00%, 0, 0). The
**hand-authored half is byte-identical across both arms** — 77 people, 44 Argonian, 4.5%, 22.73%,
12.12% — which is the strongest available proof that not one authored name was touched. §4 is held
at 9.1% against an attested 11%. `Sedura Nine-Teeth` is gone.

Two things the AFTER arm carries that are not this round's, and should be said:

- **22 of the 282 "generated" people are a neighbour's.** `game/data/npcs/pop-trades.json` did not
  exist at the baseline; it arrived at `34d1387` and is classified `generated` by the tool's
  path rule. So the before/after populations are 260 and 282, not the same set.
- **Two of those 22 collide with real Bethesda/ZOS names** — `Heem-La` and `Okur`, both attested
  Argonian NPCs. `namegen.mjs` has an `ATTESTED_EXCLUDE` guard against exactly this, it is live
  (377 names), and it works on everything this round generated. The neighbour's roster did not go
  through it. Not this round's defect; it is in the number this round published.

### 3a. Fifty names, read by ear

Deterministically sampled across the eight rosters. The Jel is mostly good — *Vosh-Vastei,
Tsith-Zuka, Neekhuj, Deekzash, Shekhuj, Tuk-Wuxal, Jul-Kaan, Seil-Shaa, Xal-Tsona, Naheesh-Meer*
are names, and repetition is genuinely low (the commonest first element appears 3 times in 186).
Three things I can hear, in increasing order of seriousness:

**(i) The `x` is laid on far too thick, and the item told the round to do it.** RI-LOR04 comparison
method §5 demands *"at least 40% [of Jel-classified names] must contain `x`"*; the round hit 42%
(39.2% over all generated Argonians) and cleared it. But **the attested corpus this project mined
itself carries `x` in 48 of 324 Jel-shaped names — 14.8%** (`tools/uesp/mine-argonian-names.mjs`,
re-derived today). The pre-fix roster was at 4%, which was too flavourless; the fix overshot to
nearly three times attested, and it did so *because the item's own §5 threshold contradicts the
item's own §4 table*. **This is an item defect and the round followed the item correctly.** It
should be fixed in RI-LOR04, not blamed on the builder — but the roster does now rasp:
*Ixuxith, Thuxis-Uxith, Anxthari, Wuxshaneekh, Wuxvakh, Ixvakh, Jix-Ixt* in one sample of fifty.

**(ii) The Tamrielic-facing names are a cross-product and four of twelve do not parse.**
`namegen.mjs descriptiveCandidates()` is every verb × noun and verb × determiner × noun from the
lexicon's sanctioned inventories — which is why every one is *legal*. RI-LOR04's `register_rule` is
not a legality rule, it is a semantic one: *"something an adult once said ABOUT a hatchling, or the
thing that person is known for doing badly."* `Hides-No-Coin`, `Sleeps-Eggs`, `Wades-Rain` and
`Buries-Nets` clear it. **`Sleeps-Doors`, `Bites-Ledgers` and `Paints-Flies` are not English**, and
`Hides-Many-Names` is closer to the title register the item forbids than to the domestic one it
wants. This is the item's own *How we lose* #4 — *"Rules produce validity, not character"* — landing
on the Tamrielic half rather than the Jel half. It is 12 names, so it is a small deduction, and the
remedy is small too: hand-pick the ~20 descriptive names the roster needs instead of drawing them.

**(iii) Eight villagers are named on words that are not naming stock, and this one is a breach.**
`jelCompoundCandidates()` is an unfiltered 75 × 74 root cross-product, and `ROOTS` includes the
lexicon's semantically loaded entries. RI-LOR04 §1: *"Attested exceptions (exempt because canon
predates our rules; **nothing new may be coined on their pattern**): `kaoc, naga, hist, jel,
lukiul`."*

```
  Lukiul-Skei, Lukiul-Teekh   lukiul = "salted one; an Argonian assimilated to foreign ways" — a slur
  Xan-Jel                     jel    = the root-tongue itself. A person called "Stone-Language".
  Shal-Naga, Wux-Naga         naga   = the serpent-form of the people, not a root
  Haj-Saxhleel                saxhleel = the Argonians' own word for Argonian. "Hidden-Argonian".
  Histlith, Shaa-Hist         hist   = the tree
```

Every one passes `jel-phonotactics.py`, because phonotactics is about sound. **A phonotactic
validator cannot catch a semantic error and this round has no other filter.** The remedy is a
`no_coin` flag on those five roots in `jel-lexicon.json` and one line in `jelCompoundCandidates()`.

## 4. RI-LOR04's validator (D) — the correction is upheld, and the band is not stuck

**Round 3 said the validator did not exist; round 4 says round 3 was half wrong. Round 4 is
right.** `corpus/80-methods/jel-phonotactics.py` exists, takes `--culture jel`, and self-tests
33/33 today. What round 3 actually found — and phrased as absence — is that nothing could **band**
with it, and the round-4 diagnosis of why is correct and is worth quoting because it is a general
lesson: run the python's own harvester over `game/data` and it checks 2,463 "names" at 74.7%
violations, because it pulls every `name`/`title` key and ends up judging
*"A 1.20 m doorway between two walls"* against Jel phonotactics. **A validator whose population is
wrong cannot be banded.** `lor04-validate.mjs` supplies the population and delegates every per-name
judgement over a pipe. That is the right architecture and I say so.

**Can it report non-zero on a real population?** The shipped `--self-test` clears a *fixture* at
band ≥ 4; that is not the same question. `tools/lore/critic-w1-23-r4-band.mjs` asks it of the
shipped roster, four arms:

| arm | band | M1 | M2 | M3 | M4 | why |
|---|---:|---:|---:|---:|---:|---|
| 1 shipped | **0** | 9.1% | 5.93% | 2.37% | 0 | hard 0: unclassifiable > 2% |
| 2 the eight blamed names repaired | **3** (ladder 6) | 9.1% | 3.86% | 0.30% | 0 | violation rate → 3 |
| 3 arm 2 + one apostrophe | **0** | 9.1% | 3.86% | 0.30% | 1 | hard 0: **a different clause** |
| 4 arm 2 + 10% legal-but-wrong | 0 | 8.7% | 8.61% | 5.93% | 0 | my degradation also tripped M3 — my arm's fault, reported |

**The band is live, in both directions, and arm 3 is the one that proves it** — a needle stuck at
zero would make arms 1 and 3 indistinguishable, and here they zero for different stated reasons.
Arm 4 is a defect in my own instrument, not in theirs: my "legal but wrong" names came out
unclassifiable, so there is no clean intermediate arm and I do not claim one.

**And the eight names are the whole of it.** `lor04-validate` blames `Hallow, Sedd, Tesh, Ashul,
Veth, Mek, Eshi, Archein Weighs-The-Season` and stops at band 0. **Six of the eight are not bad
names.** Five are Imperial officers whose rank lives in the record id and not in the name field —
`prefect-hallow`, `quartermaster-sedd`, `harbourmistress-tesh`, `innkeeper-veth` — so the validator
is shown a bare mononym where RI-LOR04 §5's Imperial row wants praenomen + nomen. One is
`Archein Weighs-The-Season`, and `Archein` is simply not in
`jel-lexicon.json → tamrielic_name_grammar.title_prefixes`, which §4 says is *"stripped before
classification"*. **See §7 for what that costs and what it takes to fix.**

**A defect the round left in its own tool.** `tools/lore/lor04-validate.mjs` ends with a bare
`process.exit(argv.includes('--self-test') ? selfTest() : run(argv));` and **no `IS_MAIN` guard**,
so importing it runs the whole validation and exits the importer — which is precisely the defect
this same round found, documented and fixed one file over in `place-library.mjs`
(*"the consumption run reported exit 0 having measured nothing"*). It cost me a run and a
workaround: `critic-w1-23-r4-band.mjs` copies the shipped bytes beside the original, removes that
one line, prints the line it removed, and refuses to run if the line is no longer there. Nothing
in the tree imports `lor04-validate` today, so no published number is wrong — this is a trap, not
a wound. `tools/lore/name-rosters.mjs` has it too.

## 5. The blind culture-attribution test (E) — taken, by me, and it is a strong result

`lor04-validate` reports `blind_attribution: null` and caps its own band at 4 for it, correctly:
no tool can take a judgement test. I can, and I did. `tools/lore/critic-w1-23-r4-blind.mjs` builds
the pack, **enforces six tell checks on itself, and rejected its own first two drafts** — the first
for a 5.2-character per-culture length spread, the second for 4.2 — before printing anything.

**Deviation from the protocol, declared:** RI-LOR04 §6 asks for 20 nouns across the cultures. Mine
is 21 across **three** — Argonian, Imperial, Dunmer. **Khajiit is excluded on purpose**: §5 makes
Khajiit the only culture permitted an apostrophe, so all six on the roster are answerable from one
character without hearing the word, and at 7–9 characters they are also a length tell my own T2
check catches. What RI-LOR04's band-2 text calls the failure is *"the languages bleed into each
other"*, and the three that can bleed here are the three in the pack. It is a harder test than the
item's, not an easier one.

**Score: 21 / 21 = 100%.** RI-LOR04 §6's threshold is 80%; §Scoring's band-5 row wants ≥90%.

Three honest deductions, none of which changes the outcome:

- **Contamination.** I had already seen `Halvo Nirith`, `Paints-Flies` and `Xthari-Shuja` with
  their culture attached earlier in this session. Excluding them: **18 / 18, still 100%.**
- **The hyphen is a real cue.** Six of the 21 are hyphenated and all six are Argonian, because
  RI-LOR04 forbids hyphens to Imperial and Dunmer. That is the system working rather than a pack
  artifact, but it is free information. On the 15 non-hyphenated names alone: **15 / 15, 100%** —
  including `Deekshaneekh`, and including telling `Falco Aldeyn` (imperial) from `Velas Uveran`
  (dunmer) and `Sethan Dares` (dunmer) from `Andus Prelo` (imperial).
- Half the pack is generated and half hand-authored, asserted by the tool, and I got both halves
  right. The generated names are not distinguishable by ear from the authored ones.

**This is the measure the item calls "the test that actually matters", and the round passes it at
band-5 level.** It is the single strongest thing in this piece and it should be recorded as such.

## 6. CONSUMPTION (F) — demonstrated; the C-versus-D distinction is earned in the data and not in the assertion

The round's four arms are real and I re-derived the two that matter with my own tool and my own
control. `reports/w1-23-r4/consume.json` shows arm C with `prop: interior-readable:…, reach_m: 2,
stood 5 m, opened: null` and arm D with `prop: NONE`. **Those are genuinely different world states,
so the builder's reasoning is correct as an observation.** My own control repeats it independently:
4/4 legs with the prop present, standing at reach + 3 m, nothing opened.

Two things it did not earn:

- **Arm C's pass predicate is `!r.opened`.** It does not require the prop to be present, so it is
  satisfied for free by a room with nothing in it — exactly the shape RULES #6 was rewritten to
  catch. The builder watched the distinction and did not encode it. Mine asserts
  `r.prop && !r.opened`, which is one clause and the difference between a control and a claim
  about a control.
- **Arm D never presses the button.** `runLegs()` bails at `if (!props.length) … continue` before
  the teleport and the input queue, so the report's *"the press opens nothing"* describes a press
  that did not happen. The arm is still red for the right reason — the object is gone — but the
  sentence overstates what was run.

**And the consumption report is stamped `e00e6fe`, which is older than the round's own commits, and
11 of the 110 placements landed after it.** `place-library.mjs` gained the ten T5/T8 rooms and the
Gideon toll rhyme at `8043f15`; `consume.json` records `placements_removed: 99`. Those eleven texts
— every poem and story written this round — have never been through a consumption arm. My twelve
legs do not cover them either. That is a small hole and it is the kind that has bitten this project
before: the newest content is the least measured.

## 7. The single biggest remaining gap

**RI-LOR04 bands 0 on the shipped tree, its own §Scoring says "below 3 blocks the wave", and the
whole of it is eight names out of 337 — six of which are not naming defects at all.**

M3 is 2.37% against a 2.00% hard gate: 8 unclassifiable of 337 in scope. Five are Imperial officers
stored as bare mononyms because their rank lives in the record id; one is an Argonian title the
lexicon does not list. Everything else in the item passes — §4 held at 9.1%, zero apostrophes,
zero reuse, zero slugs, and blind attribution at 100% (§5 above), which is the band-5 row.

**Remedy, concrete, buildable, and verified with the shipped validator — not estimated:**

1. Put a praenomen in front of the five Imperial mononyms — `Aurelia Hallow`, `Marcus Sedd`,
   `Ottavia Tesh`, `Gaius Veth`, `Publius Mek`, `Livia Eshi`. **Nothing breaks:** the prose in the
   nine dialogue files that say "Sedd" and the thirteen that say "Ashul" still reads correctly,
   because a Roman is referred to by his nomen and the existing word is kept intact rather than
   replaced. This is not "rewriting the cast", which the round rightly refused to do.
2. Give the one Argonian a second element — `Ashul` → `Ashul-Vei`.
3. Add `Archein` to `jel-lexicon.json → tamrielic_name_grammar.title_prefixes`, where the other
   fifteen attested title-prefixes already are.

Measured on the shipped `lor04-validate` with exactly those eight edits and nothing else:

```
  shipped                    band 0/5   M2 5.93%   M3 2.37%   -> FAIL, blocks the wave
  praenomen-only repair      band 3/5   M2 3.49%   M3 0.00%   -> ladder 6, clears the threshold
```

Then arm the gate: `lor04-validate` already exits 1 below band 3, so putting it in `tools/check-*`
makes the next roster regression a CI failure instead of a critic's finding.

**Runner-up, recorded because it is larger in the world even though it is not this round's to
fix: 13 of 28 registered disputes still change nothing anybody says** (round 4 measured 12; on
today's tree `CF-D018` has joined them, because the dialogue files moved under both of us). The
books that argue them are now all reachable, which is the half this piece could reach without
taking a live sibling's file, and the round says so plainly. The other half needs infos in
`game/data/dialogue/topics/**` gated on those facts.

## 8. What round 4 did not do, each verified and given a number

| Claim in `not_done` | Verified today | Acceptance |
|---|---|---|
| 12 inert disputes unchanged | **13** inert of 28 on today's tree (`CF-D018` newly inert); 15 move | accepted, and it is worse than reported by one, through no fault of the round |
| Taxa shortfall 15 | T1 −3, T2 −4, T3 −5, T7 −2, T9 −1 = **15**; unfiled **0**; T5 and T8 at their floors | accepted |
| T4 still 61 against a floor of 16, nothing deleted | **61**. §2's caption says the counts are minimums, and every one of those ledgers is a quest reveal source | accepted — deleting them to flatter a census would be the wrong trade |
| Blind attribution cannot be taken by a tool | true, and **I took it: 21/21** | **discharged** — see §5 |
| `getRenderedText()` blind to the book screen | **false** — it is a build-cache artifact, cured by clearing the register before the press | **discharged** — see §1a |
| No prose rewritten, no verdict touched | confirmed by diff | accepted |

Two more of my own: **HEAD boots** (`git archive HEAD game` into a clean tree, `boot-check` exit 0,
re-run today — the earlier report to the contrary was taken before the fix landed), and the
**quest-mechanism share is 27.6%** (45 of 163) against RI-LOR03's *"quest-hint ratio >10%"* hard-fail
clause. I uphold round 3's reading and do not call it a breach: almost all 45 are evidence ledgers
and charter files where the document **is** the evidence, and dialogue is demonstrably the primary
lore vector. I record the number because the next critic should not have to rediscover it.

## 9. Score

**Read this first.** Round 3 scored RI-LOR04 **4** and marked it `partial`, because *the item's
validator did not exist and the band could not be computed*. Round 4 built the tool that computes
it. The first thing that tool did was report **0**, and `SCORING.md` §1.2 step 1 makes an item's
own band a **ceiling** — a native 0 caps the axis at ladder 4. So the axis is pinned at the same
number it had before, **by a cliff rather than by a plateau**, on a measure that did not exist last
round. That is the correct behaviour of an honest instrument and it is the whole reason the
headline has not moved. Consumption moved 5 → 7 on evidence I took myself.

I record RI-LOR04 at the ceiling of 4 rather than below it, and I decline to treat its band-0 row
as a `SCORING.md` §1.1 hard fail, which would cap the axis at **2**. §1.1's cap is for
disqualifiers — AR-1 and AR-2 leakage and their kin. This is the item's own 0-row, tripped by
0.37 of a percentage point on a population of 337, and six of the eight names that trip it are not
naming defects. Scoring a round that correctly renamed 521 people *below* the round that found the
defect would be an artefact of the modelling, not a judgement of the work.

Min-over-axes, per the brief.

| Axis | r3 | r4 | Native | Why |
|---|---:|---:|---:|---|
| RI-LOR03 in-world book structure | 6 | **6** | 3/5 | 163 texts, median 547, 21 wrong-on-purpose, unfiled 7→0, shortfall 27→15, T5 and T8 filled. Still 5 of 10 taxa short, T4 at 61 against 16, and no blind prose comparison is possible — no reveal key exists on disk for any pack |
| RI-LOR06 contradiction discipline | 6 | **6** | 3/5 | 19 mutual pairs, 0 dangling, no adjudicator, and now both sides of every one of them standing in a room. Against that: 13 of 28 disputes inert, and the band-4 secret-leak clause is still unreviewable at 657 hits, unchanged since round 1 |
| ARBITRATION §3 / RI-MTH07 consumption | 5 | **7** | 4/5 | The library half went from consumed-by-nothing to demonstrated: 12/12 books opened by pressing interact at the shelf, each drawing its own words on the screen, zero cross-talk, control with the prop present staying shut. Not 8, because half the register half is still inert and the round's own control is asserted more weakly than it was observed |
| RI-LOR04 naming and language | 4 | **4** | **0/5** | 521 real renames, §4 held at 9.1% against 11%, reuse and slugs to zero, cross-culture 125→14, blind attribution 100%. Pinned at the band-0 ceiling by 8 names of 337 — see the paragraph above and §7 |

**Overall 4 / 10. FAIL against 7.0.** Repair the eight names in §7 and the same tool reports native
3 / ladder 6, which makes the piece **6** — one afternoon from within reach of the gate, and the
cheapest six points available anywhere in this piece.

`seam_sterile: false`. The register crosses the seam and round 3 demonstrated it at 78 answers
(`converse.js:239 canon.allows(info, npc)` — a faction standing decides which side of a registered
dispute a speaker gives you). This round adds a second crossing in the other direction: a book you
can only reach by walking into a particular building now carries one side of a fact the dialogue
system arbitrates, so the lore a player holds is a function of where they have physically been.

**AR-1 (Souls leakage into the fight):** not applicable — nothing here touches combat, checked
against the round's diff rather than assumed.
**AR-2 (Morrowind leakage):** **pass, and it is the right call.** The rule exists to catch *"item
descriptions replacing NPC dialogue as the primary lore vector"*. Dialogue remains primary — 172,692
(npc, topic) pairs against 163 books — and this round's whole contribution is making the secondary
vector *reachable*. Making a secondary vector readable is not the same defect as promoting it to
primary, and a critic who scored it as one would be punishing the repair.

## 10. What I could not do

- **A blind prose comparison for RI-LOR03.** Round 3 found the four RI-LOR03 packs carry a
  one-sided `[REDACTED]` tell and that **no reveal key is on disk**. That is unchanged, so the
  packs still cannot be scored. I substituted nothing this round: I relied on round 3's end-to-end
  reading for the prose and measured only what moved.
- **RI-LOR06 methods 2 and 3** (secret-leak triage), still 657 hits and still unreviewable.
  Unchanged since round 1 and nobody has been asked to fix it.
- **The eleven texts written this round were not walked to.** My twelve legs were chosen to test
  the contradiction pairs; the T5/T8 shorts each sit alone in an inn or a market and are the
  eleven placements that postdate the round's own consumption run.
- **No timing figure**, because none was taken.
