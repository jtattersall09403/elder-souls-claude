# PLAN — W1-02: regions, borders and the strange

Plan-State: satisfied

**Current-state continuation plan; no browser launched and no game code changed.** This plan was
reconciled at **`79fc41b4`**. It does not restart W1-02. It preserves the clock, weather machine,
staggered borders and physical threshold work already on HEAD, then closes the evidence and
consumption holes that independently held the piece at **5/10**. The canonical sources are
`docs/PLAN.md` §3, the six subsystem paths in `orchestration/status/W1-02.json`, the governing
items, `corpus/90-verdicts/wave1/W1-02-r1.{md,json}`, and the open gap ledger entry
`GAP-W1-02-m68-unanswerable`.

This owner-reviewed batch contains **one plan**. W1-02 is already a six-path, multi-system piece;
adding another piece would make the review boundary wider while making the acceptance shallower.

## 0. Reconciliation: what is true now

### Preserve — landed and independently supported

| subsystem | standing evidence at current HEAD | preservation condition |
|---|---|---|
| `world.time.daynight` | `tools/world/env-consumption.mjs` is **9/9**. The fixed step advances exactly 259,200 frames per 24 game-hours (72 real minutes); frozen-clock controls hold; stealth and soul awards change behaviour. | The same tool remains 9/9 and its frozen-clock arm remains observably different. |
| `world.weather.systems` model and visible consumption | All 13 region machines transition deterministically; each declared state changes realised sightline; weather light changes stealth ambient. | Preserve state sets, deterministic transition, sky and stealth consumers. This does **not** close the inert `weatherEffect` table below. |
| `world.region.transition` core | `tools/world/border-traverse.mjs` still measures all 24 borders with zero texture-swap automatic fails: crossover σ min/median/max **16.49/22.82/62.03 m**. `tools/world/threshold-consumption.mjs` remains **8/8** and demonstrates rendered/solid marker consumption with sabotage arms. | M65 and threshold consumption remain green; do not replace `BorderField.traverse()` or create a second border model. |
| `world.strangeness.budget` | The shipped opacity register is inherited and was not the critic's blocking finding. | Run its existing checks in aggregation; do not manufacture replacement work without a fresh failure. |
| `world.region.identity` ONLY-HERE inventory | Prior independent evidence credits W1-01's 840 placed instances across 13 regions. | Preserve and re-run the existing region-axis instrument after re-arming weather; do not rebuild signatures. |
| event vocabulary repair | `day_phase` and `weather_change` are now in the closed event vocabulary; the critic's delete arm made stepping fail when removed. | `node tools/boot-check.mjs` plus a stepping environment probe must remain green. |

### Stale or incomplete claims — do not carry them forward

1. The status says round 3 is in progress and that the M68 pack is ready to run. At current HEAD,
   `reports/wld12-blind/` contains **zero files**; the verdict establishes that the historical run
   stopped at 24/48, at 1280×720, with no final key or question. The pack must be rebuilt, not
   described as merely awaiting judgement.
2. The status treats ONLY-HERE signatures as architecture existence. RI-WLD14 measures building
   grammar, not ONLY-HERE props. `game/data/world/architecture.json` is still absent and no
   RI-WLD14 instrument exists. `world.strangeness.architecture` is unmeasured.
3. `env.weatherEffect` is still a write/report island: current source search finds its world write
   and report, but no stamina, hazard, perception or traversal consumer. The engine's effect map is
   descriptive metadata, not consumption.
4. M66 is not 2/3 evidence of a nearly closed check. `tools/world/tier-announcement.mjs` currently
   fails T3 on **5 of 7** tier jumps (first hostile 3.60–25.84 m past crossings, required 40 m),
   while the item also requires the remaining T4/T5 clauses.
5. The old corpus-routing hole is stale: `node tools/corpus-index.mjs` now reports 330/330 judged,
   zero corpus holes and zero unresolved paths. Do not amend front matter unless the critic finds a
   present mismatch.

## 1. Build boundary and order

The next builder owns one continuation, in this order. A later stage may not hide an earlier red:

1. **Measurement before content:** complete M66/M67/M69 instruments and falsify each one before
   changing borders, population or markers.
2. **Repair the measured border failures:** 40 m encounter grace, MARKED widths, marker legibility,
   and any M67/M69 failure the new instruments expose.
3. **Build an admissible M68 pack:** fix capture pose selection and pack separation, then stop. A
   fresh judge, not the builder, supplies the perceptual result.
4. **Consume weather effects:** wire a deliberately small, typed subset into world-side behaviour
   and prove each effect by perturbation. Unsupported effect keys must be removed or explicitly
   rejected by a content check; a 41-state table may not imply mechanics it does not have.
5. **Build or formally defer architecture:** the default decision is to build the RI-WLD14 data
   contract and static census because this is a declared W1-02 path. Deferral is permitted only by
   a written `ARBITRATION.md` ruling that also changes `docs/PLAN.md`; the builder cannot silently
   drop it.
6. Run the relevant item checks together and hand the result to an independent build critic.

## 2. Acceptance — predicates, numbers and units

### A. RI-WLD12 border completion

| id | population and predicate | acceptance | units / instrument |
|---|---|---|---|
| **A1 M64** | Enumerate adjacency independently from the shipped navmesh and compare it bidirectionally with border data: every walkable navmesh adjacency is declared and every declared adjacency is walkable. Every adjacency has a declared kind and either a landform or threshold object; every trunk-road crossing is marked; census HARD share, distinct object types, and widths by actual kind. The repository's `MARKED` kind is not an alias for item-native `GRADED`: MARKED retains its 60–120 m + object row, while every actual GRADED row is tested at the native **120–180 m** bar. | zero navmesh↔data mismatches; zero empty border; zero unmarked road-borne border; HARD ≥40%; ≥6 object types; every GRADED width 120–180 m; every MARKED width 60–120 m. Reclassification must satisfy the replacement kind's complete row. | adjacencies, borders, metres; extend `tools/world/border-traverse.mjs` or the existing inventory path, no second border reader. |
| **A2 M65** | For every border, crossover coordinates for all nine axes on the live `BorderField.traverse()` path. | zero border with `stddev(c) < 5 m`; HARD σ ≥8 m; graded σ ≥25 m and span ≥60 m; every border resolves all **9/9** axes within the prescribed traverse. | metres; `tools/world/border-traverse.mjs`. This strengthens the currently partial 6/9 marauders-coast--western-rootlands row rather than scoring its subset. |
| **A3 M66** | For every border with `|Δtier| ≥ 2`, run native T1–T5: census ≥2 in-world warnings within 100 m on different channels; measure first hostile on the higher-tier side; inspect crossing gates/enemy scaling; measure unobstructed sightline from the border into the higher region; and census the directionality of every province border. | T1–T5 all pass; first hostile **≥40 m** beyond all 7 tier jumps; zero level check, soft wall, readiness message or scaled crossing enemy; lower-tier expected-level player can see **≥100 m** into the higher region and retreat at every tier jump; **≥3 borders** have a lower tier on the far side. | metres, warning channels, borders, booleans; extend `tools/world/tier-announcement.mjs`. Declared channel strings alone do not satisfy T1. T5 is a census, not a damage predicate. |
| **A4 M67** | Drive a 400 m live traverse of all 24 borders and execute native C1–C5: trace load/fade/cell events; sample both regions' asset residency and visual crossover; pair collision hits with visible meshes and fire a projectile through every player-blocking locus; sample fog, colour-grade and ambient-light parameters; sample water height across every tidal border at all four tide states. | C1 zero load/fade/door/interior transition; C2 both asset sets resident through the zone and residency boundary **≥150 m** from visual crossover; C3 zero meshless player blockers and zero projectile/player asymmetry; C4 fog, grade and ambient-light interpolation each spans **≥80 m** and is **not centred on the axis crossover**; C5 zero cross-border water-height discontinuity at each of four tide states and the named Padomaic/Topal phase relationships hold. All channels observed for every applicable border; an empty population is red. | metres, frames, collider/projectile pairs, four tide states, 24 borders; extend the trace emitted by `tools/world/border-traverse.mjs`. |
| **A5 M69** | Three 1920×1080 screenshots inside each transition zone, shuffled and judged as RI-WLD04 region identity samples. | ≥60% of **72** frames correctly identify one of the two adjacent regions; no border contributes fewer than 3 valid frames. | frames, percent; reuse the M68 capture path and RI-WLD04 scoring protocol. Fresh judge required. |
| **A6 M70** | Preserve the governing item's deliberate seam: sample the **declared** ambient-bed id on both sides of every border; runtime audibility/crossfade quality remains RI-AUD03's responsibility and is not promoted into this gate without a ruling. | every border changes declared bed id and the 13 regions do not all declare the same bed. | borders and distinct declared ids; use the M65 data reader. |

**A-stage score gate:** all six RI-WLD12 automatic fails are false, and the weighted M64–M70
score is **≥70/100**. The plan targets all rows rather than treating 70 as permission to leave an
automatic-fail clause unmeasured.

### B. M68 perceptual pack and open gap

Population: two directions for each of 24 borders = **48 opaque PNGs**, 1920×1080. Candidate poses
must be tested against the rendered world's occluders, not only node-side border coordinates.

| predicate | acceptance | units |
|---|---|---|
| pack completeness | exactly 48 PNGs; every border and direction represented once; no missing/blank frame | frames |
| pose validity | geometry within the first 4 m of the eye-to-subject ray on ≤4/48 frames; invalid candidates are rejected before capture | frames, metres |
| quarantine | question and PNGs in pack directory; deterministic reveal/key in sibling `reports/wld12-blind.reveal/`; no key, border id, region id, scoring rule or deterministic-seed recipe exposed to judge | files / leak audit |
| perceptual result | a fresh judge who did not build the pack answers **“two places” ≥80%** of usable frames, answers “one” <25%, and among “two” answers names an object or landform ≥70%; fog/colour is not the most common cue | percent of usable frames |
| diagnosis | current historical 24-frame result remains recorded as 52.6%, not overwritten; new results report by border kind and occlusion stratum | percent / strata |

The builder does **not** score B. It audits completeness, resolution, occlusion, blankness and leaks,
then reports judgement `not_run` and requests a fresh agent through normal orchestration.

### C. Marker legibility

For every threshold type against every ground albedo on which it is placed, compute perceptual
colour difference and projected size at the M68 camera distance. Acceptance: **ΔE >12** for every
placed type/ground pair; marker height **≥1.5 m** or projected silhouette height ≥24 px at its
farthest intended announcing distance; existing colour-free/size-free silhouette distinctness
remains above its **0.12** pair-distance bar. The measurement must include the two known red rows
(`imperial_border_cairn` 4.2, `corpse_in_a_cage` 10.8) and the 1.1 m salt-glass marker.

### D. Weather-effect consumption

Define and validate a closed effect schema. At minimum, retain and consume:

| key | world-side consumer | acceptance |
|---|---|---|
| `stamina_regen_mult` | the one shipping stamina regeneration path, not a parallel calculator | Over 600 non-guarding fixed steps at fixed initial stamina, identical weather states differing only in multiplier produce the analytically expected stamina delta; multiplier 1 control matches baseline. |
| `chip_dps` | the shipping hazard/damage tick | Over 600 fixed steps, positive chip weather reduces player HP by `chip_dps × 10 s` within one fixed-step quantum; zero control changes HP by 0. |
| `disease_buildup_per_s` | the shipping status-buildup path | Over 600 fixed steps, buildup increases by `rate × 10 s` within one quantum and the zero control holds. |

Acceptance is **100% of retained keys** having a named runtime consumer and a perturb-and-watch row;
unknown or retained-but-unread keys make the content check fail. Sightline and weather-light evidence
remain green. Extend `tools/world/env-consumption.mjs`; do not create a second environment model.

### E. RI-WLD14 architecture

Author the `architecture.json` contract RI-WLD14 M78 names by extracting facts from the shipping
settlement/building geometry, not by writing labels that the renderer ignores. Implement and run
**all M78–M84**; M81–M84 are not conditional merely because they require render/blind evidence.
Acceptance is the item's native weighted score **≥70/100** (ladder 4; 80 and 92 anchor ladders 6
and 8), with every F1–F8 field measured over actual meshes/settlements, ≥9 complete and regionally
confined grammars, and every item automatic fail false. M81 must obtain ≥32/40 correct process
verbs; M82 must satisfy E1–E5 with zero Imperial buildings in good order; M83 must find zero banned
instances; M84 must get ≥10/12 buildings read as made by a people and, until a real Morrowind crop
set exists, is unmeasurable and scores 0 rather than being omitted. Add a consumption differential:
perturb one grammar parameter on a shadow data tree and observe at least one built settlement mesh
statistic change. Deleting the architecture data must make M78 fail, not reduce its population to
zero and pass vacuously.

If current renderer structure makes a truthful `architecture.json` impossible, stop this stage and
write an S-ruling with the measured obstruction. Do not substitute ONLY-HERE prop counts.

### F. Aggregation and closure

At the builder's commit, run the relevant standalone instruments, `node tools/boot-check.mjs`,
`node tools/check-data.mjs`, `node tools/corpus-index.mjs`, and `node tools/publish.mjs`. Stamp every
artifact with the full commit. The open gap closes only after B's independent result meets its bar;
the builder may at most mark the remedy built and awaiting judgement.

## 3. Null controls — what must become worse

| subject | red arm | required worsening | inert-control signature |
|---|---|---|---|
| M65 stagger | shadow `borders.json` with every axis offset collapsed to zero | 24/24 borders report σ=0 and texture-swap fail | any border remains green, meaning the tool did not read the shadow data |
| M66 grace | shadow population restores the five current hostile placements | T3 returns exactly the five current violations, all <40 m | both arms list the same distances after population moved |
| M67 | disable border collision/streaming observation independently | the corresponding C row becomes unmeasurable/red while the other rows still execute | 0 observed events scored as pass, or both teardowns yield identical complete traces |
| M68 pose gate | run validator on the historical occluded 24-frame pack | it reproduces 16/24 near-lens occlusions and fails the ≤4/48 proportional ceiling | validator calls historical pack clean or sees zero rays |
| marker legibility | restore known colours/heights on shadow data | ΔE rows return 4.2/10.8 and salt-glass size fails | expected values move with edited colours, or known rows disappear |
| weather consumption | zero each retained effect independently | exactly its entity-behaviour delta returns to zero; unrelated weather rows remain green | both arms change identically, or only a harness report changes |
| architecture | delete/neutralise grammar data on shadow tree | census fails closed and rendered mesh statistic returns toward baseline | empty grammar population passes, or mesh statistic is unchanged |

For every repair, also run delete-the-fix against the pre-repair shape. A single shared teardown is
not enough for multiple consumers: each retained weather key and each M67 observation channel needs
its own red arm.

## 4. Existing instruments and files to reuse

- `tools/world/border-traverse.mjs` and `game/src/world/borders.js` — canonical border traversal;
  extend its trace for M64/M67 rather than inventing another crossover implementation.
- `tools/world/tier-announcement.mjs` — canonical M66 path and current five-violation baseline.
- `tools/world/threshold-consumption.mjs` — marker geometry/collision consumption and sabotage
  precedent; preserve all 8 rows.
- `tools/world/wld12-blind-pack.mjs` — repair capture selection and pack separation in place.
- `tools/world/env-consumption.mjs` — extend the existing RI-MTH07 consumer proof.
- `tools/analysis/region-axes.mjs` (or the current indexed equivalent named by RI-WLD04) — re-arm
  weather and consume M69 samples; do not score strings from `regions.json` as observations.
- `tools/capture/` — all browser captures. Run `node tools/contention.mjs --gate` first and keep one
  browser instance.

No existing RI-WLD14 tool was found. Building that item-named instrument is required by rule 24,
not a prohibited duplicate.

## 5. Recommended model and dispatch shape

**Opus.** The next work includes designing M67/M69 and the missing RI-WLD14 instrument, resolving a
data-versus-runtime contract, and building a blind pack. PLAN-LOOP assigns measurement design and
blind work to Opus. Keep one builder for the coherent border/environment change; dispatch the blind
judge separately only after the pack is sealed.

## 6. Risks carried to build criticism

`CARRIED-1` — M69 and M68 share captures, so a leaked M69 region-label key could contaminate M68.
Seal them as separate opaque packs/reveals and have the critic audit cross-directory filenames and
metadata.

`CARRIED-2` — weather effects can stack with unrelated stamina/status modifiers. Fixtures must hold
attributes, equipment, guard state and hazards constant and publish both raw per-step deltas and the
final entity state; otherwise the right total can come from the wrong consumer.

`CARRIED-3` — architecture may overlap W1-01 settlement geometry ownership. This is not permission
to omit it. Claim exact files after reading live statuses; if the renderer has no grammar seam, seek
a ruling rather than introducing a parallel renderer.

## 7. What the plan critic should attack first

1. Whether A4's M67 predicates exactly match RI-WLD12 C1–C5; a plausible trace that omits one clause
   would repeat the current “half measured” failure.
2. Whether the M68 occlusion test can see rendered scatter rather than only declared solids. The old
   camera failed precisely because node-side data could not see renderer-produced boulders.
3. Whether D's retained effect subset improperly makes deletion look like consumption. The critic
   should inspect every remaining effect key and reject descriptive promises masquerading as mechanics.
4. Whether E's architecture contract measures the geometry that actually ships, and whether the
   perturbation reaches the renderer. A census over a new JSON file alone is explicitly insufficient.
5. Whether one Opus build is still too broad after instrument-first staging. If so, split at the
   clean boundary after A/C and before D/E, while retaining this single acceptance document.

## Plan critique — 2026-08-09

**Verdict: BLOCKING.** The continuation boundary is well reconciled, but its RI-WLD12 acceptance
table is not faithful enough to the governing item to dispatch. The single biggest gap is that the
plan silently substitutes several border predicates, so a builder could satisfy this plan while
leaving native automatic-fail evidence unmeasured.

1. **BLOCKING — A1 changes the inventory bar.** RI-WLD12 M64 requires `GRADED` widths of
   **120–180 m** and checks undeclared navmesh adjacencies and unmarked road-borne borders. A1 instead
   requires `MARKED` widths of **60–120 m** and omits both census clauses. Resolve the repository's
   `MARKED`/item `GRADED` naming seam explicitly, retain the native 120–180 m bar, and enumerate
   navmesh↔data plus road marking in the predicate.
2. **BLOCKING — A3 omits M66's 100 m sightline and changes T5.** The native test fails on a far-region
   sightline below **100 m** and asks whether at least three province borders descend in tier. The
   plan substitutes “3/3 descents traversable without damage,” which is neither the sightline check
   nor T5. Restore all five native clauses without adding a different damage bar.
3. **BLOCKING — A4 does not cover C1–C5.** It omits the projectile/player collider asymmetry in C3,
   restricts C4's ≥80 m interpolation test incorrectly to “each graded visual axis” rather than the
   named fog/grade parameters and their centre, and omits C5's water-height continuity at all four
   tide states. Its null controls consequently cannot falsify those missing channels.
4. **BLOCKING — A6 upgrades deliberately declared-only M70 into a runtime-audio gate.** M70 says
   audio is unreachable through this harness and fails only when a declared bed does not change or
   all 13 regions declare the same bed. Runtime ambience quality belongs to RI-AUD03. Preserve that
   seam or obtain a ruling that amends the item; do not change the bar inside the plan.
5. **BLOCKING — E misstates RI-WLD14's native scale and leaves required checks conditional.** The
   item's playable threshold is **70/100** (ladder 4), not “native ≥7/10,” and M78–M84 are all part
   of its score. Replace “add M81–M84 only as the item requires” with an explicit disposition for
   every method, including the two blind packs and their independent judges. A static M78–M80
   build alone cannot make this declared path build-ready.

**Remediation acceptance:** make the A/E tables a lossless transcription of the native populations,
bars, automatic fails and declared-unmeasurable seams; add a red arm for every restored C3–C5/M66
channel; then mark `awaiting-recriticism`. `CARRIED-1..3` remain useful and may carry unchanged.


---

## Fresh plan re-critique — 2026-08-09

**Verdict: PASS.** Read independently against RI-WLD12 M64–M70 and RI-WLD14 M78–M84. The
remediation restores the native border inventory, tier sightline and directionality, collision and
projectile symmetry, four-state tidal continuity, deliberate declared-audio seam, full architecture
score, automatic fails, and blind-test fail-closed rule. Each acceptance states a population,
predicate, number and unit; each repair has a red arm that can distinguish an inert fix or control.
No qualifying blocking gap remains.

**Recommendation:** plan satisfied; dispatch the continuation builder without reopening the plan.
The builder must still stop before judging either blind pack, exactly as §1 and §2 require.
