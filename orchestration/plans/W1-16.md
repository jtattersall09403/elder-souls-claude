# W1-16 — current-state continuation plan

Plan-State: satisfied

## Builder/critic execution allocation (binding)

This plan incorporates [`BUILDER-EXECUTION-CONTRACT.md`](BUILDER-EXECUTION-CONTRACT.md).

**Builder-owned:** implement remaining equip/burden numerator, tier, pin persistence, magic-offset,
progression and directly owned economy/seam repairs; check the level formula through at least 140,
all 48 load cells and exact static tables; instantiate the six legal builds, verify every affected
tier boundary, exercise one representative per distinct burden/equip consumer, complete one real
use-growth→hearth flow, and live save/load bare, dressed, Feather and Burden states; run targeted
red/delete/2x2 controls for each changed mechanism; and finish the bounded builder aggregate.

**Critic-owned:** the complete nineteen-item method matrix, every burden/equip consumer population,
full regional economy and character/AI/quest/stealth/travel/UI populations, comprehensive save/load
and delete matrices, native aggregation, scores and verdict.

Formula/table completeness, exact boundaries and representative live consumers establish the
mechanics. Broad systemic balance and cross-piece outcomes require the full critic suite.
Canonical-HEAD: `1c4c487d` (reconstruct and restamp before building)

## 0. Authority, scope, and history reconstructed

The authoritative owner is `docs/PLAN.md` §3 W1-16: all ten progression paths, judged by **RI-AI05, RI-CHR01, RI-CHR02, RI-CHR03, RI-DLG04, RI-LOR05, RI-PRG01, RI-PRG02, RI-PRG03, RI-PRG05, RI-PRG06, RI-PRG07, RI-QST03, RI-QST08, RI-STL01, RI-STL02, RI-TRV01, RI-TRV02 and RI-UIX03**. Seam S23 additionally gives RI-CMB01 authority over equip-load effects inside combat. `docs/PLAN.md`'s amendment records that W1-07 already built `progression.level.attributes`, `progression.skill.usegrowth`, and `progression.build.identity`; that is evidence to preserve and verify, not permission to drop those paths.

Authored history is the non-bank path history through the initial W1-16 build, round 2 (`97db1496`), round 3, its independent FAIL verdicts `W1-16-r2` and `W1-16-r3`, and round 4 (`c4d424dd`). Round 2 established the correct four roll tiers, the burden/equip-load anti-merge, slot handling, burden pin, held-sprint denial, and falsifiable break arms, but found that the fighting hand had no weight. Round 3 coupled fighting hands to the ratio, added shield weights and overloaded sprint denial, and made LIGHT/MEDIUM/HEAVY reachable; its critic found a save/load changing the roll class and Feather's clamp asymmetry. Round 4 asserts that pin persistence and the magic offset were repaired. **There is no independent round-4 verdict.** Treat every round-4 statement as a builder assertion until a fresh critic reproduces it.

Established evidence to preserve: r2/r3 critic closure rows, with their exact cited reports; W1-SAVE-AI's independently verified hostile-save repair; the three exact tier cliffs and frame tables. Stale evidence: any stepping burden number before `c36653e`; any claim that loadout hands weigh nothing (closed in r3); any r3 claim about post-save persistence (r4 changed it); any fixed-state-only test used as save evidence. Cross-piece seams remain explicit: W1-07 supplies attributes/use-growth/build identity; W1-13 owns hearth spacing but RI-PRG04 method 9's 13-region economy rebasing remains W1-16; W1-21 owns the UI implementation, but W1-16 must prove the governing RI-UIX03 information/readout predicate and file a named dependency rather than waive it.

## 1. Build-ready acceptance contract

The builder must first emit a machine-readable **bar matrix** with one row for every method and hard fail in every governing item above. Each row names population, unit, comparator, threshold, evidence path, and `PASS|FAIL|UNMEASURABLE`; `UNMEASURABLE => 0`, never omission. The following predicates are the minimum operative contract, not a replacement for a stricter native item clause.

### A. Souls, levels, attributes, skills, and build identity

1. **RI-PRG01 curve:** for every shipped level-table row and sampled formula row through at least level 140, cost equals `round(0.015n^3 + 2.00n^2 + 55n + 300)` exactly (souls; integer equality), is strictly increasing, souls—not gold—are debited at a hearth, and no invented level cap is introduced. Independent expected values must be computed from the item formula, not read from the implementation/table being tested.
2. **RI-PRG06 pace:** run the native population/yield and regional-bank methods on current HEAD. Publish placed-world souls, kills/level, levels per region and the RI-PRG04 method-9 13-region death-cost comparison in souls, kills, and minutes. Apply every native band/hard fail exactly. Use the kill ledger (`statblock.souls`) and reconcile `population-posts.json`; cached data alone is not evidence.
3. **RI-PRG02 stat sheet:** enumerate every governed displayed/derived stat over the item's full sheet/build population; values, units, caps/softcaps and derived formulae must match native tables exactly. Perturb each source attribute independently and observe its named live consumer. Any absent display or consumer is fail-closed.
4. **RI-PRG03 use-growth:** execute the item's complete skill population and comparisons. Only use raises the used skill; unrelated skills remain byte-identical; gates/thresholds and save/load persistence match the item; scripted grants cannot substitute for a real-use path. Preserve W1-07 only if its existing instrument remains green and still goes red under its deliberate break.
5. **RI-CHR01/02/03 and RI-AI05:** run the native character-creation, race, birthsign and archetype grids. Starting values and legal combinations match exactly; race/birthsign/build perturbations produce the required downstream live differences without level scaling enemy statblocks. Execute RI-CHR02's AR-3 encounter comparison on the stated race population and native thresholds.

### B. Encumbrance and combat seam

6. **RI-PRG07 max load:** all **48** published cells equal the item to its stated precision; monotonic in both governing stats; uncapped tail `maxLoad(99)-maxLoad(60)=58.5 kg` exactly. Equip ratio denominator is max load, not burden capacity.
7. **Declared numerator:** equipped weapon + shield + armour + talisman weights are counted once each from the same live objects the fight uses. Inventory carried-but-not-equipped contributes exactly 0 kg. For every shipped equipment category, swapping to a different declared weight changes the ratio by `Δkg/maxLoad` within floating tolerance `1e-6` percentage points. No parallel hand may remain.
8. **Tier cliffs and RI-CMB01:** sweep hundredth-percent samples and exact boundary pairs. The only transitions are `30.00→30.01`, `70.00→70.01`, `100.00→100.01%`; within-tier variance is zero. At 60 Hz, LIGHT=`startup 4, i-frames f5–30 (26), recovery 22, total 52, stamina 22`; MEDIUM=`4, f5–26 (22), 34, 60, 26`; HEAVY=`6, f7–16 (10), 72, 88, 34`; OVERLOADED=`no roll, 0 i-frames, recovery/total 120, stamina 40`, and forbids sprint/jump attack. I-frames must be one contiguous entity-side run.
9. **Reachability:** instantiate the six RI-PRG07 §5 builds from legal shipped content and meet their stated tier targets. Also report the heaviest legal set and kg/% shortfall for every unreachable tier; a pin is never reachability. If content still cannot instantiate a native fixture, it is red—do not invent weights or exempt the method.
10. **Anti-merge:** on the same fixture, double carried pack weight while holding equipped set constant: burden/travel observables must change and roll trace must remain byte-identical. Then alter equipped weight with pack constant: equip ratio/tier must change as predicted. Both divisors and sources remain separately named.
11. **Save and magic invariance:** for every shipped named state plus arena fixtures, save at bare, dressed, active Feather, active Burden, and hostile-present moments; after live load and 4+ steps, equip kg/%/tier, i-frame window, burden, offsets and live AI equal pre-save. Pinned states remain pinned; deriving states resume derivation. Feather/Burden apply then expire back to the exact baseline within `1e-6`, including baselines below magnitude; repeated equip changes do not eat or double offsets.
12. **All RI-PRG07 consumers:** enumerate, do not sample: movement speed, sprint, travel, fatigue, sneak, jump, fall damage, roll, stamina, and any item-declared consumer including talismans. For each, perturb only the governing ratio/tier and observe the named world-side entity behavior. Missing consumer is zero/fail-closed. AR-1 guard: burden cannot affect in-fight frames/hitboxes/stamina/poise; equip tier cannot leak into unrelated Morrowind logistics.

### C. Gold, barter, quests, stealth/travel, and UI

13. **RI-PRG05 gold economy:** execute the item's complete source/sink basket, denomination, price, wealth and inflation comparisons in gold and percent; every declared source and sink has a live caller. Souls never buy merchandise/readmission/travel, and gold never buys levels. Preserve any already-green rows only after current-HEAD rerun.
14. **RI-DLG04 / RI-QST03 / RI-QST08 / RI-STL01 / RI-STL02 / RI-TRV01 / RI-TRV02:** run each native comparison population and hard fail. Progression inputs (skill, attribute, faction standing, burden, gold) must alter disposition/persuasion, quest gates/rewards, sneak/theft/fence outcomes, transport fare/time/schedule and travel magic exactly where those items say; no harness-only consumer counts. Cross-piece data owners may implement their side, but W1-16 closure requires an end-to-end perturbation at the seam.
15. **RI-UIX03:** the inventory/pause population and forbidden-overlay probes meet every native predicate. The player-visible burden and equip-load readouts show correct units and current values and change color/label at the item threshold; game pause policy remains as specified. W1-21 may own rendering code, but absence is a W1-16 dependency and a red row, not an exemption.
16. **RI-LOR05:** soul terminology, sources/sinks and player-facing metaphysics remain canon-coherent across the item's full text census; mechanical soul use cannot contradict the governing dossier.

## 2. Instruments, evidence, and hard controls

Reuse `tools/harness/prg-encumbrance.mjs`, all W1-16 r3/r4 offline/live/chart tools, `tools/check-souls-world.mjs`, creation/quest/stealth/travel/UI instruments indexed by each reference item, and the aggregate gates. Extend rather than replace a native tool when a required method is missing. Every report stamps full commit and dirty state.

* **Red controls:** each instrument is run after an isolated mutation of its subject; all graded rows that mutation should affect must turn red. Empty populations, absent fields, thrown errors and `null` are failures.
* **Null/control:** pack-only versus equipped-only; pinned versus deriving; pre-save versus post-live-load; spell active versus expired; souls versus gold. Assert each arm's support genuinely differs before comparing outcomes.
* **Delete-the-fix:** on a detached scratch worktree remove r4 pin restoration and offset separation independently, then together. The save arm must reproduce the r3 tier change; the magic arm must reproduce non-return to baseline; the shipped arm must not. Retain `__breakW116` only as a diagnostic secondary control, not the sole delete-the-fix.
* **Aggregation:** run the complete bar matrix and repository gates together. Overall is the minimum governing-item/axis score, with native hard-fail caps; no averaging away a red requirement.

## 3. Ordered build/closure sequence

1. Freeze HEAD, rerun all existing W1-16 instruments and classify each historical claim as preserved, stale, builder-only, or red.
2. Close round-4 criticism first: save/load live-world matrix and magic offset lifecycle, with physical delete-the-fix.
3. Complete the full governing-item bar matrix, addressing only red/unmeasurable rows. Do not rewrite genuinely green subsystems.
4. Resolve cross-piece seams by named dependency plus end-to-end evidence; never silently delete a row.
5. Run all item-native methods, controls, aggregation and global gates; publish machine-readable reports and a human summary. Hand to a fresh critic.

## 4. Completeness audit

A builder cannot turn this plan green while failing a governing bar because every governing item is enumerated, every native method/hard fail is an explicit fail-closed row, the known W1-16 failures have exact populations and counterfactuals, and cross-piece ownership changes implementation responsibility but not acceptance.


## Independent plan critique — 2026-08-09

**Verdict: SATISFIED.** I reconstructed the piece assignment from `docs/PLAN.md`, then read the
19 governing items, seam S23/RI-CMB01, the current r2/r3 verdicts, the round-4 implementation
claims, and current indexed instruments rather than accepting this plan's summary as the bar. I
actively looked for a green route that could omit a native method, hard fail, population, unit,
consumer, save/live-load state, or independent counterfactual. None remains.

The decisive feature is the fail-closed row-per-method-and-hard-fail matrix: native clauses remain
authoritative where stricter, `UNMEASURABLE` is red, and aggregation is minimum-over-axes with
native caps. The operative clauses additionally close the known laundering routes: independent
formula expectations; kill-ledger rather than cache evidence; full 48-cell and six-build
populations; equipped/live-object numerator; exact threshold pairs and 60 Hz frame traces; complete
burden-consumer enumeration; real-use skill growth; end-to-end cross-piece consumers; post-load
stepping; spell expiry; physical round-4 delete-the-fix arms separately and jointly; and an
aggregate run. In particular, the generic matrix obligation retains RI-PRG07's burden tiers,
Shell-Warden 200-run viability test, gold-weightlessness check, and every other native method even
where the clarifying list does not restate it.

**Recommendation:** plan satisfied; dispatch the continuation builder. Empirical outcomes may still
be red, but no governing pre-build requirement is deferred or carried.
