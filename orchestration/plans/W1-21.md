# W1-21 current-state continuation plan

Plan-State: awaiting-recriticism

## Authority and reconciled state

The authoritative `docs/PLAN.md` §3 decomposition owns six UI paths and names eleven judges:
**RI-CAM03, RI-CAM05, RI-PRG01, RI-PRG02, RI-UIX01, RI-UIX02, RI-UIX03, RI-UIX04,
RI-UIX06, RI-VIS05 and RI-WLD06**. Read those complete current items, their reference artifacts,
comparison methods, scoring, hard fails and later rulings. UIX05 belongs to the readable/book
owners and is not a W1-21 prerequisite. Historical “no map” JU8 text is superseded by S35: the
soundness-limited travelled-terrain map exists, but no objective/quest route may exist. Historical
W1-21 evidence is credit only where its recorded commit is current HEAD (or a narrow source/fixture/consumer diff proves the governing row unaffected) and its fixture, population and consumer remain valid.

Preserve and reuse commit-matched/diff-validated behaviour rather than freshly replaying it: six screens reachable by keyboard/gamepad; pure model
screens; `UISurface.el()` as the sole clipped draw entry; marker forgery resistance; pause/no-pause
model; current content breadth. Do not add a raw canvas escape hatch. Remaining work includes
fail-closed detectors, actual observed framebuffer coverage, native ΔE fringe, complete closure of
the eleven governing items, UIX04 JU12, UIX03 step 8 and UIX06's six-judge bootstrap.

## Acceptance: all governing items

| Item | Required native population and exact green condition |
|---|---|
| **UIX01 U1–U10** | Capture every HUD state across idle/combat/boss/status/lock/equipment at all supported resolutions and the three named worst backgrounds. E1–E9 coverage exact; U1/U2 budgets; centre non-reticle coverage 0; X1–X12 zero. Stamina same-frame error ≤0.005 on 100% frames, regen states ≥3:1, contrast ≥4.5:1 fill/empty and ≥3:1 background in all three, ≥8 px at 1080p and ±10% fraction, spend/exhaust timing exact. U5 uses a known post-hit frame at pinned DPR1 and yields 0 false digits; hard fails exact. |
| **UIX02 K1–K6** | Full `game/data/` scan; ≥14 viewpoints; all five quest fixtures; complete UI element census. F1–F5 zero; no unidentified component; quest differential empty outside declared map exceptions; yaw sweep zero world-tracked elements; rendered prose has zero coordinate strings. S35 map derivation is tested for soundness and contains travelled terrain/visited places/self only—no objectives. Any skipped state or empty list makes K6 fail. |
| **UIX03 M-P1–M-E3** | 120-step outside/inside-combat fixtures: delta 0 and exactly 120; combat transition same frame, roll/block/light live, no dilation; opacity ≤55%, area ≤60%, centre 40% legible; equip commitment ≥30 frames. Census all items: ≥8 categories, required fields/weights, closed exemptions, no procedural drops, real encumbrance, sort/search/detail/two-panel containers. Description budget native; N1–N7 zero. Every hearth and off-hearth controls verify L1–L8, 10 attributes, no gold, correct curve/preview/soft caps. Any native hard fail caps item. |
| **UIX04 JU1–JU12** | Four-quest fixture and complete live journal: chronological default, interleave ≥0.5 (hard red ≤0.30), verbatim append-only entries, names-only index scrolls chronology, chronological search/topic link with no position, visible extent, native pause rule, Q1–Q12 zero under declared+OCR detection. JU8 follows S35 (map may exist as separate sound map; journal must not expose quest-location route). JU12 sealed pair judged independently; builder does not judge. |
| **UIX06 AD1–AD7** | Complete six-screen set: ≥6/9 materials, zero uniform rounded panels, project faces ≤2, every hue ΔE≤10 to world palette, no UI blue/furniture/pictogram/focus rings. Six fresh isolated judges run Bootstrap; worst-of-six must pass and any hard fail is red. |
| **UIX06 FD1–FD7** | Eight native capture configurations. Sharpness ≤1.5 device px at DPR1 and DPR2; vector/SDF and ≥18 px; zero clipping/overlap/overflow; scaling ±10%, undistorted corners/icons ≥1:1; **fringing measured in ΔE ≤3** (hard red >8); ≥7 bits and overdraw ≤3×. Each emits sample count/configuration and empty is red. |
| **CAM03/CAM05** | Run every native camera/UI and death-surface method on its stated viewport, combat/death state and frame population. Preserve exact visibility/occlusion, transition, timing and hard-fail predicates; an absent camera/death state is zero, not waived as another piece's implementation. |
| **PRG01/PRG02** | Run the native player-visible level/attribute presentation and interaction methods over their complete attribute, tier, hearth/off-hearth and control populations. Values, previews, costs, caps and restrictions must agree with the production progression consumer; no duplicated UI-only calculator may earn credit. |
| **VIS05** | Run its Morrowind art-direction comparison only on the paths this piece owns, with its native reference isolation, populations, predicates, scoring and hard fails. Reuse the UIX06 ART capture pool only where configuration and declaration are identical; do not run a second capture sweep. |
| **WLD06** | Run every native no-marker/navigation predicate touching the HUD, journal or map over the full stated journey/viewpoint/state population. Reuse UIX02/S35 captures and traces where identical, but preserve WLD06's own denominators, thresholds and hard fails. |

Each of the eleven governing items must reach its native meets-bar threshold and overall min must be
≥7/10; every automatic/hard fail must be false. UIX05 is neither scored nor rebuilt here. Do not
average away one failed screen/item.

UIX03 comparison step 8 is mandatory after the automatic structure gates: make an opaque,
neutralised structure-only pack pairing the shipped inventory screenshot with the Morrowind
inventory reference, preserving density, category breadth, weight/value and item-detail-panel
shape while stripping labels/provenance. A fresh independent judge records its structure judgement
before a sealed reveal; neither UI art nor UIX06 criteria may enter this judgement. Store the
pack, prompt, response, reveal and judge identity, and report `not_run` until that result exists.

UIX06 uses its native comparison steps 1–13, in order, without fusion:

1. **ART_DIRECTION pass first.** Emit the RI-VIS01 declaration for P09, permitting only RI-VIS05
   (Morrowind) §H and forbidding RI-VIS02/RI-VIS03 metrics. With the world blacked, capture the
   six named UI screens at 1920×1080. Run palette conformance (ΔE≤10 and forbidden hits), the
   zero-rounded-panel detector, and the project-font census; then record the required manual
   nine-material/screen census with one crop per claim and the six isolated Bootstrap judgements.
2. **FIDELITY pass second.** Emit a separate declaration for F17–F19, permitting only RI-VIS02 and
   forbidding RI-VIS05. Capture those six screens at 1280×720, 1920×1080, 2560×1440 and
   3840×2160, each at DPR 1 and 2 (the item's 48 captures), then run the native stem, layout/scaling,
   alpha-fringe, known-50%-composite, gradient and overdraw measurements. Do not consult the ART
   references or use an art explanation in this pass.
3. Only after both passes, run `cc-scan.mjs` with CC-7; if the installed scanner lacks CC-7, do
   the item's phrase-list scan manually and declare that fact. Report **two** native scales and
   gaps (`UI ART DIRECTION` and `UI FIDELITY`) and the `min`, never a fused score. Preserve both
   declarations, permitted/forbidden sets, capture hashes, manual records and contamination result.

## Instrument repair and controls

1. Repair existing canonical tools in place: `ui-census`, `ui-forbidden`, `ui-layer`, `marker-diff`, `journal-ui`, `book-budget`, `ui-metrics`; no parallel detector suite. Every row emits subject population, sampled population, skipped reasons, native unit and threshold.
2. Pin viewport/backing buffer 1:1 for OCR calibration; require non-null hit frame. A no-digit fixture must yield 0 and a drawn “247” fixture ≥2 detections. Remove subject/state from each tool: it must return `EMPTY`, never PASS. Restore it and verify positive population.
3. Add a synthetic map-place element: `getUIState().map.markers` must change and every S35 filter/detector must reject it regardless of id prefix. Add a state-writing accessor: mutator report must flag it.
4. Red controls: tween stamina one frame; add each X5–X8 route; group journal by quest; inject coordinate prose; pause one combat step; draw system-font rounded card/UI blue; render a DPR2 upscaled bitmap; add ΔE>8 fringe. Each native row and only the relevant row goes red.
5. Delete-the-fix on clipping/census, marker derivation and each detector; demonstrate historical empty-green/false-positive baseline returns. Blind packs contain actual screenshots, opaque names, no reveal, and are judged by fresh agents.

### Exhaustive UI model consumption gate

Before implementation, freeze a census of every player-facing model shipped through this piece:
`_hudModel`, `_inventoryModel`, `_containerModel`, `_journalModel`, `_bookModel`, `_levelModel`,
`_sheetModel`, `_spellModel`, `_mapModel`, the rebinding/refusal view, the touch-overlay model and
the rotate/calibration model. If HEAD adds another `UISystem` model/drawer before the run, it joins
the census; an unenumerated model is a hard incomplete result, not an exemption. For **each** row,
name the production drawer/world-side consumer and one visible or actionable player consequence;
hold the fixture constant, drive two well-separated valid model values, and record the changed
pixels/text/action. Then disconnect or equalise that consumer as the null and observe the
consequence disappear/go red before restoring it. Harness state alone is only the observer.
Every row requires non-zero RI-MTH07 coupling; zero coupling scores the owning native dimension
zero and is written to status reasons. This single census replaces ad-hoc repeated model probes.

## Evidence and closure

One aggregate report at commit HEAD must contain all eleven governing items, raw per-screen/config
samples, pixel crops, state traces, blind-pack manifests (`not_run` until independent results), the
hard-fail matrix and native score arithmetic. Share legitimate UIX02/WLD06 and UIX06/VIS05 evidence
rather than recapturing identical states. Run cheap source/data/population gates first, then one
capture pool and one browser session, and stop before blind work whenever a hard fail or missing
population makes the aggregate unsatisfiable. Run the named project gates and aggregate. The builder
may not call the plan complete while a governing blind result is absent.

Existing raw samples and independent judgements may feed that report when their artifact commit is
HEAD or a recorded narrow diff proves the relevant source, fixture, consumer, capture configuration
and blind-pack contents unchanged. Do not recapture or re-judge such rows. Rerun only stale,
changed, partial/red/unmeasured rows and the minimum shared aggregation needed to detect interaction;
any uncertainty about blind-pack identity makes that blind result stale rather than reusable.

---

## Independent plan critique — 2026-08-09

**Verdict: BLOCKING / awaiting-remediation.** A fully green run can omit governing comparison
methods. The plan never requires UIX03's independent inventory-layout blind pair, and it reduces
UIX06 to measurements plus a six-judge Bootstrap result without requiring the item's two-pass
ART_DIRECTION/FIDELITY declarations, forbidden-reference isolation, ordering, contamination scan,
or paired reporting. Those are method requirements, not optional evidence formatting; a builder
could knowingly use fidelity criteria to excuse art (or art criteria to excuse fidelity), obtain
7/7 on both self-selected readings, and leave the required anti-fusion control unperformed.

**Required remediation:** add UIX03 comparison step 8 with the actual neutralised structure pack,
fresh independent judge and reveal discipline. Add UIX06 comparison steps 1–13 verbatim in
operative form: two ordered passes, separate declarations and permitted/forbidden reference sets,
world-black ART captures, eight-configuration FIDELITY captures, CC-7 contamination scan (manual
where the item requires), and two separately reported native scales. Also enumerate every shipped
UI model and add RI-MTH07 perturb/observable/null consumption evidence; the current plan's model
purity and detector controls do not prove that every authored model reaches what the player sees.

**Recommendation:** remediate only these method/consumption omissions while preserving the strong
native metric matrix, then request fresh independent criticism.

### Remediation outcome — 2026-08-09

All recorded blockers above are repaired: UIX03 step 8 now has its neutral structure-only blind
pack and reveal discipline; UIX06 now runs the authoritative ordered two-pass declarations,
reference isolation, black-world ART and eight-configuration/48-capture FIDELITY populations,
manual and automatic CC-7 scan, and paired reporting; and the complete shipped UI-model census now
has player-visible/actionable RI-MTH07 perturbation and proven-red null requirements. Material edit:
this version is **awaiting-recriticism**, never self-approved.

### Fresh reviewer-editor outcome — 2026-08-09

**Material edit / awaiting-recriticism.** The received version used a by-directory UIX01–06 set,
contrary to the authoritative path-to-`judges:` decomposition. It required the non-governing UIX05
book population while omitting six actual judges (CAM03, CAM05, PRG01, PRG02, VIS05 and WLD06).
The plan now names all eleven governing items, removes the duplicate book route, and explicitly
shares compatible marker/art capture evidence. A different fresh reviewer-editor must judge it.

### Fresh independent reviewer-editor decision — 2026-08-09

**Material cost edit / awaiting-recriticism.** The received version has the complete eleven-item
bar, including S35/S38, the UIX03 and UIX06 independent comparisons and exhaustive model
CONSUMPTION, but its “freshly reproduced” instruction could force redundant browser captures and
fresh judgements even when current-HEAD evidence is unchanged. The executable plan now uses a
narrow source/fixture/consumer/configuration/pack diff as the reuse gate and reruns only stale,
changed, partial/red/unmeasured rows plus the necessary aggregate. The full native bar and blind
independence remain unchanged.
