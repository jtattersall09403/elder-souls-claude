# W1-10 current-state continuation plan

Plan-State: awaiting-recriticism

## Governing set and history

The authoritative decomposition is `docs/PLAN.md` §3, not the set accumulated by historical
builders: W1-10 owns its 13 listed paths and is judged by **RI-WPN01, RI-WPN02, RI-WPN03,
RI-WPN04 and RI-WPN06**. Read those five complete current items, their amendments, methods, blind
comparisons, scoring and hard fails. RI-WPN05 (`weapon.feel.*`) belongs to W1-11; WPN07 and the
four wave-2 paths recorded in `docs/PLAN.md` §5b must not silently expand this build. Historical W1-10 evidence is reusable where it proves an applicable row and its recorded commit is current HEAD, or a narrow source/fixture/consumer/configuration diff proves that row unaffected; rerun stale, changed, red, partial or unmeasured rows only.

Proven if reproduced: 87 generated movesets reach the live hitbox solver; model perturbations alter
combat; class separation recovered; visible blade height/reach improved and drawn tip matches
socket. Current governing red/unclosed: renderer identity/animation differentiation, socket
teleports and any WPN01–04 motion-integrity failures, exact trace fields/events needed by WPN01
M2/M6, residual threat/reach mismatch, and complete contextual/offhand native gates.

## Acceptance matrix

| Item | Complete measurable acceptance |
|---|---|
| **WPN01 slot contract** | Every equipped shipped weapon (87) exposes every required slot for its class, exact frame windows/roots/reach/hurtbox/chain rules, no aliases forbidden by item. Drive live traces for all weapon×slot cells; declared vs observed exact, including `anim_slot`, hitstop and tip. All M1–M6 thresholds/hard fails in the item must pass; unavailable trace fields make item red/unmeasurable, not inferred from JSON. |
| **WPN02 class differentiation** | All 15 classes × native spine slots over declared and live traces. Recompute every item metric (timing, reach, arc, motion, poise, stamina and class separations) in native units and exact bands; blind class comparison uses rendered clips. No class collapse, sharing or hard fail. |
| **WPN03 within-class subtlety** | Full 87 roster: ARI 0.42–0.60; no VEC collision; SHARE≤4 and same-class only; UNQ≥1 each and mean UNQ≥2.0; `CLIPS(k)/N(k)≥1.6`; DEV≥4 for every non-baseline weapon; forged-unique ≤10%, same-id divergence zero; distinct normalised path shapes at 10% ≥`0.20×C`; F87 `W_min≥0.20`, `W_med∈[0.35,1.00]`, `SEP≥1.4`; chain grammar `Chg_min≥0.15` within every class and `Chg_med∈[0.30,1.10]`; ≥85% DEV_id≥1; 1–2 signatures/class. Apply the native ARI/VEC/SHARE/cross-class/consumer hard fails before score. Fresh blind grouping names within-group differences; builder does not judge. |
| **WPN04 contextual attacks** | Every class's roll/run/backstep/jump cells exact to amended 60-Hz table. `CFS=1.00`; buffers exactly 8 f@60, no multi-fire, insufficient stamina drops input; equip-load tiers exact, overloaded none, heavy≤32 f; no rising jump, plunge height/target gate and root landing; every slot superior on its own metric in ≥70% trials; observed=declared 100%. |
| **WPN06 two-hand/offhand** | Full class/config census: median TDV≥0.60, ≥2 exclusive slots/class, ≥4 chain-length changes; each configuration pair differs ≥4 verbs and O2/O3 have no block/counter; stance exactly 36 f@60, root-locked/uncancellable, 8-f buffer, legal/illegal states exact; all bearings×shield classes exact; exact parry set/windows; shield frames/bash MV≤0.40/charge greatshield-only; dual chain correct, alternating hands, independent clips. |
| **Rendered geometry cross-gate** | Run the exact rendered/live geometry predicates and native populations required by WPN01–04, including finite/drawn identity, clip/path integrity and the native reach/threat ≤0.10 m comparison. Preserve the 1 mm socket diagnostic and underground/teleport probes as targeted repair diagnostics where a stale/red row needs them, not as a new 87×every-frame prerequisite. Do not use r4's 91.8% as a new bar. |

All five governing native scores must independently reach their meets-bar band and overall
`min ≥7`; every governing hard fail must be false. Generated files come only from
`node tools/weapons/build-movesets.mjs`; `--check` has zero drift. WPN05 evidence may be reused by
W1-11, but is not a W1-10 prerequisite and must not trigger an impact/audio/material rebuild here.

## Instruments, falsification and evidence

* Extend `tools/weapons/measure.mjs`, canonical live trace/browser launcher and blade tools; do not score authored declarations as observation. Capture source execution counters and world entity deltas for every model perturbation.
* Build missing trace fields/events at the established harness seam before scoring. Null one field/event: corresponding row must become unmeasurable, never use a fallback calculator.
* Red arms: collapse all classes to one clip; alias four weapons; disconnect the moveset consumer;
  restore the rigid 0.95 m rendered blade; restore the r3 underground arm line; inject a socket
  teleport; exceed a parry window by 1 frame. Do not run WPN05-only material/impact controls here.
* For each repair run delete-the-fix from a shadow tree. Geometry controls are factorial: pose consumer × blade-length/body solve, because two guards may mask one defect.
* Keep the two governing blind questions distinct: WPN02 rendered class comparison and WPN03
  within-class grouping. Use opaque names, sealed reveals and fresh independent judges; report each
  `not_run` until its judgement is recorded. WPN05 impact packs belong to W1-11 and are not rerun.
* Run cheap schema/build-drift/population gates first; stop before browser/blind work on an unsatisfiable hard fail. Reuse commit-matched/diff-validated raw traces and independent blind judgements, collect only missing/affected live cells in one browser session, then run the minimum full aggregation needed to expose interactions. Aggregate all WPN tools together at full commit, plus `node tools/boot-check.mjs`, `node tools/check-data.mjs`, `node tools/check-content.mjs`. Artifacts include 60-Hz frames, metres, m/s, degrees, populations, per-cell raw trace, screenshots/clips, exact item score math and commit.

A future builder cannot satisfy this plan using the excellent data model while leaving the
renderer, combat consumer, contextual attacks or offhand matrix disconnected: every governing cell
is compared declared-to-live-to-rendered and missing populations fail closed. Conversely, the
builder need not rebuild W1-11's impact layer to close W1-10.

---

## Independent plan critique — 2026-08-09

**Verdict: BLOCKING / awaiting-remediation.** WPN05's acceptance omits its separately weighted
**M5b per-clip motion-integrity** population. The plan measures class-level ES-MASS curves and adds
an active-frame geometry cross-gate, but M5b applies §E.2 to **every clip in the game**, accepts at
most 2% violating any row, hard-fails any clip with displacement >1.0 m, and hard-fails more than
5% of clips with fewer than three distinct keyframes. Class baselines and active frames can all be
green while recovery/anticipation or non-baseline clips remain two-pose lerps, so the planned
aggregate can score a result that the governing item would not.

**Required remediation:** add WPN05 M5b as its own exhaustive per-clip gate with its 6-point weight,
complete §E.2 predicates, ≤2% acceptance denominator and both native hard fails. Add M7 presentation
discipline and five-seed M8 determinism explicitly to the acceptance matrix rather than relying on
“native gates,” and name the WPN05 blind impact-sequence comparison separately from the generic
clip packs. Preserve the stricter rendered-geometry diagnostics as additional controls; they do
not substitute for M5b.

**Recommendation:** remediate WPN05's missing native rows, then request fresh independent criticism.

### Remediation outcome — 2026-08-09

The recorded WPN05 omission is repaired without replacing the stricter geometry diagnostics: M5b
is an exhaustive separately weighted clip population with every §E.2 predicate, denominator and
hard fail; M7 and five-seed M8 are explicit; and the native impact-sequence identification and
paired blind comparisons are separated from the other weapon blind packs. Material edit: this
version is **awaiting-recriticism**, never self-approved.

### Fresh reviewer-editor outcome — 2026-08-09

**Material edit / awaiting-recriticism.** Independent reconstruction from `docs/PLAN.md` found
that the received plan had adopted historical over-declaration as authority: it made RI-WPN05 and
its impact/material/blind populations mandatory even though W1-11 owns those paths and W1-10's
canonical judges are RI-WPN01/02/03/04/06. The plan now preserves that five-item bar while removing
the duplicate build and evidence route. A different fresh reviewer-editor must judge this version.


### Fresh reviewer-editor outcome — 2026-08-09 (current round)

**Material edit / awaiting-recriticism.** The five-item governing set remains correct, but the received WPN03 acceptance could green while omitting the native mean-UNQ, clips-per-class, non-baseline DEV, normalised-path and chain-grammar populations. Those exact denominators and thresholds are now binding above. The cost route is otherwise retained: WPN05 work remains outside this piece, existing instruments/evidence are reused when dependency-valid, and blind populations remain independent. A different fresh reviewer-editor must judge this edited version.

### Fresh independent reviewer-editor decision — 2026-08-09 (cost correction)

**Material deliverability edit / awaiting-recriticism.** The five-item owned-path bar and repaired WPN03 populations are sound, but the received route still made a non-native 1 mm 87-weapons × every-active-frame diagnostic a mandatory cross-gate and did not admit narrow diff validation of still-valid evidence. The plan now keeps that diagnostic for affected geometry repairs, binds acceptance to the exact native WPN01–04 geometry populations/thresholds, reuses dependency-valid evidence and independent judgements, and orders cheap hard stops before one bounded browser/blind route. No governing weapon predicate, native threshold, population, hard fail, blind independence, delete-fix or CONSUMPTION gate was weakened.
