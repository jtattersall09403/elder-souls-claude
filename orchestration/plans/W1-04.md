# W1-04 current-state continuation plan — settlements, interiors, and people

Plan-State: awaiting-remediation

## Authority, scope, and HEAD reconciliation

This is a continuation plan, not a restart and not a declaration that round 6 passed. The governing bar is the complete set assigned by `orchestration/status/W1-04.json`, with the most direct predicates in `RI-WLD03`, `RI-WLD08`, `RI-WLD13`, and universal `RI-MTH07`; the coarse-path items named in that status remain regression obligations. Canonical lineage is the original status and survey; verdicts `W1-04-r1` through `r5`; builder statuses `W1-04-r2` through `r6`; their artifacts/tools; and the gap ledger. At current HEAD there is **no r6 critic verdict**: `critic-w1-04-r6.json` is merely `running`. Therefore r6 builder claims are hypotheses requiring fresh independent confirmation, not preserved independent evidence.

Historical reconciliation:

| Round | Independent conclusion that still governs | Current interpretation |
|---|---|---|
| initial/r1 | data existed but settlement/interior/schedule models were largely unconsumed; doors wrote a mirror position | landed consumers and `_placeBody` supersede the old implementation state; preserve the critic-established failure as a required delete-fix baseline |
| r2 | interiors became drawable but settlements had no exteriors | later critic evidence established exterior meshes; do not rebuild them |
| r3 | exteriors/consumption improved; 41/112 enterable exteriors were smaller than their rooms and collision control was inert | r4 independently closed footprint mismatch; never reuse the inert `_townCell` null control |
| r4 | roofs/joins held, but live exits placed 39/40 bodies within closed buildings | r5 changed the join; this is an historical red baseline, not current truth |
| r5 | FAIL 4.8: at 1/30/120/600 frames inside counts were 0/8/10/10; re-entry 96 correct/10 wrong/9 none; 61 props overhung; three orphans were skipped; M13 remained unrun | r6 claims 0/0/0/0, 112/1/2, zero overhang, and explicit orphan enumeration. None has independent r6 judgement, so all require fresh evidence |
| r6 builder | collision-fixed doorsteps, single derivation, 115-record census, seven-arm delete-fix, 23/23 coupling | preserve code unless a fresh test falsifies it; do not cite its self-authored numbers as acceptance |

## Governing-predicate coverage map

| Governing predicate | Preserved independent evidence | Fresh evidence required at current commit | Remaining build/remediation if red |
|---|---|---|---|
| WLD03 anatomy: each of 8 settlements has legible functional plan, paths/streets, landmarks, services, outskirts and non-template identity; M1–M14 including **M13 blind settlement-street comparison** | r3/r4 critics established 202 exterior building meshes, distinct towns and corrected joins | rerun complete WLD03 method, not only mesh census; independent blind M13 using actual VP04 street captures, leak-gated and judged by an agent who neither built nor packed them | repair only failed anatomy axis; likely authored street composition/legibility rather than more census fields |
| WLD08 living world: population density and schedules create visible daily change rather than static records | original consumption critic established schedule defect; later tools report 260+ scheduled NPCs and different cells by time | census all shipped NPCs and observe same identities at multiple times (03:00/09:00/12:00/20:00), population denominator explicit, motion allowed to settle; prove schedules change entity positions/cells and persist after save/load | repair missing/invalid schedules or world-side schedule movement only where census fails |
| WLD13 N1 containment | r4 critic independently established footprint reconciliation at its commit | all 115 interiors enumerated; every enterable exterior footprint contains declared interior bounds and every placed prop/light satisfies the item’s exact containment predicate; separately report orphans | data repair, never renderer shrink-to-fit, for every failing record |
| WLD13 N2 door/spawn continuity | r5 independent red: 0/8/10/10 inside at 1/30/120/600; 96/10/9 re-entry | live all-115 sweep at all four times; real `interact` latch; same-room identity; exit/entry displacement and orientation per item; explicitly include thorn-house-0, barge-hold, writ-house | author reachable exterior membership/door-table entries for the three orphans; resolve any collision slide or nearest-door alias |
| WLD13 lighting continuity and N4 pane/light attenuation | r5 critic independently established lamp consumption on an entity; r6 did not remeasure it | compare matched sides of every entrance and representative panes using item units/ratios; perturb record lights and observe both rendered light and entity detection | unify any remaining special cells (`barge-hold`, `writ-house`) with the canonical interior-light producer |
| data integrity: settlement/interior/NPC/property/locks/factions/persistence references resolve | prior checks and critic verdicts independently established broad integrity at older commits | current `check-data`, `check-content`, `check-quests`, building-fit self-break, plus explicit 115-interior/8-settlement/whole-NPC denominators | repair dangling records or checks; no constructor assertion before data exists |
| RI-MTH07 for **every** W1-04 model (settlement membership, door table, interior placement, schedule, ownership, locks, hours, faction access, persistence, exterior geometry, doorstep, props, lights) | earlier critics established several consumers and exposed mirror-position/inert-control failures | exhaustive harness-surface enumeration; per model name shipping call site/frame/entity, perturb two separated values, entity-side observable, null, numeric coupling; no skipped rows; partial harness VERIFY longer than longest cited run | wire missing producer/consumer, or score relevant dimension zero; never hand-feed world facts as proof |
| coarse assigned-item regressions (`RI-AI07`, `CAM05`, `CHR02`, `CRM01`, `DLG02/03`, `LOR01/02/04/06`, `PRG03`, `QST03/07/08`, `STL02`, `TRV01`, `WLD07`) | status documents mappings and prior consumers | targeted seam matrix: population encounter use, camera/interior transition, faction/property/crime, settlement topic supply, quest hubs, locks, travel anchors, vertical interiors, persistence; run each governing item’s hard-fail checks where W1-04 data is in its denominator | fix W1-04-owned data/consumer only; record cross-piece red with owner but do not waive acceptance |

## Sequential build/verification plan

### 1. Freeze lineage and enumerate the canonical population

At the execution commit, write a machine-readable manifest containing commit hash, 8 settlement ids, all settlement `buildings`, all 115 interior ids (including orphans), every NPC record and schedule, property zones, locks, faction bindings, and every W1-04 harness computed method. Reuse `tools/world/w1-04-r6-census.mjs`, `tools/check-building-fits-room.mjs`, `tools/world/w1-04-consumption.mjs`, and the r5 critic tools; extend rather than fork. Fail if a denominator silently changes or any record is skipped.

### 2. Independently falsify round 6 before changing anything

Run the shipped arm, exact r5-behaviour arm, full-derivation deletion, lamp deletion, prop-inset deletion, and combined 2×2 arms. A teardown must be observed red. Confirm code execution with counters at the actual `leaveInterior → placeBody → world collision` and renderer consumers. If shipped is green and each cut restores its historical defect, preserve implementation unchanged.

### 3. Close the three orphan door journeys

If the fresh all-record test reproduces r6's 112/1/2, that is not acceptable: add the minimum canonical settlement/door membership needed for thorn-house-0, barge-hold, and writ-house so all 115 have a unique exterior latch and correct return. Do not exclude Tidewrack, invent a reduced denominator, or count “explicitly unreachable” as continuity.

### 4. Establish the full reference bar, not only round 6's gap

Run WLD03 M1–M14 (including fresh blind M13), WLD08 time-series, WLD13 N1–N4, all assigned seam checks, and RI-MTH07 aggregation. Captures must be actual running-game artifacts, commit-stamped; pack/reveal separation and leakcheck precede a fresh judge. The builder stops at `not_run` for blind judgement.

## Acceptance contract

Every row is mandatory; a hard failure fails the continuation regardless of averages.

| ID | Predicate; units | Threshold and denominator/population | Comparison method | Hard failures | Null/red/delete-fix controls | Existing instrument to reuse | Required output evidence |
|---|---|---|---|---|---|---|---|
| A1 | interior/exterior continuity; count and rate of correct journeys | **115/115** exit outside at frames 1,30,120,600; **115/115** real-interact re-entry to same id; denominator is every interior record, including 3 historical orphans | one browser, reset per record, shipping door path, fixed camera/body, report identity and `buildingAt` each sample | any skip, wrong/none door, inside footprint/wall at any time, page/step error | r5 doorstep arm must restore historical failures; full derivation cut must go red; inert control if consumer counter/geometry unchanged | `w1-04-r6-live`, `r6-census`, `critic-w1-04-r5-live/offline` | commit-stamped JSON rows for 115×4, summary, trace, screenshot(s) of representative pass and red arm |
| A2 | containment; metres of exterior shortfall and prop/light overhang | **0.00 m positive shortfall/overhang** for every enterable building and every placed prop/light; all 115 enumerated, orphans separately zero by A1 after closure | exact WLD13 N1 footprint math; scene record vs declared bounds, no pixel proxy | any record skipped; any positive epsilon beyond documented numeric tolerance; renderer-only scaling | perturb one bound +1 m and one prop outward ≥1 m: check must fail and name each; remove inset reproduces known 61 if lineage still applicable | `check-building-fits-room --self-break`, `w1-04-r6-census` | full per-record CSV/JSON, self-break logs and exit codes |
| A3 | living schedules; entity count/percentage changing location and valid occupancy | **100%** of scheduled NPC records resolve valid slots; item’s WLD08 density/schedule bands met over whole NPC population; report counts at 03/09/12/20 and after ≥600 frames | same-identity longitudinal observation, not respawned snapshots; save/load running-world audit | invalid slot/interior, teleport-only hand-feed, static world while data differs, schedule loss after load | remove one schedule consumer: entity must cease its planned change while control identity remains live | `w1-04-consumption`, population harness verbs | commit, manifest, entity traces, aggregate and delete-fix artifact |
| A4 | WLD03 settlement anatomy and identity; method-specific counts/ratios plus blind preference | every numeric M1–M12/M14 threshold in current RI-WLD03; M13 judged on **all 8 settlement VP04 street frames** against valid reference design and reaches item pass band | exact reference methods; blind randomized pack with image artifacts, leak gate, fresh judge | missing method; non-street frame; builder judges own pack; any WLD03 hard fail | cut exterior draw → zero buildings and reference failure; remove five buildings → count −5; shuffled reveal | WLD03 tools, `w1-04-r3-exterior`, capture tooling, blind leakcheck | metric ledger, pack, key held separately, gate output, fresh judgement, 8 commit-stamped captures |
| A5 | WLD13 light/pane continuity; metres, lux/light scalar, ratios stated by item | every N2–N4 threshold over all applicable entrances/panes; denominator and exclusions printed | paired outside/inside observations plus entity detection response | drawn-only light, detector-only light, special-cell bypass, missing denominator | delete record lights and alter pane coefficient: rendered and entity-side measures must move predicted direction; beyond-radius null unchanged | r5 lamp critic, W1-15 lighting tools where shared | per-entrance/pane artifact and coupling tuples |
| A6 | world coupling | every required model enumerated; **coupling >0** with clean null (pass floor), target `[0.95,1.05]`; denominator = every model, zero skipped | RI-MTH07 §B–D and aggregation | any 0, trace-only reader, hand-feed-only path, missing/short VERIFY | each consumer cut goes red; null predicts no entity change; delete fix restores old behavior | `w1-04-consumption --self-test`, r6 deletefix | `world_coupling` array with model, call site/frame/entity, values, prediction, observations, coupling, evidence |
| A7 | repository and seam aggregation | all named checks exit 0 together; all assigned coarse-item hard fails clear for W1-04 records | run aggregation after individual probes | check not run, commit mismatch, regression masked by unrelated population | inject dangling interior/NPC/topic and confirm relevant check nonzero; restore and rerun aggregate | existing repository checks and item tools | commands, exits, commit, complete logs |

## Remediation: mandatory native-method matrix

This matrix is the execution contract. It supersedes narrower populations in A1–A5 wherever they conflict; no row may be inferred from another. Run cheap static rows before browser/video rows, stop on the first hard fail, repair only that failing seam, then rerun the complete matrix once at one commit.

| Reference method | Native population and acceptance (all mandatory) | Bite-capable control and evidence |
|---|---|---|
| WLD03 M12 | All 8 shipped settlements (the item's “all 24” instruction is reconciled to the shipped named-settlement population): enumerate every in-radius mesh; classify `interior`/`sealed-with-reason`/`structure`; compute enterable %, named interiors/NPCs, barter/trainers/quest-givers/inns/bonfires/halls. Each metric is within −10% of its tier target; hard fail at any metric below −40%, enterable <50%, <5 named interiors, zero named NPCs, or residential props inside a `structure`. | Reclassify one residential building as `structure` and remove enough records to cross one tier threshold; audit exits nonzero and names both. Row ledger + manifest. |
| WLD03 M13 | Exactly 8 label/icon/UI-free 512 px orthographic plans; fresh judge answers the native power/production question, ≥6/8 correct and never ≤3/8. Rank the same 8 with the named 8 Morrowind plans (16 images); our median must not be in the bottom half. | Leakcheck rejects an injected label; shuffled reveal cannot be consulted before answers. Pack, sealed key, answers/ranks; builder records `not_run` and hands off. |
| WLD03 M14 | Mesh-id sets for all 8: no pair shares >30%; ≥8 bespoke meshes and ≥1 unique silhouette element per settlement. | Duplicate one kit into another; overlap gate fails. Full pair matrix. |
| WLD03 M15 | Seeded, predeclared 20 named interiors across tiers: each reports ≥12 distinct prop meshes, ≥2 containers, ≥1 named hand-placed item, ≥2 lights, ≥1 readable/dialogue actor, ≥18 m²; fail if >4/20 miss at least two (≤2 flags required for native score 8). | Remove two criterion classes from a sampled copy; gate identifies the interior. Seed/list fixed before results. |
| WLD03 M16 | 8 settlements × every §4 service; shipping road graph walk-time ≤25 min for every cell. | Remove one service/road edge; matrix goes red at the affected origin/service. |
| WLD08 M40 | Fixed 10 points (4 settlement/4 road/2 wilderness), static camera/no action, 10 real minutes each: ≥4 ambient events at ≥8/10, first event ≤150 s at ≥8/10; zero events anywhere is automatic fail. | Cut ambient-event producers (not logger) at one point and require zero/red; 10 commit-stamped videos/event traces. |
| WLD08 M41 | All named NPCs simulated through one 72-minute game day, hourly identity/position log; compare every §2 percentage, ≥4 waypoints, sleep/shop/weather-route fields; <50% scheduled in any settlement or any 24-hour shop fails. | Cut schedule consumer for one NPC/shop; observed entity/shop stops changing and audit fails. |
| WLD08 M42 | Native 30-minute wilderness walk plus 30-minute player-absent observer at 400 m/forced high LOD: ≥3 sightings/min, ≥2 predations, ≥6 functioning predator/prey pairs; empty absent arm/aggro-only behaviour fails. | Cut ecology step while retaining spawn/logging; absent arm goes empty/red; present/absent logs retained. |
| WLD08 M43 | Every region × 3 simulated hours: ≥4 reachable weather states, no transition probability 0 or 1; each worst state demonstrably changes its declared sightline/stamina/damage/disease/enemy/navigation mechanic. | Disable one worst-state consumer; numeric effect remains unchanged and row fails. Transition/effect matrix. |
| WLD08 M44 | Same 6 vantages at 00/06/12/18: sun/moon differ, noon–midnight ambient colour ΔE >25, ≥3/6 species and ≥4/6 NPC positions differ. | Freeze clock consumers; ΔE/positions go invariant and gate fails. Pixel samples plus entity census. |
| WLD08 M45 | 12 points × day/night, 60 s actual audio each: ≥3 positional emitters at ≥8/12 and spectral day/night difference at ≥10/12. | Mute one ambient/time consumer while capture remains live; affected thresholds fail. Audio artifacts, not feature-only substitutes. |
| WLD08 M46 | Six 60 s static-camera silent game clips interleaved with six Morrowind clips; fresh judge answers the native “world doing something without you?” ranking; our median not below Morrowind's. | Injected provenance tell fails leakcheck; sealed reveal shuffled. Builder does not judge; `not_run` until independent answers. |
| WLD13 M71 | **All 250 authored interiors required by the item**, joined both ways to exteriors: per-record N1 size, N2 bearing, N4 aperture, N5 storey/height. Zero orphans; zero N2 >5°; ≤2% breaches each other axis; worst ten reported. If HEAD has only 115, fail cheaply and author missing population before runtime work. | Add orphan; rotate door >5°; expand one room/aperture/storey—each mutation independently red. |
| WLD13 M72 | Seeded 60 interiors + all 8 loop entrances: real exterior `interact`, 60 frames, exit; position ≤1 m, bearing ≤5°, state diff empty except bounded clock, clock ≤frames×timescale; five cycles accumulate no drift. | Delete canonical placement derivation and require historical drift/red; seed fixed before measurement. |
| WLD13 M73 | Seeded 24 interiors × 4 times {6,12,18,1} × {clear,storm}, matched aperture captures: interior incident bearing within 10° exterior sun, no invariant aperture over times, storm changes at least one of 24. | Freeze time and sever weather-light consumer separately; each produces the relevant invariant/red. |
| WLD13 M74 | Every water-intersecting interior × all four tide states: inside/outside water delta ≤0.05 m; Lilmoth drowned-storey bands change and Ceyatatar-Zel drain changes the plane, not a one-shot. | Cut tide consumer; water becomes invariant/delta fails. Full population printed. |
| WLD13 M75 | Every settlement interior and every loop entrance: ≥32/160 seamless, 8/8 loops have no cell transition, ≥3 open-air loops, transition ≤30 frames/fade ≤12, exterior visible from every seamless doorway. | Force a transition/fade at one loop and hide one doorway exterior; trace/image gates red. |
| WLD13 M76 | Fixed street pose in each settlement; cast every building door/window: ≥50% settlement interiors see-into; every impostor palette Bhattacharyya distance ≤0.20. | Replace one impostor with black/foreign palette and close enough apertures to cross threshold; gate red. |
| WLD13 M77 | RI-WLD06 prose-navigation routes through 3 interiors versus the identical no-interior routes; success-rate drop ≤10 percentage points. | Rotate an interior exit beyond N2 and prove route success drops/red. |

All WLD13 automatic fails remain binding: N2 >5°, own-clock interior, independent water, loop loading screen, or orphan. MTH07 applies to each computed producer above; one deliberate producer/consumer cut per model plus a clean null is sufficient—do not duplicate evidence when one cut validly covers several rows.

## Adversarial self-attack

**Could a competent builder follow this plan, pass every acceptance, and still fail a governing item?** The first draft could: it could leave three orphans “reported,” skip WLD03 M13, validate schedules as data rather than observed people, and pass continuity without WLD13 lighting/pane rules or the coarse assigned seams. A1 now requires all 115 real journeys, A3 observes same entities over time, A4 requires the full method including independent blind judgement, A5 covers N2–N4, and A7 binds every assigned seam/hard fail. No historical score is promoted to current PASS; only independently established implementation is preserved pending current verification.

## Critic brief

Attack denominator completeness (especially the 3 orphans), actual reference-method fidelity, settlement-street pack validity, r6 control bite, special-cell lighting, and whether every computed surface has an entity-side consumer. Recommendation: **awaiting criticism**; no builder dispatch until a fresh plan critic finds no blocking omission.


## Independent plan critique — BLOCKING / awaiting-remediation (2026-08-09)

The falsification proposition does **not** hold. A competent builder can make A1–A7 green while
missing governing requirements because the contract substitutes narrow slices and an invalid method
range for the reference items themselves.

**Biggest actionable gap:** replace A3–A5 with acceptance rows that enumerate and execute every
native comparison method and hard fail in RI-WLD03 M12–M16, RI-WLD08 M40–M46, and RI-WLD13
M71–M77. In particular:

- A3 tests schedules only. RI-WLD08 also requires the 10-point diorama observation, wilderness
  ecology/predation with player-absent logging, every-region weather transitions and mechanical
  effects, day/night pixel/species/NPC-position comparisons, audio liveness, and the blind 60-second
  video comparison. None can be inferred from valid schedule slots or four time samples.
- A4 names nonexistent WLD03 “M1–M12/M14” coverage and therefore can go green without M15's
  20-interior quality sample or M16's all-settlement service-reachability matrix. It must explicitly
  cover M12, M13, M14, M15, and M16, including the residential-props-in-`structure` immediate fail,
  all native score-0 conditions, the exact 16-image rank comparison in M13, and the item-defined
  sample/populations. “Every numeric threshold” is not a substitute for methods with qualitative,
  reachability, and blind-comparison predicates.
- A2/A5 cover only selected WLD13 notions. They omit the prescribed M71 250-interior static audit,
  M72 seeded 60-plus-8 round trip, M73 24×4×3 light/weather matrix, M74 every water-intersecting
  interior at four tides, M75 every settlement/loop traversal, M76 casts against every building,
  and M77 navigation consequence. The plan must carry their native denominators, timings,
  comparisons, thresholds, and hard fails rather than replacing them with 115 door records and a
  generic pane/light row.

The repaired rows must also name bite-capable red controls for each newly restored instrument
(weather effect, ambient event, ecology, audio, blind pack, service reachability, tide/water,
see-into, and navigation), and aggregate them at one stamped commit. This is pre-build definitional
work, not a CARRIED empirical risk.


## Remediation outcome (2026-08-09)

All recorded blocking findings above are closed by the mandatory native-method matrix. The underlying item text and applicable amendments, not the critic wording, govern conflicts. No new defect catalogue was attempted. **Plan-State: awaiting-remediation**; only a fresh reviewer-editor may mark this plan satisfied.

Plan-State: awaiting-remediation

## Fresh reviewer-editor escalation — 2026-08-09

**AUTHORITY SEAM BLOCKED — awaiting remediation/arbitration.** Independent reconstruction found a
material contradiction that cannot safely be locally edited away. `docs/PLAN.md` says 250 interiors
is the **wave-3 floor** and wave 1 requires every settlement to exist; RI-WLD13 M71 and its automatic
orphan fail speak in the native 250-interior population. This plan instead requires all 250 to be
authored now, while its principal contract and current HEAD census use 115. The same plan expands
all of RI-WLD08 (ecology, weather, audio) although W1-04 owns NPC population/schedules, not the
neighbouring ecology/weather/audio paths, yet leaves most predicates from the other 17 authoritative
metadata judges behind a generic seam sentence. Thus it is simultaneously capable of under-covering
owned governing predicates and materially over-building neighbouring work.

Do not begin browser/blind work. The repository arbiter must rule (reversibly) the Wave-1 WLD13
population/score interpretation and the owned-path applicability rule for mixed judge predicates.
After that ruling, a remediator must replace the three full-item matrices with a fail-closed ledger
over all 20 metadata judges: every native row whose population intersects W1-04's nine owned paths
is mandatory at the Wave-1 governing gate; wholly external rows are cited to their owner and not
rerun; mixed rows remain mandatory. Preserve the existing current-HEAD lineage, 115-record tools,
independent evidence and cheap-first stopping order until the ruling says otherwise.
