# W1-04 current-state continuation plan — settlements, interiors, and people

Plan-State: satisfied

## Resolved authority and ownership

ARBITRATION S42 controls this continuation. W1-04 owns exactly the nine paths in `docs/PLAN.md` §3 and is judged by its 20 listed items. Applicability is row-level: execute every native row whose population or producer/consumer predicate reads an owned path; execute the owned portion of mixed rows and consume valid sibling evidence for the rest; cite wholly sibling-owned rows without rerunning them. No W1-04-owned failure may be waived as “owned elsewhere.”

RI-WLD13's Wave-1 denominator is the complete shipped-interior manifest at the execution commit (currently 115), not the wave-3 target of 250. All records, including `thorn-house-0`, `barge-hold`, and `writ-house`, participate; every orphan is a hard failure. M71 uses all shipped records; M72 deterministically samples 60 from that manifest; M73 samples 24; M74 uses all water-intersecting records; M75/M76 use all shipped settlement interiors. Apply proportional S1 ≥20% and S2 ≥50%; fixed 250/160/8-loop/32 counts are future or sibling obligations unless the corresponding records are shipped in an owned seam.

## Cheap-first continuation

1. Freeze commit, the eight settlements, all shipped interiors, NPCs/schedules, property/lock/faction/persistence rows, and a 20-judge applicability ledger. Each native method row names the owned path it reads, population, sibling owner/evidence if any, and hard fail. A missing classification fails closed.
2. Run static integrity and RI-WLD13 M71 over the entire manifest. Repair every orphan or owned-row breach; never shrink the denominator. Then run schedule and RI-MTH07 producer/consumer perturbations for every owned model.
3. Run M72–M76 only on S42 populations, reusing the existing r5/r6 instruments and independent artifacts. The exit samples remain observed at frames 1/30/120/600 as an additional regression guard; the native M72 60-frame/five-cycle contract remains controlling.
4. Execute only applicable rows from the other 19 judges. Consume commit-matched sibling evidence rather than rebuilding dialogue, quests, ecology, weather/audio, travel, combat encounters, or loop-dungeon content. If shared evidence is incomplete, report the row `not_run`; do not infer pass.
5. Capture/pack blind WLD03 evidence only after cheap gates pass. The builder records blind rows `not_run` and hands the pack to a fresh judge.

## Acceptance and controls

- **Population/continuity:** manifest cardinality equals the data census; zero skipped/orphan records. M71 native tolerances and hard fails apply to all shipped interiors. M72/M73/M74/M75/M76 use exactly S42's populations. Door journeys use real `interact`, same identity, native displacement/bearing/state limits, plus 1/30/120/600 collision-settle observations.
- **Owned living-world models:** every NPC/schedule/property/lock/faction/persistence/settlement/interior model has a named running-world consumer and RI-MTH07 perturbation with coupling >0 and a clean null. Any zero, hand-feed-only path, or save/load loss is a hard failure.
- **Mixed judges:** an owned-row failure fails W1-04 even if a sibling also participates. Sibling evidence is accepted only when commit-stamped, independently produced, and population-complete for the consumed cells. Wholly external rows cause no W1-04 build work.
- **Red controls:** inject an orphan; perturb a bound/aperture/bearing; cut door placement, schedule, and persistence consumers individually; confirm each applicable gate turns red. Reuse the historical r5 doorstep arm and ensure it is not the inert `_townCell` control.
- **Aggregation:** run all applicable native hard-fail gates together at one commit after individual repair. No historical verdict or r6 builder claim is promoted to current acceptance.

## Result and next action

**Fresh independent reviewer-editor outcome (2026-08-09): SATISFIED.** S42 is applied consistently: the shipped manifest is the complete Wave-1 denominator, applicability is row-level without waiving owned failures or duplicating wholly sibling-owned work, and every relevant population and hard fail retains native force. The full two-gate review found no material bar-sufficiency or cost-effective-deliverability defect and made no material plan edit.

**Exact next owner action:** dispatch a fresh W1-04 builder/remediator against this satisfied plan and current build evidence; run cheap manifest/applicability/consumer gates before any browser or blind work.
