# PLAN — W1-06: the camera

Plan-State: awaiting-recriticism

## Builder/critic execution allocation (binding)

This plan incorporates [`BUILDER-EXECUTION-CONTRACT.md`](BUILDER-EXECUTION-CONTRACT.md).

**Builder-owned:** finish the camera collision geometry/cell corpus, framing, target-height,
input-response and transition repairs; run deterministic cell generation and static checks; test
every camera mode and known problem cell materially changed by the builder, mouse/stick extrema,
lock/death/dialogue transitions, moving targets and settled/late frames; inspect a small real
front/back and combat/exploration rendered sample; prove each changed mechanism with a targeted red
and delete arm; and run the bounded builder aggregate.

**Critic-owned:** the complete Cartesian population of modes, target heights/distances,
trajectories, geometries, input values and render rates; long traces; complete masks/capture packs;
all fresh/blind visual and decision comparisons; comprehensive controls; native scoring and verdict.

The full C1–C7/native-item contract below remains mandatory at criticism. Builder completion uses
the representative production-path population above and never self-scores an independent row.

Planning-only current-state continuation at HEAD `50821cb2`. W1-06 is an Elevated piece. Governing items: RI-AI06, RI-CAM01/02/03/05/06/07, RI-CMB06, RI-MTH01 and RI-VIS08; S18's eighteen clauses and S25 are binding.

## 1. Reconstructed state

Three build rounds established a live third-person/lock rig, death release, stick input reachability, size-dependent framing and consumption controls. The pitch pin and dead `archetype_id` height lookup were fixed. Existing camera tools cover probe, pitch, consumption, death-in-lock, projectPoint and item methods. Three amendments matter: AMENDMENT-01 is resolved by S48 at a 1.80 m crawl ceiling; the arm-floor/zero-clip AMENDMENT-02 is resolved by S49's narrowly proved penetration-guard exception; the separately named projectPoint AMENDMENT-02 records the posed-camera correction. Stale assumptions: bare-node height evidence is not live-world evidence; old clip totals predate fixes; an old method claimed M1 unrunnable although `look_stick` is now reachable. Open history names 2,536 clip-through frames outside the crawl case, renderer blindness to enemy height (cross-piece), unrun bloom sight arms, and incomplete native-method coverage.

Dependencies: W1-03 supplies water depth/body silhouette; W1-09/10 supply lock movement and actor rig; W1-13 death; W1-21 dialogue/UI. Do not use camera data as proof those systems satisfy their own bars.

## 2. Requirement-by-requirement disposition

| requirement | established evidence | current-HEAD re-verification | remaining work criterion |
|---|---|---|---|
| RI-CAM01 | rig geometry and collision functions exist; crawl contradiction filed | run every §A–D native fixture including cistern/stair/boardwalk and exact `clip_through` definition | eliminate every clip frame; enforce S48's crawl dimensions and S49's independently proved below-floor necessity/maximality contract |
| RI-CAM02 | `look_stick` reaches shaping; rate perturbation 180→45 proved consumption | run M1–M7 native tool over exact deflections/time units | build only failed control/response/recentre rows |
| RI-CAM03 | pitch law, size lookup and lock framing partly proved | live entities at every specified height/range; all SD/SE predicates and UI containment | repair only current misses; no bare-object proxy |
| RI-CAM05 | world fixtures exist; S48 resolves crawl height and S49 resolves unavoidable below-floor containment | run every native world-camera row, including exact S48 geometry and S49 emergency audit | close every zero-clip, exposure-band or emergency-proof failure under the resolved corpus |
| RI-CAM06 | basis/handedness and shake paths partly exercised | run every native metric including lock, hit, death and effect cases | only failing rows |
| RI-CAM07 | no complete current verdict | run prescribed reference/camera presentation comparison | build exact missing clauses |
| RI-AI06 | lock target behaviour overlaps camera | run AI06 native target/containment cases with moving enemies | camera-owned failures only |
| RI-CMB06 | camera-relative/target-relative frame is owned here; animation catalogue is not | run M4/M6 camera clauses and record dependency rows separately | fix frame/containment only |
| RI-MTH01 | deterministic measurement required | repeat identical camera traces at item sample count/tolerance | remove nondeterminism until exact bar |
| RI-VIS08 | character presentation includes camera view | perform item-native paired/reference comparison, not numerical proxy | remediate only failed visual clauses |
| S18 (18 clauses) / S25 | third-person is present and waterline view exists | make an 18-row S18 ledger and exercise each; run S25 silhouette/water cases at W0–W5 | zero missing clause; preserve body-readable water depth |

## 3. Acceptance contract

Population is the Cartesian fixture set explicitly named by the items: every camera mode; unlocked/locked/death/dialogue; all native target heights and distances; moving and still targets; flat, wall, corner, stair, cistern, boardwalk and crawl geometry; mouse and stick extrema; required frame rates. Units remain degrees, degrees/s, metres, pixels and frames at 60 Hz as each item defines.

* **C1 native completeness:** every row of all ten items and every S18 clause has observed, threshold, unit, fixture, method and result; 100% run, no proxy or silent N/A.
* **C2 collision:** `clip_through == 0` on every geometrically valid RI-CAM01/05 fixture at every prescribed pitch/yaw trajectory. S48's crawl geometry and S49's normal-floor/emergency distinction are binding; every emergency frame requires independent necessity/maximality proof and the zero-clip threshold is unchanged.
* **C3 lock containment:** for every specified height/range and moving trajectory, the target bounds/aim point remain inside the exact RI-CAM03 containment band; acquisition/switch/release/death timings equal item frames.
* **C4 controls:** RI-CAM02 deadzone, curve, saturation, yaw/pitch rate and recenter predicates meet their exact item bands over full input sweep.
* **C5 coupling:** perturb rig arm, rate, target height, obstacle and lock target independently; the running world's camera/entity framing changes in the predicted dimension and unrelated dimensions remain invariant.
* **C6 visual:** RI-CAM07/VIS08 item-native paired comparisons meet their own rubric/threshold; a blind visual requirement is never replaced by `projectPoint` numbers.
* **C7 aggregate:** native item aggregation leaves every governing item ≥7.0 and triggers zero hard fails.

**Independent-method actor contract (part of C6/C7):** the builder may run mechanical metrics and
produce commit-stamped complete, tell-audited source packs, but may not score any row below. A
separately dispatched eligible critic/judge administers and records each native protocol before C7
can be green:

| governing row | independent actor and native evidence |
|---|---|
| RI-AI06 M4 | blind critic predicts 20 decision-frame clips, then after the 20-minute trace/transition graph predicts 20 **fresh** clips; accept only native ≤35% naive and ≥60% post-study |
| RI-CAM01 blind pair | critic receives two unlabelled same-route `arm_len_m` series, records the fast-in/slow-out versus spring pick before reveal; picking ours triggers the doctrine's harsher rerun |
| RI-CAM02 blind pair | critic receives the two unlabelled 180°-reversal angle series, records the mass pick before reveal |
| RI-CAM05 M1 blind | critic receives ten matched HUD-off 1920×1080 shots (5 combat/5 exploration), with the discriminating question fixed first; sorting better than chance fails |
| RI-CMB06 blind pair | critic receives two unlabelled 64-row M4 result tables and records which permits trustworthy thumb control before reveal |
| RI-VIS08 D1 / blind pass | fresh context-only judge sees only the 40 px masks for D1; separately apply RI-VIS06 Protocol A to the `character_closeup`/RI-VIS02 REF-M6 pair when the reference is usable, otherwise record the native skip rather than a pass |
| RI-CAM07 M6 | critic first records the required disjoint bifurcation; F2–F5 are judged blind under RI-VIS06 against RI-VIS05 only, with no modern reference in the art set; fidelity and art scores remain separate and their minimum governs |

The critic controls pack assignment/reveal and applies every native calibration, harsher-rerun or
void rule. Any builder-administered, context-exposed, leaked, wrong-reference or self-scored result
is inadmissible and leaves its row `not_run`; it is not evidence awaiting review.

Hard fails include any item hard fail, any unresolved unchanged-bar crawl failure reported green, clipping in a valid fixture, target loss hidden by a still target, renderer/projector disagreement, model without world consumption, or unmeasured row.

Controls/evidence: inject wrong handedness, quarter rate, constant target height, disable obstacle shortening, pin pitch, and delete each fix in scratch arms. Each targeted check must go red and arms must differ. Run long enough for settled and late frames. Publish commit-stamped raw per-frame traces, fixture geometry, screenshots/reference pack where item-native, and aggregate reports. Reuse `m-cam02-control`, `m-cam04-locked-movement`, `m-cam05-world-camera`, `cam-probe`, `cam-pitch-instrument`, `cam-consume`, `cam-death-in-lock`, and projectPoint tools; extend them rather than inventing easier probes.

## 4. Sequential build dispatch

1. Apply S48 and corrected S49 as written and enumerate exact native rows/S18 clauses. Repair the
   M4/M5 **instrument fixture only** so its wall coordinates denote player-facing surface distance:
   for the current 0.40 m half-depth use M4 centres −6.40/−0.90 m and M5 centre −3.40 m, and add a
   body-collision/pivot-side assertion to M5. Do not change production camera code for this repair.
2. Make every native method runnable and red-seed it before feature work.
3. Re-run current HEAD; classify closure-only versus remediation.
4. Fix valid collision/containment/control/coupling failures; route cross-piece actor-rig work to owner.
5. Run builder-admissible metrics and delete-fix controls once at one commit; assemble only the
   complete source artifacts and leak/eligibility audit for the independent-method table.
6. Dispatch eligible independent critics/judges to administer those native rows, including their
   pre-reveal answers, calibration/rerun/void controls and reference bifurcation. Incorporate their
   results, then run the full native aggregate; any missing independent row remains `not_run`.

**Escape audit:** Could all planned checks be green while a governing bar fails? **No.** C1/C7 quantify every native requirement and hard fail, C6 preserves visual-native judgement, and the resolved S48/S49 amendments cannot be coloured green without their geometry and independent emergency predicates.

## Plan critique — 2026-08-09

**Verdict: BLOCKING / awaiting-remediation.** C6 preserves the *kind* of comparison but not its
required independence. Step 5 directs the builder to “run visual comparisons,” and step 6 merely
submits the resulting evidence to an independent critic. That permits the builder to administer
the judgement that makes C6/C7 green. This is inadmissible for governing methods that explicitly
require fresh or critic-side judgement: RI-VIS08 D1 uses a fresh mask-only judge and its blind pass
uses RI-VIS06; RI-AI06 M4 is a critic prediction panel; RI-CAM01/02/05 and RI-CMB06 prescribe
critic blind comparisons; RI-CAM07 bifurcates fidelity from an art-direction blind judgement. A
competent builder can therefore complete the written step 5 with context-contaminated/self-judged
packs, pass C6 as phrased, and still fail to demonstrate those governing rows.

**Required remediation:** enumerate every critic/fresh-judge/blind row among the ten items and
assign it to the actor its native method requires. The builder may generate commit-stamped source
artifacts and prove pack completeness/leak checks, but may not score those rows. Require separately
dispatched, context-eligible critic/judge runs, including calibration, reveal and rerun controls
where the native protocols demand them, before C7 can be green. State that builder-administered or
context-exposed results are inadmissible, not merely evidence awaiting review.

## Critique remediation — 2026-08-09

The operative contract now enumerates the governing critic/fresh/blind rows and assigns each to its
native eligible actor, including RI-AI06 M4's fresh clip panel, all four camera/combat blind pairs,
RI-VIS08's fresh mask judge/Protocol A, and RI-CAM07's bifurcated blind art judgement. Builder work
is limited to reusable mechanical evidence and one complete audited source pack; independent work
is paid for only after those cheap gates pass. Inadmissible self/context-exposed results remain
`not_run` and cannot enter C7. Materially edited; awaiting fresh re-criticism by a different
reviewer-editor.

## Fresh reviewer-editor re-criticism — 2026-08-09

**SATISFIED.** Independent reconstruction of the five owned paths, ten governing items, S18 and S25 found the actor boundary complete: every native blind/fresh row is assigned to an eligible critic or judge and cannot enter C7 from builder-authored evidence. Existing mechanical tools and one source-pack pass precede independent work. The plan preserves the authority boundary rather than choosing a threshold locally. S48 and S49 have since resolved the filed geometry contradictions without weakening zero clipping; the operative build route consumes those rulings directly.

## S49 authority correction — 2026-08-11

Builder-final recovery made M4/M5 executable and exposed a defect in the plan's S49 premise. The
native fixtures specify distance to a wall; the committed instrument passed those distances as the
centre of the 0.80 m-thick `rail_wall`. M4 therefore swept its player-facing surface from 5.60 m to
0.10 m, not 6.0 m to 0.5 m. M5 likewise began 2.60 m, not 3.0 m, from that surface and did not prove
the pivot stayed on its playable side. At the realised M4 endpoint, zero clipping requires the
camera adjacent to the pivot while camera-to-head ≥0.35 m requires it away from the pivot: the
reported 12 frames are the exact signature of that 0.40 m coordinate error, not a shipping-camera
implementation defect. The five-row S49 synthetic used different geometry, explaining its green
result without making either instrument inert.

Corrected S49 and RI-CAM01 now make surface distance explicit. The builder must repair only the
instrument coordinates/assertion, deliberately red-seed the centre/surface conversion, and rerun
M4/M5 before interpreting any residual. The 0.80 m wall thickness, 6.0→0.5 m sweep, 240 frames ×12,
M5 3.0 m start/120+60+120 frames, zero clipping, non-negative boom, complete near-plane clearance,
camera-to-head ≥0.35 m, normal 0.90 m floor/S49 emergency proof, opacity/shadow, rate/dwell and
orientation/FOV bars are unchanged. Pinch and stair retain S49 emergency authority; no production
game-code change or population/quality reduction is authorised.

This is a material canonical plan edit. Under `PLAN-LOOP.md`, this version is
**awaiting-recriticism** and W1-06 may not proceed directly to its final builder. The exact next
owner action is to dispatch one fresh reviewer-editor, different from this arbiter-editor, to judge
this corrected plan and authority chain. On SATISFIED, dispatch the final builder against the narrow
instrument repair first; any surface-registered residual infeasibility returns to arbitration with
the constructive trace rather than weakening a bar locally.
