# W1-13 current-state continuation plan

Plan-State: awaiting-recriticism

## Authority, history, and continuation boundary

**Governing items (complete, not summaries):** the authoritative `docs/PLAN.md` §3 decomposition
owns seven paths and names **RI-AI05, RI-AI06, RI-AI07, RI-AUD04, RI-CAM05, RI-CAM06, RI-CMB08,
RI-JRN06, RI-LOR05, RI-PRG04, RI-TRV02, RI-WLD04 and RI-WLD07**. Read all thirteen current items,
their native methods, populations, units, thresholds, scoring, hard fails and applicable later
rulings immediately before building. The received plan's JRN06/PRG04-only set was not authoritative.
Historical evidence is reusable only where its current-HEAD fixture and consumer still establish a
governing row. The superseded PRG04 spacing table is historical only.

Current HEAD has genuinely proven: exact single/double-death soul conservation; one-stain semantics; named-actor permanence; a walked recovery loop; hearth-only levelling and ten-row presentation; stain persistence/visibility after the camera correction; fixed-step clock/weather; AR-1's death-price invariant via a separate award clock; and aggregation coverage. Preserve these only where fresh runs reproduce them. Remaining current defects/dependencies are: the RI-PRG04 M8 roster consequence passes on empty/mismatched populations; `awardTimeOfDay` is not saved and a reload changes a kill from 42 to 57 souls; world and award clocks can disagree by >0.30 h after repeated deaths; R3 encounter-on-run-back and R4 shortcut ratio are unmeasured pending real placed hostiles/shortcut geometry; PRG04 method 9 depends on the current RI-PRG06 economy; fall/drown and centre-fog-gate placement paths require regression coverage; historical path decomposition assigned death *cost* here, not combat lethality. Do not claim dependency populations as zero or implement parallel fake geometry.

## Build order

1. Repair instruments first: assert fixtures/populations, top-level coverage/pass fields, commit stamp, and fail-closed empty handling.
2. Close M8 roster and award-clock durability/coherence without weakening AR-1.
3. Run every native method and aggregate for all thirteen governing items, sharing identical death,
   run-back, boss, camera, world and roster populations rather than repeating them.
4. Run R3/R4 and PRG04 M9 only if their real governing populations exist at HEAD; otherwise record `unmeasurable`, exact missing dependency, and zero credit. Never generate local stand-ins.

## Complete acceptance matrix

Run the authoritative item procedures below; a dependency that prevents a row from running remains
present as `unmeasurable`/zero. The existing empty-roster and award-clock work is an additional defect
gate, never a substitute for a native row.

### Governing cross-item legs omitted by the received plan

| Governing item(s) | Required closure without proxy credit |
|---|---|
| RI-AI05/06/07 | Run every native archetype, boss and encounter/difficulty method touching lethality, arena or run-back paths on the complete roster/tier/load population and exact bands. Preserve introductions, no-scaling, fairness, density and hard fails; empty/mismatched rosters are zero. |
| RI-AUD04 | Run the complete death/boss/arena audio methods and blind comparison on actual audible artifacts with native event populations, units, thresholds and hard fails. Reuse stamped deaths/boss attempts rather than commissioning a duplicate playthrough. |
| RI-CAM05/06 | Run every native death, boss-arena and combat-camera method over its named geometries, events, frame/viewpoint populations and thresholds. Reuse compatible JRN06/arena captures; screenshots never replace required traces. |
| RI-CMB08 | Run every native healing/death/lethality predicate over its full charge, enemy, timing and state population, including AR-1 and hard fails. Death-cost evidence is not combat-lethality evidence. |
| RI-LOR05 | Run native lore/system agreement checks for hearths, death, bosses and named actors over the complete relevant data/live population. A correct mechanic with contradictory player-facing lore is red. |
| RI-TRV02 | Run native traversal/transport predicates touching hearth placement, run-backs or boss access on the full route/mode population in exact path-time units. Share PRG04/JRN06 route traces where admissible. |
| RI-WLD04/07 | Run complete native dungeon/boss-space and encounter-placement methods over every governing site/region/roster, preserving topology, access, density, populations and hard fails. Local fake hostiles or geometry are inadmissible. |

For every row above, the aggregate records each current item's exact method row with numerator,
denominator, native unit, target/fail band and hard-fail result. Missing dependencies are
`unmeasurable`/zero and cannot be averaged away. Each of the thirteen item scores must reach its
native meets-bar band, the piece aggregate must meet the governing min rule, and every automatic
fail must be false. Run shared cheap static/population gates first, then one shared browser/capture
pass; stop before blind or long runtime work if a hard fail or missing population makes the aggregate
unsatisfiable.

### RI-JRN06 — explicit M-D1–M-D19 mapping

| Row | Native fixture / denominator | Target, units, and hard-fail consequence |
|---|---|---|
| M-D1 | Souls held, stain-created and recovered over 20 deaths | Exact integer equality, 20/20; any discrepancy is HF1. |
| M-D2 | D1 position to stain and standability, 20 deaths | ≤2.0 m, 20/20; zero stains in geometry/air. |
| M-D3 | RN5 sealed/one-way death; path from respawn HEARTH | Path exists and souls recoverable; unreachable stain is HF7. |
| M-D4 | Pre-kill vs post-respawn state hash, all five scenarios, excluding only named `death-volatile` fields | Empty diff, 5/5; any other changed field is HF2. |
| M-D5 | Alive entities before/after respawn, enumerated by archetype | 100% ordinary enemies respawn; zero named NPCs, quest actors or merchants; any named respawn is HF3. |
| M-D6 | Kill named NPC, then die/respawn | NPC remains dead and `quests.failure.severed` remains intact. |
| M-D7 | Sell to merchant, then die/respawn | Merchant gold pool unchanged. |
| M-D8 | RN3: die with 4,200, then die again; global stain census | Exactly one stain at all times; first destroyed and unrecoverable; violation is HF4. |
| M-D9 | UI-text stream across 20 deaths plus complete item-data census | Zero loss/insurance/recovery-message hits and zero compensating items; any is HF5. |
| M-D10 | Last-hit active window to HP=0, 20 deaths | ≤12 frames at 60 Hz, 20/20. |
| M-D11 | Screenshots every 100 ms from death; input on first rendered surface | Surface ≤3.0 s, gone within 30 frames of input, zero statistics/tips; unskippable surface is HF8. |
| M-D12 | Death frame to first input-caused movement, 20 samples | Median ≤4.0 s and p95 ≤6.0 s. |
| M-D13 | Shortest-path auto-walk at RI-WLD01 speed for RN1/RN2/RN4; hostile census; RN2 original-approach comparison | R1 median **1.5–3.0 min**; R2 p95 **≤4.0 min**; R3 **1–4 encounters**; R4 shortcut ratio **0.30–0.60**. Native fail bands remain binding: R1 >4.5 min, R2 >6.0 min, R3 0 or >6, R4 >0.85. RI-PRG04 wins any spacing disagreement. |
| M-D14 | 8 LOS viewpoints at 12 m, plus 8 at 6 m in RN1 dark cave | Visible pixels 8/8 and 8/8, with screenshots. |
| M-D15 | Recovery while rolling, mid-attack, at 1 HP, hostile aggroed, and one-frame input | Souls returned 5/5 and zero started-without-credit cases; consumed stain without credit is HF9. |
| M-D16 | Die, rest, change region, save, close browser, reopen, Continue | Same-position, same-souls stain persists; reuse valid RI-JRN05 M3 evidence rather than duplicate the round trip. |
| M-D17 | 20-death agent-driven session in a difficulty-appropriate region | R5 stain-loss fraction 0.10–0.35 (fail at 0 or >0.60). |
| M-D18 | Every transport option reachable from respawn HEARTH | Zero routes ending at/within 60 s of death point except pre-existing station; return-to-death affordance is HF6. |
| M-D19 | RI-WLD06 M31 over all run-back HUD frames | Zero stain-pointing markers/arrows/minimap/compass; any is HF6. |

The JRN06 aggregate must execute once, score every row, assert HF1–HF9 false and reach native
≥92/100. R1–R7 are therefore all explicit: R1–R4 above, R5 in M-D17, R6 target ≤4.0 s (fail >8.0
s; M-D12 additionally binds p95 ≤6.0 s), and R7 median arrival-within-15-m to recovery ≤12 s (fail
>30 s), measured alongside M-D14 rather than inferred from visibility.

### RI-PRG04 — lossless methods 1–10 mapping

| Method | Native fixture / population | Binding acceptance |
|---|---|---|
| 1 Rest effects | Partial HP/FP/flask/Fatigue and 40 pre-killed ordinary enemies; census every named NPC, merchant, trainer, quest actor and dead boss | Resources max; all 40 ordinary enemies alive; every named/boss row unchanged. Any named/merchant respawn is automatic fail. |
| 2 S7/no teleport | All `game/src/` warp/teleport/travel hits plus HEARTH menu; two-directional RI-TRV01 network control | Every hit only in the in-fiction paid fixed-route/time transport module; no HEARTH destinations; network must exist (pass floor ≥3 modes; top ≥5 modes, ≥17 lines, ≥60 services). Missing network or player-reachable warp-to-pin/objective is automatic fail. |
| 3 Spacing | All shipped HEARTHs (native target count 24–32), navmesh and all reachable points, path-time at standard speed | Mean critical-path spacing 3.0–4.0 min; no pair <2.0 min; no point >11.0 min from nearest HEARTH; every boss gate 60–110 s away with zero respawning enemies. Use path time, never Euclidean distance. |
| 4 Single death | Rest at A, pass B, die holding N, recover | Respawn at A; stain holds/returns exactly N; gold/inventory/durability/flask upgrades unchanged; world clock unchanged. |
| 5 Double death | Die with N then M before recovery | Exactly one stain holding M; N unrecoverable by any means. |
| 6 Persistence | 12 quest stages, 2 dead named NPCs, 3 shortcuts, 20 looted containers, 5 dispositions; then 3 deaths | Every state change survives byte-identical; quest/faction reset is S6 automatic fail. |
| 7 Edge cases | Boss arena, bottomless drop, one-way sealed area | Stain respectively outside fog gate, at last grounded point, nearest reachable path point; all three recoverable. |
| 8 Clock consequence | Rest **17:00→23:00**, inspect the same live consequence chain; then rest four times | Night roster active, merchants closed, active timed quest consumes exactly 6 h; four rests advance 24 h and restore same shop state. Keep the ≥20-NPC same-settlement rest/no-rest roster ownership and empty/wrong-settlement red controls as extra gates, not replacements. |
| 9 Death-cost sim | Current RI-PRG06 §7 world budget: 86 deaths, 22% second-death rate, per-region banks | Permanent loss 9–15%; retention within 3 percentage points of 0.88. Missing current budget scores zero; do not use historical arithmetic. |
| 10 Flask purity | Complete item and merchant inventories | Sap-bud and Heart-resin in zero merchant inventories and with no gold/soul price. |

Score the eight native axes by their item bands and require **every axis ≥6** under min-over-axes.
Preserve the stricter plan goal of ≥7 only as optional headroom; it is not allowed to turn otherwise
bar-satisfying work into mandatory rebuild/polish. Run cheap static Methods 2 and 10 and population
assertions before browser/runtime work; stop and repair/escalate on any hard fail or missing governing
population rather than paying for later runs that cannot satisfy the aggregate.

### Current-defect gates retained

`award_frames` must round-trip exactly; the 04:48 enemy must pay 42 both before and after reload
(not 57); identical enemies at every 15-minute boundary and 0/1/20/40 deaths must pay identically in
death and equal-lived-time arms. World/award coherence must use an item-compatible bound or a shared
consumer without reintroducing death reward. These gates repair current defects but do not alter any
JRN06 or PRG04 predicate.

## Controls and evidence

* Run a source-level 2×2: environment award-clock fix present/absent × souls consumer present/absent. Both off-diagonal arms must identify which layer is load-bearing; deleting durability must reproduce 42→57, deleting AR-1 must reproduce death-inflated reward.
* Empty every roster arm deliberately: M8 must report `EMPTY/FAIL`, not zero moved/pass. Swap in the wrong settlement: ownership assertion must fail.
* Delete fall-cause retention, hearth level gate, stain camera/aim independently; each relevant native row must return to its historical red while unrelated rows remain green.
* Re-run `tools/journey/journey-run.mjs --journey jrn06-death` once as the aggregation; standalone probes cannot substitute. Artifact records full commit, fixture seeds, frames at 60 Hz, metres, in-world minutes, populations, raw events and pass/coverage separately.
* Required gates: `node tools/boot-check.mjs`, `node tools/check-data.mjs`, `node tools/check-souls-world.mjs --totals`, `node tools/check-quests.mjs`, and the journey aggregation. Browser load declaration required.

## Blocking-completeness test

A builder cannot make this plan green while omitting a governing method: every native method is either measured on its item-native population or explicitly scores unmeasurable/zero, all hard fails are asserted, PRG04's min axes must each reach the native pass floor of 6, and JRN06 must reach its ≥92 meets-bar band. Independent criticism remains required.

---

## Independent plan critique — 2026-08-09

**Verdict: BLOCKING / awaiting-remediation.** The acceptance table does not preserve the native
JRN06 predicates it claims to preserve. In particular, its M-D13 row changes R1 from the binding
**1.5–3.0 min** interval to only `≤3 min`, changes R2 from **p95 ≤4.0 min** to `≤5 min`, and does
not state the binding R3 **1–4** or R4 **0.30–0.60** green intervals. Thus a 30-second corpse run,
a 4.8-minute p95, zero encounters, or a shortcut ratio outside its target can be made green under
the plan while failing the reference item. The grouped M-D rows also substitute broad prose for
several native predicates: D1/D2 death event and 12-frame attribution, D5/D7 surface/control
timing, D8's complete respawn-state vector, D15's five recovery-robustness arms, D18 transport
census, and D19 marker sweep are not assigned their item procedures and exact thresholds.

**Required remediation:** replace the JRN06 portion with an explicit M-D1–M-D19 matrix (or a
lossless incorporation that names each row), preserving each row's native fixture, denominator,
units, target, fail threshold and hard-fail consequence. Correct R1/R2 and state the complete
R1–R7 targets. Do the same lossless mapping for PRG04 methods 1–10 rather than letting the
non-native M8 three-arm roster experiment substitute for method 8's required 17:00→23:00
merchant/night-roster/deadline consequence chain. Keep the useful empty-roster and award-clock
controls as additional current-defect gates, not replacements for the governing comparisons.

**Recommendation:** remediate the acceptance matrix, then request fresh independent criticism.


## Remediation — 2026-08-09

Resolved the recorded predicate/population defect with explicit native JRN06 M-D1–M-D19 and PRG04
Methods 1–10 mappings, restored R1–R7 bands, and retained roster/clock checks only as additional
current-defect gates. This material edit awaits a fresh reviewer-editor.

### Fresh reviewer-editor outcome — 2026-08-09

**Material edit / awaiting-recriticism.** Independent reconstruction from the owned paths and
authoritative `judges:` set found that the received plan covered only two of W1-13's thirteen
governing items. It could therefore pass every written row while failing lethality, boss/encounter,
camera, audio, lore, traversal and world-placement bars. The missing eleven native item legs are now
mandatory, with shared evidence and cheap-first stop conditions. A different fresh reviewer-editor
must judge this version.
