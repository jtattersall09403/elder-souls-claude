# W1-13 current-state continuation plan

Plan-State: awaiting-criticism

## Authority, history, and continuation boundary

**Governing items (complete, not summaries):** `corpus/88-journeys/RI-JRN06-death-and-recovery.md` and `corpus/20-progression/RI-PRG04-hearth-and-death.md`, including every comparison method, hard fail, arbitration check, score band, and RI-PRG04 min-axis ladder. Read them immediately before building. The history to preserve and falsify is `W1-13-r1` through `W1-13-r4` (Markdown, JSON, artifacts and cited reports), all W1-13 statuses, and the current implementations/instruments they name. The superseded PRG04 spacing table in `orchestration/amendments/AM-W1-13-01-prg04-spacing-table-superseded.md` is historical only; the current item is the bar.

Current HEAD has genuinely proven: exact single/double-death soul conservation; one-stain semantics; named-actor permanence; a walked recovery loop; hearth-only levelling and ten-row presentation; stain persistence/visibility after the camera correction; fixed-step clock/weather; AR-1's death-price invariant via a separate award clock; and aggregation coverage. Preserve these only where fresh runs reproduce them. Remaining current defects/dependencies are: the RI-PRG04 M8 roster consequence passes on empty/mismatched populations; `awardTimeOfDay` is not saved and a reload changes a kill from 42 to 57 souls; world and award clocks can disagree by >0.30 h after repeated deaths; R3 encounter-on-run-back and R4 shortcut ratio are unmeasured pending real placed hostiles/shortcut geometry; PRG04 method 9 depends on the current RI-PRG06 economy; fall/drown and centre-fog-gate placement paths require regression coverage; historical path decomposition assigned death *cost* here, not combat lethality. Do not claim dependency populations as zero or implement parallel fake geometry.

## Build order

1. Repair instruments first: assert fixtures/populations, top-level coverage/pass fields, commit stamp, and fail-closed empty handling.
2. Close M8 roster and award-clock durability/coherence without weakening AR-1.
3. Re-run every native JRN06 D-method, PRG04 method/axis and the combined journey aggregation.
4. Run R3/R4 and PRG04 M9 only if their real governing populations exist at HEAD; otherwise record `unmeasurable`, exact missing dependency, and zero credit. Never generate local stand-ins.

## Complete acceptance matrix

| Gate | Population / fixture | Exact predicate and acceptance |
|---|---|---|
| JRN06 M-D1–D4 | 20 deterministic deaths: ≥6 second deaths; combat, fall, hazard/water, sealed/one-way and boss-centre fixtures | Every banked/held/stain/recovered leg conserves integer souls exactly (0 discrepancy); exactly one stain; every placement rule lands on standable reachable ground, fall/hazard at last grounded point, sealed/one-way toward hearth, boss stain outside the closed fog gate even at zero radial offset; live world after load, not blob bytes. Any discrepancy is HF1. |
| M-D5–D8 | all named dead actors, quest/world/faction/crime state before/after each death and load | zero named actor resurrection (HF3); only the item-declared volatile player/death paths change; no quest/journal/faction/world rollback; unknown paths are red, not silently allow-listed. |
| M-D9–D12 | every shipped hearth and no-hearth corrupted fixture | valid last-rested hearth always respawns; missing/invalid hearth fails safely without free same-place recovery; rest/region/load persistence holds; respawn control returns only after the specified frames/input, with no modal deadlock. |
| M-D13–D17 | ≥20 walked loops, not teleports; real routes and state restart | native R1 median ≤3 min and R2 p95 ≤5 min in-world; R3 counts real hostile encounters on the run-back; R4 measures real shortcut distance ratio; bloom survives rest/region/named-state/blob/IndexedDB round trips; second-death destruction rate and all item thresholds exact. Empty hostile/shortcut populations are `unmeasurable`, never pass. |
| JRN06 presentation/AR | 8 bearings × item day/night distances; all death causes; aggregation | stain visible at native distance/contrast bar on every bearing; cause/refusal text truthful; AR-1 no extra reward, AR-2 no marker/waypoint route, seam sterility reported; all JRN06 hard fails false and native score ≥92/100 (item's meets-bar band). |
| PRG04 Function | every shipped hearth + off-hearth controls | rest, bank/risk/recover, hearth-only level-up and all item functions work; level-up refuses off-hearth and opens at every hearth; ten attributes simultaneously, correct RI-PRG01 price and derived preview; no gold. Axis reaches the item ≥7 rung. |
| PRG04 Placement/spacing | complete shipped hearth graph, route network and all death placement branches | run the item's spacing/Dijkstra comparison in its native units and current thresholds; no Euclidean proxy; every sampled stain standable/reachable and within item offsets. Use current item table, never the superseded amendment. Axis ≥7. |
| PRG04 Clock cost M8 | three arms from the same ≥20-NPC settlement: one rest, equal no-rest, four rests; one live deadline | before/after roster population ≥20 in **each** arm and belongs to the well's settlement; rested `npcs_moved ≥1`; populated no-rest `npcs_moved=0`; four-rest populated roster returns; clock delta exact; at least one real quest deadline crosses only in rested arm and aggregation row passes. Empty/mismatched roster hard-red. |
| PRG04 economy M9 / loop M10 | current complete RI-PRG06 soul budget; ≥86-death native sim; real shortcuts/encounters | execute item-native 88.2% retention and all thresholds against live current budget, not historical arithmetic. Run full loop axes with real geometry/populations. Missing dependency is 0/unmeasurable per item, not an invented proxy. Every PRG04 axis ≥7 because aggregation is `min`. |
| Clock durability/coherence | starts at every 15-minute boundary; 0,1,20,40 deaths; save/load at 04:48 and 20:42 | `award_frames` round-trips exactly; identical enemy before/after reload pays identical souls; 04:48 fixture remains 42 (not 57) after reload; world-vs-award offset stays within an explicitly item-compatible bound or one clock becomes the shared consumer without reintroducing death reward. At every boundary, death arm equals equal-lived-time arm for award and schedule consequences. |

## Controls and evidence

* Run a source-level 2×2: environment award-clock fix present/absent × souls consumer present/absent. Both off-diagonal arms must identify which layer is load-bearing; deleting durability must reproduce 42→57, deleting AR-1 must reproduce death-inflated reward.
* Empty every roster arm deliberately: M8 must report `EMPTY/FAIL`, not zero moved/pass. Swap in the wrong settlement: ownership assertion must fail.
* Delete fall-cause retention, hearth level gate, stain camera/aim independently; each relevant native row must return to its historical red while unrelated rows remain green.
* Re-run `tools/journey/journey-run.mjs --journey jrn06-death` once as the aggregation; standalone probes cannot substitute. Artifact records full commit, fixture seeds, frames at 60 Hz, metres, in-world minutes, populations, raw events and pass/coverage separately.
* Required gates: `node tools/boot-check.mjs`, `node tools/check-data.mjs`, `node tools/check-souls-world.mjs --totals`, `node tools/check-quests.mjs`, and the journey aggregation. Browser load declaration required.

## Blocking-completeness test

A builder cannot make this plan green while omitting a governing method: every native method is either measured on its item-native population or explicitly scores unmeasurable/zero, all hard fails are asserted, PRG04's min axes must each reach 7, and JRN06 must reach its ≥92 meets-bar band. Independent criticism remains required.
