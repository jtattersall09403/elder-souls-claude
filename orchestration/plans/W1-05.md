# W1-05 — current-state continuation plan

Plan-State: satisfied
Canonical-HEAD: `1c4c487d` (reconstruct and restamp before building)

## 0. Authority and current state

`docs/PLAN.md` §3 W1-05 owns six paths—roads, transport, stations, schedules, directions and map legibility—and is governed by **RI-DLG05, RI-LOR02, RI-LOR05, RI-PRG05, RI-TRV01, RI-TRV02, RI-WLD01, RI-WLD02, RI-WLD06 and RI-WLD09**, plus AR-2/S35: the discovery-only map may exist, but wayfinding must work with it closed and may not rely on a marker, route line, distance readout, or undiscovered place square.

The single builder history delivered 10 road legs, 32 posts/68 arms, spoken directions, usable sign reading, a two-hop blind navigator with signpost ablation, corrected named-route ids, waylamps, and six falsified audit checks. Its required body-walk proof remained open after three failed attempts. Canonical `NEXT-DISPATCH.md` Q14 supersedes that diagnosis: W1-ROAD-JOIN and steering work made `soulrest-blackrose` walkable both directions with 0.00 m water; stop re-testing the old drowning hypothesis. However `walkPath` itself was not the fixed `_pursue` path, so old W1-05 journey reports are stale for reachability. There is **no independent W1-05 verdict**. All status claims remain builder evidence, including the 7/10 route arrival count and prose/waylamp closure.

Preserve only after rerun: signpost topology and bearings, live sign-reader and RoadBook consumption, prose gates, route-id correction, S35-compliant no-marker behavior, and waylamp day/night perturbation. Explicit seams: W1-ROAD-JOIN owns the road/building repair; W1-04 owns settlement geometry; W1-02 owns region borders; W1-21 owns map rendering; these do not waive any route/reachability/map predicate here.

## 1. Complete acceptance contract

Before code changes, build a row-per-method **governing bar matrix** for all ten items. Native thresholds and hard fails control wherever stricter than the predicates below; absent/unmeasurable rows score zero.

### A. Scale, density, and physical traversal

1. **RI-WLD01:** run every native traversal method on the exact item population and modes. Publish path distance (m/km), elapsed world time and wall/sim time (s/min), speed (m/s), off-road frames, teleports, water depth and arrival radius. Apply the item's exact bands—including its stated route-time/distance comparisons—without substituting straight-line distance. A body must actually walk; plan-only or camera arrival is not evidence.
2. **Mandatory reachability:** traverse all **10 adjacent settlement road legs in both directions** (20 directed legs) from a valid walkable approach to the destination arrival volume, through the live collision/streaming world. Acceptance: **20/20 arrive**, zero teleport/reanchor, zero fatal water, zero stuck/abort; publish worst final distance, maximum water depth and off-road fraction. Then execute each named multi-leg route in both directions under the same predicates. Use the fixed live steering path; do not cite stale `walkPath` output.
3. **RI-WLD02:** compute encounters/landmarks/services/decisions per travel minute over the item's complete route/time population using its windows and comparison method. Meet every native density band and hard fail; a route point, unrendered JSON row or repeated object cannot count as experienced content.
4. **RI-WLD09:** on the full item population, measure opacity/occlusion and landmark/readability windows using its exact units and comparator. Roads, signs, lamps and stations must remain legible through the required ranges without clearing Black Marsh into an open plain. Run the native hard fails.

### B. Wayfinding without prohibited aids

5. **RI-WLD06 layer census:** satisfy every L1/L2/L3 navigation layer and exact native threshold: road geometry/material, junction/approach signs, readable destinations/bearings, spoken directions/landmarks and fallback recovery. Census all 10 legs, every junction, every sign arm and all named destinations. Every arm's named destination and bearing resolves against road topology; no dangling ids/topics/tells.
6. **Blind journey:** a fresh navigator receives only information available to a player in the running world, starts without route coordinates, and travels the native WLD06 population with the map closed. Apply the item's exact success and wrong-turn/recovery bars. The artifact must contain the actual visual/textual evidence under judgment, not feature-only labels.
7. **Ablation:** remove sign/direction access while keeping roads/collision/world identical. The governed journeys must worsen by the native comparison; first assert the positive arm read at least one sign or heard at least one direction and the arms differ. A route with no decision point is not a load-bearing signpost trial.
8. **AR-2/S35:** run all three RI-UIX02/AR-2 marker detectors plus a live map census: zero player marker, quest marker, route line or distance readout; zero squares for places never personally stood in; discovery-only geography is allowed. Repeat the successful journeys with the map closed. W1-21 ownership is a dependency, not a waiver.

### C. Transport, schedules, economy, dialogue, lore

9. **RI-TRV01:** enumerate every transport route, station, directed edge, schedule window, fare and transfer in the native population. Every station is physically reachable on foot; every declared edge can be boarded, advances the exact world time, charges the exact gold fare once, and arrives at the declared station. Apply the item's connectivity, wait/headway, detour and hard-fail predicates exactly.
10. **RI-TRV02:** execute every travel-magic type and restriction in the item, including origin/destination, cost, failure/refusal and world-time effects. It may complement but cannot make the no-marker road proof vacuous. No orphan spell/route data or harness-only caller.
11. **RI-PRG05:** transport/wayfinding costs participate in the item's complete gold source/sink basket and price comparisons. Fares are gold, never souls; every charged amount has a player-facing quote and live debit; no double charge on transfers/retries.
12. **RI-DLG05:** journal/direction records meet the item's exact completeness, update, ordering, legibility and persistence predicates. A spoken direction must become available through a real topic/conversation path, be journaled where required, survive save/load, and not reveal a prohibited marker.
13. **RI-LOR02/LOR05:** census all sign, station, route, direction and journal prose against political/era and religion/soul doctrine. Names, factions, destinations and metaphysics must resolve to canonical entities; invented location or anachronistic route is a hard failure wherever the items say so.

## 2. Evidence and adversarial controls

Reuse `signpost-audit.mjs`, `wayfind-journey.mjs`, `waylamp-probe.mjs`, `rawleg-check.mjs`, W1-ROAD-JOIN's body walker, item-native transport/scale/density/opacity tools, and aggregate gates. Extend a native tool instead of replacing its comparator.

* **Red:** corrupt one arm bearing, one destination id, one spoken topic, one road connection, one station schedule and one fare independently; the corresponding graded row must fail. Move one blocking building back over the road and require the directed body walk to fail.
* **Null:** map closed versus open; sign/directions present versus ablated; body walk versus plan-only; day versus night lamp; scheduled versus off-schedule boarding. Confirm support differs and populations are non-empty.
* **Delete-the-fix:** in a detached scratch worktree remove the road-join/steering fix, not the signs. The known Soulrest–Blackrose directed walks must regress while the shipped arm arrives; separately delete sign consumption and require navigation decisions/success to regress while geometry stays identical.
* **Temporal control:** measure at departure, junction decisions, arrival, and after sufficient settling frames; one instant cannot establish arrival or accessibility.
* **Aggregation:** all item rows and the 20-leg matrix run together, followed by boot/data/content/quest/prose/corpus gates. Every number stamped with commit/dirty state.

## 3. Ordered continuation

1. Re-run established offline sign/direction/prose/waylamp evidence at HEAD and classify it; make no work from green rows.
2. Replace stale journey evidence with the 20-directed-leg live body-walk matrix and named-route runs on the fixed path.
3. Execute the remaining native WLD01/02/06/09, TRV01/02, PRG05, DLG05 and lore rows; implement only actual reds.
4. Resolve cross-piece failures explicitly with named owner/dependency and retain the red acceptance row until end-to-end green.
5. Run controls, delete-fix, aggregation and gates; hand the awaiting-criticism plan/build result to an independent critic.

## 4. Completeness audit

No builder can pass this contract using a plan-only navigator, stale drowning evidence, signs that are never read, a transport table that cannot be boarded, or a map that supplies the answer. Every governing item remains a fail-closed matrix row and every ownership seam retains end-to-end acceptance.


## Independent plan critique — 2026-08-09

**Verdict: SATISFIED.** I reconstructed the six assigned paths and ten governing items from
`docs/PLAN.md`, then checked RI-WLD06 M27–M32, RI-TRV01 M0–M12, the remaining native methods, S35,
Q14, W1-ROAD-JOIN evidence, and the stale W1-05 journey evidence independently. I found no route by
which every planned row could go green while markerless wayfinding, physical reachability, scale,
density, opacity, transport, travel magic, economy, journal, or lore still missed its governing bar.

The plan preserves the reference populations and comparators through a fail-closed row for every
method and hard fail, while making the live proof stricter rather than substituting for it: 20/20
directed adjacent legs plus named routes require a collision-bound capsule, no teleport/reanchor,
and settled arrival. RI-WLD06 remains independently decisive through its 200-point/24-gate
sightline census, complete junction/sign census, ten-target fresh-agent test, full directions
coverage, marker leakage hard fail, and blind prose comparison. The plan also requires the actual
artifact, player-available information only, load-bearing ablation support, all three independent
marker detectors, complete station/edge/schedule/fare populations, real boarding and debit, and
separate geometry and sign-consumption delete-fix arms. Cross-piece ownership leaves rows red rather
than exempt.

**Recommendation:** plan satisfied; dispatch the continuation builder. Browser contention and the
actual 20-leg outcomes are build-stage empirical risks, not planning exceptions.
