# PLAN — W1-03: water, marsh, and the amphibious body

Plan-State: satisfied

Planning-only continuation reviewed against canonical HEAD `3b783560`. Governing items are RI-VIS02, RI-VIS04, RI-WLD10 and RI-WLD11, plus S25's body-readable depth ruling. Their native methods and hard fails are the bar.

## 1. Full state reconstruction

Round 1 independently confirmed a sound regional water census and live Saxhleel differences, but scored 2/3/1/0 across WLD10/WLD11/VIS04/VIS02. The automatic RI-WLD10 fail was non-tidal regions moving up to 1.2 m. Other established gaps: flat rather than END-derived breath; two water-native enemies with `ai:none`; no pursuit/leash; producer/consumer water-denial seam not proven in-world; incomplete hazard methods; missing planar reflection/depth colour/Fresnel/normal; no native paired visual comparison; dropped water quads; tide mesh sampled only at tile build; drying timer visually inert. Later work added a visible body waterline/drying attempt and W1-16 fixed sprint denial's second implementation. Those landings make old screenshots and counts evidence about history, not HEAD. Q2 requires verifying held sprint denial at the locomotion consumer.

Dependencies: W1-06 controls the required third-person silhouette; W1-12 owns pursuit/AI action; W1-14 may interact with environment effects; W1-16 owns burden/fall seams. Coordinate rather than masking their failures.

## 2. Governing reconciliation

| item requirement | independent evidence | must be reverified at HEAD | remaining build only if failing |
|---|---|---|---|
| RI-WLD10 M47 | WCI .294, spread .86, class/tidal-region census passed | exact native census over current field | closure if unchanged |
| M48/49/52/55/56 | not run | run native methods with exact populations and units | measured misses only |
| M51 | amphibious W4/W5 divergence passed; breath formula failed | all subchecks, END 10..cap, moving depth path, held actions | derive breath exactly; fix consumer seams only if reproduced |
| M53 | roster minimum only nominal; leash/acting/consumption failed | full roster land/water DPS, pursuit across moving boundary, producer→consumer | coordinate AI motion; make required natives active and capable |
| M54 | automatic fail outside five tidal regions | 13 regions × item phases/points | non-tidal amplitude ≤ item limit everywhere while preserving five tidal regions |
| S25 §10.3/10.4 | live body band passed once; drying, quads, live tide and regional reflection failed | W0–W5 moving body, exit-water time series, live tide without restream, surface/body alignment | fix exact failed clauses |
| RI-WLD11 M57 | 19 hazards/13 signatures/one KILL/8 zero damage passed | native census and confinement | closure if current |
| M58–M63 | previously unrun | exact telegraph, escape, counter, scaling/arena, everyone-hurt and spread methods | remediate native misses only |
| RI-VIS04 §9 | minimum water feature set absent | source plus live renderer evidence for each named feature | planar half-res reflection, depth colour, Fresnel, scrolling normal and every exact §9 clause |
| RI-VIS02 REF-M4 | never judged | build item-native paired pack and obtain independent blind judgement | visual remediation until rubric threshold; no source proxy |

## 3. Acceptance contract

* **W1 water model:** run every RI-WLD10 weighted method M47–M56. Native weighted score **≥90/100** (the item's `Meets the bar` band, which permits a Wave-1 ladder score ≥7), no automatic fail, and an artifact-backed ladder-≥7 justification. Individual misses may remain only when the native weighted result still reaches 90 and the item does not make that miss an automatic fail; record each as debt rather than silently upgrading it to PASS. Units/fixtures are exactly the item definitions; tide population is all 13 regions at the prescribed spatial/phase sample.
* **W2 tide confinement:** the declared tidal-region set equals exactly the five §8 regions, and every other region authors `0.00 m` tide amplitude and consumes exactly zero tide-driven change in the world field, rendered mesh, and body waterline. Separately, M54 retains its observational fitted-amplitude failure tolerance (`> 0.02 m`) for measurement noise; that tolerance is not permission to author or consume a small non-tidal tide. Comparison is `max(surface)-min(surface)` at fixed ground coordinates, not a data-table flag.
* **W3 amphibious:** exact M51 breath formula `min(100,40+2*(END-10)) s`; same moving W0–W5 path for Saxhleel/control races; all denial/drain/regen thresholds native; held sprint/attack tested through transition, not only press frame.
* **W4 water enemies:** native M53 population; ≥4 qualifying natives, each active; W5 DPS ≥60% roster land median where specified; pursuit/leash and world producer→consumer demonstrated with moving targets.
* **W5 surface:** at every sampled player square the rendered surface covers the field-wet footprint within item tolerance; live tide moves mesh and body line together without restream; waterline anatomical error meets S25's own bound.
* **W6 drying:** item §10.3 time population at exit, 1 s, intermediate points, 20 s and after; wet appearance persists and visibly decays exactly as specified, then equals dry control.
* **W7 hazards:** measure every RI-WLD11 M57–M63 row; native weighted score **≥85/100** (the item's top/meeting band), no automatic fail, and an artifact-backed ladder-≥7 justification. A measured miss may remain only if the native aggregation still reaches 85 and it is not an automatic fail; no row may be omitted or called passing merely because the aggregate clears.
* **W8 renderer:** every RI-VIS04 §9 minimum and scoring clause present in live pixels, including half-resolution planar reflection, depth colour, Fresnel and ≥1 scrolling normal; whole-item feature score ≥7, no §9 cap, and an artifact-backed ladder-≥7 justification. Other checklist features are measured for the native `min(raw, cap)` aggregation but are remediated only as needed to keep that whole-item score at or above 7.
* **W9 visual reference:** REF-M4 paired shots at prescribed fixture/camera/settings, judged by a fresh blind critic using the item rubric; **every paired shot ≥7** because RI-VIS02 aggregates by the minimum across shots, with no hard fail and an artifact-backed ladder-≥7 justification.
* **W10 aggregate:** all four native item reports at the same commit, zero hard fails, each ≥7 Wave-1 gate.

Hard fails: any authored or consumed tidal amplitude anywhere outside the exact five §8 tidal regions, including a non-zero value below M54's observational fit tolerance; replacing visual judgement with source checks; stationary-only response tests; stale tile rebuild disguised as live tide; a water model with no world consumer; unmeasured rows passed; any item-native automatic fail.

Null/red/delete controls: zero tide range then restore; plant and consume a `0.01 m` tide in one non-tidal region and require the exact-set/automatic-fail check to go red while the separate M54 fitted-amplitude-tolerance arm remains below its `> 0.02 m` failure threshold; restore flat breath; remove amphibious branch; hold sprint before crossing; disconnect engine water producer; set water-native AI inert; freeze tile mesh; delete reflection/normal/depth/Fresnel individually; compare wet-exit arm to always-dry. Every control must turn the relevant check red and its arms must differ. Publish commit, seeds, raw time series, coordinates, roster rows, pixel diffs and blind pack/reveal. Reuse the round-1 four probes and native `m-wld10`/`m-wld11` tools, extending them to all missing methods.

## 4. Build sequence

1. Run M47 and the exact tidal-set/authored-zero static gate first; then re-run only native rows lacking still-valid commit-matched evidence at HEAD and classify closure/remediation. Hard-stop before browser work if the static gate fails.
2. Fix the automatic tide fail and model/consumer defects; prove controls.
3. Fix surface coverage/live tide/drying, preserving S25 and camera coupling.
4. Complete hazard methods and measured remediation.
5. Implement/evaluate exact VIS04 §9, then prepare the unchanged-native REF-M4 blind pack for a fresh judge.
6. Run aggregate and delete-fix suite; hand evidence to independent critic.

**Escape audit:** Could a builder get every planned check green yet miss a governing bar? **No.** W1/W7/W8/W9/W10 require every native row to be measured, each item's native aggregation and meets-bar band, every automatic fail, the shared Wave-1 ladder gate with evidence, and the native visual comparison across all four items. They do not wastefully require 100% of weighted rows where the authority expressly permits a meets-bar aggregate below 100.

## Plan critique — 2026-08-09

**Recorded blocker (resolved in this remediation; fresh review required):** W2 and the hard-fail list weaken RI-WLD10's native
automatic fail. The item distinguishes M54's observed-fit tolerance (`> 0.02 m` fails the fitted
amplitude probe) from the governing rule: *any tidal amplitude anywhere outside the five tidal
regions* is an automatic fail. The plan converts that rule into “non-tidal amplitude never exceeds
0.02 m” and lists only amplitude “above 0.02 m” as a hard fail. A build can therefore deliberately
author and consume a 0.01–0.02 m global tide in all eight non-tidal regions, pass W2 exactly as
written and pass the M54 tolerance, yet violate the item-native automatic fail and the exact-five
population predicate.

**Remediation applied:** W2, the hard-fail list, the controls, and build ordering now separate declaration/consumed behaviour from M54 measurement tolerance. Require the
tidal-region set to equal exactly the five named regions; require every other region's authored
tide amplitude and tide-driven world-field/mesh/body response to be exactly zero; retain
`> 0.02 m` only as M54's observational fit-failure threshold, not as permission for a small
non-tidal tide. Add a red seed with a consumed 0.01 m tide in one non-tidal region and require the
hard-fail/exact-set check to fail even though the fitted-amplitude tolerance arm would not.

## Fresh reviewer-editor pass — 2026-08-09

**Outcome: materially edited; awaiting fresh re-criticism.** The full plan mixed the native
`we lose` floors with the Wave-1 gate: RI-WLD10 75 and RI-WLD11 70 map to ladder 4, while W10
requires every assigned item at ladder ≥7. It also let an RI-VIS02 shot score 6 even though that
item aggregates by the minimum, and simultaneously required every weighted water/hazard predicate
to pass (effectively 100) despite the native items defining lower meeting bands. W1, W7, W8 and W9
now require the exact native meeting bands that can support ladder ≥7, hard-fail freedom,
artifact-backed high-score justification and complete measurement, without turning optional
weighted misses into mandatory Wave-1 construction.

## Fresh independent reviewer-editor pass r3 — 2026-08-09

**Outcome: SATISFIED; no material plan edit.** Reconstructed from `docs/PLAN.md`'s three owned
paths and four `judges:`-derived items, then checked every M47–M63 predicate, both visual native
aggregations, S25, S24 and the current sprint-denial seam. The received plan preserves exact
populations, weighted aggregations, automatic fails, minimum-over-shot fidelity, live world
CONSUMPTION and causal red/delete controls. Its cheap-static-first ordering and evidence reuse do
not mandate a materially avoidable browser, blind, corpus or implementation pass.
