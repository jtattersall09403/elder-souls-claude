# W1-23 current-state continuation plan — canon registry, books, disputes, and names

Plan-State: awaiting-recriticism

## Authority and full lineage

The authoritative `docs/PLAN.md` metadata governs, not the earlier plan summary. Its complete judge
set is **RI-CHR03, RI-LOR01, RI-LOR02, RI-LOR03, RI-LOR04, RI-LOR05, RI-LOR06, RI-LOR07, and
RI-QST06**, plus universal `RI-MTH07` for shipped world models. Their current text outranks prior
scores. Canonical lineage comprises `W1-23` and `W1-23-r2/r4` statuses; verdicts r1, r3, r4 (there
is no canonical r2 verdict and no r5 verdict); canon/library/name tools and artifacts; and the gap
ledger. Current HEAD must be measured anew.

| Historical stage | Independent result | Reconciliation with HEAD |
|---|---|---|
| original build | projected canon registry, 20 disputes, dialogue stance consumer and census | preserve registry/projector; old “nothing reads it” no longer describes HEAD |
| r1 critic | deleting register left provincial disagreements intact; both slavery voices were colonists; Method 4/secret review and freshness gaps | later rounds improved reachability but gap ledger still lists `GAP-W1-23-both-registered-voices-on-slavery-are-colonists`; require direct current closure, not inferred closure |
| r2 builder | expanded opposing voices/topics and name/library work | claims require r3/r4 independent evidence |
| r3 critic | FAIL 4/10; prose not principal gap; library reachability/names incomplete | r4 independently preserved prose quality while finding four of 45 loaded-room documents inert |
| r4 critic | FAIL 4/10: 163/163 referenced, but 4/45 interact-inert; 13/28 disputes inert; 8/337 names pin RI-LOR04 at band 0; 657 secret-leak hits unreviewed; RI-LOR03 taxonomy shortfalls and no valid blind prose reveal | this is latest independent verdict and starting red baseline; no later builder/critic verdict may be assumed |

## Predicate coverage map

| Governing predicate | Preserved independent evidence | Fresh evidence at HEAD | Remaining build/remediation |
|---|---|---|---|
| LOR03 all texts filed, structured, distributed, readable; length/taxonomy/quest-hint ratios and blind prose comparison | r4: 163/163 referenced, median 547, 21 intentionally wrong, 0 unfiled; 4/45 loaded-room interactions inert; 5/10 taxa short; quest-mechanism share 45/163=27.6% hard-fail region | exact full corpus census against every LOR03 threshold; walk every placed document, not sample; construct valid blind prose pack only if reference/reveal provenance exists | wire all inert placements; rebalance taxonomy and quest-hint ratio without deleting useful texts; establish legitimate comparison source/key or report unmeasurable per item |
| LOR04 legal and culturally attributable names, morphology, reuse/cross-culture/error bands | r4: 521 renames; reuse/slugs zero; cross-culture 125→14; blind 21/21; but 8/337 names produce band 0 | rerun validator over canonical population and current lexicon; exact violation categories/denominators; fresh blind 20+ nouns/names per protocol with leakcheck | repair the eight named failures and any current census failures in source roster/generator; do not weaken validator or item x-rules |
| LOR06 contradictions registered, both sides voiced and reachable, secrets controlled, adjudication absent where required | r4: 19 mutual pairs, zero dangling; 13/28 disputes inert; 657 hits untriaged; no adjudicator | Method 1–5 complete: registered/dangling scan, world placement and real conversation/book reachability, secret leakage triage, unregistered contradiction scan; verify slavery pair has materially opposed indigenous/colonist perspectives | add/wire missing world holders/topics/books; repair leaks or register/qualify intentional contradictions; diversify slavery voices if still colonist-only |
| MTH07 every registry/library/naming model consumed | r4 established 12/12 sampled books open and stance distinction; also exposed 4/45 and 13/28 inert | exhaustive all-document and all-dispute consumption, entity/UI observables, perturb/null/coupling; projector check against corpus | unify the two room placement mechanisms; connect every inert dispute/document; no assertion-only “placed” proof |

## Plan of execution

1. **Commit-stamped applicability ledger and census before edits.** Reuse `canon-census`,
   `canon-consumption`, `critic-w1-23-r1*`, `critic-w1-23-r3`, and r4 tools. For every native method,
   predicate and hard fail in all nine authoritative judges, record whether its native population
   intersects one of W1-23's nine owned paths. An intersecting or mixed predicate is mandatory;
   exclusion is allowed only when its complete native population is outside those paths, with the
   owning path/piece cited. Emit every fact, dispute side/holder/topic, book/text placement
   mechanism, interaction target, name/culture/category and exact applicable reference population.
   Do not use the r4 sample of 45 as denominator when the shipped population is larger. Any
   unclassified row, disputed ownership, or authority conflict stops before runtime work and goes
   to arbitration.
2. **Close reachability at the common seam.** Diagnose the parallel room placement/index mechanisms identified by r4. One canonical interaction index must see both; demonstrate each document by walking/pressing interact and each dialogue position through a naturally available speaker/topic. Preserve prose and registry content unless predicate failure requires edits.
3. **Repair eight-name and current validator failures in authored sources.** `build-canon --check` and name generator/validator must remain fail-closed. Do not special-case ids in the checker.
4. **Complete contradiction discipline.** Triage all 657 historical secret hits into true leak/false positive/intentional disclosed fact with file/line/reason; zero unreviewed. Resolve true leaks. Ensure every dispute has two materially distinct positions, distinct reachable holders, and no omniscient adjudicator unless the registry explicitly permits one. Specifically test slavery perspective gap.
5. **Meet LOR03 distribution rather than gaming counts.** Fix five short taxa and reduce quest-hint/mechanism share below the item hard fail using substantive non-hint works, not labels, splitting, or deleting good quest text. If a valid blind reference/reveal cannot be sourced from existing canonical artifacts, mark blind axis `not_run/unmeasurable`; builder must not judge its own pack.

## Acceptance contract

| ID | Predicate; units | Threshold / denominator | Comparison | Hard failures | Null/red/delete-fix | Reuse | Required evidence |
|---|---|---|---|---|---|---|---|
| L1 | readable placement; documents and percent | **100%** of every referenced/placed document opens through shipping interaction and renders its own correct text; denominator = all current placed document instances, not 12 or 45 | one browser, walk/teleport only to stage location then real interact, UI text hash/id cross-check | any inert/cross-talk/missing/duplicate-hidden instance; page error; denominator omission | keep prop but cut interaction index: target must remain visible and fail to open; restore opens; delete each placement adapter separately to expose parallel-mechanism defect | r4 library probe and harness interaction verbs | per-instance JSON: room, prop, source, mechanism, opened id/hash; trace, current commit |
| L2 | LOR03 structure/distribution; words, counts, percentages by taxon/mechanism | every current RI-LOR03 band-7 threshold; **quest-hint ratio ≤10%** (r4 45/163=27.6%); all taxonomy minima; zero unfiled | exact item census over all texts, substantive classification audited by fresh critic | >10% hints, any explicit item hard fail, relabel/split gaming, missing blind requirement treated as pass | perturb one filing/taxon and one hint label/source; checker goes red and names it | `critic-w1-23-r3`, book stats/census | row-level census, classification rationale, self-break logs |
| L3 | blind prose quality/provenance, preference rate | exact RI-LOR03 trial count, population and pass band; both artifacts in each trial; valid reference/reveal provenance | randomized paired pack, `blind/leakcheck`, fresh judge independent of author and pack builder | absent artifact/key, provenance-deciding tell, builder judgement, wrong question | shuffled reveal; leakcheck deliberate marker must fail; delete one artifact makes pack gate fail | blind tools and any valid LOR03 pack only after provenance audit | pack, sealed key, leak report, independent answers and score; otherwise explicit `not_run` and item consequence |
| L4 | name-rule violations; names and % per current culture/category | all RI-LOR04 band-7 thresholds and wave block: no axis below 3; denominator = every shipped proper name after specified title stripping; the known **8/337 becomes 0** unless population legitimately changes (both numbers reported) | official validator plus independent recomputation; exact morphology inventories | any prohibited category above threshold; exemption without lexicon authority; checker weakened | reinsert each known bad form and cross-culture form; validator nonzero/name; restore green | r4 name validator/tool, lexicon | complete violation ledger and exits |
| L5 | blind cultural attribution; correct choices/% | RI-LOR04 §6: ≥20 valid trials, ≥80% pass and target ≥90% for top band; cultures balanced as item specifies | blind nouns/names, randomized, fresh judge; distinguish culture not pipeline | invalid culture balance, leaked ids/orthographic marker-only pack, self-judgement | shuffled labels at chance; injected illegal bleed reduces score/check | r4 blind method + leakcheck | artifact-bearing pack, key, gate, judgement |
| L6 | contradiction integrity; pairs/counts/reachability | 100% registered pairs mutually linked; 0 dangling; **100% of every side/holder** naturally reachable; 0 unreviewed of historical/current secret-hit population; 0 unregistered material contradictions; all current Method 1–5 thresholds | static registry + live dialogue/book reachability + line-by-line secret triage | any inert side, leak, dangling, omniscient adjudicator forbidden by item, or unreviewed hit | delete register: stance/reachability behavior must change; cut one holder/topic gives red; inject a secret sentence and dangling id gives red | `canon-census --self-test`, r1 ablate/voices, r4 tools | registry report, all hit dispositions, live per-side trace, self-tests |
| L7 | slavery dispute perspectives; number/type/material difference | at least both registered sides reachable and not both colonist voices; denominator all holders/lines for that dispute; satisfy LOR06 opposition rules | content/holder provenance audit plus live conversations; compare claim propositions, not speaker labels | both sides same colonial position, indigenous token voice saying same proposition, unreachable holder | swap/remove indigenous holder or collapse propositions; test must red | r1 voices tool | quoted-short/proposition-coded evidence, holder metadata, live trace |
| L8 | world coupling; coupling ratio | every computed model in canon/library/naming harness: coupling >0, clean null; no skipped rows | RI-MTH07 §B–D; entity/UI state observable | zero coupling, hand-feed-only, trace-only, missing partial-harness VERIFY | ablate registry/index/model separately; beyond-topic/unrelated-book null unchanged | `canon-consumption`, r1 ablate, library tool | exhaustive `world_coupling` artifact with call sites/frame/entity/UI |
| L9 | aggregate integrity | all individual checks and `build-canon --check`, data/content/quest/boot gates together green at same commit | aggregate after all repairs | stale generated canon, check omitted, commit mismatch | perturb source canon without projection: `--check` red; restore and rerun | existing gates | exact commands/exits/full logs |
| L10 | complete authoritative-judge coverage | 100% of native methods/predicates/hard fails in all nine metadata judges classified; 100% of applicable rows executed at their item-native threshold, population and comparison method; 0 unresolved or silently excluded rows | owned-path applicability ledger, with mixed predicates mandatory | omitted judge/method; exclusion based only on another piece sharing the predicate; unresolved authority treated green | delete one ledger row and misclassify one known lore-owned row: completeness gate must fail | item-native tools plus the census in step 1; never fork an established instrument | stamped ledger with item section, owned path, population, threshold, method, hard fail, tool/evidence and result |

## Remediation: mandatory native-method matrix

The following one-to-one matrix supersedes shorthand in L2/L3/L5/L6. Run static corpus gates first; stop and repair on a hard fail before browser or blind work. A single row-level census may feed several methods, but every native predicate and control below must be reported at one stamped commit.

| Method | Native population and acceptance | Deliberate red / evidence |
|---|---|---|
| LOR03 M1 | Every readable in `game/data/books/`; publish p10/p25/median/p75/p90. Median ≥350, p90 ≥1,200, ≥3 multi-volume series, ≤20% under 150, ≥40 total. | Shorten enough copied books to cross median/p90; `book-stats.py` must fail. |
| LOR03 M2 | Exactly 8 unlabelled full texts: 4 shipped books matched by taxon/length to the four item-named Morrowind books. Fresh critic performs the native “which world?” ranking before reveal; distinct clustering better than chance fails and its tell is recorded. | Inject provenance marker and require leakcheck red; builder/packer never judges. |
| LOR03 M3 | Predeclared seeded sample of 12 shipped books; fresh critic states author, want, and wrong belief for each; ≥3 unanswered “wrong” fails. | Replace three sampled author perspectives with omniscient text; scorer must fail. |
| LOR03 M4 | Every `disputed:true` registry entry: ≥2 named in-world books that actually exist; ≥8 actually written pairs. | Delete one named book and enough pair links to cross 8; census red. |
| LOR03 M5 | Every book against active quest objectives; hint_books/total ≤10%; no primary-lore-vector hard fail. | Mark/text-match enough objectives to exceed 10%; gate red. |
| LOR03 M6 | Whole corpus sentence-length, type-token, imperative, first-person metrics; mean-sentence-length IQR ≥6 words (≥10 for native score 5). | Normalize sentence lengths in a copied corpus; IQR gate red. |
| LOR03 M7 | Whole corpus authorship census: ≥14 Argonian-authored target, hard fail below 10. | Relabel enough authors below 10; gate red. |
| LOR04 M1–M5 | Harvest every actual proper-name string; validator ≤5%, Argonian culture-forced zero violations/apostrophes, unknown ≤2%, Jel x-density ≥40% and long-vowel ≥20%. Preserve map spelling CF-088. | Reinsert a known bad name, apostrophe, unknown, and flavourless batch separately; appropriate gate red. |
| LOR04 M6 | Exactly 20 context-free **actual proper-name strings** from the game, culture-balanced per the item; fresh critic assigns culture. ≥80% pass, ≥90% top band. The strings themselves are the judged artifacts—no screenshots, provenance pairing, or different question. | Shuffle culture key to chance and inject illegal cultural bleed; scorer/validator red. |
| LOR04 M7–M8 | Whole UI/dialogue coined-term grep has zero out-of-world terms; after any lexicon edit, item 34-name regression corpus remains 0.0% violations. | Inject `bonfire` and mutate one regression name; each gate red. |
| LOR06 M1 | Registry plus the authoritative books manifest passes `canon-check.py --validate-registry`; missing `game/data/books/manifest.json` is a failure to repair, not a waived phantom. | Add dangling side/book reference; exit 1. |
| LOR06 M2 | Strict claims sweep over all lore-bearing `game/data/**/*.json`; every secret hit dispositioned with file/line/reason, zero unreviewed. | Inject a pre-discovery secret sentence; sweep red. |
| LOR06 M3 | Predeclared ≥30 triage-hit sample weighted to hard-canon/disputed; apply §3 decision procedure and report PASS/FAIL-canon/FAIL-unregistered/NEW-LORE. Any hard-canon contradiction is automatic fail. | Inject one hard-canon contradiction; adjudication red. |
| LOR06 M4 | Every dispute side checked in shipped books/NPC dialogue: ≥8 fully voiced; no position voiced zero times. | Cut one holder/topic and enough voiced pairs below 8; live/static reachability red. |
| LOR06 M5 | Review every native adjudicator-hunt hit; zero narrator/journal adjudications of a registered dispute. | Insert “the truth is” resolution; gate plus human disposition fails. |
| LOR06 M6 | Whole shipped lore corpus checked specifically against CF-D002; it remains unanswered, severity-high automatic fail otherwise. | Add a candidate answer; open-question guard red. |
| LOR06 M7 | Seeded predeclared sample of 10 CF-C facts; no source presents one in established-scholarship/consensus register. | Rewrite one sampled invention as consensus; opacity scorer red. |
| LOR06 M8 | Diff current `canon-facts.json` against the canonical prior-wave snapshot identified by the latest accepted pre-W1-23 registry commit (`git log --invert-grep --grep=Orchestrator-Bank -- corpus/.../canon-facts.json`); no constructed→hard-canon tier move or post-publication `authorially_true` change. Record both commits. | Mutate one copied tier and truth field; drift gate red. |

Hard-canon contradiction, CF-D002 answer, narrator/journal adjudication, or CF-C-as-established-canon is an automatic LOR06 fail. Reuse the existing census, projector, library interaction, name validator and blind/leak tooling; create no parallel registry or prose pipeline.

## Adversarial self-attack

**Could exact execution pass while failing a governing item?** A weaker plan could sample 12 books, fix eight names, and still leave four inert documents, thirteen inert disputes, 657 unreviewed leaks, invalid LOR03 distribution, or a blind test with no valid reference. L1/L6 use exhaustive populations, L2 binds every structural threshold/hard fail, L3 refuses fabricated blind closure, L4/L5 cover both mechanical and perceptual name axes, L7 protects the still-open slavery predicate, and L8 proves gameplay consumption. Thus no historical 4/10 is waved away and no prose is rewritten merely because old work is large.

## Critic brief

Try to find hidden denominators, two placement systems, label-based taxonomy gaming, unreviewed secret hits, invalid blind provenance, same-position “opposition,” checker exceptions, and direct model calls masquerading as consumption. State remains **awaiting-recriticism**.


## Independent plan critique — BLOCKING / awaiting-remediation (2026-08-09)

The falsification proposition does **not** hold. L1–L9 can all be reported green while mandatory
native methods remain unexecuted.

**Biggest actionable gap:** make LOR03 methods 1–7 and LOR06 methods 1–8 explicit acceptance rows
(or an equally explicit one-to-one matrix), with their exact populations, sampling rules, predicates,
hard fails, evidence, and controls. The present shorthand does not bind successful execution to them:

- L2's structure/distribution census and L3's paired pack do not require LOR03 method 3's 12-book
  author-voice sample (“who, want, wrong,” failing at three unanswered), method 4's ≥8 actually
  written contradiction pairs, or method 6's sentence-length-IQR register-variance computation.
  A corpus can therefore pass the listed taxonomy, hint, and blind checks while remaining an
  omniscient single voice. Preserve method 1's exact length-distribution hard fails and method 7's
  Argonian-author floor explicitly too; “every threshold” plus a row census is not the required
  comparison method.
- L6 requires only “Methods 1–5.” RI-LOR06 methods 6–8 are independent hard gates: CF-D002 must
  remain unanswered, 10 sampled CF-C inventions must not be presented as established scholarship,
  and cross-wave registry drift/retconning must be checked. Those can all fail while L6 is green.
  The repair must name the canonical comparison base for the drift check and retain the item’s
  hard-canon/constructed/adjudicator automatic failures.
- L5 incorrectly adds an “artifact-bearing” pack requirement without defining the item-native
  artifact (RI-LOR04 asks for 20 context-free proper nouns). Clarify that the judged artifacts are
  the actual name strings, use exactly the item’s culture-attribution question, and do not import
  LOR03 provenance mechanics in a way that substitutes a different task.

Add deliberate red cases that cause the author-voice, register-IQR, open-question, invention-opacity,
and drift instruments themselves to fail; then run these with L1–L9 at one stamped commit. These are
planning-resolvable governing gates, so they cannot be CARRIED.


## Remediation outcome (2026-08-09)

All recorded blocking findings above are closed by the mandatory native-method matrix. The underlying item text and applicable amendments, not the critic wording, govern conflicts. No new defect catalogue was attempted. **Plan-State: awaiting-recriticism**; only a fresh reviewer-editor may mark this plan satisfied.

Plan-State: awaiting-recriticism

## Fresh reviewer-editor outcome — 2026-08-09

**MATERIALLY EDITED — awaiting recriticism.** The received plan summarized only three of the nine
authoritative metadata judges, so exact green execution could still fail CHR03, LOR01/02/05/07 or
QST06 on W1-23-owned canon, naming, religion, geography, history, faction, prophecy or metaphysics
populations. The authority list and a fail-closed, owned-path applicability/coverage gate now close
that route without requiring whole-item work belonging entirely to another piece. A different fresh
reviewer-editor must judge this version.
