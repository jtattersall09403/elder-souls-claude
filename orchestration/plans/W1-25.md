# CURRENT-STATE CONTINUATION PLAN — W1-25: experience instruments and seam crossings

Plan-State: awaiting-criticism

Planning only. State reconstructed from canonical `50821cb25dadbe253c2f6394118d16fb6baaa2b9`.

## 1. History, verdicts and current truth

Round 1 built the sabotage facility, generated RI-CMP01/RI-EXP06 instrument data, static breakage
checks and anecdote/session tools. The independent verdict was **FAIL 2/10 on every axis**. It
validated much of the honesty, but found optional support, missing-measurement misclassification,
taxonomy collisions, untracked replay inputs, a scanner undercount (4/41 was false), an inert
still-session fixture, zero demonstrated crossings, no recall protocol, and static “probes” that
performed no running breakage.

Round 2 builder claims to close the facility verdict acceptance: mandatory support/unit,
`NO_MEASUREMENT`/`SHORT_CIRCUIT`, MASKED precedence, all-arm agreement, tracked SHA-256 replay twins,
14 self-tests/10 facility breaks/10 cases, matrix-scan 8/41, an active fixture, and one external
adopter. These claims have **no r2 verdict** and are not independent proof. Current offline checks
confirm only that `lib/sabotage.mjs --self-test` exits 0 and `matrix-scan.mjs` still reports 8/41,
33 no-data crossings, only 8 rules covering either end, and explicitly “STAGE 1 ONLY; none
demonstrated.” The builder also states: matrix probe never ran; verified anecdotes/hour remains
unmeasurable; SAB-P matched A/B and SAB-E absent; RI-EXP03 contracts absent; RI-EXP06 remains static.

Preserve the existing instrument architecture and tracked replay corpus if fresh criticism confirms
it: `tools/experience/lib/sabotage.mjs`, its callers/cases/corpus, `anecdote-{trace,verify}.mjs`,
`event-histogram.mjs`, `breakage-probe.mjs`, `pbrule-audit.mjs`, and
`tools/composition/{cells-from-md,matrix-scan,matrix-probe}.mjs`. Do not confuse tool adoption with
the four governing experience outcomes.

## 2. Governing requirements: proof, verification, and build

| Governing item | Current classification | Required work |
|---|---|---|
| **RI-CMP01 seam matrix** | **Concrete build + fresh verification.** The generated 342-cell/206 non-none matrix was independently recounted in r1, but qualitative crossing rulings were disputed; current scanner shows only data claims and zero demonstrated crossings. | Preserve generated totals only after re-derivation from the current item. Pre-rule every ambiguous crossing. Demonstrate at least the Wave-1 floor of **4 distinct declared crossing cells**, in both directions where the item requires, through live A/B forks; then run the complete matrix score/coverage method. A data claim, source grep, harness flag set or self-report is not a demonstration. |
| **RI-EXP02 anecdote/recall** | **Concrete build.** SAB-P’s content filter separated, but no compliant played chain, isolated recall, verification, class floors, blind pair or verified anecdotes/hour exists. Active fixture found 62 candidates and 0 tellable/proper nouns. | Build/use the item’s actual session and baton protocol, `recall-run.mjs`, verifier and leak-gated blind pair. Measure exact `verified_anecdotes_per_hour = verified − unverified`, T1–T5 floors, arose fraction, generic fraction, confabulation rate and mean hops. Hard fail below 1.5/hour remains; capacity/tellable moments cannot substitute. |
| **RI-EXP03 session shape** | **Concrete build.** A zero-event fixture and an active synthetic fixture do not establish a 90-minute session curve. SAG/SH/SS/stop instruments and SAB-E are absent. | Run matched ≥90-minute played sessions; implement the exact classes, sag fit, shape invariants, beats and stop-point methods; run encounter-cloning SAB-E and the matched SAB-P on the scored outcome. Every hard fail and tier rule applies; fragment/unmeasurable cannot pass. |
| **RI-EXP06 permissiveness durability** | **Concrete build.** Static absence-of-closure checks and a single in-memory demo are weaker than the required live per-probe breaks; chain cross-check is null. | Execute every current register probe in the running build, performing its declared breakage and observing player-facing closure; compare against frozen baseline; run playthrough chain cross-check and PB-rule audit at the item’s correct wave scope. `probes_run_fraction` is performed/declared, never parser-found/declared. |

## 3. Acceptance contracts

### A. Control facility gate (prerequisite, not a governing-axis substitute)

Fresh critic reruns `critic-w1-25.mjs --a --b`, existing self-tests, all replay cases and corpus
verification. Exact r1 predicates remain: A1/A3 `ERROR|NO_MEASUREMENT`; A4 `NO_MEASUREMENT`;
B-COLLIDE-1 `MASKED`; B-COLLIDE-2 not `INERT`; B-COLLIDE-4 throws; 14/14 cases and 10/10 injected
breaks (or current larger declared populations) green. Additionally classify the known W1-14
short-circuit under both honest support readings, print every concurrent failure, reject missing
unit/support, and verify at least two external callers. Any expected verdict collision, absent
artifact, unreadable SHA origin, or injected break staying green hard-fails all W1-25 evidence.

### B. RI-CMP01

Generate a manifest of all 342 off-diagonal cells and freeze the qualitative decisions before live
results. For each tested crossing, use the item’s declared source/target systems and a real world
consumer. Minimum passing Wave-1 population: **≥4 distinct declared crossing cells**, not four
fixtures of one cell. Each fixture has matched baseline and one-factor perturbation, non-zero
entities/support, and prints source state, target observable, entity ids and time series. Required:
baseline versus perturbation differs in the predicted target predicate; unrelated target fields
stay equal; deleting the consumer restores baseline; no harness direct target mutation. Run
`matrix-probe.mjs` live and aggregate with `cells-from-md`/`matrix-scan`; discrepancies are errors,
not majority votes. The full RI-CMP01 score and both hard-fail conditions must clear, not merely the
Wave-1 count.

### C. RI-EXP02

Population: the reference item’s complete played-session chain, with session duration and player
fixed before recording, followed by its 500-word baton chain using isolated agents. The pack must
contain the actual text/trace excerpts under judgement, be leak-gated before judges, and preserve
answer keys separately. Compute all required denominators and confidence/comparison methods.
Acceptance: every RI-EXP02 metric at its governing threshold, including
`verified_anecdotes_per_hour ≥ 1.5`; every T1–T5 minimum; confabulation/generic/arose/hops bounds;
blind comparison at the item’s threshold. A recalled item counts only if independently located in
the trace and verified; unverified recollections subtract. Zero events/names, empty classes,
builder-as-judge, or a feature-only pack hard-fails.

### D. RI-EXP03

Use at least the item’s matched 90-minute A/B population and exact event classes. Collect real
interaction, combat, travel, dialogue, reading, trade/menu and consequences—never inject events to
fill bins. Run `classes-from-md`, `sag-fit`, `shape-check`, `session-beats`, event histogram and
stop-point method; require all SAG-1…7, SH1…6 and SS1…6 thresholds and every hard fail. Unknown
events remain unclassified and count exactly as the item prescribes. SAB-P truncates prose in one
matched arm; SAB-E clones encounters in another, with seeds/start state/player/actions held fixed.
Each sabotage must worsen the governed outcome by its declared margin, not merely change event
counts.

### E. RI-EXP06

Freeze the current register (currently 15 entries/40 assertions historically; re-derive current
counts). For **100%** of live entries, start from a fixture where permissiveness is visibly usable,
perform the exact `Red if` closure in a scratch build, then repeat the same action and observe the
named player-facing refusal/loss. Require baseline open, mutation executed, mutated red and restore
open. Unavailable substrate is `UNMEASURABLE` and fails `probes_run_fraction`; it is not static
PASS. Run at least one uninterrupted playthrough that exercises every probe eligible for the
item’s chain check and report isolated-vs-chain discrepancies. Apply the PB-rule denominator and
hard fails exactly as the governing text resolves its wave scope.

## 4. Null/delete controls and evidence

Every acceptance has a seen-red arm:

* sabotage facility: omit support/unit, return null for every arm, hide a joint guard, cancel two
  factors, collapse support after a short circuit, flip/delete a replay byte;
* matrix: delete each consumer, perturb an irrelevant source (must stay equal), and directly mutate
  target in a fenced self-test (must be rejected);
* recall: replace the session with the known 36,000-frame zero-event trace; redact proper nouns;
  inject one unverifiable recollection; leak an answer-key feature—the appropriate metric/gate reds;
* session: clone encounters and truncate prose separately; substitute an idle session and an empty
  event register; no arm may pass on zero classified support;
* permissiveness: for each entry perform its own closure, plus delete one probe and one baseline
  record so completeness checks red.

For every code remediation, delete the fix on an isolated copy and require the old exact failure to
return. Assert changed hashes and executed lines; for dual guards run a 2×2. Evidence: commands and
exit codes, raw JSON/JSONL, frozen population manifests, live traces, packs/judgements, SHA-256
origins, tested commit, browser/load facts, and explicit deviations. Run the four governing tools
together after individual passes. Builder/round-2 claims are preserved as history only until a
fresh critic independently reruns them.

## 5. Dependencies and completeness challenge

W1-25 measures other systems; it does not own their game fixes. A red crossing or permissiveness
probe may be remediated by the owning piece, but remains red here until demonstrated. Missing event
payloads belong to the harness and missing boss/roster content to their pieces; neither silently
shrinks an RI-EXP02/03/06 population.

**Could a competent builder follow this plan exactly, get every planned check green, and still
fail any governing reference-item bar?** No: all four complete governing methods and hard fails are
conjunctive, the facility is only a prerequisite, static claims cannot substitute for live
demonstrations, and independent recall/blind judgement cannot be self-awarded. Any conflict between
an implemented tool and current reference text blocks the plan and returns it to criticism.

