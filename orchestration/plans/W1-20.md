# W1-20 — current-state continuation plan

Plan-State: satisfied
Canonical-HEAD: `1c4c487d` (reconstruct and restamp before building)

## 0. Authority, history, and corrections

`docs/PLAN.md` §3 W1-20 owns `quests.faction.joining` and `quests.faction.rankgating`, governed by **RI-CRM02, RI-LOR01, RI-LOR02, RI-QST01 and RI-QST03**. The plan's “every line present, one quest deep” describes sequencing, but it does not repeal a governing reference-item hard fail. The reference items are the bar.

The builder added joining quests for Rootkeepers, Ixtu-Vakh, Drowned Court and Dockhands; live voiced refusals; crime standing rows; and rank-consumption probes. The independent `W1-20-r1` verdict is **FAIL 2/10**. Established findings: three carried lines have 18 quests/2.25 per rank/zero empty bands; the other five have 1–4 quests and 6–7 empty bands; Dockhands has no deceit; four joining quests pay 20 reputation despite the builder asserting 10; all joinable ladders exceed RI-QST03's 1.6× slack warning; only three lines have discipline; most have no faction interior; two recruiters are not reachable from four approaches. The verdict also corrects stale assertions: `deep_kin` is observably distinct from Xul-Aneekh in runtime, and rank does reach dialogue when the control actually changes derived rank.

Preserve: dynamic voiced thresholds, joining width for seven lines, exclusivity/standing/law consumers, wrap behavior (but HUD instrument generalisation belongs to W1-HUD-TOAST), and the three already-deep lines. Builder claims about 10 reputation, deep_kin aliasing and universal reachability are disproven. Do not expand the three green quest lines merely to touch them. Explicit seams: property interiors belong physically to W1-04/stealth systems, prose rendering to W1-HUD-TOAST/W1-21, and Q-MAG-07 owns a deep_kin reference; none may silently erase faction acceptance.

## 1. Complete governing acceptance contract

Create a row for **every method and hard fail** in the five items, with exact native population/unit/comparator/threshold and fail-closed result. The predicates below are mandatory clarifications; stricter native clauses win.

### A. RI-QST01 faction escalation

1. Population is **all eight ladder ids** currently shipped, including `deep_kin` until canonical data truly merges/deletes it without orphaning Q-MAG-07. Per line: **at least 18 faction quests**, **mean ≥2.0 quests per each of 8 ranks**, and **zero empty rank bands**, using the item's exact quest attribution and rank assignment method. Do not average rich lines over empty ones.
2. Meet every Table A/B/C texture/escalation predicate at the specified ranks and populations. In particular every line must contain deceit, and Dockhands must have `deceit != null` at rank ≤3. Run all native motif, escalation, honesty, consequence and reuse thresholds; a renamed duplicate quest does not count as variety.
3. Joining is playable in the faction's own voice for every joinable body; escalation continues through ranks 1–7 rather than piling a second quest at rank 1. Every quest has discoverable opening, reachable giver/location, at least the native resolution count, journal states and consequences.
4. All RI-QST01 listed hard fails cap the item exactly as specified. Current volume and Dockhands honesty failures must both be absent; `critic-w1-20-census.mjs` must exit 0 but is not allowed to replace any additional native method.

### B. RI-QST03 rank gating and arithmetic

5. For every faction/rank row, gates use the exact reputation, attribute, two-skill and world-state requirements in the governing ladder. Test one below, exactly at, and one above each threshold; below refuses with all unmet terms, at/above admits when all others meet. No level substitution.
6. Each quest consequence pays faction reputation exactly once. The four joining quests must net **10**, not the current doubled 20, and every other delta must remain inside RI-QST03's native `+4..+10` per-quest band unless the item explicitly defines otherwise.
7. Completability: for every joinable line, legally earnable reputation reaches rank 7; no required flag/skill/giver is impossible. Slack ratio `earnable/112` is **≤1.6×** on each line (and satisfies any native lower bound), computed after deduplicating double-applied consequences. Publish numerator and denominator per line.
8. Refusal census covers all **six** authored kinds—reputation, attribute, skill_1, skill_2, world_state, welcome—for all seven joinable lines. Sentences dynamically state the player's value and required threshold from the live gate; no hardcoded drift. Each is heard via real play, not merely returned by a harness method.

### C. RI-CRM02 discipline/crime and live ownership

9. Every joinable line has the native crime-standing mapping, expulsion trigger/flag, readmission `price_gold`, reputation floor and reachable `offered_by`. Exercise offence→expulsion→refusal→payment/reputation recovery→readmission end to end and meet all RI-CRM02 thresholds/hard fails.
10. Every joinable faction has a reachable faction property/interior sufficient to make theft and trespass classification non-null. Perturb rank on the same live object/zone and observe the required ownership/trespass/law response. A harness-only `priceQuote` or `arrestTopics` caller is not consumption.
11. Rival/exclusivity edges change admission/standing exactly once and remain lore-consistent. Deep Kin must be resolved canonically: either a genuinely separate eighth playable line meeting the whole contract, or one canonical id with data migration and no orphaned quest/reaction/standing reference. A spoken assertion of aliasing cannot override divergent runtime ledgers.

### D. RI-LOR01 and RI-LOR02

12. Census every faction id, name, rank, leader, location, allegiance, rivalry, quest premise and refusal against both governing dossiers. Zero unresolved or contradictory entities; era/political relations match the exact item facts. Run the native comparison methods and hard fails, not an informal lore review.
13. Quest prose and consequences preserve each line's distinct institutional voice and politics; mechanical reuse must not collapse factions into reskins. Blind comparison is required wherever either item specifies it, with the actual prose/artifact and a fresh judge.

### E. Reachability and preserved UI seam

14. Walk to every recruiter, quest giver, discipline/readmission giver and required faction interior from four valid approach bearings. Acceptance: all arrive inside the actual interaction radius without teleport, zero stuck/abort. Diagnose and repair cutter-neeth and npc-porter-eeja rather than accepting `travelToGiver()` presence.
15. Preserve W1-20's voiced-threshold and wrapped-toast behavior through the independent text-register/rect instrument. Do not edit HUD under this piece unless the W1-HUD-TOAST dependency explicitly hands it over. A missing `widest_px` must fail closed, but closure of the generalized coupled-yardstick problem is not falsely claimed here.

## 2. Controls and evidence

Reuse `critic-w1-20-census.mjs`, `play.mjs`, `ledgers.mjs`, `reach.mjs`, `wraptest.mjs`, the builder joining probe, and every item-native tool. Reports must be regenerated at current HEAD and stamped.

* **Red:** delete one rank-band quest, clear Dockhands deceit, double one reputation award, remove one discipline row, break one property zone, falsify one lore relation, and make one giver unreachable in isolated scratch arms. Each associated row must fail.
* **Null:** rank below/at/above; faction member/non-member; property owner/non-owner; pre/post expulsion; sufficient/insufficient gold and reputation; reachable/unreachable approach. Positive controls must first move the expected observable.
* **Delete-the-fix:** remove only the new per-rank quest content: the five shallow lines must regress while the three carried 18-quest lines stay green (proves teardown is neither inert nor blunt). Separately remove reputation deduplication and require net reward to return 20; remove one discipline/property repair and require that line's end-to-end consumer to go null/red.
* **Aggregation:** execute all five item matrices and all faction lines together, then global data/quest/content/prose/corpus gates. Overall uses native hard-fail caps and minimum governing axis; no wave-sequencing rationale may average away RI-QST01.

## 3. Ordered build plan

1. Freeze HEAD and rerun the independent census/ledgers/play/reach tools; record established versus stale rows.
2. Fix arithmetic and canonical identity first (10 reputation exactly; deep_kin decision without orphaning Q-MAG-07), because both alter denominators/populations.
3. Author only missing rank-band content and texture required by RI-QST01; preserve the three complete lines.
4. Add missing discipline/readmission and physical property seams, then prove every giver/property reachable and consumed in play.
5. Run full lore/gating/refusal/quest/crime matrices, adversarial arms, delete-fix, aggregation and global gates. Hand to a fresh critic; the authored plan itself remains awaiting criticism.

## 4. Completeness audit

The plan cannot go green with one quest per line but empty ladders, doubled reputation, a fake alias, harness-only faction effects, unreachable recruiters, absent halls, or dossier contradictions. Every governing method and hard fail remains an explicit fail-closed matrix row regardless of cross-piece ownership.


## Independent plan critique — 2026-08-09

**Verdict: SATISFIED.** I independently reconstructed the two assigned paths, all five governing
items, RI-QST01's native scoring/hard fails, RI-QST03's full gate/exclusivity/expulsion contract,
RI-CRM02, both lore dossiers, and the W1-20-r1 evidence. The plan does not adopt the original
“one quest deep” interpretation over the reference bar: it treats every native method and hard fail
as binding and explicitly keeps the eight runtime ladder ids in the denominator until Deep Kin is
canonically migrated or built as a full separate line.

No green laundering route remains. Quest volume and escalation are per line rather than averaged;
all rank bands, task/stakes/deceit/corruption/rival/succession predicates and blind comparisons stay
in scope; gate thresholds are tested below/at/above with all other terms controlled; reputation is
deduplicated and checked for legal earnability; exclusivity, expulsion, readmission, ownership and
law are exercised through live consumers; every required person and property has four-bearing body
reachability; and lore uses native censuses rather than informal review. The delete-fix design is
non-blunt (five repaired lines regress while three carried lines stay green) and separately restores
the doubled reward and missing discipline/property failures. Native aggregation and hard-fail caps
prevent the plan's stricter 18-quest and 1.6x constraints from weakening any reference predicate.

**Recommendation:** plan satisfied; dispatch the continuation builder. No pre-build requirement is
being carried into implementation.
