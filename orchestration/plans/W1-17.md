# PLAN — W1-17: dialogue, the topic graph, and the people using it

Plan-State: satisfied

Planning-only continuation plan reconstructed at HEAD `50821cb2`; no game source was changed. The governing bar is the nineteen-item set in `docs/PLAN.md`, not the narrower round-1 verdict sample.

## Builder/critic execution allocation (binding)

This piece uses the shared [builder execution contract](BUILDER-EXECUTION-CONTRACT.md). The original
native predicates and populations below remain the final critic acceptance bar.

**Builder-owned:** finish the shipped-reader ordering and merge-order work; graph reachability and
answer provenance; placed-actor/register availability; live consumption of `res`, `f`, rumours and
other authored reader fields; Act-V routing; and dialogue-owned parley seams. Run the complete cheap
graph, topic, ordering and actor census. Exercise every newly connected field through its running-world
consumer, plus representative rumour, quest and parley paths. Run the named targeted red seeds and
delete-the-fix controls. For RI-DLG07, deliver only the complete stamped candidate corpus and prove
that every eligible category pool is large enough for the native comparison.

**Critic-owned:** the complete live reach and item-native dialogue aggregate; construction,
normalisation, randomisation and sealing of the RI-DLG07 pack; fresh context-free judging,
calibration, mandatory harsher rerun and any void-run rebuild; and final scoring and verdict.

**Completion rule:** a builder continues through all available work in the builder paragraph in one
run. RI-DLG07 judgement is an explicit critic handoff and cannot make builder delivery incomplete.

## 1. History and current state

The original build created a 468-node, nine-root topic graph, settlement rumours, disposition/persuasion and six voice registers. Round 1 (`W1-17-r1`) found graph hard fails (orphans and 133 unreachable INFOs), a mouthless magister, incomplete voice instrumentation, dead `res` and `f` fields, and an unresolved RI-DLG09 conflict. Later work reduced/reclassified reachability, added the Act-V argument, added provenance/collision checks, and implemented S37 first-admissible authored order. S37 invalidates the old scoring-reader reach instruments and the published `136 -> 6` claim; it also requires declared cross-file merge order. The Act-V critic found the old `50-mainline` response still wins for three signatures; round 2 fixed detection/provenance, not that hard fail. `NEXT-DISPATCH` Q6 and T remain binding: retake reach against the shipped reader; `res` still needs a consumer. The library was independently expanded and must be measured, not rebuilt.

Dependencies: W1-09 owns the parley implementation but RI-DLG09 owns its numbers; W1-18 owns quest/journal machinery consumed by `res`; W1-04 owns population/placement; W1-23 owns canon. Do not duplicate their systems. S37 governs selection; ARBITRATION §3 and RI-MTH07 govern consumption.

## 2. Governing-item reconciliation

| item | independent evidence already establishes | re-verify at HEAD | necessary continuation only |
|---|---|---|---|
| RI-DLG01 | topology mostly met; prior reach counts are stale; Act-V collision is real | run the item §D graph and first-match reader over shipped manifest/order; provenance-test every answer | declare merge order; eliminate all item-defined orphans/unreachable INFOs and Act-V wrong-source answers; consume `res`/AddTopic |
| RI-DLG02 | NPC/dialogue records exist | execute every § comparison over the full NPC population | remediate only failing identity/availability rows |
| RI-DLG03 | tier counts, locality and live act gates previously passed | rerun exact tier/Jaccard/diegetic method with the complete act flag sequence | correct any current misses; do not rewrite passing rumours |
| RI-DLG04 | 100k oracle comparison once had zero mismatch | rerun all 22 GMST/formula, distribution and band tests | only current formula/distribution misses |
| RI-DLG06 | global spread passed; tool omitted declared lexicon/type-breaker fields; genuine rhythm and mouthless-register misses remain | repair instrument to read every §B field, then run six complete registers from placed speakers | create/alias placed speakers and repair only genuine metric misses; never tune to the broken tool |
| RI-DLG07, RI-DLG08 | no complete standing verdict | run their item-native comparison methods, populations and hard fails | build only measured gaps |
| RI-DLG09 | shipped parley historically 78f/0 stamina/4 routes versus binding 102f/20/5 | run native frame trace at 60 Hz including cancel, commit, refusal and persistent yield | coordinate W1-09-owned data change to exact RI-DLG09 values if still failing |
| RI-CHR02, RI-PRG03 | partial cross-piece evidence exists | run only their dialogue-owned race/reaction/service clauses | repair dialogue clauses only |
| RI-LOR01/02/03/05/06 | library and canon expanded after original work | run each native lore census/comparison over current books and spoken corpus | closure if bars pass; otherwise content strictly matching failed rows |
| RI-QST02/05/07 | quest corpus and hooks changed materially | execute dialogue-owned discovery, solution and giver clauses through the running reader | repair only measured dialogue seam failures |
| RI-WLD03 | settlements/population changed | run native settlement-specific speech/identity comparison on placed NPCs | repair only dialogue-owned misses |
| RI-MTH07 | positive controls moved answers; `res` and `f` moved none | perturb every authored field and observe world-side consequence, including rumours, `res`, `f` | carry `res` through reader and execute its journal/AddTopic effects; implement faction filter if still dead |

## 3. Acceptance contract

The builder must produce one row per requirement in all nineteen governing items, preserving each item's native predicate, unit, threshold and hard-fail rule. The following decisive contract is additional bookkeeping, not a replacement.

| id | exact predicate / population / comparison | acceptance |
|---|---|---|
| A1 graph | RI-DLG01 §D over every shipped topic/INFO loaded in declared manifest order; compare exact graph statistics to §D | every threshold met; **0 item-defined orphans, 0 unreachable INFOs, no hard fail** |
| A2 reader/order | for every `(topic, speaker, distinct admissibility class)`, shipped `infoFor` response id/source equals the first admissible INFO in declared authored order | 100%; `arbiter-order-divergence --gate` 0 mismatches |
| A3 reach | enumerate all placed speakers × canonical player classes using the shipped reader, not a model | no absent actor; item/gap bar `<=3` unreachable INFOs and no `no such actor` bucket; every six-register archetype has nonzero placed speech |
| A4 Act V | eleven documented signatures plus every Q-MAIN-26 entry/topic path; compare returned `said_from` and id to `main-quest-argument.json` | 100% intended provenance; 0 legacy stub wins; all knowledge/disposition predicates exact |
| A5 voice | RI-DLG06 full §B matrix, six archetypes, placed-speaker utterances only | every band and all six separation requirements pass; tool reads `second_regex`, lexicon/lift, type-breakers and address limits |
| A6 rumours | all settlements and required act states using exact RI-DLG03 flag sequence | all tier minima; town-name ≥70%; cross-town Jaccard <0.15; act Jaccard ≤0.70; lint 0 |
| A7 parley | native 60 Hz trace over every route and refusal/yield path | 102 total frames, free cancel 1–12, commit/stamina 20 at 13, event 43, recovery through 102, K>F>R>G>D, all persistence clauses |
| A8 consumption | perturb each authored reader/output field independently in a scratch copy and observe a world consumer | every field changes its named consumer; `res` changes journal/topic knowledge and `f` changes eligible response; no zero-effect field |
| A9 builder aggregate | all builder-admissible native rows from the nineteen items plus standard content gates at one stamped commit; RI-DLG07 is explicitly excluded | no governing hard fail in builder-owned evidence; every builder-admissible owned item/axis ≥ Wave-1 gate 7.0 under its native aggregation; RI-DLG07 remains `not_run`, never green, pending A10 |
| A10 RI-DLG07 independent blind gate | builder supplies the stamped complete candidate corpus and proves each of the six category pools has ≥3 eligible, extractable, length-matchable candidates; a separately dispatched critic assembles the 18 random pairs, normalises/randomises and seals the key, then a fresh context-free judge receives only the §B pack/instruction | native RI-DLG07 metrics and ladder threshold pass with no automatic fail; calibration, reveal and mandatory harsher rerun are applied, and any void run is rebuilt by a different eligible agent |

Hard fails: any item-native hard fail; filesystem-derived undeclared order; wrong-source Act-V answer; a model without a demonstrated consumer; unplaced register; proxy thresholds; unmeasured scored green.

Controls and evidence: each instrument publishes commit, fixture enumeration, raw rows and summary. Seed one orphan, one shadowed first-match INFO, one wrong Act-V source, one absent actor, one dead `res`, one broken rumour gate and one off-by-one parley frame; each must turn its corresponding check red. Delete each remediation on a scratch worktree and recover the old failure. For consumption, use paired positive/null arms and assert the arms differ. Reusable instruments are the shipped graph/order/reach/voice/rumour/parley/quest tools after removing S37-stale reader models; missing item-native tools must be built, never stubbed.

## 4. Build order and closure decision

1. Stamp HEAD; inventory all nineteen item methods/hard fails and existing artifacts.
2. Repair/replace S37-stale instruments first and demonstrate red seeds.
3. Re-run the entire contract before changing content; classify each row `established`, `reverify`, or `build`.
4. Close merge-order, reach/provenance and consumption hard fails; coordinate rather than silently editing W1-09/W1-18 ownership.
5. Repair only remaining native-row failures; rerun individual and aggregate gates; perform delete-the-fix controls.
6. Deliver raw machine-readable outputs, traces, item-native reports and the complete stamped
   RI-DLG07 candidate corpus/pool census. The builder must not assemble, randomise, reveal or judge
   the blind pack.
7. Dispatch an eligible critic to assemble/normalise/randomise and seal the RI-DLG07 pack, and a
   fresh judge with no project/corpus context to administer §B. Apply the item's calibration,
   mandatory harsher-rerun and void/rebuild rules before incorporating A10 into closure.

**Escape audit:** Could a competent builder execute exactly this plan, get all builder checks green,
and still fail any governing reference-item bar? **Yes, until A10 is independently completed; that
state is deliberately not closure.** A9 cannot colour RI-DLG07 green. Closure requires the native
independent A10 result as well as A9, so builder-authored or context-exposed judgement is
inadmissible rather than evidence awaiting review.

## Plan critique — 2026-08-09

**Verdict: BLOCKING / awaiting-remediation.** The proposition is falsified by RI-DLG07's
independence requirements. A9 assigns the builder “all nineteen native item instruments,” while
the controls paragraph likewise treats every item instrument as builder-produced evidence.
RI-DLG07 instead requires its pack to be assembled by the critic, judged by a fresh agent with no
project context, and rebuilt by a different agent after a void run. A builder can follow this plan
literally, self-assemble/self-administer the blind comparison, report its rows green, and still
have supplied no admissible RI-DLG07 evidence. The generic A9 sentence does not cure the conflict:
it assigns the native method to the wrong actor and the build order never creates the required
independent handoff.

**Required remediation:** split RI-DLG07 out of builder acceptance. Require the builder only to
prove that all six eligible pools meet §D and to deliver the complete candidate corpus plus a
commit stamp. Then require a separately dispatched critic (not the builder and not an agent that
has seen the project corpus) to assemble/randomise/normalise the pack, run the fresh blind judge,
apply the calibration and mandatory harsher-rerun/void rules, and record the native metrics. Make
W1-17 closure conditional on that independent result having no automatic fail and meeting the
item's native PASS/ladder threshold. Explicitly state that a builder-authored or context-exposed
judge result is inadmissible and cannot make A9 green.

## Critique remediation — 2026-08-09

RI-DLG07 is now split out as A10 with the exact actor boundary: builder eligibility/corpus handoff,
critic assembly/normalisation/randomisation/sealed reveal, and a fresh context-free judge. Native
calibration, harsher-rerun and void/rebuild controls remain binding. A9 explicitly leaves the item
`not_run`; no self-administered result can close W1-17. The corpus is extracted once and the blind
work begins only after cheap pool eligibility succeeds. Materially edited; awaiting fresh
re-criticism by a different reviewer-editor.

## Fresh reviewer-editor re-criticism — 2026-08-09

**SATISFIED.** The nineteen-item set was independently reconciled to the twelve canonical dialogue paths. A9 cannot colour RI-DLG07 green while A10 is absent: A10 preserves critic-side pack construction, a fresh context-free judge, calibration, harsher rerun and void rules. Current-state measurement precedes repairs, candidate pools are checked cheaply before blind work, and existing library/tools/evidence are reused.
