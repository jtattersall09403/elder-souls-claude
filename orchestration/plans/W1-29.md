# W1-29 current-state continuation plan

Plan-State: awaiting-recriticism

## Arbitration S53 — 2026-08-14. State changed from `satisfied`; this plan's naive clause does not reach two zero-weight rows

`ARBITRATION` **S53** rules that `RI-JRN03`'s and `RI-JRN04`'s naive blocks are a **gate, not a
weight**: no native score ≥90 and no ladder ceiling above the 70–89 band is available unless the
naive pass ran and cleared the naive checks that no hard fail already protects. It changes no
weight, band, threshold or hard fail in either item.

**Why this plan's state changed, and it is the only plan this pass moved.** This plan's clause —
*"independent naive **pass** … is not closure"*, the one `W1-08` was told to copy — is a gate and
was the right instinct; S53 upholds it and moves it into the item so no third plan has to copy it.
But it does not enumerate, and `RI-JRN04`'s arithmetic is **worse than the audit recorded**: its
naive block is **4 of 100**, and **M-N2** (pre-registration: attack and dodge land where the agent
expected) and **M-N4** (0 instruction quotations outside settings) carry **no weight at all**. A
build can fail both outright, score 96, and nothing written here notices. §Native acceptance's
*"Naive (4)"* row inherits the same hole.

**What a re-criticism must add** — no new run, no new population, no new tool:

- The **S53 gate for `RI-JRN04`, named**: **M-N1** (≥11 of 14 actions discovered, none from a text
  instruction), **M-N2** (attack and dodge both land where the agent expected, recorded as a
  quotation) and **M-N4** (0 instruction quotations outside the settings surface). All three must
  pass on a nonempty naive population before any ≥90 claim, **independently of their point weight**,
  which for M-N2 and M-N4 is zero.
- Bind the §Native acceptance "Naive (4)" row on those three checks by name rather than on the
  block's four points, and report their values on the same line as any native total.
- Keep everything else: nothing above is weakened, and no threshold moves.

S53 is **reversible** (delete the row and restore this plan to `satisfied`) and carries its own
falsifier. **Exact next action:** a fresh reviewer-editor judges this plan against S53. Do not
dispatch a builder against it while it reads `awaiting-recriticism`.

## Authority/current state

Governing item: complete `corpus/88-journeys/RI-JRN04-mobile-and-gamepad.md`, all ES/PAD maps (including souls-handheld), analog/lifecycle/mobile/touch rows, instrumented and naive methods, consumption, scores and HF1–HF10. Read joint W1-08/W1-29 history/verdicts and current pad/touch/viewport/render paths. RI-CAM02 owns resulting camera speed; RI-JRN04 owns device transform. Apply the current 0.14 movement deadzone and route any true right-stick conflict through an S-ruling—do not choose the easier table. W1-08 desktop counters remain separate.

Preserve if reproduced: standard/non-standard translation, touch overlay and 11-control consumption, DPR/safe-area model, lifecycle switching, trigger hysteresis, tap/hold, profile perturbation. Current reds: in-progress calibration survives disconnect and eats the next pad; M-P20 is 0/3 (dialogue 7.00, journal/book 5.06 CSS px vs 18); M-P25 artifact exception; JC-04N absent; rendered prompts/text coverage needs proof.

## Native acceptance

* **Map (30):** exact ES/PAD and souls-handheld mappings for all 16 physical inputs/reserved/unbound Guide; 12-frame roll/sprint and two-hand discriminators exact; non-standard GameSir HID calibration completes and maps correctly. M-P1/M-P3/M-P5/M-P15 full points.
* **Analog (18):** movement radial 0.14/0.92 linear and walk threshold 0.55; right transform per authoritative ruling; triggers fire .35/release .20/full .85 with rest auto-zero; drift guard; one snapshot/rAF, edge latching. Sweep full magnitudes/bearings and hair-trigger mode; native tolerances exact.
* **Lifecycle (17):** connect-on-first-press, unplug release within required frame, simulation continues, most-recently-active of two pads, identity/index replacement clean. **Disconnect at every one of six calibration prompts**: `calibrating=false` after removal; new standard pad index0=`interact`, index5=`light`; no swallowed inputs or stale map after 600 frames. M-P11–15 complete.
* **Phone (17):** all H1–H12 across the item's viewport/orientation/DPR/safe-area matrix; no `100vh` defect, no crop/overlap; dialogue, journal and book minimum text each ≥18 CSS px (not device px), plus every other native H10 predicate; M-P25 completes without exception. Thermal H8 only as item tier specifies, reported honestly.
* **Touch parity (10):** all 14 required actions/controls T1–T10, floating stick radial behaviour, menu suppression/restoration under S35; every control changes its named world behaviour, release drift 0, dead-glass null 0; overlay drawn rather than model-only.
* **Restraint/reach (4):** full rendered text/control-name prohibition on nonempty complete surfaces and native reach/occlusion checks.
* **Naive (4):** JC-04N enforced isolation, fresh participant(s), exact native transcript/success. Builder does not judge; absent result scores 0.
* **Consumption census (fail-closed):** before scoring, emit a machine-readable ledger that
  exhaustively enumerates the journey's required runtime models: both profile action maps and all
  physical/reserved bindings; every `pad-quirks` entry plus unknown-device calibration prompt,
  result and persistence records; profile-selection and most-recent-device state; disconnect,
  reconnect and index-replacement state; movement/camera radial deadzones, response curves,
  walk/charge/trigger thresholds and hysteresis, roll/sprint timing and edge-latch/polling rules;
  viewport, orientation, DPR, safe-area and chrome-collapse records/budgets; every touch binding,
  floating-stick/gesture/menu-visibility rule and overlay drawing model; and every authored glyph,
  prompt, calibration, refusal and active-device string. Derive the rows from the shipped tables,
  records and complete rendered UI/text inventory—this named list is a classification checklist,
  not permission to sample it. For **each ledger row**, hold all other inputs fixed, perturb two
  well-separated valid values, and demonstrate a changed player-visible/doable outcome (drawn
  string/object, appearing surface, accepted/refused input, movement/camera behaviour, or surviving
  state), then disable/bypass that model and show the same outcome goes red. A harness return or
  trace alone is observer evidence, never the consumer. Any missing row or `coupling == 0` scores
  its owning native dimension **0 with no partial credit** and is recorded in `status_reasons`.
  This includes orphan text/drawing and apparently correct models exposed only through the harness.
* All HF1–HF10 evaluated on real populations; none true. Native score ≥90/100 Meets bar.

## Falsification/evidence

Run input groups separately plus shim self-test, viewport matrix and joint aggregation. Red arms: resume historical mid-calibration bug; swap standard map; axial deadzone; single trigger threshold; cached Gamepad object; drop a between-step edge; remove safe-area CSS; restore 5.06 px book text; remove overlay draw while leaving model; disconnect touch consumer. Every relevant row red, with unrelated rows still populated. Delete fixes on shadow source; in-progress and completed calibration are separate factorial arms. Assert `self_test.legs` nonempty and no exception before accepting artifact.

Evidence stamps commit/device preset/browser/load, reports CSS px and backing px separately, all magnitudes/bearings/frames, touch world deltas, calibration prompts, viewport screenshots where item evidence requires, hard-fail matrix, native score, complete consumption ledger and sealed naive transcript. Reuse the existing journey runner, shim and shipped tables; generate the ledger from them once, run cheap completeness/text checks before the bounded runtime perturbations, and do not create a second input model or repeat viewport runs when one run supplies multiple native rows. Required checks include boot/data/content and complete RI-JRN04 aggregate. A green implementation without the exhaustive fail-closed consumption census, independent naive pass, M-P20 all three surfaces, M-P25, or all calibration disconnect positions is not closure.

---

## Independent plan critique — 2026-08-09

**Verdict: BLOCKING / awaiting-remediation.** The plan contains useful touch world-delta and
profile perturbations but never executes RI-JRN04's governing CONSUMPTION contract over an
**exhaustive enumeration** of every table, profile, quirk/calibration map, threshold/curve, viewport
record, touch binding and authored string the journey requires. Consequently the input maps and
overlay can change visible behaviour while a calibration result, safe-area model, active-device
prompt model, or refusal string remains orphaned; every listed acceptance and red arm can still be
green despite a governing `coupling == 0` that must score its dimension zero.

**Required remediation:** add an exhaustive model ledger to the operative acceptance. For each
model require two well-separated perturbations with all else fixed, a player-visible/doable
observable (not merely a harness return or trace), a null control demonstrated red, and the native
consequence that any zero coupling scores the owning dimension 0 with no partial credit. Explicitly
include orphan text/drawing, calibration persistence, profile selection, device switching,
safe-area/viewport consumption and every analog transform. Retain the existing falsification arms
as examples, not as a sample replacing the census.

**Recommendation:** remediate the consumption contract, then request fresh independent criticism.

## Remediation — 2026-08-09

The operative acceptance now implements RI-JRN04's exact exhaustive, per-model CONSUMPTION
contract: generated complete ledger, two separated perturbations, player-visible/doable observable,
red null and dimension-zero/no-partial consequence. The named classifications explicitly cover the
models and orphan-text/drawing cases in the finding without converting them into a sample. Existing
native runner/shim evidence is reused and completeness fails cheaply before bounded runtime work.
Materially edited; a different fresh reviewer-editor must re-criticise this version.

## Fresh reviewer-editor re-criticism — 2026-08-09

**SATISFIED.** Independent reconstruction from W1-29's five owned paths and sole governing item, RI-JRN04, found no remaining false-green or material-waste route. The plan preserves native populations, thresholds, hard fails, exhaustive consumption and independent naive evidence, while reusing the existing runner/shim and gating bounded runtime work behind cheap completeness.
