# PLAN — W1-26: the opening as a played scene

Plan-State: satisfied

Planning-only continuation reviewed against canonical HEAD `3b783560`. Governing items: RI-JRN01, RI-JRN09 and RI-EXP01 after W1-AMENDMENT-01. RI-PLT03 and RI-CHR01 are cited dependencies, not scored here.

## 1. History, HEAD, dependencies and stale claims

The redefined piece owns seven opening paths only. Round 1 exposed accessor and presentation holes. Round 2 proved the title's New path soft-locked at race observation, hid errors as refusal dialogue, lost `hold.out`, and left one explicit tutorial. Round 3 made keyboard creation complete, restored `hold.out`, produced four body-specific reactions/writs and raised RI-JRN09 to 8, while RI-JRN01 remained capped by modality HF5 and RI-EXP01 remained unmeasured. Its largest robustness gap was invalid body race being accepted at scene start and refused eleven nodes later. The round-3 critic also found pre-character HUD on title, an imperative regex too weak to establish diegesis, unread placed inscriptions, and only keyboard end-to-end. Round 4 builder/status reports title HUD fixed and further play evidence, but its critic status remains `running` with no canonical r4 verdict; therefore none of those claims closes a bar.

Dependencies: W1-00 launch/harness/TTFP, W1-07 creation content, W1-08 desktop action set, W1-29 gamepad/touch, W1-04 interior. The continuation must test seam outcomes but route component defects to owners. RI-EXP01 requires an independent driven session; the builder must not judge a pack/session whose answers it authored or reveal it read.

## 2. Reference-item reconciliation

| item | already established independently | current-HEAD re-verification | actual remaining work |
|---|---|---|---|
| RI-JRN09 | round 3: hold.out drawn, four bodies distinct, prior native 92; one generic refusal remains | run complete M1–M4 native exchange tool and every HF at HEAD, real play path | only rows below native threshold; diversify refusal only if item requires it |
| RI-JRN01 O6/O17 | keyboard whole journey passed; controllable body existed | exact native O/M matrix, all timing/area/text/placement predicates | build only failures |
| RI-JRN01 M13/HF5 | only keyboard journey leg established | complete whole journey separately by keyboard, mouse+keyboard, gamepad and touch using released inputs | coordinate missing modality seams; 4/4 required |
| M9 | register regex passed but known tutorial/wayfinding prose escaped | apply item's semantic/native method, not start-anchored proxy; inspect every drawn string | remove only item-forbidden instruction, preserve diegetic direction |
| M10 | inscriptions exist in data but no running read proved | world placement/read census before first meaningful choice | place/consume within native 1–6 and 8 m bounds |
| title/legibility | pre-character HUD failed r3; r4 claims fix without verdict | pixel/register title and every opening surface at native fixtures | fix only reproduced leak/legibility misses |
| invalid body race | planted altmer fails late | validate at scene start, enumerate all legal races end-to-end | early fail with id/source plus static reconciliation check |
| RI-EXP01 | B01 independently hit; B02+ never scored | independent, driven native session and beat extraction | evidence-first; build only missed beats owned by opening paths |

## 3. Acceptance contract

* **O1 exchange:** RI-JRN09 M1–M4 exact weighted predicates over every specified node/body/signature; require native numerator **≥90/100**, the item's `Meets the bar` band, with an artifact-backed shared ladder score ≥7 reported and no hard fail. DTR thresholds and AC/NAMED counts remain exactly as the item states; any hard fail caps the item at 2 and overrides the numerator.
* **O2 launch matrix:** every released RI-JRN01 method/obligation, with native units (seconds, surfaces, characters, metres, pixels and modality journeys) and item fixtures. At the current **67-point runnable denominator**, require a native numerator of **≥61/67** (≥0.90; 61/67 = 0.9104), report numerator, denominator and resulting ladder/band, and require zero hard fail. Recompute the denominator only if the item itself changes the corpus-debt disposition; preserve the ≥0.90 fraction. Any hard fail caps the item at 2 and overrides the fraction.
* **O3 modality:** four independent full journeys from first launch through `census.done`: keyboard, mouse+keyboard, gamepad, touch. Acceptance 4/4; each uses real public input, zero harness state mutation, zero refusal/error, and records every node. Title-only checks do not count.
* **O4 body validation:** every legal `races.json` id completes; one planted invalid id is rejected at `censusBegin` before node 1 with offending id and source named; static check reconciles every shipped writer/default. Delete guard restores late refusal and turns red.
* **O5 no explanation:** every actually drawn opening string is evaluated by RI-JRN01's native M9/HF method plus a committed semantic fixture set containing the known escaped directions/tutorial. Acceptance exact item zero-forbidden count; fixture recall 100%, false positives 0 on allowed diegetic lines.
* **O6 world legibility:** title has 0 pre-character HUD strings/pixels; each surface fits native pixel/area bars; inscriptions count 1–6 before choice and 100% are placed/readable within 8 m using running-world positions.
* **O7 experience:** a fresh agent who has not read the reveal drives the full RI-EXP01 prescribed session, ships the artifact itself and runs the five native tools/beat extraction. Require native numerator **≥85/100**, the item's `Meets the bar` band, with an artifact-backed shared ladder score ≥7 reported; every required beat/derived target is measured, with no fail-closed zero or proxy feature-only pack. Any hard fail caps the item at 2 and makes the axis fail regardless of numerator.
* **O8 aggregation:** at one stamped commit report and aggregate **JRN09 ≥90/100; JRN01 ≥61/67 at the current denominator (≥0.90); EXP01 ≥85/100**. Each must be independently translated to an artifact-justified shared ladder score ≥7, meet its native gate, and remain free of hard fails, `unmeasurable`, or missing required methods; neither a generic average nor the other items may carry a failing axis.

Hard fails are every item-native HF (especially HF5), any journey completed through harness mutation, swallowed exception, player-facing engine error, title HUD leak, invalid body accepted until late, unmeasured EXP01 scored as pass, or self-judged session.

Controls/evidence: break one node's draw, collapse body reactions, shadow text entry, remove starting race, plant invalid race, re-enable title HUD, disconnect inscription placement, remove one input modality, and seed a forbidden tutorial. Each exact check must go red. Delete each fix in scratch arms and require historical failure return; assert arm divergence. Publish commit-stamped raw input events, node/drawn-string traces, screenshots where item-native, race matrix, placement distances and complete session artifact/reveal. Reuse `opening-play`, `jrn09-exchange`, `journey-run`, `census-newgame`, `name-entry`, W1-26 r3/r4 verifiers and RI-EXP01 native tools; fix vacuous checks rather than replacing thresholds.

## 4. Sequential continuation

1. Reuse commit-matched independent row evidence that is still valid; run cheap static/race/default reconciliation and hard-fail probes first, then rerun only stale or missing JRN09/JRN01 rows at HEAD. Stop before the modality/session passes if an early hard fail remains.
2. Close body-validation, title/legibility and semantic-instruction instrument defects.
3. Run four real-input modality journeys; coordinate dependencies, never waive a leg.
4. Prepare a tell-free complete RI-EXP01 session protocol and hand it to a fresh driver/judge; do not self-grade.
5. Remediate only evidenced owned beat misses; rerun all axes, red seeds and delete-fix controls together.
6. Independent critic scores native axes and min aggregation.

**Escape audit:** Could all checks be green while a governing reference bar fails? **No.** O1/O2/O7 cover every native requirement and hard fail, O3 prevents title-only modality proxies, and O8 fail-closes any unmeasured axis.

## Plan critique — 2026-08-09

**Recorded blocker (resolved in this remediation; fresh review required):** O1, O2 and O7 substitute native scores that the
governing items explicitly classify as below-bar for a Wave score of 7. RI-JRN09 says native
70–89 is below bar and maps native 75 to ladder 6 (92 to 8), yet O1 calls `≥70/100` “Wave score
≥7.” RI-JRN01 likewise requires at least 0.90 of the runnable denominator to meet its bar and maps
0.70–0.89 to ladder 6, while O2 accepts native 70. RI-EXP01 maps native 75 to ladder 6 and 88 to
8, while O7 accepts native 70. Thus the three headline checks can all be recorded green at values
the reference items call below-bar. O8's generic `≥7` does not repair the plan because the earlier
rows expressly provide the erroneous native-to-wave conversion a builder is told to aggregate.

**Remediation applied:** O1, O2, O7 and O8 now replace every `≥70` shortcut with the exact native threshold that
establishes the selected Wave ladder gate under that item's own anchors/interpolation policy (or,
more simply and conservatively, require each native item to reach its published ladder-8 anchor:
RI-JRN09 92, RI-JRN01 at least 0.90 of the current runnable denominator, and RI-EXP01 88). State
the native numerator, current denominator, resulting ladder score and hard-fail override in each
acceptance row and in O8; do not label a published ladder-6 result as Wave 7.

## Fresh reviewer-editor pass — 2026-08-09

**Outcome: materially edited; awaiting fresh re-criticism.** The remediation correctly removed
the below-bar 70 shortcuts but overshot the governing Wave-1 gate by making the published ladder-8
anchors (JRN09 92 and EXP01 88) mandatory. Their native scoring tables already define `Meets the
bar` at 90 and 85 respectively; those are the minimum native bands that may support the required
shared ladder ≥7 when backed by the mandated artifact justification. O1, O7 and O8 now use those
bar-preserving lower thresholds, retaining every native method and hard fail while avoiding
unrequired ladder-8 polish. JRN01 remains at its native ≥0.90 runnable-fraction bar.

## Fresh independent reviewer-editor pass r3 — 2026-08-09

**Outcome: SATISFIED; no material plan edit.** Reconstructed the amended seven-path decomposition
and the exact RI-JRN01, RI-JRN09 and RI-EXP01 methods rather than adopting the prior review. The
received plan keeps JRN01's runnable denominator fail-closed, requires all four full real-input
journeys, native exchange and first-hour populations, every hard-fail override, independent
first-hour evidence, world-visible CONSUMPTION and non-vacuous controls. It reuses the landed
opening/session instruments and puts static and hard-fail gates before modality and isolated
hour-long work, so no materially cheaper bar-equivalent route was found.
