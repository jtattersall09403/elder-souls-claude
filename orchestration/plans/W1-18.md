# CURRENT-STATE CONTINUATION PLAN — W1-18: quest engine and journal

Plan-State: satisfied

Planning only. Canonical reconstruction and offline spot checks are against
`50821cb25dadbe253c2f6394118d16fb6baaa2b9` (2026-08-09). No builder claim below is promoted to
independent proof. A fresh plan critic must falsify this contract before any build resumes.

## Builder/critic execution allocation (binding)

This piece uses the shared [builder execution contract](BUILDER-EXECUTION-CONTRACT.md). The original
native predicates and populations below remain the final critic acceptance bar.

**Builder-owned:** close every remaining legitimate reveal route and finish the quest engine,
journal, direction, resolution and consequence implementation required by this plan. Run the full
cheap/static reveal, quest-schema, reference and journal audit. In the running game, prove every
distinct reveal mechanism—dialogue, document, environment, eavesdrop, corpse and person—using a
representative positive and null case; complete representative quests across the distinct resolution
families; and prove ordered journal writes and save/load. Run targeted red and delete-the-fix controls
for each repaired mechanism.

**Critic-owned:** the complete live reveal and resolution populations; both fresh-agent journey
families; Q1–Q5 twice each; the week-return protocol; every sneak resolution; the full marker-viewpoint
population; native navigation success rate; blind/naive prose and journey work; and the complete
24-item aggregate and verdict.

**Completion rule:** the builder continues through every available implementation and bounded proof
above in one run. A missing canonical combat, stealth, travel, world or dialogue dependency is recorded
precisely; it does not excuse unrelated W1-18 work.

## 1. Canonical history, current implementation, and stale claims

The original build created the quest calendar/book/gates/machine/journal/screen and quest corpus.
Round 2 then found that 186 `deceit.revealed_by` declarations had no reader, added
`buildRevealRoutes()`/`QuestEngine.learnFrom()` and the `Engine.talkTo()` consumer, and associated 37
reveals with authored middle-journal entries. Its own red controls were strong builder evidence, but
there is **no W1-18 verdict**. Subsequent independent work is relevant, not substitutive:

* W1-19 r2 independently exercised topic, direction, disposition and quest/roster crossings but
  failed the piece; its then-blocking giver-presence gap was later closed by W1-GIVER-PRESENCE.
* W1-READABLES and its critic independently established that document and environment routes are
  consumed in play. Current `reveal-route-audit.mjs` reports **100/121** demanded pairs routed and
  exits 1: all 9 eavesdrop, the corpse row, and 11 person rows remain unrouted. Preserve the routed
  100; do not revive the stale r2 figure 48/121 or its “73 remain” conclusion.
* Current `quest-audit.mjs` sees 124 quests/18 files and zero integrity problems, but below-bar
  `mean_branches=1.05`, `silent_failure_pct=22.4`, `multi_faction_consequence_pct=27.42`,
  `X7_cosmetic_pairs=189`, and `X8_foreshadowed_pct=67.02`. The old X7=0 and X8=76.92 claims are
  stale. An exit-0 census with red rows is not acceptance.
* Browser verification is stale. Planning attempted `node tools/harness/boot-check.mjs`, but this
  environment lacked the configured Chromium executable; fresh build/critic runs must use the
  repository browser environment and stamp the tested commit.

Current implementation paths to preserve unless a measured defect requires change:
`game/src/sim/quest/{calendar,defs,gate,journal,machine,screen,reveal-routes}.js`, quest state/save
round-trip, `game/src/render/journal-overlay.js`, the UI journal projection, `game/data/quests/**`,
`tools/analysis/quest-audit.mjs`, `tools/corpus/dump-journal.mjs`,
`tools/analysis/journal-ui.mjs`, and `tools/quests/reveal-route-audit.mjs`. Schema amendments
`AM-QST04-W1-18-01/02` are canonical and additive.

## 2. Governing-bar disposition — every assigned reference item

“Preserve” means current **independent** evidence proves the named narrow fact, not the complete
item. “Verify” means a fresh independent run is required. “Build” means current HEAD already
demonstrates a concrete deficit or the required instrument/fixture is absent.

| Governing item | Current disposition | Continuation obligation |
|---|---|---|
| RI-DLG01 topic graph | **Preserve narrow / verify.** W1-19 r2 proved live AddTopic consumption. | Census the whole filtered topic graph and speaker-conditioned reachability; no self-contained giver cycles or generic unconditioned replacement bodies may satisfy it. |
| RI-DLG03 greetings/rumours | **Preserve narrow / verify.** Rumour ablation 10→0 was independently reproduced. | Re-run all settlement/band/count/false-rumour floors over reachable speakers, not files alone. |
| RI-DLG05 journal | **Build + verify.** Middle writes work on person/document/environment routes, but 21 demanded reveals remain unrouted; current X8 is 67.02%. | Close legitimate route/journal gaps, dump the complete TSV, apply every prohibition and voice/length predicate, and prove live ordered writes plus save/load immutability. |
| RI-DLG07 blind prose | **Verify.** Historical prose praise is not a compliant independent blind pack. | Fresh critic assembles and leak-gates the required category-balanced artifact, then a separate judge scores it; builder must not judge its pack. |
| RI-DLG08 tonal range | **Verify/build if red.** W1-19 r2 independently found a severe “eleven” and three-info fingerprint in adjacent quest prose. | Run the complete corpus comparison/lint and register/entropy/good-bad tests; remediate only failing W1-18-owned text. |
| RI-DLG09 parley | **Verify dependency without dropping bar.** Combat owns mechanics, but quests own knowledge/flag/faction exits. | Demonstrate all quest-side routes feed the five-route ladder and AR-3 result in played fights; missing combat support is a named dependency and a W1-18 fail, not an exclusion. |
| RI-JRN07 unmarked quest | **Build + verify.** No compliant five-quest naive journey verdict exists. | Run the exact ES/QUEST-SET Q1–Q5 twice each, all M-Q1…M-Q16, naive M-N1…M-N7, blind pair, marker sweep and consumption perturbation. |
| RI-JRN08 return after a week | **Build + verify.** No ES/COLD fresh-agent verdict exists. | Produce the binding save and full M-R/naive/blind/consumption runs, including 100% direction prose, immutable entries, no recap, night landmarks and ≤5 min orientation. |
| RI-LOR03 books | **Verify dependency.** Readables independently proved many objects open; that does not prove taxonomy/length/placement. | Run the complete book census and live placement/openability checks for quest-owned books. Preserve readable wiring. |
| RI-LOR06 contradictions | **Verify.** Schema amendments and prose can introduce canon claims. | Run registry conformance and decision procedure on every quest/journal/book claim; zero undeclared or unruled contradictions. |
| RI-MAG04 magic solutions | **Verify/build if red.** Static quest methods are not live magic consumption. | Re-run Q1–Q8; every one of seven AR-3 crossings demonstrated, all eight shapes ≥2 quests, and no skeleton key. |
| RI-QST02 deceit | **Build + verify.** Route audit has 21 hard missing world routes. | Meet every deceit coverage target and hard-fail predicate, then play each demanded channel. Do not route eavesdrop through greeting or corpse through crime discovery merely to improve a count. |
| RI-QST04 schema/anatomy | **Build.** Current branches 1.05 <1.3; other rows below bar. | 100% schema validation; mean resolutions ≥2.4; mean branches ≥1.3; failures ≥70%; silent failures 5–15%; multi-faction consequences ≥30%; integrity 2–5 clean and journal bands exact. |
| RI-QST05 noncombat | **Verify.** Current static 124/124 nonviolent is narrow only. | Measure PACIFIST-ALL ≥45%, option ≥70%, gated ≥80%, verb spread ≤40%, faction spread and each verb minimum, with live attempt/refusal/consequence traces. |
| RI-QST07 side texture | **Build/verify.** No current complete bar proof. | Apply volume/type/discovery/density/weird/direction floors to the full side population and prove cross-town rumours and reachable givers. |
| RI-QST08 rewards | **Verify/build if red.** Consequence declarations do not prove award/consumption. | Full unique/type/gold-only/nonviolent-parity/no-scaling/information-resolution census plus played award and save/load controls. |
| RI-QST09 irreversibility | **Concrete remediation.** Current X7=189 and X8=67.02 fail. | X1–X6 and X10 remain at bar; X7 exactly 0; X8 ≥80%; execute exclusive pairs and diff ≥3 durable flags, ≥1 reward and ≥1 entity, then save/load and illegal-diff checks. |
| RI-STL01 sneak | **Verify dependency.** Quest methods depend on stealth but may not assume it. | Play every sneak resolution with detected/undetected and light/noise/equipment controls; compute bypass census and verify zero HUD meter. |
| RI-TRV02 travel magic | **Verify dependency.** Quest destinations can turn travel into a solvent. | Run P1–P3/D1–D5 against quest destinations; exact four spells, 12 destinations, combat/health/arena/mark/refuge/no-picker hard fails. |
| RI-UIX02 no markers | **Verify.** Historical “zero markers” traces are builder claims and detector gaps were later found in W1-21. | Run all six current detectors across ≥14 viewpoints and ≥5 named quest states, subject-removed red controls, data scan, state differential, pixel sweep, and whole-journey M31; any marker is hard fail. |
| RI-UIX04 journal UI | **Verify/build if red.** UI exists now; original “no UI” assumption is stale. | Exact ≥4 quest/≥12 entry seed-1337 fixture; every JU predicate, order preservation, navigability, resize/input modes and pixel evidence; `interleave_ratio ≥0.5`; every §B prohibition zero. |
| RI-WLD03 settlement anatomy | **Verify dependency.** Giver placement improved independently. | For all eight settlements prove quest givers/services exist in reachable loaded worlds and satisfy template/anatomy/service coverage; data rows alone do not count. |
| RI-WLD06 markerless navigation | **Build + verify.** Directions existing in 100% of data is insufficient. | M27–M31: landmark fan, signposts, prose grammar and 9/10 route completion, with M31=0. Use real fog/light and fresh-agent navigation. |
| RI-WLD09 opacity | **Verify/build if red.** Mainline B3 is W1-19; quest/journal clarity is W1-18's non-opaque boundary. | Full mystery/fact/void audit; 0 assembled-fact leakage, 3–6 main unanswered, no ending closure, while every actionable quest direction remains explicit. |

## 3. Build order and acceptance contract

1. **Freeze populations at the tested commit.** Emit quest ids by category, every resolution,
   branch, reveal/channel/source/journal index, giver, discovery route, reward, consequence and
   destination. Hard fail on loader/schema errors, duplicate ids, missing sources, empty graded
   populations, or a tool reading a parallel transcription rather than `QuestBook`/running state.
2. **Repair demonstrated structural defects, not scores.** For each of 189 X7 pairs author truly
   different resolution consequences; raise X8 to ≥80% with first-person pre-choice foreshadowing;
   bring branch/silent-failure/multi-faction metrics to the exact RI-QST04 bars. Re-run aggregate
   `quest-audit`, not only touched quests.
3. **Close route gaps honestly.** For each of 121 demanded pairs print `{quest,reveal,channel,
   source,source_exists,source_reachable,world_action,journal}`. Required routed=121/121 unless the
   governing item explicitly permits absence (none currently does). Eavesdrop requires an unnoticed
   proximity/conversation consumer; corpse requires player examination. Eleven missing people need
   authored, placed actors or corrected sources. A route without its world entity/action is red.
4. **Prove journal consumption.** On at least one fixture per channel and every newly built route:
   before gate refuses by reveal id; perform only the world action; knowledge changes; authored
   numbered/dated/first-person entry appends in index order; gate opens; save/load and 600 frames
   preserve both. Wrong person/object, unopened quest and pre-PONR controls change neither register.
5. **Run the complete governing suite** in the table, including the two fresh-agent journeys.

Hard fails: any governing hard fail; any missing/unmeasurable mandatory row; any empty sample scored
PASS; any direct `questReveal/questNote/questSetFlag` in a world-route probe; any inferred journal
text; any dependency used to omit a W1-18 bar; or aggregate regression.

## 4. Null, red, and delete-the-fix controls; evidence

For every remediation, on an isolated copy delete only that fix and require the exact governing
row to return to its pre-fix red value while unrelated rows remain within their declared tolerance.
Additionally: remove each route reader/call site separately; remove each world source separately;
strip the new journal link; collapse each exclusive pair to identical consequences; remove the
foreshadow clause; forge a marker; reorder journal entries; and retroactively edit one entry after
load. Each must make its corresponding check non-zero/red. Assert the mutation hash and executed
line/call count, and prove the negative arm's population/support is non-zero. Two guards require a
2×2, never two one-factor “inert” claims.

Evidence must include commands, exit codes, raw machine-readable reports, exact population lists,
screens/traces where the item requires them, tested commit, browser/load context, and before/fixed/
deleted-arm comparison. Builder evidence remains builder evidence. A fresh critic reruns and owns
the verdict.

## 5. Completeness challenge

**Could a competent builder follow this plan exactly, get every planned check green, and still
fail any governing reference-item bar?** No: acceptance is conjunctive over all 24 assigned items,
includes each item's complete current method and hard fails rather than the historical headline
subsets, and cross-piece dependencies remain measured obligations. Any newly discovered mismatch
between a named instrument and its reference item is a plan defect: stop, write it back, and return
the plan to criticism rather than weakening or substituting the bar.


## 6. Independent plan critique — 2026-08-09

**Verdict: SATISFIED.** Independently reconstructed the 24-item assignment from `docs/PLAN.md` and
read the current comparison methods, native scales, hard fails, blind requirements and relevant
current evidence rather than adopting this plan's historical interpretation. Falsification did not
find a route by which exact execution could make acceptance green while missing the governing bar:
§2 keeps all 24 items in scope, §3 makes their complete current methods and hard fails conjunctive,
dependencies cannot become exclusions, and §4 requires non-vacuous route/consumer/delete controls
with independent judgement where blindness governs. The narrower numeric summaries are not
substitutes for the governing items because §3.5 and the hard-fail paragraph expressly require the
complete suite. No CARRIED risk is used.
