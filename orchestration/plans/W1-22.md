# W1-22 — current-state continuation plan

Plan-State: awaiting-recriticism

## Historical independent plan critique — 2026-08-09 (authority blocker resolved by S41)

**Historical verdict: BLOCKING / awaiting-remediation. Resolved by S41; the materially edited plan now awaits re-criticism.**

The plan changes the governing comparison instead of preserving it. `RI-AUD03` fixes the capture
at seed 1337 in its comparison method, but the plan expressly allows seeds to be preselected so
the required signatures occur. A builder can therefore tune or select favorable event schedules,
pass B1/B2 on those clips, and never demonstrate that the reference method's fixed-seed population
passes. Disclosure does not remove that selection bias.

The authority error is broader: `AMENDMENT-W1-22-01` labels itself **proposed** and says that none
of its three changes has been applied to a reference item, while this plan declares the amendment
governing and adopts its bed-alone B5 interpretation and synthesized-bed crest substitute. That
lets a green plan answer a different comparison from canonical RI-AUD03. In particular, the plan
can exclude positional emitters from B5 even though the unamended comparison captures from the
region fixture and does not contain that exception.

**Required remediation:** require the literal seed-1337 B1/B2 capture population as the scored
arm. Extra signature-forced or multi-seed clips may be diagnostic only and may not replace or
contribute to the native score. Remove the proposed amendment from the governing set unless and
until a canonical ruling applies its choices; before dispatch, resolve the B5 listener/emitter and
crest contradictions through that ruling and pin the chosen capture graph and metric in the plan.
Fresh criticism must then verify the resulting text against the unchanged RI-AUD03 method and all
RI-WLD04/08 aggregation requirements.

## Authority remediation — S41 applied 2026-08-09

S41 fixes one scored capture per region: seed **1337**, the authoritative region-centroid listener,
clear weather, 13:00, 240 f@60 settle, then ambience-only frames 240–1440. B5 consumes that exact
pre-normalisation clip and its unchanged live graph, including L1–L4 and any R7 emitter audible at the
centroid; B1/B2 consume the same captures after −23 LUFS-I normalisation. The manifest freezes all
thirteen centroids and graph membership before rendering. Bed-only, moved-listener, forced-signature and
other-seed arms are diagnostic only. Neither absolute crest >20 dB nor synthesized-bed crest lift >=3 dB
is a governing predicate; both are corroborating diagnostics. Literal B1/B2/B3, per-event L3/L4 bands,
and B5's existing LUFS bands govern. This material edit is not approval.

## Authority and reconstructed state

The sole owned path is `audio.ambience.region`. Authoritative path machinery maps it to exactly
**RI-AUD03, RI-WLD04 and RI-WLD08**; these three whole items are the bar wherever their methods judge
this path. RI-MTH07 and ARBITRATION govern interpretation. `AMENDMENT-W1-22-01` is a proposal only,
not authority unless a canonical ruling adopts it. The old note that
RI-WLD04 named `world.audio` is superseded by the canonical alias in `corpus/00-doctrine/INDEX.md`.
Combat impact, music and voice are not this piece.

Round 1 built thirteen procedural four-layer beds, live/offline consumers, region changes, PCM render,
census, spectra and positional models. Its critic verified layer census/separation but found emitters
computed without scheduling sound, non-scalar gain modulation, day-only evidence, silent interiors and
settlements, unbounded irritating emitter periods, an inert B4 tool flag and no admissible listening
test. Round 2 fixed live emitter consumption and gain scaling and added settlement/interior beds and
event analysis, but ended honestly red: fixed-seed `street`/`well` nondeterminism; B5 only 11/13
(Clay Moor -31.818 LUFS-I and Deep Marshes -28.003); onset O1 278/453 with three silent bed/tod arms;
O2 13/40 below +3 dB crest; corrected per-bed O3 had three failures. The following critic upheld
determinism as strong but found 54/413 events outside the event band, a vacuous 4 s mute control, and
no valid quality/listening conclusion. The r3 blind judge proved its feature-only SAME/DIFFERENT pack
could not answer the requested quality question and exposed duplicate/non-independent trials and
byte-identical day/night labels. `W1-22-r3` remains an unfinished build status, not a verdict.

Open ledger entries are `GAP-W1-22-event-level-graded-on-a-median` and
`GAP-W1-audio-emitter-consumption`; the latter has strong later closure evidence but remains open under
the ledger's later-wave closure rule. Preserve that evidence, do not rebuild it gratuitously. Stale
claims: audio is not absent; emitters are not silent; 20 s feature vectors are not a listening panel;
13 exterior beds do not satisfy RI-WLD08's settlement/interior mass; a province median cannot enforce a
per-bed/per-layer band; day labels do not establish a different night selection; and spectral proxy
1.000 is not RI-AUD03 B2 or RI-WLD04 M20.

## Governing-requirement map

| Bar | Preserve if ancestry/digests match | Fresh verification or build/remediation required |
|---|---|---|
| RI-AUD03 R1–R3 | Independent census: 13 beds, four declared layers, 13 unique L1; seeded renderer substantially established. | Re-run B4. Prove fixed-seed byte identity and changed-seed event scheduling on all 13 scored exterior captures and fixed-seed byte identity for every settlement/interior bed counted toward R4/RI-WLD08 mass. A shared scheduler is not equivalence evidence for data-dependent renders: the prior `street`/`well` failures already falsified that shortcut. Root-cause those failures. Representative runtime coverage is permissible only for beds outside the counted population after a digest/code-path proof of equivalence. The named `content-stats --audio` must either implement B4 and self-test red or the item must formally point to `ambience-census.mjs`. |
| R4 interiors | Later beds exist and audio-side consumption instrument exists. | Census >=8 distinct settlement + >=4 distinct interior beds, all reachable named cells selecting the correct bed rather than silence/filtered exterior. Perturb each class and observe live PCM/state change. |
| R5 night | No admissible existing proof; byte-identical day/night labels were found. | Same point/seed at day and night for all 13; >=10/13 genuinely change selection as RI-WLD08 requires, not merely filter/gain. Night-only L2/L4 must execute. Null ToD routing must make comparison red. |
| R6 lore cross-references | Data declarations exist. | Render and listen to both paired motifs; prescribed relationship recognizable by fresh judges, with motif-deleted null. |
| R7/B6 positional emitters | Critic's unchanged probe moved live peak by 32.5 dB after +40 dB buoy perturbation and audioLog gained emitter rows. | Three required emitters, 200 m transects, audioLog pan/gain monotonic with bearing/distance, correct 600 m buoy reach, real PCM audible. Bound every emitter period so none meets the item's 15 s irritation failure; no silent default period. No-listener arm must remain silent and placed-listener arm must differ. |
| B1 | None. | 13 fresh judges, actual 20 s playable audio from the literal seed-1337 §Comparison-method capture, one clip/region, exact §C prompt; >=10/13 Q2 triples correct, hard fail <=4. Signature-forced or additional-seed clips are diagnostic only and cannot enter B1's score. |
| B2 | Existing feature pack is inadmissible. | 78 fresh independent pair judgements over actual audio, exact SAME/DIFFERENT prompt; >=0.80 correct DIFFERENT, hard fail <0.50. Use the canonical thirteen seed-1337 recordings in all 78 unordered region pairs, so each recording necessarily appears in twelve scored pairs; independence comes from one fresh judge context per pair, not from replacing or duplicating the canonical recordings. Add separately labelled, preregistered SAME catch trials; >0.25 false-DIFFERENT invalidates the instrument, and catch trials never enter the 78-pair score. Mapping sealed outside pack. |
| B3 | None. | Q5 from 13 fresh B1 judges; generic_rate <=0.25, hard fail >=0.75. |
| B4 | Strong independent census exists. | 13/13, all four layer keys including explicit null, 13 unique L1, L3 8–40 s and L4 45–180 s per region, concurrency <=8. Per-emitter period audit added because signatures moved outside L3. Any shared L1 hard-fails. |
| B5 | Prior calibration is stale/red. | Apply S41 to the frozen seed-1337 centroid/full-live-graph pre-normalisation clips: L1–L4 plus every R7 emitter audible there; 13/13 at -28..-24 LUFS-I except Stone Wastes -34 +/-2; any >-18 hard-fails. Calibrate to convergence, then fresh rerender. The scorer rejects bed-only or moved-listener input. R7-muted same-centroid control must change at least one in-range clip; an alternate-listener/bed-only arm must not count. Per-event levels are enforced per bed/layer/event—not median—with every event in the governing band. Absolute >20 dB crest and >=3 dB crest lift are report-only diagnostics. |
| RI-WLD04 M20 audio identity | PCM/spectra show machine separation but cannot be substituted. | 39 actual 20 s clips, 3 preregistered points/region, no music/combat, shuffled once; one fresh judge assigns all clips from the audio-bed column alone; >=26/39, fail <18/39. Report the confusion matrix. Where a frozen authoritative centroid is one of the preregistered M20 points, reuse that region's canonical **pre-normalisation** seed-1337 recording rather than record an identical second artifact; M20 does not direct loudness normalisation, so its point may not consume the B1/B2 −23 LUFS-I derivative. Record only the other 26 clips. Do not add night/worst-weather M20 panels: those arms belong to M17, while audio day/night is covered natively by M45 and the R5/§6 gates. |
| RI-WLD04 M17–M19/M21 seams | Primarily other paths; audio axis is now measurable from PCM. | Establish the score-8 aggregation without rerunning sibling work: cite current-HEAD admissible M17 (>=33/39 day, >=27/39 night) and M18 (all 78 pairs >=6/9 axes), recomputing only the fresh digest-matched PCM audio axis. For M19/M21, score 8 does not require their positive targets, but their explicit whole-item failures still govern: cite/check that no ONLY-HERE element is shared and no enemy level-scales. Any <=3-axis pair or other item lose condition is hard red. Run a sibling method afresh only when its existing commit/digest/independence evidence is absent or stale; W1-22 never forges it. |
| RI-WLD08 audio §6/M45 | Exterior bed mass partly established. | >=13 exterior + >=8 settlement + >=4 interior; >=10/13 ToD changes; >=10/13 weather changes; >=25 positional emitters/km²; audio identifies >=9/13. M45: 60 s at 12 preregistered points day/night, >=3 audible positional emitters at >=8/12 and spectral difference at >=10/12. |
| RI-WLD08 M40–M46 aggregation | No audio-only result can pass this whole item. | Cite current-HEAD admissible sibling evidence for the score-8 predicates: M40 >=4 events/10 min at >=8/10 and first event <=150 s at >=8/10, with no zero point; M41 target; M42 >=3 sightings/min, >=2 predations and >=6 pairs, with a nonempty player-absent log; M43 all 13 >=4 reachable states and fewer than 6 mechanically inert worst-weather regions. Beyond that band, check only explicit lose conditions: M44 noon/night DeltaE >25 and M46 our median not below Morrowind's. Reuse sealed independent M44/M46 evidence when commit-relevant inputs and artifacts remain valid; do not rerun their full visual point counts or blind panel merely because audio changed. Run a sibling method afresh only if evidence is missing, stale, unsealed or dependency-invalid. |

## Sequential execution and exact evidence

1. Stamp HEAD and bed/data digests; run boot/data/corpus gates and the census. Enumerate every computed
ambience model and world-fact-accepting method on the harness surface before claiming RI-MTH07. For each
model, name the shipping world-side consumer and frame/call site, perturb only that model between two
well-separated values, record the predicted and entity-side/live-PCM outcomes and coupling ratio, and run
the predicted-no-change null. Audit hand-fed facts for an equivalent world-derived route and declare every
trace/log-only field. Any cited partial-world/offline harness must implement and run `VERIFY` against the
shipping entry point, including a scenario longer than its longest cited run. Confirm the live AudioContext
and offline paths consume the same graph, including at least a bed, selection routing and an emitter; do not
treat a harness return or `audioLog` row as the observable. Preserve existing results only when commit/code/
data digests and the verified consumer path match.
2. Fix determinism and correctness before mixing: each of the thirteen scored exterior
region/bed/tod/seed captures must yield byte-identical PCM; different seed must change scheduled events.
Every settlement and interior bed counted toward the >=8/>=4 population must also reproduce byte-identically
at its fixed seed; static census plus a representative render cannot establish that data-dependent property,
and the existing `street`/`well` counterexample forbids assuming shared-code equivalence. Day/night and weather changes must be content selection.
Sabotage RNG, ToD and weather consumers individually and require their target check to go red.
3. Complete content mass and reachability: >=8 settlement, >=4 interior beds, and the RI-WLD08 density of
>=25 positional emitters/km² over the authoritative province area/population. Report numerator,
denominator, square-kilometre unit, per-region distribution and reachable live cells; a pile in one cell
does not satisfy the world-wide population.
4. **Freeze and apply S41 before calibration.** Manifest the thirteen authoritative centroids and
full live graph before rendering. Use only the 20 s seed-1337, clear, 13:00, settled centroid captures:
pre-normalisation for B5, their −23 LUFS-I derivatives for B1/B2. R7 participates wherever audible.
Longer, forced-signature, other-seed, bed-only and moved-listener captures are diagnostics only. Report
crest values without gating them. Calibrate fresh PCM; B5 is conjunctive 13/13 and the event band is per
individual event. The R7-muted same-centroid control must change at least one in-range clip, and the scorer
must reject bed-only/moved-listener inputs. The event mute control uses >= the maximum interval (never 4 s),
requires `full.fired>0 && muted.fired==0`, and a no-op mute must exit nonzero.
5. Build consolidated blind packs containing actual audio. Normalize only what the item directs; audit
metadata, filenames, duration, loudness and ordering for tells. For B2, reuse the thirteen canonical clips
across their required 78 pairs while assigning each pair to a fresh context; never manufacture 156 different
scored captures. Preregister catch-trial invalidation separately, seal mapping, get fresh judges, and hash
answers before reveal. Reuse each canonical **pre-normalisation** recording as one M20 point wherever its frozen centroid
is preregistered and eligible; the −23 LUFS-I B1/B2 derivative is not native M20 input. Render only the remaining points. Run one shuffled 39-clip M20 panel with its
single fresh judge, not duplicate night/weather panels. Do not infer a quality verdict from identity answers.
6. Run RI-AUD03 B1–B6 and complete RI-WLD04/08 aggregation. Acceptance is conjunctive: RI-AUD03 **6/6**
with no hard fail; RI-WLD04 native score >=8, its score-8 predicates, and no lose condition; RI-WLD08 native
score >=8, all §6 audio predicates, M45, and no lose condition. Reuse dependency-valid independent sibling
evidence for non-audio aggregation and rerun it only on the invalidity conditions above. RI-MTH07
must pass min-over-axes for every enumerated model, with no zero coupling or unverified cited partial harness.
No item score, median, machine feature or cross-world average substitutes for a failed governing predicate.
7. Evidence pack: commit/timestamp/load, commands, raw PCM and lossless listening artifacts, all manifests
and sealed/revealed hashes, per-bed/tod/weather metrics, event rows, transects/audioLog, population census,
fresh sidecar digest, blind answers/confusions, and baseline/fix/delete-fix/null outputs. Run aggregation,
boot-check, check-data and corpus-index. The eventual critic independently reruns B4/B5/B6 and samples
audio; it does not accept builder feature summaries as listening evidence.

## Hard-fail/control policy and decisive test

Any governing hard fail/lose condition, missing actual audio, stale PCM sidecar, nondeterminism at fixed
seed, inert positive arm, no-op mute passing, listener arm identical to no-listener, duplicated blind trial
counted as independent, opened reveal, or unmeasurable required method is a hard stop. Delete the relevant
fix on a copy: emitter scheduling must return to bit-identical +40 dB arms; gain scaling must return to
non-scalar behavior; ToD removal must return day/night equality; bed deletion/shared L1 must fail B4;
event deletion must fail onset and blind signature checks. Restore and rerun the combined suite.

Dependencies: W1-02 supplies region identity/area and other M18 axes; W1-04 supplies settlements/interiors;
W1-01 supplies weather/world; W1-11 supplies impact headroom; W1-05 supplies wayfinding. Cite their
current-HEAD evidence or mark the governing aggregation red—not waived.

**Decisive test:** A builder cannot get this plan green and fail a governing bar because green requires
every literal RI-AUD03 B predicate, the audio-relevant and aggregate RI-WLD04/08 methods, native scores,
hard-fail/lose clauses, actual-audio blind methods and live consumption controls. No proxy is allowed to
replace the reference comparison.

## Plan critic focus

Attack the apparent tension between the item's 20 s blind clips and sparse signatures, whether seed
selection biases the population, whether >=25 emitters/km² can be gamed by clustering, whether “actual
audio” reaches the judge interface, and whether requiring whole RI-WLD04/08 native score over-claims
W1-22 ownership while still correctly preventing an audio-only false pass.
