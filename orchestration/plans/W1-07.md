# W1-07 — character creation and opening: current-state continuation plan

Plan-State: awaiting-recriticism

## Canonical history and governing items

Governing items: RI-JRN09, RI-JRN01, RI-CHR01, RI-CHR02, RI-CHR03, RI-PRG02 and RI-PRG03.
Three criticised rounds moved creation from API/grey panels to a scene with a title, two named
people, readable questionnaire, named-class model, stamped/readable writ and consumed race greetings.
The current verdict still fails: the class-naming **line** is drawn 0/12 at its only opportunity
because UI overflow sacrifices prior spoken lines; the builder's 12/12 test searched the class-name
substring over the whole route and was satisfied by the later writ. Full-roster race dialogue is
370/520 mute pairs. Several native axes were never measured and min-over-axes correctly makes
RI-CHR01/PRG02/PRG03 zero. W1-07-r4 added quest race-gate work, but that dependency must be re-derived
against these items rather than treated as global closure.

## Classification

| Requirement | State | Standing |
|---|---|---|
| Creation is a rendered scene; questions readable; writ rendered/readable; title surface | independently established and preserve | Prior independent captures/readbacks closed these gaps. |
| Named profession computed (14/14) | independently established and preserve | Model diversity passes; do not confuse it with speaking the line. |
| Naming moment on frame, RI-JRN09 M3 | remaining remediation | 0/12; governing predicate is exact responsive line at its node in 100% named runs (critic continuation raises statistical population below). |
| Responsive exchange / AC | fresh verification + remediation | Existing AC evidence is mostly writ fields; require responsive spoken lines, not printed identity. |
| Race greeting/gate consumer | established locally; fresh full-population verification | Perturb/restore passed scoped census; full roster volume/distribution remains deficient. |
| RI-JRN01 full first-launch method | fresh verification | Previously unrun checks are not corpus debt now that tools exist. |
| RI-CHR01 legibility and all native axes | fresh verification | Unmeasured axis makes min score 0. |
| RI-CHR02/03 full race/birthsign methods | fresh verification/remediation | Preserve AR-3 race consequences; verify every race/upbringing/sign consumer and distribution. |
| RI-PRG02 M5 earned fraction | remaining measurement/build if red | Never measured; min-over-axes zero. |
| RI-PRG03 skill→damage and air swings | remaining measurement/build if red | Both never measured; do not award from data census. |

## Exact continuation

1. **Naming beat.** Give the responsive class-naming sentence its own non-sacrificable beat and
   acknowledgement, or an equivalently provable rendered surface. Reuse unchanged
   `corpus/90-verdicts/wave1/artifacts/W1-07-r3/critic-w1-07-r3g.mjs` first. Population: ≥240
   completions covering all 10 races × 4 upbringings and named-match/nearest paths, with published
   a/b/c/d histogram. PASS: exact `on_match_named_class`/`on_nearest_named_class` line appears in
   `getRenderedText()` at the speaking node in ≥99% and RI-JRN09 M3 remains 100% on its required
   named runs. Deleting the line yields 0%; restoring returns the result. A class noun on the writ
   never counts.
2. **Overflow observability.** If sacrifice remains, expose sacrificed lines in UI metrics. Force an
   overfull panel: at least one line must appear in `sacrificed`; normal naming beat must report none.
   An accessor that reports computed-but-clipped text as rendered is a hard instrument failure.
3. **Exchange quality.** Run RI-JRN09's native DTR/AC/NAMED methods on the full matrix. DTR_q must
   remain 1.0 and scene DTR above its native band; AC evidence must contain ≥4 responsive spoken
   lines, not six static writ fields. Preserve readable questions, scene and writ. Run aggregate,
   not isolated M3.
4. **Race/birthsign.** Use `tools/analysis/creation-audit.mjs`, AR-3 encounter traces and live
   conversation census. Cover every shipped NPC × 10 races, all four upbringings and all birthsigns.
   Report mute fraction and per-race distribution. Perturb each model and require dialogue/world or
   combat behaviour to change, then restore. An empty NPC population or API-only return fails.
5. **RI-JRN01 complete released population.** Drive first launch through the first consequential
   choice with the native journey tool and execute **M1–M15 and M20**, on every native seed, route,
   viewport, first-run and second-run fixture. Use the named, demonstrated-non-empty rendered-text
   accessor for M9 and for M15's sweep of every string rendered before minute 45; M15 requires zero
   prophecy-vocabulary hits and any hit is a hard fail. Execute M20 from a fresh browser profile with
   an existing save in IndexedDB: a title must precede control, expose the exact O3 set, and `Continue`
   must load that save; no title or no pre-control route to the save is HF9. Then dispatch an
   **enforced-isolation JC-01N** agent with only the URL, driver protocol and one-sentence premise.
   Record, verbatim and pre-reveal, the item's six questions in their stated order (including the
   pre-touch expectation; the told/found ordered account; “Who are you? How do you know?”; “What were
   you asked, and by whom, and where were you standing?”; “What are you supposed to do now, and who
   told you?”; and “List everything you did not understand.”). Apply M16–M19 without paraphrase:
   questions 3/4/5 answerable without a menu, 3/3 (answer 4 naming a character-creation screen is a
   hard fail); expectation delta recorded as the required quotation; `found_fraction ≥ 0.30`; and
   every confusion triaged, with defects ≤1, hard fail at ≥3 or any progress-blocking defect.
   Build the native anonymised, length-matched 12-line ours/MW blind pair, recorded coin flip and
   prewritten identity-produced-by-an-in-world-character question for a fresh judge. The builder does
   not judge it. Carry HF1–HF9 into the aggregate. Canonical `corpus_debt` rows remain present and
   score fail-closed under the item's runnable-fraction rules; they never disappear from coverage.
6. **RI-CHR01.** Execute every axis including legibility on the rendered scene at governing viewport.
   Report min-over-axes. Long names, pangram, every race and both named-class branches are fixtures.
   Clipped/hidden text, missing input route or an unmeasured axis keeps native score 0.
7. **Progression.** For RI-PRG02 M5, drive level 1→10 and report attribute points earned through use
   divided by total points, in fraction/percent against the item's band. For RI-PRG03, same weapon,
   statblock, attributes and RNG at skill 5 versus 75; report native damage ratio. Count air swings
   that connect with nothing against the exact item band. Confirm the live combat consumer by
   perturbation; sheet-only changes fail RI-MTH07.
8. **Blind/reference work.** Where an item calls for comparison, package actual rendered frames/text,
   equal presentation, shuffled seed, quarantine/reveal and a fresh judge. No feature-vector-only or
   builder-judged substitute.

## Controls, artifacts, dependencies

Required controls: delete naming line; remove independent beat; force overflow; blank writ class;
perturb/restore race and birthsign; freeze skill register; skill 5/75; air-swing/no-target; creation
API without render. Each must uniquely go red; if deleting one of two guards changes nothing, run a
2×2 and label the interaction. Hard fail on any governing hard fail, unmeasured min axis, stale
whole-route substring test, missing population, or aggregation regression. Artifacts go to a new
`reports/w1-07-r4/` and eventual critic directory, preserving prior evidence.

Dependencies: W1-26 title/character surfaces; W1-08/W1-29 input modalities; quest race-gate content;
W1-09 combat and progression consumers. Their work is reused where proven, but W1-07 retains every
applicable acceptance row. Stale assumptions include “class name visible somewhere means named
aloud,” “model text is rendered text,” and “tool absent” for the previously unmeasured axes.

## Falsification audit

The main pass-through loopholes are the writ substring, API-only values, static fields counted as
responses, and min axes omitted from aggregation. Exact-node line matching, render/sacrifice
readback, responsive-line evidence, live consumers and full native aggregation close them. Strongest
critic move: run the existing r3g instrument unchanged and compare exact line versus class substring
side by side before reading any new builder summary.

**Critic hand-off:** awaiting fresh re-criticism; no satisfaction claimed.

## Independent plan critique — 2026-08-09

**Verdict: BLOCKING / awaiting-remediation.**

Step 5 promises RI-JRN01's “full” method but explicitly scopes execution to **M1…M14**. The governing item continues through M15 and the isolated naive M16–M19 checks, and adds M20's fresh-profile/existing-save title/Continue route with a named hard fail. A builder can therefore execute the plan literally, pass every listed check, and never test prophecy leakage before minute 45, whether a naive player recognises the scene/choice, or whether Continue reaches an existing save. Existing title captures do not establish M20's fresh-profile persistence predicate. The generic blind paragraph does not repair omitted non-blind rows.

**Required remediation:** replace M1…M14 with the complete currently released RI-JRN01 population, explicitly including M15, enforced-isolation M16–M19 with their verbatim questions/thresholds, M20's fresh-profile existing-save Continue fixture, and the item's blind pair. Carry each named hard fail into the acceptance aggregate; rows blocked by a canonical corpus dependency must score fail-closed rather than disappear.


## Remediation — 2026-08-09

Resolved the recorded RI-JRN01 population blocker by making M15, enforced-isolation M16–M19, M20,
the native blind pair and HF1–HF9 explicit. This material edit awaits a fresh reviewer-editor.
