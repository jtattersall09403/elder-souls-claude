# W1-14 — magic: current-state continuation plan

Plan-State: satisfied

## Builder/critic execution allocation (binding)

This plan incorporates [`BUILDER-EXECUTION-CONTRACT.md`](BUILDER-EXECUTION-CONTRACT.md).

**Builder-owned:** implement every missing effect consumer, duration/area behaviour, moving-target
projectile repair, summon side, frenzy movement/targeting, on-use/on-strike/constant enchanting,
reachable service and travel-magic/VFX change; validate all 55 effects, 72 spells, legal dials,
recipes and service records statically; run one live representative per distinct delivery,
behaviour and dial family, every enchantment class, one reachable service/quest path, clean
Mark→Recall, one travel refusal and a small rendered VFX sample; run targeted red/delete arms; and
finish the bounded builder aggregate.

**Critic-owned:** the complete 55-effect/dial/behaviour populations, 200-cast determinism, 40 moving
plus 40 stationary projectile trials and all native projectile classes, complete thirteen-pair
travel-magic protocol, full quest/skill populations, complete VFX metrics and packs, independent
blind/reference judgement, final aggregation, scores and verdict.

Exhaustive static mapping plus one production example per implementation family proves that the
magic build is wired. Large causal/statistical populations and visual quality remain critic work.

## 0. Canonical reconstruction

At HEAD `50821cb25dadbe253c2f6394118d16fb6baaa2b9a`, five build rounds and four independent
verdict rounds exist. The history moved from 5/55 functional effects, dead quest runtime and no VFX;
through 55 handlers and a frozen private skill register; through skill unification, kind-aware
wards, status consumers, travel fences and three-dial instrumentation; to player-facing
spellmaking. The latest independent verdict (r4) still lost: RI-MAG01 55/100 (moving targets),
RI-MAG02 5/10 (duration), RI-MAG03 5/10 (enchanting inaccessible), RI-MAG06 6/10, while
RI-MAG04/PRG03/QST05 were not remeasured and RI-MAG05 carried an unretired forbidden-anchor hard
fail. Round 5 subsequently added/fixed work but explicitly left on-strike/constant enchantments
unconsumed, four duration effects blind, summon side dropped, frenzy unread, dial census without a
world-only signature, five of six service posts unreachable, and four governing items unmeasured.

Governing items from the owned paths and current `judges:` metadata: RI-MAG01–RI-MAG06 and **RI-TRV02**, plus controlling ARBITRATION seams and accepted amendments. `RI-MAG06` joins the six items named by the decomposition because its current metadata directly judges four owned magic-effect paths. `RI-PRG03` and `RI-QST05` do not directly judge a W1-14 owned path; MAG03 owns this piece’s magic-skill gate and MAG04 owns its magic quest-solution bar, so those broader progression and quest items are dependency evidence rather than additional closure authorities. Old PASS/fixed labels are preservation hints only.

## 1. Requirement disposition

| Bar | Current state | Required action |
|---|---|---|
| MAG01 cast frames/cost/determinism/focus | Previously strong, **stale**; moving-target projectiles failed beyond ~6 m. | Preserve exact frame tables and S27; remediate intercept/steering against moving populations without extending cutoff. |
| MAG02 three dials and behaviours | Magnitude broadly consumed; **duration unmet** for fire/frost/poison/restore; area/world-only proof incomplete. | Make each legal dial causally affect its specified observable, with saturation/clamps; no archetype mutation or touch-per-frame multiplier. |
| MAG03 spellmaking/enchanting | Spellmaking reachable/proven; **enchanting unmet**: on-strike and constant records have no combat/equip consumer. | Wire both through real player service, equipment and combat paths; preserve price/capacity/soul rules and S19 travel fence. |
| MAG04 / QST05 | Earlier live routes existed but latest rounds **did not remeasure**. Five service NPCs are unreachable. | Fix via owning traversal piece, then replay native quest samples and AR-3 crossings with zero hand-fed gates. |
| TRV02 travel magic | **Governing but omitted from the previous acceptance contract.** Mark/Recall and both Interventions exist, while the complete positive/solvent pair has not been re-established at current HEAD. | Run the native P1–P6 and D1–D7 paired checks. Preserve exact catalogue, destination, placement, cost, clock, quest-route and soul-stain predicates; absence cannot pass the solvent half. |
| MAG05 | **Unresolved hard fail/stale.** r3 forbidden white anchor stands; later VFX work did not receive retirement. | Rebuild blind/reference pack from actual rendered artifacts; measure ART and FID separately, exact ΔE2000 and feature rows; independent judge required. |
| MAG06 census | Instrument improved but **unmet/partial**: 53/55 all-path, 45/55 world-side in r3; round-5 admits world-only signature absent and inert-control risks. | Make world-side consumer the binding signature; vary magnitude, duration and area independently across all 55 effects; controls must reach handlers. |
| MAG03 skill gate | Skill register/by-use repaired and earlier measured; **stale**. | Prove creation→casting→growth→MAG03’s spellmaking/enchanting gates→save/load without `setSkills`/`setMagicSkills`; consume dependency-valid general skills-by-use evidence rather than rerunning RI-PRG03 wholesale. |
| Cross-system seams | **Unmet:** `engine.spawn` drops side; frenzy target unread; service reachability dependency; enchant hooks overlap W1-09/16. | Coordinate, do not duplicate. Consumption must show world behaviour, not registers. |

## 2. Execution order

1. Freeze the 55-effect, 72-spell, legal-dial, quest-resolution, service-NPC, enchant recipe and VFX
   manifests at HEAD. Any denominator loss is red.
2. Repair the census before trusting a build: snapshots exclude private magic registers; every arm
   proves `effect_apply`; null-vs-null drift is zero; `--break` reaches the same handler and changes
   only the dial/consumer under test.
3. Fix duration/area consumers and any projectile defects exposed by MAG01's native M4–M6 checks.
   Run the cheap declared-data/zero-PRNG checks before browser flight traces. Keep stationary and
   lateral near/mid/far strata only as bounded adversarial diagnostics; do not tune to a local hit
   rate or require a particular guidance algorithm.
4. Connect summon side, frenzy movement/targeting, on-strike, and constant enchantments to real
   world consumers. Commission through conversation; equip/use/fight without harness-only verbs.
5. Consume W1-04 reachability repairs, replay MAG04’s native quest/AR-3 population, and prove MAG03’s skill gates from a newly composed
   character. In the same player-path journey, run TRV02's clean Mark→Recall positive first, then its
   cheap static catalogue/topology/picker gates, and only then the remaining live Intervention/combat/
   dungeon/soul-stain pairs; stop immediately on a hard fail instead of completing an already-lost sweep.
6. Retire or confirm MAG05's hard fail with an actual blind pack and aggregate every item. The plan
   cannot be satisfied while that carried hard fail or any mandatory not-run remains.

## 3. Acceptance contract

| Predicate | Exact population and comparison | Instrument/evidence |
|---|---|---|
| MAG06 full census | Native M1–M8 over all **55** effects: delivery at the method's 6 m/1.4 m fixtures, treatment versus no-cast paired world read, native behaviour classes and denominators, mechanically flattened world-side signatures, declared-flag cross-check, named unmeasurable owners, and arena audit. Binding thresholds are the item's min-over-axes bands, including EFFECT-FUNCTION and DISTINCT-VERBS ≥50/55 for the top band and every hard fail. Private registers, commission gold and caster shuffle are excluded. | Reuse `tools/harness/w1-14-r3-dials.mjs` and the existing census/reference implementation. Per-effect leaf diffs, events, clamps and build stamp. `--break=nocast`, bind/flee/consumer and arena-gate arms must reach the same delivery/consumer path and go red. MAG02 dial causality is tested separately below rather than multiplying MAG06 into a non-native Cartesian sweep. |
| MAG01 M4 commitment/aim latch | Every cast class, every competing action on every `k ∈ [1,total]`: none before `ceil(0.60 × recovery)` past last active changes ACTIONABLE or later executes; roll during startup/active grants 0 i-frames. In **30** full-speed-strafe-after-`Tc` trials, median release residual aim error **≥25°** (fail <10°); yaw rate ≤120 deg/s on frames 1..Tc, ≤2 deg/s afterwards, integrated startup yaw ≤100°. | Existing cast/frame probe. A latch-disabled arm must breach the residual/yaw predicate; cancellation/i-frame guards are independently broken and observed red. |
| MAG01 M5 geometry, not dice | At three seeds, **200** identical stationary casts have byte-identical damage and **0 PRNG draws**. For **40** perpendicular 3 m/s moving and **40** stationary projectile outcomes, offline swept geometry reconstructed from per-frame `pos` and exact radius explains **40/40** in each population. | Existing projectile/geometry trace and offline reconstruction. Perturb radius/position in the reconstruction so agreement drops; static random/chance path audit remains zero. |
| MAG01 M6 projectile ballistics | Every projectile class: speed within **±0.2 m/s** of §E, lifetime **±1 f@60**, radius exact, turn rate **≤2 deg/s** on every post-cutoff frame, and no declared spell speed >30 m/s. Every enemy spell at its encounter design range has `min_dodge_window ≥20 f@60`. | Existing projectile trace/data audit. Force post-cutoff steering above 2 deg/s and require red; no algorithm-specific delete-lead requirement. Stationary/lateral 3/6/12/20/30 m results may be retained as supplementary diagnostics only. |
| Duration/area causal | fire, frost, poison, restore plus every effect declaring those dials. 0/base/max duration yields item-specified hp/status curve and exact expiry; 0/base/max area changes only bodies/props inside measured metres. Read at apply, expiry, +30/+120/+600 f@60. | Dial census plus live body/world snapshots. Equal curves or private-only diffs fail. |
| Enchanting consumed | Full legal recipe classes, including on-use/on-strike/constant. Commission via reachable NPC conversation, pay exact quote, save/load, equip, and observe combat/equip behaviour. Treatment vs delete-hook must differ; unrelated item control must not. | Player-path journey trace, inventory/equipment/combat events, live post-load audit. No call to harness quote/make/open verbs. |
| Magic quest/skill gates | Exact MAG04 native method population, including M6 sample size, M7 crossing threshold and every MAG04 hard fail; a new character casts correct schools until skill grows, MAG03’s spellmaking/enchanting gates open, and state survives load. No setter/hand-feed token in probe. General QST05/PRG03 rows are consumed only as dependency evidence and do not expand this build. | Existing magic quest and skill instruments; terminal journal, zero-death traces and dependency stamps. |
| MAG05 ART/FID | Exact current palette ΔE2000 thresholds and feature checklist, mandatory V1/V2/V4, budgets, forbidden-anchor hard fails, and item blind thresholds. Pack contains rendered images/video, not features; independent judge, concealed labels, reveal after answers. | Capture manifest with pose/light/build, pixel samples and raw judge answer. Delete VFX and forbidden-white injection must respectively make FID/ART red. |
| TRV02 travel-magic network | All **13 paired checks P1–P6/D1–D7**, scored 2/1/0 on the native **26-point** scale, with P1/P2 required before any solvent pass. Bind: exactly four spells at 3,400/3,400/900/900 g, ≥3 vendors in ≥2 settlements, 210 f RITUAL and distance-scaled Focus; clean Mark→Recall within 5 m plus 20 world minutes; discovery-gated distinct Interventions ≥1,500 m apart; correct affordability/skill gates and ≥4 quest routes. Bind all five placement gates, all **12** destinations in the closed-shortcut reachable set, Mark refused in **all 8 dungeons + 82 caves**, 0 wave-wide teleports within 300 f of combat, the 40-cast no-picker determinant, ≥60 m HEARTH separation, no `legs_walked` laundering, and the death-position soul stain. Every native hard fail and the absence-cannot-comply rule blocks closure. | Reuse the item-named travel/world graph and harness routes; one shared journey supplies P2, D1/D3 and D7 where fixture state permits, while static P1/P5/P6/D2/D4/D5 runs first. Positive and teardown arms must both be live: a clean cast must succeed, while combat/undiscovered/locked-volume/picker perturbations go red. |

The rows above target the known defects; they do not replace the complete item decisions below.
The frozen aggregate must consume every native row and preserve each item's own population and
aggregation—MAG06's bounded census does not narrow any other item:

| Item | Binding native decision |
|---|---|
| RI-MAG01 | M1–M8, weighted sum over the stated 15/15/15/15/15/10/10/5 weights; native **≥70/100**, every automatic fail absolute, and artifact-justified shared score **≥7**. M1–M3 and M7–M8 remain required even though the defect rows above expand M4–M6. |
| RI-MAG02 | All eight native min-over-axes populations (catalogue, out-of-fight breadth, formula, determinism, levitation, teleport, ten §H probes, economy), every axis **≥6/10**, every automatic fail absolute, and shared **≥7**. The targeted duration/area runs supplement rather than replace M1's 55-row diff, M2's complete spell arithmetic, M3's grep, M4/M5 live fixtures, M6's ten named effects and the full shelf/economy populations. |
| RI-MAG03 | All eight native min-over-axes populations: **20** unauthored spells, **100/100** both sides of all four gates, **30** enchant triples, complete S15 merchant/gem fuzz population, anti-farm sources, `xul_hesh`, **11** breakage rows and economy; every axis **≥6/10**, hard fails absolute, shared **≥7**. Player-path enchanting/skill evidence above proves reach/consumption but does not replace these populations. |
| RI-MAG04 | Q1–Q12, all eight solution shapes, M1–M8 including native **10** live samples and **7** AR-3 crossings; every min-over-axis result **≥6/10**, Q8/M7 and all other hard fails absolute, shared **≥7**. Use the canonical quest denominator and consume QST05 evidence without rerunning unrelated quest acceptance. |
| RI-MAG05 | Independent `min(ART,FIDELITY)`, each **≥6/10**, all native capture/reference/blind populations and hard fails, shared **≥7**; one fused number is void. |
| RI-MAG06 | Native M1–M8 and its 55-effect/arena populations exactly as stated in the census row; every axis **≥6/10**, hard fails absolute, shared **≥7**. No legal-dial Cartesian expansion. |
| RI-TRV02 | Native P1–P6/D1–D7, 13 paired checks on the 26-point scale; native **≥16/26**, P1/P2 prerequisite, all hard fails absolute, shared **≥7**. The MAG02 teleport axis cannot stand in for this population. |

All quantities use `f@60`, metres, degrees/second, Focus, gold, ΔE2000, and explicit
numerator/denominator. A missing consumer, unreachable required service, denominator shrink, carried
hard fail, inert arm, absent artifact, unrun native row, native meeting-band miss, or shared Wave-1
score below 7 is a hard block. Existing independent row evidence is reused when its producer,
consumer, population and commit dependencies remain valid; only affected/missing rows and their
controls are rerun.

## 4. Adversarial closure question

Yes, an earlier builder could green the 55-row census while pinning two dials, hand-feeding skill,
reading private registers, using still targets, and never exposing enchanting to a player. The
contract now binds the world-only signature, native MAG06 census plus MAG02's own targeted dial populations, moving targets,
player-path commissioning, post-load consumption, native quest/progression rows, and independent
visual judgement. A fresh critic must still try to construct a legal dial tuple, moving trajectory,
or enchant class omitted by the manifest.

## 5. Independent plan criticism (2026-08-09)

**Verdict: BLOCKING / awaiting-remediation.** The projectile acceptance invokes a nonexistent
“MAG01 native threshold” for hit rate in every moving-target stratum and requires a
pure-pursuit/delete-lead arm to go red. RI-MAG01 does not set a moving-target hit-rate floor. Its
governing comparisons instead require M4's aim-latch residual (median at least 25°, fail below
10°), yaw-rate/commitment limits; M5's 40 moving plus 40 stationary outcomes explained 40/40 by
recomputed swept geometry with zero PRNG draws; and M6's speed, lifetime, radius, post-cutoff
turn-rate and dodge-window predicates. A builder could define an accommodating local hit-rate
threshold, green the planned range table, and still violate aim latch, geometry, or enemy dodge
windows. Conversely, optimizing lead to an undefined hit-rate bar can directly weaken the native
anti-homing requirement.

**Required remediation:** delete the invented hit-rate acceptance and replace it with the exact
MAG01 M4–M6 predicates, populations and units, including the 30-trial aim-latch residual, 40+40
offline sweep reconstruction, zero RNG draws, every projectile class, and every encounter's enemy
`min_dodge_window >= 20 f@60`. Define sabotage arms against those native predicates (for example,
post-cutoff steering must breach `2 deg/s`, and a geometry perturbation must break the 40/40
reconstruction) rather than requiring a particular guidance algorithm to lose. Keep stationary and
lateral range strata as additional adversarial evidence, not a substituted bar.

No CARRIED finding is appropriate: the native comparison is fully specified before build.


## 6. Fresh reviewer-editor outcome — 2026-08-09 (current round)

**Superseded by §7.** Its governing-set correction remains valid, but its MAG06 acceptance retained
a materially wasteful non-native Cartesian population.

## 7. Fresh reviewer-editor outcome — 2026-08-09 (this round)

**Material edit / awaiting-recriticism.** The seven-item governing set is correct, but the received
MAG06 row required all 55 effects across every legal range and low/high values of three dials. MAG06
instead specifies M1–M8 over 55 effects at its 6 m/1.4 m delivery fixtures; MAG02 separately binds
formula fidelity and targeted live dial predicates. The row now states the complete native MAG06
population, axes and hard fails without commissioning a combinatorial sweep that cannot change its
decision. The separate duration/area causal row, world consumption, travel, blind independence and
all native thresholds remain mandatory. A different fresh reviewer-editor must judge this version.


## 8. Fresh independent reviewer-editor outcome — 2026-08-09 (r5)

**Material edit / awaiting-recriticism.** The MAG06 optimisation is valid: its actual M1–M8
population is the 55 canonical effects at the native delivery fixtures, not a legal-dial Cartesian
sweep. But the received plan did not executablely preserve the complete native MAG01–05 decisions:
its targeted defect rows and one generic `unrun native row` sentence could be green without the
MAG02 catalogue/formula/economy populations, MAG03's 20/100/30/merchant/breakage populations, or
the other full item aggregations reaching the Wave-1 gate. A binding seven-item decision manifest
now keeps every item's native population, hard fails and aggregation separate, while preserving
MAG04/QST05 and TRV02 seam ownership and reuse. A different fresh reviewer-editor must judge this
version.

## 9. Fresh independent reviewer-editor outcome — 2026-08-09 (r6)

**SATISFIED.** Independent reconstruction from the canonical owned paths and current `judges:`
metadata confirms that RI-MAG01–RI-MAG06 plus RI-TRV02 are the complete direct governing set. The
received seven-item decision manifest preserves each item's native population, decision rule,
meeting band, hard fails and shared Wave-1 gate. In particular, MAG06 uses only its native M1–M8
55-effect/arena population rather than the removed Cartesian dial sweep, while MAG01–MAG05 and
TRV02 retain their separate full populations and predicates. Dependency-stamped QST05/PRG03 reuse
does not substitute for MAG03/MAG04 evidence, and the plan orders cheap/static gates before bounded
runtime and independent visual judgement. No material edit was required beyond recording this
review outcome and state.
