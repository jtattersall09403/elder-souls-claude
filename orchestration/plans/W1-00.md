# W1-00 — harness, determinism, persistence: current-state continuation plan

Plan-State: satisfied

## 0. Canonical history

Planned against HEAD `50821cb25dadbe253c2f6394118d16fb6baaa2b9a`. Round 1 failed seed
sensitivity (the seed echo was the sole difference), signed PRNG round trips, allocation claims,
inside-step trace construction, and fail-quiet API shapes. Two fix rounds repaired PRNG
consumption/serialisation, moved trace construction, and added step-depth guards. Round-2 critic
independently proved RI-MTH02 R1–R9 (24/24), 0/40 round-trip hash mismatches, render-rate
decoupling and catch-up cap, but still returned **FAIL 4.1/10**: RI-JRN05 M5 found
`enemies[].prev_state` missing after load on 219/600 frames; platform/load/control rows were absent
or unmeasurable; API no-key events and null build metadata remained; allocation's literal-zero bar
was below instrument resolution. Amendments AM-W1-00-01/02 and C1 sharpen R4/R5 and absolute frame
semantics and remain binding.

Governing items from the canonical decomposition and current `judges:` metadata: RI-AUD02,
RI-CAM02, RI-CAM06, RI-CMB07, RI-JRN03, RI-JRN05, RI-MTH01, RI-MTH02, RI-PLT01 and
RI-PLT03, **plus RI-MTH07**, whose later metadata directly judges
`platform.determinism.harness`. RI-MTH04 remains the mandatory evidence-integrity doctrine for every measurement; it
is not a substitute for the omitted RI-AUD02 platform rows. Their current text and amendments are
the bar.

## 1. Disposition

| Requirement | State at last independent evidence | Required continuation |
|---|---|---|
| RI-MTH02 R1–R9 | **Independently proven then; stale now.** R4 used four seed pairs, moving `anim_frame`, with a no-entity discriminator; R5 used re-anchor/no-re-anchor. | Preserve semantics and rerun complete ladder at HEAD, including amendments and absolute indices. |
| RI-JRN05 M1/M2 round trip | **Proven then; stale now.** 20 seeds × 2 states, only volatile frame differed. | Expand moments, not just seeds; audit live world after load. |
| RI-JRN05 M5 continuation | **Unmet.** `prev_state` diverged 219/600 frames and triggered HF1. | Persist/reconstruct it correctly and require empty durable field diff for the entire continuation. |
| RI-MTH01 API | **Unmet/partial.** no-key input accepted; build commit/builtAt null; several surfaces untested. | Execute all 33 rows, reject malformed/empty events, and make metadata real or explicitly item-legal. |
| RI-PLT01 | **Partial.** M1/2/6–9/15 evidence; M3 corpus CPU index absent; M4 literal zero unresolved; M5 and Tier-H rows absent/forbidden. | Apply current item/rulings. Never turn SwiftShader N/A into PASS. Build missing census/instrument or record fail-closed. |
| RI-PLT03 | **Unmet/unproven.** one region prevented stream rows; named tools absent; Tier-H unavailable. | Do not fake a border. Consume current world topology, build item-named tools, and run all Tier-S rows; hardware rows remain explicit N/A only where item permits. |
| RI-AUD02 platform seam | **Implemented and independently measured in W1-11, but not bound by this plan.** It judges `platform.determinism.harness` as well as impact audio; A3 was hardware-unavailable. | Reuse dependency-valid W1-11 audio-budget evidence. Run the current 29 scored rows once at HEAD, including the pre-first-combat A3 observation on an audio-capable environment; every HF1–HF5 remains fail-closed. Do not rebuild the impact graph or duplicate W1-11's audio fixtures. |
| RI-JRN03 input seam | **Partial.** repeat suppression, no scroll theft and render-rate look passed; some input-pipeline rows remain unproven. | Run the exact rows that exercise `platform.input.pipeline`, including real pointer-lock/blur delivery where applicable. Reuse dependency-valid W1-08 evidence for rebinding, discoverability and its other six paths; do not commission another naive tester unless an in-remit dependency changed or that evidence is absent. |
| RI-CAM02 pitch | **Proven then; stale now.** unlocked −55/+38, locked −50/+32, no creep, 400° swipe ->30°. | Re-run item fixture and null a clamp to force red. |
| RI-CAM06 / RI-CMB07 | **Insufficiently measured.** Old continuation did not establish their full applicable rows. | Enumerate and run all in-remit native rows; no source-read substitution for behaviour. |
| Measurement integrity | **Partial.** evidence lineage good, but allocation displayed `max(0,slope)` and result changed with step count. | Publish raw signed samples, estimator uncertainty and resolution; never claim literal zero unless the method can distinguish it. |
| RI-MTH07 world coupling | **Governing and previously omitted.** Harness/model outputs can be correct while the running world ignores them. | Enumerate every relevant model W1-00 ships **and** every model from which a W1-00 harness result is derived, whether or not the model itself is exported on the harness; name every actual shipping world consumer and run the native treatment/null perturbation with non-inert controls. Raw entity-state getters are not models, and neighbouring input/journey models that W1-00 neither ships nor derives a result from stay with their owners. An unresolved cross-piece consumer is explicit seam debt and blocks this model rather than being averaged away. |

## 2. Sequence

1. At current HEAD generate one eleven-item/native-row manifest and verify every named tool exists; use
   it as the aggregate rather than rebuilding populations in per-item wrappers.
2. Repair RI-JRN05 M5 first on the five binding SV fixtures. Run the cheap in-process hash/diff and
   manifest checks before cold reloads, hostile-store runs or long continuations; stop on the first
   unexplained durable diff. Add `prev_state` to the durable contract or derive it identically from
   persisted state as the item permits, then require the focused delete-field arm to reproduce the
   known continuation failure.
3. Once all five SV fixtures pass the cheap gate, run their cold reload and 600-frame continuations,
   followed by CR1–CR12 and M10–M19 exactly once at their native populations. Idle/attack/hitstop/
   falling moments are optional diagnostics only if a binding fixture fails; they are not a second
   acceptance population and do not receive a 20-seed sweep.
4. Preserve the independently proven RI-MTH02 ladder and existing controls unless the save repair or
   HEAD changes their code/data dependency. Re-stamp its cheap aggregate at HEAD; rerun only an
   affected row and its existing falsifier, with full two-process R4/R5 required if dependency or
   aggregate output changed. Then close fail-quiet API rows and the outstanding in-remit native control, camera and combat rows.
   For RI-JRN03, rerun only rows whose predicate exercises this piece's `platform.input.pipeline`;
   consume dependency-valid W1-08 evidence for its other six owned paths rather than repeating the
   naive tester and unrelated UI/browser work.
5. Build/run only missing RI-PLT03 named instruments and RI-PLT01 allocation/GC methods; do not fork
   existing tools. Separate Tier-S failures from hardware N/A and finish with one aggregate.

## 3. Acceptance contract

| Predicate | Population / units / comparison | Evidence |
|---|---|---|
| RI-MTH02 = **24/24**, exact R1–R9 | Two processes; item scenarios; R4 >=5% differing frames outside seed echoes and max draws >0 on each qualifying moving-world pair, while no-entity null is 0%; R5 durable records byte/field equal after re-anchor and differ without it. | `tools/determinism.mjs` and committed critic probes; JSONL traces, canonicalised diff, seeds, absolute frames, HEAD/dirty. |
| RI-JRN05 M1–M9: binding save population | **SV1:** three stages into a five-stage faction quest, branch, giver disposition, learned topic and optional-conversation flag. **SV2:** shortcut, fog gate, four dead enemies, empty chest, unlocked door and kicked ladder, without HEARTH rest. **SV3:** rank-up <60 s old, rivalry lock, unlocked topics and trainer. **SV4:** bounty ≥500/0 across two jurisdictions, 2 live + 1 dead witness, 3 stolen items and 2 hunting guards. **SV5:** 40+ ordered journal entries, two-rooms-back bloodstain with 3,400 souls, disease with 6 h left, unread book and Mark. M1 hashes **5/5**, M2 diff empty unconditionally, M3 cold reload **5/5**, M4 bidirectional manifest set differences 0/0, and M5 never-saved versus loaded 600-frame `body_sha256` **5/5**, including `prev_state` mismatch exactly 0/600. M6 is individually **6/6** (shortcut, gate, enemies, chest, door, ladder); M7 exact bloodstain position/count/recovery; M8 exact bounty jurisdictions, witness identities, stolen flags and hunt state **4/4 + pay-bounty control**; M9 exact count, every number/date, and ordered-text SHA-256. | Existing RI-JRN05 tools plus `p10-m5.mjs`; one shared fixture manifest, pre/post live snapshots and first divergent frame. |
| RI-JRN05 M10–M19 and CR population | M10 IndexedDB and ≤1024 B localStorage/no payload. M11 CR1 = 3 kill points × 5 SVs = **15/15** generation n/n−1. M12 CR2–CR5+CR12 = **5/5** detected/reported. M13 CR6–CR10 = **5/5** playable, 0 silent loss/blank/unhandled. M14 CR11 export→wipe→import empty diff. M15 300-frame write: 0 dropped/over-budget steps. M16 endgame-200q ≤4 MB/1.5 MB/48 MB. M17 3 slots then one out-of-band deletion: exact loadable list. M18 one 20-minute session: required three autosave triggers, none in combat/manual overwrite. M19 restored stain then second death: exact replacement. All native hard fails remain binding. | Mechanically enumerated in the same manifest; reuse item-named storage tools and capture UI only for rows that require UI evidence. |
| API is fail-closed | All RI-MTH01 rows. Every malformed type, unknown key, empty event, invalid vector/EID/state throws and names the argument; all valid rows return exact schema; build info non-null if mandatory. | API matrix artifact. For each validator mutate one valid request and one implementation guard; both must make the check red. |
| Fixed-step/platform | M6 hashes and behaviour identical at render 60/30/15/0; M8 <=5 steps/rAF after 400 ms stall; M9 unit frame/dt exact. All other current RI-PLT01/03 thresholds apply verbatim. Allocation reported in B/step with confidence/resolution, not clipped. | Current item tools. Include software/hardware identity and load. Missing CPU reference/tool is red/unmeasured, never green. |
| RI-AUD02 Web Audio platform | **29/29** scored A/B/C/D/E rows with HF1–HF5 absent: one interactive context; `running` before first combat frame; timer/worklet scheduling at ≤10 ms with 25–100 ms lookahead and explicit future `start(playAt)`; 0 HTML audio elements, post-ready combat decodes and clean-run misses; all voice caps/steal/collapse/lifecycle rows; exact panner/listener rules. A3/A4/A5 may be reported N/A only where the item permits, never converted to PASS; A3 remains HF3 when testable and non-running. | Reuse `tools/analysis/audio-budget.mjs`, `tools/audio/impact-browser.mjs`, and the existing W1-11 fixture/artifacts when dependency stamps match. Run static/runtime budget first; use one audio-capable browser run only for rows Node cannot decide. The existing suspended-context and over-budget controls must go red. |
| Controls/camera/combat | Every applicable in-remit RI-JRN03, RI-CAM02, RI-CAM06, RI-CMB07 row at its exact units/tolerance and full population. Pointer-lock rows use actual browser events. Reuse still-valid independent artifacts where dependency stamps match; do not commission RI-JRN03’s naive/discoverability pass for this pipeline-only ownership seam or repeat browser work merely to refresh a date. | Per-item artifacts plus one aggregate manifest. |
| RI-MTH07 CONSUMPTION | Freeze an exhaustive census of **every relevant model W1-00 ships and every model from which a W1-00 harness result is derived**, including underlying runtime formulae/tables/predicates even when they are not directly exported, every harness method accepting world facts as arguments, every W1-00 trace-only field, and every partial-world harness whose number is cited. Exclude only raw entity-state getters and neighbouring input/journey models that W1-00 neither ships nor uses to derive a result; record that exclusion basis so it cannot hide an orphan. For each counted model name the **actual shipping** world call site/entity/frame before running evidence; a trace, census, probe, field write or sibling harness is never a consumer. Treatment and null runs must drive that same shipping path, hold other causes fixed, produce the item-native `{model_value_a,b, observed_a,b}` and `coupling`, and create distinct model values **and** an entity/world-side behavioural delta; a reset/config-only delta is insufficient. Every hand-fed result must also be reachable from world-derived facts. Every cited partial-world harness must run `VERIFY` through the shipping entry point over state sequences, including a scenario longer than its longest cited run. Min-over-axes must support an artifact-justified shared score **≥7**; any required model at `coupling == 0`, missing actual consumer, trace/probe-only reader, hand-feed-only predicate, undeclared trace debt, missing/failed `VERIFY`, inert treatment/null arm or native hard fail blocks closure. | Reuse the item-native coupling instrument/evidence and existing W1-08/JRN consumer evidence only where its producer, shipping consumer, population and code/data dependency stamps remain valid. Record producer/owned path, harness method/model, actual consumer call site/entity/frame, treatment, entity/world leaf delta, null delta, coupling and commit; do not create a second framework or rerun sibling journeys merely to prove the same unchanged consumer. |

**Null/delete arms:** unsigned PRNG restore must recreate old R9 failure; disabling one PRNG draw must
make R4 red; building trace inside step must throw; deleting `prev_state` persistence must recreate
the 219/600-class divergence; removing catch-up cap must exceed five; disabling camera clamp must
breach bounds. Each arm must be observed red and restored green. Empty populations, missing tools,
unreachable mandatory rows, a stale build stamp, any governing hard fail, or any required
`not_run` blocks closure.

## 4. Adversarial closure question

Could exact execution still miss a bar? The previous plan could, because two-seed round trips and
hash equality missed a live continuation field, and platform omissions were averaged away. This
contract repairs that with moment diversity, live-world continuation, full native-row manifests,
fail-closed omissions and item-native aggregation. Fresh criticism must especially test whether the five binding SV fixtures cover every RI-JRN05 durable population, whether hardware N/A is item-legal, and whether the frozen MTH07 census includes every W1-00-owned computed/hand-fed/partial-world harness surface without treating a field or probe as a consumer.

## 5. Independent plan criticism (2026-08-09)

**Verdict: BLOCKING / awaiting-remediation.** The proposed `20 seeds × 7 moments` save population
does not instantiate RI-JRN05's binding SV1–SV5 scenarios. The seven listed moments are engine-state
instants (idle, attack, hitstop, falling, death, money/inventory, AI transition), but the native
population deliberately includes a quest three stages in with exhausted dialogue and disposition,
a half-mutated dungeon, post-rank consequences, cross-jurisdiction crime/witness/stolen-item/hunt
state, and the post-death bloodstain/journal state. A builder can pass all seven planned moments and
the `prev_state == 0/600` headline while never testing M6–M9 or the exact five-scenario M1/M3/M5
denominators. “Every shipped state required” is not an executable replacement for those fixtures.

**Required remediation:** make SV1–SV5, with every state named in RI-JRN05 §D, the binding
round-trip/cold-reload/600-frame population; state M1/M3/M5 as exactly `5/5`, and enumerate M6
`6/6`, M7 exact bloodstain recovery, M8 `4/4 + control`, and M9 exact ordered journal checks.
Additional attack/hitstop/falling moments may remain adversarial supplements, but must not replace
the five native scenarios. The acceptance also needs the native M10–M19 and CR populations stated
or mechanically enumerated in a manifest, rather than hidden behind “all rows.”

No CARRIED finding is appropriate: the omitted native save populations are knowable before build.


## 6. Fresh reviewer-editor outcome — 2026-08-09 (current round)

**Superseded by §7.** This round correctly removed duplicated RI-JRN03 participant work, but its
ten-item governing-set conclusion predated the later direct RI-MTH07 metadata edge.

## 7. Fresh reviewer-editor outcome — 2026-08-09 (this round)

**Material edit / awaiting-recriticism.** Independent current-metadata reconstruction found an
eleventh governing item: RI-MTH07 directly judges W1-00's `platform.determinism.harness`. The
received plan could green every written row while a harness/model output remained unconsumed in the
running world. The governing set, disposition, manifest denominator and acceptance now include the
native exhaustive coupling/CONSUMPTION method and non-inert treatment/null controls. A different
fresh reviewer-editor must judge this version.


## 8. Fresh independent reviewer-editor outcome — 2026-08-09 (r5)

**Material edit / awaiting-recriticism.** RI-MTH07 genuinely governs through the direct
`platform.determinism.harness` metadata edge, but the received acceptance covered only the basic
consumer perturbation. It could go green while a hand-fed predicate, undeclared trace-only field or
cited partial-world harness without native `VERIFY` still failed MTH07. The acceptance now freezes
the complete W1-00-owned harness/model census and binds all five native axes, the shared Wave-1 gate,
entity-side proof and dependency-valid W1-08/JRN evidence reuse. A different fresh reviewer-editor
must judge this version.

## 9. Fresh independent reviewer-editor outcome — 2026-08-09 (r6)

**Material edit / awaiting-recriticism.** The received MTH07 census was exhaustive only over
W1-00-owned computed models *exposed through the harness*. That qualifier could omit an underlying
runtime formula, table or predicate from which a harness result is derived but which is not itself
exported—the same orphan shape MTH07 exists to catch. The binding census now covers every relevant
model W1-00 ships or derives a harness result from, requires the actual shipping entity/call
site/frame for every counted model, and makes both treatment and null arms produce genuine
model-value and entity/world-side behavioural discrimination. It expressly excludes raw state
getters and unrelated sibling-owned journey/input models, and permits sibling evidence reuse only
when the precise consumer and dependency stamps remain current. A different fresh reviewer-editor
must judge this version.

## 10. Fresh independent reviewer-editor outcome — 2026-08-09 (r7)

**SATISFIED; no material edit.** Independent reconstruction of the eight owned paths, current
`judges:` metadata, eleven governing items, native methods and populations, applied W1-00
amendments and later CONSUMPTION doctrine found no remaining route by which every planned check
could pass while a governing predicate stayed red. In particular, the r6 census now reaches every
relevant model W1-00 ships or uses to derive a harness result without misclassifying raw state
getters or unrelated sibling models; requires the actual shipping consumer/entity/frame rather
than a trace, write, probe or harness; and fails closed on absent or zero coupling, inert arms,
hand-feed-only reachability, reset-only deltas, undeclared instrumentation debt, failed long-run
`VERIFY`, unresolved cross-piece consumption, or a native hard fail. The plan also preserves valid
independent evidence, native populations and cheap-first ordering rather than rebuilding sibling
journeys, audio fixtures or instrumentation. No authority ambiguity or escalation remains.
