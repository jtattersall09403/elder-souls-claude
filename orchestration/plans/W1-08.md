# W1-08 current-state continuation plan

Plan-State: awaiting-recriticism

## Authority and boundary

Canonical ownership gives this piece six paths and four governing items: `RI-DLG09`, `RI-JRN02`, `RI-JRN03` and `RI-JRN04`. `RI-JRN03` supplies the complete desktop-native bar (canonical 16 actions/key table, hazards, RB1–RB11, discoverability, naive protocol, consumption, scoring and HF1–HF10), but it is not the whole governing set. Read the joint W1-08/W1-29 reports and both verdict rounds because implementation/history was shared, while scoring only W1-08 owned paths. Reconcile seams: RI-CMB09 owns intentional combat buffer misses; RI-JRN03 M-K24 measures pipeline loss, so retain separate counters per S33 rather than making either zero by relabelling. S35 permits the sound map but no quest markers. W1-29 owns device/touch specifics.

Preserve if fresh: 16/16 desktop primary paths, `.code` layout handling, one fire under repeat, 1-frame measured latency, real rebinding model and inscriptions. Current gaps: rendered-text census still lacks observed dialogue/title populations; naive JC-03N absent; empty-population/self-test provenance must fail closed.

## Native acceptance

Run every M-K method in the item, not only currently named gaps:

* **Controls work (28 points):** all 16 actions on canonical primary/secondary bindings, QWERTY/AZERTY/QWERTZ, 200 inputs/action where specified; exactly one edge per press, auto-repeat one fire, held/discrete semantics exact, input latency p100≤2 fixed frames, `pipelineDrops=0` over native 10,000-input population. Intentional >8-f buffer misses remain separately reported.
* **Browser hazards (26):** pointer lock requires one gesture and no click-to-play prose; Escape ordering and menu/lock transitions exact; blur/visibility release all held inputs within one frame; text focus never moves/fights; browser-reserved combinations handled per item. Each M-K8/M-K9 state transition is frame traced.
* **Look (10):** native mouse sweep at 0/15/30/60 render rates produces rate-independent result within item tolerance, correct yaw/pitch/clamp, hidden cursor while locked.
* **Rebinding (12):** RB1–RB11 all modalities, conflict/refusal/recovery, persistence through save/reload, no unbound required action, and authored refusal text actually drawn. Delete binding produces explicit fail, never fallback invisibly.
* **Discoverability (14):** complete rendered-text census over HUD, menus, dialogue **and title**, each with `surfaces_with_entries>0`; zero forbidden control-name prompts; native inscription population before first choice and DS-distance bands; matcher anchors hit/miss. Empty surface is `EMPTY/FAIL`.
* **Naive (10):** JC-03N under enforced isolation with required fresh participants/transcript and item-native success predicates. Builder cannot self-run or self-score; until result exists score 0.
* **Hard fails:** explicitly evaluate HF1–HF10 on nonempty populations; any true caps native at 2. Native score must reach ≥90/100 (item Meets bar), not merely overall Wave threshold.

### Owned clauses from the other governing items

These are closure gates, not permission to rebuild systems owned by other pieces:

* **RI-DLG09 / `input.action.set`:** expose parley without a seventeenth action; exercise the native parley input timing/commitment path through the desktop pipeline and preserve its hard fails. Reuse W1-17/W1-09's stamped parley instrument/evidence; repair only an input/action-set failure here.
* **RI-JRN02 / `input.discoverability`:** on its native first-hour population, accept only the controls-owned clauses: required canonical verbs are exercisable without control-name instruction and no blocked-known-intent event is attributable to controls. W1-28 owns the journey; consume its one trace rather than commissioning another journey or blind comparison.
* **RI-JRN04 / `input.modality.parity`:** compare the canonical action-set and held/discrete semantics exported here with the separately measured desktop, pad and touch reachability rows. All 16 actions retain identical meaning and every modality reaches them. W1-29's mapping, analog, lifecycle and viewport rows are not rebuilt here.
* **Aggregation:** report every owned clause with its native predicate, population, comparison, threshold and applicable hard fail at the same stamped commit. A green RI-JRN03 score cannot substitute for a missing/red clause in another governing item.

## Controls/evidence

Run `node tools/journey/input-checks.mjs --group <group>` separately—never combine stateful groups—plus `--self-test`. Red arms: duplicate KeyR, swap `.code` for `.key`, drop between-step edges, let key repeat refire, freeze pointer lock, empty each text surface, inject forbidden “Press E”, remove inscriptions and disconnect input pipeline. Every arm must make its own M-row red and leave controls populated. Delete each fix on shadow source.

### Exhaustive RI-MTH07 consumption matrix

Freeze the model census from the complete RI-JRN03 artifact and current profile before build; the
minimum exhaustive rows are: (1) closed action-set/held-discrete semantics, (2) desktop movement
and action primary/secondary binding maps, (3) layout-code→display-label map, (4) reserved-control
table plus authored refusal strings, (5) pointer-lock/Escape/menu and fallback state graph,
(6) mouse-look scalar/clamp model, (7) rebinding records including conflict, zero-binding,
modality and persisted-save state, (8) instruction/forbidden-prompt budget, and (9) the in-world
inscription records and DS-distance placement model. Enumerate any additional table, graph, budget
or record that current HEAD requires this desktop journey to act; omission is `EMPTY/FAIL`.

For every row, the aggregate must name the production consumer and the admissible player-visible
or player-doable observable, then hold all other factors and fixture/seed fixed while testing two
well-separated legal values plus an inert null that is deliberately disconnected/equalised and
has been seen go red:

| Model row | Production consumer | Required two-value observable and null |
|---|---|---|
| Action semantics | input latch/fixed-step action consumer | Change held↔discrete for a safe test action: sustained vs one-edge player action changes; null disconnects latch from the player and neither acts. |
| Desktop binding maps | real DOM mapping → pipeline → player/world | Move one action between two distinct keys (and movement between two keys): the accepted key and resulting player action/displacement swap; dead-glass/null accepts neither. |
| Layout labels | rebinding/settings drawer | Select two layout maps with visibly different labels while physical `.code` behaviour stays fixed; null removes the label-map consumer and pixels do not change. |
| Reserved controls/refusals | rebind offer/commit and refusal drawer | Offer one allowed and one reserved value (including two distinct reserved records): commit versus visible authored refusal changes; null suppresses the record/renderer and must lose the refusal. |
| Pointer-lock graph | browser event handler/menu surface/click-drag fallback | Drive locked-play and forced-lock-error states: visible menu/cursor/fallback look and actionable control differ; null disconnects the transition so the expected surface/action is absent. |
| Mouse-look model | real mouse transform → camera consumer | Use two separated legal sensitivity/clamp values with identical movement stream: rendered camera turn/clamp differs; null disconnects look and the view is stationary. |
| Rebinding/persistence | `RebindModel` → live map plus settings/save consumers | Commit two distinct bindings/conflict outcomes, cold reload, and show the visible binding and doable key survive/change; null bypasses restore/live-map application and must revert or stop acting. |
| Instruction budget/text | rendered-text consumers across HUD/menu/dialogue/title | Swap present/absent forbidden authored prompt values: player-visible text census changes and forbidden arm fails; null strands the text before drawing and yields orphan text/empty evidence. |
| Inscription records/placement | world entity loader/drawer/interact reader | Use two inscriptions/DS-distance bands: rendered object/text and readable interaction change at the player; null removes the world-side read/draw and neither appears nor can be read. |

Harness returns, traces and internal state may record the observation but never substitute for the
visible/doable consequence. Every enumerated member requires `coupling != 0`; any zero scores its
owning RI-JRN03 dimension **0 with no partial credit** and is copied into piece `status_reasons`.
Reuse one bounded fixture per row and stop after its positive, separated-value and proven-red null
legs pass; do not repeat the same coupling in browser, trace and screenshot forms.

Aggregate at commit with boot/data/content checks. Evidence: commit, browser/load, layouts, frames@60, populations, raw edges/world deltas, rendered-text entries by surface, skipped reasons, self-test red legs, naive sealed transcript. Complete plan green is impossible without the independent naive result and every native hard-fail gate.

Use cheap static action-set/text/profile completeness and existing stamped artifacts first. Run one bounded shared browser fixture for remaining desktop/runtime and cross-item rows, stopping on the first hard fail or unpopulated required surface before naive work. Do not rerun W1-29's device matrix, W1-28's first hour, or W1-17/W1-09's parley trace when candidate-commit dependency evidence remains valid; rerun only an affected row after an input seam change, then aggregate once.

---

## Independent plan critique — 2026-08-09

**Verdict: BLOCKING / awaiting-remediation.** The plan narrows the governing CONSUMPTION method to
perturbing “shipped profile/binding.” RI-JRN03 requires an **exhaustive enumeration of every model
this journey requires to act** and a two-well-separated-values plus null observation for each,
with a player-visible/doable observable and `coupling == 0` scoring that dimension zero. A binding
sample can pass while the layout-label map, rebinding/refusal records, inscription model, pointer
lock state model, or authored text remains an orphan. The trace/world delta evidence named here is
also only an observer unless the plan requires the item-native player-visible/doable consequence.

**Required remediation:** enumerate the complete RI-JRN03 model population before build; for every
member name its world/player-side consumer, two separated perturbations, held-fixed factors, visible
or actionable observable, and an inert null that has been seen go red. State that every member must
have nonzero coupling and that any zero scores its owning dimension 0 and enters status reasons.
Keep the current binding/pipeline perturbation as one member of that census, not the whole method.

**Recommendation:** remediate the consumption population and consequence, then request fresh
independent criticism.

### Remediation outcome — 2026-08-09

The recorded consumption blocker is repaired with an exhaustive current-HEAD RI-JRN03 model census,
production consumers, two separated values, held-fixed factors, player-visible/doable observables,
proven-red nulls and the native zero-coupling consequence. The prior binding/pipeline check remains
one row rather than a proxy for the population. Material edit: this version is
**awaiting-recriticism**, never self-approved.

## Fresh reviewer-editor re-criticism — 2026-08-09

**Edited / awaiting-recriticism.** Canonical ownership names four governing items, but the operative plan previously made only RI-JRN03 a closure authority. Every written check could therefore pass while RI-DLG09's action-set parley clause, RI-JRN02's control-discoverability clause, or RI-JRN04's modality-parity clause failed. The authority, owned native gates, hard-fail aggregation and cheap reuse/stop order above repair that false green without duplicating parley, first-hour or mobile systems. This is a material edit and is not self-approved.
