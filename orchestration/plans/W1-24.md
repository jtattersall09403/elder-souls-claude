# W1-24 — visual protocol and the player's body: current-state continuation plan

Plan-State: awaiting-recriticism

## The reference premise this plan was built on has been false since 2026-08-06

Step 8 read *"Missing reference images are SKIPPED, not fabricated; **zero runnable pairs caps
fidelity at 7**."* This piece's own bar is a native score **≥7**, so the plan as written could not
demonstrate above the bar it must clear — the trap `W1-30` R1 found in the visual foundation plan,
live here in a second document. It rested on `RI-VIS06`'s conditional (*"**Until `refs/modern/` is
populated**, Protocol A runs only on the pairs for which we hold a legally usable reference image"*),
and **that condition has been satisfied for eight days.**

**Counted directly at HEAD in this worktree, not taken from the audit** — which is the point, since
this whole class of defect is stale text nobody re-read:

```
corpus/70-visual/refs/modern/ → 131 files across 7 folders
exterior_daylight 43 · exterior_lowlight 24 · interior_darkemissive 22
character_closeup 13 · combat 12 · material_closeup 9 · ui 8
```

That is the third independent count and it agrees with the two before it to the file.

**Which of the fixed-eight capture slots now have a paired reference, from `refs/MANIFEST.json`'s
own `slot` field rather than from filenames** (`RI-VIS02` §Comparison method supplies the mapping):

| our capture slot | reference | files present | routed to RI-VIS02 / Protocol A by `RI-VIS09` §2? |
|---|---|---|---|
| `character_closeup` | REF-M6 | **2**, in `modern/character_closeup/` | **yes, named explicitly** |
| `combat_midfight` | REF-M6 + REF-M2 | **2** (M6); M2 absent | yes for the M6 side |
| `exterior_marsh_dusk` | REF-M1, REF-M4 | **2** (M4), in `modern/exterior_lowlight/`; M1 absent | **no row — open** |
| `water_edge` | REF-M4 | **2**, in `modern/exterior_lowlight/` | **no row — open** |
| `foliage_dense` | REF-M2, REF-M3 | **1** (M3), in `modern/exterior_daylight/`; M2 absent | **no row — open** |
| `exterior_marsh_noon` | REF-M3 | **1**, in `modern/exterior_daylight/` | **no row — open** |
| `interior_rootway` | REF-M5 | **0** | n/a |
| `xanmeer_vista` | REF-M7 | **0** | n/a |

**So: six of eight slots hold at least one paired reference, and two genuinely hold none.** The
`SKIPPED` discipline is correct and stays — a blind test needs two images and a verbal spec is not
one — but it now applies to `interior_rootway` and `xanmeer_vista`, and to those two only.

**The routing question, stated precisely, because the audit and `W1-30` R1 both left it open.**
`RI-VIS09` §2's table names `refs/modern/character_closeup/` for RI-VIS02 and for RI-VIS06 Protocol
A. It has **no row** for `exterior_lowlight/` or `exterior_daylight/`, the folders where REF-M3 and
REF-M4 now live — only `RI-VIS04` carries a blanket `refs/modern/`. **Whether §2's routing extends
to the six folders acquired on 2026-08-06 is a genuine open corpus question and is recorded for
arbitration; nothing here rules on it.** But it does not need to be ruled on to fix this plan,
and that is the useful finding: **under the strictest possible reading — only `character_closeup`
is routed — at least one Protocol A pair runs, so "zero runnable pairs" is false and the cap cannot
fire either way.** Under the wider reading, five pairs run and Protocol A runs at full strength.

**Two stale statements in the corpus itself, recorded and not patched.** `RI-VIS06`'s worked
example still uses REF-M4 as its illustration of *"no image available — run SKIPPED"*, and
`RI-VIS09` §3.1 still reads *"five of six `modern/` profile folders are empty and the sixth holds
one image … RI-VIS02's slots REF-M1, M2, **M3**, M5, M7 … are unfilled."* Both were true when
written and both are contradicted by the manifest at HEAD. A corpus owner should correct them, or
the next three plans will re-derive this the same way.

## Builder/critic execution allocation (binding)

This piece uses the shared [builder execution contract](BUILDER-EXECUTION-CONTRACT.md). The original
native predicates and populations below remain the final critic acceptance bar.

**Builder-owned:** finish the player model, equipment display, cloth, animation, viewpoint, UI and
other W1-24-owned visual implementation defects. Run complete cheap asset, slot, animation and
viewpoint completeness checks. In one reusable running-game session, inspect front and back views,
combat and exploration motion, one equipment swap for every slot, one representative cloth state and
the six principal UI screens. Record the bounded geometry/readability metrics and run targeted red and
delete-the-fix controls for each distinct repaired visual mechanism.

**Critic-owned:** both 3,600-frame duel captures; all 28 roll pairs; every 600-frame surface; the full
360-pose population; three sets per equipment slot; the 48-screen UI matrix; the fixed-eight native
M1–M12 evaluation; the 20-pair calibration; independent visual protocols; and final scoring and
verdict.

**Completion rule:** the builder delivers every available visual implementation and representative
rendered smoke above in one run. Complete capture matrices and independent aesthetic judgement are
critic handoffs.

## Canonical state

Governing items are RI-CAM07, RI-UIX06, RI-VIS01, RI-VIS03, RI-VIS06 and RI-VIS09, with
RI-VIS08's character/animation field contract and M-series a fail-closed prerequisite incorporated
by RI-CAM07. The sole
builder round is partial and has no verdict. It created `corpus/80-methods/cc-scan.mjs`,
`tools/render/visual-reading.mjs`, `tools/render/w1-24-audit.mjs` and
`tools/render/cam07-back.mjs`, with self-tests that deliberately disable each check. The survey
established that 0/70 Wave-1 verdicts carried RI-VIS01 declarations, 15 made visual claims and are
structurally void on the literal item; S45 now confines the declaration bar to current W1-24 evidence and rejects a retroactive corpus migration. The audit found one of four published claims survived and caught canvas-liveness,
clock contamination, build-record substitution, wrong units and inert nulls. RI-CAM07 close-up
viewpoint/capture work and the complete live audit were unfinished. S46 preserves both native questions as separate derived windows over the same capture pair. S47 makes thumbnail calibration a disjoint, sealed precondition to Protocol B rather than tuning on its scored set.

## Classification

| Bar | State | Requirement |
|---|---|---|
| RI-VIS01 structural declaration scanner | independently established; preserve and freshly verify on corpus | S1–S8 are load-bearing; CC-1..7 lexical only. Exit 0/4/5/6 semantics and NOT_APPLICABLE must remain. |
| Visual reading Q1–Q5 | independently established; preserve | Surface admissibility, subject×clock 2×2, sabotage null, identical units and gradeable degenerate must all hold. |
| Four-claim audit | fresh verification | Re-run live at HEAD; artifact hashes/build stamps must match. It demonstrates protocol discrimination, not global visual quality. |
| RI-VIS01 applicability (S45) | ruled; fresh verification | Governs current W1-24 visual outputs only; no retroactive all-verdict migration. Historical pixels/traces are reusable only with intact hashes/provenance/native method. |
| RI-CAM07 viewpoints/back presentation | remaining build + verification | Add/amend canonical close-up viewpoints, capture actual front/back and run M1–M6, including the incorporated RI-VIS08 prerequisite. |
| RI-CAM07 B1 / RI-VIS03 M5 (S46) | ruled; fresh verification | One source pair, two derived native windows and separate rows: body-window HFR parity belongs to CAM07; player-centred 1024² absolute M5 belongs to VIS03. |
| RI-UIX06, RI-VIS03/06/09 full applicable bars | fresh verification/remaining artifacts | Execute their native methods, reference sets and blind protocol where W1-24 makes claims; do not infer from protocol self-tests. |

## Acceptance plan

1. **Instrument integrity, without re-proving valid work.** Verify the existing stamped self-test
   artifacts and tool hashes first. Re-run a suite only if its tool or fixture hash changed: required
   results are cc-scan 14 cases/9 deliberate defects/0 inert and visual-reading 11 readings/7
   defects/0 inert, with each deliberately disabled check releasing its unique red case. Empty
   samples and all-void populations fail. Do not repeat the already-valid disable-one-at-a-time
   campaign merely because the game build changed; these are offline tool tests.
2. **Current-output scan (S45).** Run cc-scan on the bounded manifest of W1-24 visual claims and
   verdicts authored or re-authored in this run, not the complete historical verdict population.
   Each current visual claim has a declaration before first capture, singular legal judgement side,
   item-parsed properties, matching permitted/forbidden references, resolvable capture with exact
   SHA-256, no fused sides, and no opposite-side judgement in prose. Lexical tripwire never alone
   changes verdict. Publish counts and filenames for that manifest. Preserve the existing 70-file
   census only as diagnostic context; neither re-run nor migrate unrelated historical verdicts.
3. **RI-VIS01 population (S45).** Apply RI-VIS01 only to every reference-bearing visual claim and
   visual verdict authored or re-authored as W1-24 evidence at this HEAD. Emit the side-specific
   declaration before the first reference/capture-specific judgement; require legal single-side
   properties, exact capture SHA-256, correct reference routing, human-confirmed CC-1…CC-7, separate
   `(ART, FIDELITY)` scores and native score ≥7. Preserve an old pixel/trace only when build SHA,
   capture SHA, provenance, admissible surface, units and native method remain valid; never preserve
   or retrofit a noncompliant old judgement. The all-Wave-1 cc-scan is diagnostic census only: do
   not rewrite or re-run unrelated verdicts. The four-claim audit tests discrimination, not those
   pieces' acceptance.
4. **Live audit.** Reuse `w1-24-audit.mjs`, one browser, four named claims. For each publish Q1–Q5,
   subject×clock values, all sabotage arms, degenerate support count, concurrent failures, capture
   hash and commit. Expected discrimination: constant canvas goes `PASSES_ON_DEGENERATE`; eight-town
   claim with buildings removed goes `NULL_CONTROL_FAILED`; valid scene-graph interior claim stays
   OK; FD6 empty-edge case remains `DEGENERATE_NOT_GRADEABLE` until a known-bad nonempty capture exists.
5. **RI-CAM07 fidelity M1–M5.** Amend `tools/harness/viewpoints.json` with canonical
   `player_back_closeup`/`player_front_closeup` only if absent at HEAD, then capture both at required
   native frame, camera mode and ≥45% character-height framing. First run RI-VIS08's bone-field,
   B1–B8 model-quality and C1–C5 animation methods on the player at this build; absence of any row
   makes CAM07 score 0. Then run every M1–M4 predicate and hard fail in RI-CAM07: B1–B5 on the
   close-up pair; C1–C7 over every player attack, all 28 pairs of eight locked rolls, block, idle
   and hitstun; C8 separately on both ≥3600-frame infantry and corridor duels with the minimum
   recovery-legibility fraction ≥0.95 in each; D1–D7 for 600 frames apiece on flat, both directions
   of a 20° slope, ≥12 stairs, shallow marsh water and mangrove roots; E1 over all five weapon
   classes, E2 over its native 360-pose yaw/pitch sweep, and E7 at arm ≤0.91 m. Apply every native
   threshold and worst-surface/pair/pose aggregation—never an average or selected frame.
   Back/front measurements use identical crop, pose, light and animation
   frame. Delete/hide the player mesh: character-support predicate must fail while world render
   remains live. Swap front/back or freeze animation: the relevant arm must change. Then run M5
   E3–E6 over **three authored sets for each of head, chest, hands, legs and back**: each slot swap
   changes ≥18% of back pixels; lightest/heaviest outline Hausdorff distance is ≥0.08 character
   height; cloth lag is 3–12 f with ≥0.08 m amplitude; and same-seed cloth positions are identical.
   Fewer than three authored sets in any slot scores M5=0, not not-applicable. Save the slot masks,
   pixel-diff tables, outline artifacts and deterministic cloth traces; a no-geometry slot swap and
   rigid/nondeterministic cloth are independent red controls.
6. **B1 parity and absolute M5 (S46).** Reuse the two 1920×1080 close-ups from step 5, but emit
   two distinct metric rows and crop artifacts. CAM07 B1 uses each ID-mask box expanded by 5% of
   character height and the largest common power-of-two native square `L≥256`; same relative centre,
   no clip/resize/pad. Run M5's Hann/FFT HFR equations only: pass `back≥0.85×front`, hard fail
   `<0.60×`, middle is a named gap. The player-hidden control must fail extraction; swapped labels
   must reverse the directed comparison. VIS03 M5 separately uses a native 1024² square centred on
   the player centroid in each source and applies character-closeup `HFR 0.05–0.24`,
   `NYQ_ratio≤0.15`, `alpha 1.6–2.8` and native hard fails. Background remains; sky-only support,
   resampling or using either row for the other is red. No second browser/capture pass is required.
7. **RI-CAM07 M6/F1–F5 art direction.** Produce the 32-pixel binary silhouette mask sheet for the
   player and five most common humanoid enemies and the complete pairwise IoU matrix; F1 requires
   every player/enemy IoU ≤0.72. Put the player back, masks and authored-set evidence through the
   disjoint art-direction protocol for F2 Argonian proportion, F3 Black-Marsh equipment vocabulary,
   F4 composed back and F5 strangeness contribution. Score the native 30/25/25/10/10 art set, with
   ≥90 parity, 70–89 named gap and <70 loss; never average it with fidelity. Removing crest/tail,
   replacing equipment silhouettes with generic plate, and flattening the back composition must
   independently worsen their relevant row. Missing imagery or an ineligible/contaminated judge is 0.
8. **RI-VIS06 Protocol A — fidelity, separately quarantined.** Use only RI-VIS02/VIS03/VIS08 and
   CAM07 §B–E plus RI-VIS09-permitted modern references; never expose RI-VIS05, Morrowind, Black
   Marsh or project context to this judge. Build five pairs from the fixed-eight capture slots where
   legally usable paired images exist: actual 1920x1080 PNGs, subject-neutral crops when needed,
   same dimensions/writer/bit depth, stripped metadata/overlays, filenames A/B, coin-flipped order,
   and a prewritten sealed `fidelity_*.key`. Give a fresh eligible judge RI-VIS06 §A's **verbatim
   RENDER QUALITY ONLY prompt** and nothing else; capture the forced winner, confidence and gaps
   before reveal. Missing reference images are SKIPPED, not fabricated — but **`refs/modern/` is
   populated and the SKIP now applies to `interior_rootway`/REF-M5 and `xanmeer_vista`/REF-M7 only**
   (see the reference-premise section at the head of this plan for the per-slot table and the counts
   behind it). **Build the five pairs from slots that hold a routed reference; a Protocol A recorded
   as "zero pairs ran" is a defect in the run, not a property of the corpus, and must be
   investigated before it is reported.** `RI-VIS06`'s *"zero pairs ran (no reference images
   available) → cap 7"* row is native and is left exactly as written; it is simply no longer
   reachable, and reaching it would now be evidence that something went wrong. The wave verdict must
   still state **how many of the five pairs actually ran and which slots they came from**, per the
   item. If the `RI-VIS09` §2 routing question is still open when this runs, proceed under the
   strict reading — `character_closeup`/REF-M6 is routed by name — and record the other four pairs
   as run-pending-routing rather than as skipped. Apply §C's fresh-judge, harsher-prompt and three
   512x512 crop escalation whenever ours wins; only the native post-escalation outcome table sets
   the fidelity cap.
9. **RI-VIS06 Protocol B/B1/B2 — art direction, separately quarantined.** A different fresh judge,
   who has not read RI-VIS02/03/04 or judged Protocol A, receives no modern imagery or fidelity
   metrics. For B1, pass both ours and a permitted Morrowind image through the single global transform
   selected and sealed by step 13/S47 (stock B1 only if it is the first qualifying candidate),
   identically on both arms; use A/B, coin flip and a separate sealed `art_*.key`, then quote RI-VIS06 §B1's
   **verbatim IMAGINATION / HARDER TO PLACE prompt**. For B2, before showing any image, a fresh
   eligible art-direction judge gets only RI-VIS05 §C–F excerpts and writes the required 200-word
   screenshot description; only then show ours and record MATCH, three missing elements and every
   present-but-unspecified asset. Record answers before reveals. B1 loss caps art at 3; low-confidence
   win/existing-setting answer caps 6; B2 weak/contradicts caps 5; ≥2 substantial unspecified assets
   cap 6; only high-confidence B1 win plus strong B2 avoids a cap. No best-of-N or discretionary
   rerun is allowed.
10. **Blind-pack validation.** Before either protocol, mechanically reject filename/path/metadata,
    dimension/aspect, encoder/compression, HUD/watermark, order/border or key-access tells. A planted
    one-bit tell must be detected. Step 13's disjoint 20-pair calibration must satisfy its fidelity
    neutralisation and subject-readability thresholds before B1; otherwise Protocol B stops red rather
    than tuning or substituting a transform against the scored pack. Protocol A and B keys, prompts, raw answers,
    discards/reasons, reveals and caps remain separate. One agent judging both protocols voids both.
11. **UI style.** Keep ART and FIDELITY declarations, evidence and native /7 scores disjoint.
   ART uses the six shipped UI screens blank-world at the canonical UI viewpoints: run AD1–AD6 and
   the worst of six fresh Bootstrap-test judges for AD7, with RI-VIS05 permitted and modern refs
   forbidden. FIDELITY reuses those six screen states but captures the required 48-shot matrix
   (4 resolutions × 2 DPRs × 6 screens) and runs FD1–FD7, including the DPR-2, layout, ΔE,
   banding and overdraw hard fails, with modern refs permitted and RI-VIS05 forbidden. Run the
   prescribed null styles/flat composite backdrop and contamination scan; any missing screen,
   unimplemented row or forbidden reference is red. Report the pair, aggregate by the worse native
   side, and require the ship gate `min(UI_ART, UI_FIDELITY) ≥ 6`. Capture these UI configurations
   in one daemon batch after offline gates; world captures cannot substitute for them.
12. **RI-VIS03 image metrics.** On the applicable fixed-eight native capture slots, run the complete
    M1–M12 battery at each method's native resolution/window and profile, not only CAM07's disputed
    B1/M5 row. Emit the RI-VIS01 fidelity declaration first; retain per-shot metrics, support counts,
    temporal traces and every anti-reference/control. Apply all absolute hard fails and aggregate by
    the native **minimum across shots**, never a mean or selected beauty frame. A missing applicable
    slot, empty support, resampled native window, default/anti-reference pass, or unimplemented metric
    is red. Share the already-produced captures and camera traces; do not launch a second browser.
13. **RI-VIS09 gate and Protocol-B calibration (S47).** First run all five register/compliance
    checks: every cited file resolves under `refs/`, routing is legal, numeric citations name the
    population and `n`, and neither `modern/hud/` nor `anti-generic/` is a target. Before any scored
    pair/key is viewed, pre-register the scored W1-24 files and a disjoint 20-pair calibration panel.
    A fresh calibration actor who will not score Protocol B tests, least-to-most destructive: stock
    B1; stock preceded by 320² centre-crop; that path at 4-bit/channel; then 3-bit/channel. Each
    transform is global and identical on both arms. The undegraded known-quality red control must
    identify original higher fidelity ≥16/20. Select and seal the first candidate with fidelity
    identification ≤13/20 and subject/silhouette nameability ≥16/20; a planted one-bit tell must be
    detected. If none qualifies, stop before paid judging and remain red. Calibration files, actor,
    answers and keys never enter or reach the pre-registered scored population/judge. Run the scored
    B1 pairs once under the sealed transform plus one spec-first B2; no per-pair tuning, rerun or
    best-of-N. Apply RI-VIS06's unchanged caps. Reuse a prior calibration only for byte-identical
    transform/source-thumbnail class when none of its files is scored here.

## Hard failures, outputs, dependencies

Hard fail: missing declaration, hash mismatch, capture cited before declaration, fused judgement,
inadmissible surface, subject/clock blindness, inert sabotage, passing gradeable degenerate, empty
support reported green, omitted RI-VIS08 prerequisite or applicable RI-VIS03 metric/slot, RI-VIS09 routing/register failure,
unsealed or contaminated B1 thumbnail calibration, either M5 window substituted for the other, blind tell/reveal leak, self-judgement, or any
item-native hard fail. Outputs: `reports/w1-24/` (new stamped run files, without rewriting historical
ones), canonical viewpoint amendment, current-output declarations/manifests, and quarantined blind artifacts.
Dependencies: W1-06 camera implementation, W1-26 player presentation, W1-21 FD6 captures and the
shared visual-method corpus. Dependency failure is recorded as a red row, never omitted.

## Falsification audit

A builder could pass protocol self-tests yet ship a poor/invisible character; therefore native
RI-CAM07/UIX06/VIS03/06/09 methods and real captures are separately mandatory. Strongest critic move:
clear the player while leaving the world intact, advance the clock, and show which claimed body
metric changes; then inspect the blind pack before reveal for a one-bit tell.

**Critic hand-off:** awaiting criticism; no satisfaction claimed.

## Independent plan critique — 2026-08-09

**Verdict: BLOCKING / awaiting-remediation.**

The plan's singular “RI-VIS06 pack” can go green while violating the governing visual comparison method. RI-VIS06 mandates **two separate protocols**, one per side of the bifurcation, never judged by the same agent: Protocol A uses its exact fidelity prompt and modern reference routing; Protocol B/B1 uses the art-direction/strangeness procedure, mandatory equal degradation, and a judge who has not read the fidelity references. The plan instead describes one generic fresh judge and one generic quality question. It also makes RI-CAM07 step 5 explicitly run M1–M4 although RI-CAM07 has M5 equipment/cloth plus F2–F5 blind art-direction rows; the earlier phrase “native runnable” does not supply their populations or acceptance and the later explicit range permits omission. Thus protocol self-tests, M1–M4, and a single blind pack could all pass while the player's back still fails governing equipment and art-direction requirements.

**Required remediation:** split acceptance into RI-VIS06 Protocol A and Protocol B/B1 with separate eligible judges, exact native prompts, permitted reference sets, B1 degradation, B2 follow-up, escalation/caps, and separate quarantined keys. Expand RI-CAM07 acceptance through M5 and F2–F5 (including three authored sets per slot, mask/IoU artifacts, and native thresholds), or mark any currently impossible row fail-closed rather than omitting it.

## Remediation record — 2026-08-09

Completed editorially: acceptance now includes CAM07 M5's three-authored-set equipment/cloth
population and thresholds, CAM07 M6/F1–F5's mask/IoU and disjoint art score, and separate RI-VIS06
Protocol A and Protocol B/B1/B2 contracts. Those contracts preserve eligible-judge separation,
verbatim native prompts, reference exclusions, exact B1 degradation, B2 spec-first ordering,
separate keys/reveals, escalation and caps. Missing content/judgement fails closed. A single shared
capture/mask production pass feeds the applicable native checks, avoiding redundant browser work;
pack validation remains offline and precedes judging. Deferred to the build: authoring any absent
required content, captures, packs and independent judgements. Escalated/preserved: the existing
CAM07 B1/VIS03 M5 window contradiction and VIS01 retroactivity seam remain on their stated amendment
paths; this remediation invents neither ruling.

**Remediator hand-off:** materially edited; awaiting fresh re-criticism. No satisfaction is claimed.

## Fresh reviewer-editor outcome — 2026-08-09

**Edited — awaiting re-criticism.** The two-protocol and CAM07 remediation is retained. Independent
reconstruction found that the executable steps still reduced RI-VIS03 to CAM07's B1 seam and never
required its complete M1–M12 population/min-over-shots aggregate, while RI-VIS09's five-part register
gate and later 320×320 Protocol-B degradation ruling were absent. In particular, the received plan
mandated RI-VIS06's stock B1 recipe even though governing RI-VIS09 says that recipe is not trustworthy
for the available thumbnails until re-tuned by amendment. Steps 12–13 now add the missing native
metric/register gates, reuse the one capture pass, and stop before paid judging when the B1 seam is
unresolved. This material edit is not an approval.


## Fresh independent reviewer-editor outcome — 2026-08-09

**ESCALATED / awaiting remediation.** The received plan correctly exposes rather than papers over
three authority/instrument seams: RI-VIS01 historical effective date, RI-CAM07 B1 versus RI-VIS03
M5's native window, and RI-VIS09's mandatory re-tuning/amendment of Protocol B for the held
320×320 reference population. Those seams prevent a truthful build-ready approval: exact execution
currently must leave governing rows red, while choosing local proxy windows, retroactivity or
degradation values would invent authority. No ordinary bar or cost repair can safely resolve them
inside this bundle's permitted piece-only files. Route the three already-specified questions through
the repository arbitration/amendment mechanism before another reviewer-editor round. Preserve the
plan's cheap-first stop before browser/blind judgement and its shared-capture route.

## Arbiter-editor application — 2026-08-09

S45–S47 resolve the three escalated seams without approving this materially edited plan. S45 makes
RI-VIS01 prospective for W1-24 evidence rather than a historical migration. S46 preserves CAM07's
directed back/front parity and VIS03's absolute native metric as distinct windows over shared PNGs.
S47 adopts VIS09's re-tuning precondition through a disjoint calibration/scored split and fixed
stopping rule; no proposal supplies authority and no scored answer may tune the transform.

**Arbiter-editor hand-off:** materially edited; awaiting fresh re-criticism. No satisfaction claimed.

## Fresh S45–S47 reviewer-editor outcome — 2026-08-09

**Edited — awaiting re-criticism.** S45–S47 are coherent: current W1-24 outputs are declared without
retroactive migration; CAM07 parity and VIS03 absolute M5 remain distinct native windows derived
from one source pair; and Protocol-B calibration is disjoint, globally sealed before scoring, and
stops without a second scored panel. Full-bar falsification nevertheless found ordinary omissions:
the executable brief did not carry CAM07's mandatory RI-VIS08 prerequisite or its exact M1–M4
populations/worst-case aggregation, and reduced UIX06 to an unbounded “all applicable” instruction.
Those populations and hard-fail handling are now explicit. Cost falsification also removed the
unnecessary repeat of the historical 70-verdict scan and offline sabotage campaign; current W1-24
outputs remain fully checked, while valid tool evidence is reused by hash. This reviewer does not
approve its own material edits.

## Fresh independent W1-24 reviewer-editor outcome — 2026-08-09

**SATISFIED.** Independent reconstruction from the canonical ownership/judges and governing items
found no remaining material defect and made no material edit. S45 remains prospective and bounded
to current W1-24-authored or re-authored evidence. S46 retains one authoritative source pair but
two non-substitutable native crop populations, metric rows, thresholds, hard fails and controls.
S47 seals the preregistered scored population before a genuinely disjoint 20-pair calibration,
permits only its fixed global ladder, forbids scored-data tuning, and stops red before paid scoring
when no candidate qualifies.

The CAM07 execution population remains complete through its incorporated player-only RI-VIS08
prerequisite, M1–M6, both 3,600-frame C8 duel surfaces, every D surface, the 360-pose near-plane
sweep, and three authored equipment sets in every named slot. VIS06's fidelity and art protocols
remain separately declared, referenced, keyed, judged and capped. UIX06's six native ART screens
and six-screen × four-resolution × two-DPR FIDELITY matrix are not an invented expansion: they
are its literal comparison populations, kept on separate /7 scales with worst/min aggregation and
native hard fails. Applicable fixed-eight VIS03 shots retain M1–M12, native windows/profiles,
per-shot hard fails and minimum-over-shots aggregation. Missing rows, support, dependencies,
eligible judges or artifacts fail closed. Cheap stamped static/image gates precede the single
shared browser capture batch; valid hash-identical offline evidence, source captures and native
derivations are reused without collapsing populations or duplicating blind panels.

**Reviewer hand-off:** plan satisfied. The next owner action is to dispatch the W1-24 builder on
this canonical plan; do not schedule another plan-remediation or plan-criticism round unless the
governing authority or plan changes materially.

## Reviewer-editor round — 2026-08-14 (stale reference premise)

**Outcome: MATERIAL EDIT / awaiting-recriticism. No escalation.** One premise, checked and found
false, and the smallest of the five edits this round covered.

**What I checked myself rather than taking on report.** The audit and `W1-30` R1 both report 131
files across seven folders; I counted them in this worktree and got the same 131 and the same seven
per-folder figures, so this is now the third independent count. But the count alone does not decide
whether *this* plan's rows are runnable, so I went further and read `refs/MANIFEST.json`'s `slot`
field, which resolves each acquired file to an `RI-VIS02` slot: **REF-M6 ×2, REF-M4 ×2, REF-M3 ×1,
and nothing at all for REF-M1, M2, M5 or M7.** Mapped through `RI-VIS02` §Comparison method's
capture→reference table, that makes **six of the eight capture slots pairable and two genuinely
not**, which is the per-slot table above and is the actual repair. I also read `RI-VIS09` §2's
routing table and `RI-VIS06`'s skip note in full rather than trusting a paraphrase of either.

**Where I go further than the audit, and where I stop short of it.** Further: the audit called this
"three sentences", and it is more than that — the useful artefact is the slot-by-slot table, because
a builder told only "the references exist" still has to work out which five pairs to build and will
re-derive it. Short: the audit expects `W1-30V`'s arbitration on `RI-VIS09` §2 routing to be written
back here when it lands. It has not landed — I checked `corpus/00-doctrine/ARBITRATION.md`, where
the newest visual ruling is S47 and nothing rules on the 2026-08-06 folders. **But this plan no
longer needs it**, and that is the finding worth carrying to `W1-06` and `W1-03` as well:
`character_closeup/` is routed to Protocol A **by name** in §2's table, and it holds 13 files
including two REF-M6, so at least one pair runs under any reading and the zero-pairs cap is
unreachable regardless of how the arbitration goes. The arbitration governs how many of the five
pairs run, not whether Protocol A can run at all.

**What I did not do.** I changed no native threshold, population, denominator or hard fail.
`RI-VIS06`'s cap table — including *"zero pairs ran → cap 7"* — is untouched; I made it unreachable
by correcting the plan's false antecedent, which is a different thing from editing a bar, and I have
said so in the step itself so a later reader cannot mistake one for the other. I did not correct
`RI-VIS06`'s worked example or `RI-VIS09` §3.1, both of which still assert the empty-folder state;
those are item edits and are recorded above for a corpus owner. I ran no browser and produced no
capture.

**Weakest joint in my own edit, for the next reviewer to attack first.** The per-slot table rests on
`MANIFEST.json`'s `slot` field being an authoritative routing statement rather than an acquisition
convenience — I read the field, and it is stated per record, but I did not verify that any tool
*consumes* it, and this project's own rule is that a declaration nothing reads scores zero. If
`slot` turns out to be advisory metadata, the six-of-eight claim weakens to "REF-M6 is filled and
named in §2", which is still enough to kill the cap but not enough to plan five pairs from. The
cheap check is `grep -rn "slot" tools/metrics/ tools/blind/` before the pack is built, and it should
happen before a builder relies on the table.

**Exact next action:** a **different** fresh reviewer-editor judges this version. Do not dispatch a
builder against it while it reads `awaiting-recriticism`.
