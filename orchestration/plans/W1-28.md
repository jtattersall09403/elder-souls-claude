# W1-28 — the first hour as interaction: current-state continuation plan

Plan-State: awaiting-recriticism

## Canonical state and governing bar

The governing item is `corpus/88-journeys/RI-JRN02-the-first-hour-as-interaction.md`, including
interaction M-I1…M-I14, cadence C1…C10, competence K checks, frustration/naive M-N checks, blind
comparison and hard fails. Round 1 is partial and uncriticised. It correctly found the old registered
journey did not drive an hour, built `tools/journey/jrn02-hour.mjs`, `jrn02-competence.mjs` and a
chart, and began a need-driven road run. It also proved the live combat path emits no `input_action`;
the only combat emitter is dead `sim/player.js`, while harness-side interact can be cleared before
recording. Consequently C1/C2 and the verb ledger are unmeasurable: the reported 496 s gap is
instrument blindness, not hour emptiness. The run reached 18.3 simulated minutes at recorded handoff,
not 60. Analytic repeated-windup competence showed capacity to learn but did not establish live-fight
competence across the ordinary four-move set. No cadence aggregate, text-purity, necessity, naive,
modality repeat or blind judgement exists.

## Classification

| Bar component | State | Standing |
|---|---|---|
| Real driven-hour instrument and its offline self-tests | independently established and preserve | Driver exists; former idle `journey-run` assumption is stale. Must verify it completes exactly 216,000 f@60. |
| Need-driven acquisition through 18.3 min | partial evidence requiring fresh full verification | Useful checkpoint only; never an hour verdict. |
| `input_action` observability / M-I1 / C1-C2 | remaining build/remediation | Live combat actions execute but are absent from trace. No cadence claim admissible until fixed. |
| Driver-side combat fraction | fresh native verification | 0.223 at 18.3 min is within C5's 0.12–0.30 band but is partial and not trace-native. |
| M-I3 no text teaching; M-I5 necessity | remaining verification | Not run; `--no-acquire` only self-tested. |
| Competence K checks | fresh live verification/remediation | Analytic single-move learning is established as instrument evidence, not governing live outcome. |
| M-I13/M-N1…5 naive frustration | remaining independent run | Requires isolated naive role/population; builder cannot substitute its policy. |
| M-I14 gamepad/touch | dependency + remaining verification | W1-29/W1-08 seam does not waive it. |
| RI-JRN02 blind comparison | remaining artifact/judgement | No valid actual-hour pack or independent judge. |

## Exact continuation and acceptance

1. **Repair trace observability at the live path.** Emit closed-vocabulary `input_action` events from
   the actual combat/input consumers, inside the fixed step, for every RI-JRN02 verb. Reuse
   `--verb-coverage`. Population: all 16 tested actions plus the governing 14-verb ledger. PASS:
   every executed action produces exactly one corresponding trace event within 1 f@60; held inputs
   do not duplicate; non-executed actions emit none. Consequence events remain as independent
   corroboration, not substitutes. Break event emission while action still executes: coverage must
   go red. Break action execution while forging the event: corroboration must go red.
2. **Complete the hour.** One current-HEAD run of exactly 216,000 fixed frames (60 min @60), with
   checkpoints, trace streamed incrementally so a killed process is not mistaken for completion,
   seed/build/dirty stamp and start/end state. The policy may react only to world observations named
   in the artifact. Hard fail on fewer/more frames, idle remainder, driver timeout silently truncated,
   teleport, or missing trace interval.
3. **Native cadence.** Run the existing RI-JRN02 cadence instrument over the completed trace. Apply
   C1…C10 exactly, with each predicate, numerator/denominator, seconds/minutes and population stated.
   C1/C2 count corroborated state-changing inputs only. C5 combat fraction must be 0.12–0.30; apply
   every other item threshold without paraphrase. Missing `ui`, dialogue, traversal or menu fields
   are unmeasurable/fail-closed, never driver-side approximations. Dead-hour and uncorroborated-roll
   self-tests must remain red.
4. **Verb acquisition.** For M-I1 report first-use minute and exercised count for every governing
   verb. For M-I3 capture the preceding 30 s UI-text stream and require no textual introduction
   forbidden by V2/HF2. For M-I5 rerun each introducing encounter with its required verb disabled;
   necessity passes only when the intended state change becomes impossible/worse by the item's
   predicate, while unrelated progression remains possible. `--no-acquire` existing is not evidence
   until all encounters run.
5. **Competence.** Drive live fights, identical policy code, gear, tier, RNG schedule and enemy.
   Compare early versus late outcomes per K1…K7. Do not learn one mean windup across a heterogeneous
   moveset: report each of the four `inf_trash` startups (22/44/54/68 f@60) and stratify by move,
   plus aggregate exactly as the item says. Controls: learner frozen; repeated learnable move;
   randomised/unlearnable timings; full shipped moveset; delete learned state. Improvement must meet
   each native threshold and the frozen/delete arms must not.
6. **Naive/frustration pass.** A fresh isolated JC-02N/naive agent receives only the governing
   allowed information, not builder policy/source. Run the complete hour and M-I13/M-N1…M-N5,
   publishing deaths, repeats, stalls and frustration units at item thresholds. This is independent
   evidence, not self-grading.
7. **Modalities.** Repeat the required acquisition/necessity hour legs gamepad-only and touch-only
   using W1-29/W1-08 native mappings. Every required action must be reachable and observable; missing
   modality is a red dependency row.
8. **Blind pair.** Package actual timeline/trace-derived presentation plus the actual comparable
   Morrowind/Dark Souls reference required by the item, coin-flipped and tell-audited. The question
   must be the item's quality question, not SAME/DIFFERENT. Fresh judge; quarantine reveal; builder
   never judges.
9. **Mandatory CONSUMPTION manifest and aggregate gate.** Before accepting the hour, enumerate
   exhaustively every model RI-JRN02 requires the running journey to act on—every table, graph,
   binding map, budget and record, including the verb/order tables, cadence budgets, competence and
   frustration records, modality bindings and journey state they depend on. For every row record:
   model/path and producer; the actual live world-side consumer; the player-visible or player-action
   observable; two well-separated values with everything else fixed; predicted and observed entity/
   frame/action change; a clean null; and a delete/perturb control that has been seen to make that row
   red. Trace output and harness return values may observe coupling but cannot be the consumer, and
   hand-fed values are inadmissible. Require complete manifest coverage, zero skipped rows and zero
   `coupling == 0`; any omitted, unconsumed or trace-only row scores its dimension 0, enters
   `status_reasons`, and fails the combined RI-JRN02 aggregate before cadence, competence, naive,
   modality or blind success can satisfy the plan. Reuse one perturbation where it legitimately
   proves the same model/consumer pair; do not repeat browser hours merely to duplicate coupling
   evidence.
10. **Integrity rows that the headline analyses do not subsume.** Run M-I10 against RI-EXP01's
    own artifact and require this verdict to contain zero independently recomputed EXP01-owned
    values. Run M-I11 over every combat frame and require zero modal/full-screen surface frames
    outside the S13/S14 parley exception, in addition to C8's hour-wide menu fraction. Run M-I12
    over every objective-changing RI-EXP01 beat and require a first-person journal entry within
    5 s, 100% of the population. Missing EXP01 evidence or an empty objective-beat population is
    `unmeasurable`, not a pass.
11. **Native result gate.** Compute RI-JRN02's published weighted 0–100 score from M-I1…M-I14 and
    M-N1…M-N5, require **≥90/100**, and assert HF1…HF7 false. Stop before blind judging or
    additional full-hour runs whenever a cheap trace/static gate already makes ≥90 or a
    hard-fail-free result impossible; repair that blocker, then resume from reusable stamped
    artifacts rather than replaying unaffected hour arms.

## Controls, hard failures, artifacts, dependencies

Null/red/delete controls are: dead hour, idle remainder, forged event without consequence, executed
action without event, uncorroborated rolling, each verb disabled, frozen learner, random/unlearnable
enemy, deleted learned state and modality mapping removed. Each must fail its intended predicate;
inert arms invalidate the measurement. Any RI-JRN02 named HF, HF2 text teaching, empty denominator,
partial hour reported as whole, missing event vocabulary entry that crashes stepping, blind tell, or
aggregate omission is a hard failure.

Reuse `tools/journey/jrn02-hour.mjs`, `jrn02-competence.mjs`, `cadence.mjs` and the journey runner;
do not create parallel graders unless a native method is absent. Required new artifacts live under
`reports/w1-28-r2/` and quarantined critic paths. Dependencies: W1-08 discoverability, W1-29
gamepad/touch, live input/combat event ownership, and W1-07 opening. They remain explicit red rows.

## Falsification audit

A builder could otherwise pass by forging input events, driving only 20 minutes, teaching verbs in
text, learning a single averaged windup, silently recomputing EXP01 beats, opening a modal surface
in combat, dropping objective journal updates, or judging its own policy. Consequence corroboration,
exact frame count, UI lookback, the native integrity rows, per-move live fixtures and independent
naive/blind roles close those paths.
Strongest critic move: execute a combat action while disabling only its event, then forge only the
event with execution disabled; the two arms must fail for different recorded reasons.

**Critic hand-off:** awaiting fresh re-criticism; no satisfaction claimed.

## Independent plan critique — 2026-08-09

**Verdict: BLOCKING / awaiting-remediation.**

RI-JRN02's governing CONSUMPTION section requires an **exhaustive enumeration of every model the journey requires to act**, followed for each by two well-separated perturbations, a null, and a player-visible/player-action observable; a sample is expressly forbidden and an unconsumed model scores zero. The plan repairs `input_action` instrumentation and lists behaviour controls, but nowhere enumerates or accepts consumption of the journey's tables, graphs, binding maps, budgets and records. An event emitted from the live path proves the tracer hears an action; it does not prove every model producing the hour is world-consumed. Consequently the exact hour, cadence, competence, naive, modality and blind checks could all go green while a shipped journey model remains disconnected.

**Required remediation:** add a mandatory RI-MTH07/RI-JRN02 consumption manifest covering every journey-required model, naming producer, live world-side consumer, player-visible/action observable, two separated values, prediction, observed coupling and clean null. Require zero skipped/unconsumed rows, fail on trace-only or hand-fed evidence, include per-model delete/perturb controls, and bind the manifest into the aggregate before satisfaction.


## Remediation — 2026-08-09

Resolved the recorded consumption blocker with an exhaustive, fail-closed per-model RI-MTH07
manifest bound into the aggregate. This material edit awaits a fresh reviewer-editor.

### Fresh reviewer-editor outcome — 2026-08-09

**Material edit / awaiting-recriticism.** The received plan could complete its headline verb,
cadence, competence, naive, modality and consumption work without running M-I10, M-I11 or M-I12,
and it stated no native ≥90/100 result gate. Those integrity rows, the exact score floor and
HF1–HF7 assertion are now explicit, with cheap-failure stops before costly blind/hour reruns. A
different fresh reviewer-editor must judge this version.
