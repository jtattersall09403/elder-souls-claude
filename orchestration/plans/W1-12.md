# W1-12 — enemy AI and encounter composition: current-state continuation plan

Plan-State: awaiting-recriticism

## Directives and rulings in force

`orchestration/OWNER-DIRECTIVES-2026-08-14.md` binds this plan, specifically **Ruling W2** and
**Ruling W1**, and Directive §2 (*statistics can fail a build and can never pass one*).

**Which W2 case each human row is in — stated, not assumed.**

| row | pack | case | disposition |
|---|---|---|---|
| step 8, `RI-AI01` blind | two anonymised 60 s `dist_m`+`state` series | **mechanism/leak check** | kept verbatim; it is `RI-AI01:252`'s own native protocol and it does test a real property — whether the distance trace is shaped like negotiation. It is **not** the quality verdict and may never be reported as one. |
| step 9, `RI-AI05` blind | ten unlabelled 90 s behaviour fingerprints + ten generated | **mechanism/leak check** | kept verbatim; it is `RI-AI05:276`'s own native protocol, and "do these cluster into six roles" is genuinely a question about role distinctness. It is a *genericness* test in Ruling W2's sense — **distinguishable is not good** — and it is not the quality verdict. |
| step 8a (new) | real fights, played | **quality verdict** | this is the gate Ruling W2 requires and the piece did not have. |

Ruling W2's falsifier does **not** cover this piece: enemy AI is a feel system and a fight can be
shown to a judge, so an instrument read is not the honest gate here. The audit's finding stands
exactly as written — *"nobody in this plan ever fights an enemy and says whether the fight was
good"* — and steps 8 and 9 are `RULES.md` rule 25's convicted shape when they are asked to carry the
quality question, which they no longer are.

**RULED — `ARBITRATION` S51, 2026-08-14. The corpus note below is answered; read this box first.**
The referral was correct and the conflict is real in substance. **`RI-CMB07` governs**, and it is not
`RI-CMB07`-local: `RI-WPN02`'s wave-1 amendment and `RULES.md` rule 25 reached the same principle
independently, so the wave-0 items are unamended rather than dissenting. As a seam ruling S51 sits
above every item's comparison method (`ARBITRATION` §5.3) and binds without either item being edited.

- **Step 8 (`RI-AI01`) is VOID as written and is replaced below, not deleted.** Its reference arm
  is `blind_status: not_possible`, reason `"no admissible counterpart; item's own §C/§D tables are
  inadmissible under S51"`. In its place, run S51's **class-D within-build contrast**, which this
  plan already pays for: the shipped build against the **deleted-repair copy §4 already builds** —
  same fixture, same start ranges, same seed, both arms runtime-generated, every axis label, band
  annotation and row number stripped; question written first, *"which of these is an enemy
  negotiating distance, and which is closing to contact?"*; judged by a fresh agent who is not the
  builder. A class-D contrast is **not** a reference pair and must never be recorded as one. If the
  two arms read the same, the result is `inert`, not a pass.
- **Step 9 (`RI-AI05`) is NOT void — it is reclassified.** Its two arms do not compete: the scored
  predicate reads only whether *our* ten resolve into ≥6 clusters, so the ten §B-generated members
  cannot make us pass. They are recorded as an `instrument_positive_control` — if a judge cannot
  separate ten fingerprints that are distinct by construction, our ten failing means nothing — and
  the check stays a **mechanism check** that may never be reported as a quality read.
- **Step 8a is unaffected and remains the quality verdict.** Nothing here lowers a threshold or
  adds a run; S51 costs this piece no new population and no new tool.

*(S51 is reversible; it invalidates no landed verdict, because neither of these packs has ever been
built — `W1-12-r1` §490, `W1-12-r2` §423.)*

**Corpus note, now answered by S51 — kept for the record.** `RI-AI01:252` builds its blind counterpart *"from the tables in
§C/§D"* — from the item's own published design tables. That is precisely the shape
`RI-CMB07`'s wave-1 amendment (`BAR-CRITIQUE-W1-09-R1` §R5) rules **VOID**: *"a pack whose
counterpart is generated from §D, from this item's text, or from any published design table is
`VOID`"*, because it becomes a test of which side better recites a table both sides can read.
`RI-AI05:276`'s *"ten generated from §B"* has the same shape. Two governing items in one directory
therefore disagree about whether a design-table counterpart is admissible. That is a **corpus-level**
incoherence, it is recorded here for a corpus owner and an `ARBITRATION` ruling, and this plan does
not patch around it: steps 8 and 9 still run their items' protocols verbatim, and their result is
reported as a mechanism check whose counterpart provenance is disclosed on the same line.

## Builder/critic execution allocation (binding)

This plan incorporates [`BUILDER-EXECUTION-CONTRACT.md`](BUILDER-EXECUTION-CONTRACT.md).

**Builder-owned:** repair acquisition, spacing, pursuit/leash, moving-target behaviour, punish
reads, roster consumers and group behaviour; run exhaustive cheap AI-table/model/consumer and
roster manifests; run short deterministic production-browser fixtures for M1, M3 and M9 across
representative bearings, ranges and archetype families plus one multi-enemy encounter; ensure heal
and LONG_RECOVERY trigger paths reach STRIKE where required; run targeted red/delete controls for
each changed mechanism; and finish the bounded builder aggregate.

**Critic-owned:** complete 30/60/90-second spacing populations, all repeated acquisition/punish
trials, complete roster pair/distance matrix, ten 90-second build fingerprints plus generated
counterparts, both blind protocols, **the played fight gate of step 8a including its deleted-repair
arm and its judgement**, every other governing-item population, final aggregation, scores and
verdict. The builder produces the capture and the two arms for step 8a; a fresh agent plays and a
fresh agent judges, and neither is the builder.

Short deterministic fixtures expose broken AI decisions and world coupling. Long traces and full
roster comparisons establish stable cadence, distinctness and quality.

## 1. Governing bar and canonical history

Canonical ownership in `docs/PLAN.md` §3 makes the governing set **RI-AI01–RI-AI07,
RI-CMB07, RI-CMB08, RI-DLG09, RI-PRG06, RI-STL01, RI-WLD02 and RI-WLD07**, including each
item's native methods, populations, thresholds, aggregation and hard fails. RI-AI01 and RI-AI05
are the current headline gaps, not substitutes for the other twelve governing items.
S22 fixes bare combat frames to `f@60`; AR-1 owns combat authority; RI-MTH07 requires demonstrated
world-side consumption. Round 1 scored RI-AI01 14/18 and RI-AI05 4/44, then round 2 implemented
RECOVER, fleeing-player closure, seven distinct behaviour signatures, and expanded consumption.
The independent round-2 verdict nevertheless scored 2/10 because RI-AI01 M3's chase-bot hard fail
fires on six of seven fighting statblocks on **M3's stationary-player fixture**. That verdict, not
the builder's moving-player 0.2521 result, is the current canonical conclusion.

Current HEAD retains `game/data/combat/ai.json`, the live consumer in
`game/src/combat/ai.js`/`enemy.js`/`system.js`, browser probes in `tools/harness/ai-browser.mjs`, and
the round-2 probes, teardown, unit check and critic M3 chart under `tools/combat/`. The seven realised
behaviours and the accepted AR-1/S22 work are not invitations to rebuild the system.

## 2. Bar classification

| Governing requirement | State | Reason / continuation obligation |
|---|---|---|
| RI-AI01 M2 alert ladder | independently established and preserved | Independent browser evidence: median SUSPICIOUS dwell 244 f@60, IDLE→AGGRO jump fraction 0. Preserve the live perception consumer. |
| M4 commitment cadence, M5 feints, M6 leash, M7 commitment yaw, M8 group tokens | independently established and preserved | Round-2 critic accepted their native predicates; regressions are hard failures where the item says so. Re-run aggregation after changes. |
| M1 acquisition geometry/fill | remaining remediation + fresh verification | Rear and 1.5R controls pass, but 0.9R/0° never acquires within 3 s. |
| M3 spacing loop | remaining remediation + fresh verification | Hard fail: non-COMMIT `min_dist_dwell > 0.35` on 6/7 at 60 s; measure 30/60/90 s and all specified start ranges so a favourable instant cannot hide it. |
| M9 punish reads | fresh verification, possibly remediation | Heal trigger passes; LONG_RECOVERY remains 7/12 against the item’s ≥80% within-12-f bar. Do not silently substitute the heal trigger. |
| RI-AI01 blind comparison | remaining artifact/judgement | No valid anonymised ours/reference 60 s distance+state pair has ever been built and independently judged. |
| RI-AI05 behavioural distinctness | independently established and preserved | Seven realised combat statblocks have seven distinct traces; the former four-way identical hash is closed. |
| RI-AI05 full roster, placement, mix, pacing and soul-yield checks | remaining build/verification | Current roster realises 7/10 archetypes against ≥8 and prior verdict measured only distinctness. Dependency on content/population does not waive these rows. |
| Model consumption/self-description | remaining remediation + verification | 60/99 leaves had demonstrated consumers; three were unverified and shadowed/equal-valued rows can be observationally inert. |

## 3. Exact continuation and acceptance

1. **Preflight and execution proof.** Re-run `node tools/combat/check-ai-units.mjs`, the round-2
   census/teardown, and `node tools/harness/ai-browser.mjs`. Perturb one live spacing leaf in a
   scratch arm and require an enemy trajectory to differ; a byte-identical trajectory is a hard
   instrument failure. Stamp commit and dirty state on every artifact.
2. **M3 remediation.** Change only live AI/recovery behaviour needed to avoid parking in the strike
   band. Population: all seven shipped fighting statblocks; stationary player; never attack;
   start ranges exactly those in RI-AI01 M3; 1,800/3,600/5,400 f@60. Native predicate uses only
   `state != COMMIT`: PASS requires `min_dist_dwell <= 0.10`, spacing variance and entropy within
   the item bands, for every statblock/duration/range; `>0.35` in any row is a hard fail. Publish
   per-row numerator, denominator and ratio, not only an aggregate.
3. **Moving-target guard.** Reuse `tools/combat/w1-12-r2-probe.mjs` over stand/walk/jog/sprint,
   strafe/circle/back-away, four ranges, and readings at 300/600/1,200/1,800 f@60. Every fighting
   statblock must enter RUSH and reach `min_gap_m <= 0.85*omega_m` at 2.0 and 3.2 m/s; the 5.0 m/s
   player must still be able to open the gap. This prevents an M3-only tune from making pursuit
   impossible or inescapable.
4. **M1.** In the browser, use the item’s bearings/radii and repeated trials. At 0.9R/0° acquisition
   must occur within 180 f@60 in every trial; rear 180° and 1.5R must never acquire; the acquisition
   set must not be a circle. Report trials and latency in frames and seconds.
5. **M9.** Run every declared punish trigger, including LONG_RECOVERY, for at least 12 trials each;
   the native heal fixture at `dist = 5.0*omega` is the scored population. PASS is conjunctive:
   ≥80% of heal trials enter PUNISH_READ within 12 f@60 **and** the enemy reaches STRIKE before the
   heal animation ends. Report both numerators over the same trials, plus committed-at-start
   separately; do not discard trials unless the governing method does. A timely state entry that
   never reaches STRIKE is red, not a partial substitute for the second conjunct.
6. **RI-AI05.** Use the item’s full native 44-point method over every shipped fighting statblock,
   placed encounter and required mix. At least 8/10 archetypes must be realised; every pair must
   clear the item’s **PASS floor of 1.8**; any pair below 1.0 is additionally a hard fail. Missing
   placements, empty populations or absent soul-yield rows fail closed. Where content belongs to
   another piece, name the exact absent fixture and leave this acceptance red until it lands.
7. **Consumption.** Reuse the round-2 consumption census. Every shipped, behaviour-affecting leaf
   must have a world-side consumer and a two-sided perturbation that changes an entity trace. A
   declared-but-unrealised archetype row may be reported separately but cannot count toward realised
   roster acceptance. Equal branch/default values are unmeasured, not consumed.
8. **RI-AI01 blind artifact.** Build two anonymised 60 s distance/state series with identical presentation:
   current build and a reference generated strictly from RI-AI01 C/D. Shuffle with recorded seed,
   quarantine reveal, include the actual series, and obtain a fresh judge. The builder must not judge.
8a. **Played fight gate — the quality verdict (Ruling W2).** Steps 8 and 9 are mechanism checks and
   neither of them contains a fight. This row is the one where a person meets the enemies as a
   player does. It is a **closure gate**: no native threshold below is changed by it, and a green
   M1–M9 sweep plus two passed blind packs without this gate is **not closure**.

   - **Population, bounded.** **Three shipped fighting archetypes** — the one with the best M3
     number, the one with the worst, and one ranged/GANK member — **× three fights each**, plus
     **one multi-enemy encounter**, all in the running game at production render with the shipped
     HUD. Ceiling **≤ 15 minutes of driven play per arm**. The archetypes are named and published
     before the run. No full play-through is authorised and none is needed.
   - **Conditions.** A fresh agent that has not read this plan, the AI source or the corpus receives
     the URL, the driver protocol and one sentence of premise. It plays; it does not read a trace.
   - **The questions, recorded verbatim before any reveal.** These are the items' own quality
     questions, put to somebody who has just fought:
     - `RI-AI01`: *"Did this enemy negotiate distance with you, or did it park?"* — the same
       question step 8's series asks, now asked of the fight instead of its plot.
     - `RI-AI02`/`RI-AI03`: *"Could you see an attack coming, and did you get a turn afterwards?"*
     - `RI-AI05`: *"You fought several kinds of enemy. Did they ask you different questions, or the
       same question in different costumes?"* — and, decisively, *"were any of them good to fight?"*
     - `RI-AI07`: of the multi-enemy encounter, *"was that a fight or a pile-on?"*
   - **The null arm that must come out worse.** Repeat the identical sessions on the
     deleted-repair copy §4 already requires (spacing/RECOVER remediation removed, everything else
     byte-identical). **Effect size:** a fresh judge reading the two arms' verbatim answers with arm
     labels removed must separate them correctly, **and** the shipped arm must be preferred on the
     `RI-AI01` question in **at least 3 of the 4 encounters**, bound on the **worst archetype, never
     the mean** (Ruling W1). If the arms read the same, the gate is **inert and cannot pass**: it is
     reported as inert exactly as an inert producer is, and the piece does not close on it.
   - **Independence.** Builder captures, fresh agent plays, fresh agent judges, arm identity
     quarantined until the answers are recorded. `node tools/blind/leakcheck.mjs` over the material.
   - **Instruments reused, by path — no new tool is authorised.** `tools/harness/ai-browser.mjs`
     (the existing browser probe and its one-browser discipline), `tools/combat/w1-12-r2-probe.mjs`
     (the moving-player fixture step 3 already pays for), `tools/capture/client.mjs`
     (`CaptureSession`, one connection per sequence) for the motion sequences Directive §2 requires —
     many frames from many angles, never one still — and `tools/play.mjs` to serve the build.
   - **Cost.** Two ≤15-minute driven arms on a browser this piece already pays for, reusing fixtures
     step 3 already builds. Small against one build round, and it is the only evidence in the piece
     that anyone fought anything.

9. **RI-AI05 blind artifact.** Separately build the native twenty-member pack: ten unlabelled
   90-second behaviour fingerprints from the build mixed with ten generated from RI-AI05 §B. Use
   identical fields, units, normalisation and presentation; randomise with a recorded seed, quarantine
   the key/reveal, and give the actual fingerprints to a fresh independent judge to cluster into roles
   without labels. Record the clustering before reveal. Ours collapsing into fewer than six clusters
   loses. This is not interchangeable with RI-AI01's distance/state judgement and is not satisfied by
   the numeric M1 pairwise matrix.
10. **Other twelve governing items.** Before browser or blind work, reconcile current HEAD against
    the complete native methods in RI-AI02–04, RI-AI06–07, RI-CMB07–08, RI-DLG09, RI-PRG06,
    RI-STL01, RI-WLD02 and RI-WLD07. Reuse the canonical enemy trace from step 1 and existing
    authoritative fixtures; do not create a parallel trace or rerun a population already emitted at
    the same stamped commit. Run cheap data/schema/census rows first, then one shared live population
    for the applicable behaviour rows. Every native row, denominator, threshold, comparison and hard
    fail remains binding, including attack-string/coordination/encounter-exit, exemplar/trace,
    parley, soul-yield, stealth-perception and world-placement/verticality seams. A missing fixture,
    dependency, trace field, authored population, comparison, or consumer is red, never omitted or
    averaged away. Preserve still-valid independent evidence only when its commit and inputs remain
    identical; otherwise rerun the affected row, not the whole corpus. Stop implementation when the
    first cheap governing row proves a dependency absent, record that exact red row, and defer its
    downstream expensive evidence until the dependency lands.

## 4. Controls, hard failures and artifacts

Required arms: shipped; spacing/remediation deleted; whole behaviour consumer disabled; M1 fill
perturbed; M9 timely-entry disabled while strike closure remains, M9 strike closure disabled while
timely entry remains; LONG_RECOVERY trigger disabled; one consumed leaf doubled and halved;
RI-AI01 reference arm; RI-AI05 generated-fingerprint arm. The RI-AI05 pack must also include a
known collapsed wrong-population control (ten copies/near-copies of one realised fingerprint), which
must cluster below six, and an intentional one-bit tell audit that must be detected before judging.
Deletion must restore the old M3 failure or otherwise materially worsen the native score. If deletion
does not move the trace, classify inert fix versus two-guard interaction with a 2×2; do not pass.
Degenerate controls are empty roster, stationary trace with behaviour disabled, and equal-valued
branch/default. Each must go red. Required outputs live under a new `reports/w1-12-r3/` plus the
quarantined verdict artifact directory; preserve existing `reports/w1-12*` evidence unchanged.

Aggregation must run all M1–M9, the full RI-AI05 method, and every row of the other twelve
canonically governing items together. Any item-native hard fail, missing trace field, empty or
weakened population, unexecuted live call path, blind reveal leak, omitted governing row, or
regression of an independently established row fails the piece regardless of a summed score.

**Closure clause (Ruling W2).** A green implementation without the **played fight gate of step 8a
passing on its own terms, with its deleted-repair arm judged worse at the stated effect size**, is
not closure — no more than a green implementation without the two blind protocols is. A step 8a
result that is missing scores this piece red; a step 8a result whose arms are inseparable is
reported `inert` and is likewise not closure. Steps 8 and 9 may never be reported as the quality
verdict, and every citation of them must carry its counterpart's provenance on the same line.

**Bind on the worst archetype, not the mean (Ruling W1).** Wherever a predicate runs over the
roster — M3 spacing, M9 punish, RI-AI05 pair distance, step 8a preference — the binding number is
the worst constituent with the full per-archetype table published. A player fights one enemy at a
time. Where a row states "must differ" or "must change", it also states the magnitude that counts:
for M3 remediation the deleted-repair arm must return `min_dist_dwell > 0.35` on **≥ 6 of 7**
statblocks (the measured baseline failure), and for the perturbation controls a trace delta below
**one whole authored step** of the perturbed leaf is `inert`, not a pass.

## 5. Dependencies and stale assumptions

W1-15 owns perception multipliers; W1-09 owns attack/root-motion semantics; population/content owns
additional realised statblocks; soul economy owns yields. These are explicit dependencies, not
exemptions: W1-12 remains red for any unmet governing row and reports the blocking fixture/path.
Stale assumptions: the moving Lissajous M3 fixture is not the governing M3 fixture; a favourable
headline improvement does not prove closure code executed; “seven distinct” does not satisfy the
≥8-archetype roster; and the original anchor-release and attack-root-motion gaps may have changed at
HEAD and require verification, not automatic re-filing.

## 6. Falsification audit

Could a competent builder do exactly this and still fail a governing item? Only if the native
RI-AI05 subchecks or RI-AI01 blind protocol were replaced by summaries. This plan therefore requires
the native methods, complete populations, per-row artifacts, aggregation, and independent blind
judgement. The strongest critic move is to reproduce M3 unchanged on the exact stationary fixture,
then delete the repair and inspect whether the old six-of-seven failure actually returns.

**Critic hand-off:** awaiting criticism. No satisfaction is claimed.

## Independent plan critique — 2026-08-09

**Verdict: BLOCKING / awaiting-remediation.**

The plan can go fully green while missing both governing items. First, step 6 lowers RI-AI05 M1's acceptance from the native **minimum pairwise distance ≥1.8** to “clear ... 1.0”; 1.0 is only the item's hard-fail boundary. A roster whose minimum distance is 1.2 could therefore pass the written plan while failing the headline check. Second, the only blind artifact specified in steps 8/§4 is RI-AI01's two 60-second distance/state series. RI-AI05 separately requires ten unlabelled 90-second fingerprints mixed with ten generated fingerprints and loses if ours collapse into fewer than six clusters; that comparison is absent. Third, step 5's M9 PASS checks only entry into `PUNISH_READ` within 12 f@60 and omits the conjunct that the enemy **reaches STRIKE before the heal animation ends**. Running extra LONG_RECOVERY trials does not substitute for the native heal predicate.

**Required remediation:** restore RI-AI05 M1's 1.8 pass floor (while retaining <1.0 as a hard fail), add the complete RI-AI05 ten-plus-ten clustering blind protocol and its fewer-than-six-clusters loss condition as a distinct artifact/judgement, and make M9 acceptance conjunctive: ≥80% enter within 12 f **and** reach STRIKE before heal completion. Controls and aggregation must fail on each omitted conjunct independently.

## Remediation record — 2026-08-09

Completed editorially: §3 now restores RI-AI05's 1.8 headline pass floor while preserving its
<1.0 hard fail; specifies its separate ten-build-plus-ten-generated, 90-second unlabelled clustering
pack and fewer-than-six loss; and makes M9 require both timely PUNISH_READ entry and STRIKE before
heal completion over the same native heal trials. §4 gives each M9 conjunct an independent red arm
and adds collapsed-population/tell controls for the roster pack. No implementation or evidence was
repeated. Deferred to the planned build: producing and independently judging both blind artifacts.
Escalated: none; all recorded findings were resolved directly from RI-AI01 and RI-AI05.

**Remediator hand-off:** materially edited; awaiting fresh re-criticism. No satisfaction is claimed.

## Fresh reviewer-editor outcome — 2026-08-09

**Edited — awaiting re-criticism.** Independent reconstruction from canonical ownership found that
the received remediated version treated RI-AI01 and RI-AI05 as the complete governing set although
`docs/PLAN.md` assigns fourteen items to W1-12. A builder could therefore make every written check
green while omitting RI-AI02–04, RI-AI06–07, RI-CMB07–08, RI-DLG09, RI-PRG06, RI-STL01,
RI-WLD02 and RI-WLD07. The plan now keeps the two headline remediations, adds the twelve missing
native item populations and hard fails to the acceptance/aggregate, shares the canonical trace and
orders cheap fail-closed dependency gates before live/blind work. This is a material edit and is not
approved by its editor.


## Fresh independent reviewer-editor outcome — 2026-08-09

**SATISFIED.** Independent reconstruction from `docs/PLAN.md` ownership and judge metadata through
the governing item texts, native methods and applicable rulings found no remaining material
bar-sufficiency or cost-effective-deliverability defect. The received plan is approved unchanged
apart from this state/outcome record; its authoritative-file references, shared evidence reuse,
cheap-first gates, bounded native populations, fail-closed aggregation, controls, independence and
world-consumption requirements remain binding. Escalated/deferred planning ambiguity: none.

## Reviewer-editor round — 2026-08-14 (Ruling W2 propagation)

**Outcome: MATERIAL EDIT / awaiting-recriticism. No escalation.** The 2026-08-09 approval was sound
against the bar as it then stood. The bar moved on 2026-08-14 and this plan did not.
`orchestration/audits/PLAN-AUDIT-2026-08-14.md` ranks this piece second of thirty because both of
its human gates are packs of *numbers* and nobody in the plan ever fights an enemy.

**What I read, and what it changed.** I read `RI-AI01` and `RI-AI05` themselves rather than trusting
the audit. Both blind protocols are transcribed **verbatim and correctly** from the items
(`RI-AI01:252`, `RI-AI05:276`) — the plan is not the author of the chart packs, the corpus is. So I
did not delete or weaken them, and I did not patch the plan around a bar defect. Instead I stated,
per Ruling W2's own instruction to say which case each row is in, that they are mechanism/leak
checks; added the quality verdict the piece lacked as step 8a; bound aggregation on the worst
archetype and named effect sizes (Ruling W1); and recorded as corpus-level the genuine incoherence
that `RI-AI01`/`RI-AI05` build counterparts from their own published design tables while
`RI-CMB07`'s wave-1 amendment rules exactly that `VOID`. That needs an `ARBITRATION` ruling, not a
plan edit.

**Weakest joint in my own edit, for the next reviewer to attack first:** step 8a's null arm reuses
the deleted-repair copy §4 already builds, which is cheap and honest but couples the quality gate to
one specific repair. If the M3 remediation turns out to be small enough that a player cannot feel it
even when the numbers move, the arms will be inseparable and step 8a will report `inert` — correctly
under my own wording, but a reviewer should decide whether the right response is a different null
(a build with the whole behaviour consumer disabled, already listed in §4) or an accepted CARRIED
risk. I have not pre-empted that call.

**Cost note.** Step 8a adds two ≤15-minute driven arms and a capture pass to a piece that already
pays for a browser, `ai-browser.mjs`, the r2 moving-target probe and two blind packs. No new tool is
authorised and no population is enlarged.

**Exact next action:** a **different** fresh reviewer-editor judges this version. Do not dispatch a
builder against it while it reads `awaiting-recriticism`.
