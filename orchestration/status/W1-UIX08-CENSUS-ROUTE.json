{
  "could_not_do": [
    "WIP — the delete-the-fix arm has not run yet. Everything else below is measured.",
    "THE PAUSED HAND-BACK NODES KEEP THE FULL WINDOW GEOMETRY AND I DID NOT FIX THAT. At `hold.out` the census hands the body back and the player walks out of the barge hold with a centred, translucent, ~43%-of-frame panel up, where the vellum panel was a bottom strip. It takes no input and the world is visible through it (§E1), and routing it anywhere else would be the partial route the previous piece correctly refused — but a reviewer who thinks a centred panel is the wrong shape for a frame in which the player is walking is looking at a real question this piece does not answer. Recorded in RI-UIX08 §H5 as well as here so it cannot be lost.",
    "NO GAMEPAD ARM. Same as the previous piece: no pad shim is wired into this probe, so CRITIC-DOCTRINE §1.2b clause 4's gamepad leg is scored 0 FAIL-CLOSED for the census, not passed. Keyboard is driven with real DOM KeyboardEvents throughout. Mouse and touch on the census columns are UNMEASURED here — the previous piece measured both on the conversation column and the census uses the SAME `hits` list, the SAME `pickHit`, the SAME `dialoguePointer` and the SAME `_dialogueConfirm`; that is an argument from shared code and NOT a measurement, and it must not be quoted as one.",
    "PERSUASION IS UNTOUCHED AND STILL UNDIAGNOSED. The previous piece measured it as a control that looks dead (69 -> 69, no prose). The census does not touch it: `_censusDialogueCtx` builds no action rows except the typed buffer, so no persuade row exists during creation and nothing I changed can have moved that number either way. I did not re-measure it and I am not claiming anything about it.",
    "INLINE LINKS ARE STILL UNMEASURED BY CLICK. Unchanged from the previous piece and out of scope here — the census produces zero links by construction (no topic vocabulary exists before the writ is stamped), which is a different fact from the conversation-side hole.",
    "I DID NOT DRIVE THE TITLE SCREEN'S 'NEW GAME' BUTTON. The scene is opened with `censusBegin({})` — what `_titleApply('new')` calls — exactly as the world arm opens a conversation through `A.talkTo`. The two paused hand-back nodes are resumed through `censusEnter` for the same reason: walking a body across a room is not one of this window's affordances. Every control ON the window is operated by a real DOM event.",
    "`node tools/contention.mjs` said WAIT before the first run and I proceeded anyway, one browser at a time, sequentially; later runs were taken at 1-3 browsers and load 9-15 over 4 cores. Rule 21 says say so; this is me saying so."
  ],
  "task_id": "W1-UIX08-CENSUS-ROUTE",
  "state": "in_progress",
  "date": "2026-08-15",
  "roadmap_item": "RI-UIX08 §H / the second half of the defect the owner reported from playing the deployed build",
  "headline": "Character creation now runs on the RI-UIX08 dialogue window, whole. The route needed two affordances the window had no element for; both are expressed INSIDE §A's six elements, and RI-UIX08 is amended with a §H that says so and says what a critic measures.",
  "the_defect_the_probe_found_in_my_own_fix": {
    "headline": "THE SCENE DEAD-ENDED FOREVER AT `writ.class-verdict`, ON ALL THREE CLASS ROUTES, AND ONLY DRIVING EVERY NODE FOUND IT.",
    "mechanism": "The dialogue window owns its own caret (`UISystem.dialogueFocus`), and I reset it only when the SPEAKER changed. Inside the Writ House the speaker never changes, so the caret carried the previous node's row index into the next node. `writ.class-verdict` offers exactly ONE row; the caret arrived on row 2 or row 6; `_dialogueConfirm` read `m.topics[6]`, got undefined, and returned null. A row you can see, a button you can press, and a scene that cannot be finished — CRITIC-DOCTRINE §1.2b's drawn-and-does-nothing, in the one place in the game a player cannot walk away from.",
    "the_second_symptom_which_a_weaker_check_would_have_called_healthy": "The run that asked for class route ROW 0 ('Name a trade the Office already has a word for') silently took ROW 1 ('Tell her what you actually do'), because the caret arrived carrying the previous node's index. The node advanced, so any check asking only 'did it advance' would have passed it. It was caught because the probe records WHICH row it confirmed, not merely that something happened.",
    "why_it_existed": "`CensusSurface.sync()` has always done this for the vellum panel — `if (this.node !== wasNode) this.sel = 0`, then a clamp. The window has a second caret, so it needed its own copy of the rule. This is precisely the two-implementations hazard RULES rule 10 names, and it is why the answer semantics were deliberately left in ONE place (`CensusSurface.commit()`) while the caret could not be.",
    "the_fix": "game/src/ui/system.js `_censusWindowModel`: the focus resets when `${node}#${question_id}` changes, and is clamped when the row count shrinks under the same key. BOTH, not either — the key covers `writ.class-questions`, one node that asks ten different dilemmas, and the clamp covers a list that shrinks without the key moving. Neither substitutes for the other."
  },
  "a_second_defect_found_by_accident_and_fixed": {
    "headline": "STARTING A NEW GAME WITH A CONVERSATION OPEN KILLED THE SIMULATION LOOP. `stepFrames() threw: npcDisposition('agaceph-sheem'): nobody by that name is in the world`, exit 12.",
    "mechanism": "`talkTo()` throws 'the census has the conversation' — the invariant was enforced in ONE direction only. Nothing stopped `censusBegin()` running while a conversation was open. It moves the player into the barge hold, the person they were talking to is no longer in the world, and the next fixed step runs `_conversationStep -> _uiCtx -> _dialogueCtx -> npcDisposition`, which throws INSIDE THE FIXED STEP.",
    "how_it_was_found": "By accident, and the accident is worth recording. The probe was reordered so the presence control (an ordinary conversation) runs BEFORE the creation walk — a change made for an unrelated reason, that a control which only runs when the experiment succeeded is absent exactly when it is needed. The whole run then died on the first step of the new game.",
    "pre-existing_or_mine": "PRE-EXISTING. `_conversationStep`'s `_uiCtx()` call site is the previous piece's (W1-UIX08-INPUT-FIX), and the `if (this.conversation.open) return this._conversationStep(input)` branch in `_censusStep` is older still. Nothing I added is on that path. I fixed it anyway because the probe was not doing anything the game cannot: `_titleApply('new')` calls `censusBegin`, and a player who opens the pause menu mid-conversation and starts a new game arrives by exactly this route.",
    "the_fix": "game/src/engine.js `censusBegin()`: one line, `if (this.conversation && this.conversation.open) this.conversationClose();` — the same invariant `talkTo()` already enforces, now enforced from both sides."
  },
  "the_owner_defect": "Owner, having played it: 'It also wasn't being used for the dialogue in the character creation/new game flow, which it should be.' That is the first conversation any new player ever has, and it was the last one still drawn by render/ui.js's bottom-anchored vellum reply panel — the surface RI-UIX08's hard fail is written about.",
  "the_ruling": {
    "question": "RI-UIX08 §A says 'Six elements, no more.' The census asks in shapes the window has no element for. Amend the item, or fit the census inside the six?",
    "answer": "BOTH, and they are not alternatives — that framing was the one thing in the brief I did not follow. The census FITS inside the six: no seventh element is added, and the two missing affordances are expressed as row semantics inside element 4. But TWO of the six are required to be ABSENT during creation, and no reading of §A as written permits that, so the item is AMENDED to say which four are present, why the other two must not be, and what a critic now measures. Fitting inside six without amending would have left a critic scoring a census capture at '4 of 6 elements' against a bar that never contemplated the scene.",
    "the_two_affordances_and_where_they_went": {
      "multi_select (4 `pick` nodes)": "A `· ` prefix INSIDE THE ROW'S OWN TEXT. Not a colour: §E3 measures one bronze for every column entry and §F1 rules read/unread marking off, so a colour mark would be indistinguishable from the affordance §F1 exists to prevent. A prefix lives in the row's `text`, so it is in the declared element census a probe reads and not only in the pixels. The count ('She is waiting for 2 more.') is AUTHORED text in her voice and goes into the transcript as prose. Nothing on this route composes English out of ids, stat names or numbers.",
      "typed_name (3 `text` nodes)": "A ROW in the column, above §D2's rule, drawn as the typed characters plus the `▁` caret the vellum panel used. §D2 already divides the column into 'what you can DO with this person' and 'what you can ASK them about the world'; telling somebody your name is the first, and that is the section the reference itself puts Persuasion and Barter in. It is not a new section and not a new element. Both of RI-JRN01 O17's routes still work: type on a keyboard, or walk the caret onto a ledger name and confirm that instead."
    },
    "the_two_absences_and_why_they_are_required_not_permitted": {
      "element_5_disposition": "Before the writ is stamped there is no character for anybody to have a disposition toward. Drawing one puts sim.player's boot default on screen as a fact about a person who does not exist yet — the same defect W1-26 r3 photographed on the title screen. RI-DLG04 has nothing to move here and no route to move it with.",
      "element_6_goodbye": "There is no way out of character creation. §D5 says Goodbye is 'the only way out that the window advertises'; where there is no way out it must advertise none. A Goodbye drawn here would be CRITIC-DOCTRINE §1.2b's control that is drawn and does nothing — a hard fail, and worse than an absent control because it lies to the player. A greyed-out one is the same lie with an excuse attached."
    },
    "not_partial": "The route is all-or-nothing by construction, not by care. `Engine._censusRouted()` is ONE predicate read by the three sites that have to agree — `_censusSync` (suppresses the old panel), `_conversationSync` (must not un-suppress it out from under the census) and `_censusStep` (routes the input to whichever surface is drawing). The window is drawn under EXACTLY the condition the vellum panel was drawn under before: wherever `buildCensusModel()` returns a model. Same visibility rule, one surface.",
    "reversible": "Ruling: creation runs on this window in this shape. Overturned by a §G-form human gate in which a participant who has played both reports creation as WORSE to use on this window — most plausibly on the typed name, the one affordance with no reference behind it and the one place a player could expect a text field rather than a row. The fix named in advance if that happens: a text affordance defined as a SEVENTH element scoped to census mode, argued as a deliberate amendment to §A, not added quietly."
  },
  "what_the_corpus_amendment_says": {
    "file": "corpus/86-ui/RI-UIX08-dialogue-window.md",
    "shape": "APPEND-ONLY. Nothing struck, nothing weakened, nothing deleted. §A gains a blockquote pointing at §H; §H is new; the Scoring section gains a second hard fail and an explicit statement that the native scale stays out of 48 because §H is a GATE, not an axis — an amendment that re-based the denominator would make every score recorded before today incomparable with every score after it.",
    "H1": "which of the six elements are present (1,2,3,4) and which are required absent (5,6), with the reason for each.",
    "H2": "the two row semantics inside element 4, and that neither is a new element.",
    "H3": "census affordances may not leak into an ordinary conversation, stated as a measurable pair: a conversation frame declares six elements including the exit and the meter and carries no picked mark and no typed row; a census frame declares four and no exit.",
    "H4": "what a critic must now measure — four checks, all §1.2b-shaped.",
    "H5": "what the amendment does NOT claim. Morrowind's character creation is its own full-screen menus, not its dialogue window (REF-A12b-char_creation__mw-132183942.jpg is a panel over Seyda Neen). So §H has NO REFERENCE PLATE BEHIND IT and must not be read as measured; it is this build's own ruling, argued from RI-JRN01 rather than from Morrowind. §H5 also records the one thing this piece does not fix: at the paused hand-back nodes the window keeps its full geometry, where the vellum panel was a bottom strip."
  },
  "the_census_as_counted_from_the_repo_not_from_memory": {
    "command": "node -e over the parsed `nodes` array of game/data/dialogue/topics/writ-house.json, 2026-08-15",
    "nodes": 22,
    "kinds": "8 choice, 4 pick, 3 text, 1 questionnaire (one node, ten dilemmas), 1 observed, 2 conditional, 5 with no input (hold.come-to, hold.wake, hold.out, writ.enter, writ.strange-check)",
    "note": "The previous piece's grep over the raw file gave the same five input kinds. I re-derived it from the parsed nodes rather than inheriting the figure, per CLAUDE.md's four-line check clause 2."
  },
  "verified": {
    "instrument": "tools/ui/dialogue-drive-probe.mjs --census. Real DOM events only: KeyboardEvent for the caret and for confirm, printable KeyboardEvents through input/real.js's TEXT route for the typed name, Backspace, Enter. Nothing in the census arm calls a function the UI also calls and nothing sets UI state. The two instrument traps the previous piece documented are guarded by I0 (a real key must reach input.moveX / pendingPress before anything below is believed) — that guard PASSED on every run.",
    "run": "16 of 16 checks passed. reports/uix08/drive-probe-census.json, copied to corpus/90-verdicts/wave1/artifacts/W1-UIX08-CENSUS-ROUTE/ because reports/ is gitignored and HAZARDS §9 says cited evidence there never reaches the remote.",
    "commit": "287f7ff7 (the landed fix). engine.js at that commit is byte-identical to the tree the run was taken on — checked with `diff <(git show 287f7ff7:game/src/engine.js) game/src/engine.js`.",
    "C0": "PASS. `hold.come-to` is silent by design (RI-JRN01 O6 — a body before anybody asks you anything) and NO surface draws there, correctly. The first node that speaks, `hold.hatch-name`, opens on the RI-UIX08 window in census mode with render/ui.js suppressed. Speaker 'Jeeh-Ei'.",
    "C1": "PASS. 49 node visits over all three class routes: 46 drew the RI-UIX08 window, 0 left render/ui.js unsuppressed, and the 3 that drew no surface at all are the paused hand-back node. Input kinds exercised: none, text, observed, choice, pick, questionnaire — all five shapes the census has.",
    "C1b_PIXELS": "PASS, and this is the check that matters most because run 1 got it wrong. The old vellum panel painted ZERO distinct strings on the 'dialogue' surface across all three passes, read from the RENDERED-TEXT REGISTER (`getRenderedText({surface:'dialogue'})`), which is fed by the draw call and cannot drift from the frame. `complete: true` on all three, so it is absence and not ignorance.",
    "C2": "PASS. All three routes RUN TO COMPLETION: custom done (16 visits), questionnaire done (21), named done (12).",
    "C3": "PASS. Over 46 census frames: 0 declared a `dialogue_exit`, 0 a `disposition_meter`, and all 46 declared exactly elements [1,2,3,4]. §H1 measured rather than asserted.",
    "C4": "PASS on all four pick nodes. First confirm -> 1 mark in the column; confirming the same row again -> 0 marks; completing the count advances the node. writ.class-custom-favoured -> -neglected -> -primary -> -secondary -> writ.class-verdict.",
    "C4b": "PASS. What the marks committed reached the spec: favoured 2, neglected 2, primary 3, secondary 2 — e.g. {\"name\":\"Sathe\",\"favoured\":[\"strength\",\"endurance\"],\"neglected\":[\"agility\",\"speed\"],\"primary\":[\"blades\",\"axes-maces\",\"polearms\"],\"secondary\":[\"greatweapons\",\"marksman\"]}. RI-MTH07-shaped: the drawn mark and the written field are the same fact.",
    "C5": "PASS. The typed row went '▁' -> 'Sathel' under real printable keydowns -> 'Sathe' after a real Backspace. The word contains an 'e' on purpose: KeyE is `interact`, and it TYPED rather than committing (node still 'hold.hatch-name' afterwards), which is W1-26 r2 §3's exact defect measured rather than assumed.",
    "C5b": "PASS. spec.hatchName == 'Sathe' after Enter — the typed name, not a ledger name.",
    "C5c": "PASS. RI-JRN01 O17's other half on a different text node: the caret walked past the typed row onto a ledger name, Enter committed it, spec.givenName == 'Teeba-Ei' == the row that was focused.",
    "C6": "PASS. The questionnaire asked 10 dilemmas with 10 distinct question ids (q1..q8, q10, q11) and every answer moved it on. RI-JRN01/W1-26 r2 measured 0 of 10 questions reaching the frame once; they reach it here and each is its own block in the transcript.",
    "C6b": "PASS. 70 ArrowDown presses over 11 nodes: 70 moved rowIdx by exactly 1 and 70 left a drawn row marked focused. Capped at 8 rows per node (writ.birthsign offers thirteen) and the cap is recorded per node in the report.",
    "C6c": "PASS. No node failed to advance across three full routes. This is the check that was RED before the caret fix.",
    "C7_PRESENCE_CONTROL": "PASS, and it runs FIRST on purpose. An ordinary conversation in the same browser session still declares all six elements including the exit and the disposition meter. Without this, C3's zeros could be a window that draws nothing.",
    "C8_LEAK_CONTROL": "PASS. Over 11 conversation column rows: 0 typed rows, 0 carrying a picked mark. §H3's no-leak clause, measured."
  },
  "delete_the_fix": {
    "method": "A REAL `git worktree add --detach` at the landed commit 287f7ff7 — not a hard-link of the live tree — with `tools/node_modules` and the root `node_modules` symlinked in (HAZARDS §7: a worktree cannot render the game without them and fails as GAME_BROKEN). Both arms are served from that frozen clone, which is HAZARDS §11's freeze-by-construction rather than a drift detector.",
    "the_teardown_is_one_line": "`diff <(git show 287f7ff7:game/src/engine.js) <worktree>/game/src/engine.js` prints exactly `142c142 const CENSUS_WINDOW = true;` -> `false;` and nothing else.",
    "result": "THE CONTROL ARM GOES RED, at the check the whole piece rests on. With CENSUS_WINDOW false, the first speaking node `hold.hatch-name` reports `window open=false, census mode=false, render/ui.js suppressed=false, speaker null` — the census is back on the old vellum panel, which is exactly the behaviour the owner reported.",
    "the_control_is_not_over-broad_either": "C7 and C8 still PASS in the control arm: an ordinary conversation keeps all six elements and the window works normally. So the teardown removes the census route and nothing else — it is not a switch that simply turns the window off."
  },
  "files_touched": [
    "orchestration/status/W1-UIX08-CENSUS-ROUTE.json",
    "corpus/86-ui/RI-UIX08-dialogue-window.md",
    "game/src/engine.js",
    "game/src/ui/system.js",
    "game/src/ui/screens/dialogue.js",
    "game/src/character/scene.js",
    "tools/ui/dialogue-drive-probe.mjs",
    "corpus/90-verdicts/wave1/artifacts/W1-UIX08-CENSUS-ROUTE/",
    "docs/shots/2026-08-15-uix08-census-CONTROL-old-vellum-panel.png",
    "docs/shots/2026-08-15-uix08-census-FIX-new-dialogue-window.png",
    "reports/blog-feed.jsonl"
  ],
  "what_changed_in_the_code": {
    "game/src/character/scene.js": "`CensusSurface.step()` split into `step()` (unchanged behaviour, the vellum route and the control arm), `takeBack()` (block = delete a typed character, else un-pick) and `commit(state, {useTyped})` (the WHOLE of the answer semantics, and the only copy). Both drivers call the same two functions — RULES rule 10. One real bug fixed on the way: `via: this.typed ? 'typed' : 'ledger'` was evaluated two statements AFTER `this.typed = ''`, so it reported `ledger` on every text node in the scene including the ones the player typed. Trace data only, nothing branched on it, but a trace that says the opposite of what happened is worse than none.",
    "game/src/engine.js": "`CENSUS_WINDOW` (the one-boolean control arm); `_censusDialogueCtx()` (the census as the window sees it, shaped like `_dialogueCtx()` on purpose, with the utterance keys that turn a one-node-at-a-time surface into §D3's running transcript); `_censusRouted()` (ONE predicate, read by the three sites that must agree); three lines in `_censusSync` (the missing `setSuppressed` — the actual defect); two in `_conversationSync` (do not un-suppress out from under the census); one in `_uiCtx`; the routed branch of `_censusStep`; and the `conversationClose()` guard in `censusBegin`.",
    "game/src/ui/system.js": "`_censusWindowModel()` (the census branch of `_dialogueModel`, including the caret reset that the probe proved was needed); census branches in `dialogueStep` (no Goodbye row, no `roll`-to-exit) and `_dialogueConfirm` (returns which row, decides no answer); the `dsig` extension so a typed character, a pick mark and a questionnaire dilemma each repaint.",
    "game/src/ui/screens/dialogue.js": "`DialogueHistory.appendKeyed()`; `census` mode in `drawDialogue` — elements 5 and 6 not declared, the `· ` picked prefix and the typed row inside element 4, and `census` / `census_node` / `picked_rows` / `elements_present` published so a probe reads the mode from the frame rather than inferring it.",
    "tools/ui/dialogue-drive-probe.mjs": "the `--census` arm rewritten to drive the real input path over all three class routes, plus `--shot` and `--open-only` for the delete-the-fix pair. Reused rather than replaced, as the brief required."
  },
  "files_claimed": [
    "corpus/86-ui/RI-UIX08-dialogue-window.md",
    "game/src/character/scene.js",
    "game/src/ui/screens/dialogue.js",
    "tools/ui/dialogue-drive-probe.mjs",
    "reports/uix08/"
  ],
  "declared_overlap": {
    "T4-r2-builder": "live in game/src/ui/ on the screens. My edits to game/src/ui/system.js are confined to the dialogue-window block (_dialogueModel, _censusWindowModel, dialogueStep, _dialogueConfirm, the dsig line) and to game/src/ui/screens/dialogue.js, which nobody live has declared. I touch no other screen.",
    "game/src/engine.js": "13 live pieces are registered against it; ownership.mjs reports UNDECLARED OVERLAP as the normal state of that file. My additions are `CENSUS_WINDOW`, `_censusDialogueCtx()`, `_censusRouted()`, three lines in `_censusSync`, two in `_conversationSync`, one in `_uiCtx` and the routed branch of `_censusStep`. `git diff HEAD -- game/src/engine.js` to be checked immediately before landing."
  },
  "next_step": "read reports/uix08/drive-probe-census.json; then the delete-the-fix arm on a git worktree with CENSUS_WINDOW=false"
}
