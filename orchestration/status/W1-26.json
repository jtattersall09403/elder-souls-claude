{
  "task_id": "W1-26",
  "brief": "docs/PLAN.md \u00a73 \u2014 W1-26 The opening, as a played scene (ULTRACODE). Seven paths: journey.firstlaunch.flow, journey.chargen.diegesis, journey.onboarding.explanation, journey.opening.exchange, journey.opening.legibility, experience.opening.hook, experience.opening.beats. Judged by RI-JRN01, RI-JRN09, RI-EXP01.",
  "started": "wave-1",
  "state": "building",
  "outputs_expected": [
    "a title surface (RI-JRN01 M20/HF9) with the exact O3 set and a working Continue",
    "a demonstrated, enumerable rendered-text accessor (RI-JRN01 M9/M15 gate, RI-JRN09 M1)",
    "the writ drawn at the node that stamps it and carried out as an object (RI-JRN01 M8/O10)",
    "DTR / AC / NAMED measured live (RI-JRN09 M1/M2/M3)",
    "tools/journey/journey-run.mjs + beat-extract.mjs if the tool builder has not landed them",
    "tools/experience/session-run.mjs, beat-diff.mjs, beats-from-md.mjs, isolation-check.mjs (RI-EXP01, five phantom tools)",
    "RI-EXP01 run for the first time on any build: beat log, beat diff, derived targets"
  ],
  "outputs_written": [
    "game/src/render/text-register.js (the rendered-text accessor, fed by fillText, clip-aware)",
    "game/src/render/title.js (the title surface, RI-JRN01 O1-O4/O17/O18/M20/HF9)",
    "game/src/render/renderer.js (instruments both 2D surfaces; composites the title last)",
    "game/src/engine.js (title boot, title input step, _titleApply, titleShow/Dismiss/Activate/getTitleState)",
    "game/src/harness/api.js (getTitleState, titleShow, titleDismiss, titleActivate, getRenderedText, renderedTextClear)",
    "tools/harness/w1-26-opening.mjs (M20/HF9, the accessor demonstration, M9/M15 greps, DTR through the draw call, O6/M4-1, O17 parity, two CONSUMPTION perturbations, instrument falsification)",
    "corpus/95-experience/briefs/first-hour.md (RI-EXP01 step 1 names this path; it did not exist, and it is instruction-neutral by construction)",
    "tools/experience/beats-from-md.mjs (RECONSTRUCTED after I overwrote the tools agent's copy \u2014 see findings)"
  ],
  "findings": [
    "smoke.mjs PASS at session start (6/6, chromium 141, SwiftShader).",
    "BUILT + MEASURED: a title surface now exists. option_ids is ALWAYS exactly O3's five (continue,new,load,settings,quit-to-menu) with `enabled` telling the truth rather than rows being hidden; with no save, focus defaults to `new`; with a save, `continue` is row 0 and focused. Shown in play mode and under ?title=1; constructed but down under `harness` so the ~30 existing probes that boot and immediately drive the world are unaffected. HF9 is closed.",
    "BUILT + MEASURED: the rendered-text accessor. `__HARNESS.getRenderedText()` is fed by wrapping CanvasRenderingContext2D.fillText/strokeText on every 2D surface the renderer owns, so nothing can appear in it that was not handed to a draw call. It ALSO shadows the clip stack (save/restore/clip + the path calls) and computes each string's box from measureText, so a string painted outside its surface's clip comes back `clipped:true` and is excluded by default. RI-JRN01 M9/M15 are no longer `unmeasurable => 0`.",
    "FIRST RUN OF THE ACCESSOR, and it immediately found something: at 640x360 the hold.hatch-name node hands 13 strings to fillText and 1 of them lands outside the vellum clip \u2014 12 drawn, 1 clipped. That is orphan text ONE LAYER BELOW the round-2 defect: the model computed it, the layout laid it out, fillText was called, and the player cannot read it. A DTR computed from the layout's own `text` array cannot see this; this accessor can.",
    "NOTE for any probe: with setRenderRate(0) the title never redraws, so its strings never reach the register. A probe measuring title text must call renderFrame()/screenshot() first. Dialogue text does register under renderRate 0 because UILayer.metrics() performs the layout.",
    "RI-JRN01 M4 has NO WEIGHT in the Scoring table (Diegesis M5/M6/M7/M8, Flow M1/M2/M3/M12/M20, Restraint M9/M10/M11/M15, Reach M13/M14, Naive M16/M18/M19 = 100). O6's >=60s is the item's own best idea and costs zero points; clause 1 is corpus_debt regardless.",
    "BUILT: `first_input` and `first_control` are now EMITTED. Both types have been in the closed A-JRN7 vocabulary (sim/events.js) since it was written and were emitted by NOTHING \u2014 which is the whole reason BAR-CRITIQUE-W1-07-R1 blocked RI-JRN01 M4 clause 1 ('the interval between first_control and the first field-writing dialogue_open, and neither event exists'). Engine._journeyStamps() emits them, _censusSync() stamps the first node with a `sets` field, and __HARNESS.getJourneyStamps() returns the O6 interval in available play seconds.",
    "BLOCKED (not mine, in-flight): at 07:04 the build STOPPED BOOTING in every viewport. game/src/input/touch.js `_assertLayout` throws 'RI-JRN04 H11: touch controls heavy and parry are 6.9 px apart, below the 8 px minimum' out of RealInput's constructor, out of Engine._boot, so __HARNESS.ready() rejects and every browser probe in the tree dies at exit 12. This is W1-29 (mobile/touch) mid-edit on game/data/input/profiles.json against their own assertion. It booted fine at 06:56. Not fixed here; retried.",
    "COLLISION I CAUSED, and repaired: another agent is building tools/experience/ (lib/md.mjs 07:06, session-run.mjs 07:09, beat-extract.mjs 07:10). I checked `ls tools/experience` at 06:55 when it did not exist, wrote my own beats-from-md.mjs at 07:12, and OVERWROTE theirs. It was untracked, so git had no copy. Reconstructed it against the two surviving artifacts (their emitted RI-EXP01.beats.json and their lib/md.mjs); `--check` now passes byte-for-byte apart from `generated_at`, using THEIR schema (elder-souls/beat-sheet@1) and field names so their beat-extract/beat-diff still read it. The incident is recorded in the file's header. Lesson for the next agent: re-`ls` a shared directory immediately before writing into it.",
    "O6 OPEN, and it is cheap: censusBegin() emits dialogue_open and puts hold.hatch-name up IMMEDIATELY, so available play before the first character-defining question is ~0 s. Morrowind's actual trick is that Jiub asks only when you TALK to him. Making the hold open census-paused and the hatch-name node open on `interact` with Jeeh-Ei would make O6 pass for real and is better design. Not taken this round: census.js is W1-07-r3's live working file and the check carries no weight.",
    "COORDINATION: W1-07-r3 is live in the same tree and owns the questionnaire fix. Its status file records question texts 0/10 -> 10/10 drawn and named classes 0/14 -> 12/14 over a 240-run sweep, plus tools/harness/jrn09-exchange.mjs already written. Its next_step is 'expose the built census model via harness; fix the jrn09 probe; DRAW THE WRIT'. Do not duplicate the questionnaire or the matcher.",
    "TOOLS ALREADY LANDED by the tool builder while I was working, so I did NOT write them: tools/journey/journey-run.mjs (50 KB, A-JRN1, and it already prefers my __HARNESS.getRenderedText over getUIState().text and refuses to report M9/M15 unless the accessor is demonstrated non-empty), tools/journey/beat-extract.mjs, cadence.mjs, competence.mjs, gamepad-shim.mjs; tools/experience/session-run.mjs, beat-extract.mjs, beat-diff.mjs, isolation-check.mjs, lib/md.mjs. RI-EXP01's five phantom tools are five no longer.",
    "VERIFIED (no browser needed): beat-extract --self-test 8/8 PASS, beat-diff --self-test 9/9 PASS, isolation-check --self-test 6/6 PASS. All three falsify: the combat detector stays silent without an AGGRO hostile, the death detector ignores enemy deaths, the odd detector is silenced by a nearby quest_stage, one observation cannot satisfy two sheet beats, and a leak hidden inside a Bash command is caught.",
    "journey-run.mjs does NOT cover M20/HF9 (the title surface) \u2014 it has no title verb at all. tools/harness/w1-26-opening.mjs covers it, in the tools/harness piece-probe convention rather than in the tool builder's directory.",
    "RI-EXP01 ISOLATION: I cannot be the driving agent. Step 0 requires isolation `enforced` with an inbox of section C only, and I have read section D; isolation-check's own self-test confirms an inbox containing the item is VOID by construction. The honest first run is --drive none / a scripted opening walk, reported as an UNDRIVEN hour with negative artifacts per step 5, and NOT scored as if a player had played it.",
    "BLOCKED, still not mine: the live tree has not booted since 07:04. First game/src/input/touch.js (W1-29, fixed), then quests/mainline-act3+4 cross-refs (W1-19, fixed), now quests/hooks.json \u2014 'hooks.json: 23 quest(s) seed no topic from a journal write (RI-DLG05 A.3)' out of QuestEngine's fail-loud constructor. Every browser probe in the tree exits 12. A mirror at HEAD data + live src does not help because W1-19 changed both sides.",
    "MEASURED LIVE (07:32, tools/harness/w1-26-opening.mjs, run 1, 18 pass / 4 fail / 1 hard fail): TITLE \u2014 surface present, option_ids exactly O3's five, `Continue` focused with a save present, 8 distinct strings drawn, `Continue` LOADED THE SAVE and the surface yielded. M20 passes and HF9 is closed. ACCESSOR \u2014 getRenderedText() demonstrated non-empty (12 distinct strings on a frame known to carry text) while document.body.innerText is 0 chars, so M9/M15 are scoreable for the first time: M9 0 hits, M15 0 hits. CONSUMPTION K1 \u2014 the save store changes the drawn frame, null control stable, delete-the-fix restores. K2 \u2014 the register goes red when the surface stops drawing (8 -> 0 -> 8).",
    "MEASURED LIVE, ORPHAN TEXT FOUND AND FIXED: exactly one string in the whole opening was handed to fillText and painted outside its clip \u2014 the option-scroll indicator `1 of 13` at hold.hatch-name, clipped by the dialogue panel's height cap. Fixed in ui.js by moving the scroll position into the header row (which is always inside the panel) and removing its height from the content budget. This is the class of defect RI-JRN09 calls orphan text, one layer below round 2's, and no instrument in the corpus other than this register could see it.",
    "MEASURED LIVE: DTR_scene = 0.7222 through the DRAW CALL, worst node hold.hatch-name at 0.6667 (HF1 fires below 0.50, so HF1 does NOT fire). The layout accessor and the draw-call accessor agreed at every node. The gap from 1.00 is the OPTION WINDOW: OPTION_WINDOW=9 against 13 hatch-names and 19 skills, so the answers past the ninth are computed and not painted in the frame the probe sampled. RI-JRN09 M1's authored set explicitly includes 'the answer set'.",
    "MEASURED LIVE, O6: `first_control` never fired. The probe drove the census through the harness rather than walking, which is a probe artefact \u2014 but the second run walks with real forward input and the finding stands on its own: at hold.hatch-name the census surface has takes_input, so it eats the buttons and the body cannot move. Available play before the first character-defining question is 0 s against O6's >= 60 s. RI-JRN01 How-we-lose #5 verbatim: 'control arrives after definition'. MEASURED for the first time; it was corpus_debt before.",
    "O17 touch: run 1 fired HF5, and that was MY probe's fault \u2014 it dispatched invented PointerEvent coordinates. W1-29 exposes touchLayout()/touchDown/touchMove/touchUp/setTouchEnabled; run 2 reads the control geometry from the layer and reports `unmeasurable` rather than HF5 if the layer exposes none. Keyboard, mouse+keyboard and gamepad all drive the title.",
    "MEASURED LIVE (run 2, 20 pass / 5 fail / 0 hard fail): the orphan-text fix WORKS \u2014 'no orphan text: every string handed to a draw call landed inside its clip'. HF5 withdrawn: the touch leg now reads the layer's own geometry and reports `unmeasurable \u2014 touchLayout() returned no buttons` instead of firing a hard fail on my own bad coordinates. Keyboard, mouse+keyboard and gamepad all drive the title.",
    "ROOT CAUSE FOUND for DTR staying at 0.7222 even when the probe scrolled the answer list: THE CARET NEVER REACHED THE FRAME. buildCensusModel() snapshots `selected`, `picked` and `typed` off the surface, and `_censusSync()` \u2014 the only thing that rebuilds the model \u2014 was called on census STATE changes only. Moving the caret changes `censusSurface.sel`, which nothing re-read, so UILayer.setModel() was never called again and the surface did not repaint. A player pressing down watched a still panel, and at hold.hatch-name (13 names, a 9-option window) the four answers past the ninth were computed, offered, SELECTABLE and never painted at all. FIXED in Engine._afterStep(): re-sync outside the fixed step when and only when (sel|picked|typed) changed.",
    "SECOND BUG FIXED: `first_input` did not fire under real input because `input.consumeUI(CENSUS_ACTIONS)` clears the UI actions inside the step, so `_afterStep` saw an empty pipeline. The stamp is now taken in `_censusStep`, which `sim.censusDriver` calls every frame with the freshly latched input."
  ],
  "next_step": "run tools/harness/w1-26-opening.mjs and tools/journey/journey-run.mjs --journey jrn01-opening the moment the tree boots again; then the first-ever RI-EXP01 session-run/beat-extract/beat-diff chain",
  "notes_for_successor": "Everything W1-26 builds is on disk. What is missing is MEASUREMENT, and only because the tree has not booted since 07:04 (W1-19's quest data). Run, in order: (1) node tools/harness/w1-26-opening.mjs ; (2) node tools/journey/journey-run.mjs --journey jrn01-opening --seed 4711 --profile desktop-1080p --input-mode real --trace-events ; (3) node tools/experience/session-run.mjs --session exp-w1-firsthour --brief corpus/95-experience/briefs/first-hour.md --seed 1337 --minutes 60 --render-policy on-demand --profile first-hour --drive none, then beat-extract --in, then beat-diff --log. Do NOT drive the session yourself if you have read RI-EXP01 section D."
}