{
  "task_id": "tool-critic-r4",
  "brief": "inline: critique tools/analysis/build-viability.mjs round 5",
  "started": "wave-tool-r4",
  "state": "researching",
  "commit_at_start": "e29ec21",
  "outputs_expected": [
    "corpus/80-methods/TOOL-COVERAGE-R4.md",
    "corpus/80-methods/TOOL-COVERAGE-R4.json",
    "reports/blog-feed.jsonl (one line appended)"
  ],
  "outputs_written": [],
  "findings": [
    "COMMIT e29ec21. boot-check PASS before work. Box hostile: 24 headless_shell, loadavg 12.3 -> all analysis Node-side; browser deferred.",
    "--audit-grants exits 0, prints 15 rows, 'substitutions remaining: 0'.",
    "DEFECT A (attack 1, the clean kill): `substitutions_remaining` is `rows.filter(r => r.basis === 'SUBSTITUTION').length` over a HAND-WRITTEN literal array (build-viability.mjs:1432-1449). No row is ever labelled 'SUBSTITUTION'. The count is 0 by construction and cannot be anything else. --self-test asserts it to 0 \u2014 asserting a tautology. Worse, the ledger's own doc comment at :1422 defines `player-optimal-bound` as 'derived, then taken at its independent maximum. THE ONE SURVIVING SUBSTITUTION, stated as one.' So the file simultaneously says there are 4 surviving substitutions and reports 0.",
    "DEFECT B (attack 1+2, measured): REPUTATION is not a player-optimal bound, it is a JOINTLY UNREACHABLE vector. REPUTATION_GIFTS (:1379-1398) takes, per quest, the max delta PER FACTION INDEPENDENTLY across all resolutions, and drops every negative (`if (d <= 0) continue`). Measured on the shipped book (121 quests): 64 of 121 quests have their per-faction maxima coming from DIFFERENT, mutually exclusive resolutions; 200 negative faction_reputation deltas summing -3443 are discarded outright. Example Q-ASSZ-04 credits imperial_assize +42 (res_hand_hallow), xul_aneekh +25 (res_lodge_them) and drowned_court +20 (res_hide_vell) from ONE quest. A real one-resolution-per-quest playthrough optimising the_dockhands reaches dockhands 521 but house_dres -236 and rootkeepers -3; the tool's context holds dockhands 521 AND house_dres 169 AND rootkeepers 184 at the same time.",
    "VERIFIED INDEPENDENTLY (attack 5): 12 of 24 faction-gates world_state.flag rows have no producer anywhere in game/data \u2014 exactly the dockhands/drowned_court/ixtu_vakh/rootkeepers ranks 5/6/7 the builder names. My own recursive scan over all of game/data found 786 produced flags and the same 12 missing, with the 12 grounded controls also matching.",
    "VERIFIED INDEPENDENTLY (attack 5): the six ungrounded requirement tokens are real \u2014 items item_pitch_flask, item_ash_barrow, saltrice_bundle; knowledge topic_the_low_tide_hour, topic_ledger_assembly_procedure, topic_the_barge_schedule.",
    "BONUS CONTENT FINDING the tool did NOT surface: the_xul_aneekh's rank 5/6/7 world_state.flags are deepkin_hollow_kept / deepkin_coast_decided / deepkin_speaker_vacant \u2014 the SAME three flags as deep_kin's. Two ladders share one flag triple.",
    "CONTRADICTION with the builder's own status file: it records 'deep_kin attainable reputation is 5 against a rank-1 demand of 10, so that ladder cannot be entered at all'. --audit-grants on e29ec21 prints deep_kin 159 and the rank-1 demand is 10. That finding is stale/wrong against the shipped tree.",
    "Rank-5 reputation demand is 70 for ALL EIGHT ladders. Every one of the 56 triples of ladder factions is reputation-feasible under a single consistent playthrough, so the builder's 'no three rank-5-capable factions' finding cannot rest on reputation; it rests on the ungrounded rank-5 world flags + exclusivity.",
    "WALK LANDED (commit e29ec21, reports/viability.json, 540 records): `0/540 viable, 45 not viable, 495 UNMEASURABLE (target 486)`. Per criterion: main_quest 0 fail/0 unm; three_factions_rank5 **0 fail / 540 UNMEASURABLE**; no_unpassable_gate 45 fail / 495 unm; tier5 0/0. Exit non-zero (attestation STALE, 18/540 verdicts move with the offer model).",
    "DEFECT D (attack 3, and it is the round's headline): `viable` is 0 BY CONSTRUCTION. criterion three_factions_rank5 is UNMEASURABLE for 540 of 540 signatures because 12 rank-5/6/7 ladder world flags have no producer. `evaluateSignature` sets viable = all four criteria PASS, so NO signature on this tree can be viable no matter what the build does. The headline `0 of 540 viable (target 486)` is once again a number the instrument's own shape guarantees rather than a measurement of the build \u2014 the same class as round 4's `0 of 540` from reputation=100, arrived at from the restrictive side instead of the permissive one. Stop-gate tally from the artifact: {\"ungrounded requirement\":495, \"giver xul-aneekh-speaker requires disposition >= 45\":18, \"offer\":27}.",
    "DEFECT C (attack 7, a LIVE phantom read the sweep missed): PRODUCERS' npcs/** branch is `for (const g of Object.values(d)) if (g && Array.isArray(g.npcs)) ...` \u2014 a nested-group shape. All 14 shipped npcs/*.json carry `npcs` at the TOP level, which is what loadNPCs() reads (`doc.npcs || doc.records`). Measured inside the tool's own module state: the branch collects 0 topic mentions; the loader shape holds 719 across 336 records. The grant ledger prints topicsKnown ... 'from: ... npcs[].topics ...' \u2014 a cited source contributing nothing. The builder's round-5 sweep recorded 'npc record fields ... topics 336/336', a FIELD census, while its own code path reached 0 of those records: the builder's own lesson ('read the loader') was not applied to its own producer branch.",
    "DEFECT C impact, measured honestly: exactly one shipped gate names a topic produced only by an npc record \u2014 Q-LILM-01.opens_by.prerequisite_topics[0] = 'the rotting quarter'. Repairing the branch in a probe copy moves GRANTED_TOPICS 226 -> 227 and adds that topic. On THIS tree the verdict does not move (Q-LILM-01 already passes, because the dialogue layer separately produces the slug 'the-rotting-quarter' and canOffer's topicsInclude() folds it). So the read is dead, cited, and currently harmless \u2014 a false red waiting for a topic that only an NPC teaches.",
    "SWEEP RESULT (attack 7): I re-ran the shape check across every PRODUCERS branch against its loader. dialogue/topics, books/, items/, magic/, hooks are all shape-correct. npcs/** is the only dead branch, and it is dead for all 14 files / 336 records.",
    "ATTACK 2 (rows_compared) largely SATISFIED by shape: crossCheckVerdict() takes comparedRows.length, refuses on a non-boolean `agrees`, and returns agrees:false when nothing was compared. TWO TAUTOLOGIES remain: (a) `pairs_enumerated` is DEFINED as comparedRows.length + unresolvedRows.length (line 3309), so `accounting_holds: comparedRows.length + unresolvedRows.length === pairsEnumerated` (line 3332) is identically true and can never fail \u2014 the self-test asserts it; (b) the loop at 3412 `if (engineValue === undefined) continue;` drops pairs into NEITHER array, so the `why` string's claim that '${pairsEnumerated} (signature, giver) pairs were enumerated' under-reports the real enumeration."
  ],
  "next_step": "attack 4: break the model detector with a fire-but-wrong-value break on a shadow tree; then write TOOL-COVERAGE-R4.{md,json}",
  "notes_for_successor": "Probe copies live at tools/analysis/.r4probe/ (bv-probe.mjs, bv-orig.mjs) \u2014 DELETE before finishing, they are inside the repo. Scratch probes at scratchpad/r4/.",
  "method_deviations": []
}