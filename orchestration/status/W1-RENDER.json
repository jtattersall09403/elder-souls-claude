{
  "task_id": "W1-RENDER",
  "brief": "orchestration/NEXT-DISPATCH.md \u00a71 \u2014 character and weapon renderer",
  "started": "wave-1 round-1",
  "state": "building",
  "outputs_expected": [
    "game/src/render/ \u2014 skinned character + weapon mesh driven by CombatBody rig sockets",
    "tools/harness/ \u2014 capture probe proving three weapon classes differ",
    "corpus finding notes"
  ],
  "outputs_written": [
    "game/src/render/actor.js (new) \u2014 skinned actor + 15 weapon silhouettes, driven by Rig.world[]",
    "game/src/render/renderer.js \u2014 poseFromRig consumer; playerMesh.visible no longer gated on camera override",
    "game/src/render/scene.js \u2014 player is a rigged actor",
    "game/src/combat/moveset.js \u2014 weapon block carries hitbox_span_m / arc_sweep_deg",
    "game/src/render/ui.js + title.js \u2014 texture.dispose() on resize (ghost-UI fix)",
    "game/src/harness/api.js \u2014 getDrawnGeometry() debug channel",
    "tools/harness/wpn-render-probe.mjs (new)"
  ],
  "findings": [
    "load average at start: 7.43 (four cores) \u2014 capture expensive; plan for it",
    "Rig.evaluate() already computes world 3x4 transforms for ALL 20 bones in this.world[] every frame. The renderer needs to CONSUME this, not build a skinning pipeline.",
    "engine.js:509 `this.sim._combat = this.combat` \u2014 the renderer's render(sim) can reach sim._combat.player (CombatBody, has .rig) and sim._combat.bodies / bodyOf(eid).",
    "CombatBody.socketA/socketB ARE the hit capsule endpoints in world space. Drawing the blade from socketA to socketB makes the drawn tip identically the hit socket \u2014 not merely tracking it.",
    "socket dists: a = GRIP_OFFSET_M = 0.10, b = MovesetLibrary._bladeLength(weaponId) solved so tip horizontal radius == class reach_m.",
    "classes.json reach_m: SSW 1.95, HLB 2.85, CGS 2.75 \u2014 exactly the three the critic screenshotted as byte-identical. hitbox_span_m gives the EDGED span (blade vs haft): SSW 0.95, HLB 0.75, CGS 1.55.",
    "makeActor() (render/scene.js:387) welds a static Group: capsules at fixed offsets + a 0.95m BoxGeometry blade at (0.40,1.28,0.10). Nothing reads any pose.",
    "renderer.js render(): playerMesh.position + rotation.y only. syncEntities/syncNPCs likewise.",
    "SECOND DEFECT, previously unrecorded: renderer.js:401 read `playerMesh.visible = !c.override`. Any posed-camera capture (H.camera(...)) HID THE PLAYER. The round-3 critic's three byte-identical screenshots contained no character at all. Fixed \u2014 S18 says third person always.",
    "THREE.js trap that cost a cycle: setting matrixWorldAutoUpdate=false on the ROOT bone only is not enough. Object3D.updateMatrixWorld recurses into children with force=true and each child with the flag still true recomputes matrixWorld from its REST offset. Must set it on EVERY bone. Symptom is a swinging weapon on a body whose bones move 0.00% relative to root.",
    "MEASURED post-fix (browser, 60-frame light attack): drawn tip vs CombatBody.socketB = 0.0000 mm max over 60 frames on all three subjects. Guard vs socketA likewise 0.0000 mm. Identical by construction (weapon rides rig.world[gripIdx]; tip authored at local -Y*socket_b_dist_m).",
    "MEASURED: mean bone travel as % of character box diag (1.73 m): SSW 8.88%, HLB 13.94%, CGS 16.04%. Max bone 33.6/44.1/46.8%. Pre-fix figure was 0.19%.",
    "Solved blade lengths differ from declared reach_m: SSW draws 1.518 m (reach 1.95), HLB 2.908 (2.85), CGS 3.098 (2.75). That is MovesetLibrary._bladeLength's solve, not a render choice - the renderer draws socket_b_dist_m exactly.",
    "Drawn weapon meshes differ per class: tri counts 60 (SSW) / 72 (HLB) / 132 (CGS), distinct cache keys."
  ],
  "next_step": "screenshots at 1280x720 for three classes; then delete-the-fix control",
  "notes_for_successor": ""
}