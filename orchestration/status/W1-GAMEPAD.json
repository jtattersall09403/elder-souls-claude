{
  "task_id": "W1-GAMEPAD",
  "title": "The gamepad path: end-to-end play on a pad alone, stick response curve, shim lives or dies",
  "state": "done",
  "started_at": "2026-08-08T00:00:00Z",
  "commit_at_start": "52cffa1",
  "next_step": "none \u2014 handing to a critic",
  "findings": [
    "F1 (SETTLED, the brief's central question): the NEXT-DISPATCH note `gamepad-shim.mjs \u2014 does not run, 3/3 crash` is STALE BY TWO ROUNDS. TOOL-COVERAGE-R1 said 3/3 crash; R2 recorded 'gamepad-shim runs (8/8, was 3/3 crash)'; R3 line 370 says **Accept, 12/12**. I ran `node tools/journey/gamepad-shim.mjs --self-test` at 52cffa1: PASS 12/12, exit 0. The note sits under a NEXT-DISPATCH heading that says 'The R1 list below is kept because parts of it are still open' \u2014 this bullet is not one of the open parts.",
    "F2: there is no contradiction with the W1-21-r2 critic. The critic used `__HARNESS.gamepad()` (RealInput.pushGamepadState -> pollGamepad). The shim injects one layer higher, at navigator.getGamepads(). RI-JRN01 \u00a70.1(a) STRUCK the shim reference for reachability legs deliberately and kept it for the descriptor leg. Two instruments, two seams, both alive. Neither is stale.",
    "F3 (a real, already-identified, uncharged gap I intend to close): TOOL-COVERAGE-R3 line 370 notes the shim's entity-side check is `dist(p0,p1)` \u2014 a SCALAR. A router with inverted axes, or one that walks a fixed heading regardless of stick direction, passes both the green arm and the red control. That is rule 4 territory and it is in my remit.",
    "F4 (NEW, MEASURED, the round's headline): A SECOND DEADZONE. `input/gamepad.js shapeMoveStick()` removes the documented 0.15 inner deadzone and RESCALES the remainder onto [0,1] \u2014 its own comment says the rescale exists 'so there is no dead step at the deadzone edge'. `combat/player.js:994` then applies `locomotion.move_deadzone: 0.15` (declared at engine.js:707) A SECOND TIME to that already-rescaled magnitude. Measured at 52cffa1 on one body in one browser, matched arms: SHIPPED, the body does not move until stick deflection 0.27 (predicted 0.2655 by double application); with move_deadzone set to 0 live, the same body moves at 0.16 and three previously dead deflections come alive (0.16 -> 0.047 m/s, 0.20 -> 0.236, 0.25 -> 0.472). 15% of the live stick range is dead. Only an ANALOGUE device can reach the branch: real.js _pushMove() normalises the keyboard to magnitude 1, and touch.js pre-shapes exactly as the pad does \u2014 so the pad and the touchscreen are the only two devices affected, which is why a keyboard-driven project never saw it.",
    "F5: `_applyButtons` takes the hold_gate branch and `continue`s BEFORE consulting `actionFor(i)`, so a pad index that carries a hold_gate (1, 3, 10 in souls-default) CANNOT take a plain button binding at all. Found because my first CONSUMPTION perturbation remapped `menu` onto index 3 and correctly failed. Not a defect \u2014 gated indices are gated by design \u2014 but it is a real constraint on rebinding that nothing states, and a rebinding surface that offers index 3 for a plain action would silently do nothing.",
    "F6: the shim's entity-side DIRECTION gap is closed. `entitySideCheck()` now drives four bearings from one start and requires them 90 degrees apart in the right order, and a NEW teardown `--break-direction` swaps the two movement axes at the seam so distance survives and only bearing breaks (`--break-router` could not falsify it: it zeroes the axes, so the scalar check already goes red). Watched going red: the broken arm walks 6.4 m against the correct arm's 5.44 m \u2014 FURTHER \u2014 while every bearing is 90 degrees off. Self-test 12/12 -> 15/15."
  ],
  "files_touched": [
    "orchestration/status/W1-GAMEPAD.json",
    "tools/gamepad/pad-run.mjs",
    "tools/gamepad/deadzone-deletefix.mjs",
    "tools/journey/gamepad-shim.mjs",
    "game/src/engine.js",
    "orchestration/NEXT-DISPATCH.md",
    "reports/w1-gamepad-survey.md",
    "reports/w1-gamepad/",
    "docs/shots/2026-08-08-w1-gamepad-the-map-screen-opened-by-a-gamepad.png",
    "reports/blog-feed.jsonl"
  ],
  "files_claimed": [
    "orchestration/status/W1-GAMEPAD.json",
    "tools/gamepad/pad-run.mjs",
    "tools/gamepad/deadzone-deletefix.mjs",
    "tools/journey/gamepad-shim.mjs",
    "game/src/engine.js",
    "orchestration/NEXT-DISPATCH.md",
    "reports/w1-gamepad-survey.md",
    "reports/w1-gamepad/",
    "docs/shots/2026-08-08-w1-gamepad-the-map-screen-opened-by-a-gamepad.png",
    "reports/blog-feed.jsonl"
  ],
  "not_done": [
    "Did not walk from the writ-house out into the open province on the pad in one continuous run; the opening ends where the game's opening puts you and the walk/curve legs measure in open ground, declared in locomotion_venue.",
    "Did not drive the camera on the right stick - pointer lock does not survive headless. shapeRightStick (exponent 2.0) is UNMEASURED by me; only the LEFT stick curve is published.",
    "Did not measure the pad inside a fight. The screen-walk branch is guarded !inCombat and the W1-21 r2 critic could not enter combat either (aggro() threw). The in-combat pad arm is unmeasured by anyone.",
    "Did not test a physical GameSir X2s - nothing in this container can. The descriptor leg via gamepad-shim.mjs is the closest available.",
    "Did not verify the OTHER FOUR bullets in NEXT-DISPATCH.md section 2's R1 list (build-viability, journey-run, cadence, competence). One of five was two rounds stale; someone should check the rest.",
    "Did not measure combat/player.js:1134, move_deadzone's second reader in the levitation branch."
  ],
  "contention_note": "node tools/contention.mjs --gate returned GO (2 browser instances, load 2.20/core vs 4.0 ceiling) at start. One browser per instrument, killed by pid. Never pkill.",
  "ruling_name_entry": {
    "decision": "a name from a LIST on a pad; free typing stays on the keyboard and overrides the highlight",
    "rule": "RULES 0 - decided and recorded, not asked",
    "why": "already shipped in character/scene.js:_nameOptions(), whose own docstring cites RI-JRN01 O8: an on-screen keyboard would be the full-screen panel O8 forbids. step() commits `this.typed ? this.typed : opt.id` and tags it via:'ledger'.",
    "made_measured_by": "tools/gamepad/pad-run.mjs --leg opening dispatches NO key event, so a ledger that did not commit would stall the census at hold.hatch-name forever and O4 would go red. It committed: \"Silence-Under-Salt\".",
    "reversible_if": "play-testing shows players want a name the ledger does not carry and reach for a keyboard to get it - then an O8-compatible INLINE (never full-screen) character wheel is owed."
  },
  "commit_at_end": "561b8db",
  "results": {
    "pad_run_aggregate": "24 pass / 0 fail (node tools/gamepad/pad-run.mjs --leg all)",
    "deadzone_deletefix": "5 pass / 0 fail (all three RULES 6 failure shapes)",
    "gamepad_shim_selftest": "15/15 (was 12/12; direction check + --break-direction teardown added)",
    "teardowns_watched_going_red": [
      "pad-run --break-gate: hold_gate[1].frames 12->1, L3 goes RED (tap sprints instead of rolling) while L0/L1/L2 stay green",
      "gamepad-shim --break-direction: axes swapped, distance check stays GREEN at 6.4 m (MORE than the correct 5.44 m) and every bearing is 90 degrees out",
      "deadzone-deletefix arm B: move_deadzone put back to 0.15 on a live body, first-live returns to 0.27 and three deflections return to 0.000 m/s"
    ],
    "source_change": "game/src/engine.js:707 locomotion.move_deadzone 0.15 -> 0",
    "artifacts_are_gitignored": "reports/*.json is ignored by project policy (reports/.gitignore: run artifacts are reproducible from the tools). Reproduction commands are in reports/w1-gamepad-survey.md section 8."
  },
  "overlap_note": "orchestration/NEXT-DISPATCH.md is also claimed by arbiter-dlg-s37 and arbiter-wpn-s36. My edit is ONE bullet in section 2's R1 list (striking the stale gamepad-shim line) and touches nothing either arbiter is writing. Declared here rather than left to read as a collision.",
  "bank_note": "Most of this piece's files (tools/gamepad/, tools/journey/gamepad-shim.mjs, game/src/engine.js, the status file, the screenshot) were carried into HEAD by orchestrator bank commits while the piece was still running - RULES 17's documented behaviour. Content was verified present and correct in HEAD before the final commit; not unpicked."
}