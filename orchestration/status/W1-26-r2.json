{
  "task_id": "W1-26-r2",
  "brief": "corpus/90-verdicts/wave1/W1-26-r1.md (FAIL 1.3/10)",
  "started": "wave-1 round 2",
  "state": "successor-2 (post-restart): red-team verification pending; NEW scope from dispatch \u2014 S35 wayfinding, arrival hitch, \"asking is the verb\"",
  "outputs_expected": [
    "game/src/render/text-register.js \u2014 sees EVERY drawing surface incl. the vector-stroked glyph path; blind surfaces declared, never an empty set masquerading as clean",
    "game/src/ui/glyphs.js \u2014 drawText() reports to the register",
    "game/src/render/renderer.js \u2014 declare + instrument all three 2D surfaces",
    "game/src/harness/api.js \u2014 getRenderedText() derives surfaces from the contexts actually wrapped; reports blind[]",
    "game/src/render/ui.js \u2014 overflow policy stops sacrificing the scribe's replies (no panel-budget widening)",
    "the naming line drawn (NAMED_line coupling > 0)",
    "hold opens census-paused; scribe asks on interact; >= 60 s of body before the first question",
    "locomotion actually moves the body",
    "tools/harness/w1-26-opening.mjs \u2014 exits non-zero when M9 searches zero strings",
    "delete-the-fix confirmation for each fix",
    "docs/shots/ image + reports/blog-feed.jsonl line"
  ],
  "outputs_written": [
    "game/src/render/text-register.js \u2014 declare()/coverage() roster; instrument() installs ctx.__esNoteText; noteVector(); _record() shared by both draw paths",
    "game/src/ui/glyphs.js \u2014 drawText() reports every stroked run to the register",
    "game/src/render/renderer.js \u2014 all THREE 2D surfaces declared and instrumented (menus was the missing one)",
    "game/src/harness/api.js \u2014 getRenderedText() derives surfaces from the register and publishes complete/measurable/blind_surfaces; registerSurfaces(); drawSentinels(); drawOnMenus(); censusEnter(by)",
    "game/src/engine.js \u2014 _interactPrompt() names the object, never commands; censusBegin no longer emits a false dialogue_open at frame 0; censusEnter(by) resumes into the node's own place; talk-to-resume in _censusStep; getUIState().dialogue_surface.sacrificed",
    "game/src/render/ui.js \u2014 overflow policy: type scale -> option window -> writ block -> her reply LAST; two-column lists; metrics().sacrificed",
    "game/src/character/census.js \u2014 resume/resume_by (two hand-backs); _nameYou(); %ClassVerdict; writ.class-verdict answer case",
    "game/data/dialogue/topics/writ-house.json \u2014 hold.come-to (start, paused, resume_by talk); hold.out resume/resume_by; writ.class-verdict with route_lines; all three class routes land on it",
    "tools/harness/w1-26-r2-diag.mjs \u2014 the before-measurement",
    "tools/harness/w1-26-r2-scene.mjs \u2014 the round's own instrument (accessor coverage, DTR x3 routes x2 policies, naming coupling, O6, delete-the-fix); exits non-zero",
    "tools/harness/w1-26-opening.mjs \u2014 fail-closed on an incomplete/empty M9 domain; surfaces_searched read off the register instead of asserted",
    "tools/harness/jrn09-exchange.mjs + tools/harness/chr-custom-route.mjs \u2014 one-shot `entered` flag replaced by st.paused/resume_by (there are two hand-backs now)",
    "tools/harness/w1-26-r2-shot.mjs \u2014 blog frames",
    "game/src/sim/events.js \u2014 'npc_schedule'/'npc_presence' added to the closed vocabulary (W1-04's emits landed without their amendment; the guard threw inside the fixed step)",
    "game/src/sim/settlement.js - the way out is a place (reach-gated on continuity.interior_spawn), and sim.doorVeto lets a running scene hold its door with a spoken reason",
    "game/src/engine.js - sim.doorVeto installed beside sim.censusDriver; the opening cannot be walked out of half-finished"
  ],
  "findings": [
    "boot-check PASS at start. loadavg 16.9 (above the ~12 distortion threshold named in AGENT-PROTOCOL).",
    "Choke point confirmed: ALL menus/HUD text goes through ui/glyphs.js drawText() (re-exported by ui/type.js). It strokes vector paths; the register wraps fillText/strokeText only. Hence menus surface = 0 entries even when instrumented.",
    "render/ui.js (dialogue) uses ctx.fillText directly \u2014 that is why 'dialogue' is the only surface with entries.",
    "render/ui.js:199 \u2014 `while (contentH > maxH && spokenLines.length) spokenLines.shift()` is the line that eats the scribe's replies. PANEL_MAX_FRAC=0.48, measured area cost 0.498 of 0.55 \u2014 no budget headroom.",
    "MEASURED (before any change): the round-1 '60 frames of forward input move the body 0.0000 m' is an INSTRUMENT artefact as well as a real defect. InputPipeline.queueInputs() does `this.script = script.slice()` \u2014 it REPLACES the timeline and re-bases it on the calling frame. Both the round-1 builder's probe and the round-1 critic's probe called it 60 times in a loop, so exactly ONE event survived. Measured side by side: round-1 call shape 0.0533 m; one call with the full 60-event array, census closed, in the same hold: 3.2000 m. With the census surface open at hold.hatch-name: 0.0000 m. So the body CAN move and consumeUI() zeroing moveX/moveY is the real cause \u2014 the named fix is the right one.",
    "MEASURED DTR (own denominator getCensusModel(), on-arrival draw policy, whitespace-stripped match, watermark taken BEFORE the repainting call): worst nodes hold.hatch-name 0.6667 and writ.stamp 0.6667; writ.given-name 0.9000; writ.birthsign 0.9091. Undrawn everywhere = `spoken` rows (hold.wake's opening line, the upbringing reply, the on_match_named_class NAMING LINE, the birthsign scribe_line) plus 4 of 14 hatch-name options outside the 9-option window.",
    "The naming line is `on_match_named_class`/`on_nearest_named_class` on node writ.class-questions; census.js pushes it into `spoken`, and it is displayed at writ.birthsign where 9 birthsign options + the question exceed the 0.48-height cap and ui.js:199 shifts it off. It is never a node's own `line`, which is why it is always the first thing sacrificed.",
    "HF3/AR-2 hit source: engine.js _interactPrompt() built 'Take ' + o.name / 'Speak to ' + n.name. Fixed to name the object only; the verb is carried as a field.",
    "BLOCKED at 13:1x: boot-check FAILS on the shared tree \u2014 game/data/quests/hooks.json lost 10 Q-MAIN entries relative to HEAD~1 (Q-MAIN-17..23, 30, 31, 32) and the pre-existing RI-DLG05 A.3 guard in sim/quest/machine.js throws at Engine._boot. NOT my change; another agent's live working file. Boot-check PASSED at my start.",
    "AFTER: register roster declared[dialogue,title,menus] instrumented[all three] blind[]. Vector sentinel through glyphs.drawText() SEEN; fillText sentinel SEEN. Set equality against UISurface.elements[].text holds. M9 domain = 5 distinct strings incl. 'A tithe-gourd, empty' and 'A knife somebody did not find'; 0 substring hits, 0 imperative-led hits.",
    "AFTER: DTR arrival 1.0000 on the questionnaire and custom routes; 0.9595 on the named route (writ.class-named, 14 classes, window 11 at the type floor \u2014 the three off-window options ARE reachable and drawn once the caret pages). DTR_q 1.0000. 0 of the scribe's lines sacrificed on any route. Max panel area 0.3822 of M5's 0.55.",
    "AFTER: jrn09-exchange.mjs (the item's own tool) DTR_q 1.0000, DTR_scene 1.0000, AC 6 of 7.",
    "AFTER O6: hold opens at hold.come-to, paused, no question on the frame; 60 frames of forward input move 3.2000 m; 61 s of wandering leaves the scene paused; walking over and pressing interact opens it; available_play_s_before_first_field = 63.5 s against O6's >= 60 s.",
    "AFTER naming: writ.class-verdict is its own beat on all three routes. Coupling 1 \u2014 two answer patterns give Wet-Foot vs Hollow-Hunter, both lines drawn, null control identical.",
    "Delete-the-fix: 4 of 4 reproduce. Removing ctx.__esNoteText re-blinds the vector path (fillText unaffected); declaring an uninstrumented surface flips complete to false and names it; moving the naming line back into `spoken` under page pressure loses it while the node's own line survives; re-drawing 'Take A tithe-gourd, empty' through the real vector path trips M9's imperative clause.",
    "THIRD instrument trap, new: UILayer repaints ON DEMAND, so a probe that queues 50 frames of caret movement and calls getUIState() ONCE paints only the last window and reads back as 'scrolling revealed nothing'. Step, repaint, step.",
    "SUCCESSOR (r2 continuation, 2026-08-07): predecessor's code+tools survive on disk and are COMMITTED (b7d249a swept them in). Tree at start: loadavg 4.68 (quiet, well under the ~12 distortion threshold). Uncommitted in tree = other agents' work (engine.js bookKnowledge guard removal by W1-LIBRARY, 50-factions.json). Not mine, left alone.",
    "SUCCESSOR, TREE-WIDE BLOCKER FOUND AND FIXED: at HEAD, sim/npc.js stepSchedule() emits 'npc_schedule' and 'npc_presence'; NEITHER was in the EVENT_TYPES closed vocabulary in sim/events.js, so EventBus.emit() threw INSIDE the fixed step the moment a scheduled person's presence flipped. Any probe that STEPS frames in a cell with a scheduled NPC dies; boot does not step, so boot-check stayed green and the breakage was invisible. It killed my O6 section on the first re-run. Landed in 23fd1dc (an orchestrator bank commit); nothing else in the tree references either name. Fixed additively by the HARNESS.md \u00a75 amendment clause the file's own comment blocks cite.",
    "SUCCESSOR, SECOND VOCABULARY HOLE: an audit of EVERY guarded bus.emit() literal in game/src against EVENT_TYPES found six more unamended names beyond npc_schedule/npc_presence: settlement_enter/settlement_exit (settlement.js:163, emitted through a ternary so a literal grep does not see them), door_refused/interior_enter/interior_exit (settlement.js:195/204/222 - every doorway in the game), and census_refused (engine.js:2200 - W1-26's OWN, emitted from the CATCH arm of _censusApplyPending(), so a rejected answer in the opening threw a second error out of the handler written to absorb the first). All added.",
    "SUCCESSOR, THE OPENING WAS UNFINISHABLE AND THE INSTRUMENT SAID 0.272 m: measured, at HEAD. sim/settlement.js stepSettlement() built sim.door = {way:'out', dist_m:0} UNCONDITIONALLY whenever the player was inside any interior - the way-out door had no proximity test at all, while the way-in door uses DOOR_REACH_M=2.6. In the opening: the player presses interact to talk to Jeeh-Ei from 8.4 m, the census reach gate correctly declines, the press FALLS THROUGH to the door latch, and the body is placed on the Tidewrack dock at (3798,909) - 3.9 km from the hold - with the census still paused at hold.come-to. Every later press is then at dist 3900 m and the scene can never be resumed. The round-2 scene probe reported dist_to_her 0.272 m throughout, because the census measures its speaker in the hold's own frame: a STILL probe never saw it and a MOVING one did.",
    "FIX 1 (settlement.js): the out-door is reach-gated on continuity.interior_spawn - the inside face of the same door RI-WLD13 already does arithmetic with - at the same DOOR_REACH_M. All 115 interiors in game/data/world/interiors/** declare interior_spawn (checked before landing it), and the !inFace arm is fail-OPEN so a future interior without one is not a room you cannot leave.",
    "FIX 2 (engine.js + settlement.js): sim.doorVeto, an Engine-installed hook of the same family as sim.censusDriver/populate/placeBody. While a creation is running, the cell the CURRENT NODE is set in holds its door ('The hatch is barred until you are written down.'), emitted as door_refused. The place comes from the graph (node.place), so settlement.js still knows nothing about censuses. Without it the opening was still escapable at the companionway, where the out-door legitimately IS in reach.",
    "SUCCESSOR, CONSUMPTION (RI-MTH07 \u00a7B) DONE END TO END, not through setCharacter(): the opening is PLAYED twice, differing in exactly one answer the player gives with their own hands (writ.upbringing interior vs foreign-born), and then an entity is asked what it does. Consumer named: sim/dialogue/disposition.js derivedDisposition() -> Engine.talkTo() -> character/converse.js greeting cell, and sim/quest/topic-supply.js topicsFor() for what she will discuss. MEASURED: clerk-avelia-doren disposition 26 band 'cold' cell 130 ('That is provincial business. Take it to the provincial office. Provincial.') vs disposition 40 band 'neutral' cell 135 ('Provincial. Something to report?'). coupling 1.",
    "SUCCESSOR, THE NULL CONTROL CAUGHT MY OWN PROBE: two runs differing ONLY in the hatch-name came back with different greeting LINES. Cause is not the model - Engine._greetCount (the visit counter that stops a person repeating their opening sentence) is NOT cleared by loadState, so the second play in the same page draws the second line of the SAME cell. Cell 130, key [RG-EMPIRE,cold,saxhleel], band, disposition and all four topics were identical in both. Probe now clears _greetCount before each talkTo. NOTE FOR OTHERS: any probe that plays a scene twice in one page and compares what somebody said is measuring the rotation unless it clears that Map.",
    "SUCCESSOR, RED TEAM: tools/harness/w1-26-opening.mjs now takes --red-team=empty|blind|plant, which sabotages the instrument in the page (empty M9 domain / a declared-but-uninstrumented surface / an imperative HUD string through the real vector path) and INVERTS the exit code, so 'the probe goes red on purpose' is a command a critic can run rather than a claim I made.",
    "SUCCESSOR-2 (2026-08-07, after the second container restart): predecessor's code, tools, shots (docs/shots/2026-08-07-w1-26-*.png x3) and blog line (blog-feed.jsonl:12) are all ON DISK and COMMITTED. Contention at start: pgrep -c headless_shell = 12 (above the 8 cap), loadavg 4.44. So authoring phase first, browser phase after."
  ],
  "next_step": "AUTHORING FIRST (browsers at 12): read ARBITRATION S35; audit whether the opening teaches \"asking is the verb\" (findability landed: 10/35 quests open by asking); audit whether the opening arrives anywhere (province arrival hitch 337-484ms / 2.2s dropped sim). THEN browser: three red-team modes, clean opening.mjs, boot-check + 3 data checks, fresh shot.",
  "notes_for_successor": "Do NOT re-derive the ui.js wrap() space-dropping bug (strip all whitespace both sides). Do NOT take the register watermark after getCensusState()/censusAnswer() \u2014 they repaint and clear dirty. Do NOT queue many caret moves and repaint once \u2014 UILayer repaints on demand and you will measure one window. queueInputs() REPLACES the script: pass the whole array in ONE call or you deliver one frame of input. The scene now has TWO hand-back nodes (hold.come-to resume_by 'talk', hold.out resume_by 'walk'); any walker using a one-shot `entered` flag will stop dead at the second and report zeros. RI-EXP01 is NOT closed: 8 of 15 derived targets are not computable from a trace and the missing input is a dispatchable agent at isolation enforced. I did not read RI-EXP01 so as not to disqualify myself from being that agent."
}